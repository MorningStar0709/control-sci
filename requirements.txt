# ControlMind — Core Dependencies
# Generated: 2026-05-07 | P6 端到端验证
# Install: pip install -r requirements.txt

# ── LLM API & HTTP ──
openai>=1.0.0
anthropic>=0.30.0
httpx>=0.27.0

# ── Dataset & Hub ──
datasets>=2.14.0
huggingface_hub>=0.20.0
pyarrow>=12.0.0

# ── Scientific Computing & Analysis ──
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
scikit-posthocs>=0.8.0
faiss-cpu>=1.8.0
rank-bm25>=0.2.2

# ── Visualization ──
matplotlib>=3.7.0
seaborn>=0.13.0

# ── Schema & Config ──
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn>=0.27.0

# ── Text & File Processing ──
requests>=2.31.0
pypdf>=3.17.0
rich>=13.0.0
typer>=0.12.0

# ── Finetuning (implicit deps of transformers / accelerate + peft + trl + bitsandbytes for QLoRA) ──
torch>=2.0.0
transformers>=4.36.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.0
bitsandbytes>=0.41.0
safetensors>=0.4.0

# ── HuggingFace hub implicit deps ──
fsspec>=2023.6.0
filelock>=3.12.0

# ── Image Processing ──
pillow>=10.0.0
