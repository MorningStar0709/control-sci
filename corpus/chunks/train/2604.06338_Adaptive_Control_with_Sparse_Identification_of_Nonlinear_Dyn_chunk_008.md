To facilitate the subsequent stability analysis, we show that $\hat { \theta } ( t ) \in \Theta \oplus \bar { \mathcal { B } } ( 0 , \epsilon ) = : \Theta _ { \epsilon }$ , where $\bar { B } ( 0 , \epsilon )$ is the closed Euclidean ball of radius $\epsilon > 0$ centered at the origin, and ⊕ denotes the Minkowski sum, even with the signum term in (11).

Lemma 2: If $\hat { \theta } ( t _ { 0 } ) \in \Theta _ { \epsilon }$ , then Θϵ is positively invariant under the differential inclusion (11). Consequently, ${ \hat { \theta } } ( t ) \in$ Θϵ for all $t \geq 0 .$

Proof: Let $\partial \Theta _ { \epsilon }$ denote the boundary of $\Theta _ { \epsilon } ,$ , and let $T _ { \Theta \epsilon } ( \hat { \theta } )$ denote the tangent cone of $\Theta _ { \epsilon }$ at ˆθ. Because $\Theta _ { \epsilon }$ is a Euclidean ball centered at the origin, the outward-pointing unit normal vector at any boundary point $\hat { \theta } \in \partial \Theta _ { \epsilon }$ ϵ is given by $\begin{array} { r } { n (  { \hat { \theta } } ) = \frac {  { \hat { \theta } } } { \|  { \hat { \theta } } \| } } \end{array}$

From (11), and the standard properties of the projection operator onto a closed convex set (see [4, Lemma E.1]), for any $\hat { \theta } \in \partial \Theta _ { \epsilon } ,$ we have

$$n (\hat {\theta}) ^ {\top} \operatorname{proj} _ {\Theta} (\hat {\theta}, \phi (x, e, \hat {\theta}), \Gamma) \leq 0. \tag {12}$$

Next, we evaluate the inner product of the normal vector with the set-valued term $v _ { \theta } : = - \lambda \gamma \Gamma \sigma$ in (11), where $\sigma \in$ SGN(ˆθ). For $\hat { \theta } \in \partial \Theta _ { \epsilon }$ and all $\sigma \in \mathrm { S G N } ( \hat { \theta } )$ , we have

$$
\begin{array}{l} n (\hat {\theta}) ^ {\top} v _ {\theta} \leq - \lambda \gamma n (\hat {\theta}) ^ {\top} \Gamma \sigma = - \lambda \gamma \left(\frac {\hat {\theta}}{\| \hat {\theta} \|}\right) ^ {\top} \Gamma \sigma \\ \leq - \lambda \gamma \lambda_ {\min} (\Gamma) \| \hat {\theta} \| _ {1} \leq 0. \tag {13} \\ \end{array}
$$

Then, it follows from (12) and (13) that for all $\rho \in G ( t , { \widehat { \theta } } )$ ,

$$n (\hat {\theta}) ^ {\top} \rho \leq 0, \tag {14}$$

which implies that, for all $\rho \in G ( t , { \hat { \theta } } ) , \rho \in T _ { \Theta \epsilon } ( { \hat { \theta } } )$ . Invoking [34, Theorem. 7], Θ is positively invariant. Since $\Theta _ { \epsilon }$ is compact, it follows that ${ \hat { \theta } } ( t )$ remains bounded for all $t \geq t _ { 0 }$ .

![](image/6387218d140877211f22de04ae9e8b63789f04be84a6974266885fc0b95bacb7.jpg)
