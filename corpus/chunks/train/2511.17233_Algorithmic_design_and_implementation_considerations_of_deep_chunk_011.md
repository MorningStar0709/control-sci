The Algorithm 3 provides $K _ { t }$ for $t \in \left\{ t _ { j } + 1 , \dots , t _ { j + 1 } \right\}$ and is used to generate the learning component $u _ { t } ^ { a } = - K _ { t } ^ { \top } \phi _ { j } ( x _ { t } )$ for $t \in \left\{ t _ { j } , \ldots , t _ { j + 1 } - 1 \right\}$ . The pair $( x _ { t } , u _ { t } ^ { a } )$ are stored in a replay buffer for the supervised fine tuning of the neural network on the basis of some experience selection criterion [10], [23]. The Algorithm 3 has also been used in [24].

Once all these modules are ready, we can create the main module of deep MPC. In order to design the online reference tracking MPC, we first choose symmetric positive definite matrices $Q , R \succ \mathbf { 0 } .$ , which can be different from those chosen for the reference governor. Let $c _ { \mathrm { s } } ( x _ { t + i \mid t } , u _ { t + i \mid t } ) : =$ $\left\| x _ { t + i \mid t } - x _ { t + i } ^ { r } \right\| _ { O } ^ { 2 } + \left\| u _ { t + i \mid t } - u _ { t + i } ^ { r } \right\| _ { R } ^ { 2 }$ be the cost per stage at time ?? + ?? predicted at time ?? and let $\bar { c _ { \mathrm { f } } ( x ) } : = x ^ { \top } Q _ { f } x$ be the terminal cost with $Q _ { f } \succ 0 .$ .

Let us define

$$V _ {m} \left(x _ {t \mid t}, \left(u _ {t + i \mid t}\right) _ {i = 0} ^ {N - 1}\right) := c _ {f} \left(x _ {t + N \mid t}\right) + \sum_ {i = 0} ^ {N - 1} c _ {s} \left(x _ {t + i \mid t}, u _ {t + i \mid t}\right). \tag {5}$$

The online reference tracking MPC minimizes (5) at each time instant ?? under the following constraints:

$$x _ {t \mid t} = x _ {t} \tag {6}u _ {t \mid t} + u _ {t} ^ {a} \in \mathcal {U} \tag {7}x _ {t + i + 1 \mid t} = f (x _ {t + i \mid t}) + g (x _ {t + i \mid t}) u _ {t + i \mid t} \text { for } i = 0, \dots , N - 1 \tag {8}u _ {t + i \mid t} \in \bar {\mathcal {U}} \text { for } i = 1, \dots , N - 1. \tag {9}$$

Notice that the constraint (7) is different from the constraints present in tube-MPC formulation [3]. We define the underlying optimal control problem

$$V _ {m} (x _ {t}) := \min _ {(u _ {t + i | t}) _ {i = 0} ^ {N - 1}} \{(5) \mid (6), (7), (8), (9) \}. \tag {10}$$

The overall deep MPC algorithm is given in Algorithm 4.
