We assess this via a testing dataset $\{ ( X ^ { \prime } , Y ^ { \prime } ) \} = \{ ( x _ { i } ^ { \prime } , y _ { i } ^ { \prime } ) \in \mathbb { R } ^ { d + 1 } \} _ { i = 1 } ^ { N ^ { \prime } }$ , where $N ^ { \prime } \in \mathbb { N } _ { + }$ , distinct from the training data, drawn independently from the same underlying distribution.

The network’s generalization quality is quantified by its performance on $( X ^ { \prime } , Y ^ { \prime } )$ , measured through a comparison between the true outputs $\{ y _ { i } ^ { \prime } \} _ { i = 1 } ^ { N ^ { \prime } }$ , and the corresponding predictions $\{ f _ { \mathrm { s h a l l o w } } ( x _ { i } ^ { \prime } , \Theta ) \} _ { i = 1 } ^ { N ^ { \prime } }$ .

There are several ways of proceeding to this comparison. Rather than focusing on pointwise errors, we analyze discrepancies at the distributional level to gain more robust insights. This approach provides several advantages: a more comprehensive evaluation of model behavior, reduced sensitivity to individual outliers, and better alignment with statistical learning theory.

Let us denote by

$$m _ {X} = \frac {1}{N} \sum_ {i \in [ N ]} \delta_ {x _ {i}}, \quad m _ {Y} = \frac {1}{N} \sum_ {i \in [ N ]} \delta_ {y _ {i}}, \quad m _ {\text { train }} = \frac {1}{N} \sum_ {i \in [ N ]} \delta_ {(x _ {i}, y _ {i})}, \tag {2.8}$$

the empirical distribution of the training dataset, and by

$$m _ {X} ^ {\prime} = \frac {1}{N ^ {\prime}} \sum_ {i = 1} ^ {N ^ {\prime}} \delta_ {x _ {i} ^ {\prime}}, \quad m _ {Y} ^ {\prime} = \frac {1}{N ^ {\prime}} \sum_ {i = 1} ^ {N ^ {\prime}} \delta_ {y _ {i} ^ {\prime}}, \quad m _ {\text { test }} = \frac {1}{N ^ {\prime}} \sum_ {i = 1} ^ {N ^ {\prime}} \delta_ {(x _ {i} ^ {\prime}, y _ {i} ^ {\prime})}, \tag {2.9}$$

the one of the testing datasets. Let us also consider the measure of predictions:

$$m _ {\text { pred }} (\Theta) = \frac {1}{N ^ {\prime}} \sum_ {i \in [ N ^ {\prime} ]} \delta_ {(x _ {i} ^ {\prime}, f _ {\text { shallow }} (x _ {i} ^ {\prime}, \Theta))}. \tag {2.10}$$

In this measure theoretical setting, distances between the different datasets will be calibrated in terms of the Kantorovich–Rubinstein (KR) distance $d _ { \mathrm { K R } } ( \cdot , \cdot )$ , that respects the underlying geometry of the data, accounting for how far points must be transported to transform one dataset into another. It is equivalent to the 1 -Wasserstein distance, defined as follows:

$$\mathcal {W} _ {1} (\mu , \nu) := \min _ {\pi \in \Pi (\mu , \nu)} \int_ {\mathbb {R} ^ {d} \times \mathbb {R} ^ {d}} | x - y | d \pi (x, y),$$
