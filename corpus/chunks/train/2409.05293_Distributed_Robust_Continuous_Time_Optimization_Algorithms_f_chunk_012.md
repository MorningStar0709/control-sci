Note that the domain of the penalized objective function $\tilde { L } _ { i } ( x _ { i } , t ) ~ \mathrm { i s } ~ D _ { i } ( t ) ~ = ~ \{ x _ { i } ~ \in ~ \mathbb { R } ^ { n } | g _ { i } ( x _ { i } , t ) ~ \prec ~ \sigma _ { i } ( t ) \mathbf { 1 } _ { q _ { i } } \}$ . To ensure that the distributed controller $( 7 \mathrm { a } ) \mathrm { - } ( 7 \mathrm { d } )$ functions correctly, the initial states $x _ { i } ( 0 )$ must satisfy the following condition:

$$g _ {i j} \left(x _ {i} (0), 0\right) < \sigma_ {i} (0) \tag {9}$$

This requirement guarantees that the initial states of each agent are within the feasible region defined by the slack function $\sigma _ { i } ( 0 )$ , facilitating the successful application of the controller.

Remark 4: A comparable problem was addressed in [14] where the nonlinear inequality constraints were not taken into account. Consequently, the control strategies proposed in [14] can not be directly implemented. The controller algorithm proposed in (7) consists of three components: the first term, $- \beta \left( \nabla ^ { 2 } \tilde { L } _ { i } ( x _ { i } , t ) \right) ^ { - 1 } \sum _ { j \in \mathcal { N } _ { i } } \mathrm { s i g n } ( x _ { i } - x _ { j } )$ , aims to tackle the consensus problemsensus on states $\begin{array} { r } { ( \operatorname* { l i m } _ { t  \infty } ] | x _ { i } ( t ) - \frac { 1 } { N } \sum _ { j = 1 } ^ { N } x _ { j } ( t ) | | _ { 2 } = 0 ) } \end{array}$ . The second term $( \dot { \psi } _ { i } ( t ) )$ serves as an optimizer to minimize the penalized objective function $\tilde { L } _ { i } ( x _ { i } , t )$ provided in (5). This step includes the use of log-barrier penalty functions to integrate inequality constraints into the penalized objective framework. The final term, described in (7c), plays a crucial role in suppressing disturbance, thus ensuring the system preserves the nominal optimization algorithm’s performance during the sliding phase, even with disturbances. Notably, this paper’s controller algorithm (7b) eliminates the restriction that the Hessians of all the local objective functions must be identical.

Assumption 5 (Bounds on Objective Functions): If all local states $x _ { i }$ are bounded, then for all $i \in \mathcal V$ and $t \geq 0$ , there exists α¯ such that $\begin{array} { r } { \operatorname* { s u p } _ { t \in [ 0 , \infty ) } \left\| \frac { \partial } { \partial t } \nabla f _ { i } ( x _ { i } , t ) \right\| _ { 2 } \leq \bar { \alpha } } \end{array}$ .
