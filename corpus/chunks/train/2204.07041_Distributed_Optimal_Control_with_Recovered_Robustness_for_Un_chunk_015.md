$$\dot {e} _ {i} = (A - G C _ {2}) e _ {i} + (G D _ {0} - B _ {0}) \sum_ {j = 1} ^ {N} a _ {i j} (w _ {0 i} - w _ {0 j}). \tag {6}$$

Therefore, if G is chosen such that $A - G C _ { 2 }$ is Hurwitz, $v _ { i }$ in (4) is actually an estimate of $\begin{array} { r } { \sum _ { i = 1 } ^ { N } a _ { i j } \big ( x _ { i } - x _ { j } \big ) } \end{array}$ P for agent i. That is, $D O ( s )$ in Fig. 1 is in fact represented by the distributed observer in (4a).

Denote $\boldsymbol { v } = [ v _ { 1 } ^ { T } , \cdots , v _ { N } ^ { T } ] ^ { T }$ and $\boldsymbol { \xi } = \left[ \boldsymbol { x } ^ { T } \boldsymbol { v } ^ { T } \right] ^ { T }$ . By substituting (4) into (2), the closed-loop network dynamics can then be written in compact form as

$$\dot {\xi} = \mathscr {A} \xi + \mathscr {B} _ {0} w _ {0}, \tag {7}z = \mathcal {C} _ {1} \xi ,$$

where

$$
\mathscr {A} = \left[ \begin{array}{c c} I \otimes A & I \otimes B _ {2} F \\ \mathcal {L} \otimes G C _ {2} & I \otimes (A - G C _ {2}) + \mathcal {L} \otimes B _ {2} F \end{array} \right], \tag {8}

\mathcal {B} _ {0} = \left[ \begin{array}{c} I \otimes B _ {0} \\ \mathcal {L} \otimes G D _ {0} \end{array} \right], \mathcal {C} _ {1} = \mathcal {M} \otimes \left[ \begin{array}{c c} C _ {1} & 0 \end{array} \right], \mathcal {M} \triangleq I - \frac {1}{N} \mathbf {1 1} ^ {T}.
$$

The following theorem provides a necessary and sufficient condition for the $H _ { 2 }$ suboptimal consensus problem.

Theorem 1. Assume that the graph $\mathcal { G }$ is connected. Let $\gamma _ { 2 } > 0$ . Then, the distributed protocol (4) achieves $H _ { 2 }$ consensus for the network (7) if and only if the following $N - 1$ subsystems

$$
\dot {\tilde {\xi}} _ {i} = \left[ \begin{array}{c c} A & \lambda_ {i} B _ {2} F \\ G C _ {2} & A - G C _ {2} + \lambda_ {i} B _ {2} F \end{array} \right] \tilde {\xi} _ {i} + \left[ \begin{array}{c} B _ {0} \\ G D _ {0} \end{array} \right] \tilde {w} _ {0 i}, \tag {9}

\tilde {z} _ {i} = \left[ \begin{array}{c c} C _ {1} & 0 \end{array} \right] \tilde {\xi} _ {i}, \quad i = 2, \dots , N,
$$

are internally stable and $\begin{array} { r } { \sum _ { j = 2 } ^ { N } \| \tilde { T } _ { \tilde { w } _ { 0 i } \tilde { z } _ { i } } \| _ { 2 } ^ { 2 } \ < \ \gamma _ { 2 } ^ { 2 } , } \end{array}$ , where $\tilde { T } _ { \tilde { w } _ { 0 i } \tilde { z } _ { i } }$ denotes the transfer function matrix of (9) from $\tilde { w } _ { 0 i }$ to $\tilde { z } _ { i }$ .
