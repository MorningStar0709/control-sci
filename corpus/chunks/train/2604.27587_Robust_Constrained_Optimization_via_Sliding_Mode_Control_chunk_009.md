# Problem statement:

(1) Design a feedback control law λ for the system such that the state x(t) converges to the optimal solution $x ^ { * }$ asymptotically, and the constraint violation $y ( t )$ vanishes in finite time. Specifically, ensure that li ${ \begin{array} { r } { 1 } \\ { 1 _ { t  \infty } x ( t ) = x ^ { * } , \operatorname* { l i m } _ { t  T } y ( t ) = 0 } \end{array} }$ , for some $T \in [ 0 , \infty ]$ .   
(2) Design an NTSMC control law λ to guarantee finitetime reaching of the sliding manifold, while the gradient term in (3) is replaced by a normed gradient flow to ensure finite-time convergence to optimum point under a µ-strongly convex objective φ.

To address this, we develop an SMC strategy in the following sections. 3
