# end

Algorithm 1: Algorithm of reinforcement learning with closed-loop stability guarantee based on adaptive control.

$\Theta \subset \mathbb { R } ^ { p } , \mathbb { U } \subset \mathbb { R } ^ { m }$ and any $z \in \mathbb { R } ^ { n } , \exists L _ { \mathcal { F } } ( z , r ) > 0$ such that for all $x , y \in \mathcal { B } _ { r } ( z ) , \theta \in \Theta , u \in \mathbb { U }$

$$\| \mathcal {F} (x, u, \theta) - \mathcal {F} (y, u, \theta) \| \leq L _ {\mathcal {F}} (z, r) \| x - y \|. \tag {22}$$

Let

$$
\bar {J} := \max_ {\substack {x \in \mathcal {B} _ {R} \\ w \in \mathbb {W}}} \hat {J} (x, w). \tag{23}
$$

Motivated by $V _ { c }$ of Lemma 1, a stability guarantee is established in the subsequent Section III using the Lyapunov function candidate

$$\mathcal {V} _ {c} (x, \tilde {\theta}) := \hat {J} (x, w) + \frac {1}{2} (\theta - \hat {\theta}) ^ {\top} \Gamma^ {- 1} (\theta - \hat {\theta}). \tag {24}$$

Observe that with Assumption 2, there exists $q _ { 1 , 2 , 3 , 4 } \in K _ { \infty }$ such that

$$q _ {1} (\| x \|) + q _ {3} (\| \tilde {\theta} \|) \leq \mathcal {V} _ {c} (x, \tilde {\theta}) \leq q _ {2} (\| x \|) + q _ {4} (\| \tilde {\theta} \|), \tag {25}$$

in which $\begin{array} { c c l } { q _ { 3 } ( \| \tilde { \theta } \| ) } & { = } & { \frac { 1 } { 2 } \lambda _ { \operatorname* { m i n } } ( \Gamma ^ { - 1 } ) \| \tilde { \theta } \| ^ { 2 } } \end{array}$ and $q _ { 4 } ( \lVert \tilde { { \boldsymbol { \theta } } } \rVert ) \ =$ $\frac { 1 } { 2 } \lambda _ { \operatorname* { m a x } } ( \Gamma ^ { - 1 } ) \Vert \tilde { \theta } \Vert ^ { 2 }$ . Regarding the overshoot and core balls as well as respective state membership, note that

$$q _ {1} (\| x \|) \leq \hat {J} (x, w) \leq q _ {1} (r) \Rightarrow x \in \mathcal {B} _ {r} \tag {26}$$

for any $r \geq 0$ and with $r ^ { * } : = q _ { 2 } ^ { - 1 } ( v ^ { * } / 2 )$ ,

$$\frac {v ^ {*}}{2} < \hat {J} (x, w) \leq q _ {2} (\| x \|) \Rightarrow x \notin \mathcal {B} _ {r ^ {*}} \tag {27}$$

for any $v ^ { * } > 0$ . Ensuring that the core ball radius satisfies $r ^ { * } < r ,$ take $v ^ { \ast } : = q _ { 1 } ( r )$ .
