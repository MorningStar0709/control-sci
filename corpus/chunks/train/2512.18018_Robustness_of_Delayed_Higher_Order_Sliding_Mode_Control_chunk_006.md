• A function $\hbar \in C ( \mathbb { R } _ { + } ^ { \star } \times \mathbb { R } _ { + } ^ { \star } , \mathbb { R } )$ is said to be of the class $\scriptstyle { \mathcal { T } } { \mathcal { K } } _ { \infty }$ (implicit $\kappa _ { \infty } )$ [37] if: 1) for any $s \in \mathbb { R } _ { + } ^ { \star }$ there exists $\alpha \in \mathbb { R } _ { + } ^ { \star }$ such that $\hbar ( \alpha , s ) = 0 ;
2 )$ for any fixed $s \in \mathbb { R } _ { + } ^ { \star }$ , the function $\hbar ( \cdot , s )$ is strictly decreasing on $\mathbb { R } _ { + } ^ { \star } ;
3 )$ for any fixed $\alpha \in \mathbb { R } ^ { \star }$ , the function $\hbar ( \alpha , \cdot )$ is strictly increasing on $\mathbb { R } _ { + } ^ { \star } ;
4 )$ for all $\begin{array} { r } { ( \alpha , s ) \in \Gamma = \{ ( \alpha , s ) \in \mathbb { R } _ { + } ^ { \star } \times \mathbb { R } _ { + } ^ { \star } : \hbar ( \alpha , s ) = 0 \} \colon \operatorname* { l i m } _ { s \to 0 ^ { + } } \alpha = 0 , \quad \operatorname* { l i m } _ { \alpha \to 0 ^ { + } } s = \alpha , } \end{array}$ 0, lim $_ { 1 _ { s \to + \infty } } \alpha = + \infty$ .

2. Problem Statement. This paper addresses the problem of uniform stabilization at the origin with an accelerated (hyperexponential) rate of convergence for a linear single-input system with matched and mismatched perturbations of the form:

$$\dot {x} (t) = A x (t) + b (u (t) + d (t)) + \delta (t), \quad t \geq 0, \tag {2.1}y (t) = x (t) + w (t), \tag {2.2}$$

where

$$
A = \left[ \begin{array}{c c c c c} 0 & 1 & 0 & \dots & 0 \\ 0 & 0 & 1 & \dots & 0 \\ \vdots & \vdots & \vdots & \ddots & \vdots \\ 0 & 0 & 0 & \dots & 1 \\ 0 & 0 & 0 & \dots & 0 \end{array} \right], \quad b = \left[ \begin{array}{c} 0 \\ 0 \\ \vdots \\ 0 \\ 1 \end{array} \right],
$$
