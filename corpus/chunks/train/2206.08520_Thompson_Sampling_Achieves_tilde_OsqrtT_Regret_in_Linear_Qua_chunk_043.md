# Appendix A. Notation

This section contains two tables which list the notations used throughout the paper for improving readability. In particular, Table 3 provides the system dependent notations and the useful notations for presenting the design of TSAC. In Table 4, we present the notation used in deriving theoretical results, namely, the regret analysis and the lower bound on the probability of selecting optimistic parameters. Further details are also referenced to the related parts of the paper.

<table><tr><td>System Not.</td><td>Definition</td></tr><tr><td> $\Theta_{*}$ </td><td>Unknown discrete-time LTI system with dynamics of (1);
[A* B*] $^{\intercal}$ </td></tr><tr><td> $x_{t}$ </td><td>State of the system ∈  $\mathbb{R}^{n}$ </td></tr><tr><td> $u_{t}$ </td><td>Input to the system ∈  $\mathbb{R}^{d}$ </td></tr><tr><td> $w_{t}$ </td><td>Process noise as defined in Assumption 2;
$\mathcal{N}(0, \sigma_{w}^{2}I)$ </td></tr><tr><td> $z_{t}$ </td><td>Stack of current state and input;
[xt $^{\intercal}$  ut $^{\intercal}$ ] $^{\intercal}$ </td></tr><tr><td>Q,R</td><td>Known cost matrices;
||Q||, ||R|| &lt;
$\bar{\alpha}$  and  $\sigma_{\min}(Q)$ ,  $\sigma_{\min}(R) > \underline{\alpha} > 0$ </td></tr><tr><td> $c_{t}$ </td><td>Quadratic cost at time t;
xt $^{\intercal}$  Qxt + ut $^{\intercal}$  Rut</td></tr><tr><td>S</td><td>Set of (κ,γ)-stabilizable and bounded systems that Θ* belongs (Assumption 1)</td></tr><tr><td>P(Θ)</td><td>Unique p.d.
solution to DARE (3) for a stabilizable system Θ = [AB] $^{\intercal}$ </td></tr><tr><td>K(Θ)</td><td>Optimal controller for Θ;
-(R + BT P(Θ)B) $^{-1}$ BT P(Θ)A</td></tr><tr><td>J(Θ)</td><td>Average expected cost of system Θ;
$\sigma_{w}^{2}$  tr(P(Θ))</td></tr><tr><td>κ</td><td>Bound over all possible optimal controllers in S;
sup $_{\Theta \in S} K(\Theta)$ </td></tr><tr><td>D</td><td>Bound over all possible solutions to (3) in S;
$\bar{\alpha}\gamma^{-1}\kappa^{2}(1+\kappa^{2})$ </td></tr><tr><td>TSAC Not.</td><td></td></tr><tr><td> $\hat{\Theta}$ </td><td>Least squares estimate of Θ* using the history of inputs and states;
[ $\hat{A}$   $\hat{B}$ ] $^{\intercal}$ </td></tr><tr><td>μ</td><td>Regularizer for least squares;
set to (1+κ $^{2}$ )X $_{s}^{2}$ </td></tr><tr><td> $V_{t}$ </td><td>Regularized design matrix;
$\mu I + \sum_{s=0}^{t-1} z_{s} z_{s}^{\intercal}$ </td></tr><tr><td> $\eta_{t}$ </td><td>Random matrix with iid standard normal entries used for sampling systems</td></tr><tr><td> $\mathcal{R}_{\mathcal{S}}(\cdot)$ </td><td>Rejection sampling to make sure that sampled system belongs to S</td></tr><tr><td> $\tilde{\Theta}$ </td><td>System obtained via TS;
