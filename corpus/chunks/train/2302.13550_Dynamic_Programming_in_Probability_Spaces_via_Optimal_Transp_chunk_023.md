![](image/4aeda41692a92cb1fe45790b2996630dcf795eba7b0d59c8ed833a8bb1007c6d.jpg)  
(b) Online requirements. Computational time for DPA in probability spaces (empty marker) and the recipe in Corollary 4.2 (filled marker) in settings with 10 agents (left y-axis) and 100 agents (right y-axis).   
Fig. 1: Sensitivity of offline and online computational effort to the number of agents M and states |X| for the DPA in probability spaces (empty marker) and Corollary 4.2 (filled marker), with |U|, N, O being the number of actions, time-steps, and operations, respectively. We omit markers when numbers exceed the IEEE 754 floating point representation. The DPA in probability spaces is infeasible already for a small number of agents. The recipe in Corollary 4.2, instead, remains feasible for large fleet sizes.

Online computation. As long as the dynamic programming table in probability spaces has a reasonable size (which, as discussed above, is not the case already for a few states and agents), the cost amounts to the lookup2 of the optimal state-input distribution. Instead, the recipe in Corollary 4.2 requires solving an optimal transport problem (i.e., an allocation problem), which with efficient solvers is real-time feasible even in large instances (e.g., a few milliseconds for thousands of agents with [35])3. While the computational time depends on the specific instance, we randomly generate a cost-to-go for variable problem sizes, and we juxtapose the timings (averaged over thousand samples) in Figure 1b. As the DPA in probability spaces only amounts to a look-up, its deployment entails almost no computational effort (empty marker in the plot), but it is only feasible in instances where the dynamic programming table can be computed (i.e., up to $| X | = 1 0 0$ for 10 agents and $| X | = 1 0$ for 100 agents). The recipe in Corollary 4.2, instead, yields real-time-feasible computational times across all instances (filled markers in the plot), with moderate dependence on the problem size.
