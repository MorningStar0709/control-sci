# C. Lagrangian Neural Network

Lagrangian Neural Network (LNN) [8] is used to learn the Lagrangian function of mechanical systems from data. Unlike conventional supervised learning, LNN is constructed to respect the Euler-Lagrange equation in an unsupervised manner. By endowing the laws of physics, LNN has better inductive biases.

In LNN, the Lagrangian function is approximated by an FCNN $\pmb { \mathcal { L } } ( \pmb { q } , \pmb { \dot { q } } ; \gamma )$ with the parameters γ. During training, $N +$ 1 samples $\{ \pmb q ( i ) , \pmb { \dot { q } } ( i ) , \pmb { \ddot { q } } ( i ) , \pmb { u } ( i ) \} _ { i = 0 } ^ { N }$ are collected. Then the LNN is trained by solving the following optimization problem

$$\min _ {\boldsymbol {\gamma}} \quad \sum_ {i = 0} ^ {N} \left(\hat {\ddot {\boldsymbol {q}}} (i) - \ddot {\boldsymbol {q}} (i)\right) ^ {\top} \boldsymbol {Q} \left(\hat {\ddot {\boldsymbol {q}}} (i) - \ddot {\boldsymbol {q}} (i)\right)
\begin{array}{l} \text { s.t., } \quad \hat {\ddot {\boldsymbol {q}}} (i) = \frac {\partial^ {2} \mathcal {L} (\boldsymbol {q} (i) , \dot {\boldsymbol {q}} (i) ; \boldsymbol {\gamma})}{\partial \dot {\boldsymbol {q}} (i) \partial \dot {\boldsymbol {q}} (i)} ^ {- 1} [ \boldsymbol {u} (i) \tag {4} \\ + \frac {\partial \mathcal {L} (\boldsymbol {q} (i) , \dot {\boldsymbol {q}} (i) ; \boldsymbol {\gamma})}{\partial \boldsymbol {q} (i)} - \frac {\partial^ {2} \mathcal {L} (\boldsymbol {q} (i) , \dot {\boldsymbol {q}} (i) ; \boldsymbol {\gamma})}{\partial \boldsymbol {q} (i) \partial \dot {\boldsymbol {q}} (i)} \dot {\boldsymbol {q}} (i) ] \\ \end{array}
$$

where $Q = Q ^ { \top } > 0$ is the weight matrix, and the Jacobian and Hessian matrix of L are calculated using automatic differentiation [33]. By solving (4), one can obtain the optimal parameter $\gamma ^ { * }$ . Then, using automation differentiation, we can obtain an approximation of M as $\begin{array} { r } { \hat { M } \ = \ \frac { \partial ^ { 2 } \mathcal { L } ( q , \dot { q } ; \gamma ^ { * } ) } { \partial \dot { q } \partial \dot { q } } } \end{array}$ ∂2L(q,q˙;γ∗) , C as Cˆ = ∂q˙∂q˙ $\hat { C } =$ $\dot { \hat { M } } ( \pmb q ) - \frac { 1 } { 2 } \dot { \hat { M } } ( \pmb q ) ^ { \top }$ , and G as $\begin{array} { r } { \hat { \pmb { G } } = - \frac { \partial \mathcal { L } ( \pmb { q } , \dot { \pmb { q } } ; \pmb { \gamma } ^ { * } ) } { \partial \pmb { q } } + \frac { 1 } { 2 } \pmb { \dot { M } } ( \pmb { q } ) ^ { \top } \pmb { \dot { q } } . } \end{array}$ − ∂L(q,q˙;γ∗)∂q + 12 Mˆ˙ (q)⊤q˙. 0q

To effectively solve the optimization problem (4), a special initialization strategy is necessary [31], which is defined according to the depth and width of the LNN

$$
\nu = \frac {1}{\sqrt {n}} \left\{ \begin{array}{c c} 2. 2 & \text { First   layer } \\ 0. 5 8 i & \text { Hidden   layer } i \in \{1, \dots \} \\ n & \text { Output   layer } \end{array} , \right. \tag {5}
$$

where i represents the i-th layer of the neural network, n is the number of neurons in this layer. The parameters of every layer are then initialized according to $\mathcal { N } ( 0 , \nu ^ { 2 } )$ , where $\mathcal { N }$ represents a Gaussian distribution.
