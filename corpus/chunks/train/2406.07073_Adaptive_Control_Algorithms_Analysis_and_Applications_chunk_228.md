We will also assume that the structure and the complexity (order the various polynomials) of the adjustable predictor is such that there is a parameter vector θ (or several) such that the prediction error $\varepsilon ( t + 1 )$ in a deterministic environment be null and, in addition, in a stochastic environment either the prediction error is a white noise or $\mathbf { E } \{ \varepsilon ( t + 1 ) \phi ( t ) \} = 0$ , where $\phi ( t )$ is the observation vector. In fact, one assumes that the true plant + disturbance model and the adjustable predictor have the same structure and, therefore, it is a value of the parameter vector θ for which the adjustable predictor will correspond to the true plant + disturbance model. One can say that the true plant + disturbance model is in the set of the possible estimated models parameterized by $\hat { \theta }$ or, in short, that “the true plant + disturbance model is in the model $s e t ^ { \prime }$ (Ljung 1999).

Table 5.1 summarizes a number of significant recursive parameter estimation techniques. They all use PAA of the form:

$$\hat {\theta} (t + 1) = \hat {\theta} (t) + F (t) \Phi (t) v (t + 1) \tag {5.7}F (t + 1) ^ {- 1} = \lambda_ {1} (t) F (t) ^ {- 1} + \lambda_ {2} (t) \varPhi (t) \varPhi^ {T} (t)0 < \lambda_ {1} (t) \leq 1; 0 \leq \lambda_ {2} (t) < 2; F (0) > 0F ^ {- 1} (t) > \alpha F ^ {- 1} (0); 0 < \alpha < \infty \tag {5.8}\nu (t + 1) = \frac {\nu^ {0} (t + 1)}{1 + \Phi^ {T} (t) F (t) \Phi (t)} \tag {5.9}$$

Integral + proportional PAA can also be used.

In the next section the various algorithms will be discussed and analyzed.

a)
