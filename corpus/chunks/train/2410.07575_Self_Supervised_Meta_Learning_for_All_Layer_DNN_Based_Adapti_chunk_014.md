Proof: Since the input and disturbance to system (18) are bounded, we can prove the boundedness of the states $s ,$ $q , \dot { q }$ via Input-to-State Stability (ISS) [11], [44].

According to [42], [45], there exists a function ζ that the network Jacobian J is bounded by input q, q˙ and network parameter θ:

$$\| J \| \leq \bar {J} = \sup _ {t} \zeta (\| q \|, \| \dot {q} \|, \nu) \tag {19}$$

where ν is the layer spectral norm bound of network parameter $\theta .$

Next, we prove that the tracking error $\tilde { q }$ converges to an error ball exponentially fast.

Theorem 2: Consider the system with dynamics (1), controller (13), and adaptation law (15). Suppose Assumptions 1, 2, and 3 are satisfied. The parameter estimation error ˜θ and trajectory tracking error $\tilde { q }$ converge to an error ball exponentially fast.

Proof: Consider the Lyapunov function given by

$$
V = \left[ \begin{array}{c} s \\ \tilde {\theta} \end{array} \right] ^ {\top} \left[ \begin{array}{c c} M & 0 \\ 0 & \gamma^ {- 1} I \end{array} \right] \left[ \begin{array}{c} s \\ \tilde {\theta} \end{array} \right] \tag {20}
$$

Using the fact that $\dot { M } - 2 C$ is skew symmetric, and substituting $\tilde { \theta } = \theta - \theta ^ { * }$ , the adaptation law (15), and the network Taylor expansion (17), we have

$$
\begin{array}{l} \dot {V} = - 2 \left[ \begin{array}{c} s \\ \tilde {\theta} \end{array} \right] ^ {\top} \left[ \begin{array}{c c} K & 0 \\ 0 & J ^ {\top} \Gamma J + \lambda I \end{array} \right] \left[ \begin{array}{c} s \\ \tilde {\theta} \end{array} \right] \\ - 2 \left[ \begin{array}{c} s \\ \tilde {\theta} \end{array} \right] ^ {\top} \left[ \begin{array}{c} r - \mu - \epsilon \\ J ^ {\top} \Gamma r + \lambda (\theta^ {*} - \theta_ {0}) \end{array} \right] \tag {21} \\ \end{array}
$$

Since K, λI, M and $\gamma ^ { - 1 } I$ are all positive definite matrices, and $J ^ { \top } \Gamma J$ is positive semidefinite, there exists a constant $\rho > 0$ such that

$$
- 2 \left[ \begin{array}{c c} K & 0 \\ 0 & J ^ {\top} \Gamma J + \lambda I \end{array} \right] \preceq \rho \left[ \begin{array}{c c} M & 0 \\ 0 & \gamma^ {- 1} I \end{array} \right] \tag {22}
$$

We define the upper bound for the disturbance term D as

$$
D = \sup _ {t} \left\| - 2 \left[ \begin{array}{c} r - \mu \\ J ^ {\top} \Gamma (r + \epsilon + \mu) + \lambda (\theta^ {*} - \theta_ {0}) \end{array} \right] \right\| \tag {23}
$$
