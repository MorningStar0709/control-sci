$$
z = \left[ \begin{array}{c c} z _ {1} ^ {\top} & z _ {2} ^ {\top} \end{array} \right] ^ {\top}: \mathbb {Z} _ {0} \to \mathbb {R} ^ {n _ {1} + n _ {2}}
$$

satisfies $z _ { 1 } ( 0 ) = z _ { 1 } ^ { 0 }$ . This state $z ( k ) , k \in \mathbb { Z } _ { 0 }$ , is given by

$$z _ {1} (k) = A _ {1} ^ {k} z _ {1} (0) + \sum_ {m = 1} ^ {k} A _ {1} ^ {k - m} B _ {1} u (m - 1), \tag {5}$$

and

$$z _ {2} (k) = R ^ {l - k} z _ {2} (l) - \sum_ {m = 0} ^ {l - k - 1} R ^ {m} B _ {2} u (k + m), \quad l \geq k. \tag {6}$$

Here, (5) captures the dynamics of the slow subsystem of system (1), while (6) the fast subsystem. Since in the fast subsystem, the current state depends on the future inputs (from $( 6 ) , z _ { 2 } ( k )$ is determined by $\{ u ( k ) , \cdot \cdot \cdot , u ( k +$ $s - 1 ) \}$ , discrete-time descriptor systems are generically non-causal.

Definition 1 [23] System (1) is called R-controllable if its slow subsystem (5) is controllable, i.e.,

$r k ( [ B _ { 1 } , A _ { 1 } B _ { 1 } , \cdot \cdot \cdot , A _ { 1 } ^ { n _ { 1 } - 1 } B _ { 1 } ] ) = n _ { 1 }$ . System (1) is Ccontrollable if both of its slow and fast subsystems are controllable, $i . e . , \ r k ( [ \bar { B } _ { 1 } , A _ { 1 } B _ { 1 } , \cdot \cdot \cdot , A _ { 1 } ^ { n _ { 1 } - 1 } \bar { B } _ { 1 } ] ) \ = \ n _ { 1 }$ and $r k ( [ B _ { 2 } , R B _ { 2 } , \cdot \cdot \cdot , \stackrel { . . } { R } { } ^ { s - 1 } B _ { 2 } ] ) \ = \ n _ { 2 } . \stackrel { . . } { S y s t e m } \ ( 1 )$ is called stabilizable if there exists a matrix $\ b { K } \in \mathbb { R } ^ { m \times n }$ , such that $\sigma ( E , A + B \bar { K } )$ is within the unit circle. It is called detectable if its dual system $( E ^ { \top } , A ^ { \top } , C ^ { \top } )$ is stabilizable.

Definition 2 [23] System (1) is called observable if its state x(k) at any time point k is uniquely determined by $\{ u ( i ) , \dot { y ( i ) } , i = 0 , \cdot \cdot \cdot , l \}$ where l is sufficiently large.

It is known that system (1) is detectable if and only if [14]

$$
r k \left[ \begin{array}{c} \lambda E - A \\ C \end{array} \right] = n, \quad \forall \lambda \in \mathbb {C}, | \lambda | \geq 1. \tag {7}
$$

Moreover, system (1) is observable if and only if

$$
r k \left[ \begin{array}{c} \lambda E - A \\ C \end{array} \right] = n,   \forall \lambda \in \mathbb {C},   \text { and }   r k \left[ \begin{array}{c} E \\ C \end{array} \right] = n.
$$
