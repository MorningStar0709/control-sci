$$\left| \left| \boldsymbol {A} _ {\boldsymbol {\theta}} \psi_ {\boldsymbol {\theta}} (\boldsymbol {x} _ {t}) + \boldsymbol {B} _ {\boldsymbol {\theta}} \boldsymbol {u} _ {t} - \psi_ {\boldsymbol {\theta}} (\boldsymbol {x} _ {t + 1}) \right| \right| \tag {3}$$

supervised learning3. State prediction in the original system state, combining all elements of the model. The associated loss function is given by

$$\left| \left| C _ {\boldsymbol {\theta}} \left(\boldsymbol {A} _ {\boldsymbol {\theta}} \psi_ {\boldsymbol {\theta}} \left(\boldsymbol {x} _ {t}\right) + \boldsymbol {B} _ {\boldsymbol {\theta}} \boldsymbol {u} _ {t}\right) - \boldsymbol {x} _ {t + 1} \right| \right|. \tag {4}$$

The norm || · || is the mean-squared error, averaging over the number of samples. Depending on the system, it can be beneficial to use multi-step versions of the prediction loss (Equation 3) and the combined loss (Equation 4) to improve model accuracy when predicting over multiple time steps in closed-loop fashion. We follow standard SI practices, such as using a separate validation data set for early stopping and normalizing the inputs and outputs of the model.
