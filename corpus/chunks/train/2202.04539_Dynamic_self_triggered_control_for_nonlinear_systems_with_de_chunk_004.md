# 2. SETUP

In this section, we describe the setup of the paper and give a precise problem statement. The plant is given by

$$\dot {x} = f _ {p} (x, \hat {u}), \tag {1}$$

where $x ( t ) \in \mathbb { R } ^ { n _ { x } }$ is the plant state with initial condition $x ( 0 ) ~ = ~ \dot { x } _ { 0 } ~ \mathrm { a n d } ~ \hat { u } ( t ) ~ \in ~ \mathbb { R } ^ { n _ { u } }$ is the last input that has been received by the plant. The input is generated by the static state-feedback controller $u = g _ { c } ( x )$ . The function $f _ { p }$ is assumed to be continuous and $g _ { c }$ is assumed to be continuously differentiable.

The plant state $x ( t _ { k } )$ is sampled at sampling instants 1 $t _ { k } , k \in \mathbb { N } _ { e } ,$ that are determined by a sampling mechanism to be specified later. At each sampling instant, the input $u ( t _ { k } )$ is computed based on the sampled state $x ( t _ { k } )$ and send over the network. The values arrive at the actuator after a transmission delay of $\tau _ { k }$ time units, resulting in an update of ˆu at the corresponding times $t _ { k + 1 } = t _ { k } + \tau _ { k }$ . We assume that the maximum delay is bounded as $\tau _ { k } \le \tau _ { \mathrm { m a d } }$ for all $k \in \mathbb { N } _ { e }$ and some known $\tau _ { \mathrm { m a d } } > 0$ . Between update times, the values of ˆu are kept constant, which resembles a zero-order-hold (ZOH) scenario. We assume for simplicity, that the plant is sampled at $t _ { 0 } ~ = ~ 0$ and that initially $\hat { u } ( 0 ) ~ = ~ \bar { g _ { c } } ( 0 )$ for $t \in \overline { { [ 0 , \tau _ { 0 } ) } }$ . For analysis purposes, we denote by ˆx the state that is corresponding to the current value of $\hat { u } , \mathrm { i . e . , } \hat { x } ( t ) = x ( t _ { k } ) , t \in [ t _ { k } + \tau _ { k } , t _ { k + 2 } + \tau _ { k + 2 } )$ and ${ \hat { x } } ( t ) = 0$ for $t \in [ 0 , \tau _ { 0 } )$ , resembling again a ZOH scenario. The sampling induced error is denoted by $e : = { \hat { x } } - x .$ .
