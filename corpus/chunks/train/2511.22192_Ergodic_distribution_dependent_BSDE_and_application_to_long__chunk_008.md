(b) $\mathrm { L } _ { \mathcal { P } } ^ { 2 } ( \Omega ; \mathrm { L } ^ { 2 } ( [ 0 , T ] ; \mathbb { R } ^ { d } ) )$ ) the set of predictable processes ?? on [0, ??] such that

$$\| X \| _ {\mathrm{L} ^ {2}} ^ {2} := \| X \| _ {\mathrm{L} _ {\mathcal {P}} ^ {2} ([ 0, T ]; E)} ^ {2} = \mathbb {E} \left[ \int_ {0} ^ {T} | X _ {s} | ^ {2} \mathrm{d} s \right].$$

(c) $\mathrm { L } _ { \mathrm { l o c } } ^ { 2 } ( \Omega ; \mathrm { L } ^ { 2 } ( \mathbb { R } _ { + } ; \mathbb { R } ^ { d } ) )$ the set of predictable processes $X \in \mathrm { L } _ { \mathcal { P } } ^ { 2 } ( \Omega ; \mathrm { L } ^ { 2 } ( [ 0 , T ] ; \mathbb { R } ^ { d } ) )$ for any $T > 0$

(d) $S _ { T } ^ { 2 }$ the set of continuous, F −adapted processes ?? on [0, ??] with value in $\mathbb { R } ^ { 2 }$ satisfying

$$\| X \| _ {\mathcal {S} _ {T} ^ {p}} := \mathbb {E} \left[ \sup _ {t \in [ 0, T ]} | X _ {s} | ^ {2} \right] ^ {1 / 2} < \infty .$$

(e) $\mathcal { P } _ { p } ( \mathbb { R } ^ { d } )$ be the set of probabilities measures having finite $p \mathrm { - t h }$ moment over $\mathbb { R } ^ { d }$ endowed with the $p { \mathrm { - } } \mathrm { W }$ asserstein distance defined by: for $\nu , \mu \in \mathcal { P } _ { p } ( \mathbb { R } ^ { d } )$

$$\mathcal {W} _ {p} (\nu , \mu) = \inf _ {\Pi \in \mathcal {P} _ {\nu , \mu}} \left(\int_ {\mathbb {R} ^ {d} \times \mathbb {R} ^ {d}} | x - y | ^ {p} \Pi (\mathrm{d} x, \mathrm{d} y)\right) ^ {1 / p},$$

where $\mathcal { P } _ { \nu , \mu }$ is the set of all couplings of probability distributions ?? and $\mu ,$ i.e. the set of all probability distributions on $\mathbb { R } ^ { d } \times \mathbb { R } ^ { d }$ with marginals $\nu , \mu .$ .

(f) $\mathcal { C } ^ { k , m } ( \mathbb { R } ^ { d } \times \mathcal { P } ( \mathbb { R } ^ { d } ) ; \mathbb { R } )$ be the set of ?? times continuously differentiable w.r.t the first variable and ?? times differentiable w.r.t to the second one.

We also denote $\nabla _ { x } f$ the gradient of ?? w.r.t ?? which will be seen as an element of $\mathbb { R } ^ { 1 \times d }$

2. Norms: We naturally endow $\mathbb { R } ^ { d }$ with the Euclidean norm denoted |·| and for a $\sigma \in \mathbb { R } ^ { d \times d }$ we denote $\| \sigma \|$ the Frobenius norm. Namely, $\| \sigma \| ^ { 2 } : = \mathrm { T r } \left( \sigma \sigma ^ { \top } \right)$

3. Constants: For $\varphi : \mathbb { R } ^ { d } \times \mathcal { P } _ { p } ( \mathbb { R } ^ { d } ) \mapsto H$ a Lipschitz function w.r.t both variables and ?? an Hilbert space, we set, up to transformations, $K _ { x } ^ { \varphi }$ (resp. $K _ { \mathcal { L } } ^ { \varphi } )$ the Lipschitz constant for the space variable (resp. for the distribution variable). The constants ?? will be denoted with a index ?? to enlighten the dependency w.r.t ??. The constants ?? might change from one line to another.
