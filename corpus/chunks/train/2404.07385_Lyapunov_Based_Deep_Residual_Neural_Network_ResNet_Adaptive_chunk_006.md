The ResNet-based adaptive controller shows a 64% improvement in the tracking and function approximation performance, in comparison to a fully-connected DNN-based adaptive controller.

Notation and Preliminaries: The space of essentially bounded Lebesgue measurable functions is denoted by $\mathcal { L } _ { \infty } .$ . The right-to-left matrix product operator is represented by Q, ↶ i.e., $\prod _ { p = 1 } ^ { m } A _ { p } = A _ { m } \cdot \cdot \cdot A _ { 2 } A _ { 1 }$ m and Q m $A _ { p } = I { \mathrm { ~ i f ~ } } a > m$ . The p=a Kronecker product is denoted by ⊗. Function compositions are denoted using the symbol $\circ , \mathbf { e . g . } , ( g \circ h ) ( x ) = g ( h ( x ) )$ , given suitable functions g and h. The Filippov set-valued map defined in [22, Equation 2b] is denoted by $K \left[ \cdot \right]$ . The a.a.t. notation (·) denotes that the relation (·) holds for almost all time (a.a.t.). Consider a Lebesgue measurable and locally essentially bounded function $h : \mathbb { R } ^ { n } \times \mathbb { R } _ { > 0 } \to \mathbb { R } ^ { n }$ . Then, the function $y : \mathcal { T }  \mathbb { R } ^ { n }$ is called a Filippov solution of $\dot { y } ~ = ~ h ( y , t )$ on the interval $\mathcal { T } \subseteq \mathbb { R } _ { \geq 0 }$ if y is absolutely continuous on I and $\dot { y } \stackrel { a . a . t . } { \in } K \left[ h \right] \left( y , t \right)$ . Given $w \in \mathbb { R }$ and some functions f and $^ { g , }$ the notation $f ( w ) ~ = ~ \mathcal { O } ^ { m } ( g ( w ) )$ means that there exists some constants $M \in \mathbb { R } _ { > 0 }$ and $w _ { 0 } \in \mathbb { R }$ such that $\left\| { f ( w ) } \right\| \le M \left\| g ( w ) \right\| ^ { m }$ for all $w \geq w _ { 0 }$ . Given some matrix $A \triangleq [ a _ { i , j } ] \in \mathbb { R } ^ { n \times m }$ , where $a _ { i , j }$ denotes the element in the $i ^ { t h }$ row and ${ \bf { \bar { \rho } } } _ { j } t h$ column of $A ,$ , the vectorization operator is defined as $\operatorname { v e c } ( A ) \triangleq [ a _ { 1 , 1 } , \dotsc , a _ { 1 , m } , \dotsc , a _ { n , 1 } , \dotsc , a _ { n , m } ] ^ { T } \in$ $\mathbb { R } ^ { n m }$ . The p-norm is denoted by $\left\| \cdot \right\| _ { p } ,$ where the subscript is suppressed when $p = 2$ . The Frobenius norm is denoted by $\| \cdot \| _ { F } \triangleq \| \mathrm { v e c } ( \cdot ) \|$ . Given any $A \in \mathbb { R } ^ { p \times a } , B \in \mathbb { R } ^ { a \times r }$ , and $C \in \bar { \mathbb { R } } ^ { r \times s }$ , the vectorization operator satisfies the property [23, Proposition 7.1.9]

$$\operatorname{vec} (A B C) = \left(C ^ {T} \otimes A\right) \operatorname{vec} (B). \tag {1}$$
