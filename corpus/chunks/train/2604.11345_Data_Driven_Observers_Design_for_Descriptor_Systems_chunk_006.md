# 2 Preliminaries and model-based observers

Consider a discrete-time linear descriptor system $\Sigma _ { 1 }$

$$E x (k + 1) = A x (k) + B u (k) \tag {1a}y (k) = C x (k) \tag {1b}$$

with a consistent initial condition x(0) such that $A x ( 0 ) +$ $B u ( 0 ) ~ \in ~ I m ( E )$ , where $A , E \ \in \ \bar { \mathbb { R } } ^ { n \times n } , \ B \ \in \ \mathbb { R } ^ { \bar { n } \times m }$ ,

$C \in \mathbb { R } ^ { p \times n }$ . We assume that $d e t ( \lambda E - A ) \neq 0$ for some $\lambda \in \mathbb { C } .$ , i.e., regularity of system (1). Particularly, we are interested in the case where the matrix E is singular, i.e., $r k ( E ) < n$ . We denote the finite spectrum set of $( E , A ) { \mathrm { ~ b y ~ } } \sigma ( E , A )$ , defined to be $\sigma ( E , { \bar { A } } ) = \{ \lambda \in \mathbb { C }$ : $d e t ( \lambda E - A ) = 0 \}$ .

Since the descriptor system (1) is regular, there exist invertible matrices $P , \breve { S } \in \mathbb { R } ^ { n \times n }$ such that (cf. [23])

$$
S E P = \left[ \begin{array}{c c} I _ {n _ {1}} & 0 \\ 0 & R \end{array} \right], \quad S A P = \left[ \begin{array}{c c} A _ {1} & 0 \\ 0 & I _ {n _ {2}} \end{array} \right], \tag {2}

S B = \left[ \begin{array}{c} B _ {1} \\ B _ {2} \end{array} \right], \quad C P = \left[ \begin{array}{c c} C _ {1} & C _ {2} \end{array} \right],
$$

where $R \in \mathbb { R } ^ { n _ { 2 } \times n _ { 2 } }$ is nilpotent with nilpotency index $s \in \mathbb { Z } _ { 0 } ~ ( \mathrm { i . e . , } ~ R ^ { s - 1 } \neq 0$ and $R ^ { s } = 0 )$ , and $\mathbf { \check { A } } _ { 1 } \in \mathbf { \check { R } } ^ { n _ { 1 } \times n _ { 1 } }$ , $B _ { 1 } \in \mathbb { R } ^ { \dot { n } _ { 1 } \times m } , B _ { 2 } \in \mathbb { R } ^ { n _ { 2 } \times m } , C _ { 1 } \in \mathbb { R } ^ { p \times n _ { 1 } } , C _ { 2 } \in \mathbb { R } ^ { p \times n _ { 2 } }$ 2 with $n _ { 1 } + n _ { 2 } = n .$ . Accordingly, P is partitioned as $P =$ $\left[ P _ { 1 } \ P _ { 2 } \right]$ , where $P _ { 1 } \in \mathbb { R } ^ { n \times n _ { 1 } }$ and $P _ { 2 } \in \mathbb { R } ^ { n \times n _ { 2 } }$ . Upon the introduction of the coordinate change $z = P ^ { - 1 } x ,$ , system (1) can be equivalently written in the following form

$$
\left[ \begin{array}{c c} I _ {n _ {1}} & 0 \\ 0 & R \end{array} \right] z (k + 1) = \left[ \begin{array}{c c} A _ {1} & 0 \\ 0 & I _ {n _ {2}} \end{array} \right] z (k) + \left[ \begin{array}{c} B _ {1} \\ B _ {2} \end{array} \right] u (k), \tag {3}

y (k) = \left[ \begin{array}{l l} C _ {1} & C _ {2} \end{array} \right] z (k). \tag {4}
$$

Given an input trajectory u : $\mathbb { Z } _ { 0 } \to \mathbb { R } ^ { m }$ and an initial value $z _ { 1 } ^ { 0 } \in \dot { \mathbb R ^ { n _ { 1 } } }$ there is a unique trajectory such that the state
