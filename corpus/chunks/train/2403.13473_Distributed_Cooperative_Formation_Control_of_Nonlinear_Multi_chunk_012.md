$$\dot {\widehat {W}} _ {i} (t) = \Gamma_ {i} \left(\Phi_ {i} (x _ {i}, v _ {i}) \big (e _ {x i} (t) + e _ {v i} (t) \big) ^ {T} - \sigma_ {i} \widehat {W} _ {i} (t)\right) (1 6)i = 1, 2, \dots , N$$

where $\Gamma _ { i } \in \mathbb { R } ^ { m \times m } , m = N - 1$ is a positive definite matrix, $\sigma _ { i } > 0$ is a design constant.

Theorem 2. Lyapunov Stability Theory

Consider a dynamical system which satisfies

$$
\left\{ \begin{array}{l} \dot {x} _ {i} (t) = v _ {i} (t) \\ \dot {v} _ {i} (t) = f (x _ {i}, v _ {i}) + u _ {i} (t), t > 0 \end{array} \right. \tag {17}
$$

The equilibrium point $x ^ { * } = 0$ is stable and $f ( x _ { 0 } , v _ { 0 } ) =$ $\varepsilon _ { i } ( 0 )$ , if there is a scalar function $V \left( x \right)$ which has a continuous first-order derivative, and satisfies $V ( 0 ) = 0$ any non-zero point ???? in the state space satisfies:

1. $V ( x )$ is positive;   
2. $\dot { V } ( x )$ is negative;   
3. When $t \to \infty , V \to \infty ;$

So, if the origin of the system is balanced in a large range of increasingly stable, the system is stable.

For the non-linear second-order multi-agent system, if the system formation control with NN weight updating law is performed for the system and can design constants are chosen to satisfy

$$\gamma_ {x} > 1, \gamma_ {v} > 1. 5 + 1 / 2 (\lambda_ {\mathrm{min}} ^ {L}) ^ {2}\gamma_ {x} + \gamma_ {v} > \frac {1}{\lambda_ {\min} ^ {L}} \tag {18}$$

where $\lambda _ { \operatorname* { m i n } } ^ { L }$ is the minimal eigenvalue of matrix ????, then the following control objectives can be achieved.

The Lyapunov theory will be used to prove that the nonlinear multi-agent system is stable:

$$
\begin{array}{l} V (t) = \frac {1}{2} z ^ {T} (t) \left(\left[ \begin{array}{c c} \left(\gamma_ {x} + \gamma_ {v}\right) \hat {L} \hat {L} & \hat {L} \\ \hat {L} & \hat {L} \end{array} \right] \otimes I _ {N}\right) \tag {19} \\ \times z (t) + \frac {1}{2} \sum_ {i = 1} ^ {N} \mathrm{Tr} \left\{\widehat {W} _ {i} ^ {T} (t) \Gamma_ {i} ^ {- 1} \widehat {W} _ {i} (t) \right\}, \\ \end{array}
$$

where $\widehat { L } = L + D$ , and $\widetilde { W } _ { i } ( t ) = \widehat { W } _ { i } ( t ) - W _ { i } ^ { * }$

Given equation (15) the fact $( \gamma _ { x } + \gamma _ { v } ) \hat { L } \hat { L } - \hat { L } > 0 .$ , hence $\left[ \begin{array} { c c } { ( \gamma _ { x } + \gamma _ { v } ) \hat { L } \hat { L } } & { \hat { L } } \\ { \hat { L } } & { \hat { L } } \end{array} \right]$ is a positive definite matrix, thus the function $V ( t )$ can be considered as a Lyapunov function candidate, and $V ( t ) > 0$ .

The time derivative of $V ( t )$ is:
