where the nonlinearity $f ( x _ { t } , t )$ satisfies the sector bound denoted by a constant matrix $U _ { * } ,$ $\| f ( x _ { t } ) \| \leq \| U _ { * } x _ { t } \|$ . For given parameters $\lambda > 0 , \gamma > 0$ , if there exists $P > 0$ satisfying

$$
\left[ \begin{array}{c c c c} G ^ {T} P G - P + \lambda^ {2} U _ {*} ^ {T} U _ {*} & G ^ {T} P F & G ^ {T} P H & C ^ {T} \\ F ^ {T} P G & F ^ {T} P F - \lambda^ {2} I & F ^ {T} P H & 0 \\ H ^ {T} P G & H ^ {T} P F & H ^ {T} P H - \gamma^ {2} I & 0 \\ C & 0 & 0 & - I \end{array} \right] <   0 \tag {54}
$$

then (53) is robustly asymptotically stable with sector-bounded $f ( x _ { t } , t )$ in the absence of $w _ { t }$ and satisfies robust performance $\| z \| _ { 2 } < \gamma \| w \| _ { 2 }$ in the presence of $w _ { t }$ .

Proof 1: The entire proof is divided into two parts. Part 1 provides the necessary conditions for asymptotic stability, while Part 2 extends the proof to guarantee robust control performance.

⇒ Part 1 - Robust stability under sector-bounded $f ( x )$

Consider a Lyapunov function $V ( t )$ and $V ( t + 1 )$ at time step ?? and $t + 1$ , respectively, as

$$V (t) = x _ {t} ^ {T} P x _ {t} + \lambda^ {2} \sum_ {\tau = 0} ^ {k - 1} (\| U _ {*} x _ {\tau} \| ^ {2} - \| f (x _ {\tau}) \| ^ {2}) \tag {55}V (t + 1) = x _ {t + 1} ^ {T} P x _ {t + 1} + \lambda^ {2} \sum_ {\tau = 0} ^ {k} (\| U _ {*} x _ {\tau} \| ^ {2} - \| f (x _ {\tau}) \| ^ {2}) \tag {56}$$

Here, $V ( t ) \geq 0$ since $\| f ( x _ { t } ) \| \leq \| U _ { * } x _ { t } \|$ . In the absence of $w _ { t } , \Delta V ( t )$ can be derived as

$$
\begin{array}{l} \Delta V (t) = V (t + 1) - V (t) \\ = x _ {t + 1} ^ {T} P x _ {t + 1} - x _ {t} ^ {T} P x _ {t} + \lambda^ {2} (\| U _ {*} x _ {t} \| ^ {2} - \| f (x _ {t}) \| ^ {2}) \\ = [ G x _ {t} + F f (x _ {t}) ] ^ {T} P [ G x _ {t} + F f (x _ {t}) ] \\ - x _ {t} ^ {T} P x _ {t} + \lambda^ {2} \left[ x _ {t} ^ {T} U _ {*} ^ {T} U _ {*} x _ {t} - f ^ {T} \left(x _ {t}\right) f \left(x _ {t}\right) \right] \tag {57} \\ = \left[ \begin{array}{c} x _ {t} \\ f \end{array} \right] ^ {T} \left[ \begin{array}{c c} G ^ {T} P G - P + \lambda^ {2} U _ {*} ^ {T} U _ {*} & G ^ {T} P F \\ F ^ {T} P G & F ^ {T} P F - \lambda^ {2} I \end{array} \right] \left[ \begin{array}{c} x \\ f \end{array} \right] \\ \end{array}
$$

If there exists a positive definite matrix ?? that satisfies

$$
Q _ {1} = \left[ \begin{array}{c c} G ^ {T} P G - P + \lambda^ {2} U _ {*} ^ {T} U _ {*} & G ^ {T} P F \\ F ^ {T} P G & F ^ {T} P F - \lambda^ {2} I \end{array} \right] <   0,
$$

then $\Delta V ( t ) = V ( t + 1 ) - V ( t ) < 0$ , li $\boldsymbol { 1 } _ { k \to \infty } \boldsymbol { V } ( t ) = 0$ , therefore, the system achieves asymptotic stability with sector-bounded $f ( x _ { t } )$ .

⇒ Part 2 - Robust performance:
