$$[ T ^ {2} L ^ {0} ] (b) = r (b) + \beta \sum_ {o \in \mathcal {O}} \mathbb {P} (o | b) \max _ {\alpha \in \mathcal {V} ^ {0}} [ \tau^ {2} (b, o) ] ^ {T} \alpha= r (b) + \beta \sum_ {o \in \mathcal {O}} \max _ {\alpha \in \mathcal {V} ^ {0}} \sum_ {s ^ {2} \in \mathcal {S} ^ {2}} b (s ^ {2}) \sum_ {s ^ {\prime} \in \mathcal {S}} \mathbb {P} (s ^ {\prime}, o | s ^ {2}) \alpha (s ^ {\prime})$$

where $\begin{array} { r } { r ( b ) : = \sum _ { ( s , a ) \in \mathcal { S } \times \mathcal { A } } b ( s , a ) r ( s , a ) } \end{array}$ .

Since we have no action in this stage, we can compute the new alpha vector $\boldsymbol { \alpha } ^ { b } \in \mathbb { R } ^ { S ^ { 2 } }$ in the same way as in (2)-(4) (except that there’s no action), i.e. we compute

$$\alpha^ {b, o} := \underset {\alpha \in \mathcal {V} ^ {0}} {\arg \max} \sum_ {s ^ {2} \in \mathcal {S} ^ {2}} b (s ^ {2}) \sum_ {s ^ {\prime} \in \mathcal {S}} \mathbb {P} (s ^ {\prime}, o | s ^ {2}) \alpha (s ^ {\prime})\forall o \in \mathcal {O} \tag {13}\alpha^ {b} (s ^ {2}) := r (s ^ {2}) + \beta \sum_ {o \in \mathcal {O}} \sum_ {s ^ {\prime} \in \mathcal {S}} \mathbb {P} (s ^ {\prime}, o | s ^ {2}) \alpha^ {b, o} (s ^ {\prime})\forall s ^ {2} \in \mathcal {S} ^ {2} \tag {14}$$

The lower bound update algorithm is given in Algo. 4.

Function L.Update(b):
    // L stores three sets of $\alpha$ -vectors: $V^{\ell}, \ell = 0, 1, 2$ Set $\ell \in \{0, 1, 2\}$ to be such that $b \in \Delta(S^{\ell})$ ;
    if $\ell < 2$ then
    | Compute $\alpha^{b}$ with (8) – (12);
    else
    |Compute $\alpha^{b}$ with (13)(14);
    Add $\alpha^{b}$ to $V^{\ell}$ ;
    Prune dominated vectors in $V^{\ell}$ once in a while;

Algorithm 4: Lower Bound Update
