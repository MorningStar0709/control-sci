Proof. Consider the Lyapunov function (15), and the time interval when the regressor vector is not satisfying the finite excitation condition, an orthogonal matrix $\Phi _ { b }$ can not be created, inferring $\eta = \operatorname * { d e t } \left( \bar { \Phi } _ { m } \right) = 0 .$ , hence the time derivative of the Lyapunov function along the error trajectories (10) and (29), and using (5) is $\dot { V } \leq$ $- \lambda _ { \mathrm { m i n } } \left( \mathbf { Q } \right) \left\| \mathbf { e } \right\| ^ { 2 }$ , hence uniform stability follows from the Proposition 1. Now, after the time interval $t \ = \ t _ { q } ,$ the regressor vector satisfies the finite excitation condition, and we can create an orthogonal matrix $\Phi _ { b } .$ and $\Phi _ { m } = \mathbf { I }$ , inferring $\eta = 1$ , hence the time derivative of the Lyapunov function along the error trajectories (10) and (29), and using (5), we obtain

$$\dot {V} \leq - \lambda_ {\min} (\mathbf {Q}) \| \mathbf {e} \| ^ {2} - 2 | k _ {p} | ^ {2} \mathbf {b} ^ {T} \mathbf {b} \left\{\left\| \tilde {\mathbf {k}} _ {x} \right\| ^ {2} + \tilde {k} _ {r} ^ {2} + \left\| \tilde {\boldsymbol {\theta}} \right\| ^ {2} \right\},$$

in terms of combined error vector χ, we can write

$$\dot {V} \leq - \min \left\{\lambda_ {\min} (\mathbf {Q}), 2 | k _ {p} | ^ {2} \mathbf {b} ^ {T} \mathbf {b} \right\} \| \boldsymbol {\chi} \| ^ {2}, \tag {30}$$

the Lyapunov function satisfies, min $\{ \lambda _ { \operatorname* { m i n } } \left( \mathbf { P } \right) , | k _ { p } | \} \| x \| ^ { 2 }$ $\leq V \leq$ max $\{ \lambda _ { \operatorname* { m a x } } \left( \mathbf { P } \right) , \left| k _ { p } \right| \} \left\| x \right\| ^ { 2 }$ , rewriting (30),

$$\dot {V} \leq - \overline {{\kappa}} V, \quad \overline {{\kappa}} = \frac {\min \left\{\lambda_ {\min} (\mathbf {Q}) , 2 | k _ {p} | ^ {2} \mathbf {b} ^ {T} \mathbf {b} \right\}}{\max \left\{\lambda_ {\max} (\mathbf {P}) , | k _ {p} | \right\}}, \tag {31}$$

where $\overline { { \kappa } }$ is the convergence rate. The solution of (31) is

$$V (t) \leq e ^ {- \overline {{\kappa}} (t - t _ {q})} V (t _ {q}), \quad \forall t > t _ {q} \tag {32}$$

inferring that the origin of the closed-loop system is exponentially stable $\forall t > t _ { q }$ .
