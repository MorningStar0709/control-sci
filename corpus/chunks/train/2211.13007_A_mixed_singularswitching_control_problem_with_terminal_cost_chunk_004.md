where $X _ { 0 - } ^ { \xi , \varsigma } = x _ { 0 } \in \overline { { \mathcal { O } } } \subset \mathbb { R } ^ { d } , J _ { 0 - } ^ { \varsigma } = \ell _ { 0 } \in \mathbb { M } , I _ { 0 } = I _ { 0 } ^ { ( \ell _ { 0 } ) } = \iota _ { 0 } \in \mathbb { I } , \tau { : = } \left\{ t > 0 : ( X _ { t } ^ { \xi , \varsigma } , I _ { t } ) \notin \mathcal { O } \times \mathbb { I } \right\}$ and $\tilde { \tau } _ { i } { : = } \tau _ { i } \wedge \tau$ . The parameters $b _ { \iota } \colon = b ( \cdot , \iota ) : \overline { { \mathcal { O } } } \longrightarrow \mathbb { R } ^ { d }$ and $\sigma _ { \iota } : = \sigma ( \cdot , \iota ) : \overline { { \mathcal { O } } } \longrightarrow \mathbb { R } ^ { d } \times \mathbb { R } ^ { k }$ , with $\textit { \textbf { l } } \in$ M fixed, satisfy appropriate conditions to ensure the well-definiteness of the stochastic differential equation (SDE) (1.2); see Assumption (H4).

The control process $( \xi , \varsigma )$ is in $\mathcal { U } \times \mathcal { S }$ where the singular control $\xi = ( \mathtt { n } , \zeta )$ belongs to the class U of admissible controls that satisfy

$$
\left\{ \begin{array}{l} (\mathfrak {n} _ {t}, \zeta_ {t}) \in \mathbb {R} ^ {d} \times \mathbb {R} _ {+}, t \geq 0, \text {   such   that   } X _ {t} ^ {\xi , \varsigma} \in \mathcal {O} t \in [ 0, \tau), \\ (\mathfrak {n}, \zeta) \text {   is   adapted   to   the   filtration   } \mathbb {F}, \\ \zeta_ {0 -} = 0 \text {   and   } \zeta_ {t} \text {   is   non - decreasing   and   is   right   continuous } \\ \text { with   left   hand   limits }, t \geq 0, \text {   and   } | \mathfrak {n} _ {t} | = 1 \text {   d } \zeta_ {t} \text {-a.s. }, t \geq 0, \end{array} \right. \tag {1.3}
$$

and the switching control process $\varsigma : = ( \tau _ { i } , \ell _ { i } ) _ { i \geq 0 }$ belongs to the class $\boldsymbol { \mathcal { S } }$ of switching regime sequences that satisfy

$$
\left\{ \begin{array}{l} \varsigma \text {   is   a   sequence   of   } \mathbb {F} \text {-stopping   times   and   regimes   in   } \mathbb {M}, \text {   i.e., } \\ \varsigma = (\tau_ {i}, \ell_ {i}) _ {i \geq 0} \text {   is   such   that   } 0 = \tau_ {0} \leq \tau_ {1} <   \tau_ {2} <   \dots , \tau_ {i} \uparrow \infty \text {   as   } i \uparrow \infty \mathbb {P} \text {-a.s.}, \\ \text {   and   for   each   } i \geq 0, \ell_ {i} \text {   is   } \mathcal {F} _ {\tau_ {i}} \text {-measurable   valued   in   } \mathbb {M}. \end{array} \right. \tag {1.4}
$$
