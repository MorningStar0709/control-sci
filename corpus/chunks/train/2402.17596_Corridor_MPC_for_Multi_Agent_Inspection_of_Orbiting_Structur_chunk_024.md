Computing $L _ { w } ^ { l } ( \tilde { \mathbf { x } } , \Delta t )$ and $c _ { w } ^ { l } ( \tilde { \pmb { x } } , \Delta t )$ requires the online computation of $\Delta t _ { \mathcal { R } \left( \mathcal { C } _ { S , x } ( t ) \right) }$ . While an analytical definition is impractical, we propose to find a set $\Delta ^ { _ { t } } \bar { \mathcal { R } } ( \mathcal { C } _ { S , x } ( t ) ) \ \supset$ $\Delta t _ { \mathcal { R } \dot { ( } \dot { \mathcal { C } } _ { S , x } ( t ) \rangle }$ such that suitable constants $\begin{array} { r l } { \bar { L } _ { w } ^ { l } ( \Delta t ) } & { { } \ge } \end{array}$ $L _ { w } ^ { l } ( \tilde { { \pmb x } } , \Delta t ) , \ \bar { c } _ { w } ^ { l } ( \Delta t ) \ \ge \ c _ { w } ^ { l } ( \tilde { { \pmb x } } , \Delta t )$ can be computed only based on the current sampling time $\Delta t .$ . This result is achieved in two steps: i) we first derive a constant a¯ that upper bounds the maximum total acceleration that (2) is subject to; and ii) based on a¯ we compute the maximum position and velocity tracking errors that can be reached by (2) in a sampling interval $\left[ { k \Delta t } , ( k + 1 ) \Delta t \right]$ . Namely, we compute the maximum acceleration that (2) is subject as

$$
\max _ {(\boldsymbol {x}, t) \in \tilde {\mathbb {X}} \times \mathbb {I}} \| \delta \dot {\tilde {\boldsymbol {v}}} \| = \| \boldsymbol {f} _ {v} + \boldsymbol {u} + \boldsymbol {d} \| \leq \epsilon_ {f} + \epsilon_ {u} + \epsilon_ {d} = \bar {a} \tag {15}
$$
