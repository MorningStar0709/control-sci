where ϵ is given in Assumption 3. For each sample point ω, if $\widehat { \theta } _ { t } + a _ { t } d _ { t } P _ { t } \phi _ { t } \left( \psi _ { t } + w _ { t + 1 } \right) ^ { \top } \ \in \ \Theta , t \ \geq \ t _ { 0 } ,$ , then the claim holds with $\begin{array} { r l r } { t _ { 1 } } & { { } = } & { t _ { 0 } } \end{array}$ . Otherwise, there exists a finite time $t _ { 0 } ^ { \prime } \geq \ t _ { 0 }$ such that $\widehat { \theta } _ { t _ { 0 } ^ { \prime } } \ +$ $a _ { t _ { 0 } ^ { \prime } } d _ { t _ { 0 } ^ { \prime } } P _ { t _ { 0 } ^ { \prime } } \phi _ { t _ { 0 } ^ { \prime } } \left( \psi _ { t _ { 0 } ^ { \prime } } + w _ { t _ { 0 } ^ { \prime } + 1 } \right) ^ { \top } \mathrm { ~  ~ \nsubscript ~ { ~ \nsubscript ~ { ~  ~ } ~ } ~ } \ominus$ . By the definition of the projection, we have $\hat { \theta } _ { t _ { 0 } ^ { \prime } + 1 } \in \Theta _ { \epsilon }$ . Then, for all $t \geq t _ { 0 } ^ { \prime } +$ $1 , ( \mathrm { A . 5 } )$ ensures that $\begin{array} { r l } { \phantom { \frac { 1 } { 2 } } \hat { \theta } _ { t _ { 0 } ^ { \prime } } + \sum _ { \tau = t _ { 0 } ^ { \prime } } ^ { t } a _ { \tau } d _ { \tau } P _ { \tau } \phi _ { \tau } \left( \psi _ { \tau } + w _ { \tau + 1 } \right) ^ { \top } \in } \end{array}$ Θ. Thus, $\hat { \theta } _ { t } + a _ { t } d _ { t } P _ { t } \phi _ { t } \left( \psi _ { t } + w _ { t + 1 } \right) ^ { \top } \in \Theta , \forall t \geq t _ { 0 } ^ { \prime } + 1$ , and the claim again holds with $t _ { 1 } = t _ { 0 } ^ { \prime } + 1$ . Therefore, the claim holds. By the claim, the estimate satisfies $\begin{array} { r l r } { \hat { { \boldsymbol \theta } } _ { t + 1 } } & { = } & { \hat { { \boldsymbol \theta } } _ { t _ { 1 } } + \sum _ { \tau = t _ { 1 } } ^ { t } a _ { \tau } d _ { \tau } P _ { \tau } \phi _ { \tau } [ { \boldsymbol x } _ { \tau + 1 } - f ( \hat { { \boldsymbol \theta } } _ { \tau } ^ { \top } { \boldsymbol \phi } _ { \tau } ) ] ^ { \top } \mathrm { ~ = ~ } } \end{array}$ $\begin{array} { r } { \hat { \theta } _ { t _ { 1 } } + \sum _ { \tau = t _ { 1 } } ^ { t } a _ { \tau } d _ { \tau } P _ { \tau } \phi _ { \tau } \left( \psi _ { \tau } + w _ { \tau + 1 } \right) ^ { \top } , \forall t \ \geq \ t _ { 1 } } \end{array}$ . From this and $\left( \mathrm { { A . 4 } } \right)$ , it follows that property (i) holds.

We now prove property (ii). To begin, we show that

$$\underline {{d}} := \inf _ {t \geq 0} d _ {t} > 0, \text { and } \sup _ {t \geq 0} \overline {{g}} _ {t} < \infty , \text { a.s. } \tag {A.6}$$
