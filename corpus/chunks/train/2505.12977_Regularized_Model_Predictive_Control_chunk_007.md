# III. RE-MPC ALGORITHM

To address the first objective, we introduce a sequence of key lemmas that establish fundamental connections essential to solving our problem. We start by considering a general quadratic optimization problem

$$\underset {\eta} {\arg \min} \left\{\left(G \eta - h\right) ^ {\top} W (G \eta - h) \right\} \tag {6}$$

where $\boldsymbol { \eta } \in \mathbb { R } ^ { m }$ is an unknown vector, $\boldsymbol { h } \in \mathbb { R } ^ { n }$ and $G \in \mathbb { R } ^ { n \times m }$ are known vector and matrix, respectively, and $W \in \mathbb { R } ^ { n \times n }$ is a known PD weighting matrix.

Lemma $\textit { l } ( I I 9 I )$ : Let the matrix G have full column rank, i.e., rank(G) = m, and define $\Theta _ { 1 } = G ^ { \mathsf { T } } W G$ and

$$
\Theta_ {2} = \left[ \begin{array}{c c} W ^ {- 1} & G \\ G ^ {\mathsf {T}} & 0 \end{array} \right]. \tag {7}
$$

Then, $\Theta _ { 1 }$ and $\Theta _ { 2 }$ are invertible matrices. This guarantees that the optimization problem (6) has a unique optimal solution, given $\mathsf { b y } ^ { 1 }$

$$
\hat {\eta} = \left[ \begin{array}{l} 0 \\ I \end{array} \right] ^ {\mathsf {T}} \left[ \begin{array}{c c} W ^ {- 1} & G \\ G ^ {\mathsf {T}} & 0 \end{array} \right] ^ {- 1} \left[ \begin{array}{l} h \\ 0 \end{array} \right] = (G ^ {\mathsf {T}} W G) ^ {- 1} G ^ {\mathsf {T}} W h. \tag {8}
$$

1In the case that $W \succeq 0 ,$ the solution is $\hat { \eta } = ( G ^ { \mathsf { T } } W G ) ^ { \dagger } G ^ { \mathsf { T } } W h$ . Thus, for a PSD matrix $Q \succeq 0 ,$ the pseudoinverse is used, but its definiteness does not affect the derivation of the algorithm, as will be evident from the results. For clarity, we assume $Q \succ 0$ to use standard inverse notation.

Lemma 2 ([20]): Consider a constrained version of the problem (6), where we introduce the equality constraint $F \eta =$ $\varphi ,$ with $\boldsymbol { F } \in \mathbb { R } ^ { k \times m }$ and $\varphi \in \mathbb { R } ^ { k }$ . According to [21], a solution to this problem is unique if and only if rank $( F ) = k$ and ran $\operatorname { k } ( \operatorname { c o l } \{ G , F \} ) = m$ , or equivalently, $\mathcal { N } ( G ) \cap \mathcal { N } ( F ) = \{ 0 \}$ . Using the weighting method for solving LSE, we enforce the constraint by introducing a large penalty parameter $\mu .$ . This reformulates the problem as an unconstrained least squares problem and leads to the following PLS-based formulation:
