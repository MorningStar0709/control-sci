# IV. SELF-TRIGGERED CONTROL

As highlighted in Section I, STS does not rely on extra hardware to continuously monitor the system states [35], but rather predicts the next sampling instant based on a local function and previous data. In this section, we study data-driven STS with unknown matrices $A$ and $B ,$ as depicted in Fig. 4. The challenge here is to predict the next transmission instant using the already trameasurements (i.e., $\{ x ( T ) \} _ { T = 0 } ^ { \rho } , \{ u ( T ) \} _ { T = 0 } ^ { \rho - 1 }$ d historical noisyas in Section II) without explicit knowledge of the system matrices $A$ and B. To this end, we begin by designing a model-based STS.
