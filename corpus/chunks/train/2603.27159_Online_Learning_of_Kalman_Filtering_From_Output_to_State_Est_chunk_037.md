$$
\begin{array}{l} \mathsf {R} _ {\mathbf {x}} (T) = \sum_ {t = 0} ^ {T - 1} f _ {t} (M _ {t}) - \sum_ {t = 0} ^ {T - 1} \left\| x _ {t} - \hat {x} _ {t} ^ {\mathrm{KF}} \right\| \\ = \underbrace {\sum_ {t = 0} ^ {T - 1} \left(f _ {t} (M _ {t}) - f _ {t} (M ^ {\star})\right)} _ {\text { online   optimization   regret }} + \underbrace {\sum_ {t = 0} ^ {T - 1} \left(f _ {t} (M ^ {\star}) - \left\| x _ {t} - \hat {x} _ {t} ^ {\mathrm{KF}} \right\| ^ {2}\right)} _ {\text { truncation   \&   steady - state   regret }}, \tag {42} \\ \end{array}
$$

where $f _ { t } ( M ^ { \star } ) = \left\| x _ { t } - \hat { x } _ { t } ^ { \mathrm { T K } } \right\| ^ { 2 }$ and the comparator point $M ^ { \star } = [ M _ { s } ^ { \star } ] _ { s \in [ h ] }$ with $M _ { s } ^ { \star }$ defined in (8).

Online optimization regret. Similarly to Definition 4.1, we introduce the following auxiliary function.

Definition 5.1. Let $\{ \mathcal { F } _ { t } \} _ { t \ge 0 }$ be a filtration with $\mathcal { F } _ { t } \triangleq \sigma ( w _ { 0 } , \ldots , w _ { t - 1 } , v _ { 0 } , \ldots , v _ { t - 1 } )$ for $t \geq 1$ and $\mathcal { F } _ { 0 } \overset { \triangle } { = } \emptyset$ . For any $N \in \mathbb { R } ^ { n p h }$ and some $h \in \mathbb { Z } _ { > 0 }$ , define $f _ { t ; h } ( M ) = \mathbb { E } [ f _ { t } ( M ) | \mathcal { F } _ { t - h } ]$ for all $t \geq h$ , where the expectation $\mathbb { E } [ \cdot ]$ is taken with respect to $\{ w _ { t } \} _ { t \ge 0 }$ and $\{ v _ { t } \} _ { t \geq 0 }$ .

Lemma 5.1. For any $t \geq h , f _ { t ; h } ( \cdot )$ is α0-strongly convex. In addition, under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ $f _ { t ; h } ( \cdot )$ is lx-Lipschitz.

Similarly to Lemma 4.2, we can leverage the $\alpha _ { 0 } \cdot$ -strong convexity of $f _ { t ; h } ( \cdot )$ and prove the following result.

Lemma 5.2. For any $t \geq h , \ l e t \ \varepsilon _ { t } ^ { s } = \nabla f _ { t } ( M _ { t } ) - \nabla f _ { t ; h } ( M _ { t } ) \ a n d \ \tilde { \varepsilon } _ { t } ^ { s } = \nabla \tilde { f } _ { t } ( M _ { t } ) - \nabla f _ { t } ( M _ { t } )$ . It holds that
