# Offline calculation:

1. Build the lookup table to map all the initial states $x _ { 0 } \in \mathcal X$ to the corresponding $Y ^ { * }$ and ${ \tilde { Y } } ^ { * }$ .   
2. Compute the initial feasible solutions $Y ^ { * }$ and ${ \tilde { Y } } ^ { * } ,$ , by reading the lookup table given the first initial state $x _ { 0 } ,$ , and fix the value of $Y ^ { * }$ and ${ \tilde { Y } } ^ { * }$ for the subsequent online calculation Online calculation:   
1. Read the current state $x _ { k }$ and set it as initial state $x _ { 0 } .$ Then based on $x _ { 0 } ,$ , extract the value of $Y ^ { * }$ and ${ \tilde { Y } } ^ { * }$ from the offline calculation.   
2. Compute the triple $( K _ { 0 } , K , \upsilon )$ through the two LMI procedures outlined in (25) and (26) and apply the first input of the control sequence shown in (11).   
3. Return to step 1.
