# A. Algorithm for SMFE computation: Backward Recursion

In this section, we define an equilibrium generating function $\boldsymbol { \theta } = ( \theta _ { t } ^ { l , i } , \theta _ { t } ^ { m , j } , \theta _ { t } ^ { f } ) _ { i \in \{ 1 . . . K \} , j \in \{ 1 . . . M , \} , t \in [ T ] }$ , where $\theta _ { t } ^ { L , i } : \mathcal { P } ( \mathcal { X } ^ { f } )  \{ \mathcal { X } ^ { l , i }  \mathcal { P } ( \mathcal { A } ^ { l , i } ) \} , \theta _ { t } ^ { m , j } : \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { m , j }  \{ \mathcal { X } ^ { m , j }  \mathcal { P } ( \mathcal { A } ^ { m , j } ) \} , \theta _ { t } ^ { f } : \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { f }  \{ \mathcal { X } ^ { f }  \mathcal { X } \} .$ $\mathcal { P } ( \mathcal { A } ^ { f } ) \}$ and a sequence of functions $( V _ { t } ^ { l , i } , V _ { t } ^ { m , j } , V _ { t } ^ { f } ) _ { \{ i = 1 \ldots N , j = 1 \ldots , M , 1 , 2 , t = 1 \ldots T + 1 \} }$ , where $V _ { t } ^ { l , i } : \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { l , i } $ $\mathbb { R } , V _ { t } ^ { m , j } : \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { m , j }  \mathbb { R } , V _ { t } ^ { f } : \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { f }  \mathbb { R } .$ in a backward recursive way, as follows.

1. Initialize $\forall z _ { T + 1 } \in \mathcal { P } ( \mathcal { X } ^ { f } ) , i = 1 \dots K , j = 1 \dots M , x _ { T + 1 } ^ { f } \in \mathcal { X } ^ { f } .$

$$V _ {T + 1} ^ {l, i} \left(z _ {T + 1}\right) \triangleq 0. \tag {44}V _ {T + 1} ^ {m, j} \left(z _ {T + 1}\right) \triangleq 0. \tag {45}V _ {T + 1} ^ {f} \left(z _ {T + 1}, x _ {T + 1} ^ {f}\right) \triangleq 0 \tag {46}$$

2. For $t = T , T - 1 , \dots 1 , \forall z _ { t } \in \mathcal { P } ( \mathcal { X } ^ { f } )$ , Set $\tilde { \gamma } _ { t } = \theta _ { t } [ z _ { t } ]$ , where $\tilde { \gamma } _ { t } = ( \tilde { \gamma } _ { t } ^ { l } , \tilde { \gamma } _ { t } ^ { m } , \tilde { \gamma } _ { t } ^ { f } )$ is the solution of the following fixed-point equation. For a given $z _ { t } , \gamma _ { t } ^ { l } , \gamma _ { t } ^ { m }$ , define $\bar { B R } _ { t } ^ { f } ( z _ { t } , \gamma _ { t } ^ { l } , \gamma _ { t } ^ { m } )$ as follows,

$$\bar {B R} _ {t} ^ {f} (z _ {t}, \gamma_ {t} ^ {l}, \gamma_ {t} ^ {m}) := \left\{\tilde {\gamma} _ {t} ^ {f}: \forall x _ {t} ^ {f} \in \mathcal {X} ^ {f}, \tilde {\gamma} _ {t} ^ {f} (\cdot | x _ {t} ^ {f}) \in \arg \max _ {\gamma_ {t} ^ {m, j} (\cdot | x _ {t} ^ {m, j})} \right.\mathbb {E} ^ {\gamma_ {t} ^ {m, j} (\cdot | x _ {t} ^ {m, j}) \gamma_ {t} ^ {l}, z _ {t}} \big \{R _ {t} ^ {f} (z _ {t}, X _ {t} ^ {f}, A _ {t}) + \delta V _ {t + 1} ^ {f} (\phi (z _ {t}, \gamma_ {t} ^ {l}, \gamma_ {t} ^ {m}, \tilde {\gamma} _ {t} ^ {f}), X _ {t + 1} ^ {f}) \big | z _ {t}, x _ {t} ^ {f} \big \} \big \}, \tag {47a}$$
