$$\underline {{v}} (x, i) \leq v (x, i; \theta), \text { for any } (x, i; \theta) \in \mathcal {O} \times \mathbb {R} _ {+}. \tag {4.13}$$

Then, for any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ , we have

$$\alpha_ {i} (\theta) = \sup \{x \in \mathcal {I}: v (x, i; \theta) \geq k _ {1} \} \geq \sup \{x \in \mathcal {I}: \underline {{v}} (x, i) \geq k _ {1} \} = \underline {{\alpha}} _ {i}. \tag {4.14}$$

Analogously, we find

$$\beta_ {i} (\theta) \geq \underline {{\beta}} _ {i}, \text { for any } (i, \theta) \in \mathbb {Y} \times \mathbb {R} _ {+}. \tag {4.15}$$

We then define

$$\underline {{\theta}} := F \biggl (\sum_ {i = 1} ^ {d} \int_ {\mathcal {I}} f (x) \underline {{\nu}} (d x, i) \biggr) \tag {4.16}$$

where $\underline { { \boldsymbol \nu } }$ is the unique stationary distribution of the process $( \underline { { X } } _ { t } , Y _ { t } ) _ { t \geq 0 }$ and $\underline { { \boldsymbol { X } } }$ is the solution to the $\mathbf { S P } ( ( \underline { { \alpha } } _ { i } , \underline { { \beta } } _ { i } ) _ { i \in \mathbb { Y } } ; x , i )$ . Then, by comparison principle (see Proposition B.2), since $f$ is increasing, we obtain

$$\mathbb {E} _ {(x, i)} \big [ f (\underline {{X}} _ {t}) \big ] \leq \mathbb {E} _ {(x, i)} \big [ f (X _ {t} ^ {\xi^ {*} (\theta)}) \big ], \text { for any } t \geq 0. \tag {4.17}$$

Hence, by Proposition 4.2, we can employ the ergodic theorem (see Theorem 4.4 in [53]) in order to deduce that

$$\langle f, \underline {{\nu}} \rangle = \lim _ {t \to \infty} \frac {1}{t} \int_ {0} ^ {t} \mathbb {E} \big [ f (\underline {{X}} _ {t}) \big ] d t \leq \lim _ {t \to \infty} \frac {1}{t} \int_ {0} ^ {t} \mathbb {E} \big [ f (X _ {t} ^ {\xi^ {*} (\theta)}) \big ] d t = \langle f, \nu^ {\theta} \rangle ,$$

which in turn, by monotonicity of F , leads to

$$\underline {{\theta}} \leq \mathcal {T} \theta . \tag {4.18}$$

To find an upper bound for ${ \mathcal { T } } \theta .$ , we proceed as follows. Since, $X _ { t } ^ { \xi ^ { * } ( \theta ) } \in [ \alpha _ { Y _ { t } } ( \theta ) , \beta _ { Y _ { t } } ( \theta ) ]$ , for any $t \geq 0 , \mathbb { P } { \cdot } { \mathrm { a . s . } }$ . for any $\theta \in \mathbb { R } _ { + }$ , thanks to the monotonicity of $F$ and $f$ (see Condition (1) Assumption 2.3), we have

$$\mathcal {T} \theta = F \left(\sum_ {i = 1} ^ {d} \int_ {\mathcal {I}} f (x) \nu^ {\theta} (d x, i)\right) \leq F \left(\sum_ {i = 1} ^ {d} p (i) f (\beta_ {i} (\theta))\right) \leq F \left(\sum_ {i = 1} ^ {d} p (i) f (\beta_ {i} (\underline {{\theta}}))\right),$$
