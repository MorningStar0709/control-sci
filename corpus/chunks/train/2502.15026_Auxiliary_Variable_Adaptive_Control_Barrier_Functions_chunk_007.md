where $\alpha _ { i } ( \cdot ) , i \in \{ 1 , . . . , m \}$ denotes a $( m \mathrm { ~ - ~ } i ) ^ { t h }$ order differentiable class κ function. A sequence of sets $\mathcal { C } _ { i }$ are defined based on (3) as

$$\mathcal {C} _ {i} := \{\boldsymbol {x} \in \mathbb {R} ^ {n}: \psi_ {i} (\boldsymbol {x}) \geq 0 \}, i \in \{0,..., m - 1 \}. \tag {4}$$

Definition 4 (HOCBF [14]). Let $\psi _ { i } ( { \pmb x } ) , \ i \in \{ 1 , . . . , m \}$ be defined by (3) and $\mathcal { C } _ { i } , \ i \in \{ 0 , . . . , m - 1 \}$ be defined by (4). A function $b : \mathbb { R } ^ { n }  \mathbb { R }$ is a High Order Control Barrier Function (HOCBF) with relative degree m for system (1) if there exist $( m - i ) ^ { \mathrm { t h } }$ order differentiable class κ functions $\alpha _ { i } , i \in \{ 1 , . . . , m \}$ such that

$$
\begin{array}{l} \sup _ {\boldsymbol {u} \in \mathcal {U}} [ L _ {f} ^ {m} b (\boldsymbol {x}) + L _ {g} L _ {f} ^ {m - 1} b (\boldsymbol {x}) \boldsymbol {u} + O (b (\boldsymbol {x})) + \tag {5} \\ \left. \alpha_ {m} \left(\psi_ {m - 1} (\boldsymbol {x})\right) \right] \geq 0, \\ \end{array}
$$

∀x $\in \mathrm { ~ \mathcal { C } _ { 0 } \cap , . . . , \cap \mathcal { C } _ { m - 1 } }$ , where $\begin{array} { r } { { \cal O } ( \cdot ) ~ = ~ \sum _ { i = 1 } ^ { m - 1 } L _ { f } ^ { i } ( \alpha _ { m - 1 } \circ } \end{array}$ $\psi _ { m - i - 1 } ) ( \pmb { x } ) ; L _ { f } ^ { m }$ denotes the $m ^ { \mathrm { t h } }$ Lie derivative along f and $L _ { g }$ denotes the matrix of Lie derivatives along the columns of $g . \ \psi _ { i } ( { \pmb x } ) \ge 0$ is referred to as the $i ^ { \mathrm { { t h } } }$ order HOCBF constraint. We assume that $L _ { g } L _ { f } ^ { m - 1 } b ( { \pmb x } ) { \pmb u } \neq 0$ on the boundary of set $\mathcal { C } _ { 0 } \cap , . . . , \cap \mathcal { C } _ { m - 1 }$ .

Theorem 1 (Safety Guarantee [14]). Given a HOCBF $b ( { \pmb x } )$ from Def. 4 with corresponding sets $\mathcal { C } _ { 0 } , \ldots , \mathcal { C } _ { m - 1 }$ defined by $( 4 ) , i f x ( 0 ) \in \mathcal { C } _ { 0 } \cap \cdots \cap \mathcal { C } _ { m - 1 }$ , then any Lipschitz controller u that satisfies the constraint in (5), $\forall t \geq 0$ renders $\mathcal { C } _ { 0 } \cap \cdots \cap$ $\mathcal { C } _ { m - 1 }$ forward invariant for system (1), $i . e . , \pmb { x } \in \mathcal { C } _ { 0 } \cap \cdots \cap$ ${ \mathcal { C } } _ { m - 1 } , \forall t \geq 0$ .
