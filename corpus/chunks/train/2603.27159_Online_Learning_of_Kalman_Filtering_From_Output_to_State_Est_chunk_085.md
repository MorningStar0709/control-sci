Our goal next is to lower bound (D.4) by a positive number. Using the notation $R _ { t } ^ { \mathbf { s ( i ) } }$ introduced in (D.4), our strategy is to first show that $( R _ { t } ^ { \mathbf { s } ( 2 ) } - R _ { t } ^ { \mathbf { s } ( 1 ) } ) + ( R _ { t } ^ { \mathbf { s } ( 3 ) } - R _ { t } ^ { \mathbf { s } ( 1 ) } )$ is lower bounded by a positive number. If we can also show that $R _ { t } ^ { \mathbf { s } ( 1 ) } \geq 0$ , then we obtain a positive lower bound for (D.4). To this end, for any $i \in \{ 2 , 3 \}$ and any $t \geq 0$ , we first write

$$
\begin{array}{l} R _ {t} ^ {\mathbf {s} (\mathbf {i})} - R _ {t} ^ {\mathbf {s} (1)} = \mathbb {E} _ {w, v} \Big [ \mathbb {E} _ {w, v} \Big [ (\hat {x} _ {t} ^ {\mathbf {s} (\mathbf {i})} - x _ {t} ^ {\mathbf {s} (\mathbf {i})}) ^ {2} - (\hat {x} _ {t} ^ {\mathbf {s} (1)} - x _ {t} ^ {\mathbf {s} (1)}) ^ {2} \Big ] \big | y _ {0}, \ldots , y _ {t - 1} \Big ] \\ + \mathbb {E} _ {w, v} \Big [ (\hat {x} _ {t} ^ {\mathrm{KF}, \mathbf {s} (1)} - x _ {t} ^ {\mathbf {s} (1)}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}, \mathbf {s} (\mathbf {i})} - x _ {t} ^ {\mathbf {s} (\mathbf {i})}) ^ {2} \Big ] \\ = \mathbb {E} _ {w, v} \Big [ \mathbb {E} _ {w, v} \Big [ (\hat {x} _ {t} - x _ {t} ^ {\mathbf {s (i)}}) ^ {2} - (\hat {x} _ {t} - x _ {t} ^ {\mathbf {s (1)}}) ^ {2} \Big ] \Big | y _ {0}, \ldots , y _ {t - 1} \Big ] \\ + \mathbb {E} _ {w, v} \Big [ (\hat {x} _ {t} ^ {\mathrm{KF}, \mathbf {s} (1)} - x _ {t} ^ {\mathbf {s} (1)}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}, \mathbf {s} (\mathbf {i})} - x _ {t} ^ {\mathbf {s} (\mathbf {i})}) ^ {2} \Big ], \qquad (\mathrm{D.5}) \\ \end{array}
$$

where the first equality uses the total expectation law, and the second equality uses the fact that the state estimate $\hat { x } _ { t }$ returned by the deterministic algorithm are determined given $y _ { 0 } , \ldots , y _ { t - 1 }$ , which allows us to use the same symbol $\hat { x } _ { t } ~ ( \mathrm { r e s p . } , \hat { x } _ { t } ^ { \mathrm { K F } } )$ for the state estimate returned by the deterministic algorithm corresponding to the systems ${ \mathsf { s } } ( 1 )$ and ${ \mathsf { s } } ( 2 )$ . The following results provide lower bounds for the two terms on the right-hand side of (D.5).

Lemma D.2. For any $i \in \{ 2 , 3 \}$ and any $t \geq 1$ , it holds that
