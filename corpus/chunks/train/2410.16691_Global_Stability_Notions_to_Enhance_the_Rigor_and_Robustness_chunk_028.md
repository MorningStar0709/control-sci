The proof is made by contradiction. Suppose that there exists $i = 1 , . . . , m$ and $T \in [ 0 , t _ { \operatorname* { m a x } } )$ such that $H _ { i } ( x ( T ) ) > \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R \left( \left\| d \right\| _ { \infty } \right) \right)$ . Since $H _ { i } ( x ( 0 ) ) \leq \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R \Big ( \big \| d \big \| _ { \infty } \Big ) \right)$ , by continuity of the mapping $t \mapsto H _ { i } ( x ( t ) )$ the set $\left\{ t \in [ 0 , T ] \colon H _ { i } ( x ( t ) ) = \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R { \bigl ( } \left\| d \right\| _ { \infty } \right) { \bigr ) } \right\}$ is non-empty. Define $\tau = \operatorname* { s u p } \{  t \in [ 0 , T ] \colon H _ { i } ( x ( t ) ) = \operatorname* { m a x } ( H _ { i } ( x _ { 0 } ) , R ( \| d \| _ { \infty } ) ) \}$ and notice that continuity of the mapping $t \mapsto H _ { i } ( x ( t ) )$ implies that $H _ { i } ( x ( \tau ) ) = \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R \left( \left\| d \right\| _ { \infty } \right) \right)$ and $\tau < T$ . Definition of guarantees that $H _ { i } ( x ( t ) ) \geq \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R \Big ( \big \| d \big \| _ { \infty } \Big ) \right)$ for all $t \in \left[ \tau , T \right]$ . Thus, we obtain from (53) and the fact that $R \in C ^ { 0 } \left( \mathbb { R } _ { + } ; \mathbb { R } _ { + } \right)$ is a non-decreasing function for $t \in \left[ \tau , T \right]$ a.e.:

$$\frac {d}{d t} \left(H _ {i} (x (t))\right) \leq 0 \tag {55}$$

Inequality (55) implies that $H _ { i } ( x ( T ) ) \leq H _ { i } ( x ( \tau ) ) = \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R \left( \left\| d \right\| _ { \infty } \right) \right)$ , which contradicts the estimate $H _ { i } ( x ( T ) ) > \operatorname* { m a x } \left( H _ { i } ( x _ { 0 } ) , R \left( \left\| d \right\| _ { \infty } \right) \right)$ .

The fact that $H ( x ) = \operatorname* { m a x } _ { i = 1 , \ldots , m } \left( H _ { i } ( x ) \right)$ and (54) implies that the following estimate holds for all $t \in [ 0 , t _ { \operatorname* { m a x } } )$ :

$$H (x (t)) \leq \max \left(H (x _ {0}), R \left(\left\| d \right\| _ {\infty}\right)\right) \tag {56}$$

Since $H \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ is a radially unbounded function we can guarantee from (56) that $t _ { \mathrm { m a x } } = + \infty$ .
