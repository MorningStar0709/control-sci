$$
\begin{array}{l} \mathbb {T} _ {\Sigma_ {2}} \triangleq \left\{\left(\{u (k) \} _ {k \in \mathbb {Z} _ {0}}, \{y (k) \} _ {k \in \mathbb {Z} _ {0}}, \{x (k) \} _ {k \in \mathbb {Z} _ {0}}\right): \right. \\ \exists \{\eta (k) \} _ {k \in \mathbb {Z} _ {0}} \text {   s.t.   } \left(\{u (k) \} _ {k \in \mathbb {Z} _ {0}}, \{x (k) \} _ {k \in \mathbb {Z} _ {0}}, \right. \\ \left. \left\{y (k) \right\} _ {k \in \mathbb {Z} _ {0}}, \left\{\eta (k) \right\} _ {k \in \mathbb {Z} _ {0}}\right) \text {satisfies} (3 4) \Bigg \}. \\ \end{array}
$$

Lemma 2 (Trajectory equivalence with unknown inputs) Under Assumption 3, the set of all trajectories generated by system (34) exactly coincides with the set of trajectories compatible with the historical data collected under unknown inputs $( i . e . , \mathbb { T } _ { c } ( u _ { d } , y _ { d } , x _ { d } )$ as per Definition $4 ) .$

$$\mathbb {T} _ {\Sigma_ {2}} = \mathbb {T} _ {c} (u _ {d}, y _ {d}, x _ {d}). \tag {35}$$

Proof: Let $( \{ u ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } , \{ y ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } , \{ x ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } ) \in \mathbb { T } _ { \underline { { c } } . }$ By Definition 4, there exists a coefficient vector $g _ { 2 } \in \mathbb { R } ^ { T }$ such that

$$
\begin{array}{l} \operatorname{col} \{x (k), x (k + 1), u (k), y (k), y (k + 1) \} \\ = \mathbf {c o l} \{X _ {p}, X _ {f}, U _ {p}, Y _ {p}, Y _ {f} \} g _ {2} \\ \end{array}
$$

Since the historical data is inherently generated by the descriptor system (34) under certain unknown inputs, it satisfies the system dynamics over the collected horizon:

$$
\left[ X _ {p} ^ {\top}, X _ {f} ^ {\top}, U _ {p} ^ {\top}, Y _ {p} ^ {\top}, Y _ {f} ^ {\top} \right] ^ {\top} = \mathcal {H} \bar {\mathcal {M}} \left[ \begin{array}{c} H _ {1} \left(z _ {1 d, [ 0, T - 1 ]}\right) \\ H _ {s + 1} \left(\tilde {u} _ {d, [ 0, T + s - 1 ]}\right) \end{array} \right], \tag {37}
$$

where $\bar { \mathcal { M } }$ is obtained from M in the proof of Lemma 1 by changing $B _ { 2 }$ to the augmented input matrix $[ B _ { 2 } , F _ { 2 } ]$ and H is defined after (17).

Define a state-input sequence $( z _ { 1 } ( k ) , u ( k ) , \eta ( k ) , \cdot \cdot \cdot , u ( k +$ $s ) , \eta ( k + s ) )$ as the linear combination of the historical Hankel matrices using the same vector g2:

$$
\mathbf {c o l} \{z _ {1} (k), u (k), \dots , u (k + s), \eta (k + s) \} = \left[ \begin{array}{c} H _ {1} (z _ {1 d, [ 0, T - 1 ]}) \\ H _ {s + 1} (\tilde {u} _ {d, [ 0, T + s - 1 ]}) \end{array} \right] g _ {2}, \tag {38}
$$

Substituting (37) and (38) into (36), we obtain
