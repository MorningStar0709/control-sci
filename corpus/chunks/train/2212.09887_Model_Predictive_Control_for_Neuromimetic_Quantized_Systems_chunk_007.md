# 3.1 Integer Least Squares Problem Formulation

To apply the sphere decoding algorithm, the problem (4) needs to be rewritten in the extensive form and constructed as an integer least squares problem. Define $X _ { k } =$

$$
\left( \begin{array}{c} x _ {0 | k} \\ x _ {1 | k} \\ \vdots \\ x _ {N | k} \end{array} \right) \in \mathbb {R} ^ {(N + 1) n},   U _ {k} = \left( \begin{array}{c} u _ {0 | k} \\ u _ {1 | k} \\ \vdots \\ u _ {N - 1 | k} \end{array} \right) \in \{- 1, 0, 1 \} ^ {(N - 1) m}
$$

$\begin{array} { r l r } & { } & { \mathrm { a n d } \ R _ { k } = \left( \begin{array} { c } { x _ { r e f } ( 0 , k ) } \\ { x _ { r e f } ( 1 , k ) } \\ { \vdots } \\ { x _ { r e f } ( N , k ) } \end{array} \right) \in \mathbb { R } ^ { ( N + 1 ) n } \mathrm { . ~ L e t ~ } \tilde { A } = \left( \begin{array} { c } { I } \\ { A _ { q } } \\ { \vdots } \\ { A _ { q } ^ { N } } \end{array} \right) , } \end{array}$

$$
\tilde {B} = \left( \begin{array}{c c c c} 0 & 0 & \dots & 0 \\ B _ {q} & 0 & \dots & 0 \\ A _ {q} B _ {q} & B _ {q} & \dots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ A _ {q} ^ {N - 1} B _ {q} & A _ {q} ^ {N - 2} B _ {q} & \dots & B _ {q} \end{array} \right),   \tilde {Q} = \left( \begin{array}{c c c c} Q & 0 & \dots & 0 \\ 0 & Q & \dots & 0 \\ \vdots & \vdots & \ddots & 0 \\ 0 & 0 & 0 & P \end{array} \right)
$$

an $\begin{array} { r } { \mathrm { i } \tilde { R } = \left( \begin{array} { l l l l } { R } & { 0 } & { \cdots } & { 0 } \\ { 0 } & { R } & { \cdots } & { 0 } \\ { \vdots } & { \vdots } & { \ddots } & { 0 } \\ { 0 } & { 0 } & { 0 } & { R } \end{array} \right) \cdot \mathrm { T h e n } , \tilde { H } = \tilde { B } ^ { T } \tilde { Q } \tilde { B } + \tilde { R } \mathrm { i s ~ p o s i t i v e } } \end{array}$

definite and symmetric. The reconstructed problem (4) is

$$\min _ {U _ {k}} \left. \left| \left| U _ {k} + \tilde {H} ^ {- 1} \left(\tilde {A} x _ {q} (k) - R _ {k}\right) \right| \right| _ {\tilde {H}} ^ {2} \right. \tag {6}s. t. \quad u _ {n | k} \in \{- 1, 0, 1 \} ^ {m}, \quad \forall n = 0, 1, \dots , N - 1.$$

Detailed transformation steps can be found in the appendix. It is observed that the closed form solution for the unconstrained problem (6) is $U _ { u n c o n K } ~ = ~ - \tilde { H } ^ { - 1 }$ $( \tilde { A } x _ { q } ( k ) - R _ { k } )$ . Since matrix $\tilde { H }$ is symmetric positive definite, there exists an invertible and lower triangular matrix $W ^ { ' } \in \mathbb { R } ^ { N m \times N m }$ by Cholesky decomposition to factor $\tilde { H } = W ^ { T } W$ and $\tilde { H } ^ { - 1 } = W ^ { - 1 } \Breve { W } ^ { - T }$ . Denote $\bar { U } _ { u n c o n K } =$ $W U _ { u n c o n K }$ . Then the optimization problem (6) has an integer least square objective function
