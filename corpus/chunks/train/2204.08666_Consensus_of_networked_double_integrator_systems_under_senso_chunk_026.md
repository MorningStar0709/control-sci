We now derive a necessary condition to be able to conclude collective Initial Excitation (C-IE) on the set of regressors, $Y _ { i } ( { \dot { q } } , { \ddot { q } } , t )$ . Since, $\boldsymbol { Y } = [ Y _ { 1 } , Y _ { 2 } , . . , Y _ { n } ] ^ { \intercal }$ we can write,

$$
\sum_ {i = 1} ^ {n} Y _ {i} ^ {\top} Y _ {i} (\dot {q} _ {i}, \ddot {q} _ {i}, t) = Y ^ {\top} Y (\dot {q}, \ddot {q}, t) = \left( \begin{array}{c c} A (\dot {q}, \ddot {q}, t) & B (\dot {q}, \ddot {q}, t) \\ B ^ {\top} (\dot {q}, \ddot {q}, t) & C (\dot {q}, t) \end{array} \right)
$$

$\begin{array} { r } { \mathrm { w h e r e } , A ( \dot { q } , \ddot { q } , t ) = \left( \begin{array} { c c } { \ddot { q } ^ { \top } \ddot { q } } & { k ( t ) \ddot { q } ^ { \top } \dot { q } } \\ { k ( t ) \dot { q } ^ { \top } \ddot { q } } & { k ^ { 2 } ( t ) \lVert \dot { q } \rVert ^ { 2 } } \end{array} \right) , } \end{array}$

$$B (\dot {q}, \ddot {q}, t) = \binom{\frac {k (t)}{2} \ddot {q} ^ {\top} \bar {\mathcal {Q}} (t)}{\frac {k ^ {2} (t)}{2} \dot {q} ^ {\top} \bar {\mathcal {Q}} (t)},C (t) = \frac {k ^ {2} (t)}{4} \bar {\mathcal {Q}} (t) ^ {\top} \bar {\mathcal {Q}} (t).$$

All arguments in the preceding equation have been deliberately removed for the sake of brevity and clarity. The functions $A ( { \dot { q } } , { \ddot { q } } , t )$ and $C ( t )$ as defined above, map into positive semidefinite matrices by definition. We now state the result pertinent to this section.

Lemma V.1. $I f Y _ { i } ( \dot { q } _ { i } , \ddot { q } _ { i } , t ) ^ { : }$ s are collectively initially exciting as per Definition 7, then the matrix functions, $\begin{array} { r } { \int _ { 0 } ^ { \bar { T } } A ( \dot { q } , \ddot { q } , t ) } \end{array}$ dt and $\int _ { 0 } ^ { \bar { T } } C ( t )$ dt are positive definite. Further, if the graph, G(t) is jointly (δ, T )-connected for some $\delta > 0 ,$ then it is also jointly non-bipartite over [0, max $\{ T , { \bar { T } } \}$ .
