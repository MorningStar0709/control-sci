Under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ , we first provide the following bound on the norm of the state $x _ { t }$ and output yt of system (1); the proof can be found in Appendix B.

Lemma 3.2. Under the event √ $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ and for any $t \in \{ 0 , \ldots , T - 1 \}$ , it holds that $\| x _ { t } \| \le R _ { x }$ $\| y _ { t } \| \le R _ { y }$ , and $\| Y _ { t - 1 : t - h } \| \le \sqrt { h } R _ { y }$ .

We then obtain the following properties for the cost function $f _ { t } ( \cdot )$ in the state estimation problem; the proof can be found in Appendix B.

Lemma 3.3. For any M ∈ KM with KM defined in (15), ∥vec(M)∥ ≤ pmin{p, n} κ2F1−γF . $M \in { \cal K } _ { M }$ $\kappa _ { M }$ $\begin{array} { r } { { \bf \Phi } [ M ) \} ] \leq \sqrt { \operatorname* { m i n } \{ p , n \} } \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } } } \end{array}$ Moreover, under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ , for any $t \in \{ 0 , \ldots , T - 1 \}$ and any $M \in { \cal K } _ { M }$ ,

$$\left\| \nabla f _ {t} (\boldsymbol {v e c} (M)) \right\| \leq 2 \sqrt {h} R _ {y} \left(\sqrt {\min \{p , n \}} \kappa_ {F} ^ {2} \frac {\sqrt {h} R _ {y}}{1 - \gamma_ {F}} + R _ {x}\right), \tag {27}\left\| \nabla^ {2} f _ {t} (\boldsymbol {v e c} (M)) \right\| \leq 2 h R _ {y} ^ {2}, \tag {28}f _ {t} (M) \leq 2 R _ {x} ^ {2} + 2 h R _ {y} ^ {2} \min \{p, n \} \kappa_ {F} ^ {4} \frac {1}{(1 - \gamma_ {F}) ^ {2}}. \tag {29}$$

As we discussed before, the upper bounds in Lemma 3.3 imply that $f _ { t } ( \cdot )$ is Lipschitz and smooth.

Cost function of output estimation. Recall that in the problem of learning Kalman filtering for output estimation, the cost function is given by $f _ { t } ( N ) = \| y _ { t } - \hat { y } _ { t } ( N ) \| ^ { 2 }$ with $\begin{array} { r } { \hat { y } _ { t } ( N ) = \sum _ { s = 1 } ^ { h } N _ { s } y _ { t - s } } \end{array}$ and $N = [ N _ { s } ] _ { s \in [ h ] }$ . Similarly to the state estimation problem, we have the following results for the cost function $f _ { t } { \tilde { ( \cdot ) } }$ in the output estimation problem; the proof is similar to that of Lemma 3.4 and is thus omitted for conciseness.

$N \in { \cal K } _ { N }$ $\kappa _ { N }$ $\begin{array} { r } { \| v e c ( N ) \| \le \sqrt { p } \psi \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } } } \end{array}$
