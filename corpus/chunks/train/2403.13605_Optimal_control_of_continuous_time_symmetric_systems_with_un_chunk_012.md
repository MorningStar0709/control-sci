# 1.3 Notation

The following notation is used throughout the paper. For a matrix M, we use $[ M ] _ { i j }$ to refer to the element on the $i ^ { \mathrm { t h } }$ row and $j ^ { \mathrm { t h } }$ column of M , M ′ is the transpose of M , and |M| is a matrix with components $\left[ \left| M \right| \right] _ { i j } ^ { \star } = | m _ { i j } |$ . I is used to denote the identity matrix of appropriate dimensions. The Kronecker product is denoted by ⊗ and vec : $\mathbb { R } ^ { m \times n }  \mathbb { R } ^ { m n }$ is the vectorization operator. The relation $S \succ 0 \left( S \succeq 0 \right)$ means that matrix S is symmetric positive (semi)-definite and $S ^ { 1 / 2 }$ is the principal square root of $\dot { S } \succeq 0 . \lambda _ { \operatorname* { m i n } } S$ and $\lambda _ { \operatorname* { m a x } } S$ denote the smallest and largest eigenvalues of the symmetric matrix S. The square diagonal matrix $\boldsymbol { \Sigma } \in \mathbb { R } ^ { \boldsymbol { \hat { m } } \times \boldsymbol { m } }$ is called a signature matrix if $[ \bar { \Sigma } ] _ { i i } \in \{ - 1 , + 1 \}$ for all $i = 1 , 2 , \dots , m$ . ∥.∥p denotes the standard p-norm for vectors and matrices $( p \in \mathbb { N } \cup \infty ) , \| G \| _ { \mathrm { p k } }$ is the peak-to-peak gain and $\| G \| _ { H _ { \infty } }$ is the H-infinity norm of the linear system $G ( s )$ . We use Cov (x; t) to denote the covariance matrix at time t associated with the continuous-time random process x : $[ 0 , t _ { f } ] \to \mathbb { R } ^ { m }$ . For the functionals $f , g : \mathbb { R } ^ { n }  \hat { \mathbb { R } }$ , we write $f ( x ) = O { \bigl ( } g ( x ) { \bigr ) }$  as $x  \infty$ , if there are some positive constants r, a such that $| f ( x ) | \leq r | g ( x ) |$ | holds for all $\| x \| _ { \infty } > a$ . Finally, the Dirac delta and the Heaviside step functions are denoted by δ(t) and 1(t) respectively.
