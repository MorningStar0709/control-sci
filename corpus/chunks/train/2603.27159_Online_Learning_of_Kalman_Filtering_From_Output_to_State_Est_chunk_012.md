$$\hat {y} _ {t} ^ {\mathrm{SK}} = \sum_ {s = 1} ^ {t} N _ {s} ^ {\star} y _ {t - s}, \forall t \geq 0, \tag {16}\hat {y} _ {t} ^ {\mathrm{TK}} = C \hat {x} _ {t} ^ {\mathrm{TK}} = \sum_ {s = 1} ^ {h} N _ {s} ^ {\star} y _ {t - s}, \forall t \geq 0, \tag {17}$$

where we also define $y _ { t } = 0$ for all $t < 0$ and

$$N _ {s} ^ {\star} \triangleq C (A - L C) ^ {s - 1} L \in \mathbb {R} ^ {p \times p}, \forall s \geq 1. \tag {18}$$

The following result can be proved in a similar way to Lemma 2.1.

Lemwhere $s \in \mathbb { Z } _ { \ge 1 } , \ \lVert N _ { s } ^ { \star } \rVert \le \kappa _ { F } ^ { 2 } \psi \gamma _ { F } ^ { s - 1 } , \ \lVert N _ { s } ^ { \star } \rVert \le \sqrt { p } \kappa _ { F } ^ { 2 } \psi \gamma _ { F } ^ { s - 1 }$ , and $\begin{array} { r } { \| N ^ { \star } \| \le \sqrt { p } \psi \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } } } \end{array}$ $N ^ { \star } = [ N _ { s } ^ { \star } ] _ { s \in [ h ] }$ $N _ { s } ^ { \star }$

Finally, similarly to (15), Lemma 2.2 specifies a set of matrices such that the optimal parameter $N ^ { \star } = [ N _ { s } ^ { \star } ] _ { s \in [ h ] }$ associated with $\hat { y } _ { t } ^ { \mathrm { T K } }$ belongs to, i.e., $N ^ { \star } \in { \cal K } _ { N }$ with

$$\mathcal {K} _ {N} \triangleq \Big \{N = [ N _ {s} ] _ {s \in [ h ]}: \| N \| _ {F} \leq \sqrt {p} \psi \frac {\kappa_ {F} ^ {2}}{1 - \gamma_ {F} ^ {2}} \Big \}. (1 9)$$
