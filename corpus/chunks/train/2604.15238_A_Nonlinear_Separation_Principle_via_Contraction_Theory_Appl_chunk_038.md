$$
\left[ \begin{array}{c c} - 2 \delta B ^ {\top} Q B & - 2 \delta B ^ {\top} Q W + (1 + \delta) B ^ {\top} Q \\ * & (1 + \delta) (W ^ {\top} Q + Q W) - 2 Q - 2 \delta W ^ {\top} Q W \end{array} \right]
$$

Now, let us set $A = I _ { n } - W$ . First, note that

$$- R = - 2 \delta W ^ {\top} Q W + (1 + \delta) (W ^ {\top} Q + Q W) - 2 Q,Z = - 2 \delta B ^ {\top} Q W + (1 + \delta) B ^ {\top} Q.$$

Now, we may rewrite (62) using these simplifications.

$$
\left[ \begin{array}{c c} 2 c _ {r} P - 2 \delta B ^ {\top} Q B & Z - \varepsilon P K C \\ * & - R \end{array} \right] \preceq 0. \tag {63}
$$

Reparameterizing $Y = \varepsilon P K$ we get the condition (60).

Finally, we may now design a low gain integral controller.

Theorem 34 (Reference Tracking): Consider the FRNN plant (18) with synaptic matrix $W \in \mathbb { R } ^ { n \times n }$ , input matrix $B \in \mathbb { R } ^ { n \times m }$ , output matrix $C \in \mathbb { R } ^ { p \times n }$ , and reference signal $r \in \mathbb { R } ^ { p }$ . Suppose the following conditions hold.

(A1) (Plant contraction by state-feedback) There exists a matrix $K _ { f } \in \mathbb { R } ^ { m \times n }$ such that the FRNN with synaptic matrix $\dot { W _ { } } + B K _ { f }$ is strongly infinitesimally contracting with rate $c _ { K } > 0$ in the norm $\| \cdot \| _ { \mathcal { X } }$ .   
(A2) (Observer contraction) There exists a matrix $\ b { L } \in \mathbb { R } ^ { n \times p }$ such that the FRNN with synaptic matrix $W - L C$ is strongly infinitesimally contracting with rate $c _ { O } > 0$ in the norm $\| \cdot \| _ { \mathcal { O } }$ .   
(A3) (Contraction of reduced-order dynamics) For each constant input u, let $x ^ { \star } ( u )$ denote the unique equilibrium of the FRNN with synaptic matrix $W + B K _ { f }$ and input matrix B. Assume there exists $u ^ { \star } \in \mathbb { R } ^ { m }$ such that $r = C x ^ { \star } ( u ^ { \star } )$ . The reduced dynamics

$$\dot {u} _ {\text { ext }} = \varepsilon K _ {i} \big (r - C x ^ {\star} (u _ {\text { ext }}) \big) \tag {64}$$

be strongly infinitesimally contracting with rate $c _ { r } > 0$ in the norm $\| \cdot \| _ { \mathcal { R } }$ .

(A4) (Low-gain condition) Define the induced-norm constants

$$\ell_ {u} = \| B \| _ {\mathcal {U} \to \mathcal {X}}, \quad \ell_ {K} = \| K _ {f} \| _ {\mathcal {O} \to \mathcal {U}},\ell_ {i, \mathcal {R}} = \| K _ {i} C \| _ {\mathcal {X} \to \mathcal {R}}, \quad \ell_ {i, \mathcal {U}} = \| K _ {i} C \| _ {\mathcal {X} \to \mathcal {U}}.$$

The gain parameter $\varepsilon > 0$ satisfies
