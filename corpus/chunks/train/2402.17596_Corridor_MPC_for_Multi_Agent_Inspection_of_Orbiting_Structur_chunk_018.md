Since it was showed that ${ \tilde { { \mathbf { x } } } } ( t )$ remains in a compact subset on the interval $[ k \Delta t , \tau ]$ (namely D), then x˜(t) exists and is unique over the whole interval $[ k \Delta t , ( k + 1 ) \Delta t ]$ [16, Prop.
C.3.6].
By the same arguments applied for the previous sub-intervals, we prove that $\tilde { \zeta } _ { k } ( \tilde { { \boldsymbol x } } ( t ) ) ~ \geq ~ 0 ~ \forall t \ \in \ [ k \Delta t , ( k + 1 ) \Delta t ]$ ensuring that $\begin{array} { r } { \tilde { \mathbf { \Gamma } } ( t ) \in \cap _ { r = 1 } ^ { 0 } \dot { \mathcal { C } } _ { S _ { i } } ( t ) \forall t \in \left[ k \Delta \dot { t } , ( k { + } 1 ) \Delta t \right] \dot { \mathbf { b } } \mathbf { y } \left[ \dot { 1 } 5 \right.
} \end{array}$ , Thm.5].

As in [10], satisfying (8) might require excessive control authority given $c _ { w }$ and $L _ { w }$ are calculated on Q. For this reason, we introduce the definition of reachable set, where locally valid parameters $L _ { w } ^ { l }$ and $c _ { w } ^ { l }$ can be defined.

Definition 3. Given a set $\mathcal { N } \subseteq \tilde { \mathbb { X } }$ we define $^ { \Delta t } \mathcal { R } ( \mathcal { N } )$ as the set of all states $\tilde { \pmb { x } } \in \tilde { \mathbb { X } }$ that can be reached from $\tilde { \pmb { x } } \in \mathcal { N }$ in a time interval ∆t with state dynamics as in (2) and under available control input $\mathbf { \pmb { u } } \in \mathbb { U }$ according to (3).

The following Lemma is proposed as a less conservative modification of Lemma 1.

Lemma 2.
