22 ] 6 $\begin{array} { r l } { \mathbb { E } [ \left. \nabla _ { \theta } V ( \theta _ { \widehat { T } } ) \right. _ { 2 } ^ { 2 } ] } & { { } \leqslant } \end{array}$ $\varepsilon + \mathcal { O } ( \Delta _ { c r i t i c } )$ . Hence, the total sample complexity is $\mathcal { O } \left( M ( 1 - \gamma ) ^ { - 2 } \varepsilon ^ { - 2 } \log ( 1 / \varepsilon ) \right)$ .

Here, ∆critic := maxθ∈RM Eνπθ $\begin{array} { r l r } { \Delta _ { c r i t i c } } & { : = } & { \operatorname* { m a x } _ { \theta \in \mathbb { R } ^ { M } } \mathbb { E } _ { \nu _ { \pi _ { \theta } } } \lceil V ^ { \pi _ { \theta } } ( s ) - V ^ { w _ { \pi _ { \theta } } ^ { * } } \rceil ^ { 2 } \rceil } \end{array}$ which equals zero, if the value function lies in the linear space spanned by the features.

Next we provide the global optimality guarantee for the Natural-Actor-Critic version of ACIL.

Theorem 5.5. Assume su $\begin{array} { r } { \mathrm { p } _ { s \in S } \| \varphi ( s ) \| _ { 2 } \quad \leqslant \quad 1 } \end{array}$ . Under Assumptions 5.2 and 5.3 with step-sizes chosen as $\begin{array} { r } { \alpha \ = \ \Big ( \frac { \lambda ^ { 2 } } { 2 \sqrt { M } L _ { V } ( 1 + \lambda ) } \Big ) , \ \beta \ = \ \operatorname* { m i n } \big \{ \mathcal { O } \left( \Gamma _ { L } \right) , \mathcal { O } \left( 1 / \Gamma _ { L } \right) \big \} } \end{array}$ ,

batch-sizes $H = \mathcal{O}\left(\frac{1}{\Gamma_L\varepsilon^2}\right), B = \mathcal{O}\left(\frac{1}{(1 - \gamma)^2\varepsilon^2}\right), T_c = \mathcal{O}\left(\frac{\sqrt{M}}{\Gamma_L}\log (1 / \varepsilon)\right), T = \mathcal{O}\left(\frac{\sqrt{M}}{(1 - \gamma)^2\varepsilon}\right) \text{ and } \lambda = \mathcal{O}(\Delta_{critic}) \text{ we have } V(\pi^*) - \frac{1}{T} \sum_{t=0}^{T-1} \mathbb{E}[V(\pi_{\theta_t})] \leqslant \varepsilon + \mathcal{O}\left(\sqrt{\frac{\Delta_{actor}}{(1 - \gamma)^3}}\right) + \mathcal{O}(\Delta_{critic}). \text{ Hence, the total sample complexity is } \mathcal{O}\left(\frac{M}{(1 - \gamma)^4\varepsilon^3}\log \frac{1}{\varepsilon}\right).$

where $\begin{array} { r l r } { \Delta _ { a c t o r } } & { { } : = } & { \operatorname* { m a x } _ { \theta \in \mathbb { R } ^ { M } } \operatorname* { m i n } _ { w \in \mathbb { R } ^ { d } } \mathbb { E } _ { \nu _ { \pi _ { \theta } } } [ [ \psi _ { \theta } ^ { \top } w - } \end{array}$ $A _ { \pi _ { \theta } } ( s , m ) ] ^ { 2 } ]$ and $\Delta _ { \mathit { c r i t i c } }$ is same as before.
