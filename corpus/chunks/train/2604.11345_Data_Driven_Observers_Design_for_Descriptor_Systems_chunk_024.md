$( \Longrightarrow )$ : If system (11) qualifies as a UIO for the system (34), then the kernel inclusion condition (42) must hold according to Lemma 3. In addition, the historical data $\{ u _ { d } , x _ { d } , y _ { d } \}$ collected from system (34) should satisfy (11) with $\hat { x } ( k ) = x _ { d } ( k ) , k \in \mathbb { Z } _ { 0 } , \mathrm { i . e . }$ , with zero estimation error. Then (43) holds with $\bar { \Sigma } _ { X _ { p } } = A _ { O } , \bar { \Sigma } _ { U _ { p } } = B _ { O } ^ { u } ;$ , $\bar { \Sigma } _ { Y _ { p } } = B _ { O } ^ { y }$ , and $\bar { \Sigma } _ { Y _ { f } } = N _ { O }$ , in which $A _ { O }$ is Schur stable.

Remark 8 Although both Theorem 1 and Theorem 3 utilize the identical observer structure (11), their domains of application and existence conditions are fundamentally different. Theorem 1 applies to systems without unknown inputs, requiring only the Schur stability of $A _ { O }$ . In contrast, Theorem 3 addresses systems with unknown inputs, necessitating the additional kernel inclusion condition (42) to ensure disturbance decoupling.

To establish the equivalence between the proposed datadriven approach and the traditional model-based one, we impose the following structural matching condition, which is a generalization of the dual normalizability condition in the previous section.
