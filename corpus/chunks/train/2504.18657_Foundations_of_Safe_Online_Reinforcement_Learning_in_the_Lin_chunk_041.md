# C Proof of Lemma 2

proof. Let $\begin{array} { r } { \delta _ { \mathrm { L 2 } } = \frac { 1 } { \log ^ { 1 0 } ( T ) } } \end{array}$ and $\begin{array} { r } { \epsilon _ { \mathrm { L 2 } } = \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$

Define $\epsilon = \| \theta - \theta ^ { * } \| _ { \infty }$ and $\delta = | x - y |$ . In order to bound the cost difference of the two trajectories, we will first bound the differences in states and controls of the two trajectories. We begin with the following lemma bounding the difference in future states when starting at two different initial states.

Lemma 3. In the setting of Problem 1, for any θ ∈ Θ such that ϵ := ∥θ − θ∗∥∞ ≤ 1log46(T ) , $\theta \in \Theta$ $\begin{array} { r } { \epsilon : = \| \theta - \theta ^ { * } \| _ { \infty } \leq \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$ $t \leq T , W ^ { \prime } = \{ w _ { i } \} _ { i = 0 } ^ { t - 1 }$ , and any $K \in [ \frac { a - 1 } { b } , \frac { a } { b } ]$ , there exists $\mathcal { V } _ { \mathrm { L 3 } } \in \mathbb { R } ^ { t }$ that only depends on K and θ such that the event $E _ { \mathrm { L 3 } } ( K , \theta , W ^ { \prime } ) : = \left\{ \bar { W ^ { \prime } } \in \mathcal { V } _ { \mathrm { L 3 } } \right\}$ satisfies $\mathbb { P } ( E _ { \mathrm { L 3 } } ( K , \theta , W ^ { \prime } ) ) = 1 - o _ { T } ( 1 / T ^ { 1 0 } )$ and the following holds. Suppose that |x|, |y| ≤ 4 log2(T ) and d := |x − y| ≤ 1log10(T ) . $| x | , | y | \leq 4 \log ^ { 2 } ( T )$ $\begin{array} { r } { d : = | x - y | \leq \frac { 1 } { \log ^ { 1 0 } ( T ) } } \end{array}$ Define $d _ { i }$ as the difference in state at time i when starting at $x _ { 0 } = x$ versus starting at $x _ { 0 } = y$ and using controller $C _ { K } ^ { \theta } \in { \mathcal { C } } _ { \mathrm { t r } } ^ { \theta }$ with noise variables $W ^ { \prime }$ . Then there exists an $L = \tilde { O } _ { T } ( 1 )$ ) such that for sufficiently large $T$ , conditional on $E _ { \mathrm { L 3 } } ( K , \theta , W ^ { \prime } )$ ,

$$
d _ {i} \leq \left\{ \begin{array}{l l} 2 \xi^ {i} \cdot d, & \text { for   } L <   i \leq t \\ 4 d + \tilde {O} _ {T} (\epsilon) & \text { for   } 0 \leq i \leq L, \end{array} \right. \tag {8}
$$

where $\begin{array} { r } { \xi : = \left( 1 - \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) } \end{array}$

The proof of Lemma 3 can be found in Appendix C.1.

We can also bound the difference in control in terms of the difference in state.
