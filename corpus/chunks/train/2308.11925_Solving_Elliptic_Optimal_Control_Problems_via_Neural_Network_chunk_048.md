The results are reported in Table 2(c) and Fig. 4. Both OSNN and AONN approximate the optimal state ¯y and control ¯u accurately, but AONN has much larger control error $e ( u ^ { * } )$ . The efficiency of AONN depends heavily on the step size s. The step size $s = 2 0$ was determined in a trial-and-error manner in order to achieve good efficiency. ALM yields an accurate state approximation, which is however still inferior to OSNN and AONN. The computing time (in seconds) is only slightly higher than the unconstrained case: 4.39e3, 5.07e3 and 4.54e3 for OSNN, AONN and ALM, respectively.

Last, we investigate higher-dimensional problem to show the efficiency of OSNN.

Example 4.4. $\Omega = ( 0 , 1 ) ^ { 6 }$ , and $\lambda = 0 . 0 1$ . The test cases are as follows: (i) linear unconstrained $\left( L U \right) f =$ $5 \pi ^ { 2 } \prod _ { i = 1 } ^ { 6 }$ sin(πxi) and $\begin{array} { r } { y _ { d } = \left( - 6 \alpha \pi ^ { 4 } - 1 \right) \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) ; \mathrm { ( i i ) } } \end{array}$ linear constrained $\begin{array} { r } { ( L C ) \ f = 6 \pi ^ { 2 } \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) - } \end{array}$ $\begin{array} { r } { P _ { [ 0 , 3 ] } ( \pi ^ { 2 } \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) ) , y _ { d } = ( - 6 \alpha \pi ^ { 4 } - 1 ) \prod _ { i = 1 } ^ { 6 } } \end{array}$ sin(πxi); and (iii) semilinar (semi) $\begin{array} { r } { f = 6 \pi ^ { 2 } \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) + } \end{array}$ $\begin{array} { r } { \prod _ { i = 1 } ^ { 6 } \sin ^ { 3 } ( \pi x _ { i } ) , y _ { d } = ( 1 + 6 \pi ^ { 2 } \lambda + \lambda ) \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) + \lambda \prod _ { i = 1 } ^ { 6 } \sin ^ { 3 } ( \pi x _ { i } ) } \end{array}$ , and $q ( x , y ) = y ^ { 3 }$ . All PDEs have a zero Dirichlet boundary condition. The exact states y¯ are given by $\textstyle \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } )$ , and exact controls u¯ are given $\begin{array} { r l } & { b y \ \pi ^ { 2 } \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) \ f o r \ \mathrm { ( i ) } , \ P _ { [ 0 , 3 ] } ( \pi ^ { 2 } \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) ) \ f o r \ \mathrm { ( i i ) } , \ \prod _ { i = 1 } ^ { 6 } \sin ( \pi x _ { i } ) \ f o r \ \mathrm { ( i i i ) } } \end{array}$ .

![](image/0a797379a07d1db3caaa95904c905dc047fea8a3e6323d25163ace1928e4e83f.jpg)  
Figure 3: The approximate control $u ^ { * }$ (top), its pointwise error $\left| u ^ { * } - \bar { u } \right|$ (mid) and approximate state $y ^ { * }$ (bottom) obtained by three NN-based methods for Example 4.2, cross section at $x _ { 3 } = 0 , x _ { 4 } = 0$ .
