Lemma 4.1: Let the disk margin $D ( \alpha , \sigma )$ the plant in (1), and $\begin{array} { r l r } { \Lambda _ { p } } & { { } = } & { \tilde { L } _ { \Delta _ { p } } ^ { \top } \tilde { L } _ { \Delta _ { p } } , } \end{array}$ where $\begin{array} { r l r l } { \Lambda _ { p } } & { { } \succeq } & { } & { { } 0 } \end{array}$ and diagonal, be given. If there exist $\begin{array} { r c l } { \hat { \theta } } & { = } & { \Big \{ S , R , N _ { A } , N _ { B } , N _ { C } , D _ { k u w } , \hat { D } _ { k v y } , \hat { D } _ { k v w } , \Lambda _ { k } \Big \} } \end{array}$ where $S \succ 0 , R \succ 0 , \Lambda _ { k } \succ 0$ and diagonal, and $\bigl [ \begin{array} { l } { R I } \\ { I } S  \end{array} \bigr ] \succ 0$ such that (11) holds, then a controller θ can be reconstructed from ˆθ such that the controller satisfies the specified disk margin.

We label the set of $\hat { \theta }$ that satisfy the conditions of Lemma 4.1 for a particular $\Lambda _ { p }$ as $\hat { \Theta } ( \Lambda _ { p } )$ . Given $ { \hat { \theta } } \in  { \hat { \Theta } } ( \Lambda _ { p } )$ , the controller θ can be reconstructed by solving the following for $A _ { k } , B _ { k w } , B _ { k y } , C _ { k v } , D _ { k v w } , D _ { k v y } , C _ { k u } , D _ { k u y } ,$ , where U and V are such that $V U ^ { \top } = I - { \dot { R } } S$ (for example, by taking the SVD):

$$
\begin{array}{l} N _ {A} = \left[ \begin{array}{c c} S A _ {p} R & 0 \\ 0 & 0 \end{array} \right] \\ + \left[ \begin{array}{c c} U & S B _ {p} \\ 0 & I \end{array} \right] \left[ \begin{array}{c c} A _ {k} & B _ {k y} \\ C _ {k u} & D _ {k u y} \end{array} \right] \left[ \begin{array}{c c} V ^ {\top} & 0 \\ C _ {p y} R & I \end{array} \right] \\ N _ {B} = S B _ {p} D _ {k u w} + U B _ {k w} \\ N _ {C} = \Lambda_ {k} D _ {k v y} C _ {p} R + \Lambda_ {k} C _ {k v} V ^ {\top} \\ \hat {D} _ {k v y} = \Lambda_ {k} D _ {k v y}, \quad \hat {D} _ {k v w} = \Lambda_ {k} D _ {k v w}. \\ \end{array}
$$

This controller is certified by the given $\Lambda _ { p } ,$ , the $\Lambda _ { k }$ decision variable, and the following construction for $X \colon$

$$
X \triangleq \left[ \begin{array}{c c} I & S \\ 0 & U ^ {\top} \end{array} \right] \left[ \begin{array}{c c} R & I \\ V ^ {\top} & 0 \end{array} \right] ^ {- 1}.
$$

Therefore, satisfaction of Lemma 4.1 implies $\Theta ( X , \Lambda _ { p } , \Lambda _ { k } )$ is nonempty for the above $X , \Lambda _ { p }$ , and $\Lambda _ { k }$ .

Note that the transformation between ˆθ and θ also works in the reverse: a ˆθ can be constructed from a $\theta , X , \Lambda _ { p } ,$ and $\Lambda _ { k }$ using the above equations.
