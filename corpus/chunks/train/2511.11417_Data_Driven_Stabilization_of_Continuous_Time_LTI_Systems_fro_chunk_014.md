# C. Non-Minimal Realizations Consistent with the Dataset

Given the trajectories $u ( \cdot )$ and $y ( \cdot )$ over the interval $[ 0 , T ] .$ , we can simulate offline the observer (16), which acts as a linear filter that post-processes the data. Without loss of generality and for simplicity, we consider $\hat { z } ( 0 ) = 0 ,$ , so that

$$
\int_ {0} ^ {T} \left[ \frac {L y (\tau)}{- \zeta (\tau)} \right] \left[ \frac {L y (\tau)}{- \zeta (\tau)} \right] ^ {\top} \mathrm{d} \tau - \left[ \begin{array}{c c} L \Delta L ^ {\top} + F P + P F ^ {\top} + G Q + Q ^ {\top} G ^ {\top} & 0 _ {\mu \times n} P \\ \hline 0 _ {n \times \mu} & 0 _ {n + \mu} \\ P & \end{array} \right] \succ 0, \quad P = P ^ {\top} \succ 0. \tag {S}
$$

we obtain ${ \hat { z } } ( \cdot ) : [ 0 , T ] \to \mathbb { R } ^ { \mu }$ as defined in (9). It follows that the dataset and the simulated observer trajectory zˆ(·) satisfy (20) for almost all $t \in [ 0 , T ]$ ]. Also, for all $t \in [ 0 , T ]$ , the error $\tilde { x } ( t ) = x ( t ) - \Pi \hat { z } ( t )$ can be written explicitly using (19) and $\tilde { x } ( 0 ) = x ( 0 )$ :

$$\tilde {x} (t) = e ^ {\tilde {\Lambda} t} x (0) + \int_ {0} ^ {t} e ^ {\tilde {\Lambda} (t - \tau)} (E w (\tau) - \Pi L v (\tau)) \mathrm{d} \tau . \tag {22}$$

We now exploit a technical result adapted from [20, Lemma 4] to rewrite the free response $e ^ { \tilde { \Lambda } t } x ( 0 )$ in (22) in a convenient form. The proof is reported in Appendix II-B.

Lemma 2. For any $x ( 0 ) \in \mathbb { R } ^ { n p }$ and any $( \Lambda , \Gamma )$ as defined in Algorithm 1, there exists $H _ { 0 } \in \mathbb { R } ^ { p \times n }$ such that, for all $t \in [ 0 , T ] , C e ^ { \dot { \Lambda } t } x ( 0 ) = H _ { 0 } \chi ( t )$ , where χ(t) is given in (9).

Combining $y ( t ) = C \tilde { x } ( t ) + H \hat { z } ( t ) + v ( t )$ with (22) and Lemma 2 we obtain, for all $t \in [ 0 , T ]$ ,

$$y (t) = \Theta^ {\star} \zeta (t) + d (t), \tag {23}$$

where ζ(t) is defined in (9), the matrix

$$
\Theta^ {\star} := \left[ \begin{array}{l l} H _ {0} & H \end{array} \right] \in \mathbb {R} ^ {p \times (n + \mu)}, \tag {24}
$$

denotes the parameters derived from the unknown matrices A and B and the initial condition x(0), while, for all $t \in [ 0 , T ]$ ,

$$d (t) := v (t) + C \int_ {0} ^ {t} e ^ {\tilde {\Lambda} (t - \tau)} (E w (\tau) - \Pi L v (\tau)) \mathrm{d} \tau . \tag {25}$$
