NAs in Section 2.2.3, we use for the object orientation the unit quaternion choice $\zeta _ { o } : = [ \varphi _ { o } , \epsilon _ { o } ^ { \top } ] ^ { \top } \in \mathbb { S } ^ { 3 }$ . Let hence now a desired pose trajectory, $p _ { \mathrm { d } } : \mathbb { R } _ { \geq 0 }  \mathbb { R } ^ { \bar { 3 } } , \zeta _ { \mathrm { d } } : = [ \varphi _ { \mathrm { d } } , \epsilon _ { \mathrm { d } } ^ { \top } ] ^ { \top } : \mathbb { R } _ { \geq 0 }  \mathbb { S } ^ { 3 }$ , to be tracked by $x _ { o }$ . To that end, similar to Section 2.2.3, we define the position error $e _ { p _ { o } } : = p _ { o } - p _ { \mathrm { d } }$ as well as the quaternion product $e _ { \zeta } : = \zeta _ { \mathrm { d } } \cdot \zeta _ { o } ^ { + }$ . The aim is then to regulate $e _ { p _ { o } }$ to zero and $e _ { \zeta }$ to $[ \pm 1 , 0 ^ { \top } ] ^ { \top }$ . Moreover, we aim at ensuring that the agents are always in contact with the object and slipping is avoided. Formally, the problem is defined as follows.

Problem 2.3. Given a desired bounded, smooth object pose trajectory defined by $p _ { \mathrm { d } } : \mathbb { R } _ { \ge 0 } \to \mathbb { R } ^ { 3 } , \zeta _ { \mathrm { d } } : \mathbb { R } _ { \ge 0 } \to \mathbb { S } ^ { 3 }$ , with bounded first and second derivatives, as well as uncertain agent and object dynamic parameters involved in (2.69) and (2.73), respectively, determine a control law $\tau$ in (2.70) such that the following conditions hold:

1. lim ${ \mathsf { 1 } } _ { t \to \infty } \left( e _ { p _ { o } } ( t ) , e _ { \zeta } ( t ) \right) = \left( 0 , [ \pm 1 , 0 ^ { \top } ] ^ { \top } \right)$   
2. $f _ { C _ { i } } ^ { C _ { i } } ( t ) \in \mathcal { F } _ { C _ { i } } , \forall t > 0 , i \in \mathcal { N } .$

In order to solve the aforementioned problem the following assumptions are made for the grasp:

Assumption 2.10. The grasp consists of $N \geq 3$ agents with non-collinear contact points and $\mathrm { N u l l } ( \bar { G } _ { \mathcal { R } } ) \bigcap \mathrm { I n t } ( \mathcal { F } _ { c } ) \neq \emptyset$ .

Assumption 2.11. The matrix $J _ { h } ( q )$ is non-singular, and the contact points do not exceed the fingertip surface.
