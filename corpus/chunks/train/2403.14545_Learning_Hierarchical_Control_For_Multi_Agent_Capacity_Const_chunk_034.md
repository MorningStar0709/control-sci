Assume at event k (occurring at time t) of iteration r, where the agent is at node $v _ { i }$ with state $\boldsymbol { x } _ { t } ^ { r } = \left( c _ { t } ^ { r } , \chi _ { t } ^ { r } \right)$ so that

$$x _ {t} ^ {r} \in \mathcal {N} _ {i}g (x _ {t} ^ {r}) = (i, c _ {k} ^ {r})$$

with capacity states $c _ { k } ^ { r } = c _ { t } ^ { r }$ , the high-level optimal control problem (31) is feasible. Let

$$\mathbf {x} _ {k: k + N \mid k} ^ {H, r, \star} = (\mathbf {n} _ {k: k + N \mid k} ^ {r, \star}, \mathbf {c} _ {k: k + N \mid k} ^ {r, \star}) \tag {45}$$

be the optimal high-level trajectory containing the highlevel capacity state trajectory $\mathbf { c } _ { k : k + N | k } ^ { r , \star } .$ , corresponding to input $u _ { k } ^ { H , r \star }$ uH,r⋆k . The terminal constraints (30d), (30e) dictate that $\exists k ^ { \star } \geq 0$ such that

$$n _ {k + N | k} ^ {r, \star} = n _ {k ^ {\star}} ^ {p}, c _ {k + N | k} ^ {r, \star} \leq c _ {k ^ {\star}} ^ {p},$$

where $( n _ { k ^ { \star } } ^ { p } , c _ { k ^ { \star } } ^ { p } ) \in S ^ { H , r }$ , and $p \leq r .$

Since (44) must satisfy constraint (28c), it follows that

$$C _ {l} \geq \hat {\theta} _ {i, n _ {k + 1 | k} ^ {r, \star}, l} ^ {r}, \forall l \in [ 1, L ],$$

and therefore the low-level optimal control problem will be feasible for all time steps t ∈ I(xr, i, nr,⋆k+1|k) $t \in \bar { \mathcal { T } } ( \mathbf { x } ^ { r } , i , n _ { k + 1 | k } ^ { r , \star } )$ and bring the agent to node $n _ { k + 1 | k } ^ { r , \star }$ .

The planned capacity state trajectory in (45) utilizes the high-level model estimate ${ \hat { \theta } } ^ { r }$ to predict how capacity states will drain across the planned set of tasks. However, as shown in Thm. 1, the agent’s true capacity state consumption during iteration r may be less than predicted by ${ \hat { \theta } } ^ { r }$ (as will be captured in $\hat { \theta } ^ { r + 1 }$ when updated according to (21)). Thus the high-level state at event k + 1, when the agent has reached node $n _ { k + 1 | k } ^ { r , \star } ,$ , is

$$x _ {k + 1} ^ {H, r} = (n _ {k + 1 | k} ^ {r, \star}, c _ {k + 1} ^ {r})$$

where crk+1 ≤ cr,⋆k+1|k. $c _ { k + 1 } ^ { r } \leq c _ { k + 1 | k } ^ { r , \star } .$

At event $k + 1$ of iteration r, the input matrix $u \in$ $\{ 0 , 1 \} ^ { | V | \times | V | }$ such that

$$
u _ {i, j \in \mathcal {V}} = \left\{ \begin{array}{l l} 0 & i = n _ {k} ^ {r}, j = n _ {k + 1} ^ {r} \\ 1 & i = n _ {k + N | k} ^ {r \star}, j = n _ {k ^ {\star} + 1} ^ {p} \\ u _ {k, i, j} ^ {H, r} & \text { else } \end{array} \right. \tag {46}
$$

and capacity state values
