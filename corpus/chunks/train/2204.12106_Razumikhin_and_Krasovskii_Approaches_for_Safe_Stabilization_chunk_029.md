$$
u (\phi) := \left\{ \begin{array}{l l} \kappa (\lambda , \mathfrak {a} _ {\mathrm{k}} (\phi), (L _ {g} V _ {\mathrm{k} 1} (\phi)) ^ {\top}), & i f \phi \neq 0, \\ 0, & i f \phi = 0, \end{array} \right. \tag {33}
$$

where κ is defined in (12), $\lambda > 0$ and ${ \mathfrak { a } } _ { \mathsf { k } } ( \phi ) : = L _ { f } V _ { \mathsf { k } 1 } ( \phi ) +$ $D ^ { + } V _ { \mathsf { k } 2 } ( \phi ) + \gamma _ { \mathsf { I k } } ( V _ { \mathsf { k } } ( \phi ) )$ , is continuous and ensures the system (1) to be GAS.

Proof. We first prove the stabilization of the system (1) under the controller (33). From (33), we have that if $L _ { g } V _ { \mathrm { k l } } ( \phi ) = 0$ , then $u = 0$ , and from (32) in Definition 9, we have

$$L _ {f} V _ {\mathrm{k} 1} (\phi) + D ^ {+} V _ {\mathrm{k} 2} (\phi) + L _ {g} V _ {\mathrm{k} 1} (\phi) u + \gamma_ {\mathrm{lk}} (V _ {\mathrm{k}} (\phi))= L _ {f} V _ {\mathrm{k} 1} (\phi) + D ^ {+} V _ {\mathrm{k} 2} (\phi) + \gamma_ {\mathrm{lk}} (V _ {\mathrm{k}} (\phi)) < 0.$$

If $L _ { g } V _ { \mathrm { k l } } ( \phi ) \neq 0$ , we define ${ \mathfrak { a } } _ { \mathsf { k } } ( \phi ) : = L _ { f } V _ { \mathsf { k } 1 } ( \phi ) + D ^ { + } V _ { \mathsf { k } 2 } ( \phi ) +$ $\gamma _ { \vert \mathsf { k } } ( V _ { \mathsf { k } } ( \phi ) )$ and $\mathfrak { b } _ { \mathsf { k } } ( \phi ) : = L _ { g } V _ { \mathsf { k } 1 } ( \phi )$ . From the control law (33),

$$
\begin{array}{l} L _ {f} V _ {\mathrm{k} 1} (\phi) + D ^ {+} V _ {\mathrm{k} 2} (\phi) + L _ {g} V _ {\mathrm{k} 1} (\phi) u + \gamma_ {\mathrm{lk}} (V _ {\mathrm{k}} (\phi)) \\ = \mathfrak {a} _ {\mathrm{k}} (\phi) - \mathfrak {b} _ {\mathrm{k}} (\phi) \frac {\mathfrak {a} _ {\mathrm{k}} (\phi) \mathfrak {b} _ {\mathrm{k}} ^ {\top} (\phi)}{\| \mathfrak {b} _ {\mathrm{k}} (\phi) \| ^ {2}} - \mathfrak {b} _ {\mathrm{k}} (\phi) \frac {\sqrt {\mathfrak {a} _ {\mathrm{k}} ^ {2} (\phi) + \lambda \| \mathfrak {b} _ {\mathrm{k}} (\phi) \| ^ {4}}}{\| \mathfrak {b} _ {\mathrm{k}} (\phi) \| ^ {2}} \mathfrak {b} _ {\mathrm{k}} ^ {\top} (\phi) \\ = - \sqrt {\mathfrak {a} _ {k} ^ {2} (\phi) + \lambda \| \mathfrak {b} _ {k} (\phi) \| ^ {4}} \leq 0. \\ \end{array}
$$

Hence, under the controller (33), $L _ { f } V _ { \mathrm { k 1 } } ( \phi ) ~ + ~ D ^ { + } V _ { \mathrm { k 2 } } ( \phi ) ~ + ~$ $L _ { g } V _ { \mathsf { k } 1 } ( \phi ) u \ < \ - \gamma _ { \mathsf { k } } ( V _ { \mathsf { k } } ( \phi ) )$ . From (30)-(31) and Theorem 2.1 in [19], we conclude that the closed-loop system is GAS.
