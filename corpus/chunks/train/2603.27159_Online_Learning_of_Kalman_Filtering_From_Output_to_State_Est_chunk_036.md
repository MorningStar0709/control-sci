# 5.3 Proof of Theorem 5.2

Notations used in this proof. Similarly to the proof of Theorem 4.1, we assume that any filter parameter $M \in \mathbb { R } ^ { n \times ( p h ) }$ has been readily written in its vectorized form vec $\mathbf { \Psi } ( M ) \in \mathbb { R } ^ { n p h }$ so that we set foll simply denote M. We also use $\begin{array} { r } { \mathcal { K } _ { M } = \{ M \in \mathbb { R } ^ { n p h } : \| M \| \leq \sqrt { \operatorname* { m i n } \{ p , n \} } \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } } \} } \end{array}$ $\Pi _ { K _ { M } } ( M )$ to denote the projection of a vector ur analysis in this proof, we also define thein (38): $M \in \mathbb { R } ^ { n p h }$ onto the $\{ \tilde { v } _ { t } \} _ { t \geq 0 }$

$$\mathcal {E} _ {\tilde {v}} = \Big \{\max _ {0 \leq t \leq T - 1} \| \tilde {v} _ {t} \| \leq \sqrt {5 \operatorname{tr} (\tilde {V}) \log \frac {1 2 T}{\delta}} \Big \}. \tag {39}$$

Using similar arguments to those for Lemma 3.1, one can show that $\mathbb { P } ( \mathcal { E } _ { \tilde { v } } ) = 1 - \delta / 1 2$ . Noting that Algorithm 2 updates the filter parameter $M _ { t }$ in line 9 if and only if t mod $\tau = b _ { i }$ with $b _ { i }$ sampled in line 3 of the algorithm, we denote the set of time steps t in $\{ 0 , \ldots , T - 1 \}$ such Algorithm 2 updates $M _ { t }$ as $S = \{ t \geq 0 : t$ mod $\tau = b _ { i } \} = \{ t _ { 0 } , \dots , t _ { H - 1 } \}$ , where $H = | S |$ and we know from the definition of Algorithm 2 that $t _ { i } \in \{ i \tau , \dots , ( i + 1 ) \tau - 1 \}$ for all $i \in \{ 0 , \ldots , H - 1 \}$ . Additionally, we denote

$$S ^ {\prime} = \{t _ {j}: j \in [ H ], t _ {j} \geq \tau \} = \{t _ {1}, \dots , t _ {H - 1} \}. \tag {40}$$

One can check that $\vert S \vert = \vert T / \tau \vert$ and $| S ^ { \prime } | = \lfloor T / \tau \rfloor - 1$ . Finally, the following notations will also be used in this proof:

$$R _ {M} \triangleq \sqrt {\min \{p , n \}} \kappa_ {F} ^ {2} \frac {1}{1 - \gamma_ {F}}, \beta_ {x} \triangleq 2 h R _ {y} ^ {2},l _ {x} \triangleq 2 \sqrt {h} R _ {y} (\sqrt {\min \{p , n \}} \kappa_ {F} ^ {2} \frac {\sqrt {h} R _ {y}}{1 - \gamma_ {F}} + R _ {x}), \tag {41}R _ {\tilde {v}} \triangleq \sqrt {5 \mathrm{tr} (\tilde {V}) \log \frac {1 2 T}{\delta}},$$

where $R _ { x }$ and $R _ { y }$ are defined in (26).

In the following, we provide the main steps in the proof and the omitted details are included in Appendix D.

Regret decomposition. Similarly to (31) in the proof of Theorem 4.1, we decompose the regret $\mathtt { R _ { x } } ( T )$ in (21) as
