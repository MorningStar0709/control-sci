# H.2. Proofs for MABs with noisy gradients

When value gradients are unavailable, we follow a direct policy gradient algorithm instead of softmax projection. The full pseudo-code is provided here in Algorithm 4. At each round $t \geqslant 1$ , the learning rate for η is chosen asynchronously for each controller $m ,$ to be $\alpha \pi _ { t } ( m ) ^ { 2 }$ , to ensure that we remain inside the simplex, for some $\alpha \in ( 0 , 1 )$ . To justify its name as a policy gradient algorithm, observe that in order to minimize regret, we need to solve the following optimization problem:

$$\min _ {\pi \in \mathcal {P} ([ M ])} \sum_ {m = 1} ^ {M} \pi (m) (\mathfrak {r} _ {\mu} (m ^ {*}) - \mathfrak {r} _ {\mu} (m)).$$

A direct gradient with respect to the parameters $\pi ( m )$ gives us a rule for the policy gradient algorithm. The other changes in the update step (eq 18), stem from the fact that true means of the arms are unavailable and importance sampling.

We have the following result.

Theorem H.6. With value of α chosen to be less than $\frac { \Delta _ { m i n } } { \mathfrak { r } _ { m ^ { * } } ^ { \mu } - \Delta _ { m i n } } , \ \left( \pi _ { t } \right)$ ∆min is a Markov process, with $\pi _ { t } ( m ^ { * } ) \  \ 1$ as rm∗ $t  \infty , a . s$ . Further the regret till any time T is bounded as

$$\mathcal {R} (T) \leqslant \frac {1}{1 - \gamma} \sum_ {m \neq m ^ {*}} \frac {\Delta_ {m}}{\alpha \Delta_ {m i n} ^ {2}} \log T + C,$$

where $C : = \textstyle \frac { 1 } { 1 - \gamma } \sum _ { t \geq 1 } \mathbb { P } \left\{ \pi _ { t } ( m ^ { * } ( t ) ) \leqslant \frac { 1 } { 2 } \right\} < \infty .$ t > 1

We make couple of remarks before providing the full proof of Theorem H.6.

Remark H.7. The “cost” of not knowing the true gradient seems to cause the dependence on $\Delta _ { m i n }$ in the regret, as is not the case when true gradient is available (see Theorem H.4 and Corollary H.2). The dependence on $\Delta _ { m i n }$ as is well known from the work of (Lai & Robbins, 1985), is unavoidable.

Remark H.8. The dependence of α on $\Delta _ { m i n }$ can be removed by a more sophisticated choice of learning rate, at the cost of an extra log T dependence on regret (Denisov & Walton, 2020).
