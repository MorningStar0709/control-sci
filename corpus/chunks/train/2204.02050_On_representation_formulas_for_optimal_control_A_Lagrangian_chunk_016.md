This proposition provides an important insight into numerical implementation of the representation formulas because the infinite set $U$ can be replaced with a finite set $U _ { \delta }$ when constructing approximate optimal controls that are admissible. More specifically, let $( \boldsymbol { x } ( \cdot ) , \boldsymbol { \beta } ( \cdot ) )$ be an optimal solution of (2.10) under the state constraint $x ( s ) \in \bar { \Omega }$ . Since $\beta ( s ) \in \operatorname { C o n v } ( B ( x ( s ) , s ) )$ , one cannot directly obtain an admissible control $u \in \mathcal { U }$ such that $\beta ( s ) = - f ( s , x ( s ) , u ( s ) )$ . Instead, for each s, we choose $u ( s )$ to be arg mi $\iota _ { a \in U _ { \delta } } \left| \beta ( s ) + f ( s , x ( s ) , a ) \right|$ | for a given δ-net $U _ { \delta }$ . It is a reasonable approach, as we can always find a feasible control $u \in \mathcal { U }$ such that the corresponding state trajectory approximates the optimal one by Lemma 1. Motivated by this observation, a computationally tractable scheme for controller synthesis is discussed in Section 4.

Moreover, by Proposition 2, we can further approximate an optimal control $u ( s )$ by a measurable control $\bar { u } \in \mathcal { U }$ such that $\bar { u } ( s ) \in U _ { \delta }$ . Then, it follows from Proposition 2 that the performance gap between the optimal control u and the approximate control ¯u is bounded by Cδ for some positive constant C.

Corollary 1. Suppose $H _ { f } ^ { * } ( t , \cdot )$ is linear and let $( \boldsymbol { x } ( \cdot ) , \boldsymbol { \beta } ( \cdot ) )$ be an optimal solution of $( 2 . 7 ) \ : \ : ( o r$ (2.10)). Then, for any $\delta \stackrel { \triangledown } { > } 0$ , there exists an admissible control $\bar { u } \in \mathcal { U }$ with $\bar { u } ( s ) \in U _ { \delta }$ and a state trajectory $\bar { x } : [ t , T ] \to \mathbb { R } ^ { n }$ satisfying $\dot { \bar { x } } = f ( s , \bar { x } ( s ) , \bar { u } ( s ) )$ for $s \in [ t , T ]$ and $\bar { \boldsymbol { x } } ( t ) = \boldsymbol { x }$ such that

$$\Big | \underbrace {\bigg \{\int_ {t} ^ {T} H ^ {*} (s , x (s) , \beta (s)) \mathrm{d} s + g (x (T)) \bigg \}} _ {= V (\boldsymbol {x}, t)} - \Bigg \{\underbrace {\int_ {t} ^ {T} H ^ {*} (s , \bar {x} (s) , \bar {\beta} (s)) \mathrm{d} s - g (\bar {x} (T))} _ {\text {approximation}} \Bigg \} \Bigg | < C \delta$$

for some positive constant C, where $\bar { \beta } ( s ) = - f ( s , \bar { x } ( s ) , \bar { u } ( s ) )$ .

The corollary can be shown using the argument in the proof form Theorem 3 together with the bound in Proposition 2. Thus, we have omitted the proof.
