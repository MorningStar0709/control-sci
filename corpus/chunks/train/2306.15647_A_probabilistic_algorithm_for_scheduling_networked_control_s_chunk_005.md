Let $\kappa _ { m } : \mathbb { N } _ { 0 }  \{ 0 , 1 \}$ denote the data loss signal at the 𝑚-th channel of the communication network, $m = 1 , 2 , \ldots , M$ . If $\kappa _ { m } ( t ) = 0$ , then there is no data loss in the 𝑚-th channel at time 𝑡, and if $\kappa _ { m } ( t ) = 1$ , then there is a data loss in the 𝑚-th channel at time 𝑡. We call a data loss signal admissible if it satisfies

$$
\kappa_ {m} (t) = \left\{ \begin{array}{l l} 1, & \text { with   probability } q, \\ 0, & \text { with   probability } 1 - q, \end{array} \right. t \in \mathbb {N} _ {0},
$$

$m = 1 , 2 , \ldots , M$ . Let S be the set of all distinct subsets of $\{ 1 , 2 , \ldots , N \}$ with at most 𝑀 elements. We call a function $\gamma : \mathbb { N } _ { 0 }  S$ , that specifies, at every time 𝑡, at most 𝑀 plants of the NCS which has access to the shared communication network at that time, as a scheduling logic.

We let $r _ { i } ^ { 0 }$ denote the initial mode of operation of plant 𝑖, i.e., $r _ { i } ^ { 0 } = \dot { i } _ { s } , \mathrm { i f } i \in \gamma ( 0 )$ and the communication channel accessed by plant 𝑖 does not suffer from data loss at time 0, and $r _ { i } ^ { 0 } = i _ { u } , \mathrm { i f } i \notin \gamma ( 0 ) \mathrm { o r } , i \in \gamma ( 0 )$ but there is a data loss in the communication channel accessed by the plant 𝑖 at time 0.

We are interested in stochastic stability of all plants in the NCS.

Definition 1: The 𝑖-th plant in (1) is stochastically stable if for every initial condition $x _ { i } ^ { 0 } \in \mathbb { R } ^ { d _ { i } }$ and initial mode of operation $\dot { r } _ { i } ^ { 0 } \in \{ i _ { s } , i _ { u } \}$ , we have that the following condition holds :

$$\mathbb {E} \left\{\sum_ {t = 0} ^ {+ \infty} \left\| x _ {i} (t) \right\| ^ {2} \mid x _ {i} ^ {0}, r _ {i} ^ {0} \right\} < + \infty . \tag {2}$$

We will first solve the following problem :

Problem 1: Design a scheduling logic, $\gamma ,$ that preserves stochastic stability of each plant $i \ = \ 1 , 2 , \ldots , N$ under admissible data loss signals, $\kappa _ { m } , m = 1 , 2 , \ldots , M .$

Towards solving Problem 1, we rely on the probabilistic scheduling algorithm proposed in [13] and provide sufficient conditions on the matrices $A _ { i _ { s } } , A _ { i _ { u } } , i ~ = ~ 1 , 2 , \ldots , N$ , the capacity of the communication network, $M ,$ , and the probability of data loss, 𝑞, such that a scheduling logic, 𝛾, obtained from this algorithm ensures stochastic stability of each plant in the NCS.
