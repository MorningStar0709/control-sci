# 5 Learning Kalman Filtering for State Estimation

In this section, we turn our attention to the state estimation problem, which is more challenging as $f _ { t } ( M _ { t } ) = \left\| x _ { t } - \hat { x } _ { t } ( M _ { t } ) \right\| ^ { 2 }$ 2. Rewith $\begin{array} { r } { \hat { x } _ { t } ( M _ { t } ) = \sum _ { s = 1 } ^ { h } M _ { t , s } y _ { t - s } } \end{array}$ n of the state estimation pro, where the filter parameter $M _ { t } = [ M _ { t , s } ] _ { s \in [ h ] }$ is chosen from the set $\kappa _ { M }$ defined in (15).
