where $\mathcal { G } ( t ) ~ = ~ P ( t ) C ^ { T } + P _ { 1 } ( t ) C _ { d } ^ { T }$ with the covariance matrix ${ \dot { P } } ( t )$ defined as $P ( t ) \dot { = } \mathbb { E } [ ( x ( t ) - \hat { x } ( t ) ) ( x ( t ) -$ $\hat { x } ( t ) ) ^ { T } | \mathcal { F } _ { t } ^ { y } ]$ and $P _ { 1 } ( t ) = \left\{ \begin{array} { l l } { \mathbb { E } [ e ( t ) e ^ { T } ( t - T ) | \mathcal { F } _ { t } ^ { y } ] , } & { t \ge T } \\ { 0 , } & { t < T . } \end{array} \right.$ . is the delayed error covariance matrix.

Proof. We first define the predicted output $\mathrm { d } \hat { y } ( t ) =$ $C \hat { x } ( t ) \mathrm { d } t + C _ { d } \hat { x } ( t - T ) \mathrm { d } t$ , and introduce the innovation process

$$\mathrm{d} \nu (t) = \mathrm{d} y (t) - \mathrm{d} \hat {y} (t). \tag {23}$$

Since $D _ { d } D _ { d } ^ { T }$ is positive definite, $\nu ( t )$ is an $\mathcal { V } _ { t } .$ -martingale with incremental covariance E $\mathsf { \Omega } [ \mathrm { d } \nu ( t ) \mathrm { d } \nu ^ { T } ( t ) ] = D _ { d } D _ { d } ^ { T }$ dt. For linear Gaussian system (20), the minimum mean square error filter admits the structure

$$\mathrm{d} \hat {x} (t) = A \hat {x} (t) \mathrm{d} t + A _ {d} \hat {x} (t - T) \mathrm{d} t + K (t) \mathrm{d} \nu (t), \quad (2 4)$$

where $K ( t )$ denotes the filter gain. Substituting (23) into (24) yields

$$
\begin{array}{l} \mathrm{d} \hat {x} (t) = A \hat {x} (t) \mathrm{d} t + A _ {d} \hat {x} (t - T) \mathrm{d} t \tag {25} \\ + K (t) \left(\mathrm{d} y (t) - C \hat {x} (t) \mathrm{d} t - C _ {d} \hat {x} (t - T) \mathrm{d} t\right). \\ \end{array}
$$

The optimality condition follows from the orthogonality principle $\mathbb { E } [ e ( \dot { t } ) \mathrm { d } \nu ^ { T } ( t ) ] = 0$ . Since the innovation process dν is an $\dot { \mathcal { F } } _ { t } ^ { \dot { y } }$ -Brownian motion, the observation space admits the orthogonal decomposition

$$L ^ {2} (\mathcal {F} _ {t} ^ {y}) = L ^ {2} (\mathcal {F} _ {t -} ^ {y}) \oplus \mathrm{span} \{\mathrm{d} \nu (t) \}. \tag {26}$$
