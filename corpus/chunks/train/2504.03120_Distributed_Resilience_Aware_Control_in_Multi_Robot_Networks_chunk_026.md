# V. SIMULATION RESULTS

Here, we show the effectiveness of our controller (20) through simulations in a 2D plane. We consider a timevarying network $\mathcal { G } ( t ) ~ = ~ ( \mathcal { V } , \mathcal { E } ( t ) )$ , consisting of $n = 1 1$ robots, where edges are formed based on robots’ relative distances with a communication range $R = 3$ m. Each robot has dynamics (1) and an input set $\mathcal { U } _ { i } = [ - 1 . 5 , 1 5 ] \times [ - 1 . 5 , 1 . 5 ]$ . They are simulated as point masses and to maintain a minimum distance of $\Delta _ { d } = 0 . 3$ m. Each normal robot $i \in \mathcal { L }$ updates its connectivity value $c _ { i } ( t )$ according to (4) and shares it with its neighbors every $\tau _ { 1 } = 0 . 0 0 5$ seconds. It also starts with a random consensus state $y _ { i } ( t ) ~ \in ~ [ - 5 0 0 , 5 0 0 ]$ at $t ~ = ~ t _ { 0 }$ , which it shares and updates every $\tau _ { 2 } ~ = ~ 0 .$ 1 seconds using W-MSR algorithm thereafter. We examine three scenarios, each involving $F = 2$ malicious robots $i \in \mathcal A$ following different update rules for $c _ { i } ( t )$ :

1. Nominal Updating: Follow the nominal law (4) to update:

$$c _ {i} (t) = \sum_ {j \in \mathcal {N} _ {i} (t _ {l})} a _ {i j} (\mathbf {x} (t _ {l})), \forall t \in [ t _ {l}, t _ {l + 1}), \forall l \in \mathbb {Z} _ {\geq 0}.$$

2. Understating: Understate their connectivity levels:

$$c _ {i} (t) = \sum_ {j \in \mathcal {N} _ {i} (t _ {l})} a _ {i j} (\mathbf {x} (t _ {l})) - 2. 5, \forall t \in [ t _ {l}, t _ {l + 1}), \forall l \in \mathbb {Z} _ {\geq 0}.$$

3. Overstating: Overstate their connectivity levels:

$$c _ {i} (t) = \sum_ {j \in \mathcal {N} _ {i} (t _ {l})} a _ {i j} (\mathbf {x} (t _ {l})) + 3. 5, \forall t \in [ t _ {l}, t _ {l + 1}), \forall l \in \mathbb {Z} _ {\geq 0}.$$

In all cases, all malicious robots randomly choose and share $y _ { i } ( t ) \in [ - 5 0 0 , 5 0 0 ]$ every $\tau _ { 2 }$ seconds. By Proposition 1, all normal robots have to maintain $\begin{array} { r } { F ^ { \prime } = 2 + \left\lfloor { \frac { 1 1 } { 2 } } \right\rfloor = 7 } \end{array}$ or more neighbors to ensure resilient consensus. We evaluate our controller by observing whether robots can achieve resilient consensus even when the robots’ desired control inputs directly conflict with maintaining network connectivity, despite malicious robots’ adversarial behaviors. With the desired controller $\begin{array} { r } { u _ { i , \mathrm { d e s } } ( x _ { i } ) = \frac { v ( x _ { i } ) } { \| v ( x _ { i } ) \| } } \end{array}$ for each robot $i \in \nu$ , where
