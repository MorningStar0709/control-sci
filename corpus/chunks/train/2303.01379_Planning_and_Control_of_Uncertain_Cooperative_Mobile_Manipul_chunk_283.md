$$
\begin{array}{l} \dot {V} \leq - \left(k _ {\phi} - \frac {\alpha}{2}\right) \| \nabla_ {x} \phi (x) \| ^ {2} - \frac {k _ {v}}{2} \| e _ {v} \| ^ {2} + \frac {\bar {d} ^ {2}}{2 k _ {v}} - 3 \sigma_ {\alpha} \frac {\widetilde {\alpha} ^ {2}}{4} - \sigma_ {m} \frac {\widetilde {m} ^ {2}}{2} \\ + 3 \sigma_ {\alpha} \frac {\alpha^ {2}}{4} + \sigma_ {m} \frac {m ^ {2}}{2} \\ \leq - k _ {\xi} \| \xi \| ^ {2} + d _ {\xi}, \\ \end{array}
$$

where $\begin{array} { r } { \xi : = [ \nabla _ { x } \phi ( x ) ^ { \top } , e _ { v } ^ { \top } , \tilde { m } , \tilde { \alpha } ] ^ { \top } \in \mathbb { R } ^ { 2 n + 2 } , k _ { \xi } : = \operatorname* { m i n } \left\{ k _ { \phi } - \frac { \alpha } { 2 } , k _ { v } , \frac { \sigma _ { m } } { 2 } , \frac { 3 \sigma _ { \alpha } } { 4 } \right\} } \end{array}$ . Therefore, 2is negative when , and dξ := d¯2 $\begin{array} { r } { d _ { \xi } : = \frac { \bar { d } ^ { 2 } } { 2 k _ { v } } + 3 \sigma _ { \alpha } \frac { \alpha ^ { 2 } } { 4 } + \sigma _ { m } \frac { m ^ { 2 } } { 2 } } \end{array}$ α2 $\dot { V }$ $\lVert \xi \rVert > \sqrt { \frac { d _ { \xi } } { k _ { \xi } } }$ k , which implies uniform ultimate boundedness of $\| \xi ( t ) \|$ in a set around zero, whose size is proportional to $d _ { \xi }$ , which can be shrunk by gain tuning (see Theorem A.5 of Appendix $\mathrm { A } )$ . In addition, $\dot { V }$ is sign indefinite only in the set defined by $\begin{array} { r } { \| \xi \| < \sqrt { \frac { d _ { \xi } } { k _ { \xi } } } } \end{array}$ and negative otherwise, which implies that $V ( t )$

remains bounded, $\forall t \geq 0$ , and hence collisions with the workspace obstacles and boundary are avoided.

Note that the aforementioned analysis guarantees that $\nabla _ { \boldsymbol { x } } \phi ( \boldsymbol { x } ) | _ { \boldsymbol { x } ( t ) }$ will be ultimately bounded in a set close to zero. This point, however, might be a critical point of $\phi$ and it is not guaranteed that $x ( t )$ will be bounded close to the goal configuration $x _ { \mathrm { d } }$ . Nevertheless, intuition suggests that if the disturbance vector $d ( x , v , t )$ does not behave adversarially, the agent will converge close to the goal configuration. This is also verified by the simulation results of Section 4.2.6.
