Remark 1: Earlier in [13] the notion of probabilistic allocation of the shared communication network to various subsets of the set of all plants was used. Algorithm 1 differs from [13, Algorithm 1] in the choice of subsets of the set of all plants. In [13] the authors consider that the capacity of the communication network, 𝑀, divides the total number of plants, $N , \mathrm { i . e . , } N ^ { \mathrm { q } } \mathrm { o } M = 0$ and each $s _ { j }$ contains exactly 𝑀 plants, $\begin{array} { r } { j = 1 , 2 , \dots , \frac { N } { M } } \end{array}$ . Our choice of $s _ { j } , j = 1 , 2 , \ldots , \nu$ does not require $N ^ { \mathrm { { q } } } { \circ } M \ = \ 0$ and thus caters to a larger class of NCSs. A more important difference between Algorithm 1 and [13, Algorithm 1] lies in the premise of scheduling. While in [13] analysis of a scheduling logic, 𝛾, obtained from a probabilistic algorithm is performed for NCSs whose shared communication networks have a limited bandwidth but ideal communication, in the current paper we deal with limited bandwidth and data losses. We will identify a generalized version of the stability conditions of [13] as a special case of the results presented here.

We now provide sufficient conditions on the plant dynamics, the capacity of the communication network and the probability of data loss under which a scheduling logic, 𝛾, obtained from Algorithm 1 ensures stochastic stability of each plant in the NCS.

Theorem 1: Consider an NCS described in Section II. Let the plant dynamics, $( A _ { i } , B _ { i } ) , i = 1 , 2 , \ldots , N .$ , the controller dynamics, $K _ { i } , i = 1 , 2 , \ldots , N .$ the capacity of the communication network, 𝑀, and the probability of data loss, $p ,$ be given. Each plant $i = 1 , 2 , \dots , N$ is stochastically stable under a scheduling logic, $\gamma ,$ obtained from Algorithm 1 if and only if for each $i \in s _ { j } , j = 1 , 2 , \ldots , \nu _ { : }$ , there exist symmetric and positive definite matrices, $P _ { k } \in \mathbb { R } ^ { d _ { i } \times d _ { i } } , k = i _ { s } , i _ { u }$ , such that

$$A _ {k} ^ {\top} \mathcal {P} ^ {i} A _ {k} - P _ {k} \prec 0 _ {d _ {i} \times d _ {i}}, \tag {3}$$

where $\mathcal { P } ^ { i } = p _ { j } ( 1 - q ) P _ { i _ { s } } + \bigl ( ( 1 - p _ { j } ) + p _ { j } q \bigr ) P _ { i _ { u } }$
