# 4. EXPERIMENTS

We trained 16 models in total, 4 models using 4 different datasets. For each dataset, we trained an LSTM and a GRU model and their stable variants. The dataset collection methodology was inspired by [17].
