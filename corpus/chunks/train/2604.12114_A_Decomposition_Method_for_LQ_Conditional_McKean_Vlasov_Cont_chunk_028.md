where $( y , p , q ) \in \mathcal { C } _ { \mathcal { F } ^ { 0 } } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right) \times L _ { \mathcal { F } ^ { 0 } } ^ { 2 } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right) \times L _ { \mathcal { F } ^ { 0 } } ^ { 2 } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right)$ is the adapted solution of the FBSDEs given by

$$
\left\{ \begin{array}{l l} d y _ {t} = \left[ \left(A _ {t} + F _ {t} - B _ {t} R _ {t} ^ {- 1} \bar {S} _ {t} ^ {\intercal}\right) y _ {t} - B _ {t} R _ {t} ^ {- 1} B _ {t} ^ {\intercal} p _ {t} - B _ {t} R _ {t} ^ {- 1} \varpi_ {t} + b _ {t} \right] d t + D _ {t} ^ {0} d W _ {t} ^ {0}, & y _ {0} = \mathbb {E} [ \xi ], \\ d p _ {t} = - \left[ \left(A _ {t} + F _ {t} - B _ {t} R _ {t} ^ {- 1} \bar {S} _ {t} ^ {\intercal}\right) ^ {\intercal} p _ {t} + \left(\bar {Q} _ {t} - \bar {S} _ {t} R _ {t} ^ {- 1} \bar {S} _ {t} ^ {\intercal}\right) y _ {t} - \bar {S} _ {t} R _ {t} ^ {- 1} \varpi_ {t} + \bar {\zeta} _ {t} \right] d t + q _ {t} d W _ {t} ^ {0}, & p _ {T} = \bar {Q} _ {T} y _ {T}. \end{array} \right. \tag {63}
$$

Given the conditions imposed on the relevant coefficients in Problem MFT, the system (63) is well defined, which in turn ensures the admissibility of $v ^ { * } ,$ ; that is, $v ^ { \ast } \in \bar { \mathcal { U } } : = L _ { \mathcal { F } ^ { 0 } } ^ { 2 } \left( [ 0 , T ] , \mathbb { R } ^ { d } \right)$ .

To conclude, according to Theorem 1, and from (48)-(50) and (60)-(61), an admissible pair $( x ^ { \ast } , u ^ { \ast } ) \in \mathcal { C } _ { \mathcal { F } } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right) \times$ U is an optimal pair for Problem MFT, if and only if the pair $\left( \bar { x } _ { t } ^ { \ast } , \bar { u } _ { t } ^ { \ast } \right) = \left( \mathbb { E } [ x _ { t } ^ { \ast } | \mathcal { F } _ { t } ^ { 0 } ] , \mathbb { E } [ u _ { t } ^ { \ast } | \mathcal { F } _ { t } ^ { 0 } ] \right) \in \mathcal { C } _ { \mathcal { F } ^ { 0 } } \left( [ 0 , T ] , \bar { \mathbb { R } ^ { n } } \right) \times \bar { \mathcal { U } }$ satisfies (34)-(36) and the pair $( \check { x } _ { t } ^ { \ast } , \check { u } _ { t } ^ { \ast } ) = \big ( x _ { t } ^ { \ast } - \mathbb { E } [ x _ { t } ^ { \ast } | \mathcal { F } _ { t } ^ { 0 } ] , u _ { t } ^ { \ast } - \mathbb { E } [ u _ { t } ^ { \ast } | \mathcal { F } _ { t } ^ { 0 } ] \big ) \in \mathcal { C } _ { \mathcal { F } } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right) \times \check { \mathcal { U } }$ satisfies (37)-(39). This completes the proof. □

The following Corollary characterizes the optimal control for Problem MFT through two decoupled sets of classical linear FBSDEs.

Corollary 2. (Optimal Control) Under Assumption 2, the optimal control for Problem MFT is given by
