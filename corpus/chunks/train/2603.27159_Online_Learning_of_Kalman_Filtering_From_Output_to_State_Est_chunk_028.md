$$\sum_ {t = 0} ^ {T - 1} \big (\left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| ^ {2} - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{KF}} \right\| ^ {2} \big) = \mathcal {O} (\log^ {4} T). \tag {35}$$

Overall regret upper bound. Since $\mathbb { P } ( \mathcal { E } _ { w } \cap \mathcal { E } _ { v } ) \geq 1 - ( 2 \delta ) / 3$ from Lemma 3.1 and the upper bound in (32) holds with probability at least $1 - \delta / 3$ , we can obtain the overall regret upper bound ${ \mathcal { O } } ( \log ^ { 4 } T )$ in Theorem 4.1 by combining (32) and (34)-(35) via a union bound, where we also recall the bounds $\| C \| \leq \psi$ from (13) and $\| L \| \leq \kappa _ { F }$ from Lemma E.4.
