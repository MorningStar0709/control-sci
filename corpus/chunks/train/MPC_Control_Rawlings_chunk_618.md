<table><tr><td>Set</td><td>Definition</td><td>Membership</td></tr><tr><td> $\mathbb{X}$ </td><td>state constraint set</td><td> $x \in \mathbb{X}$ </td></tr><tr><td> $\mathbb{U}$ </td><td>input constraint set</td><td> $u \in \mathbb{U}$ </td></tr><tr><td> $\mathbb{W}_{x}$ </td><td>state disturbance set</td><td> $w_{x} \in \mathbb{W}_{x}$ </td></tr><tr><td> $\mathbb{W}_{d}$ </td><td>integrating disturbance set</td><td> $w_{d} \in \mathbb{W}_{d}$ </td></tr><tr><td> $\mathbb{W}$ </td><td>total state disturbance set,  $\mathbb{W}_{x} \times \mathbb{W}_{d}$ </td><td> $w \in \mathbb{W}$ </td></tr><tr><td> $\mathbb{N}$ </td><td>measurement error set</td><td> $v \in \mathbb{N}$ </td></tr><tr><td> $\tilde{\mathbb{W}}$ </td><td>estimate error disturbance set,  $\mathbb{W} \oplus (-L\mathbb{N})$ </td><td> $\tilde{w} \in \tilde{\mathbb{W}}$ </td></tr><tr><td> $\Phi$ </td><td>total estimate error disturbance set, $\Phi = \tilde{A}_{L}\Phi \oplus \tilde{\mathbb{W}}$ </td><td> $\phi \in \Phi$ </td></tr><tr><td> $\Sigma_{x}$ </td><td>state estimate error disturbance set,  $\left[ I_{n} \quad 0 \right] \Phi$ </td><td> $\tilde{x} \in \Sigma_{x}$ </td></tr><tr><td> $\Sigma_{d}$ </td><td>integrating disturbance estimate error set, $\left[ 0 \quad I_{p} \right] \Phi$ </td><td> $\tilde{d} \in \Sigma_{d}$ </td></tr><tr><td> $\Delta$ </td><td>innovation set,  $L(\tilde{C}\Phi \oplus \mathbb{N})$ </td><td> $L\tilde{y} \in \Delta$ </td></tr><tr><td> $\Delta_{x}$ </td><td>set containing state componentof innovation,  $L_{x}(\tilde{C}\Phi \oplus \mathbb{N})$ </td><td> $L_{x}\tilde{y} \in \Delta_{x}$ </td></tr><tr><td> $\Delta_{d}$ </td><td>set containing integrating disturbancecomponent of innovation,  $L_{d}(\tilde{C}\Phi \oplus \mathbb{N})$ </td><td> $L_{d}\tilde{y} \in \Delta_{d}$ </td></tr><tr><td> $\mathbb{S}$ </td><td>nominal state tracking error invariance set, $A_{K}\mathbb{S} \oplus \Delta_{x} = \mathbb{S}$ </td><td> $e \in \mathbb{S}$  $\hat{x} \in \{z\} + \mathbb{S}$ </td></tr><tr><td> $\Gamma$ </td><td>state tracking error invariance set,  $\mathbb{S} + \Sigma_{x}$ </td><td> $x \in \{z\} + \Gamma$ </td></tr><tr><td> $\mathbb{V}$ </td><td>nominal input constraint set,  $\mathbb{V} = \mathbb{U} \ominus K\mathbb{S}$ </td><td> $v \in \mathbb{V}$ </td></tr><tr><td> $\mathbb{Z}$ </td><td>nominal state constraint set,  $\mathbb{Z} = \mathbb{X} \ominus \Gamma$ </td><td> $z \in \mathbb{Z}$ </td></tr></table>

Table 5.1: Summary of the sets and variables used in output MPC.

We assume that $\boldsymbol { x } \in \mathbb { R } ^ { n }$ , $\ b { d } \in \mathbb { R } ^ { p }$ , $u \in \mathbb { R } ^ { m }$ , $\boldsymbol { y } \in \mathbb { R } ^ { r }$ , and $e \in \mathbb { R } ^ { q } , q \leq r$ and that the system to be controlled is subject to the usual state and control constraints

$$x \in \mathbb {X} \quad u \in \mathbb {U}$$

where X is polyhedral and U is polytopic.
