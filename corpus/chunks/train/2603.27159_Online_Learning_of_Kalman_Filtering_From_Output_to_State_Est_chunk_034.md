Theorem 5.2. Let the step sizes be $\begin{array} { r } { \eta _ { j } = \frac { 2 } { \alpha _ { 0 } j } } \end{array}$ for all $j \geq 1$ and $\eta _ { 0 } = 0$ . Let the length h of the truncated filter $\hat { x } _ { t } ^ { \mathrm { T K } }$ given by (12) be $h = \lfloor \log T / \log ( 1 / \gamma _ { F } ) \rfloor$ and let $\tau \geq h$ , where $\gamma _ { F }$ is defined in (14). Then, for any $T > 1$ and any √ $\delta \in ( 0 , 1 )$ , with probability at least $1 - \delta$ , it holds that $\mathtt { R } _ { \mathtt { X } } ( T ) = \mathcal { O } \big ( \tau ( \log ^ { 4 } T ) \log ( T / \tau ) + \sqrt { T } \log ^ { 2 } T \big )$ , where $\mathcal { O } ( \cdot )$ hides polynomial factors in log 1δ and problem parameters $\begin{array} { r l } & { p , n , \bar { \sigma } , \alpha _ { 0 } ^ { - 1 } , \alpha _ { 1 } , \psi , \frac { \kappa _ { A } } { 1 - \kappa _ { A } } , \frac { 1 } { \log ( 2 \bar { \sigma } / ( 2 \bar { \sigma } - \alpha _ { 0 } ) ) } , \mathrm { t r } ( \tilde { V } ) . } \end{array}$ 4

Trade-off between queries and regret. As can be seen from the results in Theorem 5.2, there is a clear trade-off between the value of the input parameter τ to Algorithm 2 and the√ overall regret of the algorithm. In particular, we may write $\mathsf { R } _ { \mathbf { x } } ( T ) = \tilde { \mathcal { O } } ( \tau + \sqrt { T } )$ , where $\tilde { \mathcal { O } } ( \cdot )$ further compresses log T factors. Recalling that $\lfloor T / \tau \rfloor$ is the number of times that Algorithm 2 queries $\tilde { x } _ { t } .$ the algorithm achieves a sublinear regret in T provided that $\lfloor T / \tau \rfloor$ is also sublinear in T . A seemly more straightforward way to reduce the queries of $\tilde { x } _ { t }$ in Algorithm 2 is to sample $b _ { t } \stackrel { i . i . d . } { \sim }$ . Bernoulli $( 1 / \tau )$ and query for $\hat { x } _ { t }$ if and only if $b _ { t } = 1$ . However, such a sampling scheme only guarantees $\lfloor T / \tau \rfloor$ number of queries for $\tilde { x } _ { t }$ in expectation. In contrast, our proposed sampling scheme in line 3 of

Algorithm 2 ensures that the number of queries is given by $\lfloor T / \tau \rfloor$ deterministically, which facilitates our regret analysis in Theorem 5.2.
