# 4. Regret Analysis

In this section, we present our main result, an upper bound on the expected regret of STT-MPC. To state our main theorem, we introduce the following notation. Let Π be the set of all tube-MPC algorithms (which are parameterized by θ). For an algorithm $\pi \in \Pi$ , define $( x _ { 0 } ^ { \pi } , u _ { 0 } ^ { \pi } , \ldots , x _ { t } ^ { \pi } , u _ { t } ^ { \pi } )$ as the sequence of states and control inputs generated under $\pi ,$ with $x _ { 0 } ^ { \pi } = x _ { 0 }$ . We also define by $v _ { t } ^ { \pi } ( x , \theta ) : = v _ { 0 \mid t } ^ { \pi } ( x , \theta )$ , the first element of the solution of the optimization problem $\mathcal { P } _ { N } ( x , \theta )$ . We denote by $z _ { t } ^ { \pi } ( x )$ the state of the system (1) at time t under algorithm π given initial state $x ,$ and we denote by $y _ { t } ^ { \pi } ( x )$ , the associated output of $\pi .$ Let $\pi _ { \star } \in \Pi$ be the oracle tube-MPC problem $\mathcal { P } _ { N } ( x , \theta ^ { \star } )$ . We simplify the notation by replacing π⋆ by $\star , \mathrm { e . g . } , x _ { t } ^ { \star } , v _ { t } ^ { \star }$ and so on. We define the regret $R _ { T } ( \pi )$ of an algorithm π as:

$$R _ {T} (\pi) = \sum_ {t = 0} ^ {T - 1} (\| x _ {t} ^ {\pi} \| _ {Q} ^ {2} + \| u _ {t} ^ {\pi} \| _ {R} ^ {2}) - \sum_ {t = 0} ^ {T - 1} (\| x _ {t} ^ {\star} \| _ {Q} ^ {2} + \| u _ {t} ^ {\star} \| _ {R} ^ {2}).$$

Theorem 4.1. The regret o $^ { f } \pi = S T T – M P C$ with confidence δ and persistent excitation parameter $\alpha : = 2 \epsilon$ , with $\epsilon \in ( 0 , 1 )$ satisfies for all $T \geq 1$ , with probability at least $1 - \delta _ { \mathrm { { ; } } }$ ,

$$R _ {T} (\pi) \leq C \left((\log (T / \delta) ^ {1 / 2} + 1) T ^ {1 / 2 + \epsilon} + \log (1 / \delta) + 1\right),$$

for some universal constant $C > 0 .$ . Now when the confidence is set to $1 / T ^ { 2 }$ , we $g e t$

$$\mathbb {E} [ R _ {T} (\pi) \leq C ^ {\prime} \log (T) ^ {1 / 2} T ^ {1 / 2 + \epsilon},$$

where $C ^ { \prime }$ is a universal constant.

Proof. We first express the regret as the sum of differences between the states and inputs,
