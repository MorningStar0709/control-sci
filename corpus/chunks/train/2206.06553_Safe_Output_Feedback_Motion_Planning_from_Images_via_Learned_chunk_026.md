$D _ { e } \cap D _ { c } ;$ this ensures that (6) contracts towards the true state x via (2), and that a feasible feedback control exists in (13); ensuring this at planning time (when we only know $x ^ { * } ( t ) )$ ) requires a Minkowski sum of $\varOmega _ { c }$ and $\varOmega _ { e } \ominus \{ x ( t ) \}$ . Constraint-satisfying candidate extensions are added to $\tau$ (line 11); else, they are rejected (line 12). This continues until the goal is reached (line 13). We visualize our planner (Fig. 5), Contraction-based Output feedback RRT (CORRT), detailed in Alg. 1. Finally, Thm. 2 shows our method ensures safety and goal reachability if all estimated constants are valid; as our estimates are probabilistically-valid, the overall guarantees are probabilistic (cf. Rem. 2):

Theorem 2 (CORRT correctness). Assume that $L _ { \varDelta k } , \ L _ { \hat { h } ^ { - 1 } }$ , and the estimated constants in $\bar { \epsilon } _ { \{ 1 , 2 , 3 \} }$ are valid over their computed domains. Then Alg. 1 returns a trajectory $( x ^ { * } ( t ) , u ^ { * } \grave { ( t ) } )$ , which when tracked on the true system (1a) using $u ( \hat { x } , x ^ { * } , u ^ { * } )$ with state estimates xˆ generated by (6), reaches $\mathcal { G }$ while satisfying $x ( t ) \in \mathcal { X } _ { s a f e } , f o r a l l t \in [ 0 , T ]$ .
