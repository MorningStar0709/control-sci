where $\begin{array} { r l r } { { \bf k } _ { X , i } } & { = } & { [ \kappa ( x _ { i } , x _ { i } ^ { ( 1 ) } ) , \cdot \cdot \cdot , \kappa ( x _ { i } , x _ { i } ^ { ( M ) } ) ] ^ { T } , ~ { \bf K } _ { i } ~ = } \end{array}$ (M))]T , K = $[ \kappa ( x _ { i } ^ { ( p ) } , x _ { i } ^ { ( q ) } ) ] _ { p , q = 1 , \cdots , M }$ (q) and $\pmb { y } = [ y _ { i } ^ { ( i ) } , \cdots , y _ { i } ^ { ( M ) } ] ^ { T }$ (M)]T . While the obtained posterior mean $\mu _ { i } ( \cdot )$ is adopted as the compensation of the unknown function f ( ) in $( 3 ) , { \mathrm { i . e . , } } { \hat { f } } _ { i } ( \cdot ) = \mu _ { i } ( \cdot )$ , the posterior variance $\sigma _ { i } ( \cdot )$ is used to quantify the prediction error shown as follows.

Lemma 1 ( [24]): Consider the Gaussian process model on agent i for the inference of an unknown function $f ( \cdot )$ satisfying Assumption 3, which is based on a data set under Assumption 2. Choose $\delta \in ( 0 , 1 ) \subset R$ and $\tau _ { i } \in \mathbb { R } _ { + }$ such that $\begin{array} { r } { \gamma _ { i } \leq \operatorname* { m i n } _ { x _ { i } \in \mathbb { X } } \sqrt { \beta } \sigma _ { i } ( x _ { i } ) } \end{array}$ with

$$\beta = 2 \log \left(\frac {1}{2 \delta \tau_ {i}} \Big (\max _ {x \in \mathbb {X}} x - \min _ {x \in \mathbb {X}} x \Big) + \frac {1}{\delta}\right), \tag {8}\gamma_ {i} = (L _ {f} + L _ {\mu , i} + \sqrt {\beta} L _ {\sigma}) \tau_ {i}, \tag {9}$$

where $L _ { f } , \ L _ { \mu , i } , \ L _ { \sigma }$ are the Lipschitz constants for $f ( \cdot )$ , $\mu _ { i } ( \cdot ) , \sigma _ { i } ( \cdot )$ , respectively. Then, the prediction error is uniformly bounded by

$$| f (x _ {i}) - \mu_ {i} (x _ {i}) | \leq \eta_ {i} (x _ {i}) = 2 \sqrt {\beta} \sigma_ {i} (x _ {i}), \forall x _ {i} \in \mathbb {X}, \tag {10}$$

with probability of at least $1 - \delta .$ .

Although with conservatism, Lemma 1 provides a numerical way to probabilistically bound the prediction error, which is commonly used for the control in safety-critical scenarios. Note that Lemma 1 requires the Lipschitz continuity of the unknown function $f ( \cdot )$ represented by $L _ { f }$ , which is easily shown by considering the continuity of $f ( \cdot )$ in Assumption 3 and the compact domain X. The approximation of $L _ { f }$ can be obtained by using the data-driven methods as in [24]. Moreover, the Lipschitz constants $\boldsymbol { L } _ { \mu , i }$ and $L _ { \sigma }$ exist due to the Lipschitz kernel function $\kappa ( \cdot )$ in Assumption 3, whose explicit computation follows [25].
