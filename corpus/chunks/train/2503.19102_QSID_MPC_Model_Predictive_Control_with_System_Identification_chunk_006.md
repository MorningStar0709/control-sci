# Algorithm 1 QSID-MPC

Input: Reference trajectory $x ^ { \mathrm { r e f } } ,$ , identified system matrices [Aˆ Bˆ ], Cost weight matrices $Q$ and $R ,$ time horizon $T _ { h }$

,Output: Optimal control sequence $u _ { t } ^ { * } , t \in \mathbb { N } _ { 0 }$

,1: procedure Compute MPC(xref, Aˆ, Bˆ , Q, R, Th)   
2: For each time $t \in \mathbb { N } _ { 0 } ,$   
3: Set $x _ { t | t } \gets x _ { t } ;$   
4: Solve optimization (2);   
5: $u _ { t } ^ { * } \gets u _ { t | t } ^ { * }$   
6: Apply feedback control $u _ { t } \gets u _ { t } ^ { * }$ in $( 1 )$ to get $x _ { t + 1 } ;$   
7: Repeat for $t \gets t + 1$ until the end of the control task.   
8: end procedure

where

$$X = [ \tilde {x} _ {0} \dots \tilde {x} _ {T - 1} ],X ^ {+} = [ \tilde {x} _ {1} \dots \tilde {x} _ {T} ]U = \left[ \tilde {u} _ {0} \dots \tilde {u} _ {T - 1} \right], \text { and } \tag {4}\Psi = \left[ X ^ {\top}, U ^ {\top} \right] ^ {\top},$$

with $\tilde { x } _ { t }$ and ˜ut denoting the quantized versions of $x _ { t }$ and $u _ { t } ,$ respectively. Exact relationships between the unquantized and quantized variables will be provided soon after we discuss the quantization scheme.

Remark 2: When the data is not quantized and enough amount of data is present, the least-square problem (3) has a unique solution $\begin{array} { r l r } { \mathcal { G } ^ { * } } & { { } = } & { [ A , B ] } \end{array}$ . For instance assume $u _ { t } ~ = ~ 0$ for all $t ,$ , then the least square problem boils down to min $\begin{array} { r } { { \underset { \mathcal { A } } { \boldsymbol { \pi } } } \frac { 1 } { T } \| \boldsymbol { X } ^ { + } - \mathcal { A } \boldsymbol { X } \| ^ { 2 } = \operatorname* { m i n } _ { \mathcal { A } } \frac { 1 } { T } \sum _ { t = 0 } ^ { T - 1 } \| \hat { \boldsymbol { A } } \boldsymbol { x } _ { t } - \mathcal { A } \boldsymbol { x } _ { t } \| ^ { 2 } = } \end{array}$ minA $\begin{array} { r } { \operatorname { t r } \left( ( A - \mathcal { \dot { A } } ) \left( { \frac { 1 } { T } } \sum _ { t = 0 } ^ { T - 1 } x _ { t } x _ { t } ^ { \top } \right) ( A - \mathcal { \dot { A } } ) ^ { \top } \right) } \end{array}$ t=0 . If enough data is T t=accumulated to ensure $\begin{array} { r } { \frac { 1 } { T } \sum _ { t = 0 } ^ { T - 1 } x _ { t } x _ { t } ^ { \top } > 0 . } \end{array}$ , then ${ \mathcal { A } } ^ { * } = A$ is the unique optimal solution. Once matrix A is identified, we may identify matrix B in a similar fashion with enough data to ensure $\begin{array} { r } { \frac { 1 } { T } \sum _ { t = 0 } ^ { T - 1 } u _ { t } u _ { t } ^ { \top } > 0 } \end{array}$ .

While the least square algorithm (3) is quite effective in perfectly identifying A and B, as shown in Remark 2, quantization complicates things to a large extent. Our analysis first shows the effect of quantization on the identification errors $( A - { \hat { A } } )$ and $( B - { \hat { B } } )$ , and then we provide a Lyapunov-based analysis to comment on the trajectory tracking and stability performances of the MPC controller developed using the identified system.
