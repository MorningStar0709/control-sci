# 4 ε-PACS control problem and proof of Theorem 1.2

In this section, we shall verify that the value functions V given in (1.6) agrees with the solution u to the HJB equation (1.10) on O under assumptions (H2) and (H3)–(H5). To prove it, firstly, we study an ε-PACS control problem that is closely related to the value function problem seen previously.

The penalized control set $\mathcal { U } ^ { \bar { \varepsilon } }$ is defined by

$$\mathcal {U} ^ {\varepsilon} = \{\xi = (\mathfrak {n}, \zeta) \in \mathcal {U}: \zeta_ {t} \text { is absolutely continuous }, 0 \leq \dot {\zeta} _ {t} \leq 2 C / \varepsilon \} \tag {4.1}$$

where $\varepsilon \in ( 0 , 1 )$ is fixed and C is a positive constant independent of ε. Let $( x _ { 0 } , \ell _ { 0 } , \iota _ { 0 } ) \ \in$ $\overline { { \mathcal { O } } } \times \mathbb { M } \times \mathbb { I }$ be fixed. The controlled process $( X ^ { \xi , \varsigma } , J ^ { \varsigma } , I )$ evolves as

$$X _ {t} ^ {\xi , \varsigma} = X _ {\tilde {\tau} _ {i}} ^ {\xi , \varsigma} - \int_ {\tilde {\tau} _ {i}} ^ {t} [ b (X _ {s} ^ {\xi , \varsigma}, I _ {s} ^ {(\ell_ {i})}) + \mathfrak {n} _ {s} \dot {\zeta} _ {s} ] \mathrm{d} s + \int_ {\tilde {\tau} _ {i}} ^ {t} \sigma (X _ {s} ^ {\xi , \varsigma}, I _ {s} ^ {(\ell_ {i})}) \mathrm{d} W _ {s}, \tag {4.2}I _ {t} = I _ {t} ^ {(\ell_ {i})} \text { and } J _ {t} ^ {s} = \ell_ {i} \quad \text { for } t \in [ \tilde {\tau} _ {i}, \tilde {\tau} _ {i + 1}) \text { and } i \geq 0,$$

where $\tilde { \tau } _ { i } = \tau _ { i } \wedge \tau$ and τ is the first exit time of $X ^ { \xi , \varsigma }$ from the set O. Defining the Legendre transform of $H _ { \iota } ^ { \varepsilon } ( \gamma , x ) : = H ^ { \varepsilon } ( \gamma , x , \iota ) = \psi _ { \varepsilon } \left( | \gamma | ^ { 2 } - g _ { \iota } ^ { 2 } ( x ) \right)$ , with $( x , \iota ) \in \overline { { \mathcal { O } } } \times \mathbb$ , as

$$l _ {\iota} ^ {\varepsilon} (y, x) := l ^ {\varepsilon} (y, x, \iota) := \sup _ {\gamma \in \mathbb {R} ^ {d}} \left\{\langle \gamma , y \rangle - H _ {\iota} ^ {\varepsilon} (\gamma , x) \right\}, \quad \text { for } y \in \mathbb {R} ^ {d}$$

the corresponding penalized functional cost for $( \xi , \varsigma ) \in \mathcal { U } ^ { \varepsilon } \times \mathcal { S }$ is defined as
