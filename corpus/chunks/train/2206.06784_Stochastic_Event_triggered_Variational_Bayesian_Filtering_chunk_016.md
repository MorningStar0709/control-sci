Note that $P _ { k | k - 1 }$ and $R _ { k }$ follow inverse Wishart distributions, and P −1 $P _ { k | k - 1 } ^ { - 1 }$ and $\dot { R } _ { k } ^ { - 1 }$ obey Wishart distributions. Define $( \tilde { P } _ { k | k - 1 } ^ { i } ) ^ { - 1 } =$ $E ^ { i } \big \{ P _ { k | k - 1 } ^ { - 1 } \big \}$ and $( \tilde { R } _ { k } ^ { i } ) ^ { - 1 } = E ^ { i } \{ R _ { k } ^ { - 1 } \}$ . Further, $E ^ { i } \big \{ \Phi _ { k | k - 1 } ^ { - 1 } \big \}$ in (34) can be derived as

$$E ^ {i} \{\Phi_ {k | k - 1} ^ {- 1} \}
= \left[ \begin{array}{c c} I & - H ^ {T} \\ 0 & I \end{array} \right] \left[ \begin{array}{c c} (\tilde {P} _ {k | k - 1} ^ {i}) ^ {- 1} & 0 \\ 0 & (\tilde {R} _ {k} ^ {i}) ^ {- 1} \end{array} \right] \left[ \begin{array}{c c} I & 0 \\ - H & I \end{array} \right]

= \left[ \begin{array}{c c} (\tilde {P} _ {k | k - 1} ^ {i}) ^ {- 1} + H _ {k} ^ {T} (\tilde {R} _ {k} ^ {i}) ^ {- 1} H _ {k} & - H _ {k} ^ {T} (\tilde {R} _ {k} ^ {i}) ^ {- 1} \\ - (\tilde {R} _ {k} ^ {i}) ^ {- 1} H _ {k} & (\tilde {R} _ {k} ^ {i}) ^ {- 1} \end{array} \right]
= \left(\tilde {\Phi} _ {k | k - 1} ^ {i}\right) ^ {- 1}.$$

To this end, $\Theta _ { k } ^ { i + 1 }$ can be computed by using Lemma 1. Denote $A _ { \Theta } = \big ( \tilde { P } _ { k | k - 1 } ^ { i } \big ) ^ { - 1 } + H _ { k } ^ { T } \big ( \tilde { R } _ { k } ^ { i } \big ) ^ { - 1 } H _ { k } , \tilde { B _ { \Theta } } = - H _ { k } ^ { T } \big ( \tilde { R } _ { k } ^ { i } \big ) ^ { - 1 } , C _ { \Theta } =$ $- ( \tilde { R } _ { k } ^ { i } ) ^ { - 1 } \dot { H } _ { k }$ , and $D _ { \Theta } = ( \tilde { R } _ { k } ^ { i } ) ^ { - 1 } + Y _ { k }$ . Then, $P _ { k | k } ^ { i + 1 }$ and P i+1 $P _ { z z , k | k } ^ { i + 1 }$ in

(32) can be calculated as

$$
\begin{array}{l} P _ {k \mid k} ^ {i + 1} = (A _ {\Theta} + B _ {\Theta} D _ {\Theta} ^ {- 1} C _ {\Theta}) ^ {- 1} \\ = \big ((\tilde {P} _ {k | k - 1} ^ {i}) ^ {- 1} + H _ {k} ^ {T} \big ((\tilde {R} _ {k} ^ {i}) ^ {- 1} - (\tilde {R} _ {k} ^ {i}) ^ {- 1} \big ((\tilde {R} _ {k} ^ {i}) ^ {- 1} \\ \left. + Y _ {k}\right) ^ {- 1} \left(\tilde {R} _ {k} ^ {i}\right) ^ {- 1}) H _ {k}) ^ {- 1} \\ = \left(\left(\tilde {P} _ {k \mid k - 1} ^ {i}\right) ^ {- 1} + H _ {k} ^ {T} \left(\tilde {R} _ {k} ^ {i} + Y _ {k} ^ {- 1}\right) ^ {- 1} H _ {k}\right) ^ {- 1} \tag {55} \\ = \tilde {P} _ {k | k - 1} ^ {i} - \tilde {P} _ {k | k - 1} ^ {i} H _ {k} ^ {T} (H _ {k} \tilde {P} _ {k | k - 1} ^ {i} H _ {k} ^ {T} \\ \left. + \tilde {R} _ {k} ^ {i} + Y _ {k} ^ {- 1}\right) ^ {- 1} H _ {k} \tilde {P} _ {k | k - 1} ^ {i}, \\ \end{array}
$$

and
