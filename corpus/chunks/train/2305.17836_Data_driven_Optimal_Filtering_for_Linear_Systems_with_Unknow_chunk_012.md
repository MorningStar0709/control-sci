# 3.2 Optimization landscape

The first result is concerned with the behaviour of the objective function at the boundary of the optimization domain. It is known [7] that the set of Schur stabilizing gains $s$ is regular open, contractible, and unbounded when $m \geq 2$ and the boundary $\partial \cal S$ coincides with the set $\{ \breve { L } \in \mathbb { R } ^ { n \times m }$ $\rho ( A - L H ) = 1 \}$ . For simplicity of presentation, we consider a slightly stronger assumption:

Assumption 2. The pair $( A , H )$ is observable, and the noise covariances $Q \succ 0$ and $R \succ 0$ .

Lemma 1. The function $J ( . ) : S  \mathbb { R }$ is real-analytic and coercive with compact sublevel sets; i.e.,

$$L \to \partial \mathcal {S} \text { or } \| L \| \to \infty \text { each implies } J (L) \to \infty ,$$

and $S _ { \alpha } : = \{ L \in \mathbb { R } ^ { n \times m } : J ( L ) \leq \alpha \}$ is compact and contained in S for any finite $\alpha > 0 .$

The next result establishes the gradient dominance property of the objective function. While this result is known in the LQR setting ( [6, 14]), the extension to the estimation setup is not trivial as $H \bar { \mathsf { \Pi } } H$ which takes the role of the covariance matrix of the initial state in LQR, may not be positive definite (instead, we only assume $( A , H )$ is observable). This, apparently minor issue, hinders establishing the gradient dominated property globally. However, we recover this property on every sublevel sets of J which is sufficient for the subsequent convergence analysis.

Lemma 2. Consider the constrained optimization problem (10).Then,

• The explicit formula for the gradient of J is: $\nabla J ( L ) = 2 Y _ { ( L ) } \left( - L R + A _ { L } X _ { ( L ) } H ^ { \intercal } \right)$ , where $Y _ { ( L ) } = Y$ is the unique solution of $Y = A _ { L } ^ { \mathsf { T } } Y A _ { L } + H ^ { \mathsf { T } } H$ .

• The global minimizer $L ^ { * } = \mathrm { a r g }$ min $_ { \cal L \in S } J ( L )$ satisfies $L ^ { * } = A X ^ { * } H ^ { \intercal } \left( R + H X ^ { * } H ^ { \intercal } \right) ^ { - 1 }$ , with $X ^ { \ast }$ being the unique solution of $X ^ { * } = \bar { A } _ { L ^ { * } } \bar { X ^ { * } } A _ { L ^ { * } } ^ { \mathsf { T } } + Q + L ^ { * } R ( L ^ { * } ) ^ { \mathsf { T } }$ .

• The function $J ( . ) : S _ { \alpha } \to \mathbb { R }$ , for any non-empty sublevel set $S _ { \alpha } f o r$ some $\alpha > 0 ,$ , satisfies the following inequalities; for all $\dot { L } , L ^ { \prime } \in \mathcal { S } _ { \alpha }$ :
