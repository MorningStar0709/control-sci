$$\mathcal {S} _ {s} ^ {M} \left(\{\tau_ {p} \} _ {p \in \mathcal {P}}\right) = \left\{\sigma \colon [ 0, \infty) \to \mathcal {P} \colon t _ {k + 1} - t _ {k} \geq \tau_ {\sigma (t _ {k})} \text {for all} k \in \mathbb {N} \right\},\mathcal {S} _ {u} ^ {M} \left(\{\eta_ {p} \} _ {p \in \mathcal {P}}\right) = \left\{\sigma : [ 0, \infty) \to \mathcal {P}: t _ {k + 1} - t _ {k} \leq \eta_ {\sigma (t _ {k})} \text {for all} k \in \mathbb {N} \right\}\mathcal {S} ^ {M} \left(\{\tau_ {p} \} _ {p \in \mathfrak {s}}, \{\eta_ {p} \} _ {p \in \mathfrak {u}}\right) = \left\{\sigma : [ 0, \infty) \to \mathcal {P}: t _ {k + 1} - t _ {k} \geq \tau_ {\sigma (t _ {k})} \text {if} \sigma (t _ {k}) \in \mathfrak {s}, \right.t _ {k + 1} - t _ {k} \leq \eta_ {\sigma (t _ {k})} \text {if} \sigma (t _ {k}) \in \mathfrak {u} \biggr \}.$$

These signal classes are generalizations of signal classes defined earlier in this paper. If $\tau _ { p } = \tau$ for all $p \in { \mathfrak { s } }$ and $\eta _ { p } = \eta$ for all $p \in { \mathfrak { u } }$ , then $S _ { s } ^ { M } \left( \left\{ \tau _ { p } \right\} _ { p \in \mathcal { P } } \right) = S _ { s } [ \tau ] , S _ { u } ^ { M } \left( \left\{ \bar { \eta _ { p } } \right\} _ { p \in \mathcal { P } } \right) =$ $\dot { S } _ { u } [ \eta ]$ , and $\bar { S ^ { M } } \left( \{ \tau _ { p } \} _ { p \in \mathfrak { s } } , \{ \bar { \eta } _ { p } \} _ { p \in \mathfrak { u } } \right) = S \left( \tau , \eta \right)$ .

The results obtained earlier involving dwell time and flee time can be generalized to involve mode-dependent dwell time and mode-dependent flee time. Here we just state them without proof. The results follow along the lines of proofs of the earlier statements.

Theorem 6.1 (Generalization of Theorem 3.11). The reset switched system (2) governed by graph G is stable for all $\sigma \in S ^ { \mathcal { G } } \cap S ^ { M } \left( \{ \tau _ { p } \} _ { p \in \mathfrak { s } } , \{ \eta _ { p } \} _ { p \in \mathfrak { u } } \right)$ , where

$$\tau_ {p} = \max _ {q: (p, q) \in \mathcal {E} (\mathcal {G})} \frac {\ln \left(c _ {p} \left\| P _ {q} ^ {- 1} R _ {(p , q)} P _ {p} \right\|\right)}{\lambda_ {p} ^ {*}}, \quad \eta_ {p} = - \max _ {q: (p, q) \in \mathcal {E} (\mathcal {G})} \frac {\ln \left(c _ {p} \left\| P _ {q} ^ {- 1} R _ {(p , q)} P _ {p} \right\|\right)}{\mu_ {p} ^ {*}}.$$
