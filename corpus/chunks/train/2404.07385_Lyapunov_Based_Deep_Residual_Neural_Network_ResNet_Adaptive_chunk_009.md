# A. ResNet Architecture

The unknown drift vector field f can be approximated using a ResNet. A ResNet is modeled using building

1The control effectiveness term is omitted to better focus on the specific contributions of this paper without loss of generality. The method in [8] can be used with the developed method in the case where the system involves an uncertain control effectiveness term.

blocks that involve a shortcut connection across a fullyconnected DNN [14]. Let $\Phi _ { p } \mathrm { ~  ~ : ~ } \mathbb { R } ^ { L _ { p , 0 } } \times \mathbb { R } ^ { L _ { p , 0 } \times L _ { p , 1 } } \times$ $\dots \times \mathbb { R } ^ { L _ { p , k _ { p } } \times L _ { p , k _ { p } + 1 } } \to \mathbb { R } ^ { L _ { p , k _ { p } + 1 } ^ { \texttt { * } } }$ denote the $p ^ { t h }$ fully-$\Phi _ { p } ( \eta _ { p } , V _ { p , 0 } , \ldots , V _ { p , k _ { p } } ) \triangleq$ $\left( V _ { p , k _ { p } } ^ { T } \phi _ { p , k _ { p } } \circ \dots \circ V _ { p , 1 } ^ { T } \phi _ { p , 1 } \right) \left( V _ { p , 0 } ^ { T } \eta _ { p } \right)$ $p \in \{ 1 , \ldots , m \}$ $\eta _ { p } ~ \in ~ \mathbb { R } ^ { L _ { p , 0 } }$ $\Phi _ { p } , \ k _ { p } \ \in \ \mathbb { Z } _ { > 0 }$ denotes the number of hidden layers in $\Phi _ { p } ,$ and m ∈ $\mathbb { Z } _ { > 0 }$ denotes the number of building blocks. Additionally, $L _ { p , j } ~ \in ~ \mathbb { Z } _ { > 0 }$ denotes the number of nodes, and $V _ { p , j } \in$ $\mathbb { R } ^ { \sum _ { p , j } \times L _ { p , j + 1 } }$ denotes the weight matrix in the $j ^ { t h }$ layer of $\Phi _ { p }$ for all $( p , j ) \in \{ 1 , \ldots , m \} \times \{ 0 , \ldots , k _ { p } \}$ . Similarly, $\phi _ { p , j } : \mathbb { R } ^ { L _ { p , j } }  \mathbb { R } ^ { L _ { p , j } }$ denotes a vector of smooth activation functions.2 If the ResNet involves multiple types of activation functions at each layer, then $\phi _ { p , j }$ may be represented as $\phi _ { p , j } \triangleq \left[ \varsigma _ { p , j , 1 } \hdots \hdots \hdots \hdots \right] ^ { \scriptscriptstyle L }$ , where $\varsigma _ { p , j , i } : \mathbb { R } $ R denotes the activation function at the $i ^ { \mathrm { t h } }$ node of the $j ^ { \mathrm { t h } }$ layer of $\Phi _ { p } . ^ { 3 }$ All the weights of $\Phi _ { p }$ can be represented by the vector $\theta _ { p } \triangleq \left[ \begin{array} { c c c } { \mathrm { v e c } ( V _ { p , 0 } ) ^ { T } } & { \therefore } & { \mathrm { v e c } ( V _ { p , k _ { p } } ) ^ { T } } \end{array} \right] ^ { T } \in$ $\mathbb { R } ^ { \Sigma _ { j = 0 } ^ { k _ { p } } L _ { p , j } L _ { p , j + 1 } }$ . The fully-connected block $\Phi _ { p }$ can be expressed as $\Phi _ { p } = V _ { p , k _ { v } } ^ { T } \varphi _ { p , k _ { p } } .$ V Tp,kp φp,kp , where φp,0 : RLp,0 → RLp,0 $\varphi _ { p , 0 } : \mathbb { R } ^ { \bar { L } _ { p , 0 } }  \mathbb { R } ^ { L _ { p , 0 } }$ and $\varphi _ { p , j } : \mathbb { R } ^ { L _ { p , 0 } } \times \mathbb { R } ^ { \frac { s } { \hbar } L _ { p , 0 } ^ { \textit { \textbf { r } } } \times L _ { p , 1 } } \times \ldots \times \mathbb { R } ^ { L _ { p , j - 1 } \times L _ { p , j } } \to \mathbb { R } ^ { L _ { p , j } }$ $\forall j \in \left\{ 1 , \ldots , k _ { p } \right\}$ denote the recursive relation defined as
