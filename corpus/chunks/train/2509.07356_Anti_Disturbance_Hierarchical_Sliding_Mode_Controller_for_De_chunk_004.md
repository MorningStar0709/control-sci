# 2.1 Hydrodynamic Model

The hydrodynamic force $f _ { w } ( y , t )$ consists of inertial force, drag force, and unmodeled disturbances $\Delta f ( y , t )$ , which is expressed as:

$$f _ {w} (y, t) = f _ {m} (y, t) + f _ {d} (y, t) + \Delta f (y, t) \tag {3}$$

where the inertial force $f _ { m } ( y , t )$ is influenced by acceleration and is given by:

$$f _ {m} (y, t) = \frac {\pi}{4} \rho_ {w} C _ {a} d ^ {2} (\ddot {x} (t) + \ddot {w} (y, t)) \tag {4}$$

The drag force $f _ { d } ( y , t )$ is affected by velocity and is expressed as:

$$f _ {d} (y, t) = \frac {1}{2} \rho_ {w} C _ {d} d (\dot {x} (t) + \dot {w} (y, t)) | \dot {x} (t) + \dot {w} (y, t) | \tag {5}$$

The unmodeled disturbance term $\Delta f ( y , t )$ is introduced to describe the influence of complex external disturbances on the system, which is estimated and compensated in real-time using a neural network observer.

The above dynamic model comprehensively considers the movement of the trolley and the dynamic characteristics of the flexible load. The unmodeled disturbance term reflects the complex external influences. By introducing a neural network observer, the model can compensate for unknown disturbances in real-time, thereby enhancing the system’s robustness and adaptability.

![](image/594bc7e16cb94ac048207a2ae0efc5455b5c2b4c0b0902284767ac10f204969f.jpg)

<details>
<summary>text_image</summary>

Rail
u(t)
Trolley
x(t)
j
i
Water
f_w(y, t)
E_i, m_r
w(y, t)
</details>

Fig. 1: Dynamic model of the deep-sea crane system

Table 1: System Parameters 

<table><tr><td>Parameter</td><td>Description</td></tr><tr><td> $m_r$ </td><td>Load mass (kg)</td></tr><tr><td> $m_t$ </td><td>Trolley mass (kg)</td></tr><tr><td> $d$ </td><td>Load diameter (m)</td></tr><tr><td> $L$ </td><td>Load length (m)</td></tr><tr><td> $Ei$ </td><td>Young’s modulus (GPa)</td></tr><tr><td> $c$ </td><td>Viscous damping coefficient (N·s/m)</td></tr><tr><td> $ρ_w$ </td><td>Water density (kg/m3)</td></tr><tr><td> $C_a$ </td><td>Added mass coefficient</td></tr><tr><td> $C_d$ </td><td>Drag coefficient</td></tr></table>
