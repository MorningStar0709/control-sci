Applying this result, one concludes that in order to have $\begin{array} { r } { \operatorname* { l i m } _ { t \to \infty } \varepsilon ( t + 1 ) = 0 } \end{array}$ , it is sufficient that $H ( z ^ { - 1 } )$ given by (3.149) which characterizes the equivalent linear feedforward path, be a strictly positive real discrete transfer function.

This condition may be restrictive in certain situations. In order to overcome this difficulty, one may consider filtering the a posteriori prediction error before its use in the PAA. One defines the a posteriori adaptation error as:

$$\nu (t + 1) = D (q ^ {- 1}) \varepsilon (t + 1) = \varepsilon (t + 1) + \sum_ {i = 1} ^ {n _ {D}} d _ {i} \varepsilon (t + 1 - i) \tag {3.159}$$

where:

$$D (q ^ {- 1}) = 1 + \sum_ {i = 1} ^ {n _ {D}} d _ {i} q ^ {- i} \tag {3.160}$$

is an asymptotically stable polynomial with $n _ { D } \leq n _ { A } \ ( n _ { A }$ —degree of the polynomial A). Using (3.137), $\nu ( t + 1 )$ can be expressed as:

$$\nu (t + 1) = \frac {D (q ^ {- 1})}{A (q ^ {- 1})} [ - \phi^ {T} (t) \tilde {\theta} (t + 1) ] \tag {3.161}$$

and the parameter adaptation algorithm satisfying the passivity condition will take the form:

$$\hat {\theta} (t + 1) = \hat {\theta} (t) + F \phi (t) \nu (t + 1) \tag {3.162}$$

The equivalent feedback representation associated with the output error predictor using the above $\mathrm { P A A }$ is shown in Fig. 3.9. The equivalent feedback path with input $\nu ( t + 1 )$ ) and output $\tilde { \theta } ^ { T } ( t { + } 1 ) \phi ( t )$ is passive and lim $\iota _ { t  \infty } \nu ( t + 1 ) = 0$ will be assured if:

$$H (z ^ {- 1}) = \frac {D (z ^ {- 1})}{A (z ^ {- 1})} \tag {3.163}$$

is strictly positive real.

To make the algorithm of (3.162) implementable, one has to give a relation between the a posteriori adaptation error given in (3.159) and the a priori adaptation

error defined as:

$$\nu^ {0} (t + 1) = \varepsilon^ {0} (t + 1) + \sum_ {i = 1} ^ {n _ {D}} d _ {i} \varepsilon (t + 1 - i) \tag {3.164}$$

Note that the a posteriori prediction errors $\varepsilon ( t ) , \varepsilon ( t - 1 ) , . .$ . are available at $t + 1$ . Subtracting (3.164) from (3.159), one gets:

$$\nu (t + 1) - \nu^ {0} (t + 1) = \varepsilon (t + 1) - \varepsilon^ {0} (t + 1) = - [ \hat {\theta} (t + 1) - \hat {\theta} (t) ] ^ {T} \phi (t) \tag {3.165}$$

But, from (3.162), one obtains:

$$\hat {\theta} (t + 1) - \hat {\theta} (t) = F \phi (t) v (t + 1) \tag {3.166}$$

and (3.165) becomes:

$$\nu (t + 1) + F \phi (t) \nu (t + 1) = \nu^ {0} (t + 1) \tag {3.167}$$

from which one obtains:
