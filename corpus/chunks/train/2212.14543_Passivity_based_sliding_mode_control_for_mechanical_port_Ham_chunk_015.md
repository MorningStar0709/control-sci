$$\dot {U} = \nabla_ {\sigma} U (\sigma) ^ {\top} (- \Lambda (q, \eta) \nabla_ {\sigma} U (\sigma))\leq - \varepsilon \| \nabla_ {\sigma} U (\sigma) \| ^ {2}\leq - \varepsilon c ^ {2} U (\sigma) ^ {2 \rho}.$$

Thus, (19) holds with $a = a _ { 0 } = 1$ . Next for the sliding modes, i.e, there exists some $i \mathbf { \ ' } _ { \mathbf { S } }$ for which $\sigma _ { i } = 0$ . Suppose there are k sub-sliding modes and $m - k$ sub-reaching modes, i.e., k elements of sliding variable vector σ are enforced to be zero and $m - k$ elements of σ are not yet zero for $1 \leq k \leq m - 1$ . Let us denote $\sigma _ { \mathrm { s m } } \equiv ( \sigma _ { 1 } , \dots , \sigma _ { k } ) ^ { \top } , \sigma _ { \mathrm { r m } } \equiv$ $( \sigma _ { k + 1 } , \dots , \sigma _ { m } ) ^ { \top }$ and suppose $\sigma _ { \mathrm { s m } } = \dot { \sigma } _ { \mathrm { s m } } = 0$ (sliding mode) and $\sigma _ { \mathrm { r m } } \neq 0$ (reaching mode) for simplicity. Then the dynamics of sliding variable (18) is represented as

$$
\frac {\mathrm{d}}{\mathrm{d} t} \binom{\sigma_ {\mathrm{sm}}}{\sigma_ {\mathrm{rm}}} = - \left( \begin{array}{c c} \Lambda_ {1 1} (q, \eta) & \Lambda_ {1 2} (q, \eta) \\ \Lambda_ {1 2} (q, \eta) ^ {\top} & \Lambda_ {2 2} (q, \eta) \end{array} \right) \binom{\nabla_ {\sigma_ {\mathrm{sm}}} U (\sigma)}{\nabla_ {\sigma_ {\mathrm{rm}}} U (\sigma)}
= \binom {0} {*} \tag {20}$$

where ∗ is an arbitrary value. It holds from (20) and Assumption 2 that

$$c U (\sigma) ^ {\rho} \leq \| \nabla_ {\sigma} U (\sigma) \|= \left\| \underbrace {\binom {- \Lambda_ {1 1} (q , \eta) ^ {- 1} \Lambda_ {1 2} (q , \eta)} {I _ {m - k}}} _ {L ^ {k} (q, \eta)} \nabla_ {\sigma_ {\mathrm{rm}}} U (\sigma) \right\|= \| L ^ {k} (q, \eta) \| \| \nabla_ {\sigma_ {\mathrm{rm}}} U (\sigma) \|\leq l _ {\max} ^ {k} \| \nabla_ {\sigma_ {\mathrm{rm}}} U (\sigma) \|, \tag {21}$$

where $l _ { \mathrm { m a x } } ^ { k } \geq 1$ is an upper bound of $\| L ^ { k } ( q , \eta ) \|$ in a neighborhood of the origin. The equivalent sliding mode control system is described as

$$\dot {\sigma} _ {\mathrm{rm}} = - (\underbrace {\Lambda_ {2 2} - \Lambda_ {1 2} ^ {\top} \Lambda_ {1 1} ^ {- 1} \Lambda_ {1 2}} _ {\Lambda / \Lambda_ {1 1} (q, \eta)}) \nabla_ {\sigma_ {\mathrm{rm}}} U (\sigma). \tag {22}$$

Calculating the time derivative of $U ( \sigma )$ with (21) and (22), we obtain
