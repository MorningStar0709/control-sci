However, for the second subsystem, when the initial values $e _ { 1 } \left( t _ { c } \right)$ and $e _ { 2 } \left( t _ { c } \right)$ of sliding variables are in the zones II-1 and IV-1, i.e., in $\{ ( e _ { 1 } ( t _ { c } ) , e _ { 2 } ( t _ { c } ) ) | e _ { 1 } ( t _ { c } ) e _ { 2 } ( t _ { c } ) < 0$ and $| e _ { 1 } \left( t _ { c } \right) | \geq | e _ { 2 } \left( t _ { c } \right) | \}$ , the slow convergence exists for sliding mode (18) with conditions (22) and (23), but it does not happen for sliding mode (18) with conditions (19) and (20). In fact:

1) Slow convergence for sliding mode (18) with conditions (22) and (23): For the second subsystem, when the initial values of sliding variables are in the zones II-1 and IV-1, from (22), we know that $\begin{array} { r } { k _ { 1 } \in \Big ( 0 , \frac { | e _ { 2 } ( t _ { c } ) | } { | e _ { 1 } ( t _ { c } ) | } \Big ) } \end{array}$ $k _ { 1 } \in ( 0 , 1 )$ should be satisfied when $\left| e _ { 1 } \left( t _ { c } \right) \right| \geq \left| e _ { 2 } \left( t _ { c } \right) \right|$ . Therefore, the $e _ { 1 } \left( t \right)$ convergence may be slow due to the convergence law ${ \dot { e } } _ { 1 } \left( t \right) = - k _ { 1 } e _ { 1 } \left( t \right)$ .   
2) Fast convergence for sliding mode (18) with conditions (19) and (20): For the second subsystem, when the initial values of sliding variables are in the zones II-1 and IV-1, from (19), we know that $\begin{array} { r } { k _ { 1 } \in \left( \frac { | e _ { 2 } ( t _ { c } ) | } { | e _ { 1 } ( t _ { c } ) | } , \infty \right) } \end{array}$ . Because $\left| e _ { 1 } \left( t _ { c } \right) \right| \geq \left| e _ { 2 } \left( t _ { c } \right) \right|$ , we need to select $k _ { 1 } \geq 1$ for the convergence law ${ \dot { e } } _ { 1 } \left( t \right) \dot { = } - k _ { 1 } e _ { 1 } \left( t \right)$ to get a fast convergence.
