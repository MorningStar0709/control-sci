Now we will show that $\begin{array} { r } { \mathbb { P } ( E ^ { * } ( x , K , \theta , W ^ { \prime } ) ) = 1 - o _ { T } \left( \frac { 1 } { T ^ { 2 0 } } \right) } \end{array}$ . Fix any $j \leq t .$ . If $j \leq \log ^ { 2 3 } ( T )$ , then $H ( j , x , K , \theta , W ^ { \prime } )$ holds with probability 1 by definition. Now suppose $j > \log ^ { 2 3 } ( T )$ . that Because D has a density bounded by $\begin{array} { r } { \mathbb { P } ( X ( i , x , K , \theta , W ^ { \prime } ) ) \le \frac { 1 2 B _ { P } } { \log ^ { 1 0 } ( T ) } } \end{array}$ ) for all i. Define Mk = Pk−1i=0 1X(i,x,K,θ,W ′) − 12BPlog10(T ) . $\boldsymbol { B } _ { P }$ and $x _ { i } = a ^ { * } x _ { i - 1 } + b ^ { * } u _ { i - 1 } + w _ { i - 1 }$ $\begin{array} { r } { M _ { k } = \sum _ { i = 0 } ^ { k - 1 } { 1 } _ { X ( i , x , K , \theta , W ^ { \prime } ) } - \frac { 1 2 B _ { P } } { \log ^ { 1 0 } ( T ) } } \end{array}$ , we must have For sufficiently large T , $M _ { k }$ is a supermartingale with differences bounded in magnitude by 1. Therefore, by the Azuma–Hoeffding inequality, with probability $1 - o _ { T } ( 1 / T ^ { 2 1 } )$ ,

$$| \{0 \leq i \leq j: X (i, x, K, \theta , W ^ {\prime}) \} | \leq \frac {1 2 B _ {P} (j + 1)}{\log^ {1 0} (T)} + \log (T) \sqrt {j} \leq \frac {2 4 B _ {P} j}{\log^ {1 0} (T)},$$

where the last inequality holds for sufficiently large T and $j > \log ^ { 2 3 } ( T )$ . Therefore, $\mathbb { P } ( H ( j , x , K , \theta , W ^ { \prime } ) ) \ge$ $1 { - o _ { T } ( 1 / T ^ { 2 1 } ) }$ . Taking a union bound over all $\log ^ { 2 \breve { 3 } } ( T ) < j \leq t$ gives that $\mathbb { P } ( E ^ { * } ( x , K , \theta , W ^ { \prime } ) ) =$ $1 - o _ { T } ( 1 / T ^ { 2 0 } )$ .

To prove Lemma 5, will split the range of potential K into two parts, $K \in \left[ \frac { a ^ { * } - 1 + \frac { 1 } { \log ^ { 9 } ( T ) } } { b ^ { * } } , \frac { a } { b } \right]$ and $\begin{array} { r } { K \in \left[ \frac { a - 1 } { b } , \frac { a ^ { * } - 1 + \frac { 1 } { \log ^ { 9 } ( T ) } } { b ^ { * } } \right] } \end{array}$ We will also use the following bounds.

Lemma 7. For any θ ∈ Θ such that ∥θ − θ∗∥∞ ≤ 1log10(T ) , $\theta \in \Theta$ $\begin{array} { r } { \| \theta - \theta ^ { * } \| _ { \infty } \leq \frac { 1 } { \log ^ { 1 0 } ( T ) } } \end{array}$

$$\frac {a - 1}{b} = \frac {a ^ {*} - 1 - O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)}{b ^ {*}}$$

and

$$\frac {a}{b} = \frac {a ^ {*} + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)}{b ^ {*}}.$$

The proof of Lemma 7 can be found in Appendix C.4.

Now we are ready to proceed with the two cases for K.

Case 1: $\begin{array} { r } { K \in \left\lceil \frac { a ^ { * } - 1 + \frac { 1 } { \log ^ { 9 } ( T ) } } { b ^ { * } } , \frac { a } { b } \right\rceil } \end{array}$ + log9(

For $i \leq t ,$ define
