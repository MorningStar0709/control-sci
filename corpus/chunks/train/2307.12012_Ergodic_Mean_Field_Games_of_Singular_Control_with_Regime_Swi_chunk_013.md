(6) for every $( x , i ) \in \mathcal { O } ,$ , and for some $\epsilon _ { 0 } : = \epsilon _ { 0 } ( x ) \in ( 0 , 1 )$ , it holds that

$$\widehat {\mathbb {E}} \left[ \int_ {0} ^ {\infty} e ^ {- c t} \sup _ {x ^ {\prime} \in (x, x + \epsilon_ {0})} \left(| b _ {x x} (\widehat {X} _ {t} ^ {x ^ {\prime}}, Y _ {t} ^ {i}) | \partial_ {x} \widehat {X} _ {t} ^ {x} | _ {x = x ^ {\prime}}\right) d t \right] < \infty . \tag {2.9}$$

In the sequel, we enforce the following requirements on the functions $f , F$ .

Assumption 2.3. $F : \mathbb { R } _ { + } \to \mathbb { R } _ { + } , f : \mathcal { I } \to \mathbb { R } _ { + }$ are such that:

(1) F and f are strictly increasing continuously differentiable functions;   
(2) for $\beta \in ( 0 , 1 )$ , there exists $C > 0$ such that:

$( \mathbf { a } ) ~ | f ( x ) | \leq C ( 1 + | x | ^ { \beta } ) , ~ | F ( x ) | \leq C ( 1 + | x | ^ { \frac { 1 } { \beta } } ) ,$

$( \mathbf { b } ) \ \left| F ( x ) - F ( y ) \right| \leq C ( 1 + | x | + | y | ) ^ { { \frac { 1 } { \beta } } - 1 } | x - y | ;$

(3) lim $\begin{array} { r } { \mathfrak { l } _ { x \uparrow \infty } F ( x ) = \operatorname* { l i m } _ { x \uparrow \overline { { x } } } f ( x ) = \infty . } \end{array}$

Remark 2.2. As a benchmark example we take a profit function

$$\pi (x, \theta) = x ^ {\beta} (\theta^ {- (1 + \beta)} + \kappa_ {*}), \quad F (x) = x ^ {1 / \beta}, \quad f (x) = x ^ {\beta}, \quad \kappa_ {*} > 0, \beta \in (0, 1), \tag {2.10}$$

and dynamics $b ( x , i ) = - \delta _ { i } x$ and $\sigma ( x , i ) = \sigma _ { i } x ,$ , for constants $\delta _ { i } , \sigma _ { i } > 0$ for any $i \in \mathbb { Y } .$ . All the assumptions are easily verified to hold. Such an example relates to a MFG of partially reversible investment. In this regard, X represents the productivity of a representative company. Such productivity can be instantaneously increased via the cumulative investment process $\xi ^ { + }$ and decreased via the cumulative disinvestment process $\xi ^ { - }$ . The function π is the instantaneous profit accrued from the production of a final good, and this depend on the company’s productivity level, as well as on the equilibrium stationary price θ arising from a competing market where symmetric producing companies face isoelastic demand functions a la Spence-Dixit-Stiglitz (cf. Section 3 in [ \` 1], as well as Section 2.1 in [14] for a microeconomic foundation in a setting with regular controls).
