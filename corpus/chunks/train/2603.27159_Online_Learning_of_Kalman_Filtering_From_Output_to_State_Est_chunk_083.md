# D.1 Proof of Theorem 5.1

By Yao’s principle [68, 69], to establish (36) in the theorem, it is enough to demonstrate a random instance of the state estimation problem and prove that the expected regret of any deterministic algorithm when applied to solve such a random problem instance is large.8

First, we describe the random problem instance to be considered. To this end, we define a discrete random variable $r \in \mathbb { R }$ with $\mathbb { P } ( r = 1 ) = \mathbb { P } ( r = 4 ) = \mathbb { P } ( r = - 2 ) = 1 / 3$ , and consider a class of scalar LIT systems (parameterized by the value of r) given by

$$x _ {t + 1} = a x _ {t} + w _ {t} \tag {D.1}y _ {t} = c x _ {t} + v _ {t}.$$

Here, we let $a = 1 / 5 , c = 1 / r , v _ { t } \stackrel { i . i . d . } { \sim } \mathcal { N } ( 0 , \sigma _ { v } ) , w _ { t } = r \tilde { w } _ { t }$ with $\tilde { w } _ { t } \stackrel { i . i . d . } { \sim } \mathcal { N } ( 0 , \sigma _ { w } ) , \sigma _ { v } , \sigma _ { w } \in \mathbb { R } _ { > 0 }$ , and $w _ { t } , \ v _ { t }$ and r are assumed to be independent of each other for all $t \geq 0 ;$ set the initial condition to be $x _ { 0 } = 0$ . Conditioned on different values of r, the LTI system in (D.1) takes different forms. Specifically, denoting $r _ { 1 } = 1 , r _ { 2 } = 4$ and $r _ { 3 } = - 2$ , we obtain that conditioned on $\boldsymbol { r } = \boldsymbol { r } _ { i }$ for any $i \in \{ 1 , 2 , 3 \}$ , system (D.1) satisfies that

$$x _ {t + 1} ^ {\mathbf {s} (\mathrm{i})} = \frac {1}{5} x _ {t} ^ {\mathbf {s} (\mathrm{i})} + r _ {i} \tilde {w} _ {t} \tag {D.2}y _ {t} ^ {\mathbf {s (i)}} = \frac {1}{r _ {i}} x _ {t} ^ {\mathbf {s (i)}} + v _ {t},$$

where we use $x _ { t } ^ { \mathbf { s ( i ) } }$ and $y _ { t } ^ { \mathbf { s ( i ) } }$ to denote the corresponding state and output of the system indexed by $\mathbf { s } ( \mathbf { i } )$ , respectively, and note that we also have $x _ { t } ^ { \mathbf { s ( i ) } } = 0$ . One can now verify that the LTI systems ${ \mathbf s } ( 1 ) , { \mathbf s } ( 2 )$ and ${ \mathfrak { s } } ( 3 )$ are related to each other by some similarity transformation captured by r. Specifically, one can show the following result.

Lemma D.1. Given the same initial condition xs(10 $x _ { 0 } ^ { \mathbf { s } ( 1 ) } = x _ { 0 } ^ { \mathbf { s } ( 2 ) } = x _ { 0 } ^ { \mathbf { s } ( 3 ) } = 0$ = xs(0 = xs(0 , it holds that $( i ) \ x _ { t } ^ { \mathsf { s } ( 2 ) } =$ $r _ { 2 } x _ { t } ^ { \mathbf { s } ( 1 ) } , x _ { t } ^ { \mathbf { s } ( 3 ) } = r _ { 3 } x _ { t } ^ { \mathbf { s } ( 1 ) }$ xt r3xt for all $t \geq 0$ ; and (ii) $y _ { t } ^ { \mathsf { s } ( 1 ) } = y _ { t } ^ { \mathsf { s } ( 2 ) } = y _ { t } ^ { \mathsf { s } ( 3 ) } = y _ { t }$ for all $t \geq 0$ , where $y _ { t }$ is given by (D.1).
