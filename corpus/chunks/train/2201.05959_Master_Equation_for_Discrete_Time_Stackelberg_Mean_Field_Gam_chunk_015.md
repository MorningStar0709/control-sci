# A. Backward Recursion

In this section, we define an equilibrium generating function $\theta = ( \theta _ { t } ^ { f , i } ) _ { i \in \{ l , f \} , t \in [ T ] }$ , where $\theta _ { t } ^ { f , i } : \mathcal { P } ( \mathcal { X } ^ { l } ) \times$ ${ \mathcal { P } } ( \mathcal { X } ^ { f } )  \{ \mathcal { X } ^ { f , i }  { \mathcal { P } } ( { \mathcal { A } } ^ { f , i } ) \}$ and a sequence of functions $( V _ { t } ^ { l } , V _ { t } ^ { f } ) _ { t \in \{ 1 , 2 , . . . T + 1 \} }$ , where $V _ { t } ^ { l } : \mathcal { P } ( \mathcal { X } ^ { l } ) \times \mathcal { P } ( \mathcal { X } ^ { f } ) \times$ $\mathcal X ^ { l } \to \mathbb R$ and $V _ { t } ^ { f , i } : \mathcal { P } ( \mathcal { X } ^ { l } ) \times \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { f , i }  \mathbb { R }$ , in a backward recursive way, as follows.

1. Initialize $\forall \pi _ { T + 1 } \in \mathcal { P } ( \mathcal { X } ^ { l } ) , z _ { T + 1 } \in \mathcal { P } ( \mathcal { X } ^ { f } ) , x _ { T + 1 } ^ { l } \in \mathcal { X } ^ { l } , x _ { T + 1 } ^ { f } \in \mathcal { X } ^ { f } .$

$$V _ {T + 1} ^ {l} \left(\pi_ {T + 1}, z _ {T + 1}, x _ {T + 1} ^ {l}\right) \triangleq 0. \tag {13}V _ {T + 1} ^ {f} \left(\pi_ {T + 1}, z _ {T + 1}, x _ {T + 1} ^ {f}\right) \triangleq 0 \tag {14}$$

2. For $t = T , T - 1 , \dots 1$ , $\forall \pi _ { t } \in \mathcal P ( X ^ { l } ) , z _ { t } \in \mathcal P ( \mathcal X ^ { f } )$ , let $\theta _ { t } [ \pi _ { t } , z _ { t } ]$ be generated as follows. Set $\tilde { \gamma } _ { t } = \theta _ { t } [ \pi _ { t } , z _ { t } ]$ , where $\tilde { \gamma } _ { t } ~ = ~ ( \tilde { \gamma } _ { t } ^ { l } , \tilde { \gamma } _ { t } ^ { f } )$ is the solution of the following fixed-point equation. For a given $\pi _ { t } , z _ { t } , \gamma _ { t } ^ { l }$ , define $\hat { B R } _ { t } ^ { f } ( \pi _ { t } , z _ { t } , \gamma _ { t } ^ { l } )$ as follows,

$$\bar {B} R _ {t} ^ {f} (\pi_ {t}, z _ {t}, \gamma_ {t} ^ {l}) := \{\tilde {\gamma} _ {t} ^ {f}: \forall x _ {t} ^ {f} \in \mathcal {X} ^ {f}, \tilde {\gamma} _ {t} ^ {f} (\cdot | x _ {t} ^ {f}) \in \arg \max _ {\gamma_ {t} ^ {f} (\cdot | x _ {t} ^ {f})}\mathbb {E} ^ {\gamma_ {t} ^ {f} (\cdot | x _ {t} ^ {f}) \gamma_ {t} ^ {l}, z _ {t}, \pi_ {t}} \left\{R _ {t} ^ {f} (z _ {t}, X _ {t} ^ {l}, X _ {t} ^ {f}, A _ {t}) + \delta V _ {t + 1} ^ {f} (F (\pi_ {t}, z _ {t}, \gamma_ {t} ^ {l}, A _ {t} ^ {l}), \phi (\pi_ {t}, z _ {t}, \gamma_ {t} ^ {l}, \tilde {\gamma} _ {t} ^ {f}), X _ {t + 1} ^ {f}) | \pi_ {t}, z _ {t}, x _ {t} ^ {f} \right\} \bigg \}, \tag {15a}$$
