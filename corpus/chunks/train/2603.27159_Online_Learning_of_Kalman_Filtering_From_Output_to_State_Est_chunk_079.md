where (a) follows from (C.16) and − − $\mathrm { ( C . 1 7 ) . ^ { 7 } }$ To obtain (b), we used the relation $\begin{array} { r } { \sum _ { s = s _ { 0 } } ^ { t - 1 } ( t - s - } \end{array}$ $1 ) ( ( 1 + \kappa _ { F } ) / 2 ) ^ { t - s - 2 } \leq 1 / ( 1 - \kappa _ { F } ) ^ { 2 }$ 0proved in Lemma E.8. To obtain (c), we again notice that $R _ { y } = O ( { \sqrt { \log T } } )$ and $\varepsilon _ { 0 } = O ( 1 / T )$ as we argued above.

Combining (C.15) and (C.19), we obtain from (C.14) that for any $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ ,

$$
\begin{array}{l} \left\| \hat {y} _ {t} ^ {\mathrm{KF}} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| \leq (t - s _ {0}) \left(\frac {1 + \gamma_ {F}}{2}\right) ^ {t - s _ {0} - 1} \mathcal {O} \left(\frac {\log^ {1 . 5} T}{T}\right) + \mathcal {O} \left(\frac {\log^ {1 . 5} T}{T}\right) \\ = \mathcal {O} \left(\frac {\log^ {1 . 5} T}{T}\right), \tag {C.20} \\ \end{array}
$$

where the equality follows from the fact $( ( 1 + \gamma _ { F } ) / 2 ) ^ { s _ { 0 } - 1 } \leq 1 / T$ which can also be shown via the choice of $s _ { 0 }$ .

Upper bound on $\left\| \hat { y } _ { t } ^ { \tt K F } \right\|$ for all $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ . For any $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ , we know that

$$\left\| \hat {y} _ {t} ^ {\mathrm{KF}} \right\| \leq \left\| \hat {y} _ {t} ^ {\mathrm{KF}} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| + \left\| \hat {y} _ {t} ^ {\mathrm{SK}} \right\|.$$

As we have shown in the proof of Lemma 4.5 that for any $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ ,

$$\left\| \hat {y} _ {t} ^ {\mathtt {S K}} \right\| \leq \| C \| \| L \| R _ {y} \frac {\kappa_ {F}}{1 - \gamma_ {F}} = \mathcal {O} (\sqrt {\log T}).$$

It follows that for any $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ ,

$$\left\| \hat {y} _ {t} ^ {\mathrm{KF}} \right\| = \mathcal {O} \left(\frac {\log^ {1 . 5} T}{T}\right) + \mathcal {O} (\sqrt {\log T}). \tag {C.21}$$

Now, going back to (C.10) and using the upper bounds in (C.20) and (C.21), we obtain that for any $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ ,

$$\left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| ^ {2} - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{KF}} \right\| ^ {2} = \mathcal {O} \left(\frac {\log^ {1 . 5} T}{T}\right) \left(\mathcal {O} (\sqrt {\log T}) + \mathcal {O} \left(\frac {\log^ {1 . 5} T}{T}\right)\right), \tag {C.22}$$

where we also used $R _ { y } = \mathcal { O } ( \sqrt { \log T } )$ and the above upper bound on $\left\| y _ { t } ^ { \mathbb { S K } } \right\|$ . Summing over all $t \in \{ 2 s _ { 0 } , \ldots , T - 1 \}$ , we get
