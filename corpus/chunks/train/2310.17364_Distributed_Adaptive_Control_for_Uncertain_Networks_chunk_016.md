Proof. From Lemma 4, when $A = \overline { { \mathbf { a } } } I _ { N }$ , we see that $\begin{array} { r } { P _ { p } = \frac { 1 } { 1 - \overline { { \mathbf { a } } } } I _ { N } } \end{array}$ and $\begin{array} { r } { K _ { p } = \frac { 1 } { \overline { { \mathbf { a } } } - 1 } B ^ { \top } } \end{array}$ . Using these values in (27) and solving for γ, we get (28) as it is the condition required for the associated dynamic game to have a finite value. □

We now present the proposed sub-optimal distributed minimax adaptive control algorithm using the strategy given by (20) and also give an upper bound $\overline { { \gamma } } ^ { \dagger }$ for the $\ell _ { 2 }$ gain. That is, our formulation provides a family of (sub-optimal) distributed minimax control policy parameterized by γ, which are guaranteed to exist $\forall \gamma > \overline { { \gamma } } ^ { \dagger } \geq \gamma ^ { \dagger }$ .
