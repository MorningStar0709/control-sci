# 4. Blending of Uncertainty Cancallation Strategies

Overall, increasing $k _ { P }$ with the choice $\mu = 0$ leads to the uncertainty cancellation behaviour that incorporates instantaneous uncertainty rejection alongside model learning. The case of $( k _ { P } , \mu ) = ( 1 0 0 , 0 )$ in Fig. 4 shows that the model prediction output $- u _ { a d } \left( t \right) = \Phi \left( x \left( t \right) \right) ^ { T } \hat { \theta } \left( t \right)$ alone exhibits a large gap from the true uncertainty $\Delta \left( t \right)$ while the sum of two input components $- u _ { a d } \left( t \right) - u _ { c } \left( t \right)$ is close to $\Delta \left( t \right)$ . The result implies that the fast element $u _ { c } \left( t \right)$

contributes more to the control input in the absence of sufficient learned estimate, thus compensating for the lack of learning. In a similar manner to ${ \mathcal { L } } _ { 1 } { - } G { \mathcal { P } }$ in [22], the relative contribution of the learned model output will increase as the model becomes more accurate over time when using a composite adaptation law.

To understand the physical reasons for the observed trend, let us assume the presence of the observer $\Sigma _ { o }$ which is designed to have $A _ { o } = A _ { m } - k _ { P } I .$ . One can easily show that the discrepancy between $x _ { m } \left( t \right)$ and ??ˆ (??) follows $\dot { \tilde { x } } _ { m } = \left( A _ { m } - k _ { P } I \right) \tilde { x } _ { m } + b \Phi ^ { T } \hat { \theta }$ where $\tilde { x } _ { m } \left( t \right) : = x _ { m } \left( t \right) - \hat { x } \left( t \right) = e \left( t \right) - \tilde { x } \left( t \right)$ . Then, assuming the boundedness of Φ (??), increasing $k _ { P }$ results in the decrease of the uniform bound over $\widetilde { x } _ { m } \left( t \right)$ as $\hat { \theta } \left( t \right)$ is bounded as discussed in Sec. B. Therefore, the behaviour of $\Sigma _ { m }$ tends to be closer to the pure observer $\Sigma _ { o }$ used for the instantaneous rejection of lumped uncertainty with a larger $k _ { P }$ .

In summary, the proposed approach tends to adjust its uncertainty cancellation behaviour depending on $\mu .$ . The particular choice $\mu = 0$ tends to blend instantaneous rejection of lumped uncertainty complementarily with online learning of uncertainty model for uncertainty cancellation.
