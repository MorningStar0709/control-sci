$$
A = \left[ \begin{array}{l l} A _ {p} & 0 _ {n _ {p} \times m} \\ \vartheta C _ {p} ^ {\mathrm{T}} & 0 _ {m \times m} \end{array} \right] \in \mathbb {R} ^ {n \times n}, B = \left[ \begin{array}{l} B _ {p} \\ 0 _ {m \times m} \end{array} \right] \in \mathbb {R} ^ {n \times m}, B _ {r} = \left[ \begin{array}{l} 0 _ {n _ {p} \times m} \\ \vartheta I _ {m \times m} \end{array} \right] \in \mathbb {R} ^ {n \times m}, \tag {5}
\bar {C ^ {\mathrm{T}}} = \left[ C _ {p} ^ {\mathrm{T}} 0 _ {m \times m} \right] \in \mathbb {R} ^ {m \times n}, \Psi (t) = \left[ x ^ {\mathrm{T}} (t) u ^ {\mathrm{T}} (t) \right] ^ {\mathrm{T}}, \theta_ {A B} ^ {\mathrm{T}} = \left[ A B \right].$$

The vectortime-invarian $\Psi \left( t \right) \in \mathbb { R } ^ { n + m }$ is considen. The pair be measurable at is controllable if e instant  is contr $t > t _ { r } ^ { + }$ , wherea and ?????? $\theta _ { A B } \in \mathbb { R } ^ { ( n + m ) \times n }$ is $( A , B )$ $\left( A _ { p } , B _ { p } \right)$ $\left\{ \left[ { \frac { A _ { p } } { C _ { p } ^ { \mathrm { T } } } } \ { \cal B } _ { p } \right] \right\} \ne 0$ 0??×??

Considering the plant (4), the goal is to derive a control law that minimizes the cost function:

$$J = \frac {1}{2} \int_ {t _ {r} ^ {+}} ^ {\infty} x ^ {\mathrm{T}} (t) Q x (t) + u ^ {\mathrm{T}} (t) R u (t) d t, \tag {6}$$

where $Q = Q ^ { \mathrm { T } } \in L _ { \infty } \cap \mathbb { R } ^ { n \times n }$ and $R = R ^ { \mathrm { T } } \in L _ { \infty } \cap \mathbb { R } ^ { m \times m }$ are the positive definite weight matrices related to the plant states and control signal respectively, the pair $\left( A , Q ^ { \frac { 1 } { 2 } } \right)$ is detectable.

The following well-known result of the optimal control theory9, which allows one to solve the optimization problem (6) when the matrices ?? and ?? are known, will be used throughout the paper.

Proposition 1. Let (??, ??) be fully controllable, and $\left( A , Q ^ { \frac { 1 } { 2 } } \right)$ be detectable, then the control law, which minimizes (6), is written as:

$$
\begin{array}{l} u ^ {*} (t) = \theta^ {\mathrm{T}} \omega (t) = K _ {x} x (t) + K _ {r} r = - R ^ {- 1} B ^ {\mathrm{T}} P x (t) - R ^ {- 1} B ^ {\mathrm{T}} V r, \\ \quad \text {e} ^ {\mathrm{T}} [ X _ {1}, X _ {2} ] = \mathbb {D} ^ {m} (x | m), \quad (x) = [ T (x) - T ] ^ {\mathrm{T}} = D x | m. \end{array} \tag {7}

\theta^ {\mathrm{T}} = \left[ \begin{array}{c c} K _ {x} & K _ {r} \end{array} \right] \in \mathbb {R} ^ {m \times (n + m)}, \omega (t) = \left[ \begin{array}{c c} x ^ {\mathrm{T}} (t) & r ^ {\mathrm{T}} \end{array} \right] ^ {\mathrm{T}} \in \mathbb {R} ^ {n + m},
$$
