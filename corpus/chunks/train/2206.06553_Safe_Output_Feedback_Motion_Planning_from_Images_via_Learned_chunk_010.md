$$\partial_ {f + B u} W _ {e} (\hat {x}) + W _ {e} (\hat {x}) A (\hat {x}) + A (\hat {x}) ^ {\top} W _ {e} (\hat {x}) - \rho (\hat {x}) C (\hat {x}) ^ {\top} C (\hat {x}) \leq - 2 \lambda_ {e} W _ {e} (\hat {x}) (4)$$

for all $\hat { x } \in D _ { e } \subseteq \mathcal { X } , u \in \mathcal { U }$ . Here, $W _ { e } ( \hat { x } ) = M _ { e } ^ { - 1 } ( \hat { x } )$ . To show that the estimated and true trajectories ${ \hat { x } } ( t )$ and $x ( t )$ converge, we can analyze a nominal “meta-level” virtual system with state q [26], which recovers the nominal $x ( t )$ and ${ \hat { x } } ( t )$ when integrated from initial conditions $q ( 0 ) = x ( 0 )$ and $q ( 0 ) = \hat { x } ( 0 )$ :

$$\dot {q} = f (q) + B u (\hat {x}, x ^ {*}, u ^ {*}) + \frac {1}{2} \rho (\hat {x}) M _ {e} (\hat {x}) C (\hat {x}) ^ {\top} (y - h (q, \theta)). \tag {5}$$

By setting $q \ = \ { \hat { x } } .$ , we recover the estimator dynamics (3); if we set $q \ = \ x ,$ , we recover $\dot { x } = f ( x ) + B u ( \hat { x } , x ^ { * } , u ^ { * } )$ . We can then analyze the convergence of xˆ to x via (5), and [26] shows that if (4) holds, then ${ \hat { x } } ( t )$ contracts at some rate $\gamma \in \left( 0 , \lambda _ { e } \right]$ towards $x ( t )$ . If $M _ { e } ( x )$ and $C ( x )$ are constant, one can show that this holds for $\gamma = \lambda _ { e } .$ .

In particular, $\| x ( t ) - \hat { x } ( t ) \| \le \alpha _ { e } \| x ( 0 ) - \hat { x } ( 0 ) \| e ^ { - \lambda _ { e } t }$ for $\alpha _ { e } > 0$ , and ${ \hat { x } } ( t )$ remains in a tube around x(t) if (3) is perturbed. For polynomial systems of moderate dimension $( n _ { x } ~ \lesssim ~ 1 2 )$ with polynomial observation maps, CCMs and OCMs can be found via convex Sum of Squares (SoS) programs [22]. CCMs/OCMs can also be found for highdimensional non-polynomial systems via learning-based methods (e.g., [5, 23]).
