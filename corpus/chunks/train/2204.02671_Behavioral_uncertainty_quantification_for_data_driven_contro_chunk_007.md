# A. The gap between restricted behaviors

The gap metric plays a pivotal role in control theory [18 – 21] and, in many ways, it reflects the intuitive notion of distance between subspaces. This section introduces the $L _ { - }$ gap metric as a direct finite-time counterpart of the classical gap metric. To this end, we recall a few preliminary notions.

Let $s$ be a normed space with norm k · k. Let $v \in S$ and let W be subspace of S. The distance between v and W is defined as [27, p.7]

$$\delta (v, \mathcal {W}) = \inf _ {w \in \mathcal {W}} \| v - w \|.$$

$\operatorname { I f } \parallel \cdot \parallel _ { 2 }$ is the Euclidean 2-norm in $\mathbb { R } ^ { N }$ , the distance between v and W is the distance between v and its projection onto W, $i . e . , \delta ( v , \mathcal { W } ) = \left. ( I - P _ { \mathcal { W } } ) v \right. _ { 2 }$ , where $P _ { \mathcal { W } }$ is the orthogonal projector onto the subspace W.

Definition 7. [19, p.30] Let V and W be closed subspaces of a Hilbert space . The gap between  and  is defined as

$$\operatorname{gap} _ {\mathcal {H}} (\mathcal {V}, \mathcal {W}) = \| P _ {\mathcal {V}} - P _ {\mathcal {W}} \|, \tag {2}$$

where $P _ { \nu }$ and $P _ { \mathcal { W } }$ are the orthogonal projectors onto V and W, respectively.

The gap between V and W may be expressed as [19, p.30]

$$\operatorname{gap} _ {\mathcal {H}} (\mathcal {V}, \mathcal {W}) = \max \left\{\| (I - P _ {\mathcal {W}}) P _ {\mathcal {V}} \|, \| (I - P _ {\mathcal {V}}) P _ {\mathcal {W}} \| \right\}.$$

In particular, $0 \leq \tt g a p _ { \mathcal { H } } ( \mathcal { V } , \mathcal { W } ) \leq 1$ for all  and . To streamline the exposition, we also recall the notion of directed gap between V and W which is defined as

$$
\overrightarrow{\operatorname{gap}}_{\mathcal{H}}(\mathcal{V},\mathcal{W}) = \sup_{\substack{v\in \mathcal{V}\\ \| v\| = 1}}\delta (v,\mathcal{W}) = \| (I - P_{\mathcal{W}})P_{\mathcal{V}}\|. \tag{3}
$$
