# C. Notations and Definitions

The following notations are adopted in this work. Let R $( \mathbb { R } _ { + } )$ and N $( \mathbb { N } _ { + } )$ denote the sets of real (positive real) and natural (positive natural) numbers, respectively. $\mathbb { R } ^ { n }$ and $\mathbb { R } ^ { n \times n }$ represent the spaces of n-dimensional real vectors and $n \times n$ real matrices, respectively. For a vector $x \in$ $\mathbb { R } ^ { n }$ , denote $\begin{array} { r } { \| \boldsymbol { x } \| _ { 1 } ~ = ~ \sum _ { i = 1 } ^ { n } | \dot { x _ { i } } | , ~ \| \boldsymbol { x } \| ^ { * } = ~ ( \sum _ { i = 1 } ^ { n } x _ { i } ^ { 2 } ) ^ { \frac { 1 } { 2 } } } \end{array}$ , and $\mathbf { i } \mathbf { i } \mathbf { g } ^ { c } ( x ) = [ \mathrm { s i g n } ( x _ { 1 } ) | x _ { 1 } | ^ { c } , \mathrm { s i g n } ( x _ { 2 } ) | x _ { 2 } | ^ { c } , \cdot \cdot \cdot , \mathrm { s i g n } ( x _ { n } ) | x _ { n } | ^ { c } ] ^ { T } ,$ , where $\mathrm { s i g n } ( x _ { i } )$ indicates the sign function of $x _ { i }$ and $c \in \mathbb { R } _ { + }$ . The notation $\otimes$ is the Kronecker operator. $\begin{array} { l c l } { I _ { n } } & { \in } & { \mathbb { R } ^ { n \times n } } \end{array}$ represents the identity matrix. ${ \mathbf { 0 } } _ { n } \in \mathbb { R } ^ { n }$ denotes the vector with all zero entries. Given a matrix $A \in \mathbb { R } ^ { n \times n }$ , let its minimum eigenvalue and maximum eigenvalue be represented as $\lambda _ { m } ( A )$ and $\lambda _ { M } ( A )$ , respectively, and $A > 0 ( \geq 0 )$ indicates that A is positive definite (positive semi-definite).

Definition 1: [39, 40] Consider the system

$$\dot {x} (t) = g (x, t), \tag {1}$$

where $\ b { x } ( t ) \ \in \mathbb { R } ^ { n }$ is its state, $g ( x , t ) \ : \ \mathbb { R } ^ { n } \times \mathbb { R } _ { + } \ \to \ \mathbb { R } ^ { n }$ denotes a nonlinear function that may exhibit discontinuities, and $g ( 0 , t ) = 0$ . Solutions of (1) are interpreted in the context of Filippov’s framework [41]. Let $x ( 0 ) = x _ { 0 }$ and denote the solution as $x ( t , x _ { 0 } )$ . The origin is said to be:

(1) Globally finite-time stable if there is a function ${ \bar { T } } ( x _ { 0 } )$ : $\mathbb { R } ^ { n } \setminus \{ \dot { 0 } \} \setminus \mathbb { R } _ { + }$ such that:
