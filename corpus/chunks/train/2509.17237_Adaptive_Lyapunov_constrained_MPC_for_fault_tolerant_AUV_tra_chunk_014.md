$$
\begin{array}{l} \min _ {\{\boldsymbol {u} _ {k + i} \} _ {i = 0} ^ {N - 1}} \sum_ {i = 0} ^ {N - 1} \left(\| \tilde {\boldsymbol {\eta}} _ {k + i} \| _ {\boldsymbol {Q} _ {\eta}} ^ {2} + \| \boldsymbol {s} _ {k + i} \| _ {\boldsymbol {Q} _ {s}} ^ {2} + \| \boldsymbol {d} _ {k + i} \| _ {\boldsymbol {Q} _ {d}} ^ {2} \right. \\ \left. + \left\| \Delta \boldsymbol {u} _ {k + i} \right\| _ {\boldsymbol {R}} ^ {2}\right) + V \left(\tilde {\boldsymbol {\eta}} _ {k + N}, \boldsymbol {s} _ {k + N}, \boldsymbol {d} _ {k + N}\right) \tag {17} \\ \end{array}
\text { s.t. } \boldsymbol {\xi} _ {k + i + 1} = \boldsymbol {\xi} _ {k + i} + \Delta t \tilde {\boldsymbol {f}} (\boldsymbol {\xi} _ {k + i}, \boldsymbol {u} _ {k + i}), i = 0: N - 1,\boldsymbol {\tau} _ {k + i} = \boldsymbol {T} (\boldsymbol {\theta} _ {k}) \boldsymbol {\Gamma} _ {k} \boldsymbol {u} _ {k + i},\boldsymbol {u} _ {\min} \leq \boldsymbol {u} _ {k + i} \leq \boldsymbol {u} _ {\max}, \| \Delta \boldsymbol {u} _ {k + i} \| _ {\infty} \leq \dot {\boldsymbol {u}} _ {\max} \Delta t,\underbrace {V (\boldsymbol {\xi} _ {k + 1}) - V (\boldsymbol {\xi} _ {k}) \leq - \alpha \| \tilde {\boldsymbol {\eta}} _ {k} \| ^ {2}} _ {}, \alpha > 0,$$

{zLyapunov descent at the first step

$$\boldsymbol {\xi} _ {k + N} \in \mathcal {X} _ {f} = \{\boldsymbol {\xi}: V (\boldsymbol {\xi}) \leq c \}.$$

Terminal set. In this work we choose a small $c > 0$ so that ${ \mathcal { X } } _ { f } ~ = ~ \{ \pmb { \xi } : V ( \pmb { \xi } ) ~ \leq ~ c \}$ lies within the local region of attraction(ROA) of the auxiliary backstepping feedback and respects input/rate constraints; this set is obtained numerically. The first-step descent inequality enforces that the MPC’s initial move decreases V at least by $\bar { \alpha } \| \tilde { \eta } _ { k } \| ^ { 2 }$ , matching the decay of the auxiliary law (13). The terminal set $\chi _ { f }$ is chosen positively invariant under (13) with the faulty mapping ${ \pmb T } ( \pmb \theta ) { \bf \cal \Gamma }$ .
