where, $\bar { u } ( t ) \ : = \ : u ( t ) + \Delta u ( t ) - \Delta c \dot { x } ( t ) - \Delta k x ( t )$ . By identifying dierent erroneous parts, an equivalent 'input' $\overline { { u } } ( t )$ can always be found. As a consequence, systems with erroneous 'input' $\overline { { u } } ( t )$ , which have exactly the same mass and dynamic states, can be identied.

In the frequency domain, though, several methods provide a valid solution when no known (zero or non-zero) input is available, for example the frequency domain decomposition [20, 21]. Nonetheless, the input or the dynamic state estimation are not estimated, while also linear systems are only treated. The frequency domain decomposition especially is oine and it is dicult to automate.

However, a MDOF system with known zero or non-zero input at one DOF is potentially identiable. Assume $\mathbf { C } + \Delta \mathbf { C } , \mathbf { K } + \Delta \mathbf { K }$ , and $u _ { 2 } ( t ) + \Delta u _ { 2 } ( t )$ for the perturbation analysis of a linear 2-DOF system, where $\Delta \mathbf { C } , \Delta \mathbf { K }$ , and $\Delta u _ { 2 } ( t )$ denote the erroneous parts. By doing that, the linear $2 { \cdot } \mathrm { D O F }$ equation of motion, assuming without loss of generality known zero input at DOF 1,

is modied as:

$$
\mathbf {M} \left\{ \begin{array}{l} \ddot {x _ {1}} (t) \\ \ddot {x _ {2}} (t) \end{array} \right\} + \mathbf {C} \left\{ \begin{array}{l} \dot {x _ {1}} (t) \\ \dot {x _ {2}} (t) \end{array} \right\} + \boldsymbol {\Delta} \mathbf {C} \left\{ \begin{array}{l} \dot {x _ {1}} (t) \\ \dot {x _ {2}} (t) \end{array} \right\} + \mathbf {K} \left\{ \begin{array}{l} x _ {1} (t) \\ x _ {2} (t) \end{array} \right\} + \boldsymbol {\Delta} \mathbf {K} \left\{ \begin{array}{l} x _ {1} (t) \\ x _ {2} (t) \end{array} \right\} = \quad \left\{ \begin{array}{c} 0 \\ u _ {2} (t) + \Delta u _ {2} (t) \end{array} \right\} \quad \Longrightarrow
