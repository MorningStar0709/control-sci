where $u ( t ) \triangleq [ u _ { 1 } ( t ) , \ldots , u _ { n } ( t ) ] ^ { \intercal } \in \mathbb { R } ^ { n }$ , and sin $\ b { \lambda } ( t ) \in \mathbb { R } ^ { m }$ is the component-wise sine value of λ(t). Note that the transformation (2) enforces $\lambda ( 0 ) \in { \mathrm { R a n g e } } ( B ^ { \top } )$ . We refer to any λ(0) satisfying this condition as an admissible initial value. For convenience, we use $x ( t ) \triangleq ( \lambda ( t ) , \omega ( t ) ) \in \mathbb { R } ^ { m + n }$ to denote the collection of all the state variables, and skip writing its dependence on t if the context is clear.

Let $L \triangleq B Y _ { b } B ^ { \intercal }$ , and define $\begin{array} { r } { \omega ^ { \infty } \triangleq \frac { \sum _ { i = 1 } ^ { n } p _ { i } } { \sum _ { i = 1 } ^ { n } D _ { i } } , \tilde { p } \triangleq p - } \end{array}$ P i=1 P pini=1 Di , ˜p ≜ p − $\omega ^ { \infty } D \mathbf { 1 } _ { n }$ . According to [19], if

$$\left\| L ^ {\dagger} \tilde {p} \right\| _ {\mathcal {E}, \infty} < 1, \tag {4}$$

where $\begin{array} { r } { \| y \| _ { \mathcal { E } , \infty } \triangleq \operatorname* { m a x } _ { ( i , j ) \in \mathcal { E } } | y _ { i } - y _ { j } | } \end{array}$ , then there exists $\lambda ^ { \infty } \ \in \ \Gamma \ \triangleq \ \{ \lambda | | \ | \lambda _ { k } | < \pi / 2 , \forall k \ = \ 1 , . . . , m \}$ unique in $\Gamma _ { \mathrm { c l } } \triangleq \{ \lambda | | \lambda _ { k } | \leq \pi / 2 , \forall k = 1 , . . . , m \}$ such that

$$\tilde {p} = B Y _ {b} \sin \lambda^ {\infty} \text { and } \lambda^ {\infty} \in \operatorname{Range} (B ^ {\top}).$$

Provided λ(0) ∈ Range $( B ^ { \top } )$ , (3) with $u \equiv 0$ has a unique equilibrium $\left( \lambda ^ { \infty } , \omega ^ { \infty } \mathbf { 1 } _ { n } \right)$ , which is asymptotically stable.
