$$
\begin{array}{l} \| (\tilde {A} _ {d} + E _ {d} (T)) ^ {k} - \tilde {A} _ {d} ^ {k} \| = \| \mathrm{MP} (\tilde {A} _ {d}, E _ {d} (T)) \| \\ \leq \sum_ {\ell = 1} ^ {k} \binom {k} {\ell} \delta_ {T} ^ {\ell} \tilde {\delta} ^ {k - \ell} = (\tilde {\delta} + \delta_ {T}) ^ {k} - \tilde {\delta} ^ {k} = \delta_ {T} \sum_ {\ell = 0} ^ {k - 1} (\tilde {\delta} + \delta_ {T}) ^ {k - \ell - 1} \tilde {\delta} ^ {\ell} \\ \leq \delta_ {T} \sum_ {\ell = 0} ^ {k - 1} (\tilde {\delta} + \delta_ {T}) ^ {k - 1} = \delta_ {T} \cdot k (\tilde {\delta} + \delta_ {T}) ^ {k - 1}, \tag {45} \\ \end{array}
$$

where the first equality in the second row utilizes the binomial theorem and the second equality is to factor $( \tilde { \delta } + \delta _ { T } ) ^ { k } - \tilde { \delta } ^ { k }$ .

Next, substituting (45) into the norm of (43), it yields that

$$
\begin{array}{l} \| E _ {c} (T) \| \leq \sum_ {k = 1} ^ {\infty} \frac {\| (\tilde {A} _ {d} + E _ {d} (T)) ^ {k} - \tilde {A} _ {d} ^ {k} \|}{\tau k} \leq \sum_ {k = 1} ^ {\infty} \frac {(\tilde {\delta} + \delta_ {T}) ^ {k} - \tilde {\delta} ^ {k}}{\tau k} \\ \leq \frac {\delta_ {T}}{\tau} \sum_ {k = 1} ^ {\infty} (\tilde {\delta} + \delta_ {T}) ^ {k - 1} = \frac {\delta_ {T}}{\tau (1 - \tilde {\delta} - \delta_ {T})}. \tag {46} \\ \end{array}
$$

Finally, notice that when $A _ { d } \in { \cal S } _ { d } , \delta _ { T }$ will converge to zero as $T \to \infty$ according to Theorem 1. Hence, it follows that

$$\lim _ {T \to \infty} \| E _ {c} (T) \| \leq \lim _ {T \to \infty} \frac {\delta_ {T}}{\tau (1 - \tilde {\delta} - \delta_ {T})} = 0, \tag {47}$$

which completes the proof.

![](image/f7b99962fccdd4347f8c285785d0b59c55e5fcbd203563f3ceb2fca89aa6fbe5.jpg)

Theorem 3 illustrates that an unbiased $\widehat { A } _ { d } ( T )$ also ensures an unbiased $\widehat { A } _ { c } ( T )$ in the asymptotic sense. This conclusion also applies to the estimator $\widehat { A } _ { d } ^ { s } ( T )$ for the linear growth case, and the proof resembles that of Theorem 3 and is omitted here. As for cases when $\{ y _ { t } \}$ grows in an ultra-linear pattern, the inference error of $\hat { A } _ { c } ( T )$ is upper bounded by a constant. Specifically, the error will also decay to zero asymptotically√ if $\sigma _ { \operatorname* { m i n } } ( Y _ { T } ^ { - } )$ grows faster than $O ( \sqrt { T } )$ .
