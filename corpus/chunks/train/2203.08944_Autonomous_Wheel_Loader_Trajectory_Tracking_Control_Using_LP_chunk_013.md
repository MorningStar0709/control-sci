where, $\begin{array} { r } { { \cal A } ( { x ^ { * } ( k ) } , { u ^ { * } ( k ) } ) = { \cal I } + { \cal T } _ { s } \frac { \partial f } { \partial x } | _ { x ^ { * } ( k ) , u ^ { * } ( k ) } , { \cal B } ( { x ^ { * } ( k ) } , { u ^ { * } ( k ) } ) = { \cal T } _ { s } \frac { \partial f } { \partial u } | _ { x ^ { * } ( k ) , u ^ { * } ( k ) } . } \end{array}$

In the prediction horizon, a sequence of $A _ { k } , B _ { k }$ can be computed from the planned trajectory, and then the MPC problem will be formulated to solve the optimal tracking control action to follow the planned trajectory. For ease of expression, we simply write scheduling parameters of nonlinear functions in the Jacobian matrix as $\rho ( k )$ .
