in which $\vartheta \left( t \right) \in \mathbb { R } ^ { r }$ and $g : \mathbb { R } _ { > 0 } \times \mathbb { R } ^ { r }  \mathbb { R } ^ { r }$ is a continuous function satisfying $g \left( t , 0 \right) = 0$ . Let $\vartheta \left( t , \vartheta _ { 0 } \right)$ be the solution to (1) with the initial value $\vartheta \left( 0 \right) = \vartheta _ { 0 }$ . Then the system (1) is called

1) globally finite-time stable if it is Lyapunov stable and for all $\vartheta _ { 0 } \in \mathbb { R } ^ { r }$ , there is a function $T : \mathbb { R } ^ { r }  \mathbb { R } _ { > 0 }$ , called the settling time function, so that li $\begin{array} { r } { \mathrm { 1 } _ { t  T ( \vartheta _ { 0 } ) } \vartheta ( t , \vartheta _ { 0 } ) = } \end{array}$ 0 and $\vartheta \left( t , \vartheta _ { 0 } \right) = 0 , \forall t \geq T ( \vartheta _ { 0 } )$ .

2) globally prescribed-time stable if it is globally finitetime stable and the settling time function T is a constant which can be assigned arbitrarily.

The Lemma below plays an essential role in deriving the main results in Section 3.

Lemma 6. If there is a continuously differentiable positive definite Lyapunov function $V ( \vartheta ( t ) ) : \mathbb { R } ^ { r }  \mathbb { R } _ { \ge 0 }$ for system (1) so that

$$\dot {V} (\vartheta (t)) \leq - a V (\vartheta (t)) - b \frac {\dot {\varphi} (t , T)}{\varphi (t , T)} V (\vartheta (t)), \forall t \geq 0,$$

with $a \ > \ 0 , \ b \ > \ 0 , \ \varphi \left( t , T \right) \ = \ \left\{ \frac { T ^ { \kappa } } { ( T - t ) ^ { \kappa } } , t \in [ 0 , T ) \right.$ ( (T −t)κ , t ∈ [0, T ) T κ

and $\dot { \varphi } \left( t , T \right) = \left\{ \begin{array} { l l } { \frac { \kappa } { T } \varphi \left( t , T \right) ^ { 1 + \frac { 1 } { \kappa } } , } & { \dot { t } \in \left[ 0 , T \right) } \\ { 0 , } & { t \in \left[ T , + \infty \right) } \end{array} \right.$ where κ is an arbitrary real number fulfilling $\kappa \ > \ 2 , \ T \ > \ 0$ is a finite positive scalar that can be arbitrarily selected, and the derivative of $\varphi \left( t , T \right)$ at $t ~ = ~ T$ is the right-hand one, then the system (1) is globally prescribed-time stable with the settling time being T . In addition, there hold $V \left( \vartheta \left( t \right) \right) \leq$ $\varphi \left( t , T \right) ^ { - b } \exp ^ { - a t } V \left( \vartheta _ { 0 } \right) , \forall t \in \left[ 0 , T \right)$ and $V \left( \vartheta \left( t \right) \right) = 0 .$ , ∀t ∈ $[ T , + \infty )$ .

Proof. The proof could be done by following an analogous procedure to that of Lemma 1 in Reference [29]. □

Remark 1. The above Lemma 6 is a generalization of the Lemma 1 of Reference [29], which is for the case of $b = 2$ only. Moreover, it is worth mentioning that Lemma 6 is valid also for $\kappa > 0 .$ .
