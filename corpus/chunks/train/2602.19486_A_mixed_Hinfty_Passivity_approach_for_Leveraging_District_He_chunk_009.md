$$
\left[ \begin{array}{c c c c c} F ^ {G} (1, 1) & - F ^ {G} (2, 2) & 0 & \dots & 0 \\ 0 & F ^ {G} (2, 2) & - F ^ {G} (3, 3) & \dots & 0 \\ \vdots & \vdots & \vdots & \ddots & \vdots \end{array} \right]. \tag {12}
$$

The error is defined as:

$$
\boldsymbol {e} = \boldsymbol {C T} + \boldsymbol {D h} ^ {\boldsymbol {G}} = \left[ \begin{array}{l} \boldsymbol {e} ^ {\boldsymbol {G}} \\ S i g ^ {H} \end{array} \right], S i g ^ {H} = \boldsymbol {s} _ {\boldsymbol {T}} \boldsymbol {e},. \tag {13}
$$

$\pmb { C } = \left\lceil \mathbf { 1 } ^ { \top } \pmb { F } ^ { D } \right\rceil , D = \left\lceil \mathbf { 1 } ^ { M } \right\rceil$ , and $\begin{array} { r } { \pmb { s } _ { T } = \left[ \mathbf { 0 } ^ { \top } \quad \pmb { 1 } \right] } \end{array}$

3) Augmented DHS: Substituting (1) into (9b):

$$
\begin{array}{l} \dot {T} = (- A + B _ {s} C) T + (B _ {1} + B _ {s} D) h ^ {G} \\ + B _ {2} C o P \gamma^ {E} w ^ {H P} + w ^ {h} (14a) \\ = A _ {e} T + B _ {e} h ^ {G} + B _ {\omega} \omega^ {H P} + w ^ {h}, (14b) \\ \end{array}
$$

where CoP = diag(CoPi), $B _ { \omega } = B _ { 2 } C o P \gamma ^ { E }$ , and $B _ { s } = B _ { 2 } C o P \gamma ^ { H } 1 _ { n ^ { H P } } s _ { T }$ . Integrating $\dot { \pmb { \xi } } = { \pmb { e } }$ obtains:

$$
\left[ \begin{array}{l} \dot {T} \\ \dot {\xi} \end{array} \right] = \underbrace {\left[ \begin{array}{c c} A _ {e} & 0 \\ C & 0 \end{array} \right]} _ {\triangleq A _ {A u g}} \left[ \begin{array}{l} T \\ \xi \end{array} \right] + \underbrace {\left[ \begin{array}{c} B _ {e} \\ D \end{array} \right]} _ {\triangleq B _ {A u g}} h ^ {G} + \underbrace {\left[ \begin{array}{c} B _ {\omega} \\ 0 \end{array} \right]} _ {\triangleq B _ {c l} ^ {(\omega)}} \omega^ {H P} + \underbrace {\left[ \begin{array}{c} I \\ 0 \end{array} \right]} _ {\triangleq B _ {c l} ^ {(h)}} w ^ {h}. \tag {15}
$$

With $\pmb { x } = [ \pmb { T } ^ { \top } \pmb { \xi } ^ { \top } ] ^ { \top }$ , (15) can be rewritten as:

$$\dot {\boldsymbol {x}} = \boldsymbol {A} _ {a u g} \boldsymbol {x} + \boldsymbol {B} _ {a u g} \boldsymbol {h} ^ {G} + \boldsymbol {B} _ {c l} ^ {(\omega)} \boldsymbol {\omega} ^ {H P} + \boldsymbol {B} _ {c l} ^ {(h)} \boldsymbol {w} ^ {h}. \tag {16}$$

The input is $u _ { 2 } = \omega ^ { H P }$ and the output is defined as $y _ { 2 } = p ^ { H }$
