$D _ { 1 } D _ { 1 } ^ { \dagger } )$ , where $K _ { 1 }$ is an arbitrary matrix of appropriate dimensions. Partition $D _ { 1 } ^ { \dagger } = \left[ H _ { X _ { p } } , H _ { U _ { p } } , H _ { Y _ { p } } , H _ { Y _ { f } } \right]$ and $I - D _ { 1 } D _ { 1 } ^ { \dagger } = \left[ \Delta _ { X _ { p } } , \Delta _ { U _ { p } } , \Delta _ { Y _ { p } } , \Delta _ { Y _ { f } } \right]$ in accordance with $[ \Sigma _ { { X _ { p } } } , \Sigma _ { { U _ { p } } } , \Sigma _ { { Y _ { p } } } \bar { , } \Sigma _ { { Y _ { f } } } ]$ . The necessary and sufficient condition for the existence of a stable $\dot { \Sigma _ { X _ { v } } }$ is the detectability of the pair $( X _ { f } H _ { X _ { p } } , \Delta _ { X _ { p } } )$ . $I f \left( \boldsymbol { X } _ { f } H _ { X _ { p } } ^ { \star } , \Delta _ { X _ { p } } \right)$ is observable, one can select a suitable $K _ { 1 }$ to place the eigenvalues $o f \Sigma _ { X _ { p } }$ (poles of the observer) to any desirable positions. This provides a degree of freedom for designing observer convergence.

Remark 6 It is noteworthy that the mathematical form of the data-based observer in Theorem 1 aligns closely with prior data-driven observer frameworks for standard LTI systems $\ [ { \it 2 0 } , { \it 2 1 } ] .$ As seen in the next section, this resemblance also holds in the UIO case. Our analysis reveals a significant unification: the derived data-based observer structure is inherently universal, valid for both singular and nonsingular systems. This finding is particularly appealing, as it allows for observer design based purely on data, bypassing the often challenging step of identifying system singularity $[ i 6 , i 7 ]$

Next, the following Theorem 2 establishes the equivalence between model-based and data-driven observer design frameworks. In particular, it shows that the solvability conditions inferred solely from historical data coincide exactly with those obtained from explicit system representations. To facilitate this equivalence proof, it is mathematically essential to first project the collected data matrices onto the decoupled state coordinates $z _ { 1 } ( k )$ and $z _ { 2 } ( k )$ . Consistent with the temporal constructions of $X _ { p }$ and $X _ { f }$ , we define the corresponding transformed data matrices as follows:

$$Z _ {1 p} = \left[ z _ {1 d} (0) z _ {1 d} (1) \dots z _ {1 d} (T - 1) \right],Z _ {1 f} = \left[ z _ {1 d} (1) z _ {1 d} (2) \dots z _ {1 d} (T) \right],Z _ {2 p} = \left[ z _ {2 d} (0) z _ {2 d} (1) \dots z _ {2 d} (T - 1) \right],Z _ {2 f} = \left[ z _ {2 d} (1) z _ {2 d} (2) \dots z _ {2 d} (T) \right].$$

Theorem 2 (Equivalence of model and datadriven conditions) Under Assumptions 1 and 2, the following statements are equivalent:

(i) There exists a state observer of the form (11) for system $( 1 )$ .   
(ii) There exist matrices $\Sigma _ { { X _ { p } } } , \Sigma _ { { U _ { p } } } , \Sigma _ { { Y _ { p } } }$ , and $\Sigma _ { Y _ { f } }$ such that (20) holds and $\Sigma _ { X _ { p } }$ is Schur stable.

$$
r k \left[ \begin{array}{c} \lambda X _ {p} - X _ {f} \\ U _ {p} \\ Y _ {p} \\ Y _ {f} \end{array} \right] = r k \left[ \begin{array}{c} X _ {p} \\ U _ {p} \\ Y _ {f} \end{array} \right], \forall \lambda \in \mathbb {C}, | \lambda | \geq 1. \tag {26}
$$
