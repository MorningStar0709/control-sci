\text{ and}$ ii)  $\forall S': [( \exists \sigma \in \Sigma_c, S \stackrel{\sigma}{\longrightarrow} S') \implies df(S')]$ </td></tr><tr><td rowspan="2">liveness</td><td> $live(S)$ </td></tr><tr><td>i)  $\forall p \in ap(S), \exists n \geq 0 : [\exists S_1, \ldots, S_n, S' \text{ and } \exists \sigma_1, \ldots, \sigma_n, \sigma \in \Sigma_c :$  $(S \stackrel{\sigma_1}{\longrightarrow} S_1, \ldots, S \stackrel{\sigma_n}{\longrightarrow} S_n \text{ and } S_n \stackrel{p \to q:U}{\longrightarrow} S'orS_n \stackrel{q \to p:U}{\longrightarrow} S')]$ ;
andii)  $\forall S', [( \exists \sigma \in \Sigma_c, S \stackrel{\sigma}{\longrightarrow} S') \implies live(S')]$ </td></tr><tr><td>global</td><td> $G \in \mathcal{G} = \{end\} \cup \{\sigma.G \mid \sigma \in \Sigma_c, G \in \mathcal{G}\} \cup \{G_1 + G_2 \mid G_1, G_2 \in \mathcal{G}\} \cup \{loop t.G \mid G \in \mathcal{G}\} \cup \{t, t_1, \ldots\}$ </td></tr><tr><td>projection/</td><td> $\vdash \subseteq \mathcal{R} \times \mathcal{G}$ </td></tr><tr><td>composition</td><td> $\frac{\{p_i : end \mid i \in I\} \vdash end}{S, p : T, q : T' \vdash G} \quad \frac{\{p_i : t \mid i \in I\} \vdash t}{S, p : \sum_{i \in I} q_i U_i.T_i, q : \sum_{i \in I} p_j U_i.T'_i \vdash \sum_{i \in I} p \to q : U_i.G_i}$  $\frac{\{p_i : t \mid i \in I\}, \{p_j : T_j \mid j \in J\} \vdash G \quad \forall j \in J, T_j \neq t}{\{p_i : end \mid i \in I\}, \{p_j : loop t.T_j \mid j \in J\} \vdash loop t.G}$ </td></tr></table>

TABLE I FORMAL DEFINITIONS FOR AGENT INTERACTION SEMANTICS

Function ${ \mathsf { p } } : { \mathcal { T } }  2 ^ { | { \mathcal { P } } | }$ returns the participants of a local protocol, inductively defined as:

$$\mathrm{p} (\text { end }) = \emptyset ,
\begin{array}{r c l} \mathsf {p} (t) & = & \emptyset , \end{array}
\mathsf {p} (\mathsf {p} _ {!} \mathsf {U}. T) = \{\mathsf {p} \} \cup \mathsf {p} (T),\mathsf {p} (\mathsf {p} _ {?} \mathsf {U}. T) = \{\mathsf {p} \} \cup \mathsf {p} (T),\mathsf {p} (T _ {1} + T _ {2}) = \mathsf {p} (T _ {1}) \cup \mathsf {p} (T _ {2}),\mathsf {p} (\text { loop t. } T) = \mathsf {p} (T)$$

For example, $\mathsf { p } ( \mathit { T } _ { \mathsf { e s t } } ) = \{ \mathsf { s } _ { \mathsf { f } } ^ { 1 } , \mathsf { s } _ { \mathsf { f } } ^ { 2 } , \mathsf { c } \}$ .

Function $| \cdot | : \mathcal { T } $ N return the syntactic size of a local protocol, inductively defined as:
