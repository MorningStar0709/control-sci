$$
\begin{array}{l} \dot {\Gamma} ^ {p *} (t) \leq \left(1 + \rho_ {1} + \gamma \rho\right) \Gamma^ {p *} (t) + \gamma (a + \gamma \rho) m ^ {p} (t) \tag {86} \\ - \left(\gamma \beta_ {1} - \alpha_ {1}\right) \| u [ t ] \| ^ {2} - \left(\gamma \beta_ {2} - \alpha_ {2}\right) u ^ {2} (1, t) - \gamma c W ^ {p} (t), \\ \end{array}
$$

where a is given by (34). The closed-loop system $( 1 ) , ( 2 ) , ( 7 2 ) - ( 7 6 )$ has a unique solution for $t \in [ \tilde { t } _ { i } ^ { p } , \tilde { t } _ { i + 1 } ^ { \tilde { p } } ] , \tilde { \ j } \in \mathbb { N }$ as per Proposition 1, and the system $( 5 0 ) \AA \partial / ( 5 5 )$ has a solution with $m ^ { p } ( i ) \in C ^ { 0 } ( [ \tilde { t } _ { i } ^ { p } , \tilde { t } _ { i + 1 } ^ { p } ] ; \mathbb { R } )$ . Since both sides of (86) are well-behaved in $\mathsf { \Pi } ^ { \prime } \in ( n h , ( n + 1 ) h )$ and $n \in \left[ \tilde { t } _ { j } ^ { p } / h , \tilde { t } _ { j + 1 } ^ { p } / h \right) \cap \mathbb { N } .$ , we can assert the existence of a non-negative function $\iota ( t ) \in C ^ { 0 } \big ( ( \tilde { t } _ { j } ^ { p } , \tilde { t } _ { j + 1 } ^ { p } ) ; \mathbb { R } _ { + } \big )$ such that

$$
\begin{array}{l} \dot {\Gamma} ^ {p *} (t) = \left(1 + \rho_ {1} + \gamma \rho\right) \Gamma^ {p *} (t) + \gamma (a + \gamma \rho) m ^ {p} (t) \\ - \left(\gamma \beta_ {1} - \alpha_ {1}\right) \| u [ t ] \| ^ {2} - \left(\gamma \beta_ {2} - \alpha_ {2}\right) u ^ {2} (1, t) \tag {87} \\ - \gamma c W ^ {p} (t) - \iota (t), \\ \end{array}
$$

for all $t \in ( n h , ( n + 1 ) h )$ and $n \in \left[ \tilde { t } _ { i } ^ { p } / h , \tilde { t } _ { i + 1 } ^ { p } / h \right)$ ∩ N. Additionally, by using (84) to substitute $d ^ { 2 } ( t )$ into (50), we can rewrite the dynamics of $m ^ { p } ( t )$ as follows:

$$
\begin{array}{l} \dot {m} ^ {p} (t) = - \rho \Gamma^ {p *} (t) - (\gamma \rho + \eta) m ^ {p} (t) + \beta_ {1} \| u [ t ] \| ^ {2} \tag {88} \\ + \beta_ {2} u ^ {2} (1, t) + c W ^ {p} (t), \\ \end{array}
$$

for $t \in ( n h , ( n + 1 ) h )$ and $n \in \left[ \tilde { t } _ { j } ^ { p } / h , \tilde { t } _ { j + 1 } ^ { p } / h \right)$ ∩N. Then, combining (87) with (88), we obtain the following ODE system

$$\dot {z} (t) = A z (t) + v (t), \tag {89}$$

where

$$
z (t) = \left[ \begin{array}{c} \Gamma^ {p *} (t) \\ m ^ {p} (t) \end{array} \right],   A = \left[ \begin{array}{c c} 1 + \rho_ {1} + \gamma \rho & \gamma (a + \gamma \rho) \\ - \rho & - (\gamma \rho + \eta) \end{array} \right],

v (t) = \left[ \begin{array}{c} \binom{- (\gamma \beta_ {1} - \alpha_ {1}) \| u [ t ] \| ^ {2} - (\gamma \beta_ {2} - \alpha_ {2}) u ^ {2} (1, t)}{- \gamma c W ^ {p} (t) - \imath (t)} \\ \beta_ {1} \| u [ t ] \| ^ {2} + \beta_ {2} u ^ {2} (1, t) + c W ^ {p} (t) \end{array} \right].
$$
