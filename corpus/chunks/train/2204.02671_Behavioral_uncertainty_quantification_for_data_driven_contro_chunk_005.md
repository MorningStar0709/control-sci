# B. The fundamental lemma

Given a T -length trajectory $w \in \mathbb { R } ^ { q T }$ of a controllable dynamical system $\boldsymbol { B } \in \mathcal { L } ^ { q }$ , one may obtain a non-parametric representation of the restricted behavior using a result first presented in [3], which over time became known as the fundamental lemma. To state this result, we introduce some preliminary notions.

Definition 5. The Hankel matrix of depth $L \in \mathbf { T }$ associated with $w \in \mathbb { R } ^ { q T }$ is defined as

$$
H _ {L} (w) = \left[ \begin{array}{c c c c} w _ {1} & w _ {2} & \dots & w _ {T - L + 1} \\ w _ {2} & w _ {3} & \dots & w _ {T - L + 2} \\ \vdots & \vdots & \ddots & \vdots \\ w _ {L} & w _ {L + 1} & \dots & w _ {T} \end{array} \right] \in \mathbb {R} ^ {(q L) \times (T - L + 1)}.
$$

Definition 6. A vector $u \in \mathbb { R } ^ { m T }$ is persistently exciting of order L if $H _ { L } ( u )$ is full row rank, i.e., rank $H _ { L } ( u ) = m L$ .

Persistency of excitation plays a key role in system identification and adaptive control [1 , 2 , 23]. A necessary condition for $u \in \mathbb { R } ^ { m T }$ to be persistently exciting of order L is that $H _ { L } ( u )$ has at least as many columns as rows, i.e., $T \geq T _ { \mathrm { m i n } } = ( m + 1 ) L - 1$ . We are now ready to state the fundamental lemma [3].

Lemma 2 (Fundamental lemma). Consider a controllable dynamical system $B \in { \mathcal { L } } ^ { q }$ , with input/output partition $w = ( u , y )$ . Assume $w ^ { d } = ( u ^ { d } , y ^ { d } ) \in B | _ { T }$ and $u ^ { d }$ is persistently exciting of order $L + n$ . Then $\boldsymbol { B } | _ { L } =$ im $H _ { L } ( w ^ { d } )$ . y

Lemma 2 is of paramount importance in data-driven control [24]. It provides conditions for the restricted behavior $\boldsymbol { B } | _ { L }$ to be completely characterized by the image of the Hankel matrix $\bar { H _ { L } } ( w ^ { i } )$ . As a result, the subspace im $H _ { L } ( w ^ { d } )$ can be regarded as a non-parametric representation of the dynamical system B, so long as L-length trajectories are considered. The controllability and persistency of excitation assumptions can be removed by focusing on behaviors of fixed complexity and using the rank condition

$$\operatorname{rank} H _ {L} (w ^ {d}) = m L + n. \tag {1}$$

Lemma 3. [24, Corollary 19] Consider a dynamical system $\boldsymbol { B } \in \mathcal { L } ^ { q , c }$ and an associated T -length trajectory $w ^ { d } \in B | _ { T }$ . For $L > l , B | _ { L } = \operatorname { i m } H _ { L } ( w ^ { d } )$ if and only if (1) holds. L

For convenience, in the sequel a T -length trajectory $w ^ { d }$ of a dynamical system $\boldsymbol { B } \in \mathcal { L } ^ { q , c }$ is said to be sufficiently excited of order L if it satisfies the rank condition (1). All of these “low rank” results hold obviously for the deterministic LTI case, but they can also be used to design effective de-noising schemes by low-rank approximation [9].
