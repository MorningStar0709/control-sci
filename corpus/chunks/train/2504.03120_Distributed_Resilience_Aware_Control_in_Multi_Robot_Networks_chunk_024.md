(Part 2) Because $\tilde { \mathcal { Z } } ( \mathbf { w } )$ is rendered forward invariant, $\forall t \geq t _ { 0 }$ (i) $h _ { i } ( \mathbf { x } ( t ) ) = \hat { h } _ { i } ( t ) > 0 \forall i \in \mathcal { L } ,$ , (ii) $h _ { i } ( \mathbf { x } ( t ) ) =$ $\hat { h } _ { i } ( t ) - \epsilon _ { i } > 0 \forall i \in \mathcal { A }$ , and (iii) $h _ { i j } ^ { \mathrm { c o l } } ( { \bf x } ( t ) ) > 0 \ \forall i \ \in \ \mathcal { V }$ $\forall j \in \mathcal { N } _ { i } ( t )$ . Then, by Lemma 2, robots are guaranteed to move such that $\delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) \geq F ^ { \prime }$ without collision $\forall t \geq t _ { 0 } .$ Also, by Proposition 1, resilient consensus is guaranteed.

Remark 3. Our analysis in Theorem 1 assumes malicious robots conduct constant biased attacks on $c _ { i } ( t )$ (but not necessarily on $y _ { i } ( t ) )$ , which are common forms of attacks due to their stealthiness [30]. However, these represent a specific class of malicious attacks. Extending our analysis to broader attack strategies is future work.

We also examine the performance of our controller (20) in the cases where all robots $i \in \nu$ correctly update connectivity levels $c _ { i } ( t )$ according to the nominal update rule (5) below:

Corollary 1. Let all conditions in Theorem 1 hold. Suppose all robots $i \in \mathcal V$ update $c _ { i } ( t )$ according to (5), i.e., $\epsilon _ { i } = 0 ,$ , $\forall i \in A ,$ such that $\mathbf { x } ( t _ { 0 } ) \in \tilde { \mathcal { Z } } ( \mathbf { w } )$ . If each robot $\textit { i } \in \mathcal { V }$ computes its control input $u _ { i }$ using (20), then $\forall t \geq t _ { 0 } ,$ , all robots move such that $( a ) \delta _ { \operatorname* { m i n } } ( { \mathcal { G } } ( t ) ) \geq F ^ { \prime } , ( b )$ no collisions occur, and (c) resilient consensus is guaranteed.

Proof. (Part 1) By the same argument as in (Part 1) of the proof of Theorem 1, the controllers $u _ { i } ^ { * } ( \mathbf { x } ) \ \forall i \in \mathcal { V }$ together render $\tilde { \mathcal { Z } } ( \mathbf { w } )$ forward invariant.
