# 10.9 Problems

$$\lim _ {N \rightarrow \infty} \sum_ {t = 1} ^ {N} u ^ {2} (t) \leq \alpha^ {2} N + k _ {u}; \quad 0 < \alpha^ {2}; k _ {u} < \infty\lim _ {N \rightarrow \infty} \sum_ {t = 1} ^ {N} v (t) \leq \beta^ {2} N + k _ {v}; \quad 0 < \beta^ {2}; k _ {v} < \infty$$

1. Show that for the output error with fixed compensator algorithm (Sect. 5.5) the a posteriori output error $\varepsilon ( t + 1 )$ , the a posteriori adaptation error $\nu ( t + 1 )$ , the predicted output $\hat { y } ( t + 1 )$ and the observation vector $\phi ( t )$ are mean square bounded provided that:

$$H (z ^ {- 1}) = \frac {D (z ^ {- 1})}{A (z ^ {- 1})} - \frac {\lambda_ {2}}{2}; \quad \sup _ {t} \lambda_ {2} (t) \leq \lambda_ {2} < 2$$

is strictly positive real.

2. Do a similar analysis for the other algorithms given in Table 5.1.

10.3 Consider the identification in closed loop of the plant model used in Problem 10.2. The closed loop uses a RST controller which is stable and stabilizes the closed loop. In this case, it is assumed that the disturbance v(t) and the external excitation r(t) are mean square bounded.

1. Show that using the closed-loop output error algorithm (Sect. 9.2), the a posteriori adaptation error $\hat { y } ( t + 1 )$ and the observation vector φ(t) are bounded provided that:

$$H (z ^ {- 1}) = \frac {S (z ^ {- 1})}{P (z ^ {- 1})} - \frac {\lambda_ {2}}{2}; \quad \sup _ {t} \lambda_ {2} (t) \leq \lambda_ {2} < 2$$

is strictly positive real.

2. Do a similar analysis for the other algorithms given in Table 9.1.
