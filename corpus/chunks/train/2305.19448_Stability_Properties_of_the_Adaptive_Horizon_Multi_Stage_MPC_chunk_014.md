Proof. Consider a single $d ^ { r }$ and then follow the proof of Theorem 14 in [9].

![](image/0cb2fa66f3b94085803ae88347299d36df4138cf96f5459ecc8b30153dd1709f.jpg)

Analytical determination of the linearization error and its bound is tedious or impossible. The bounds are determined offline by one-step simulations instead. One-step simulations from random initial states are used to obtain the linearization error numerically by evaluating for each $d ^ { r }$ , the difference $\phi ( x , d ^ { r } ) = f ( x , h _ { { \epsilon } } ^ { r } ( x ) , d ^ { r } ) - A _ { K _ { r } } x , \forall x \in \mathbb { X }$ .

Assumption 3. For a given $d ^ { r }$ , there exists a terminal region of attraction $\mathbb { X } _ { f } ^ { r }$ around $( x _ { f } ^ { r } , u _ { f } ^ { r } )$ such that $\mathbb { X } _ { f } ^ { r } : = \left\{ z \mid \left. z - \right. \right.$ $x _ { f } ^ { r } | \leq c _ { f } ^ { r } \}$ where $c _ { f } ^ { r }$ is the terminal radius corresponding to $d ^ { r }$ .

Assumption 3 means that the terminal region set for each parameter realization is approximated by the interior of an n-sphere with radius $c _ { f } ^ { r } .$ After fitting $M _ { r }$ and $q _ { r }$ for all parameter realizations according to Lemma 1, and Assumption 3 holds, the terminal region radii are computed using Lemma 2.

Lemma 2. Suppose Assumption 3 holds, the terminal radii cr for all $d ^ { r } \in \mathbb { D }$ depend on their corresponding linearization error bounds which are given by:

$$c _ {f} ^ {r} := \left(\frac {- \bar {\sigma} _ {r} \Lambda_ {r} + \sqrt {(\bar {\sigma} _ {r} \Lambda_ {r}) ^ {2} + (\underline {{\lambda}} _ {W _ {r}} - \epsilon_ {L Q}) \Lambda_ {r}})}{\Lambda_ {r} M _ {r}}\right) ^ {\frac {1}{q _ {r} - 1}} \tag {10}$$

where $\bar { \sigma } _ { r }$ is the maximum singular value of $A _ { K _ { r } } , \ \bar { \lambda } _ { W _ { r } }$ and $\underline { { \lambda } } _ { W _ { r } }$ are the maximum and minimum eigenvalues of $W _ { r } : =$ $\begin{array} { r } { \mathcal { Q } + { K _ { r } } ^ { \top } R K _ { r } , \Lambda _ { r } : = \frac { \bar { \lambda } _ { W _ { r } } } { ( 1 - \bar { \sigma } _ { r } ) ^ { 2 } } } \end{array}$ , and $\epsilon _ { L Q } > 0$ λ λ is a small constant: an allowable tolerance for the terminal cost $\psi ( x , d ^ { r } )$ .

Proof. As in Lemma 1, consider a single $d ^ { r }$ and use the proof from [9].

![](image/881003ec4d67bb8f1bfa7d2e6866dbe26ac52d0d38ad30cd3d700c86a4186c68.jpg)
