Thus, $g ( T )$ is a strictly decreasing function, and then the equation $g ( T ) = 0$ has a unique solution $T ^ { * } > 0$ . Moreover, $g ( T ) < 0 { \mathrm { ~ i f ~ } } T > T ^ { * }$ . On the other hand, it follows from (22) that $g ( T _ { i + 1 } ) < 0$ , and then $T _ { i + 1 } = t _ { i + 1 } - t _ { i } > T ^ { * }$ , that is, the inter-event times $\{ t _ { i + 1 } - t _ { i } \} _ { i \in \mathbb { N } }$ are bounded by $T ^ { * } > 0$ from below. This contradicts to the assumption lim $_ { i \to \infty } t _ { i } = \bar { t } < \infty$ . Therefore, system (3) with (6) does not exhibit Zeno behavior when $b \geq \mu - \sigma$ , which completes the proof. □

Remark 1. It should be mentioned that the above proof does not reply on the Lipschitz condition on $\alpha _ { 1 } ^ { - 1 }$ . Nevertheless, $i f \alpha _ { 1 } ^ { - 1 }$ is locally Lipschitz, then t and ˆ  can be obtained explicitly. To be more specific, suppose $\alpha _ { 1 } ^ { - 1 }$ is locally Lipschitz, and we can show stability as follows. Combining (17) and (14) yields

$$\| x (t) \| \leq \min \left\{\| \varphi \| _ {\tau} e ^ {\lambda \left(t - t _ {0}\right)}, L _ {1} M e ^ {- \eta \left(t - t _ {0}\right)} \right\} \quad \text { for all } t \geq t _ {0}, \tag {23}$$

where $L _ { 1 }$ is the Lipschitz constant of $\alpha _ { 1 } ^ { - 1 }$ on interval [0 M]. Consider $\lVert \varphi \rVert _ { \tau } < \operatorname* { m i n } \{ \delta _ { 1 } , \bar { \delta } _ { 2 } \}$ with $\bar { \delta } _ { 2 } = \alpha _ { 1 } ^ { - 1 } ( 2 L _ { 1 } \bar { M } )$ , then $M < 2 \bar { M } , \| \varphi \| _ { \tau } < 2 L _ { 1 } \bar { M } ,$ , and there exists a unique $\hat { t } > t _ { 0 }$ such that

$$\| \varphi \| _ {\tau} e ^ {\lambda (\hat {t} - t _ {0})} = 2 L _ {1} \bar {M} e ^ {- \eta (\hat {t} - t _ {0})},$$

and then t can be derived as ˆ

$$\hat {t} = \frac {\ln \left(\frac {2 L _ {1} \bar {M}}{\| \varphi \| _ {\tau}}\right)}{\lambda + \eta} + t _ {0}.$$

By (23) and the definition of t, we have ˆ

$$
\begin{array}{l} \| x (t) \| <   2 L _ {1} \bar {M} e ^ {- \eta (\hat {t} - t _ {0})} \\ = 2 L _ {1} \bar {M} \exp \left(\frac {- \eta}{\lambda + \eta} \ln \left(\frac {2 L _ {1} \bar {M}}{\| \varphi \| _ {\tau}}\right)\right) \\ = \| \varphi \| _ {\tau} ^ {\frac {\eta}{\lambda + \eta}} \left(2 L _ {1} \bar {M}\right) ^ {\frac {\lambda}{\lambda + \eta}} \tag {24} \\ \end{array}
$$

for all $t \geq t _ { 0 }$ . For any $\varepsilon > 0 ,$ , let $\delta = \operatorname* { m i n } \{ \delta _ { 1 } , \bar { \delta } _ { 2 } , \delta _ { 3 } \}$ with $\delta _ { 3 } = \varepsilon ^ { ( \lambda + \eta ) / \eta } ( 2 L _ { 1 } \bar { M } ) ^ { - \lambda / \eta }$ . For $| | \varphi | | _ { \tau } < \delta ,$ we can derive from (24) that
