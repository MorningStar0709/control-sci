For the reader’s convenience, we introduce 2 lemmas regarding the neural networks connections from [3], that we will use in our final result (for proof see Lemma 4 and Lemma 8 from [3], respectively).

Lemma 4. Consider two feed-forward neural networks $f ^ { [ 1 ] }$ and $f ^ { [ 2 ] }$ . Each network is characterized by $r ^ { [ i ] }$ number of hidden neurons, w[i] maximum width and $\zeta ^ { [ i ] }$ number of layers, with $i = 1 : 2 .$ . Then, there exist a feed-forward neural network, denoted $f ,$ that represents the composition of two neural networks, i. $\mathsf { \bar { e } } _ { \cdot } , f = f ^ { [ \hat { 1 } ] } \circ f ^ { [ 2 ] }$ , with $\zeta = \dot { \zeta } ^ { [ 1 ] } + \zeta ^ { [ 2 ] } - 1 , r = r ^ { [ 1 ] } + r ^ { [ 2 ] }$ ] and $w = \mathrm { m a x } ( w ^ { [ 1 ] } , w ^ { [ 2 ] } )$ .

Lemma 5. Consider l feed-forward neural networks $f ^ { [ 1 ] } , \ldots , f ^ { [ l ] }$ , each network being characterized by $r ^ { [ i ] }$ number of hidden neurons, $w ^ { [ i ] }$ maximum width, $\zeta ^ { [ i ] }$ number of layers and mi the dimension of the NN output, with $i = 1 : l .$ . Then, there exist a feedforward neural network, denoted $f ,$ that represents the parallel connection of the l neural networks, i.e., $f ( x ) = \left[ \begin{array} { c } { { f ^ { [ { \hat { 1 } } ] } ( x ) } } \\ { { \ldots } } \\ { { f ^ { [ l ] } ( x ) } } \end{array} \right]$ with:

$$\zeta = \max _ {i = 1: l} \zeta^ {[ i ]}, \quad w = \sum_ {i = 1} ^ {l} \max (w ^ {[ i ]}, 2 m _ {i})r = \sum_ {i = 1} ^ {l} r ^ {[ i ]} + 2 m _ {i} (\zeta - \zeta^ {[ i ]}).$$

Now, based on the previous lemmas, we prove that any scalar min-max function can be exactly represented by a HTNN.

Theorem 1. Any min-max function $\mathcal { M } : \mathbb { X } \to [ \underline { { u } } , \overline { { u } } ]$ , with X a compact set and $u , { \overline { { u } } } \in \mathbb { R } ,$ , defined by m affine functions $\mathcal { M } _ { j } ( x ) =$ $c _ { j } ^ { T } x + d _ { j } , j = 1 : m , i . e .$ ,

$$\mathcal {M} (x) = \min _ {1 \leq i \leq l} \max _ {j \in \mathscr {C} _ {i}} \mathcal {M} _ {j} (x) = \min _ {1 \leq i \leq l} \max _ {j \in \mathscr {C} _ {i}} c _ {j} ^ {T} x + d _ {j} \tag {15}$$

can be exactly represented by a HTNN whose number of layers $\zeta ,$ , maximum width w and number of hidden neurons r satisfy:

$$\zeta \leq \lceil \log_ {2} l \rceil + \lceil \log_ {2} m \rceil + 1w \leq \mathbb {I} [ l > 1 ] l \max (m, 2)r \leq 2 l (1 + 2 m + \lceil \log_ {2} m \rceil) - 2.$$
