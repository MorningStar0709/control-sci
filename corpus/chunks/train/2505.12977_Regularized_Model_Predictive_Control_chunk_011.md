Remark 2: When inequality constraints are active, problem (16) includes the additional condition $\bar { F } \bar { U _ { k } } \le \bar { g }$ , where ${ \bar { F } } = \left( { F _ { { X } _ { k } } } \oplus { F _ { { U } _ { k } } } \right)$ and $\bar { g } = \left( g _ { X _ { k } } \oplus g _ { U _ { k } } \right)$ , with $F _ { X _ { k } } = \left( I _ { l } \otimes F _ { x } \right)$ , $F _ { U _ { k } } \ = \ \big ( I _ { l } \otimes F _ { u } \big ) , \ g _ { X _ { k } } \ = \ \big ( \mathbf { 1 } l \otimes g _ { x } \big )$ , and $g _ { U _ { k } } \ = \ \left( { \bf 1 } _ { l } \otimes g _ { u } \right)$ . This problem can be solved numerically using fmincon in $\mathbf { M A T L A B } ^ { 2 }$ , and the cost-to-go matrix $P _ { k + l - 1 }$ in (20) contributes to the design of the Re-MPC by updating the terminal cost matrix $\bar { Q } _ { k }$ and the design matrix $H _ { 1 }$ via (15), effectively serving as a regularization term in each shift.

Building on this insight and Theorem 1, we propose the Re-MPC design for the discrete LTI system in (1), summarized in Algorithm3 1. In contrast to traditional MPC [8], Re-MPC adjusts the design matrix $H _ { 1 }$ at each iteration based on the solution of equation (19), thereby enhancing the optimization process in the predictive control. In our

2Specifically, the problem can be solved as arg min $\begin{array} { r l } { \bar { \upsilon } _ { k } \ f ( \bar { U } _ { k } ) } & { { } = } \end{array}$ $\bar { U } _ { k } ^ { \mathsf { T } } H _ { 1 } \bar { U } _ { k } + c t \epsilon$ e with the equality constraint ${ \bar { B } _ { 1 } } X _ { k } ~ = ~ { \bar { A } } x _ { k \mid k } ^ { ~ \sim } + { \bar { B } _ { 2 } } U _ { k }$ rewritten as $\left[ \bar { B } _ { 1 } - \bar { B } _ { 2 } \right] \bar { U } _ { k } \dot { = } \bar { A } \dot { x } _ { k | k }$ , which corresponds to the $A _ { e q } x =$ $b _ { e q }$ formulation in fmincon, while the inequality constraint $\bar { F } \bar { U } _ { k } \ \le \ \bar { g }$ maps directly to $A x \leq b .$

3The operator [⋅]1 outputs only the first entry of the vector [⋅].
