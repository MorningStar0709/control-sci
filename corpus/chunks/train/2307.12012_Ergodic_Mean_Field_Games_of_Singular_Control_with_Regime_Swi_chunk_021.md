We then have the following result. Its proof is somewhat classical (it is based on a Picard iteration).

Theorem 3.2. There exists a unique solution $\xi ^ { * } ( \theta ) \in \mathcal { A } _ { e } t o \mathbf { S P } ( \alpha ( \theta ) , \beta ( \theta ) , x , i )$ . Moreover, $\xi ^ { * }$ admits decomposition $\xi ^ { * } ( \theta ) = \xi ^ { * , + } ( \theta ) - \xi ^ { * , - } ( \theta )$ , where

$$\xi_ {t} ^ {*, +} (\theta) := \sup _ {0 \leq s \leq t} \left(\alpha_ {Y _ {s}} (\theta) - I (X ^ {\xi^ {*} (\theta)}) _ {s} + \xi_ {s} ^ {*, -} (\theta)\right) ^ {+},\xi_ {t} ^ {*, -} (\theta) := \sup _ {0 \leq s \leq t} \left(I (X ^ {\xi^ {*} (\theta)}) _ {s} + \xi_ {s} ^ {*, +} (\theta) - \beta_ {Y _ {s}} (\theta)\right) ^ {+}. \tag {3.18}$$

Proof. See Appendix B.

![](image/f30ac1af35c8c405e886da1849f3affffd5e31cf2e18288deb028f56d72cb43c.jpg)

We now prove that $\xi ^ { * } ( \theta )$ solving $\mathbf { S P } ( \alpha ( \theta ) , \beta ( \theta ) , x , i )$ is optimal for (3.1).

Proposition 3.5. For every $( x , i ) \in { \mathcal { O } } ,$ , the solution $\xi ^ { * } ( \theta ) = \xi ^ { * , + } ( \theta ) - \xi ^ { * , - } ( \theta ) t o { \bf S P } ( \alpha ( \theta ) , \beta ( \theta ) , x , i )$ is optimal for (3.1). Moreover, it holds

$$\overline {{{\lambda}}} (\theta) = \sum_ {i \in \mathbb {Y}} p (i) \lambda (\theta , i) \tag {3.19}$$

with λ as in (3.13).

Proof. Let $T > 0$ and $( x , i ) \in \mathcal { O }$ . Recall that U as in (3.11) is such that $U ( \cdot , i ) \in C ^ { 2 } ( \mathcal { T } ) , \ i \in \mathbb { Y }$ , and $( U , \lambda )$ solves (3.12), with λ as in (3.13). Fixing $\xi \in \mathcal { A } _ { e }$ , we introduce the sequence of F-stopping times $\{ \eta _ { n } \} _ { n \in \mathbb { N } }$ such that $\eta _ { n } : = \operatorname* { i n f } \{ t \geq 0 : \left| X _ { t } ^ { \xi } \right| > n \}$ . Applying It ˆo-Meyer’s formula (in the sense of [11]) to $( U ( X _ { T \wedge \eta _ { n } } , Y _ { T \wedge \eta _ { n } } ; \theta ) ) _ { T \geq 0 }$ , we have:

$$U (X _ {T \wedge \eta_ {n}} ^ {\xi}, Y _ {T \wedge \eta_ {n}}; \theta) = U (x, i; \theta) + \int_ {0} ^ {T \wedge \eta_ {n}} \mathcal {L} _ {(X, Y)} U (X _ {s} ^ {\xi}, Y _ {s}; \theta) d s \tag {3.20}
\begin{array}{l} + \int_ {0} ^ {T \wedge \eta_ {n}} U _ {x} (X _ {s} ^ {\xi}, Y _ {s}; \theta) \sigma (X _ {s} ^ {\xi}, Y _ {s}) d W _ {s} + \int_ {0} ^ {T \wedge \eta_ {n}} U _ {x} (X _ {s} ^ {\xi}, Y _ {s}; \theta) (d \xi_ {s} ^ {+, c} - d \xi_ {s} ^ {-, c}) \\ + \sum_ {0 \leq s \leq T \wedge \eta_ {n}, s \in \Lambda^ {\xi}} \left(U (X _ {s ^ {+}} ^ {\xi}, Y _ {s}; \theta) - U (X _ {s} ^ {\xi}, Y _ {s}; \theta)\right) + M _ {T \wedge \eta_ {n}} - M _ {0} \\ \end{array}
$$
