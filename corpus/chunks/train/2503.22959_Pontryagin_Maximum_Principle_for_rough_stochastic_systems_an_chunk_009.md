The set of α-H¨older continuous two-parameter functions is denoted $C _ { 2 } ^ { \alpha } ( [ 0 , T ] ; V )$ . For a random twoparameter V -valued function $A : \Omega \times \Delta _ { [ s , t ] } \to V$ we set

$$\| A \| _ {\alpha , m, [ s, t ]} := \sup _ {(u, v) \in \Delta_ {[ s, t ]}} \frac {\| A _ {u , v} \| _ {m}}{| v - u | ^ {\alpha}}.$$

The space of all functions for which the above norm is finite is denoted $C _ { 2 } ^ { \alpha } ( [ 0 , T ] ; L ^ { m } )$ and $C _ { 2 } ^ { \alpha } L ^ { m }$ denotes the subspace of all $\mathcal { F } \otimes B ( \Delta _ { [ 0 , T ] } ) / B ( V )$ -measurable elements of $C _ { 2 } ^ { \alpha } ( [ 0 , T ] ; L ^ { m } )$ .

In the following, we usually omit the time interval in our notations and norms if $[ s , t ] = [ 0 , T ]$ and write $\mathbf { \partial } ^ { \cdot } \lesssim _ { \theta } ,$ to indicate that an inequality holds up to a generic constant that depends on a parameter θ.

Definition 2.1. (1) A pair of (deterministic) continuous functions $\eta : = ( \eta , \eta ^ { ( 2 ) } ) : [ 0 , T ] \times \Delta \to \mathbb { R } ^ { d } \times$ （4号 $\mathbb { R } ^ { d \times d }$ is called a α-H¨older continuous rough path, if the following two conditions are satisfied.

$$(a) | \delta \eta | _ {\alpha}, | \eta^ {(2)} | _ {2 \alpha} < \infty ; (b) \eta_ {s, t} ^ {(2)} - \eta_ {s, u} ^ {(2)} - \eta_ {u, t} ^ {(2)} = \delta \eta_ {s, u} \otimes \delta \eta_ {u, t}, f o r a n y s \leq u \leq t.$$

The space of α-H¨older continuous rough paths is denoted $\mathcal { C } ^ { \alpha } ( [ 0 , T ] , \mathbb { R } ^ { d } )$ and is equipped with the following seminorm and metric: for any $\pmb { \eta } = ( \eta , \eta ^ { ( 2 ) } ) , \bar { \pmb { \eta } } = ( \bar { \eta } , \bar { \eta } ^ { ( 2 ) } ) \in \mathcal { C } ^ { \alpha }$ ,

$$\| \left. \boldsymbol {\eta} \right\| _ {\alpha} := | \delta \eta | _ {\alpha} + \sqrt {| \eta^ {(2)} | _ {2 \alpha}}, \quad \rho_ {\alpha} (\boldsymbol {\eta}, \bar {\boldsymbol {\eta}}) = | \delta \eta - \delta \bar {\eta} | _ {\alpha} + | \eta^ {(2)} - \bar {\eta} ^ {(2)} | _ {2 \alpha}.$$

(2). Let $\mathcal { C } _ { g } ^ { 0 , \alpha }$ be the space of α-H¨older geometric rough paths, i.e. the completion of canonically lifted smooth paths $\left( i . e . \mathrm { \it ~ i f ~ } \eta \right.$ is smooth, $\pmb { \eta } : = ( \eta , \int \eta d \eta )$ is the canonical lift of η) under the rough path metric. Note that $\mathcal { C } _ { g } ^ { 0 , \alpha }$ is Polish and ${ \mathcal { C } } _ { g } ^ { 0 , \alpha } \subset { \mathcal { C } } ^ { \alpha }$ (see, e.g. [14] for more information on this space).
