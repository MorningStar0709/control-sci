# 3.1 Proposed Sliding Mode Controller

Consider the dynamical system (3). The sliding surface is constructed based on the output to enforce the con-

straint, and is given by:

$$S (x) = \{x \in \mathbb {R} ^ {n} \mid h (x) = 0 \}. \tag {4}$$

The control objective is to ensure sliding variable i.e. $S ( x ) \ : = \ : h ( x )$ to 0 in finite time, and the closed-loop system is driven toward a stationary point $( x ^ { * } , \lambda ^ { * } )$ satisfying the first-order necessary conditions (2). To enforce the constraint $h ( x ) \to 0$ in $0 < T < \infty .$ we propose the following discontinuous feedback law for $\lambda ( x ) =$ $- \left( J _ { h } ( x ) J _ { h } ( x ) ^ { \top } \right) ^ { - 1 } \left[ J _ { h } ( x ) \nabla \phi ( x ) - K \mathrm { s g n } ( h ( x ) ) \right]$ , where $K \in \mathbb { R } ^ { m \times m }$ is a diagonal positive-definite gain matrix, and $\operatorname { s g n } ( { \mathord { \cdot } } )$ denotes the elementwise sign function. The control input $\lambda ( x )$ consists of: (I) an equivalent control $\lambda _ { e q } ( x )$ to maintain $S ( x ) = 0$ and (II) a switching term $\lambda _ { s w } ( x )$ ) to drive $S ( x ) \stackrel { . } {  } \stackrel { . } { 0 }$ . Thus, $\lambda ( \dot { x } ) = \lambda _ { e q } ( x ) + \bar { \lambda _ { s w } } ( x )$ . The sliding mode control design is based on Utkin’s principle of equivalent control [12], which ensures the existence of sliding motion when the reachability condition is satisfied, as established in [11].

Theorem 2 Consider the controlled dynamical system defined in (3) with sliding manifold (4) defined by the equality constraints. A sliding mode on $\dot { S ( x ) }$ exists if the following conditions hold:

(1) Invariance Condition: There exists an equivalent control input $\lambda _ { e q } ( x ) \in \mathbb { R } ^ { m }$ that renders $S ( x )$ invariant, $\left. J _ { h } ( x ) \dot { x } \right| _ { \lambda = \lambda _ { e a } ( x ) } = 0$ , which yields $\lambda _ { e q } ( x ) =$ $- ( J _ { h } ( x ) J _ { h } ( x ) ^ { \top } ) ^ { - 1 } J _ { h } ( x ) \nabla \phi ( x )$ .   
(2) Reachability Condition: The switching control component is given $b y , \lambda _ { s w } ( x ) = ( J _ { h } ( x ) J _ { h } ( x ) ^ { \top } ) ^ { - 1 } K \mathrm { s g n } ( h ( x ) )$ where $K = \operatorname { d i a g } ( k _ { 1 } , \ldots , k _ { m } )$ , guarantees finite-time convergence to $S ( x )$ with reaching time bounded $\begin{array} { r } { b y , \ t _ { r } \ \leq \ \frac { \sqrt { 2 | | h ( 0 ) | | _ { 2 } } } { k _ { \mathrm { m i n } } } } \end{array}$ , where $k _ { \mathrm { m i n } } ~ = ~ \mathrm { m i n } _ { i } ( k _ { i } ) , i ~ =$ $1 , 2 \ldots m$ .

PROOF. We prove both conditions of Theorem separately.
