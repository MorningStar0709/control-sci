# 2.1 Piece-wise Smooth (PWS) Systems

Consider a PWS system represented in the following form [28],

$$\dot {\xi} = f _ {i} (\xi , u _ {i n t}, u _ {e x t}, t), \xi (t) \in S _ {i} \subseteq \mathbb {R} ^ {n}, \tag {1}$$

where the state vector $\xi ( t ) \in \mathbb { X } \subseteq \mathbb { R } ^ { n } , u _ { i n t } ( t ) \in \mathbb { U } _ { i n t } \subseteq \mathbb { R } ^ { m _ { 1 } }$ and $u _ { e x t } ( t ) \in \mathbb { U } _ { e x t } \subseteq \mathbb { R } ^ { m _ { 2 } }$ , where $u _ { e x t } ( t )$ and $u _ { i n t } ( t )$ are the external and internal input/perturbation, respectively, and are Lebesgue measurable functions. Here, $i \in P$ represents that $i ^ { t h }$ mode is active and ?? is a finite index set defined as $P : = \{ 1 , 2 , 3 , . . . , p \} , p$ is the number of switching subsystems. The vector field in (1) lies in non-intersecting regions named $S _ { i } . \ S _ { i }$ are finite, disjoint, open sets such that $\cup _ { i = 1 } ^ { p } \mathbf { \bar { S } } _ { i } = \mathbb { X } \subseteq \mathbb { R } ^ { n }$ where $\bar { S } _ { i } = S _ { i } \cup \partial S _ { i } . \ \partial S _ { i }$ corresponds to the boundary of region $S _ { i } . ~ { \bar { S } } _ { i }$ is a connected set and the intersection ${ \mathcal { M } } : = { \bar { S } } _ { i } \cap { \bar { S } } _ { j }$ is the switching manifold (see Definition 2.1). Vector fields $f _ { i } ( \xi , u _ { i n t } , u _ { e x t } , t )$ are individually assumed to be smooth or Lipschitz at least locally, i.e., map $\xi \mapsto f _ { i } ( . )$ is continuously differentiable for all $i \in P$ , and each $f _ { i }$ is continuously extended on the boundary $\partial S _ { i }$ . The existence of a solution for (1) is established in the sense of Filipov [29], [30]. The solutions are assumed to be well-posed.

Definition 2.1 [17] Switching manifold M is defined as a zero set of a smooth function $H : \mathbb { R } ^ { n } \to \mathbb { R } , i . e .$

$$\mathcal {M} := \{x \in \mathbb {R} ^ {n} | H (x) = 0 \}, \tag {2}$$

where $0 \in \mathbb { R }$ is a regular value of ??, $\begin{array} { r } { \forall x \in M , \nabla H ( x ) = [ \frac { \partial H ( x ) } { \partial x _ { 1 } } . . . \frac { \partial H ( x ) } { \partial x _ { n } } ] \neq 0 . } \end{array}$ . ????1

If the system is bimodal, the switching manifold M divides the state-space into disjoint regions such as $\{ S _ { 1 } : = x \in \mathbb { R } ^ { n }$ : $H ( x ) > 0 \}$ and $\{ S _ { 2 } : = x \in \mathbb { R } ^ { n } : H ( x ) { \overset { } { < } } 0 \}$ .

We use the notation $\xi _ { x u } ( t )$ to denote the solution of (1) at time ?? under the influence of the input signal ??, starting from the initial state ??.
