# Transpose

The T in $\mathbf { A } ^ { \top }$ denotes transpose, which flips the matrix across its diagonal such that the rows become columns and vice versa.

A symmetric matrix is equal to its transpose.
