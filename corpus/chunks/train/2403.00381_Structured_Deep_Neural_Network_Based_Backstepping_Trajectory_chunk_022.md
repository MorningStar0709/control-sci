Similar to the proof of Theorem 3, if $\begin{array} { r } { \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } \geq \alpha I , } \end{array}$ w e further have

$$
\begin{array}{l} \dot {V} \leq - \frac {\alpha}{2} \| z _ {1} \| ^ {2} + \frac {a ^ {2}}{2} \| z _ {1} \| ^ {2} + \frac {k ^ {2} + d}{2} \\ = - \frac {\alpha^ {2} - a}{2} \| z _ {1} \| ^ {2} + \frac {k ^ {2} + d}{2}. \\ \end{array}
$$

Then we can conclude that the tracking error will converge to the set $\begin{array} { r } { \{ z _ { 1 } | \| z _ { 1 } \| ^ { 2 } \le \frac { k ^ { 2 } + d } { \alpha ^ { 2 } - a } \} } \end{array}$ .

In the preceding proof, we show that the NBS tracking controller can achieve bounded tracking errors and the explicit tracking error bounds can be further obtained by carefully designing Φ. In view of Theorem 4, we can set the hyperparameter m $\begin{array} { r } { \ge \sqrt { b + \frac { b ^ { 2 } } { 2 } + \frac { a } { 2 } + 1 } } \end{array}$ in D to guarantee $D \geq ( b +$ $\frac { b ^ { 2 } } { 2 } + \frac { a } { 2 } + 1 ) { \cal I }$ . Moreover, we can set the hyperparameter $S \ge \alpha I$ in Φ or add the regularizer ReLu $\begin{array} { r } { \bigg ( \lambda _ { \operatorname* { m a x } } \bigg ( \alpha I - \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } \bigg ) \bigg ) } \end{array}$ during training to ensure that $\begin{array} { r } { \frac { \partial ^ { 2 } \dot { \Phi } } { \partial z _ { 1 } ^ { 2 } } \Big | _ { z _ { 1 } = 0 } \overset { \cdot } { \geq } \alpha I . } \end{array}$ . Similarly as in Section III-C, we can further optimize the neural network parameters to achieve better control performance.
