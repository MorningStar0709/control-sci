# B. Kronecker Product Property

The following property of Kronecker product [20] will be used in the paper:

$$\operatorname{vec} (B ^ {\top} X ^ {\top} A ^ {\top}) = (A \otimes B ^ {\top}) \operatorname{vec} (X ^ {\top}). \tag {1}$$
