If there do not exist such parameters P and Λ, then we apply a projection step to construct new controller variables which satisfy the dissipativity condition (lines 8- 11). First, we construct $\hat { \theta } ^ { \prime }$ from the current controller parameters and the most recent $P$ and Λ. Then, in Theta-HatProject, a well-conditioned dissipative $\hat { \theta }$ is constructed that is similar to $\hat { \theta } ^ { \prime }$ . This involves first solving for the minimum projection error $\delta ^ { * } = \mathrm { m i n } _ { \hat { \theta } } \| \hat { \theta } - \hat { \theta } ^ { \prime } \| _ { F }$ s.t. $\hat { \theta } \in \hat { \Theta } ( M _ { \Delta _ { p } } , X )$ . Then, to improve conditioning of the θ that is reconstructed from $\widehat { \theta } , \mathrm { ~ a ~ } \widehat { \theta }$ projection problem is solved again, modified to allow slight suboptimality in projection. In particular, we solve for $\hat { \theta }$ and an auxiliary decision variable $\epsilon _ { R S }$ to maximize ϵRS subject to $\hat { \theta } \in \hat { \Theta } ( M _ { \Delta _ { p } } , X ) , [ \mathit { \Pi } _ { I \ S } ^ { R \ I } ] \succ \epsilon _ { R S }$ , and $\lVert \hat { { \boldsymbol { \theta } } } - \hat { { \boldsymbol { \theta } } } ^ { \prime } \rVert _ { F } ^ { 2 } \leq \beta ^ { 2 } \delta ^ { * 2 }$ . Here, β is a backoff factor that tunes suboptimality, e.g. $\beta = 1$ .1 allows projection error to be at most 10% greater than optimal. The final step is to construct θ from ${ \hat { \theta } } .$ While one option would be to use the reconstruction process described in the proof of Theorem 3, we instead extract $P$ and Λ from $\hat { \theta }$ and then project $\theta ^ { \prime }$ into the set of controllers certified to be dissipative by P and Λ. This ensures that the resulting θ is as close as possible to $\theta ^ { \prime }$ . An alternate training procedure is to train directly on $\hat { \theta }$ by constructing θ from $\hat { \theta }$ through the process described in the proof of Theorem 3, as used in [9]. This leverages the reinforcement learning algorithm to take gradients through the reconstruction process of θ from ${ \hat { \theta } } ,$ and directly improve ${ \hat { \theta } } .$ This avoids the second projection step (line 11 in Algorithm 1), and further is amenable to speedup through barrier function techniques such as those explored in [12]. However, we find Algorithm 1 to perform more consistently in a range of experiments.
