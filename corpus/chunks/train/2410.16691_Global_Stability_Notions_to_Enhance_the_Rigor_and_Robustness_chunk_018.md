Estimates like the ones provided above can be combined with other estimates in order to prove stability properties. Indeed, using (26) we can derive the estimate $\xi ^ { 2 } ( t ) + z ^ { 2 } ( t ) \le \xi ^ { 2 } ( 0 ) + z ^ { 2 } ( 0 )$ for all $t \geq 0$ . Combining this estimate with the estimate $\big | \xi ( t ) \big | \le \exp \Biggl ( - \Biggl ( k - \frac { 1 } { 4 \sigma } - \theta \Biggr ) t \Biggr ) \big | \xi ( 0 ) \big |$ 一 that holds when $k > \frac { 1 } { 4 \sigma } + \theta$ , we can guarantee the following estimate for all $t \geq 0$ when $k > \frac { 1 } { 4 \sigma } + \theta$ : 4

$$
\begin{array}{l} \left| \xi (t) z (t) \right| \leq \exp \left(- \left(k - \frac {1}{4 \sigma} - \theta\right) t\right) \left| \xi (0) \right| \left| x (0) \right| \\ \leq \exp \left(- \left(k - \frac {1}{4 \sigma} - \theta\right) t\right) | x (0) | ^ {2} \\ \end{array}
$$

The above estimate is an estimate of the form (3) and shows UGAOS for system (24) with $k > \frac { 1 } { 4 \sigma } + \theta$ and output $y = \xi z$ 4
