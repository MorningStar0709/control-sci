$$\tau^ {*} = \min \{\alpha \geq 0: S _ {p, q} ^ {\alpha} \cap \mathbb {M} \neq \emptyset , \text {for all} (p, q) \in \mathcal {E} (\mathcal {G} _ {s}) \},\eta^ {*} = \max \{\beta \geq 0: U _ {p, q} ^ {\beta} \cap \mathbb {M} \neq \emptyset , \text {for all} (p, q) \in \mathcal {E} (\mathcal {G} _ {u}) \}.$$

$\eta ^ { * } \ge \eta _ { \mathrm { m i n } } > 0$ $\beta$ increases, the collection  We prove that there ex $U _ { p , q } ^ { \beta }$ s for each such that $( p , q ) \in \mathcal { E } ( \mathcal { G } _ { u } )$ $\cap _ { k \in \mathbb { N } } U _ { p , q } ^ { k } = \emptyset .$ $\beta \geq 0$ $\left( \cap _ { ( p , q ) \in { \mathcal E } ( { \mathcal G } _ { u } ) } U _ { p , q } ^ { \beta } \right) \cap \mathbb { M } = \emptyset$ . Suppose it is not true and $\left( \bigcap _ { ( p , q ) \in { \mathcal E } ( { \mathcal G } _ { u } ) } U _ { p , q } ^ { \beta } \right)$ ∩ M $\neq \emptyset$ for all $\beta \geq 0$ . Then $\{ \mathbb { M } \cap \left( \cap _ { ( p , q ) \in { \mathcal { E } } ( { \mathcal { G } } _ { s } ) } U _ { p , q } ^ { k } \cup \{ \mathbf { O } \} \right) \} _ { k \in \mathbb { N } }$ is a shrinking sequence of non-empty compact sets, hence their intersection is non-empty by the Cantor’s intersection theorem. This leads to a contradiction since $\mathbf { O } \not \in \mathbb { M }$ and $\cap _ { k \in \mathbb { N } } U _ { p , q } ^ { k } = \emptyset$ . Hence $\eta ^ { * }$ is finite.

Furthermore as α increases, the collection $S _ { p , q } ^ { \alpha }$ expands for each $( p , q ) \in \mathcal { E } ( \mathcal { G } _ { s } )$ , and also $\cup _ { \alpha \geq 0 } S _ { p , q } ^ { \alpha } = M _ { n } ( \mathbb { R } ) \setminus \{ \mathbf { O } \}$ p,q . Since M is compact and does not contain O, there exists $\tau _ { \operatorname* { m a x } } \geq 0$ such that $\mathbb { M } \subseteq \cap _ { ( p , q ) \in { \mathcal E } ( { \mathcal G } _ { s } ) } S _ { p , q } ^ { \tau _ { \operatorname* { m a x } } }$ . Clearly $0 \leq \tau ^ { * } \leq \tau _ { \operatorname* { m a x } }$ .

By the arguments given thus far, the reset switched system (2) with signal $\sigma \in$ $\boldsymbol { \mathcal { S } } ^ { \mathcal { G } } [ \boldsymbol { \tau ^ { * } } , \boldsymbol { \eta ^ { * } } ]$ table if . $R _ { ( p , q ) } \in S _ { p , q } ^ { \tau ^ { * } } \cap$ M when $( p , q ) \in \mathcal { E } ( \mathcal { G } _ { s } )$ , and $R _ { ( p , q ) } \in U _ { p , q } ^ { \eta ^ { * } } \cap$ M when $( p , q ) \in \mathcal { E } ( \mathcal { G } _ { u } )$

The following statements are equivalent leading to the expressions which can be used to compute $\tau ^ { * }$ .
