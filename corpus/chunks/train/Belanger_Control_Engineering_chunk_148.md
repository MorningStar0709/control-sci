# Solution

For the state equations used in Example 3.10, with $C = \begin{bmatrix} 0 & 0 & 1 & 0 \end{bmatrix}$ , the system breaks down by inspection into observable and unobservable parts, both controllable, as shown in Figure 3.15. Since the linear position $x$ and velocity $v$ do not couple into the output, they cannot be observed.
