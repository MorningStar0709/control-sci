$$\bar {J} (\theta , C _ {K _ {D _ {U}} ^ {\theta}} ^ {\theta}) > \bar {J} (\theta , C _ {K ^ {\prime}} ^ {\theta}). \tag {87}$$

Let $K ^ { \prime } = K _ { D _ { U } } ^ { \theta } - \epsilon$ , where

$$0 < \epsilon \leq \min \left(\frac {4 B _ {P}}{(\bar {w} + D _ {U}) ^ {2}}, \frac {\min (\bar {w} , D _ {U}) / 2}{(\bar {w} + D _ {U})}\right). \tag {88}$$

We will show that J¯(θ, CθK′) < J¯(θ, CθKθDU ) $\bar { J } ( \theta , C _ { K ^ { \prime } } ^ { \theta } ) < \bar { J } ( \theta , C _ { K _ { D _ { I 7 } } ^ { \theta } } ^ { \theta } )$ which proves the desired contrapositive result.

Because derivative a $\begin{array} { r } { a - b K _ { D _ { U } } ^ { \theta } = \frac { D _ { U } } { D _ { U } + \bar { w } } = 1 - \frac { \bar { w } ^ { \top } } { D _ { U } + \bar { w } } } \end{array}$ − w¯D + ¯w , by Lemma 28 J¯(θ, CuncK ) the function , then Lemm ${ \bar { J } } ( \theta , C _ { K } ^ { \mathrm { u n c } } )$ has a finiteies that the $K = K _ { D _ { U } } ^ { \theta }$ $F _ { \mathrm { o p t } } ( \theta ) < K _ { D _ { U J } } ^ { \theta }$ derivative of ${ \bar { J } } ( \theta , C _ { F } ^ { \mathrm { u n c } } )$ U is positive at $K = K _ { D _ { U } } ^ { \theta }$ U. Therefore, we can take a first order Taylor expansion around the point $K = K _ { D _ { U } } ^ { \theta }$ to get that for sufficiently small ϵ,

$$\bar {J} (\theta , C _ {K ^ {\prime}} ^ {\mathrm{unc}}) - \bar {J} (\theta , C _ {K _ {D _ {U}} ^ {\theta}} ^ {\mathrm{unc}}) \leq - \Omega_ {T} (\epsilon). \tag {89}$$

Because Cunc $C _ { K _ { D _ { U } } ^ { \theta } } ^ { \mathrm { u n c } } = C _ { K _ { D _ { U } } ^ { \theta } } ^ { \theta }$ K θD C θ K θDU Equation (89) implies that

$$\bar {J} (\theta , C _ {K ^ {\prime}} ^ {\mathrm{unc}}) - \bar {J} (\theta , C _ {K _ {D _ {U}} ^ {\theta}} ^ {\theta}) \leq - \Omega_ {T} (\epsilon). \tag {90}$$

Note that in Equations (89) and (90), the LHS is not a function of T . We use the notation $- \Omega _ { T } ( \epsilon )$ to indicate that the LHS is upper bounded by −cϵ for some constant c.

Now we will compare the cost of $C _ { K ^ { \prime } } ^ { \mathrm { u n c } }$ and $C _ { K ^ { \prime } } ^ { \theta }$ using the following lemma. Note that this lemma is stated very generally so that it can also be used in future results.

Lemma 29. For θ, ˆθL29 ∈ Θ, suppose β ≤ 1log2(T ) $\theta , \hat { \theta } _ { \mathrm { L 2 9 } } \in \Theta$ $\begin{array} { r } { \beta \leq \frac { 1 } { \log ^ { 2 } ( T ) } } \end{array}$ satisfies that $\theta \in \hat { \theta } _ { \mathrm { L 2 9 } } \pm \beta$ . Also, suppose $K ^ { \prime }$ satisfies $K _ { D _ { U } } ^ { \theta } - K ^ { \prime } \leq \epsilon$ for some $\epsilon > 0$ . Furthermore, suppose
