# Notations

The operator | · | represents the Euclidean norm for vectors and the induced norm for matrices. $\mathbb { Z } _ { + }$ denotes the set of nonnegative integers. The Kronecker product is represented by ⊗, and the block diagonal matrix operator is denoted by bdiag. $I _ { n }$ denotes the identity matrix of dimension n and $0 _ { n \times m }$ denotes ${ \textbf { a } } n \times m$ zero matrix. vec( $\mathbf { \Phi } _ { A } ^ { \mathrm { ~ } } ) = [  { a _ { 1 } } ^ { \mathrm { T } } ,  { a _ { 2 } } ^ { \mathrm { T } } , . . . ,  { a _ { m } } ^ { \mathrm { T } } ] ^ { \mathrm { T } }$ , where $a _ { i } \in \mathbb { R } ^ { n }$ is the $i ^ { \mathrm { { t h } } }$ column of $A \in \dot { \mathbb { R } } ^ { n \times m }$ . For a symmetric matrix $P = P ^ { \mathrm { T } } \in \mathbb { R } ^ { m }$ ×m, $\mathrm { v e c s } ( P ) = [ p _ { 1 1 } , 2 p _ { 1 2 } , . . . , 2 p _ { 1 m } , p _ { 2 2 } , 2 p _ { 2 3 } , . . . , 2 p _ { m - 1 , m } , p _ { m m } ] ^ { \mathrm { T } } \in$ R 12 m(m+1). $P \succ ( \succeq ) 0$ and $P \prec ( \preceq ) 0$ denote the matrix $P$ is positive definite (semidefinite) and negative definite (semidefinite), respectively. For a column vector v $\mathsf { \bar { \Psi } } \in \mathbb { R } ^ { m } , | v | _ { P } \overset { \cdot } { = } v ^ { \mathrm { T } } P v$ , and $\mathrm { v e c v } ( v ) = \mathrm { v e c s } ( v ^ { \mathrm { T } } v )$ . For a matrix $A \in \mathbb { R } ^ { n \times n } , \sigma ( A )$ denotes the spectrum of A. For any $\lambda \in \sigma ( A )$ , Re(λ) represents the real part of the eigenvalue λ.
