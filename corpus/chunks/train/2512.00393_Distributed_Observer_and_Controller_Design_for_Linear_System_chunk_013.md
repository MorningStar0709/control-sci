# Problem 2. Distributed observer-based linear feedback control

In the case where $\nu ( t ) \equiv 0 ,$ consider a group of controller nodes indexed from 1 to $N _ { c } .$ . The control input term in (6) can be written as

$$B u = \sum_ {\iota = 1} ^ {N _ {c}} B ^ {\iota} u ^ {\iota}, \tag {9}$$

where $u ^ { \iota }$ denotes the control input vector from the ??th controller node. Given any linear state feedback control law $u ^ { \iota } = K ^ { \iota } x$ that can stabilize the system (6), we aim to prove that the following control strategy also achieves stabilization:

$$u ^ {\iota} = K ^ {\iota} \hat {x} _ {i ^ {\prime}}, \tag {10}$$

where $\hat { x } _ { i ^ { \prime } }$ is the state estimate from any one of the aforementioned observer nodes.
