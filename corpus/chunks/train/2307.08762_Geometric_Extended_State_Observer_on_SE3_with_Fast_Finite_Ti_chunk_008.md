with $x ( t ) \in \mathcal { D } \backslash \{ 0 \} , \lambda _ { 1 } , \lambda _ { 2 } > 0 .$ , and $\alpha \in ] 0 , 1 [$ . Then the system (1) is practical finite-time stable (PFTS) at the origin, which means that the solution of (1) will converge to the following set in finite time

$$\left\{x \left| V (x) \leq \min \left\{\frac {\eta}{(1 - \theta_ {0}) \lambda_ {1}}, \left(\frac {\eta}{(1 - \theta_ {0}) \lambda_ {2}}\right) ^ {\frac {1}{\alpha}} \right\} \right. \right\},$$

where $0 < \theta _ { 0 } < 1$ . The settling time T is bounded above as follows:

$$T \leq \max \left\{t _ {0} + \frac {1}{\theta_ {0} \lambda_ {1} (1 - \alpha)} \mathrm{ln} \frac {\theta_ {0} \lambda_ {1} V ^ {1 - \alpha} (x _ {0}) + \lambda_ {2}}{\lambda_ {2}}, t _ {0} + \frac {1}{\lambda_ {1} (1 - \alpha)} \mathrm{ln} \frac {\lambda_ {1} V ^ {1 - \alpha} (x _ {0}) + \theta_ {0} \lambda_ {2}}{\theta_ {0} \lambda_ {2}} \right\}.$$

Lemma 4. Hardy et al. (1952)Let x and y be non-negative real numbers and let $p \in ] 1 , 2 [$ . Then

$$x ^ {\frac {1}{p}} + y ^ {\frac {1}{p}} \geq (x + y) ^ {\frac {1}{p}}. \tag {7}$$

Moreover, the above inequality is a strict inequality if both x and y are non-zero.

Definition 1. Define H : $\mathbb { R } ^ { 3 } \times \mathbb { R } \to \mathrm { S y m } ( 3 )$ , the space of symmetric $3 \times 3$ matrices, as follows:

$$H (x, k) := I - \frac {2 k}{x ^ {\mathrm{T}} x} x x ^ {\mathrm{T}}. \tag {8}$$

Lemma 5. Let $\mu \in \mathbb { R } ^ { n } / \{ 0 \}$ and $\alpha \in ] 0 , 1 / 2 [$ . Consider $\mathcal { D } : \mathbb { R } ^ { n } \setminus \left\{ 0 , { - } \mu \right\}$ and define $\phi (  { \boldsymbol { x } } ) : \mathcal { D } \to \mathbb { R } ^ { + }$ as:

$$\phi (x) := Y (x) ^ {\mathrm{T}} Y (x), \text {where} \tag {9}Y (x) := \| x \| ^ {- 2 \alpha} x - \| x + \mu \| ^ {- 2 \alpha} (x + \mu).$$

The global maximum of ϕ(x) is at $x = - \mu / 2$ .

We provide the proof of Lemma 5 in the appendix.
