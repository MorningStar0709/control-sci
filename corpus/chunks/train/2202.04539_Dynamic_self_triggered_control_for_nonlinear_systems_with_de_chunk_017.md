Theorem 1. Assume there are $n _ { p }$ different parameter sets $\mathcal { P } _ { p } , \ p \ \in \ \{ 1 , . . . , n _ { p } \}$ , for which Condition 1 holds for the same function $\tilde { V } ( x ) , \ x : = \left\{ x | \tilde { V } ( x ) < c _ { \mathcal { X } } \right\}$ and $\varepsilon : =$ $\{ \hat { x } - x | \hat { x } \in \mathcal { X } , x \in \mathcal { X } \}$ for some $c _ { \mathcal { X } } \in \mathbb { R } _ { > 0 }$ . Let Assumption 2 hold. Consider $\mathcal { H } _ { \mathrm { S T C } }$ with $S ( \eta , x )$ and $\Gamma ( x , \eta )$ defined according to (15) and by Algorithm 1. Then the set $\{ ( x , e , \eta , \tau , \tau _ { \mathrm { m a x } } ) : \dot { x } = 0 , e = \dot { 0 } , s = 0 , \eta = 0 \}$ is asymptotically stable with region of attraction

Algorithm 1 Computation of τmax $\tau _ { \mathrm { m a x } }$ $\Gamma ( \breve { x } ( r _ { k } ) , e ( r _ { k } ) , \eta ( r _ { k } ) )$ for the proposed STC mechanism.   
1: $K \leftarrow K(x(r_k), e(r_k), \eta(r_k), c_{\mathcal{X}})$ 2: $c_U = \gamma_{1,1} \phi_{1,1}(0)$ 3: $\bar{h} \leftarrow T_{\max}(\mathcal{P}_1, \lambda, c_U, \tau_{\text{mad}})$ 4: for each $p \in \{2, \ldots, n_p\}$ do
5: $U_p = \tilde{V}(x(r_k)) + \gamma_{1,p} \phi_{1,p}(0) \tilde{W}(1, e(r_k), -e(r_k))$ 6: $h_{\max} = T_{\max}(\mathcal{P}_p, \lambda, c_U, \tau_{\text{mad}})$ 7: if $K \geq U_p$ then
8: if $-\epsilon_p + \epsilon_1 > 0$ then
9: $\bar{h}_p \leftarrow \min \left\{ h_{\max}, \frac{\log(K) - \log(U_p)}{-\epsilon_p + \epsilon_1} \right\}$ 10: else
11: $\bar{h}_p \leftarrow h_{\max}$ 12: end if
13: else
14: $\bar{h}_p \leftarrow 0$ 15: end if
16: if $\bar{h}_p > \bar{h}$ then
17: $\bar{h} \leftarrow \bar{h}_p$ 18: end if
19: end for
20: $\Gamma(x(r_k), e(r_k), \eta(r_k)) \leftarrow \bar{h}$

$$\mathcal {R} := \left\{x \in \mathbb {R} ^ {n _ {x}} | \tilde {V} (x) + \gamma_ {1, 1} \phi_ {1, 1} (0) \tilde {W} (1, - x, x) < c _ {\mathcal {X}} \right\}$$

and for any complete solution $\xi , ~ t _ { k + 2 } - t _ { k } \ge t _ { \operatorname* { m i n } } : =$ $T _ { \mathrm { m a x } } ( \mathcal { P } _ { 1 } , \lambda , \gamma _ { 1 , 1 } \phi _ { 1 , 1 } ( 0 ) , \tau _ { \mathrm { m a d } } )$ holds for all $k \in \mathbb { N } _ { e }$ .

Proof: See Appendix A.2.

Remark 1. The region of attraction R as defined in Theorem 1 is smaller than the set X . This is not surprising as the system state can leave the set R before ˆu is updated for the first time. In fact, under the assumption that $e ( 0 , 0 ) = 0 $ , the region of attraction could be extended to $x ,$ however assuming $e ( 0 , 0 ) = 0$ is in general rather restrictive.

Remark 2. The assumption that $e ( 0 , 0 ) = - x ( 0 , 0 )$ can be relaxed in Theorem 1. In particular, the theorem holds also for all initial conditions with $U _ { 1 } ( \xi ( r _ { 0 } ^ { + } ) ) \le c _ { \mathcal { X } }$ and ${ \hat { x } } ( 0 , 0 ) \in { \mathcal { X } }$ . However, then the region of attraction has a more complex shape, which would complicate to determine the required size of X and E.
