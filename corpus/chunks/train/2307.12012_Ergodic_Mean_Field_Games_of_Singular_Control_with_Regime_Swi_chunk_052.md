$$
\begin{array}{l} \text {(1)} \left(\int_ {0} ^ {t \wedge \tau^ {*} (\theta)} \pi_ {x} (\widehat {X} _ {t}, \theta) d t + e ^ {\int_ {0} ^ {t \wedge \tau^ {*} (\theta)} b _ {x} (\widehat {X} _ {t}, Y _ {t}) d t} v (\widehat {X} _ {t \wedge \tau^ {*} (\theta)}, Y _ {t \wedge \tau^ {*} (\theta)})\right) _ {t \geq 0} \text {   is   a   } \mathbb {F} \text {-submartingale}; \\ \left. \left(\int_ {0} ^ {t \wedge \sigma^ {*} (\theta)} \pi_ {x} \left(\widehat {X} _ {t}, \theta\right) d t + e ^ {\int_ {0} ^ {t \wedge \sigma^ {*} (\theta)} b _ {x} \left(\widehat {X} _ {t}, Y _ {t}\right) d t} v \left(\widehat {X} _ {t \wedge \sigma^ {*} (\theta)}, Y _ {t \wedge \sigma^ {*} (\theta)}\right)\right) _ {t \geq 0} \text {is a F -supermartingale}; \right. (2) \\ \left. \left(\int_ {0} ^ {t \wedge \tau^ {*} (\theta) \wedge \sigma^ {*} (\theta)} \pi_ {x} (\widehat {X} _ {t}, \theta) d t + e ^ {\int_ {0} ^ {t \wedge \tau^ {*} (\theta) \wedge \sigma^ {*} (\theta)} b _ {x} (\widehat {X} _ {t}, Y _ {t}) d t} v \left(\widehat {X} _ {t \wedge \tau^ {*} (\theta) \wedge \sigma^ {*} (\theta)}, Y _ {t \wedge \tau^ {*} (\theta) \wedge \sigma^ {*} (\theta)}\right)\right) _ {t \geq 0} \right. (3) \\ \end{array}
$$

Proof of Proposition 3.3. The proof is organized in several steps.

Step 1: We start by proving that $v ( \cdot , i ; \theta ) \in C ^ { 0 } ( \mathcal { T } )$ , for any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ . Letting $( x , i , \theta ) \in$ $\mathcal { O } \times \mathbb { R } _ { + }$ and $\epsilon \in ( 0 , 1 )$ , we recall the optimal stopping times $\tau ^ { * } ( \theta ) = \operatorname* { i n f } \{ t \geq 0 : \widehat { X } _ { t } \leq \alpha _ { Y _ { t } } ( \theta ) \}$ , $\sigma ^ { * } ( \theta ) = \operatorname* { i n f } \{ t \geq 0 : \widehat { X } _ { t } \geq \beta _ { Y _ { t } } ( \theta ) \} \widehat { \mathbb { P } } _ { ( x , i ) ^ { - } \mathbf { a } . \mathbf { S } }$ . and define $\sigma _ { \epsilon } ^ { * } ( \theta ) : = \operatorname* { i n f } \{ t \geq 0 : \widehat { X } _ { t } \geq \beta _ { Y _ { t } } ( \theta ) \}$ $\widehat { \mathbb { P } } _ { ( x + \epsilon , i ) } .$ , analogously $\tau _ { \epsilon } ^ { * } ( \theta )$ . Then we have that
