where $\boldsymbol { h } ^ { G } , \boldsymbol { h } ^ { P } .$ , and $h ^ { L }$ denote the non-pump heat source generation, HP generation, and load vectors, respectively. We define

$$
\boldsymbol {A} = \frac {A _ {h}}{V}, B _ {1} = \frac {I}{V} \left[ \begin{array}{c} I \\ 0 \\ 0 \\ 0 \end{array} \right], B _ {2} = \frac {I}{V} \left[ \begin{array}{c} 0 \\ I \\ 0 \\ 0 \end{array} \right], w ^ {h} = \frac {I}{V} \left[ \begin{array}{c} 0 \\ 0 \\ - h ^ {L} \\ 0 \end{array} \right].
$$

2) Energy-sharing steady-state objectives: The steady-state economic dispatch for DHSs under given $\bar { \boldsymbol { h } } ^ { P }$ and $\bar { w } ^ { h }$ is modeled as two optimization problems.

$$\mathbf {E 1}: \min _ {\boldsymbol {h} ^ {\boldsymbol {G}} \in \mathbb {R}, \boldsymbol {T} \in \mathbb {R}} \Phi_ {1} = \frac {1}{2} \boldsymbol {h} ^ {\boldsymbol {G} ^ {\top}} \boldsymbol {F} ^ {\boldsymbol {G}} \boldsymbol {h} ^ {\boldsymbol {G}}, \tag {10a}\text { s.t. } A T = B _ {1} h ^ {G} + B _ {2} \bar {h} ^ {P} + \bar {w} ^ {h}, \tag {10b}$$

1Note that higher order generation dynamics that satisfy this property can also be considered.

where $\Phi _ { 1 }$ denotes the non-pump sources’ heat generation cost, with $\boldsymbol { F } ^ { G } = \mathrm { d i a g } \{ f _ { i } ^ { G } \}$ , where $f _ { i } ^ { \dot { G } } > ^ { \dot { 0 } }$ is the cost coefficient of source i. The temperature deviation cost is minimized in E2, formulated as:

$$\mathbf {E 2}: \min _ {z \in \mathbb {R}, T \in \mathbb {R}} \Phi_ {2} = \frac {1}{2} T ^ {\top} F ^ {D} T, \tag {11a}s. t. \boldsymbol {T} = \boldsymbol {A} ^ {\dagger} (\boldsymbol {B _ {1}} \boldsymbol {h} ^ {G ^ {*}} + \boldsymbol {B _ {2}} \bar {\boldsymbol {h}} ^ {P} + \bar {\boldsymbol {w}} ^ {h}) + \alpha \mathbf {1}, \tag {11b}$$

where $A ^ { \dagger }$ denotes the Moore–Penrose pseudoinverse of A, and $\boldsymbol { F } ^ { D } \ = \ \mathrm { d i a g } \{ F _ { i } ^ { D } \}$ , with $F _ { i } ^ { D } > 0 ;$ , is the temperature deviation penalty coefficient associated with node or edge i.

Lemma 3. (Optimality condition for E1 and E2 [3]) If the DHS (9b) achieves equilibrium at $\pmb { T } ^ { * }$ and $h ^ { \breve { G } ^ { * } }$ , and satisfies $\bar { F } ^ { M } h ^ { G * } = 0$ and $\mathbf { 1 } ^ { \top } F ^ { D } \mathbf { T ^ { * } } = 0 .$ , then it uniquely2 solves the optimization problems E1 and E2, where ${ \pmb F } ^ { M }$ is defined by the following matrix:
