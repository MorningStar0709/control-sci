# A. Neural Backstepping Tracking Control Design

Suppose that the reference trajectory is $\pmb q ^ { d } ( t )$ , which is continuously differentiable. We aim to design a controller such that the state $\pmb q ( t )$ follows $q ^ { d } ( t )$ . We define the following errors

$$z _ {1} = q - q ^ {d}, z _ {2} = \dot {q} - \phi , \tag {8}$$

where $\phi$ is a virtual signal to be designed. The controller u and the virtual signal $\phi$ are defined as

$$
\begin{array}{l} \boldsymbol {u} = \boldsymbol {G} (\boldsymbol {q}) + \boldsymbol {M} (\boldsymbol {q}) \dot {\phi} + \boldsymbol {C} (\boldsymbol {q}, \dot {\boldsymbol {q}}) \phi \\ - \frac {\partial \Phi (z _ {1} ; \theta_ {1} , S)}{\partial z _ {1}} - D (z _ {2}; \theta_ {2}, m) z _ {2}, \tag {9} \\ \end{array}
\phi = \dot {\boldsymbol {q}} ^ {d} - \frac {\partial \Phi (\boldsymbol {z _ {1}} ; \boldsymbol {\theta_ {1}} , \boldsymbol {S})}{\partial \boldsymbol {z _ {1}}}.$$

In the above control design, Φ is constructed as

$$\Phi (z _ {1}; \theta_ {1}, S) = \psi (z _ {1}; \theta_ {1}) + z _ {1} ^ {\top} S z _ {1}, \tag {10}$$

where S is a positive definite matrix; $\psi ( z _ { 1 } ; \theta _ { 1 } )$ is a eters FICNN (6) with input $W _ { 1 : k - 1 } ^ { ( y ) }$ in $\psi ( z _ { 1 } ; \theta _ { 1 } )$ ${ z } _ { 1 }$ and parameters be the output of the ReLu function, $\pmb { \theta } _ { 1 }$ . We let the paramwhose input is some free parameters. Moreover, we set $b _ { i }$ in $\psi \big ( z _ { 1 } ; \theta _ { 1 } \big )$ to 0, which ensures ${ \pmb { \psi } } ( 0 ) = 0$ . Furthermore, by selecting sReLu [34] as the activation function of $\psi$ , we can ensure that $\psi ( z _ { 1 } ) \geq 0 ,$ , which further guarantees that Φ has only one minimum at $z _ { 1 } = 0 ,$ , satisfying $\Phi ( 0 ) = 0$ . This design ensures that Φ is strongly convex with respect to the input ${ z } _ { 1 }$ .
