Example 1. Let us consider a simple static model with $A = I , n = 1$ and $\begin{array} { r } { \mathcal { O } = C = \alpha \left( 1 \quad 1 \quad 1 \right) ^ { \top } } \end{array}$ for any $\alpha \in \mathbb { R } , i . e .$ , we have 3 equivalent sensors. We assume that one of them is under attack. If we know that $h = 1$ , the identification of the attack is trivial: the sensor that provides a different measurement is clearly under attack and we can recover the state from the other two sensors. The irrepresentable condition well captures this resilience; in fact, we have O†⊤¯ $\begin{array} { r } { \mathcal { O } _ { \bar { \mathcal { S } } } ^ { \dagger ^ { \top } } = \frac { 1 } { 2 \alpha } \left( 1 \quad 1 \right) ^ { \top } } \end{array}$ and $\mathcal { O } _ { s } ^ { \top } = \alpha _ { : }$ , thus $\begin{array} { r } { \left. \mathcal { O } _ { \bar { s } } ^ { \dagger ^ { \top } } \mathcal { O } _ { s } ^ { \top } \right. _ { \infty } = \frac 1 2 < 1 } \end{array}$ for any $\alpha \in \mathbb { R }$ and Lasso is successful.

In contrast, we notice that condition 2 of Theorem $4 . 4$ in [19] that guarantees the convergence of ETPG is not satisfied. In fact, the maximum eigenvalue of $\left( \boldsymbol { \mathcal { O } } \quad \boldsymbol { I } \right) ^ { \top } \left( \boldsymbol { \mathcal { O } } \quad \boldsymbol { I } \right)$ is $q = 3 \alpha ^ { 2 } + 1$ , while the minimum eigenvalue on all the possible subsets of 2 sensors is $\begin{array} { r } { r = \frac { 3 \alpha ^ { 2 } + 1 - \sqrt { 9 \alpha ^ { 4 } + 2 \alpha ^ { 2 } + 1 } } { 2 } } \end{array}$ . Then, $r < \frac { 4 } { 9 } q$ , which contradicts condition 2.
