This formula captures the inter-variable correlations within vector x. Here, Σ is a symmetric matrix that defines the covariance, or the joint variability, between every pair of elements in x. The matrix is pivotal in determining how changes in one variable affect others, encapsulating the essence of their interdependencies. The mean vector ?? and the covariance matrix Σ are crucial parameters that define this distribution. Specifically, $\dot { \boldsymbol { \mu } } = \mathbb { E } [ \mathbf { x } ] \in \mathbb { R } ^ { M }$ represents the expected value (or the average) of the vector x, indicating the central tendency or the typical value each variable in x is expected to take. On the other hand, $\Sigma = \operatorname { c o v } [ \mathbf { x } ] \in \dot { \mathbb { R } } ^ { M \times M }$ , with each element $\Sigma _ { i j }$ , quantifies the covariance between the ??-th and ??-th elements of x. Covariance measures how two variables fluctuate together; a positive covariance indicates that two variables tend to move in the same direction, whereas a negative covariance signifies that they move inversely. The diagonal elements of $\mathrm { { } ^ { \cdot } } \Sigma ,$ which represent the variance of each variable, show the extent to which each variable varies from its mean, providing insights into the spread or dispersion of the data.

![](image/532a0f106f5f6273c053861badd90cfd4e321fe5714fbf6525958922964822db.jpg)

<details>
<summary>line</summary>
