# B. Fast Terminal Sliding Mode Control Attitude Control

In comparison with the conventional SMC, AFTSMC brings more advantages in terms of performance. AFTSMC significantly decreases chattering, which is a crucial factor for smooth trajectory tracking and actuator safety. Moreover, AFTSMC can address singularity issues of conventional SMC guaranteeing finite time convergence [45]. Consider the following attitude errors [42]:

$$e _ {0} = q _ {0} q _ {0 d} + \boldsymbol {q} _ {d} \boldsymbol {q} ^ {\top}e = q _ {0 d} \boldsymbol {q} - q _ {0} \boldsymbol {q} _ {d} + [ \boldsymbol {q} ] _ {\times} \boldsymbol {q} _ {d} \tag {29}R _ {e} = R R _ {d} ^ {\top} = (e _ {0} ^ {2} - e ^ {\top} e) \mathbf {I} _ {3} + 2 e e ^ {\top} + 2 e _ {0} [ e ] _ {\times}$$

where $e _ { 0 } \in \mathbb { R }$ and $e \in \mathbb { R } ^ { 3 }$ denoted quaternion error between UAV true and desired trajectories, $R \in S O ( 3 )$ and $R _ { d }$ refers to true and desired rotation matrix of the UAV, respectively, and $R _ { e } \in S O ( 3 )$ denotes error in orientation. The goal is to drive $R \to R _ { d }$ or in other words $R _ { e }  { \bf I } _ { 3 }$ . The equivalent unit-quaternion mapping is to drive $e _ { 0 } \to 1$ and $e \to 0 _ { 3 \times 1 }$ . Let us define the error in angular velocity is defined by:

$$\Omega_ {e} = \Omega_ {B} - R _ {e} \Omega_ {d} \tag {30}$$

where the desired angular velocity $\Omega _ { d }$ can be found using the following equation [42]:

$$\dot {\boldsymbol {Q}} _ {d} = (q _ {0 d} \mathbf {I} _ {3} + [ \boldsymbol {q} _ {d} ] _ {\times}) \Omega_ {d} \tag {31}$$

with $\pmb q _ { d } = [ q _ { 1 d } , q _ { 2 d } , q _ { 3 d } ] ^ { \top }$ . Based on (29), (30) and (5), the attitude error dynamic is obtained as [11]:

$\mathrm { E r r o r ~ d y n a m i c } \left\{ \begin{array} { l l } { \dot { e } _ { 0 } } & { = \frac { 1 } { 2 } e ^ { \top } \Omega _ { e } } \\ { \dot { e } } & { = \frac { 1 } { 2 } \left( e _ { 0 } \Omega _ { e } + [ e ] _ { \times } \Omega _ { e } \right) } \\ { J \dot { \Omega } _ { e } } & { = [ J \Omega _ { B } ] _ { \times } \Omega _ { B } + \mathcal { T } + } \\ & { J \left[ \Omega _ { e } \right] _ { \times } R _ { e } \Omega _ { d } - J R _ { e } \dot { \Omega } _ { d } } \end{array} \right.$ (32)

Let us move to the Quaternion-based AFTSMC and introduce the following sliding surface (32):

$$s = \dot {e} + \gamma_ {1} e + \gamma_ {2} e ^ {n / l} + \gamma_ {3} \Omega_ {e} \tag {33}$$

where $\gamma _ { 1 }$ and $\gamma _ { 2 }$ are positive constants and $\gamma _ { 3 } = 1$ . n and l are positive constants and $\textstyle 0 < { \frac { n } { l } } < 1$ . The derivative of (33) is calculated as:
