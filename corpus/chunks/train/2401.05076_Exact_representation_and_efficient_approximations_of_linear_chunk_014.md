$$
r (\bar {m}) = \left\{ \begin{array}{l l} 0 & \bar {m} = 1 \\ \bar {m} + r (\frac {\bar {m}}{2}) & \text { if   } \bar {m} \text {   is   even } \\ \bar {m} + r (\frac {\bar {m} + 1}{2}) & \text { if   } \bar {m} \text {   is   odd   and   } \bar {m} \neq 1, \end{array} \right.
$$

number of hidden neurons, $\zeta ( \bar { m } ) = \lceil \log _ { 2 } \bar { m } \rceil + 1$ number of layers and the maximum width $w ( \bar { m } ) = \mathbb { I } [ \bar { m } > 1 ] \bar { m } .$ .

PROOF. We start by defining the first layer of the HTNN as $W ^ { [ { 1 } ] } =$ $\left[ \begin{array} { l } { c _ { 1 } } \\ { \cdots } \\ { c _ { \bar { m } } } \end{array} \right] \in \mathbb { R } ^ { \bar { m } \times n _ { x } } , b ^ { [ 1 ] } = \left[ \begin{array} { l } { \bar { d } _ { 1 } } \\ { \cdots } \\ { d _ { \bar { m } } } \end{array} \right] \in \mathbb { R } ^ { \bar { m } } \mathrm { a n d } \eta ^ { [ 1 ] } = I _ { \bar { m } }$ . Thus, we obtain ¯m scalars $u _ { i } \in [ \underline { { u } } _ { i } , \overline { { u } } _ { i } ]$ , where $\begin{array} { r } { \underline { { u } } _ { i } = \operatorname* { m i n } _ { x \in \mathbb { X } } c _ { i } ^ { T } x + d _ { i } } \end{array}$ and $\overline { { u } } _ { i } = \mathrm { m a x } _ { x \in \mathbb { X } } c _ { i } ^ { T } x + d _ { i }$ for all $i = 1 : \bar { m }$ . In other words, we absorb the affine functions into the first layer of the HT network. Now, we need to prove for the remaining function

$$\gamma (u) = \max _ {i = 1: \bar {m}} u _ {i}, \forall u _ {i} \in [ \underline {{u}} _ {i}, \overline {{u}} _ {i} ].$$

Our proof is based on the following observation:

$$
\begin{array}{l} \max \left(u _ {1}, u _ {2}\right) \\ = \max \left(0, \min \left(u _ {2} - u _ {1}, \bar {u} _ {2} - \underline {{u}} _ {1}\right)\right) + \max \left(\underline {{u}} _ {1}, \min \left(u _ {1}, \bar {u} _ {1}\right)\right) \\ = \eta \left(u _ {2} - u _ {1}, 0, \bar {u} _ {2} - \underline {{u}} _ {1}\right) + \eta \left(u _ {1}, \underline {{u}} _ {1}, \bar {u} _ {1}\right), \\ \end{array}
$$

for any $u _ { 1 } \in [ \underline { { u } } _ { 1 } , \overline { { u } } _ { 1 } ]$ and $u _ { 2 } \in \left[ { \underline { { u } } } _ { 2 } , { \overline { { u } } } _ { 2 } \right]$ . Indeed, the first term returns either $u _ { 2 } - u _ { 1 }$ or 0, while the second term returns always $u _ { 1 }$ . Using this relation recursively and similar arguments as in [3], for any $p \geq 3$ we have that:

$$
\max _ {j = 1: p} u _ {j} = \left\{ \begin{array}{l l} \max _ {j = 1: \frac {p}{2}} \max _ {i = \{2 j - 1, 2 j \}} u _ {i}, & \text { if   } p \text {   is   even } \\ \max _ {j = 1: \frac {p + 1}{2}} \phi (j; u _ {1}, u _ {2}, \dots , u _ {p}), & \text { if   } p \text {   is   odd } \end{array} \right. \tag {13}
$$
