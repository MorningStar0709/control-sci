• Homogeneous Systems with $\begin{array} { r } { d ( t ) = 0 : } \end{array}$ When the given system is homogeneous (for the definition, see [22]) and $d ( t ) = 0$ , the process is as follows. To compute approximations of isochronous manifolds $M _ { t _ { k } }$ , the user has to specify the times $( t _ { 1 } , \ldots , t _ { k } , \ldots , t _ { q } )$ (parameter manifolds\_times, in ascending order) to which the manifolds correspond. To determine the cones $C _ { j } { \mathrm { : } }$ , we follow a similar process to the isotropic covering of [26], in which each ??-th angular spherical coordinate of the state-space is partitioned into ???? equal angles. Thus, the user has to specify the parameters $( a _ { 1 } , \dotsc , a _ { j } , \dotsc , a _ { n _ { \zeta } - 1 } )$ (angles\_discretization), to obtain at mos $\mathrm { \Delta t ^ { 2 } ~ } a _ { 1 } \times a _ { 2 } \times \dots a _ { n _ { \zeta } - 1 }$ cones $C _ { j } .$ Finally, the regions $\mathcal { R } _ { i }$ are obtained as intersections between cones $C _ { j } { \mathrm { : } }$ sets between consecutive approximations of isochronous manifolds $M _ { t _ { k } }$ and $M _ { t _ { k + 1 } }$ and the set ?? . Moreover, since the regions $\mathcal { R } _ { i }$ admit a representation that is complex for the employed reachability analysis tools, we overapproximate them via ball-segments contained inside the cones $C _ { j }$ . Then, dReach is used for reachability analysis, as it can handle ball-segments efficiently. An illustration of IM-based partitioning for homogeneous systems is shown in Fig. 1.
