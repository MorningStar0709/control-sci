Constraint (21) creates an upper bound on the QSR dissipativity condition (5) for all $x \in B _ { \epsilon } ( 0 )$ using Theorem 11. To develop this constraint, perform a Taylor series expansion on each function within the QSR inequality about the origin using $\nabla V = x ^ { \top } \mathbf { P } + \mathbf { P } x , { \mathrm { i . e . ~ } } x ^ { \top } \mathbf { P } { \hat { f } } +$ $\hat { f } ^ { \top } \mathbf { P } x - \hat { h } ^ { \top } \mathbf { Q } \hat { h } + ( x ^ { \top } \mathbf { \bar { P } } \hat { G } - \hat { h } ^ { \top } \mathbf { Q } \hat { J } - \hat { h } ^ { \top } \mathbf { S } ) \hat { \mathbf { R } } ^ { - 1 } ( \hat { G } ^ { \top } \mathbf { P } x -$ $\hat { J } ^ { \top } \mathbf { Q } \hat { h } - \mathbf { S } ^ { \top } \hat { h } ) \leq 0$ , where $\hat { \mathbf { R } } = \mathbf { R } + \hat { J } ^ { \top } \mathbf { S } + \mathbf { S } ^ { \top } \hat { J } + \hat { h } ^ { \top } \mathbf { Q } \hat { h }$ . For example,

$$
\begin{array}{l} \hat {G} = \mathbf {B} + \tilde {G} (x) = \mathbf {B} + \left[ \mathbf {J} _ {g _ {1}} (0) x \dots \mathbf {J} _ {g _ {m}} (0) x \right] + \\ \frac {1}{2} \left[ \left[ \begin{array}{c} x ^ {\top} \mathbf {H} _ {g _ {1} ^ {(1)}} (\mathbf {z} _ {g _ {1} ^ {(1)}}) \\ \vdots \\ x ^ {\top} \mathbf {H} _ {g _ {1} ^ {(n)}} (\mathbf {z} _ {g _ {1} ^ {(n)}}) \end{array} \right] x \dots \left[ \begin{array}{c} x ^ {\top} \mathbf {H} _ {g _ {m} ^ {(1)}} (\mathbf {z} _ {g _ {m} ^ {(1)}}) \\ \vdots \\ x ^ {\top} \mathbf {H} _ {g _ {m} ^ {(n)}} (\mathbf {z} _ {g _ {m} ^ {(n)}}) \end{array} \right] x \right], \\ \end{array}
$$

where the Hessian of each element is evaluated at a convex combination of x and 0.

Factor x out on either side of the inequality. Then a Schur complement is performed about terms involving Rˆ and another Schur complement is performed about terms involving Q. Apply the process of Theorem 11. Note that any term on the diagonal of the matrix that contains x is bounded above using the matrix two norm, $\mathrm { e . g . } - \mathbf { S } ^ { \top } \hat { J } \leq \| \mathbf { S } \| _ { 2 } \left\| \hat { J } \right\| _ { 2 }$ . The resulting LMI is

$$
\left[ \begin{array}{c c c} \xi_ {1} + \mathbf {P} \mathbf {J} _ {f} + \mathbf {J} _ {f} ^ {\top} \mathbf {P} & * & * \\ \mathbf {B} ^ {\top} \mathbf {P} - \mathbf {S} ^ {\top} \mathbf {J} _ {h} & \bar {\mathbf {R}} + \xi_ {2} & * \\ \mathbf {J} _ {h} & \mathbf {D} & \mathbf {Q} ^ {- 1} + \frac {1}{2} (\Pi_ {7} ^ {- 1} + \Pi_ {9} ^ {- 1}) + \Pi_ {8} ^ {- 1} \end{array} \right],
$$
