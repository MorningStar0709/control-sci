the estimated state at the beginning of the horizon, $\mathrm { i . e . , } \overline { { x } } ( t -$ $L | t )$ , and the prior estimate2 ${ \hat { x } } ( t - L )$ corresponding to the state estimate at time $t - L$ . The prior weighting hence takes into account previous measurements that are not part of the current horizon. Second, we penalize the fitting error $\sigma ^ { y }$ in the cost function (9d), so that the difference between the online measured outputs and the reconstructed outputs remains small. The stage costs $\ell _ { k }$ are defined as

$$\ell_ {k} (\sigma^ {y} (t - k | t)) := | \sigma^ {y} (t - k | t) | _ {R} ^ {2} \rho^ {k} \tag {11}$$
