$$\sum_ {\tau = 0} ^ {t - 1} \left(\| \kappa_ {\tau + 1} \| ^ {2} - \mathbb {E} \left[ \| \kappa_ {\tau + 1} \| ^ {2} \mid \mathcal {F} _ {\tau} \right]\right) = O (t ^ {\frac {2}{\gamma}} (\log t) ^ {\frac {2}{\gamma} + \eta}), \text {a.s.}$$

Combining this bound with (C.3) again gives $\begin{array} { r l } { \sum _ { \tau = 1 } ^ { t } \| \kappa _ { \tau } \| ^ { 2 } = } & { { } } \end{array}$ O(t), a.s. Applying the Cauchy-Schwarz inequality then yields ${ \textstyle \sum } _ { \tau = 1 } ^ { t } \| w _ { \tau } \kappa _ { \tau } ^ { \top } \| = O ( t )$ , a.s. Hence, there exists a real-valued random variable $c _ { 3 } > 0$ such that

$$\sum_ {\tau = 1} ^ {t} \left\| w _ {\tau} \kappa_ {\tau} ^ {\top} \right\| \leq c _ {3} t, \forall t \geq 0. \tag {C.4}$$

Using the martingale convergence theorem again (cf. Theorem 2.8 in Chen and Guo (1991)), we also have

$$\sum_ {\tau = 0} ^ {t - 1} \left(w _ {\tau + 1} w _ {\tau + 1} ^ {\top} - \mathbb {E} \left[ w _ {\tau + 1} w _ {\tau + 1} ^ {\top} \mid \mathcal {F} _ {\tau} \right]\right) = O (t ^ {\frac {2}{\gamma}} (\log t) ^ {\frac {2}{\gamma} + \eta}), \text {a.s.},$$

for any $\eta > 0$ . Then, by Assumption 2, there exists a real-valued random variable $\bar { \sigma } > \bar { 0 }$ such that

$$
\begin{array}{l} \sum_ {\tau = 1} ^ {t} w _ {\tau} w _ {\tau} ^ {\top} = \sum_ {\tau = 0} ^ {t - 1} \left(w _ {\tau + 1} w _ {\tau + 1} ^ {\top} - \mathbb {E} \left[ w _ {\tau + 1} w _ {\tau + 1} ^ {\top} \mid \mathcal {F} _ {\tau} \right]\right) \\ + \sum_ {\tau = 0} ^ {t - 1} \mathbb {E} \left[ w _ {\tau + 1} w _ {\tau + 1} ^ {\top} \mid \mathcal {F} _ {\tau} \right] \geq \bar {\sigma} t I _ {n}, \text { a.s. } \tag {C.5} \\ \end{array}
$$

Similarly, there exists a real-valued random variable $\bar { \sigma } ^ { \prime } >$ 0 such that $\begin{array} { r } { \lambda _ { \operatorname* { m i n } } \{ \sum _ { \tau = 1 } ^ { t } \epsilon _ { \tau } \epsilon _ { \tau } ^ { \top } \} \geq \bar { \sigma } ^ { \prime } t . } \end{array}$ , a.s..

For each $z = [ z _ { 1 } , z _ { 2 } ] ^ { \top } \in \mathbb { R } ^ { n + m }$ satisfying $\| z \| = 1$

$$
\begin{array}{l} z ^ {\top} \sum_ {\tau = 1} ^ {t} \zeta_ {\tau} \zeta_ {\tau} ^ {\top} z = \sum_ {\tau = 1} ^ {t} (z _ {1} ^ {\top} w _ {\tau} + z _ {2} ^ {\top} \kappa_ {\tau}) (z _ {1} ^ {\top} w _ {\tau} + z _ {2} ^ {\top} \kappa_ {\tau}) ^ {\top} \\ + \sum_ {\tau = 1} ^ {t} \tau^ {- b} (z _ {1} ^ {\top} w _ {\tau} + z _ {2} ^ {\top} \kappa_ {\tau}) \epsilon_ {\tau} ^ {\top} z _ {2} + \tau^ {- 2 b} z _ {2} ^ {\top} \sum_ {\tau = 1} ^ {t} \epsilon_ {\tau} \epsilon_ {\tau} ^ {\top} z _ {2}. \tag {C.6} \\ \end{array}
$$
