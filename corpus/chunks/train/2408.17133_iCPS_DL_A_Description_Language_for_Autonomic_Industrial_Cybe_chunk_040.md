$$\left\{\mathsf {p} _ {\mathrm{i}}: \text { end } \mid i \in I \right\}, \left\{\mathsf {p} _ {\mathrm{j}}: T _ {j} [ \mathrm{t} \Leftarrow \text { loop t. } T _ {j} ] \mid j \in J \right\}$$

is live.

The inductive step concludes the proof.

![](image/47cbf437d1e79786335e6f593d7bf9d09dd864b14b23bffab3d8beeb02a45d32.jpg)

The composition/projection algorithm has polynomial time complexity relative to the syntactic size of a local configuration.

Theorem A.3. $S \vdash G \in O ( | S | )$

Proof. The proof is done by induction on the definition of ⊢.

• Base Case: There are two cases

• Base case {pi : end | i ∈ I} ⊢ end is straightforward. The algorithm requires |I| steps, one for each pi, to check that the corresponding local protocol is inactive. Moreover,

$$\left| \left\{\mathrm{p} _ {\mathrm{i}}: \text { end } \mid i \in I \right\} \right| = \sum_ {i \in I} | \text { end } | = \sum_ {i \in I} 1 = | I |.$$

Thus,

$$\left\{\mathrm{p} _ {\mathrm{i}}: \text { end } \mid i \in I \right\} \vdash \text { end } \in O (| \left\{\mathrm{p} _ {\mathrm{i}}: \text { end } \mid i \in I \right\} |),$$

as required.

• Base case $\{ \mathsf { p } _ { \mathsf { i } } ~ { \mathrm { ~ : ~ } } \{ ~ \mathsf { i } ~ i ~ \in ~ I \} ~ \vdash ~ \mathsf { t }$ follows similar argumentation.

• Inductive Hypothesis: Assume that $S \vdash G \in O ( | S | )$   
• Inductive Step: There are three cases:

a) The case for message passing global protocol:

$$\frac {S , \mathsf {p} : T , \mathsf {q} : T ^ {\prime} \vdash G}{S , \mathsf {p} : \mathsf {q} _ {!} \mathsf {U} . T , \mathsf {q} : \mathsf {p} _ {?} \mathsf {U} . T ^ {\prime} \vdash \mathsf {p} \to \mathsf {q} : \mathsf {U} . G}$$

The rule takes one step to match the duality of actions $\mathsf { q } _ { ! } \mathsf { U }$ and $\mathsf { p } _ { ? } U$ and then verifies $S , \mathsf { p } : T , \mathsf { q } : T ^ { \prime } \vdash G$ . By the inductive hypothesis:

$$S, \mathfrak {p}: T, \mathfrak {q}: T ^ {\prime} \vdash G \in O (| S, \mathfrak {p}: T, \mathfrak {q}: T ^ {\prime} |).$$

Thus, it is safe to conclude that:

$$
\begin{array}{l} S, \mathsf {p}: \mathsf {q} _ {!} \cup . T, \mathsf {q}: \mathsf {p}? \cup . T ^ {\prime} \vdash \mathsf {p} \rightarrow \mathsf {q}: \cup . G \\ \in O (2 + | S, \mathfrak {p}: T, \mathfrak {q}: T ^ {\prime} |). \\ \end{array}
$$

Furthermore, observe that

$$| S, \mathsf {p}: \mathsf {q} _ {!} \mathsf {U}. T, \mathsf {q}: \mathsf {p} _ {?} \mathsf {U}. T ^ {\prime} | = 2 + | S, \mathsf {p}: T, \mathsf {q}: T ^ {\prime} |,$$

concluding with:
