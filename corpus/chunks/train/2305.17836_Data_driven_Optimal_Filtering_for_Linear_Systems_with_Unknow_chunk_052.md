# D.8 Gradient Descent (GD)

Here, we consider the GD policy update:

$$[ \mathrm{GD} ] \quad L _ {k + 1} = L _ {k} - \eta_ {k} \nabla J (L _ {k}),$$

for $k \in \mathbb { Z }$ and a positive stepsize $\eta _ { k }$ . Given the convergence result for the GF, establishing convergence for GD relies on carefully choosing the stepsize $\eta _ { k }$ , and bounding the rate of change of $\nabla \ J ( L ) - \mathrm { a t }$ least on each sublevel set. This is achieved by the Lipschitz bound for $\nabla J ( L )$ on any sublevel set.

In what follows, we establish linear convergence of the GD update. Our convergence result only depends on the value of α for the initial sublevel set $S _ { \alpha }$ that contains $L _ { 0 }$ . Note that our proof technique is distinct from those in [6] and [34]; nonetheless, it involves a similar argument using the gradient dominance property of J.

Theorem 4. Consider any sublevel set $S _ { \alpha }$ for some $\alpha > 0 .$ . Then, for any initial policy $L _ { 0 } \in S _ { \alpha }$ , the GD updates with any fixed stepsize $\eta _ { k } = \eta \in ( 0 , 1 / \ell ( \alpha ) ]$ converges to optimality at a linear rate of $1 - \bar { \eta } c _ { 1 } ( \alpha ) / 2$ (in both the function value and the policy iterate). In particular, we have

$$J (L _ {k}) - J (L ^ {*}) \leq [ \alpha - J (L ^ {*}) ] (1 - \eta c _ {1} (\alpha) / 2) ^ {k},$$

and

$$\left\| L _ {k} - L ^ {*} \right\| _ {F} ^ {2} \leq \left[ \frac {\alpha - J (L ^ {*})}{c _ {3} (\alpha)} \right] (1 - \eta c _ {1} (\alpha) / 2) ^ {k},$$

with $c _ { 1 } ( \alpha )$ and $c _ { 3 } ( \alpha )$ as defined in Lemma 2.

Proof. First, we argue that the GD update with such a step size does not leave the initial sublevel set $S _ { \alpha }$ for any initial $L _ { 0 } \in S _ { \alpha }$ . In this direction, consider $\bar { L } ( \eta ) = L _ { 0 } - \eta \nabla J ( L _ { 0 } )$ for $\eta \geq 0$ where $L _ { 0 } \neq L ^ { * }$ ∗. Then, by compactness of $S _ { \alpha }$ and continuity of the mapping $\boldsymbol { \eta } \to J ( L ( \boldsymbol { \eta } ) )$ on $s \supset { \mathcal { S } } _ { \alpha }$ , the following supremum is attained with a positive value $\bar { \eta } _ { 0 } ;$ :

$$\bar {\eta} _ {0} := \sup \{\eta : J (L (\zeta)) \leq \alpha , \forall \zeta \in [ 0, \eta ] \},$$
