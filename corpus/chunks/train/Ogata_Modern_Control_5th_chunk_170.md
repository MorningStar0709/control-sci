Note: The sum of gains is calculated by adding a factor (k) and sum of gains by adding b.
Note: The sum of gains in each case is calculated by adding a factor to the sum of gains from k to s, and the sum of gains from s to t is calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gains is calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gains is calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gains is calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gains is calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gain blocks from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points is calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gain blocks from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note : The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
Note: The sum of gain blocks and summation points are calculated by adding a factor to the sum of gains from s to t + 1.
