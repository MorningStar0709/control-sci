A maximum hands-off hybrid control sequence minimizes the total number of time instants when an admissible continuous control or, an admissible discrete control or, both are to be applied in order to steer the initial state $x ( 0 ) = \xi$ of the switched system (2.1) to the $\mathrm { o r i g i n } , \ 0 _ { d } ,$ in T time instants. On the one hand, a minimization of $\| \mu _ { T } \| _ { 0 }$ gives a continuous control sequence, $\tilde { \mu } _ { T }$ , with elements satisfying $\tilde { \mu } ( t ) \in \mathcal { U } , t = 0 , 1 , \dotsc , T - 1$ that minimizes the total number of time instants when a continuous control is to be applied. $\mathrm { O n }$ the other hand, a minimization of $\left\| \Delta ( \nu _ { T } ) \right\| _ { 0 }$ gives a discrete control sequence, $\tilde { \nu } _ { T }$ , with elements satisfying $( \tilde { \nu } ( t ) , \tilde { \nu } ( t + 1 ) ) \in \mathcal { E } ( \mathcal { N } ) , \ t = 0 , 1 , \ldots , T - 2$ that minimizes the total number of time instants when the discrete control changes its value. A maximum hands-off hybrid control sequence is, however, not necessarily $\left( \tilde { \nu } _ { T } , \tilde { \mu } _ { T } \right)$ . Indeed, individual minimization of $\Vert \mu _ { T } \Vert _ { 0 }$ and $\| \Delta ( \nu _ { T } ) \| _ { 0 }$ does not necessarily ensure that $\left( \tilde { \nu } _ { T } , \tilde { \mu } _ { T } \right)$ steers $x ( 0 ) = \xi \tan x ( T ) = 0 _ { d }$ . We aim for a hybrid control sequence, $( \overline { { \nu } } _ { T } , \overline { { \mu } } _ { T } )$ whose discrete and continuous control components are admissible and together achieve the desired control objective while maximizing the total number of time instants when a hybrid controller needs not take an action.

Remark 1. The optimization problem (2.4) is non-convex due to the non-convexity of (a) $\ell _ { 0 } .$ -norm [6, Chapters 2 and 3] and (b) the domain of the discrete control, ${ \mathcal { N } } .$ . It is well-known that solving such problems is numerically hard. A search for algorithms to solve the optimization problem (2.4) is, therefore, of interest.

In this paper we will solve the following:
