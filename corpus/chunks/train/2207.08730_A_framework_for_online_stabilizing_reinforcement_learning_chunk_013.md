# V. ALGORITHM

It will be shown that enforcing either $G _ { \varepsilon } ^ { \bar { \nu } } ( w _ { k } , w _ { k - 1 } , x _ { k } , x _ { k - 1 } ) \leq 0 \mathrm { o r } G _ { \varepsilon } ^ { \bar { \nu } } ( w _ { k } , w _ { k - 1 } , \Lambda _ { u } ( x _ { k } ) , x _ { k } ) \leq$ 0 is sufficient to guarantee a certain kind of stability. At the same time the latter constraints do not prevent the critic from converging to the optimal cost-to-go function.

Below are examples of particular algorithms that involve these constraints.

A SARSA-like approach can be used if the model of the noise is not priorly known is shown in Algorithm 1.

Algorithm 1: CALF as SARSA   
Data: $\varepsilon > 0$ , $\bar{\nu} > 0$ , $\zeta > 0$ Result: $U_0, U_\delta, U_{2\delta}, \ldots$ $x_0 \leftarrow X_0$ ; $w_0 \leftarrow w_\zeta^\#$ ;

for $k = 0 \ldots \infty$ do $\begin{pmatrix} \theta_k \\ w_{-1} \end{pmatrix} \leftarrow \underset{\text{s.t. } G_{\varepsilon}^{\bar{\nu}}(w, w_k, \Lambda_{(\rho^\theta(x_k))}(x_k), x_k) \leq 0}{\arg \min} \mathbb{J}_c(\theta, w)$ where $\mathbb{J}_a(\theta, w) = r(x_k, \rho^\theta(x_k)) + \hat{J}^{w_k}(\Lambda_{\rho^\theta(x_k)}(x_k))$ $U_{k\delta} \leftarrow \rho_{\theta_k}(x_k)$ perform $U_{k\delta}$ $x_{k+1} \leftarrow \textbf{observe } X_{(k+1)\delta}$ $w_{k+1} \leftarrow \underset{\text{s.t. } G_{\varepsilon}^{\bar{\nu}}(w, w_k, x_{k+1}, x_k) \leq 0}{\arg \min} \mathbb{J}_c(w)$ where $\mathbb{J}_c(w) = \left( \hat{J}^w(x_k) - r(x_k, U_{k\delta}) - \hat{J}^{w_k}(x_{k+1}) \right)^2$ end

A sort of an actor-critic can be employed provided that one has the means to estimate the expected value (for instance by computing the mean of an artificial sample). It is shown in Algorithm 2.

Although the above examples each present a case of single step temporal difference learning, the proposed approach would not in any way inhibit a different choice of temporal difference updates, such as for instance TD(N ) or TD(λ). In fact the stabilizing properties of an algorithm of this kind are not affected by how the updates are performed as long as the said updates do not violate the stabilizing constraints. Though, of course, these choices do affect the resulting total cost.

Remark 7: One can introduce regularization terms into the actor and critic loss functions with some tuning parameters $\beta _ { 1 , 2 , 3 } > 0$ as follows:
