Proof. Fix an arbitrary $\delta > 0$ and choose ˜x and $\tilde { u } \in \mathcal { U }$ from Lemma 1 such that $| x ( s ) - \tilde { x } ( s ) | < \delta$ for all $s \in [ t , T ]$ and $\tilde { { \boldsymbol { x } } } ( t ) = { \boldsymbol { \mathscr { x } } }$ . Consider the set of balls centered at the elements of a δ-net $U _ { \delta }$ with radius $\delta ,$ that is, $Y : = \{ B _ { \delta } ( a ) : a \in U _ { \delta } \}$ . For simplicity, we denote $Y = \{ B _ { 1 } , \dots , B _ { \ell } \}$ , where the center of $B _ { i }$ corresponds to $a _ { i }$ . Define a new control ¯u as

$$\bar {u} (s) := \sum_ {i = 1} ^ {\ell} a _ {i} \mathbf {1} _ {\tilde {B} _ {i}} (\tilde {u} (s)),$$

where $\tilde { B } _ { i } : = B _ { i } \setminus \bigcup _ { i = 1 } ^ { i - 1 } B _ { j }$ and ${ \bf 1 } _ { B } ( x )$ is an indicator function such that $\mathbf { 1 } _ { B } ( x ) = 1 { \mathrm { ~ i f ~ } } x \in B$ and 0 otherwise. Then, $\bar { u } ( s )$ is measurable and $| \bar { u } ( s ) - \tilde { u } ( s ) | < \delta$ . Let ¯x be the state trajectory controlled by ¯u starting from $\bar { \boldsymbol { x } } ( t ) = \boldsymbol { x }$ . We then have for any $s \in [ t , T ]$

$$
\begin{array}{l} | \bar {x} (s) - x (s) | \leq | \bar {x} (s) - \tilde {x} (s) | + | \tilde {x} (s) - x (s) | \\ \leq \int_ {t} ^ {s} | f (s, \bar {x} (s), \bar {u} (s)) - f (s, \tilde {x} (s), \tilde {u} (s)) | \mathrm{d} s + \delta \\ \leq \int_ {t} ^ {s} C \delta \mathrm{d} s + \delta \\ \end{array}
$$

for some positive constant C because $f ( s , \cdot , \cdot )$ is Lipschitz continuous under Assumption 3. Thus, the result follows. □
