# V. SIMULATION RESULTS

Consider the following system which is an uncertain version of a mobile robot used in [25]:

$$\dot {x} _ {1} = \left(1 + \theta_ {1}\right) u _ {1} + \delta_ {1}, \tag {31}\dot {x} _ {2} = (1 + \theta_ {2}) u _ {2} + \delta_ {2},$$

where $x _ { 1 } , x _ { 2 }$ are the positions in a 2D plane, and $u _ { 1 }$ , u2 are their velocities, respectively; $\theta _ { 1 } , \theta _ { 2 } , \delta _ { 1 }$ , and $\delta _ { 2 }$ are unknown functions of t and x such that they are respectively upperbounded to the known functions $\bar { \theta } _ { 1 } ( { \pmb x } ) , \bar { \theta } _ { 2 } ( { \pmb x } ) , \bar { \delta } _ { 1 } ( { \pmb x } )$ , and $\bar { \delta } _ { 2 } ( { \pmb x } )$ ) for all $\textbf { \em x } \in \ \mathbb { R } ^ { 2 }$ . We further assume that there exists $g _ { 0 } \ > \ 0$ such that $1 + \theta _ { i } \geq g _ { 0 }$ for $i = 1 , 2$ . The objective is to design a state feedback control law that ensures the robust stability of the origin and confines the system states within the safe set $\mathcal { C } = \mathbb { R } ^ { 2 } \cap \bar { B } _ { r }$ , where $B _ { l } = \{ \pmb { x } \in \mathbb { R } ^ { 2 }$ : $( x _ { 1 } - x _ { 1 c } ) ^ { 2 } + ( x _ { 2 } - x _ { 2 c } ) ^ { 2 } < l ^ { 2 } \}$ represents a ball centered at $\left( { { x } _ { 1 c } } , { { x } _ { 2 c } } \right)$ with a radius l. By defining $\zeta = x$ , we can see that the system (31) is already presented in the regular form. By considering the sliding manifold $\textbf { \em s } = \textbf { \em x } = \textbf { 0 }$ and using (15), the unsafe control law $\pmb { u } _ { \mathrm { s m c } } ( \pmb { x } ) = [ u _ { \mathrm { s m c } } ^ { 1 } ( \pmb { x } ) , u _ { \mathrm { s m c } } ^ { 2 } ( \pmb { x } ) ] ^ { \tilde { \mathrm { T } } }$ is achieved as follows:

$$u _ {\mathrm{smc}} ^ {1} (\boldsymbol {x}) = - \left(\varrho (\boldsymbol {x}) + \beta_ {0}\right) \text {sign} \left(x _ {1}\right), \tag {32}u _ {\mathrm{smc}} ^ {2} (\boldsymbol {x}) = - \left(\varrho (\boldsymbol {x}) + \beta_ {0}\right) \operatorname{sign} \left(x _ {2}\right), \tag {33}$$

where $\varrho ( { \pmb x } ) = g _ { 0 } ^ { - 1 } \operatorname* { m a x } \{ \bar { \delta } _ { 1 } ( { \pmb x } ) , \bar { \delta } _ { 2 } ( { \pmb x } ) \}$ and $\beta _ { 0 }$ is a strictly positive constant. In this example, we have $\begin{array} { r } { f = 0 , B = E = } \end{array}$ I, and we set $\hat { G } = I$ . Hence, $\varrho _ { 2 } ( { \pmb x } ) = \mathrm { m a x } \{ \bar { \theta } _ { 1 } ( { \pmb x } ) , \bar { \theta } _ { 2 } ( { \pmb x } ) \}$ . According to (23)–(25), we have
