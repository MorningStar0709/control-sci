Remark 7. Following the proof of Theorem, if $\begin{array} { r } { \exists t \ge \overline { { t } } \varsigma _ { \mathrm { U B } } + w _ { \mathrm { m a x } } < \frac { \rho \lambda _ { \operatorname* { m i n } } \left( \overline { { Q } } \right) } { 2 \left\| \overline { { P } } B \right\| } - \rho e ^ { - 0 . 5 \eta _ { \widetilde { \theta } } \left( \overline { { t } } - t _ { e } \right) } \left\| \widetilde { \theta } \left( t _ { e } \right) \right\| } \end{array}$ − , then the developed adaptive control system is robust to the external bounded disturbance $\left\| w \left( t \right) \right\| \leq w _ { \operatorname* { m a x } } .$ , which is added to the right hand side of the regression equation (31). The above inequality means that the identification of the control law parameters, which stabilize the system (4), is possible on the basis of the perturbed regression $\Upsilon \left( t \right) = \Omega \left( t \right) \theta + w \left( t \right) + \varsigma \left( t \right)$ .

Remark 8. The proposed adaptive law (32) provides stability of $e _ { r e f } \left( t \right)$ only when $\Psi \left( t \right) \in \mathrm { F E }$ . Therefore, to implement (32) in practice, the a priori information is required that this condition holds. If $\boldsymbol { \Psi } \left( t \right) \not \in \mathrm { F E }$ for a particular plant (4) and a particular reference $r ,$ then it is possible to hold $\Psi \left( t \right) \in \mathrm { F E }$ artificially by addition of the dither noise to the control or the reference signal according $\tan ^ { 4 4 , 4 5 }$ . It should be additionally noted that such noise should be added for a finite time interval only in order to ensure FE. The actual problem is to obtain conditions on the reference ??, under which the requirement $\Psi ( t ) \in \mathrm { F E }$ is met for the whole class of controllable systems (4).
