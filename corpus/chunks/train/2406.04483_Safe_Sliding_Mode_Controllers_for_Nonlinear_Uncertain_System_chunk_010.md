Assumption 2: For all $\textbf { \textit { x } } \in \textbf { \textit { D } }$ , there exists a known continuous function $\varrho _ { 2 } ( { \pmb x } )$ such that

$$\left\| \boldsymbol {G} (\boldsymbol {x}) - \hat {\boldsymbol {G}} (\boldsymbol {x}) \right\| _ {\infty} \leq \varrho_ {2} (\boldsymbol {x}). \tag {7}$$

Thanks to Assumptions 1 and 2, we can conclude that for all $( t , \pmb { x } ) \in [ 0 , \infty ) \times \mathcal { D }$ , there exists a known continuous function %(x) such that

$$\left| \frac {\Delta_ {i} (t , \boldsymbol {x})}{g _ {i} (\boldsymbol {x})} \right| \leq \varrho (\boldsymbol {x}), \tag {8}$$

where $\Delta _ { i } ( t , \pmb { x } )$ is the ith element of the uncertainty ∆. Consider the Lyapunov function candidate $V _ { \mathrm { s m c } } ( \pmb { s } ) = ( \mathrm { i } / 2 ) \pmb { s } ^ { \mathrm { T } } \pmb { s } =$ $( 1 / 2 ) \textstyle \sum _ { i = 1 } ^ { p } \bar { s _ { i } ^ { 2 } }$ , where $s _ { i }$ is the ith component of s. For $i = 1 : p ,$ , the following inequality holds using (6) and due to (8):

$$s _ {i} \dot {s} _ {i} \leq g _ {i} (\boldsymbol {x}) (s _ {i} v _ {i} + \varrho (\boldsymbol {x}) | s _ {i} |), \tag {9}$$

where $v _ { i }$ denotes the ith element of the vector v. Assume the function $\beta ( { \pmb x } ) : \mathcal { D }  ( 0 , \infty )$ satisfies the following inequality for all $\mathbf { \boldsymbol { x } } \in \mathcal { D } \mathrm { : }$ :

$$\beta (\boldsymbol {x}) \geq \varrho (\boldsymbol {x}) + \beta_ {0}, \tag {10}$$

where $\beta _ { 0 }$ is a known strictly positive constant. Using (9) and (10) and taking

$$v _ {i} = - \beta (\boldsymbol {x}) \text { sign } (s _ {i}), \tag {11}$$

we have $s _ { i } \dot { s } _ { i } \le - g _ { 0 } \beta _ { 0 } \vert s _ { i } \vert$ , and hence,

$$\dot {V} _ {\mathrm{smc}} (\boldsymbol {s}) \leq - g _ {0} \beta_ {0} \| \boldsymbol {s} \| _ {1} \leq - g _ {0} \beta_ {0} \sqrt {2 V _ {\mathrm{smc}} (\boldsymbol {s})}. \tag {12}$$

Let us show that the sliding manifold is reachable in a finite time. Starting from $\mathbf { \boldsymbol { s } } ( 0 ) \neq \mathbf { \boldsymbol { 0 } }$ and using the comparison principle [1], we reach the sliding manifold $\mathbf { \boldsymbol { s } } = \mathbf { 0 }$ within a finite time $t _ { r } \le \| s ( 0 ) \| _ { 1 } / g _ { 0 } \beta _ { 0 }$ . Since $\zeta = \phi ( \eta )$ asymptotically stabilizes (2) and due to (12), the origin of (1) is robustly asymptotically stable.

Remark 1: When considering ⇣ as the input controlling $\dot { \pmb \eta } = \pmb f _ { a } ( \pmb \eta , \pmb \zeta )$ , the construction of $\phi$ entails addressing a stabilization problem which can be solved through the application of nonlinear design methods or linearization techniques.
