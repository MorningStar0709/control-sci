Since $H \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ is a radially unbounded function the minimum $b = \operatorname* { m i n } { \left\{ \ H ( x ) : x \in \mathbb { R } ^ { n } \ \right\} }$ is well-defined and satisfies the inequality $b \geq 0$ . We define the non-decreasing function $a ( s ) { \mathrel { : = } } \operatorname* { m a x } \left\{ \left| x \right| : H ( x ) \leq s \right\}$ for $s \geq b$ , which is well-defined by virtue of the fact that  is a radially unbounded function. Then, we get $\left| x \right| \leq a { \bigl ( } H ( x ) { \bigr ) }$ for all $x \in \mathbb { R } ^ { n }$ . The function $\tilde { a } ( s ) { : = } \int _ { s } ^ { s + 1 } a ( p ) d p$ is a continuous, non-decreasing function defined on $\left[ b , + \infty \right)$ that satisfies $a ( s ) \leq { \tilde { a } } ( s )$ for all $s \geq b$ . Consequently, we get $\left| x \right| \leq \tilde { a } \bigl ( H ( x ) \bigr )$ for all $x \in \mathbb { R } ^ { n }$ . Using (56) we get the following estimate for all $t \geq 0$ :

$$\left| x (t) \right| \leq \tilde {a} \Big (\max \Big (H (x _ {0}), R \Big (\left\| d \right\| _ {\infty} \Big) \Big) \Big)$$

It follows from the above estimate that (41) holds with $B ( x , s ) = \tilde { a } \big ( \operatorname* { m a x } \big ( H ( x ) , R \big ( s \big ) \big ) \big )$ for all $( x , s ) \in \mathbb { R } ^ { n } \times \mathbb { R } _ { + }$ . Therefore, the p-UBIBS property holds for (36).

If in addition the function $H ( x ) = \operatorname* { m a x } _ { i = 1 , \ldots , m } \left( H _ { i } ( x ) \right)$ is positive definite and $R ( 0 ) = 0$ then Lemma 3.5 iz=l,.. on page 138 in [9] implies the existence of functions $a _ { 1 } , a _ { 2 } \in K _ { \infty }$ such that the following inequality holds for all $x \in \mathbb { R } ^ { n }$ :

$$a _ {1} \left(\left| x \right|\right) \leq H (x) \leq a _ {2} \left(\left| x \right|\right)$$

Combining the above inequality with (56) we obtain the following estimate for all $t \geq 0$ :

$$
\begin{array}{l} \left| x (t) \right| \leq \max \left(a _ {1} ^ {- 1} \left(a _ {2} \left(\left| x _ {0} \right|\right)\right), a _ {1} ^ {- 1} \left(R \left(\left\| d \right\| _ {\infty}\right)\right)\right) \\ \leq a _ {1} ^ {- 1} \left(a _ {2} \left(\left| x _ {0} \right|\right)\right) + a _ {1} ^ {- 1} \left(R \left(\left\| d \right\| _ {\infty}\right)\right) \\ \end{array}
$$
