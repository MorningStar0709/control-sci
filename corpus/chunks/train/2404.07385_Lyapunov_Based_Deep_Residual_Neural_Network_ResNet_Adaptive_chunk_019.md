for all $z \in \mathbb { D }$ . Based on Holder’s inequality, triangle inequality, and the fact that $\begin{array} { r l r } { \| e \| } & { { } \le } & { \| e \| _ { 1 } , } \end{array}$ the following inequality can be obtained: $e ^ { T } \left( { \mathcal { O } } \left( \left\| { \tilde { \theta } } \right\| ^ { 2 } \right) + \varepsilon ( x ) \right) \leq$ $\left\| e \right\| _ { 1 } \left( \left\| \sigma \left( \left\| \tilde { \theta } \right\| ^ { 2 } \right) \right\| + \left\| \varepsilon ( x ) \right\| \right) \leq \left( \bar { \varepsilon } + \overline { { \Delta } } \right) \left\| e \right\| _ { 1 }$ . Then, provided the gain condition in (24) is satisfied, the right-hand side of (26) can be upper-bounded as

$$\dot {\mathcal {V}} _ {L} \stackrel {a. a. t.} {\leq} - \sigma_ {e} \| e \| ^ {2}, \tag {27}$$
