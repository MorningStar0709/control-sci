for some constant $\bar { \epsilon } > 0$ . The additive perturbation $v _ { t }$ serves as a probing (exploration) signal that injects excitation into the closed-loop, which is essential for consistent parameter learning and, consequently, effective adaptation.

Under system (1) and the adaptive law (23), the resulting closed-loop system is given by

$$x _ {t + 1} = f (A ^ {*} x _ {t} + B ^ {*} (\pi_ {\hat {\theta} _ {t - 1}} (x _ {t}) + v _ {t})) + w _ {t + 1}. \tag {25}$$

To establish stability of the closed-loop system (25), we impose a stabilizability requirement on the control design mechanism π . To this end, we first recall the notion of finite-gain $L _ { q }$ stability for nonlinear systems.

Definition 1 (Finite-gain $L _ { q } { \bf - s t a b i l i t y } )$ (see, e.g., (Van der Schaft , 2000, Def. 1.2.1)) The stochastic system $x _ { t + 1 } = F ( x _ { t } , v _ { t } , w _ { t } )$ with input $\boldsymbol { v } _ { t } \in \mathbb { R } ^ { m }$ , state $\boldsymbol { x } _ { t } \in \mathbb { R } ^ { n }$ , and noise $w _ { t } \in \mathbb { R } ^ { n }$ is said to be finite-gain $L _ { q ^ { - } }$ stable (with respect to $\{ v _ { t } \}$ and $\{ w _ { t } \} )$ for some $q > 0$ , if there exists a constant $k _ { q } > 0$ such that for every initial condition $x _ { 0 } \in \mathbb { R } ^ { n }$ there exists a constant $b _ { q } ( { \dot { x } } _ { 0 } ) > 0$ with the property that

$$\sum_ {\tau = 0} ^ {t - 1} \| x _ {\tau + 1} \| ^ {q} \leq k _ {q} \sum_ {\tau = 0} ^ {t - 1} (\| v _ {\tau} \| ^ {q} + \| w _ {\tau + 1} \| ^ {q}) + b _ {q} (x _ {0}), a. s.$$

for any $t \geq 0$ and any random variable sequences $\{ v _ { t } \}$ and $\{ w _ { t } \}$ .

Consider now the following stabilizability notion.

Finite-gain $L _ { q }$ -stabilizability. Given a system of the form (1). We say that the control design mechanism π is finite-gain $L _ { q }$ -stabilizing if for each $\theta = [ A , B ] ^ { \top } \in \Theta .$ , the feedback control $\pi _ { \boldsymbol { \theta } } ( \cdot )$ ensures the closed-loop system

$$x _ {t + 1} (\theta) = f (A x _ {t} (\theta) + B (\pi_ {\theta} (x _ {t} (\theta)) + v _ {t})) + w _ {t + 1},$$

to be finite-gain $L _ { q } .$ -stable with respect to $\{ v _ { t } \}$ and $\{ w _ { t } \}$ .
