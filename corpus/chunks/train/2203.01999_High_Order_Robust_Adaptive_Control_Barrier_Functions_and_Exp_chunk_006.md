where $\{ \alpha _ { i } \} _ { i = 1 } ^ { r }$ is a collection of differentiable extended class functions. Then, the function h is said to be a HOCBF of order r for (1) on an open set $\mathcal { D } \supset \cap _ { i = 1 } ^ { r } \mathcal { C } _ { i }$ if h has relative degree r on some nonempty $\mathcal { R } \subseteq \mathcal { D }$ and there exists a suitable choice of $\{ \alpha _ { i } \} _ { i = 1 } ^ { r }$ such that for all $x \in \mathcal { D }$

$$\sup _ {u \in \mathcal {U}} \{\underbrace {L _ {f} \psi_ {r - 1} (x) + L _ {g} \psi_ {r - 1} (x) u + \alpha_ {r} (\psi_ {r - 1} (x))} _ {\psi_ {r} (x, u)} \} \geq 0.$$

If $\boldsymbol { \mathcal { U } } ~ = ~ \mathbb { R } ^ { m }$ , the above states that h is a HOCBF if $\| L _ { g } \psi _ { r - 1 } h ( x ) \| = 0 \implies L _ { f } \psi _ { r - 1 } h ( x ) \geq - \alpha _ { r } ( \psi _ { r - 1 } ( x ) )$ , implying that h need not have uniform relative degree on as illustrated in [29], provided the unforced dynamics satisfy the above condition at points where $\| L _ { g } \psi _ { r - 1 } h ( x ) \| = 0$ . The following result provides higher order conditions for safety.

Theorem 1 ([29]). Let h be a HOCBF for (1) on $\mathcal { D } \subset \mathbb { R } ^ { n }$ as in Def. 2. Then, any locally Lipschitz controller $u = k ( x ) \in$ $K _ { c b f } ( x )$ , where $K _ { c b f } ( x ) : = \{ u \in \mathcal { U } | \psi _ { r } ( x , u ) \geq 0 \}$ , renders $\cap _ { i = 1 } ^ { r } \mathcal { C } _ { i } ^ { r }$ forward invariant for the closed-loop system.
