$$B (\boldsymbol {x}, t) := \{- f (t, \boldsymbol {x}, a): a \in U \},$$

which we call the feasible set of negative vector fields.

Consider the following change of variables:

$$\beta (t) := - f (t, x (t), u (t)) \in B (x (t), t).$$

Then, the dynamical system (2.1) and the value function (2.2) can be rewritten as

$$\dot {x} (t) = - \beta (t), \quad 0 \leq t \leq T,$$

and

$$V (\pmb {x}, t) = \inf _ {\beta \in \mathcal {B}} \left\{\int_ {t} ^ {T} \tilde {L} (s, x (s), \beta (s)) \mathrm{d} s + g (x (T)): x (t) = \pmb {x}, \beta (s) \in B (x (s), s) \right\},$$

where

$$\tilde {L} (s, \pmb {x}, b) := \min _ {a \in U} \{L (s, \pmb {x}, a): f (s, \pmb {x}, a) = - b \},$$

and $\boldsymbol { B } : = \{ \beta : [ 0 , T ]  \mathbb { R } ^ { n } : \beta$ is measurable}.

To consider the formulation above from the Lagrangian perspective, we will use the convex conjugate $\begin{array} { r } { H ^ { * } ( t , \pmb { x } , b ) : = \operatorname* { s u p } _ { p \in \mathbb { R } ^ { n } } \left\{ p \cdot b - H ( t , \pmb { x } , p ) \right\} } \end{array}$ of the Hamiltonian as the Lagrangian. We first identify the domain of b in which $H ^ { * } ( t , { \boldsymbol { x } } , b )$ is well defined. Note that

$$H (t, \boldsymbol {x}, p) = \sup _ {b \in B (\boldsymbol {x}, t)} \left\{p \cdot b - \tilde {L} (t, \boldsymbol {x}, b) \right\}.$$

It is clear that $H ( t , { \boldsymbol { x } } , \cdot )$ is convex as it is the supremum of affine functions in p. Since $B ( { \pmb x } , t )$ is compact,

$$\sup _ {b \in B (\boldsymbol {x}, t)} p \cdot b - C \leq H (t, \boldsymbol {x}, p) \leq \sup _ {b \in B (\boldsymbol {x}, t)} p \cdot b + C$$

for some positive constant C depending only on x and t. It follows from the linearity of $p \cdot b$ with respect to b that

$$\sup _ {b \in B (\boldsymbol {x}, t)} p \cdot b = \sup _ {b \in \operatorname{Conv} (B (\boldsymbol {x}, t))} p \cdot b,$$

where Conv(A) denotes the convex hull of a set A. Based on the assertion above, we identify a necessary and sufficient condition in which $H ^ { * }$ is well defined.

Proposition 1. Given $\mathbf { x } \in \mathbb { R } ^ { n }$ and $t \in [ 0 , T ]$ , let

$$H ^ {*} (t, \boldsymbol {x}, b) := \sup _ {p \in \mathbb {R} ^ {n}} \left\{p \cdot b - H (t, \boldsymbol {x}, p) \right\} \tag {2.3}$$

be the convex conjugate of $H ( t , { \pmb x } , \cdot )$ . Then, $H ^ { * } ( t , x , b ) = + \infty$ for b 6∈ $\mathrm { C o n v } ( B ( { \pmb x } , t ) )$ and $H ^ { * } ( t , { \boldsymbol { x } } , b ) <$ < ∞ for $b \in \operatorname { C o n v } ( B ( \pmb { x } , t ) )$ .
