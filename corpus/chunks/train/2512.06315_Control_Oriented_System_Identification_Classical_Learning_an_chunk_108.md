Figure 15 and Figure 16 compare the inference on the $\mathcal { L } _ { 2 ^ { - } }$ - gain and input-feedforward passivity parameter, respectively, obtained from the true system and from data for various data lengths N and different large noise . Since the obtained ϵinferences are guaranteed system properties, we only obtain larger values than the true L2-gain and smaller values than the true input-feedforward passivity parameter. As expected, the determined bounds are more accurate for a larger set of data and smaller noise. Indeed, the data-driven inferences correspond almost to the true $\mathcal { L } _ { 2 }$ -gain and passivity parameter for very small noise and even for a small amount of samples. Furthermore, since the data-driven inferences do not significantly improve from $N \mathrm { ~  ~ { ~ = ~ } ~ } 7$ compared to $N \ = \ 2 1$ , we can conclude that the data $\{ x _ { k } , u _ { k } \} _ { k = 8 } ^ { 2 1 }$ is not informative for the ,(Q S R)-dissipativity verification. To further improve the ob-, ,tained bounds, one could consider a more exiting input or experiments with different initial conditions.
