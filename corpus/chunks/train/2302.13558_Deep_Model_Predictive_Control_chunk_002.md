We let R, N0, N0 denote the set of real numbers, non-negative integers and positive integers, respectively. For a given vector v and positive (semi)-definite matrix $M \succeq \mathbf { 0 } , \| \nu \| _ { M } ^ { 2 }$ is used to denote $\nu ^ { \top } M \nu$ . For a given matrix A, the trace, the largest eigenvalue, pseudo-inverse and Frobenius norm are denoted by $\operatorname { t r } ( A ) , \lambda _ { \operatorname* { m a x } } ( A ) , A ^ { \dagger }$ and $\| A \| _ { F }$ , respectively. By notation $\| A \|$ and $\| A \| _ { \infty }$ , we mean the standard 2−norm and ∞−norm, respectively, when A is a vector, and induced 2−norm and ∞−norm, respectively, when A is a matrix. A vector or a matrix with all entries 0 is represented by 0 and I is an identity matrix of appropriate dimensions. We let $\boldsymbol { M } ^ { ( i ) }$ denote the $i ^ { \mathrm { { t h } } }$ column of a given matrix M.
