which implies (8) holds on $[ t _ { i } , t _ { i + 1 } )$ with $r = \tau , g ( t ) = \| x ( t ) \| , \gamma _ { 1 } = L _ { 3 }$ , and $\gamma _ { 2 } ~ = ~ L _ { 2 }$ , where $L _ { 2 }$ is the Lipschitz constant of the function $f ( t , \cdot , u ) : C ( [ - \tau , 0 ] , \mathbb { R } ^ { n } ) \mapsto \mathbb { R } ^ { n }$ on the compact set $\{ \phi \in C ( [ - \tau , 0 ] \ \mid \ \| \phi \| _ { \tau } \ \le \ R \}$ , and $L _ { 3 }$ is the Lipschitz constant of the composite function $f ( t , \phi , k ( \cdot ) ) : \mathbb { R } ^ { n } \mapsto \mathbb { R } ^ { n }$ on the compact set $\{ x \in \mathbb { R } ^ { n } \mid \| x \| \leq R \}$ with $R = \alpha _ { 1 } ^ { - 1 } ( M )$ .

We then conclude from Lemma 2 that

$$\left\| x (t) \right\| \leq \left\| x _ {t _ {i}} \right\| _ {\tau} e ^ {\lambda \left(t - t _ {i}\right)} \quad \text {for} \quad t _ {i} \leq t < t _ {i + 1} \text {and} i \in \mathbb {Z} ^ {+} \tag {16}$$

where $\lambda = L _ { 2 } + L _ { 3 }$ and $\mathbb { Z } ^ { + }$ denotes the set of non-negative integers.

We next use mathematical induction to show that $\| \boldsymbol { x } ( t ) \| \le \| \varphi \| _ { \tau } e ^ { \lambda ( t - t _ { 0 } ) }$ for all $t \geq t _ { 0 }$ . We can see from (16) that this statement holds for $t \in [ t _ { 0 } , t _ { 1 } )$ . Suppose $\lVert x ( t ) \rVert \leq \lVert \varphi \rVert _ { \tau } e ^ { \lambda ( t - t _ { 0 } ) }$ holds for $t \in \left[ t _ { 0 } , t _ { i } \right)$ , and we will show this inequality holds for $t \in \left[ t _ { i } , t _ { i + 1 } \right)$ . By (16), we get

$$
\begin{array}{l} \| x (t) \| \leq \| x _ {t _ {i}} \| _ {\tau} e ^ {\lambda \left(t - t _ {i}\right)} \\ = e ^ {\lambda (t - t _ {i})} \sup _ {s \in [ - \tau , 0 ]} \| x (t _ {i} + s) \| \\ \leq e ^ {\lambda (t - t _ {i})} \| \varphi \| _ {\tau} e ^ {\lambda (t _ {i} - t _ {0})} \\ = \| \varphi \| _ {\tau} e ^ {\lambda (t - t _ {0})} \quad \text { for } \quad t _ {i} \leq t <   t _ {i + 1}, \\ \end{array}
$$

that is, $\| \boldsymbol { x } ( t ) \| \le \| \varphi \| _ { \tau } e ^ { \lambda ( t - t _ { 0 } ) }$ for all $t \in [ t _ { 0 } , t _ { i + 1 } )$ . By induction, we conclude that

$$\| x (t) \| \leq \| \varphi \| _ {\tau} e ^ {\lambda \left(t - t _ {0}\right)} \quad \text { for all } \quad t \geq t _ {0}. \tag {17}$$

Then (17) and (14) imply

$$\alpha_ {1} (\| x (t) \|) \leq \min \left\{\alpha_ {1} \left(\| \varphi \| _ {\tau} e ^ {\lambda \left(t - t _ {0}\right)}\right), M e ^ {- \eta \left(t - t _ {0}\right)} \right\} \quad \text {for all} \quad t \geq t _ {0}. \tag {18}$$
