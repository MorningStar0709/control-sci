# 4.1. Variable transformation

Setpoint tracking under uncertainty implies tracking different implicit references for each scenario. Therefore, the solution to (3) has states and control inputs tracking different implicit references for each parameter realization. It is possible to enforce a common zero by reformulating (3) with transformed variables. But first, let us define the following system properties:

Definition 1. (Mapping scenarios to parameter realizations) There exists a function $r \colon \mathbb { C } \mapsto \mathbb { Q }$ mapping any scenario c to its parameter realization index at its leaf node, where Q is the set of parameter realization indices.

Definition 2. (Stable and optimal equilibrium pairs for each parameter realization) For any given parameter realization $d ^ { r } \in \mathbb { D } .$ , a state-input pair $( x ^ { r } , u ^ { r } ) \in \mathbb { X } \times \mathbb { U }$ is a stable equilibrium pair for system (1) if $x ^ { r } = f ( x ^ { r } , u ^ { r } , d ^ { r } )$ ) holds. Further, , , ,if it yields the lowest stage cost among all equilibrium points, then it is the optimal equilibrium pair $( x _ { f } ^ { r } , u _ { f } ^ { r } )$ .

Then let us make the following assumption.

Assumption 1. There exists an optimal equilibrium pair $( x _ { f } ^ { r } , u _ { f } ^ { r } )$ for each parameter realization $d ^ { r } \in \mathbb { D }$ . The optimal ,equilibrium pairs for each parameter realization are the implicit references for the corresponding scenarios.

Assumption 1 implies that there exists a solution to the steady state optimization problem of (1) for each parameter realization $d ^ { r } \in \mathbb { D }$ .

To enforce a common zero to all the scenarios, reformulate (3) using the following:

$$z _ {i} ^ {c} = z _ {i} ^ {c} - x _ {f} ^ {r}, \quad \nu_ {i} ^ {c} = \nu_ {i} ^ {c} - \nu_ {f} ^ {r} \tag {4}$$

for all $c \in \mathbb { C }$ , and for all $r \in \mathbb { Q }$ . The transformed system model becomes:

$$\overline {{z}} _ {i + 1} ^ {c} = \overline {{f}} (\overline {{z}} _ {i} ^ {c}, \overline {{v}} _ {i} ^ {c}, d _ {i} ^ {c}) = f (\overline {{z}} _ {i} ^ {c} + x _ {f} ^ {r}, \overline {{v}} _ {i} ^ {c} + u _ {f} ^ {r}, d _ {i} ^ {c}) - x _ {f} ^ {r} \tag {5}$$
