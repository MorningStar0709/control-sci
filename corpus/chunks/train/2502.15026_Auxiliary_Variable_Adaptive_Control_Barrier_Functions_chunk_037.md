where g denotes the gravity constant, $c _ { d } ( t ) > 0$ and $c _ { a } ( t ) > 0$ are deceleration and acceleration coefficients respectively.

To satisfy the constraint on speed, we define a CLF $V ( \pmb { x } ( t ) ) : = ( v ( t ) - v _ { d } ) ^ { 2 }$ with $c _ { 1 } = c _ { 2 } = 1$ to stabilize v(t) to $v _ { d }$ and formulate the relaxed constraint in (6) as

$$L _ {f} V (\boldsymbol {x} (t)) + L _ {g} V (\boldsymbol {x} (t)) u (t) + c _ {3} V (\boldsymbol {x} (t)) \leq \delta (t), \tag {36}$$

where δ(t) is a relaxation that makes (36) a soft constraint.

To satisfy the constraints on safety distance and acceleration, we will define a continuous function $b ( { \pmb x } ( t ) ) = z ( t ) - l _ { p }$ as AVCBF or PACBF to guarantee $b ( { \pmb x } ( t ) ) \ge 0$ and constraint (35), then formulate all constraints into QP to get the optimal controller. The parameters are $v _ { p } = 1 3 . 8 9 m / s , v _ { d } =$ $2 4 m / s , M = 1 6 5 0 k g , g = 9 . 8 1 m / s ^ { 2 } , z ( 0 ) = 1 0 0 m , l _ { p } =$ 10m, $f _ { 0 } = 0 . 1 N , f _ { 1 } = 5 N s / m , f _ { 2 } = 0 . 2 5 N s ^ { 2 } / m , c _ { a } ( \bar { t } ) =$ $0 . 4 , \triangle t = 0 . 1 s$ .

3) Implementation with AVCBFs: Define $b ( { \pmb x } ( t ) ) = z ( t ) -$ $l _ { p } ,$ the minimum relative degree m of $b ( { \pmb x } ( t ) )$ with respect to dynamics (33) is 2. For simplicity, based on Rem. 5, we just introduce one auxiliary function as $\mathcal { A } _ { 1 } ( \pmb { x } , a _ { 1 } ( t ) ) = a _ { 1 } ( t )$ . We set the desired relative degree of the auxiliary function $m _ { a }$ equal to m. Motivated by Sec. IV-B, we define the auxiliary dynamics as

$$
\underbrace {\left[ \begin{array}{c} \dot {a _ {1}} (t) \\ \dot {\pi_ {1 , 2}} (t) \end{array} \right]} _ {\dot {\pi_ {1}} (t)} = \underbrace {\left[ \begin{array}{c} \pi_ {1 , 2} (t) \\ 0 \end{array} \right]} _ {F _ {1} (\pi_ {1} (t))} + \underbrace {\left[ \begin{array}{c} 0 \\ 1 \end{array} \right]} _ {G _ {1} (\pi_ {1} (t))} \nu_ {1} (t). \tag {37}
$$

The HOCBFs for $\mathcal { A } _ { 1 }$ are defined as

$$\varphi_ {1, 0} (\pi_ {1}) := a _ {1},\varphi_ {1, 1} (\boldsymbol {\pi} _ {1}) := \dot {\varphi} _ {1, 0} (\boldsymbol {\pi} _ {1}) + l _ {1} \varphi_ {1, 0} (\boldsymbol {\pi} _ {1}), \tag {38}\varphi_ {1, 2} (\boldsymbol {\pi} _ {1}, \nu_ {1}) := \dot {\varphi} _ {1, 1} (\boldsymbol {\pi} _ {1}, \nu_ {1}) + l _ {2} \varphi_ {1, 1} (\boldsymbol {\pi} _ {1}),$$

where $\alpha _ { 1 , 1 } ( \cdot ) , \alpha _ { 1 , 2 } ( \cdot )$ are defined as linear functions. The AVCBFs are then defined as
