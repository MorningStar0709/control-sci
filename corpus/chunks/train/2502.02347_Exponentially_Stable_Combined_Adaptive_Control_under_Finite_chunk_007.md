Assumption 3. The regressor vector ϕ satisfies the finite excitation condition. Therefore from (2), there exists positive constants γ and T so that there is at least a sequence of q numbers in $t _ { i } \in [ t , t + T ] , i \in \mathbb { N }$ , satisfying the following inequality,

$$\left\| \left[ \boldsymbol {\varphi} \left(\mathbf {x} \left(t _ {1}\right), t _ {1}\right) \dots \boldsymbol {\varphi} \left(\mathbf {x} \left(t _ {q}\right), t _ {q}\right) \right] ^ {- 1} \right\| \leq \gamma , \tag {12}$$

where $q \geq n + 1 + p .$ , and $\gamma$ is the excitation level of the regressor vector $\varphi .$ .

Next, follows the main results of the work where we propose an algorithm for extracting the system parameters’ ideal values, and the adaptation laws are designed to fulfill the control objectives under the stated assumptions.
