# B. Data-driven STS

Although thmeasurements $\{ x ( T ) \} _ { T = 0 } ^ { \rho + s } , \ \{ u ( T ) \} _ { T = 0 } ^ { \rho + s - 1 } \ ( T , \rho , s \in \mathbb { N } , \rho \ >$ $0 , s > 0 )$ of the perturbed system (2) are available. Based on the model-based function in (31), a data-based discretefunction using the data time STS is proposed. Our idea is to rebuild a self-triggering $\{ x ( T ) \} _ { T = 0 } ^ { \rho + s } , \ \{ u ( T ) \} _ { T = 0 } ^ { \rho + s - 1 }$ to replace the $[ A ^ { s } \ { \underline { { B } } } ^ { s } ]$ ]-based representation (31). To that end, we recall the data-driven parametrization of the lifted matrix $[ A ^ { s } \ \underline { { B } } ^ { s } ]$ in [25]. Similar to the system expression in (27), we firstly re-write the perturbed system (2) as follows

$$
\begin{array}{l} x (T + s) = A ^ {s} x (T) + \underline {{B}} ^ {s} \left[ \begin{array}{c} u (T) \\ \vdots \\ u (T + s - 1) \end{array} \right] \\ + \left[ \begin{array}{c c c} A ^ {s - 1} B _ {w} & \dots & B _ {w} \end{array} \right] \left[ \begin{array}{c} w (T) \\ \vdots \\ w (T + s - 1) \end{array} \right]. \tag {32} \\ \end{array}
$$

Recall that the measured data $\{ x ( T ) \} _ { T = 0 } ^ { \rho + s - 1 } , \{ u ( T ) \} _ { T = 0 } ^ { \rho + s - 2 }$ are T =0 T =0 corrupted by the unknown noise {w(T )}ρ+s−2T =0 . Let us define $\{ w ( \bar { T } ) \} _ { T = 0 } ^ { \rho + s - 2 }$ the following matrices containing the measurements

$$
\begin{array}{l} X _ {+} ^ {s} := \left[ \begin{array}{c c c c} x (s) & x (1 + s) & \dots & x (\rho + s - 1) \end{array} \right] \\ U ^ {s} := \left[ \begin{array}{c c c c} u (0) & u (1) & \dots & u (\rho - 1) \\ \vdots & \vdots & & \vdots \\ u (s - 1) & u (s) & \dots & u (\rho + s - 2) \end{array} \right]. \\ \end{array}
$$

We further define the following lifted disturbance

$$
\begin{array}{l} W ^ {1} := \left[ \begin{array}{c c c c} w (0) & w (1) & \dots & w (\rho - 1) \end{array} \right] \\ \underline {{W}} ^ {s} := \left[ \begin{array}{c c c} w (0) & \dots & w (\rho - 1) \\ \vdots & & \vdots \\ w (s - 1) & \dots & w (\rho + s - 2) \end{array} \right] \\ W ^ {s} := \left[ \begin{array}{c c c} A ^ {s - 1} B _ {w} & \dots & B _ {w} \end{array} \right] \underline {{W}} ^ {s}, f o r s > 1. \\ \end{array}
$$

Then, it is clear that

$$X _ {+} ^ {s} = A ^ {s} X + \underline {{B}} ^ {s} U ^ {s} + B _ {w} ^ {s} W ^ {s} \tag {33}$$

where $B _ { w } ^ { 1 } : = B _ { w } , B _ { w } ^ { s } : = I$ for $s > 1$ . Similar to Assumption 1, we make the following assumption on the noise.

Assumption 2 (Lifted noise bound). The noise sequence $\{ w ( t ) \} _ { t = 0 } ^ { \bar { \rho } + s - 2 }$ collected in the matrix $W ^ { s }$ satisfies $W ^ { s } \in \mathcal { W } ^ { s }$ with
