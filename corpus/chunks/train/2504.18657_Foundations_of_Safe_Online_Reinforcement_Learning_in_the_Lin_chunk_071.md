By the choice of K′, using the controller Cθ∗Kopt(θ∗,t) under dynamics θ∗ results in the exact $K ^ { \prime }$ $C _ { K _ { \mathrm { o p t } } ( \theta ^ { * } , t ) } ^ { \theta ^ { * } }$ $\theta ^ { * }$ same sequence of states as using the controller $C _ { K } ^ { \theta } ,$ ′ under dynamics $\theta .$ This is because $a - b K ^ { \prime } = a ^ { * } - b ^ { * } K _ { \mathrm { o p t } } ( \theta ^ { * } , t )$ y cons for all ction of truncated linear controllers implies The controls will however be different, and $a x + b C _ { K ^ { \prime } } ^ { \theta } ( x ) = a ^ { * } + b ^ { * } C _ { K _ { \mathrm { o p t } } ( \theta ^ { * } , t ) } ^ { \theta ^ { * } }$ $x .$

Define $x _ { 0 } , x _ { 1 } , . . . , x _ { t }$ as the sequence of states when using controller $C _ { K _ { \mathrm { o p t } } ( \theta ^ { * } , t ) } ^ { \theta ^ { * } }$ C θ Kopt(θ∗,t) under dynamics $\theta ^ { * }$ starting at state $x _ { 0 } = 0$ . Then we have the following result.

$$
\left| r C _ {K _ {\text {opt}} \left(\theta^ {*}, t\right)} ^ {\theta^ {*}} \left(x _ {i}\right) ^ {2} - r C _ {K ^ {\prime}} ^ {\theta} \left(x _ {i}\right) ^ {2} \right| = \left\{ \begin{array}{l l} \left| r x _ {i} ^ {2} \left(\left(K _ {\text {opt}} \left(\theta^ {*}, t\right)\right) ^ {2} - \left(K ^ {\prime}\right) ^ {2}\right) \right| & \text {if} x _ {i} \in \left[ \frac {D _ {L}}{a ^ {*} - b ^ {*} K _ {\text {opt}} \left(\theta^ {*} , t\right)}, \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\text {opt}} \left(\theta^ {*} , t\right)} \right] \\ \left| r \left(\frac {D _ {U} - a ^ {*} x _ {i}}{b ^ {*}}\right) ^ {2} - r \left(\frac {D _ {U} - a x _ {i}}{b}\right) ^ {2} \right| & \text {if} x _ {i} > \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\text {opt}} \left(\theta^ {*} , t\right)} \\ \left| r \left(\frac {D _ {L} - a ^ {*} x _ {i}}{b ^ {*}}\right) ^ {2} - r \left(\frac {D _ {L} - a x _ {i}}{b}\right) ^ {2} \right| & \text {if} x _ {i} <   \frac {D _ {L}}{a ^ {*} - b ^ {*} K _ {\text {opt}} \left(\theta^ {*} , t\right)} \end{array} \right. \tag {45}
$$

By Equation (44), this implies the following.
