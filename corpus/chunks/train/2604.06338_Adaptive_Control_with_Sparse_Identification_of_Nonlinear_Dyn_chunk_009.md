# IV. STABILITY ANALYSIS

To facilitate the analysis, we analyze the closed-loop system with error states $z = [ e ^ { \top } , \tilde { \theta } ^ { \top } ] ^ { \top } \in \mathbb { R } ^ { n + p }$ . As shown in Lemma 2, due to the properties of the projection operator, if $\hat { \theta } ( t _ { 0 } ) \in \Theta _ { \epsilon }$ , then $\hat { \theta } ( t ) \in \Theta _ { \epsilon }$ for all $t \geq t _ { 0 }$ . Consequently, the parameter estimation error is uniformly bounded. We define this bounding set as the closed ball $\overline { { B } } _ { \widetilde { \theta } } ( 2 r _ { \theta } + \epsilon )$ of radius $2 r _ { \theta } + \epsilon ,$ ensuring that $\lVert \tilde { { \boldsymbol { \theta } } } ( t ) \rVert \leq 2 r _ { \theta } + \epsilon$ for all $t \geq t _ { 0 }$ .

Define $d ( t , e ) : = Y ( e + x _ { d } ( t ) ) \tilde { \theta } ( t )$ . If $e \in \overline { { B } } _ { e } ( r _ { e } ) : =$ $\{ e \in \mathbb { R } ^ { n } : \| e \| \leq r _ { e } \}$ for some $r _ { e } ~ > ~ 0$ , then $\boldsymbol { x } = \boldsymbol { e } +$ $x _ { d } ( t ) \in \overline { { B } } ( r _ { e } + \overline { { x } } _ { d } ) \subset \mathcal { X }$ . Since Y is continuous, there exists a constant $\bar { Y } \ > \ 0$ such that $\| Y ( x ) \| \leq { \bar { Y } }$ for all $x \in \overline { { B } } _ { x } ( r _ { e } + \overline { { x } } _ { d } ) \subseteq \mathcal { X }$ . Consequently, the residual is bounded by

$$\| d (t, e) \| \leq \bar {d}, \quad \forall e \in \overline {{B}} _ {e} (r _ {e}). \tag {15}$$

where $d : = ( 2 r _ { \theta } + \epsilon ) \bar { Y }$ . The error dynamics (4) can then be expressed as

$$\dot {e} = - K e + d (t, e), \tag {16}$$

where $d ( t , e )$ acts as a bounded disturbance. Since $Y$ is locally Lipschitz with respect to $x ,$ and both $x _ { d }$ and $\tilde { \theta }$ are continuous in t, the vector field (16) satisfies the conditions of Picard-Lindelof Theorem [35, Theorem 3.1]. Thus, for ¨ any initial condition $( t _ { 0 } , e _ { 0 } )$ , there exists a $\delta > 0$ such that $[ t _ { 0 } , t _ { 0 } { + } \delta )$ is the maximal interval of existence for the unique solution $e ( \cdot )$ of (16).

Theorem 1: Consider the closed-loop system given by (4) and (11). Let $r _ { e } > 0$ and $r > 0$ . Supposed that Assumption 1 and the initial closed-loop state satisfies

$$z (t _ {0}) \in B _ {e} (r _ {e}) \times \overline {{B}} _ {\tilde {\theta}} (2 r _ {\theta} + \epsilon). \tag {17}$$

Assume that the feedback gain $K = K ^ { \top } \succ 0$ is selected sufficiently large such that the gain conditions

$$\left\| e (t _ {0}) \right\| ^ {2} < r _ {e} ^ {2} \quad \text { and } \quad \frac {\overline {{d}} ^ {2}}{k ^ {2}} < r _ {e} ^ {2}, \tag {18}$$
