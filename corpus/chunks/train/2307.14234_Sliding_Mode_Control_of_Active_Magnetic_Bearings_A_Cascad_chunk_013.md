which shows that Assumption 4 from (Lor´ıa, 2008) is also satisfied. The finite-time stability of the origin of $\left( \Sigma _ { 2 } \right) ~ -$ note that this holds irrespectively of the dependence on the trajectories of (Σ1) - satisfies Assumption 7, which requires UGAS for $\left( \Sigma _ { 2 } \right)$ .

The second and final condition of Theorem 2 in (Lor´ıa, 2008) requires that there exist class  functions $\alpha _ { 5 } ( \sigma )$ ), $\alpha _ { 5 } ^ { \prime } ( \lVert \dot { \boldsymbol { \xi } } \rVert )$ such that

$$\left| \frac {\partial V}{\partial \sigma} h (z, e, \tilde {v}) \right| \leq \alpha_ {5} (\sigma) \alpha_ {5} ^ {\prime} (\| \boldsymbol {\xi} \|)$$

where V was defined earlier. Since $V = V _ { 1 }$ , the foregoing inequality was shown to hold for $\alpha _ { 5 } ( \sigma ) \triangleq \alpha _ { 4 } ( \sigma ) = 2 | \sigma |$ and $\begin{array} { r } { \alpha _ { 5 } ^ { \prime } ( \lVert \pmb { \xi } \rVert ) \triangleq \alpha _ { 4 } ^ { \prime } ( \lVert \pmb { \xi } \rVert ) = \frac { \mu _ { 0 } n ^ { 2 } A } { 4 m } \sqrt { L _ { v } ^ { 2 } + 1 } \lVert \pmb { \xi } \rVert } \end{array}$ . Moreover, it is required that for every positive upper bound $\rho$ of the solutions of $( \Sigma _ { 2 } ) \ \exists \lambda _ { \rho } , \eta _ { \rho } > \bar { 0 }$ such that

$$\forall t \geq 0, | \sigma | \geq \eta_ {\rho} \Rightarrow \alpha_ {5} (\sigma) \leq \lambda_ {\rho} W (\sigma)$$

where W is a positive semi-definite function. Indeed, selecting $W ( { \boldsymbol { \sigma } } ) \triangleq 2 { \boldsymbol { \sigma } } ^ { 2 }$ one obtains:

$$W (\sigma) = 2 \sigma^ {2} \geq \eta_ {\rho} 2 | \sigma | = \eta_ {\rho} \alpha_ {5} (\sigma) \Leftrightarrow \alpha_ {5} (\sigma) \leq \lambda_ {\rho} W (\sigma)$$

with $\begin{array} { r } { \lambda _ { \rho } \triangleq \frac { 1 } { \eta _ { \rho } } } \end{array}$ ηρ . With this, all conditions of Theorem 2 are satisfied and by Proposition 2 in (Lor´ıa, 2008), the origin of $\left( \Sigma _ { 1 } \right) - \left( \Sigma _ { 2 } \right)$ is a UAS equilibrium point. ■
