Definition 2. (modified from [15, Def. 7]) Let Hi and $\mathcal { C } _ { S _ { i } }$ be defined as in equations (4) and (5), respectively, $\forall i = 0 , \ldots r .$ The function $h ( \tilde { { \boldsymbol { x } } } , t ) : \mathbb { D } \times \mathbb { I }  \mathbb { R }$ is a High Order Barrier Function (HOCBF) for (2) if it is r times differentiable in x˜ and t, and there exists $\alpha _ { j } ( t ) : \mathbb { R } _ { \geq 0 }  \mathbb { R } _ { \geq 0 } \forall j = 0 , \ldots r - 1$ class K-functions such that $\alpha _ { j }$ is of class $C ^ { r - j }$ and

$$
\sup _ {\boldsymbol {u} \in \mathbb {U}} \left[ \frac {\partial H _ {r - 1} (\tilde {\boldsymbol {x}} , t)}{\partial t} + \mathcal {L} _ {f} H _ {r - 1} (\tilde {\boldsymbol {x}}, t) + \mathcal {L} _ {g} H _ {r - 1} (\tilde {\boldsymbol {x}}, t) \boldsymbol {u} + \right.
$$

$$
\left. \mathcal {L} _ {g} H _ {r - 1} (\tilde {\boldsymbol {x}}, t) \boldsymbol {d} + \alpha_ {r - 1} (H _ {r - 1} (\tilde {\boldsymbol {x}}, t)) \right] \geq 0, \tag {6}
$$

$$
\forall \tilde {\boldsymbol {x}} \in \cap_ {r - 1} ^ {0} \mathcal {C} _ {S _ {i}} (t).
$$

If condition (6) is satisfied, then there exists a control input ${ \textbf { \em u } } \in \mathbb { U }$ that renders $\cap _ { r - 1 } ^ { 0 } { \mathcal { C } } _ { S _ { i } } ( t )$ forward invariant [15, Thm. 5]. For easiness of notation we define the function $\tilde { \zeta } ( \tilde { { \boldsymbol { x } } } , { \boldsymbol { u } } , \delta { \boldsymbol { d } } , t ) : \mathbb { D } \times \mathbb { U } \times \mathbb { W } \times \mathbb { I } \to \mathbb { R }$ as

$$
\tilde {\zeta} (\tilde {\boldsymbol {x}}, \boldsymbol {u}, \delta \boldsymbol {d}, t) := \frac {\partial H _ {r - 1} (\tilde {\boldsymbol {x}} , t)}{\partial t} + \mathcal {L} _ {f} H _ {r - 1} (\boldsymbol {x}, t) + \tag {7}
$$

$$
\mathcal {L} _ {g} H _ {r - 1} (\tilde {\boldsymbol {x}}, t) (\boldsymbol {u} + \boldsymbol {d}) + \alpha_ {r - 1} (H _ {r - 1} (\tilde {\boldsymbol {x}}, t))
$$

and forward invariance of the safe set $\cap _ { r - 1 } ^ { 0 } { \mathcal { C } } _ { S _ { i } } ( t )$ is enforced by ensuring that the condition $\tilde { \zeta } ( \tilde { { \boldsymbol { x } } } , { \boldsymbol { u } } , \delta d , t ) \geq 0$ is met everywhere inside $\cap _ { r - 1 } ^ { 0 } { \mathcal { C } } _ { S _ { i } } ( t )$ . Similarly to the function $\eta ( \pmb { x } , \pmb { u } , t )$ , we define the function $\zeta ( { \pmb x } , { \pmb u } , t ) \triangleq \tilde { \zeta } ( \tilde { { \pmb x } } , { \pmb u } , \mathbf 0 , t )$ .
