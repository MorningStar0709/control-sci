that is, $S _ { \{ a _ { 1 } : a _ { R } \} }$ is the set of all permutations of $\{ a _ { 1 } , \ldots , a _ { R } \}$ . For any two distributions $\mu , \nu$ on a metric space Z with metric $d _ { Z }$ , let $\mathsf { C p } ( \mu , \nu )$ denote the set of all couplings of $\mu , \nu$ in $\mathcal { P } ( Z \times Z )$ ; that is, $\xi \in \mathsf { C p } ( \mu , \nu ) \mathrm { i f } \xi ( \cdot \times \mathsf { X } ) = \mu$ and $\xi ( { \mathsf { X } } \times \cdot { \mathsf { \Omega } } ) = \nu .$ . In view of this, Wasserstein distance of order 1 between $\mu$ and ν is defined as follows [38]

$$W _ {1} (\mu , \nu) := \inf \{\mathbb {E} _ {\xi} [ d _ {\mathsf {Z}} (x, y) ]: \xi \in \mathsf {C p} (\mu , \nu) \}$$

Define $\bar { c } _ { \operatorname* { m a x } } : = \operatorname* { s u p } _ { ( x , a , z ) \in \mathsf { X } \times \mathsf { A } \times \mathsf { X } } \bar { c } ( x , a , z )$ , where c¯ is the non-negative one-stage cost function of the mean-field game. Introduce the following function class

$$\mathcal {F} := \{g: \mathsf {X} \to [ 0, K ]; \| g \| _ {\mathrm{Lip}} \leq 1, g (y _ {*}) = 0 \}$$

where $y _ { * } \in \mathsf { X }$ is some arbitrary fixed point and $K \ge \operatorname* { m a x } \{ \bar { c } _ { \mathrm { m a x } } , \mathrm { d i a m } ( \mathsf X ) \}$ . For any $\varepsilon > 0$ we define the ε-covering number of $\mathcal { F }$ as follows

$$\mathbb {N} (\varepsilon , \mathcal {F}) := \inf \left\{N: \exists \{g _ {1}, \dots , g _ {N} \} \subset \mathcal {F} \text { such that } \inf _ {i = 1, \dots , N} \| g - g _ {i} \| _ {\infty} < \varepsilon , \forall g \in \mathcal {F} \right\}$$

Since $\mathcal { F }$ is totally bounded in $C _ { b } ( \mathsf X )$ with respect to the sup-norm by Arzela–Ascoli theorem [10, Theorem $2 . 4 . 7 ] , \mathbb { N } ( \varepsilon , \mathcal { F } )$ is finite for all $\varepsilon > 0$ . For some cases, one can obtain an upper bound on $\mathbb { N } ( \varepsilon , \mathcal { F } )$ in terms of $\varepsilon , K$ , diameter of $\mathsf { X } ,$ , and dimension of X (see [18, Lemma 6] and [17, Lemma 4.2]). Indeed, by [18, Lemma 6], we have

$$\mathbb {N} (\varepsilon , \mathcal {F}) \leq \left(\frac {8 K}{\varepsilon}\right) ^ {\mathbb {N} \left(\frac {\varepsilon}{8 K}, \mathsf {X}\right)}$$

where $\mathbb { N } \left( \frac { \varepsilon } { 8 K } , \mathsf { X } \right)$ is $\frac { \varepsilon } { 8 K }$ -covering number of X with respect to its metric $d _ { \mathsf { X } }$ . If X is doubling space with doubling dimension ddim $. ( \mathsf { X } )$ (see [17, p. 107] for the definition of doubling spaces), we also have
