# Definition 1. Quantization and control policies in ABSA-1:

The communication policy $\pi ^ { c , A B S A - 1 } ( \cdot )$ designed $b y$ ABSA-1 will be obtained by solving the following k-median clustering problem

$$\min _ {\mathcal {P}} \quad \sum_ {i = 1} ^ {| \mathcal {C} |} \sum_ {\mathbf {s} (t) \in \mathcal {P} _ {i}} \left| \pi^ {*} (\mathbf {s} (t)) - \mu_ {i} \right|, \tag {5}$$

where $\mathcal { P } = \{ { \mathcal { P } } _ { 1 } , . . . , { \mathcal { P } } _ { B } \}$ is a partition of S and $\mu _ { i }$ is the centroid of each cluster i. The communication policy of ABSA-$I \cdot \pi ^ { c , A B \dot { S } A - 1 } ( \cdot )$ - is an arbitrary non-injective mapping such that $\forall k \in \{ 1 , . . . , B \} : \pi ^ { c , A B S \dot { A } - 1 } ( \mathbf { s } ) \stackrel { \sim } { = } { \mathbf { c } } ^ { ( k ) }$ if and only if $\mathbf { s } \in \mathcal { P } _ { k }$ . Now let $C _ { g }$ be a function composition operator such that $C _ { g } f = g \circ f .$ . We define the operator $\Pi ^ { m ^ { * } } \triangleq C _ { g } ,$ with $g = \pi ^ { * } \big ( \pi ^ { c , A B S A - 1 ^ { - 1 } } ( \cdot ) \big ) ^ { 8 }$ .

The optimality of the proposed ABSA-1 algorithm is subsequently provided in Theorem 2.

Lemma 2. The communication policy $\pi ^ { c , A B S A - 1 }$ - as described by Definition 1 - will carry out lossless compression of observation data w.r.t. the average return $i f | C | \geq | \mathcal { M } | ^ { N }$ .
