# 2.4 CONTROL LAW DESIGN

The systems motion on the sliding surface can be given as an interesting geometric interpretation, as an average of the system’s dynamics on both sides of the surface. Upon the interjection on the sliding surface, problem of reachability has to be solved. It involves selection of a state feedback control function $u : R ^ { n } \to R ^ { m }$ which forces the state ?? towards the sliding surface and thereafter maintains it on the surface i.e. the controlled system must satisfy the reaching condition. The dynamics while in sliding mode can be written as ${ \dot { s } } ( x ) =$ 0. By solving the equation formally for the control input, obtained is an expression for ??.
