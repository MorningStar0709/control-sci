Proof. In light of Theorem 2.1 in [49], in order to establish the existence and uniqueness of a stationary distribution, it is sufficient to show that $( X _ { t } ^ { \xi ^ { * } ( \theta ) } , Y _ { t } ) _ { t \geq 0 }$ is a regenerative process (cf. Definition (D1) in [49]) with finite length regenerative epochs. In particular, for $( x , i ) \in { \mathcal { O } }$ , setting $\eta : = \operatorname* { i n f } \{ t > 0 : X _ { t } ^ { \xi ^ { * } ( \theta ) } = \alpha _ { Y _ { t } } ( \theta ) \} , \ \mathbb { P } _ { ( x , i ) ^ { - } } \mathrm { a . s . }$ , by strong Markov property we have that the processes $( X _ { t + \eta } ^ { \xi ^ { * } ( \theta ) } , Y _ { t + \eta } ) _ { t \geq 0 }$ and the collection of random variables $( ( X _ { t } ^ { \xi ^ { * } ( \theta ) } , Y _ { t } ) _ { t < \eta } , \eta )$ are independent. Moreover, when starting from points in the set same distribution as the process with regenerative epoch η. In or $( X _ { t + \eta } ^ { \xi ^ { * } ( \theta ) } , Y _ { t + \eta } ) _ { t \geq 0 }$ $\{ ( \alpha _ { i } ( \theta ) , i ) | i \in \mathbb { Y } \}$ . Thus, e proof, $( X _ { t } ^ { \xi ^ { * } ( \theta ) } , Y _ { t } ) _ { t \geq 0 }$ , the process is a reow that $( X _ { t } ^ { \xi ^ { * } ( \theta ) } , Y _ { t } ) _ { t \geq 0 }$ has the ess,, for $\mathbb { E } _ { ( x , i ) } [ \eta ] < \infty .$ any $( x , i ) \in \mathcal { O }$ .

For $( x , i ) \in { \mathcal { O } }$ and $\begin{array} { r } { y \in [ \operatorname* { m i n } _ { j \in \mathbb { Y } } \alpha _ { j } ( \theta ) , \operatorname* { m a x } _ { j \in \mathbb { Y } } \beta _ { j } ( \theta ) ] } \end{array}$ , we define the F-hitting time $\eta ( y ) : =$ Xx,ξ∗(θ)t ≤ inf $X _ { t } ^ { x , \xi ^ { * } ( \theta ) } \leq X _ { t } ^ { \beta _ { i } \left( \theta \right) , \xi ^ { * } ( \theta ) } , \ \mathbb { P } _ { i ^ { - } } \mathrm { a . s . }$ $\left\{ t > 0 : X _ { t } ^ { \xi ^ { * } ( \theta ) } = y \right\}$ . If $Y _ { 0 } = i$ 0   . Thus, we have by comparison principle (see Proposition B.2) we have that

$$\mathbb {E} _ {(x, i)} [ \eta ] \leq \mathbb {E} _ {(\beta_ {i} (\theta), i)} [ \eta ] \leq \mathbb {E} _ {(\beta_ {i} (\theta), i)} [ \eta (\alpha_ {i} (\theta)) ]. \tag {4.5}$$

We now estimate $\mathbb { E } _ { ( \beta _ { i } ( \theta ) , i ) } [ \eta ( \alpha _ { i } ( \theta ) ) ]$ ]. Let τ be the first jump of the process Y , and, denoting by Z the solution to the SDE

$$d Z _ {t} = b (Z _ {t}, i) d t + \sigma (Z _ {t}, i) d W _ {t}, \quad Z _ {0} = \beta_ {i} (\theta), \tag {4.6}$$
