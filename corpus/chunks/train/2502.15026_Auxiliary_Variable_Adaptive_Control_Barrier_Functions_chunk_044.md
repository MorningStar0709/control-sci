$$\min _ {\boldsymbol {u} (t)} \int_ {0} ^ {T} [ \boldsymbol {u} (t) ^ {T} \boldsymbol {u} (t) ] d t, \tag {49}$$

when the input is constrained in the form

$$\boldsymbol {u} _ {\min} \leq \boldsymbol {u} (t) \leq \boldsymbol {u} _ {\max}, \forall t \in [ 0, T ]. \tag {50}$$

To satisfy the constraint on rechability, we define a CLF $V ( \pmb { x } ( t ) ) : = ( \theta ( t ) - \theta _ { d } ) ^ { 2 }$ with $\begin{array} { r } { \theta _ { d } = a t a n 2 \big ( \frac { y _ { d } - y ( t ) } { x _ { d } - x ( t ) } \big ) , c _ { 1 } = c _ { 2 } = } \end{array}$ xd−x(t) , c1 = c2 = ( yd−y(t) ) 1 to stabilize $\theta ( t )$ to $\theta _ { d }$ and formulate the relaxed constraint in (6) as

$$L _ {f} V (\boldsymbol {x} (t)) + L _ {g} V (\boldsymbol {x} (t)) u _ {1} (t) + c _ {3} V (\boldsymbol {x} (t)) \leq \delta (t), \tag {51}$$

where $\delta ( t )$ is a relaxation that makes (51) a soft constraint.

To satisfy the constraints on safety distance and control input, we will define a continuous function $b ( { \pmb x } ( t ) ) = ( { \pmb x } -$ $x _ { o } \hat { ) ^ { 2 } } + ( y - y _ { o } ) ^ { 2 } - r _ { o } ^ { 2 }$ as HOCBF or AVCBF to guarantee $b ( { \pmb x } ( t ) ) ~ \ge ~ 0$ and constraint (50), then formulate all constraints into QP to get the optimal controller. The parameters are $v ( 0 ) ~ = ~ 2 m / s , \theta _ { 0 } ~ = ~ 0 r a d , M ~ = ~ 1 6 5 0 k g , ( x _ { o } , y _ { o } ) ~ =$ $( 0 , 0 ) , r _ { o } = 1 m , r _ { d } = 0 . 1 m , \triangle t = 0 . 1 s , u _ { \mathrm { 1 , m a x } } = - u _ { \mathrm { 1 , m i n } } =$ $5 r a d / s , u _ { 2 , \mathrm { m a x } } = - u _ { 2 , \mathrm { m i n } } = 8 2 5 0 N , T = 1 0 s .$ ，

3) Implementation with AVCBFs: Define $b ( { \pmb x } ( t ) ) = ( { \pmb x } -$ $x _ { o } ) ^ { 2 } + \dot { ( } y - y _ { o } ) ^ { 2 } - r _ { o } ^ { 2 } .$ , the minimum relative degree m of $b ( { \dot { \pmb x } } ( t ) )$ with respect to dynamics (48) is 2. We set the desired relative degree of the auxiliary function $m _ { a }$ equal to m. Based on Rem. 5, we can introduce up to two auxiliary functions as $\mathbf { \mathcal { A } } _ { 1 } ( \mathbf { \mathcal { x } } , a _ { 1 } ( t ) ) = a _ { 1 } ( t )$ and $\mathbf { \nabla } \mathcal { A } _ { 2 } ( \mathbf { 1 } \mathbf { 1 } , a _ { 2 } ( t ) ) = a _ { 2 } ( t )$ . Motivated by Sec. IV-B, we define the auxiliary dynamics for $a _ { 1 } ( t )$ as

$$
\underbrace {\left[ \begin{array}{c} \dot {a} _ {1} (t) \\ \dot {\pi} _ {1 , 2} (t) \end{array} \right]} _ {\boldsymbol {\pi} _ {1} (t)} = \underbrace {\left[ \begin{array}{c} \pi_ {1 , 2} (t) \\ 0 \end{array} \right]} _ {F _ {1} (\boldsymbol {\pi} _ {1} (t))} + \underbrace {\left[ \begin{array}{c} 0 \\ 1 \end{array} \right]} _ {G _ {1} (\boldsymbol {\pi} _ {1} (t))} \nu_ {1} (t). \tag {52}
$$
