where $V _ { a } ( { \boldsymbol { x } } ( t ) ) = { \boldsymbol { x } } ^ { \top } ( t ) P { \boldsymbol { x } } ( t ) , P \succ 0 ; \eta ( t )$ is given in (8) and $\eta ( \tau _ { i } ^ { k } ) \geq 0$ due to Lemma 2; moreover, $V _ { l } ( x ( t ) , t )$ is a novel DLF given as

$$V _ {l} (x (t), t) = \sum_ {i = 1} ^ {3} V _ {l i} (t) \tag {13}$$

with $V _ { l 1 } ( t ) : = 2 \phi _ { 1 } ^ { \top } ( t ) S \phi _ { 2 } ( t )$ , and

$$V _ {l 2} (t) := (\tau_ {j + 1} ^ {k} - t) \Big [ \sum_ {i = \tau_ {j} ^ {k}} ^ {t} y ^ {\top} (i) R _ {1} y (i) - y ^ {\top} (t) R _ {1} y (t) \Big ],V _ {l 3} (t) := \left(t - \tau_ {j} ^ {k}\right) \left[ \sum_ {i = t} ^ {\tau_ {j + 1} ^ {k}} y ^ {\top} (i) R _ {2} y (i) - y ^ {\top} (t) R _ {2} y (t) \right],$$

where S, $R _ { 1 } \ \succ \ 0 , \ R _ { 2 } \ \succ \ 0 ;$ and, $y ( i ) : = x ( i + 1 ) - x ( i )$ , $\phi _ { 0 } : = \left[ x ^ { \top } ( \tau _ { j } ^ { k } ) , x ^ { \top } ( \tau _ { j + 1 } ^ { k } ) \right] ^ { \top } , \phi _ { 1 } ( t ) : = \left[ ( t - \tau _ { j } ^ { k } ) \phi _ { 0 } ^ { \top } , x ^ { \top } ( t ) - x ^ { \top } ( t - t _ { j } ^ { k } ) \phi _ { 0 } ^ { \top } \right] ,$ $x ^ { \top } ( \tau _ { j } ^ { k } ) , \sum _ { i = \tau _ { j } ^ { k } } ^ { t } x ^ { \top } ( i ) - x ^ { \top } ( \tau _ { j } ^ { k } ) \big ] ^ { \top } , \ \phi _ { 2 } ( t ) : = \big [ ( \tau _ { j + 1 } ^ { k } - t ) \phi _ { 0 } ^ { \top }$ ,

$$\left. x ^ {\top} \left(\tau_ {j + 1} ^ {k}\right) - x ^ {\top} (t), \sum_ {i = t} ^ {\tau_ {j + 1} ^ {k}} x ^ {\top} (i) - x ^ {\top} \left(\tau_ {j + 1} ^ {k}\right) \right] ^ {\top}.$$

According to Lemma 3, we calculate the forward difference $\Delta V ( x , t ) : = V ( x ( t + 1 ) , t + 1 ) - V ( x ( t ) , t )$ , yielding

$$\Delta V (x, t) = \Delta V _ {a} (t) + \Delta V _ {l} (t) + \Delta \eta (t) \tag {14}$$

where $\Delta { \eta } ( t ) = { \eta } ( t + 1 ) - { \eta } ( t )$ , and

$$\Delta V _ {a} (t) = \xi^ {\top} (t) \left(L _ {2} ^ {\top} P L _ {2} - L _ {1} ^ {\top} P L _ {1}\right) \xi (t),\Delta V _ {l 1} (t) = 2 \xi^ {\top} (t) \left[ \Pi_ {1} ^ {\top} S \Pi_ {2} - \Pi_ {3} ^ {\top} S \Pi_ {4} + \left(t - \tau_ {j} ^ {k}\right) \Pi_ {5} ^ {\top} S \Pi_ {6} \right.\left. + \left(\tau_ {j + 1} ^ {k} - t\right) \Pi_ {7} ^ {\top} S \Pi_ {8} \right] \xi (t),\Delta V _ {l 2} (t) = \xi^ {\top} (t) \left[ \left(\tau_ {j + 1} ^ {k} - t - 1\right) \left(L _ {2} - L _ {1}\right) ^ {\top} R _ {1} \left(L _ {2} - L _ {1}\right) \right] \xi (t)- \sum_ {i = \tau_ {j} ^ {k}} ^ {t - 1} y ^ {\top} (i) R _ {1} y (i),\Delta V _ {l 3} (t) = \xi^ {\top} (t) \left[ \left(t - \tau_ {j} ^ {k} + 1\right) \left(L _ {2} - L _ {1}\right) ^ {\top} R _ {2} \left(L _ {2} - L _ {1}\right) \right] \xi (t)- \sum_ {i = t} ^ {\tau_ {j + 1} ^ {k} - 1} y ^ {\top} (i) R _ {1} y (i),$$

where the notation ξ(t) is given as $\xi ( t ) : = [ x ^ { \top } ( t ) , x ^ { \top } ( t + 1 )$ ,
