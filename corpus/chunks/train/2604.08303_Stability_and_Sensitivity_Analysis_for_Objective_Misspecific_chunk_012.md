1) Each agent solves for open-loop, finite horizon Nash equilibrium: At time t and state $x _ { t } = \mathbf { x } ,$ , agent j formulates the following finite horizon game:

$$
i \in N: \quad \left\{ \begin{array}{l l} \min _ {u _ {i}, x} & \sum_ {k = 0} ^ {K - 1} g _ {i} ^ {(j)} (x _ {k}, u _ {k}) \\ \text { s.t. } & x _ {k + 1} = A x _ {k} + \sum_ {i \in N} B _ {i} u _ {i, k}, \\ & u _ {i, k} \in \mathcal {U} _ {i}, \\ & u _ {i, k} u _ {- i, k} \in \mathcal {C}, \quad k \in \{0, \dots , K - 1 \} \\ & x _ {0} = \mathbf {x} \end{array} \right. \tag {5}
$$

where $K > 1$ is the prediction horizon2. Observe that the open-loop finite horizon game in (5) fits the definition of a continuous action game defined in Section II-A where the control signal $u _ { i , 0 : K - 1 }$ can be cast as a vector action constrained to $( \mathcal { U } _ { i } \cap \mathcal { C } ) ^ { K }$ , inducing the joint action space $\begin{array} { r } { \mathcal { Z } = ( \prod _ { i \in N } \mathcal { U } _ { i } \cap \mathcal { C } ) ^ { K } } \end{array}$ , and the player cost functions $J _ { i } ^ { ( j ) } ( \cdot ; \mathbf { x } )$ are parameterized by the initial condition x. Therefore, from the point of view of the agent $j ,$ , in each timestep, they solve for a generalized Nash equilibrium of the parameterized game $G ^ { ( j ) } \left( \mathbf { x } \right)$ , i.e., finding $u ^ { ( j ) } \in \mathrm { v G N E } \left( G ^ { \left( j \right) } ( \mathbf { x } ) \right)$ of the form $u ^ { ( j ) } = \mathrm { c o l } ( u _ { i , k } ^ { ( j ) } ) _ { i \in N , k \in [ K ] }$ . From Proposition 1, this joint-control signal is the solution to a variational inequality. 2) Select instantaneous control action from vGNE: The mapping from the initial state x to the solutions of the variational inequality (2) or, equivalently, to the v-GNEs of (5) from the point of view of agent $j$ is $\mathcal { S } ( \mathcal { Z } , F ^ { ( j ) } ( \cdot ; \mathbf { x } ) )$ 号 which we define concisely as

$$\mathcal {S} ^ {(j)} (\mathbf {x}) := \left\{u \mid (y - u) ^ {T} F ^ {(j)} (u; \mathbf {x}) \geq 0, \forall y \in \mathcal {Z} \right\}, \tag {6}$$
