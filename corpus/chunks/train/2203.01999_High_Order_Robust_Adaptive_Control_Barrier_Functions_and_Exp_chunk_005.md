A popular tool for developing controllers that render (2) forward invariant for (1) is the concept of a CBF [3], [4], which places Lyapunov-like conditions on the derivative of h to guarantee safety. A limitation of traditional CBFs from [3], however, is that their effectiveness is conditioned upon the assumption that the function h has relative degree one. Yet, many relevant systems and safe sets fail to satisfy such a condition, which has motivated the introduction of exponential and higher order CBFs [26]–[29] to account for safety constraints with high relative degree. Before proceeding, we recall that the Lie derivative of a differentiable function $h : \mathbb { R } ^ { n } $ R along a vector field $f : \mathbb { R } ^ { n } \to \mathbb { R } ^ { n }$ is defined as $\begin{array} { r } { L _ { f } h ( x ) : = \frac { \partial h } { \partial x } f ( x ) } \end{array}$ . This notation allows us to denote higher order Lie derivatives along an additional vector field g as $\begin{array} { r } { L _ { g } L _ { f } ^ { i - 1 } h ( x ) = \frac { \partial ( L _ { f } ^ { i - 1 } h ) } { \partial x } g ( x ) } \end{array}$ (see e.g. [31, Ch. 13.2]).

Definition 1 ([31]). A sufficiently smooth function $h :$ $\mathbb { R } ^ { n } \to \mathbb { R }$ is said to have relative degree $r \in \mathbb N$ with respect to (1) on a set $\mathcal { R } \subset \mathbb { R } ^ { n } \mathrm { i f } 1 )$ for all $1 \leq i \leq r - 1$ , $L _ { g } L _ { f } ^ { i - 1 } h ( x ) \equiv 0 ; 2 ) \ L _ { g } L _ { f } ^ { r - 1 } h ( x ) \neq 0$ for all $x \in \mathcal { R }$ .

To account for high relative degree safety constraints, the authors of [27]–[29] introduce the notion of a HOCBF. Before stating the definition, we recall from [3] that a continuous function $\alpha \ : \ ( - b , a ) \ \to \ ( - \infty , \infty )$ , for some $a , b \in \mathbb { R } _ { > 0 }$ , is said to be an extended class K function if it is strictly increasing and $\alpha ( 0 ) = 0$ .

Definition 2 ([27]–[29]). Consider system (1) and a set $\mathcal { C } \subset$ Rn as in (2). Let $\{ \mathcal { C } _ { i } \} _ { i = 1 } ^ { r }$ 1 be a collection of sets of the form $\mathcal { C } _ { i } : = \{ x \in \mathbb { R } ^ { n } | \psi _ { i - 1 } ( x ) \geq 0 \}$ , where $\psi _ { 0 } ( x ) : = h ( x )$ and

$$\psi_ {i} (x) := \dot {\psi} _ {i - 1} (x) + \alpha_ {i} (\psi_ {i - 1} (x)), i \in \{1, \dots , r - 1 \},\psi_ {r} (x, u) := \dot {\psi} _ {r - 1} (x, u) + \alpha_ {r} (\psi_ {r - 1} (x)), \tag {3}$$
