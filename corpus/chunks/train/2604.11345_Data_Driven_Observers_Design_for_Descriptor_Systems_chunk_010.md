To design a data-driven state observer, we rely on historical data to characterize the system’s behavior. A prerequisite for ensuring that $\mathbb { T } _ { c }$ is fully consistent with $\mathbb { T } _ { \Sigma _ { 1 } }$ is the richness of the collected data. Consider the slow subsystem state $z _ { 1 d } ( k )$ corresponding to $x _ { d } ( k )$ derived from the system decomposition. Letof this sub-state be denoted as $z _ { 1 d } \triangleq \{ z _ { 1 d } ( k ) \} _ { k = 0 } ^ { T - 1 }$ ectory.

Assumption 2 The data $( u _ { d } , x _ { d } , y _ { d } )$ satisfy the condition that the matrix $\left[ \begin{array} { c } { { H _ { 1 } ( z _ { 1 d [ 0 , T - 1 ] } ) } } \\ { { H _ { s + 1 } ( u _ { d [ 0 , T + s - 1 ] } ) } } \end{array} \right]$ is of full row rank.

Remark 3 $A s \ z _ { 1 } ( k )$ is an internal state which is $d i f f$ cult to measure, Assumption 2 is theoretically guaranteed if system (1) is R-controllable and the $\{ u _ { d } ( k ) \} _ { k = 0 } ^ { T + s - 1 }$ is persistently exciting of order $n _ { 1 } + s + 1$ (see $[ 1 8 , \ T \bar { h } . \ 1 ] )$ . Note that $n _ { 1 } + s < n$ holds for singular systems. Compared to the data requirement for the observer design of usual LTI systems [21], which requires $u _ { d }$ to be presistently exciting of order n + 1, descriptor systems generally require less data. Compared to common data assumptions made for standard LTI systems $\ [ { \mathcal { 2 } } 0 { - } { \mathcal { 2 } } { \mathcal { 2 } } ]$ , it is worth noting that Assumption 2 does not guarantee the full row rank of $[ U _ { p } ^ { \top } , X _ { p } ^ { \top } ] ^ { \top }$ .

Lemma 1 (Equivalence of system and compatible trajectories) Under Assumption 2, the set of all trajectories generated by system (1) exactly coincides with the set of trajectories compatible with the given historical data $( u _ { d } , y _ { d } , x _ { d } )$ , i.e.,

$$\mathbb {T} _ {\Sigma_ {1}} = \mathbb {T} _ {c} (u _ {d}, y _ {d}, x _ {d}). \tag {15}$$

Proof: Let $( \{ u ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } , \{ y ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } , \{ x ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } ) \in \mathbb { T } _ { c }$ . By Definition 4, there exists a vector $g _ { 1 } \in \mathbb { R } ^ { T }$ such that

$$\operatorname{col} \left\{x (k), x (k + 1), u (k), y (k), y (k + 1) \right\} \tag {16}= \operatorname{col} \left\{X _ {p}, X _ {f}, U _ {p}, Y _ {p}, Y _ {f} \right\} g _ {1}$$

Since the historical data is generated by system (1), it satisfies the system dynamics:
