$$
\begin{array}{l} \mathcal {L} _ {(X, Y)} U (x, i; \theta) + \pi (x, \theta) \\ = \sum_ {j \neq i} q _ {i j} \left(\int_ {\alpha_ {i} (\theta)} ^ {x} v (y, j; \theta) d y - \int_ {\alpha_ {j} (\theta)} ^ {x} v (y, j; \theta) d y\right) + b (\alpha_ {i} (\theta), i) k _ {1} + \pi (\alpha_ {i} (\theta), \theta) \\ = \sum_ {j \neq i} q _ {i j} \left(\int_ {\alpha_ {j} (\theta)} ^ {\alpha_ {i} (\theta)} v (y, j; \theta) d y\right) + b (\alpha_ {i} (\theta), i) k _ {1} + \pi (\alpha_ {i} (\theta), \theta). \\ \end{array}
$$

Thus, defining

$$\lambda (\theta , i) := \sum_ {j \neq i} q _ {i j} \left(\int_ {\alpha_ {j} (\theta)} ^ {\alpha_ {i} (\theta)} v (y, j; \theta) d y\right) + b \left(\alpha_ {i} (\theta), i\right) k _ {1} + \pi \left(\alpha_ {i} (\theta), \theta\right)+ \sum_ {j \neq i} q _ {i j} \left(\int_ {\alpha_ {i} (\theta)} ^ {\beta_ {i} (\theta)} v (y, i; \theta) d y - \int_ {\alpha_ {j} (\theta)} ^ {\beta_ {j} (\theta)} v (y, j; \theta) d y\right), \tag {3.15}$$

we see that $( U , \lambda )$ solves the variational inequality (3.12).

![](image/a30a6b10d9567cfdf2f4a04ab5080f219f0b87c588717175b9e8da0b1c9d56d7.jpg)

In order to derive the optimal control rule we introduce the following definition. We denote by $\mathcal { D } [ 0 , \infty )$ the space of c\`adl\`ag functions on $[ 0 , \infty )$ , which we equip with the Skorokhod topology.

Definition 3.1 (Skorokhod reflection regime-switching problem). Given $x \in { \mathcal { T } } , i \in \mathbb { Y } , X \in { \mathcal { D } } [ 0 , \infty )$ , and a vector $( \alpha , \beta ) \ = \ ( \alpha _ { j } , \beta _ { j } ) _ { j \in \mathbb { Y } } \ \in \ \mathbb { R } ^ { 2 d }$ with $\alpha _ { j } < \beta _ { j }$ , for any $j \in \mathbb { Y } ,$ , the process $( X ^ { \xi } , \xi ) \in$ ${ \mathcal { D } } [ 0 , \infty ) \times { \mathcal { A } }$ is said to be a solution to the Skorokhod refection problem $\mathbf { S P } ( \alpha , \beta , x , i )$ for the noise (W, Y ) if it satisfies the following properties:

(1) Letting $I : \mathcal { D } [ 0 , \infty )  \mathcal { D } [ 0 , \infty )$ be such that

$$(X) _ {t \geq 0} \mapsto \left(x + \int_ {0} ^ {t} b (X _ {s}, Y _ {s}) d s + \int_ {0} ^ {t} \sigma (X _ {s}, Y _ {s}) d W _ {s}\right) _ {t \geq 0}, \tag {3.16}$$

then

$$X _ {t} ^ {\xi} = I (X ^ {\xi}) _ {t} + \xi_ {t} ^ {+} - \xi_ {t} ^ {-}, \mathbb {P} \otimes d t \text {-a.s.}; \tag {3.17}$$

(2) $X _ { t } ^ { \xi } \in [ \alpha _ { Y _ { t } } , \beta _ { Y _ { t } } ] , \quad \mathbb { P } \otimes d t \mathbf { - } a . s . ;$

t t t(3) R T0 1{Xξt >αYt }dξ+t = R T0 1{Xξt <βYt }dξ−t = 0, for any T > 0, P-a.s.
