$$
\begin{array}{l} x _ {k \mid k} = \mathbb {E} \left\{x _ {k} \mid Z _ {k _ {0}} ^ {k} \right\}, \\ \Sigma_ {k} = \mathbb {E} \left\{\left[ \begin{array}{c c c c c} 1 & 1 & 1 & 1 ^ {\top} + Z _ {k} ^ {k} \end{array} \right] \right. \end{array} \tag {7}
\Sigma_ {k | k} = \mathbb {E} \left\{\left[ x _ {k} - x _ {k | k} \right] \left[ x _ {k} - x _ {k | k} \right] ^ {\top} \mid Z _ {k _ {0}} ^ {k} \right\}.$$

(The notation $( ) _ { k | j }$ , as is typical in state estimation literature [22, Ch. 3], denotes a random variable at time k conditioned on the information $Z _ { k _ { 0 } } ^ { j }$ , for $j \geq k _ { 0 } . )$ （号

Proof. By the smoothing theorem [23, p. 348], conditioning each additive term in (5) on the information available at that

2The left expectation is $\begin{array} { r } { \mathbb { E } \left\{ \cdot \right\} = \int \cdot p ( x _ { k } ) d x _ { k } } \end{array}$ with respect to the density function $p ( x _ { k } )$ , which by the Markov property, $p ( x _ { k } ) = p ( x _ { k _ { 0 } } ) p ( x _ { k _ { 0 } + 1 } \mid$ $x _ { k _ { 0 } } ) \ldots p ( x _ { k } , x _ { k - 1 } )$ , and the initial density $p ( \boldsymbol { x } _ { k _ { 0 } } )$ is characterized by $X _ { k _ { 0 } }$ . The right expectation, however, ondition on measurements and the $\begin{array} { r } { \mathbb { E } \{ \cdot \} \dot { = } \int \cdot p ( Y _ { k _ { 0 } } ^ { k } d Y _ { k _ { 0 } } ^ { k } } \end{array}$ k0 .. That is,as in (8).

corresponding time k (respecting causality), we have

$$\mathbb {E} \left\{\left(q ^ {\top} x _ {k} - r _ {k}\right) ^ {2} \right\} = \int \left(q ^ {\top} x _ {k} - r _ {k}\right) ^ {2} p (x _ {k}) d x _ {k},= \int \int \left(q ^ {\top} x _ {k} - r _ {k}\right) ^ {2} p (x _ {k}, Y _ {k _ {0}} ^ {k}) d Y _ {k _ {0}} ^ {k} d x _ {k},= \int \left(\int \left(q ^ {\top} x _ {k} - r _ {k}\right) ^ {2} p (x _ {k} \mid Y _ {k _ {0}} ^ {k}) d x _ {k}\right) p (Y _ {k _ {0}} ^ {k}) Y _ {k _ {0}} ^ {k},= \mathbb {E} \left\{\mathbb {E} \left\{\left(q ^ {\top} x _ {k} - r _ {k}\right) ^ {2} \mid Z _ {k _ {0}} ^ {k} \right\} \right\}. \tag {8}$$
