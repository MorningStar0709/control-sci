# 8.5 METHODS BASED ON RELAY FEEDBACK

The main drawback of the transient response method is that it is sensitive to disturbances because it relies on open-loop experiments. The relay-based methods avoid this difficulty because the required experiments are performed in closed loop.
