Proof: By the duality between FRNNs and HNNs (Theorem 14), it suffices to establish the LMI equivalence for one architecture; the other follows via the transformation $( P , Q , W ) \mapsto ( P ^ { - 1 } , Q ^ { - 1 } , W ^ { \top } )$ . For continuous-time Hopfield networks, the substitution $\begin{array} { r } {  { \boldsymbol { S } } \ = \  { \boldsymbol { W } } ^ { \top }  { \boldsymbol { P } } } \end{array}$ renders both the CONE and MONE conditions linear in $( P , Q , S )$ . In discrete time, the firing-rate MONE condition is linear in $( P , Q , S )$ under $S = Q W$ . Finally, the discrete-time CONE conditions reduce to Schur diagonal stability (Lemma 13), which admits the LMI reformulation $\left[ \rho _ { S } ^ { 2 } \vec { Q } \vec { S } ^ { \top } \right] \succeq 0$ via the Schur complement with $S = Q W$ .
