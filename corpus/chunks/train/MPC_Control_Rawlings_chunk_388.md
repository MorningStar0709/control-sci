in which $e : = x - z$ is the deviation of the actual state from the nominal state. Let $\phi ( i ; x , { \mathbf v } , { \mathbf e } , { \mathbf w } )$ denote the solution at time i of $x ^ { + } = A x + B \nu +$ $B K e + \boldsymbol { w }$ if its initial state is x at time 0, the control sequence is v, the disturbance sequence is w, and the error sequence is e. The nominal system corresponding to the uncertain system $x ^ { + } = A x + B \nu + B K e + w$ is

$$z ^ {+} = A z + B \nu$$

The deviation e now satisfies the difference equation

$$e ^ {+} = A _ {K} e + w \quad A _ {K} := A + B K$$

which is the same equation used previously except that A, which is possibly unstable, is replaced by $A _ { K }$ , which is stable by design. If K is chosen so that $A _ { K }$ is stable, then the corresponding uncertainty sets $S _ { K } ( i )$ defined by

$$S _ {K} (i) := \sum_ {j = 0} ^ {i - 1} A _ {K} ^ {j} \mathbb {W}$$

can be expected to be smaller than the original uncertainty sets S(i), $i \in \mathbb { I } _ { \geq 0 }$ , considerably smaller if A is unstable and i is large, but not necessarily much smaller if A is strongly stable. Our assumptions on W imply that $S _ { K } ( i )$ , like S(i), contains the origin in its interior for each i. Since $A _ { K }$ is stable, the set $\textstyle S _ { K } ( \infty ) : = \sum _ { j = 0 } ^ { \infty } A _ { K } ^ { j } \mathbb { W }$ exists and is positive invariant for $e ^ { + } \ = \ A _ { K } e \ + \ w _ { \mathrm { i } } ^ { . }$ ; also, $S _ { K } ( i ) ~  ~ S _ { K } ( \infty )$ in the Hausdorff metric as $i ~  ~ \infty$ . Since K is fixed, the feedback policy $u \ = \ K ( x \_$ $z ) + \nu$ is simply parameterized by the open-loop control sequence v. If $x ( 0 ) ~ = ~ z ( 0 ) ~ = ~ x$ , the tube generated by the feedback policy µ is $\mathbf { X } ( x , \mathbf { v } ) = \{ X ( 0 ) , X ( 1 ; x , \mathbf { v } ) , \ldots , X ( N ; x , \mathbf { v } ) \}$ where

$$X (0) = \{x \} \quad X (i; x, \mathbf {v}) := \left\{\phi_ {K} (i; x, \mathbf {v}, \mathbf {w}) \mid \mathbf {w} \in \mathcal {W} \right\} = \{z (i) \} \oplus S _ {K} (i)$$
