In the next experiment, we consider the case where the noise process $\left\{ \mathbf { w } _ { t } \right\}$ is non-i.i.d. Specifically, we let $\begin{array} { r l } { \mathbf { w } _ { t } } & { { } = } \end{array}$ $\scriptstyle \sum _ { t ^ { \prime } = t - 4 } ^ { t } \boldsymbol { \mathfrak { n } } _ { t ^ { \prime } }$ and $\begin{array} { r } { \mathbf { v } _ { t } = \sum _ { t ^ { \prime } = t - 4 } ^ { t } \gamma _ { t ^ { \prime } } } \end{array}$ where $\mathfrak { n } _ { t } \overset { i . i . d . } { \sim } \mathcal { N } ( 0 , 0 . 0 1 )$ and $\begin{array} { r } { \gamma _ { t } \overset { i . i . d . } { \sim } \mathcal { N } ( 0 , 0 . 0 1 ) } \end{array}$ . When applying the Kalman filter in this case, we disregard the fact that $\mathbf { w } _ { t }$ and $\mathbf { v } _ { t }$ each are temporally correlated and simply use the variances of $\mathbf { w } _ { t }$ and $\mathbf { v } _ { t }$ for prediction. We note that for non-i.i.d. noise Kalman filter is no longer optimal. Fig. 2(b) shows the results for this case. We can observe the advantage of MOP over Kalman filter as Kalman filter has lost its optimality whereas MOP has learned the non-i.i.d noise prior during training.

Next, we evaluate the ability of MOP to adapt to run-time changes in the dynamics. Specifically, when generating the test trajectories, we change the underlying dynamics to a randomly generated new one at time $t = T / 2 = 5 0$ . The results are presented in Fig. 2(c). We see that when dynamics changes occur, there are sudden jumps in prediction error for both MOP and the Kalman filter; as we collect more data from the new dynamics, MOP quickly adapts, and achieves the same performance as before at around t = 100. The convergence of MOP after dynamics changes is much slower than the one at the beginning because the prompt always contains data from the original system.

![](image/f6298c53c5d9aa6881115e4db85f50c7ba297cf879c8f31157cf01fec3c2077b.jpg)

<details>
<summary>line</summary>

| Step | Extended Kalman | MOP |
| --- | --- | --- |
| 0 | 10^0 | 10^0 |
| 10 | ~10^0 | ~10^-1 |
| 20 | ~10^0 | ~10^-1 |
| 30 | ~10^0 | ~10^-1 |
| 40 | ~10^0 | ~10^-1 |
| 50 | ~10^0 | ~10^-1 |
</details>

Fig. 3. Output predictions for planar quadrotor systems.
