# 3.1 Problem statement

We formally state the output feedback motion planning problem (OFMP) as follows:

OFMP: Given start $x _ { I } .$ , external parameter $\theta \in D _ { \theta }$ , goal region ${ \mathcal { G } } \subseteq D _ { x } ( D _ { \theta } , D _ { x }$ are defined in the next paragraph), and safe set $\mathcal { X } _ { \mathrm { s a f e } }$ , we want to plan a state-control trajectory $x ^ { * } : [ 0 , T ]  \mathcal { X } , u ^ { * } : [ 0 , T ]  \mathcal { U } , x ^ { * } ( 0 ) = x _ { I }$ , under the nominal dynamics ${ \dot { x } } ( t ) = f ( x ( t ) ) + B u ( t )$ such that in execution on the true system (1a), $x ( t ) \in \mathcal { X } _ { \mathrm { s a f e } }$ for all $t \in [ 0 , T ]$ ] and $x ( T ) \in \mathcal { G }$ . At runtime, we do not observe $x ( t )$ ; we are only given observations $y ( t )$ generated by (1b), and must track $x ^ { * }$ using a (dynamic) output feedback controller that we must also design. We assume $f , B , B _ { w } ,$ and $B _ { y }$ are known; h is unknown; $w _ { x } , w _ { y }$ are not measurable but $\bar { w } _ { x }$ and $\bar { w } _ { y }$ are known. If $n _ { r } \leq n _ { x }$ of the states can be inferred directly from y, we denote these indices as the reduced observation $y _ { r } = C _ { r } x \in \mathbb { R } ^ { n _ { \tau } }$ , where $C _ { r } \in \{ 0 , 1 \} ^ { n _ { r } \times n _ { a } }$ is a boolean matrix that selects the observable dimensions of x. We assume that we are given $C _ { r } . \operatorname { L e t } x ( t )$ be the executed trajectory of (1a), and let ${ \hat { x } } ( t )$ be the trajectory of the state estimate. We are given upper bounds $\bar { d } _ { c } ( 0 ) , \bar { d } _ { e } ( 0 ) \mathrm { o n }$ the Riemannian distance between the true and estimated initial state $d _ { e } ( x ( 0 ) , \hat { x } ( 0 ) )$ ) and between the true/planned initial state $d _ { c } ( x ^ { * } ( 0 ) , x ( 0 ) ) ; d _ { e } ( \cdot , \cdot )$ and $d _ { e } ( \cdot , \cdot )$ are defined with respect to (w.r.t.) metrics $M _ { c }$ and $M _ { e } ,$ defined in Sec. 3.2.
