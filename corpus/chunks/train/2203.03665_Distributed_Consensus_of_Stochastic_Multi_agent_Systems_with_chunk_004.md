# A. Notations

Let the triplet (Ω, F , P) denote a probability space with a sample space Ω, a filtration F, and the probability measure

P. The filtration $\mathbb { F } = ( \mathcal { F } _ { s } ) _ { s \geq 0 }$ satisfies the usual conditions of right continuity and completeness [19]. Let $( W _ { s } ) _ { s \geq 0 }$ be a F-Brownian motion. We use E[·] to denote the expectation operator. The symbols N, $\mathbb { N } _ { 0 } , \ \mathbb { R } , \ \mathbb { R } ^ { + }$ , and $\mathbb { R } _ { 0 } ^ { + }$ denote the set of natural, nonnegative integer, real, positive, and nonnegative real numbers, respectively. We use $\mathbb { R } ^ { \times m }$ to denote a vector space of real matrices with n rows and m columns. We use k · k to represent Euclidean norm. For $a \in \mathbb { R }$ , we denote absolute value of a by |a|. For $a , b \in \mathbb { R }$ and $a < b ,$ , we use $( a , b )$ to represent an open interval in R. For $a , b \in \mathbb { N }$ and $a \ \leq \ b ,$ , we use $[ a ; b ]$ to denote a close interval in N. We use $I _ { n }$ and $0 _ { n }$ to denote identity matrix and zero matrix in $\mathbb { R } ^ { n \times n }$ , respectively. A diagonal matrix in $\mathbb { R } ^ { n \times n }$ with diagonal entries $d _ { 1 } , \ldots , d _ { n }$ is denoted by $d i a g \{ d _ { 1 } , \ldots , d _ { n } \}$ . Given a matrix $M \in \mathbb { R } ^ { n \times m }$ , $M ^ { T }$ represents transpose of matrix M . Given a matrix P ∈ Rn×n, $P \in \mathbb { R } ^ { n \times n }$ $\operatorname { T r } ( P )$ represents the trace of matrix P , and $P > 0$ and $P \geq 0$ denote positive definite and semi-definite matrices, respectively. Given a set A, we use |A| to represent the cardinality of the set A. We use notations K and $\kappa _ { \infty }$ to denote different classes of comparison functions, as follows: $K = \{ \psi : \mathbb { R } _ { 0 } ^ { + }  \mathbb { R } _ { 0 } ^ { + } |$ | ψ is continuous, strictly increasing, and $\psi ( 0 ) = 0 \} ; \mathcal { K } _ { \infty } = \{ \psi \in \mathcal { K } | \operatorname* { l i m } _ { r \to \infty } \psi ( r ) = \infty \}$ r→∞ .
