The p-UBIBS property is a more demanding property than simple forward completeness because the p-UBIBS property requires boundedness of solutions for all bounded inputs. Therefore, there are systems which are forward complete but do not satisfy the p-UBIBS property (e.g., the system ${ \dot { x } } = d$ with $x \in \mathbb { R } , d \in \mathbb { R } )$ . The following result provides sufficient conditions for the p-UBIBS property.

Theorem 5 (Sufficient Lyapunov-like conditions for the p-UBIBS property): Let $H _ { i } \in C ^ { 1 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ , $i = 1 , . . . , m$ be a family of functions for which there exists a non-decreasing function $R \in C ^ { 0 } \left( \mathbb { R } _ { + } ; \mathbb { R } _ { + } \right)$ such that the following implication holds for all $( \boldsymbol { x } , \boldsymbol { d } ) \in \mathbb { R } ^ { n } \times \boldsymbol { D }$ and $i = 1 , . . . , m$ :

$$H _ {i} (x) \geq R (\left| d \right|) \Rightarrow \dot {H} _ {i} (x, d) = \nabla H _ {i} (x) f (x, d) \leq 0 \tag {53}$$

Furthermore, suppose that the function $H ( x ) = \operatorname* { m a x } _ { i = 1 , \ldots , m } \left( H _ { i } ( x ) \right)$ is a radially unbounded function. Then system (36) satisfies the p-UBIBS property. Moreover, if in addition the function $H ( x ) = \operatorname* { m a x } _ { i = 1 , \ldots , m } \left( H _ { i } ( x ) \right)$ is positive definite and $R ( 0 ) = 0$ then system (36) satisfies the UBIBS 1,...,i m= property.

Proof: Let arbitrary $x _ { 0 } \in \mathbb { R } ^ { n }$ and $d \in L ^ { \infty } \left( \mathbb { R } _ { + } ; D \right)$ be given. Since  is locally Lipschitz with respect to $x \in \mathbb { R } ^ { n }$ , the initial-value problem ${ \dot { x } } = f ( x , d )$ with initial condition $x ( 0 ) = x _ { 0 }$ has a unique solution $\ b { x } ( t ) \in \mathbb { R } ^ { n }$ defined for $t \in [ 0 , t _ { \operatorname* { m a x } } )$ , where $t _ { \mathrm { m a x } } \in \left( 0 , + \infty \right]$ . Furthermore, if $t _ { \mathrm { m a x } } < + \infty$ then $\operatorname* { l i m } _ { t \to t _ { \operatorname* { m a x } } } \bigl ( \bigl | x ( t ) \bigr | \bigr ) = + \infty$ maxt t→ − .

We claim that the following estimate holds for all $t \in [ 0 , t _ { \operatorname* { m a x } } )$ and $i = 1 , . . . , m$ :

$$H _ {i} (x (t)) \leq \max \left(H _ {i} (x _ {0}), R \left(\left\| d \right\| _ {\infty}\right)\right) \tag {54}$$
