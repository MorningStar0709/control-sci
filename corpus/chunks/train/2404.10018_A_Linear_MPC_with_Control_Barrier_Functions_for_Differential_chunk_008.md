# A. Preliminaries

In this paper, R denotes the set of real numbers, ?? describes the degree of freedom, and L defines the Lie derivative operator. For a given vector field $f ( x )$ such that $f : \mathbb { R } ^ { n } \to \mathbb { R } ^ { n }$ and a scalar function $\lambda : \mathbb { R } ^ { n }  \mathbb { R } .$ , the Lie derivative of ?? with respect to $f$ can be written as $\begin{array} { r } { { \mathcal { L } } _ { f } \lambda = { \frac { \partial \lambda } { \partial x } } \cdot f ( x ) } \end{array}$ . Consider the following single input single output nonlinear affine in control system:

$$
\left\{ \begin{array}{l l} \dot {x} & = f (x) + g (x) u \\ y & = h (x) \end{array} \right. \tag {1}
$$

where $\boldsymbol { x } ~ \in ~ \mathbb { R } ^ { n }$ describes the system states, $u ~ \in ~ \mathbb { R }$ defines the system control input, $y \in \mathbb { R }$ denotes the system output, $f ~ : ~ \mathbb { R } ^ { n } \to \mathbb { R } ^ { n } , ~ g ~ : ~ \mathbb { R } ^ { n } \to \mathbb { R } ^ { n }$ , and $h : \mathbb { R } ^ { n } \to \mathbb { R }$ . The relative degree ?? of such system can be defined at point ??0 if $\mathcal { L } _ { g } \mathcal { L } _ { f } ^ { \rho } h ( x ) = 0$ for all ?? in the neighborhood of $x _ { 0 } , \rho < r - 1$ , and $\begin{array} { r } { \dot { \mathcal { L } } _ { g } \mathcal { L } _ { f } ^ { r - 1 } h ( x _ { 0 } ) \neq 0 } \end{array}$ . The Multi Input Multi Output (MIMO) square affine in control system is expressed as follows:

$$
\left\{ \begin{array}{l l} \dot {x} & = f (x) + g _ {1} (x) u _ {1} + \dots + g _ {m} (x) u _ {m} \\ y _ {1} & = h _ {1} (x) \\ \vdots & = \vdots \\ y _ {m} & = h _ {m} (x) \end{array} \right. \tag {2}
$$

Lemma 1. [32] The relative degree of (2) at $x _ { 0 }$ is describes as $r = [ r _ { 1 } , \ldots , r _ { m } ] ^ { \top } \in \mathbb { R } ^ { m }$ such that ?? exists if the following holds:

• $\mathcal { L } _ { g _ { j } } \mathcal { L } _ { f } ^ { \rho } h _ { i } ( x ) = 0$ at the neighborhood of $x _ { 0 }$ for all $1 \ \leq$ $j \leq m , \rho < r _ { i } - 1$ , and $1 \leq i \leq m$ .

• The decoupling system input matrix $A ( x ) \in \mathbb { R } ^ { m \times m }$ defined as

$$
A (x) = \left( \begin{array}{c c c} \mathcal {L} _ {g _ {1}} \mathcal {L} _ {f} ^ {r _ {1} - 1} h _ {1} (x) & \dots & \mathcal {L} _ {g _ {m}} \mathcal {L} _ {f} ^ {r _ {1} - 1} h _ {1} (x) \\ \mathcal {L} _ {g _ {1}} \mathcal {L} _ {f} ^ {r _ {2} - 1} h _ {2} (x) & \dots & \mathcal {L} _ {g _ {m}} \mathcal {L} _ {f} ^ {r _ {1} - 1} h _ {2} (x) \\ \vdots & \dots & \vdots \\ \mathcal {L} _ {g _ {1}} \mathcal {L} _ {f} ^ {r _ {m} - 1} h _ {m} (x) & \dots & \mathcal {L} _ {g _ {m}} \mathcal {L} _ {f} ^ {r _ {m} - 1} h _ {m} (x) \end{array} \right) \tag {3}
$$
