# 5. Stability properties of the adaptive horizon multi-stage MPC

Applying the optimal control input computed by the MPC controller to system (1) results in a closed-loop uncertain system whose dynamics are denoted by $x _ { k + 1 } = F ( x _ { k } , d _ { k } )$ . We will perform a stability analysis for this closed-loop ,uncertain system. Input-to-state practical stability (ISpS) is the suitable framework to analyze the stability properties of such a system. We start with recollecting some definitions in $\mathrm { I S p S }$ stability analysis of discrete-time nonlinear systems:

Definition 7. (Comparison functions [20]) A function : $\mathbb { R } _ { + } \mapsto \mathbb { R } _ { + }$ + is said to be of class K if $\alpha ( 0 ) = 0 , \alpha ( n ) > 0$ for all $n > 0$ and is strictly increasing. It becomes of class $\mathcal { K } _ { \infty }$ α αif in addition,  is unbounded. A function $\beta \colon \mathbb { R } _ { + } \times \mathbb { Z } _ { + } \mapsto \mathbb { R } _ { + }$ + is >of class $\mathcal { K L } \operatorname { i f } \beta ( \cdot , k )$ is of class K for each fixed $k \geq 0$ , and $\beta ( n , \cdot )$ α is decreasing for each fixed $n \geq 0$ and $\beta ( n , k )  0$ as $k \to \infty$ .

Definition 8. (Regional input-to-state practical stability (ISpS), [21]) An autonomous system $x ^ { + } = F ( x , d )$ is ISpS in $\Gamma \subseteq \mathbb { R } ^ { n }$ and $0 \in \Gamma$ if there exists a KL function $\beta ,$ , a K function , and a constant $c \geq 0$ such that for all $k \geq 0$

$$\left| x _ {k} \right| \leq \beta \left(\left| x _ {0} \right|, k\right) + \gamma \left(\left\| \mathbf {d} _ {[ 0, k - 1 ]} - \mathbf {d} ^ {0} \right\|\right) + c, \quad \forall x _ {0} \in \Gamma \tag {16}$$

where ${ \bf d } _ { [ 0 , k - 1 ] } : = \left[ d _ { 0 } , d _ { 1 } , \ldots , d _ { k - 1 } \right]$ is the sequence of true parameter realizations and ${ \bf d } ^ { 0 }$ is a sequence of nominal , , , . .parameters of the same length.

![](image/32027b1b8e8b573f4898872a38361db9da442343e4bfbec8d63106461c1cef04.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    A["N₀ = N_max\nk = 0"] --> B["Solve\nV_Nk^ahm(x_k, d^c)"]
    B --> C["Solve\nsensitivity update for\nz_i^{c}|_{k+1}"]
    C --> D{Are all\nz_Nk^{c}|_{k+1} ∈ X_f\n∀ c ∈ C ?}
    D -->|No| E["N_{k+1} = N_max,\nk = k + 1"]
    E --> F["N_{k+1} = min(N_max, N_T + N_min)\nk = k + 1"]
    F --> G["Determine NT\nNT = max(NT^c)"]
    G --> D
```
</details>
