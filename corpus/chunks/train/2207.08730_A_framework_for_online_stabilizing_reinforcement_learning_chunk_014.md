$$\mathbb {J} _ {a} (\theta , w) + \beta_ {1} \| w - w _ {k} \| ^ {2} + \beta_ {2} \| \theta - \theta_ {k - 1} \| ^ {2}\mathbb {J} _ {c} (w) + \beta_ {3} \left\| w - w _ {k} \right\| ^ {2}.$$

These regularization terms may be employed to regulate weight change thus imitating a learning rate of a gradient-like update rules.

Remark 8: One can formulate the loss, e. g., in terms of an experience replay of size M:

$$w _ {k + 1} \leftarrow \underset {w \in \{w ^ {\prime} \in \mathbb {W} \mid G _ {\varepsilon} ^ {p} (w ^ {\prime}, w _ {k}, x _ {k + 1}, x _ {k}) \leq 0 \}} {\arg \min} \sum_ {j = 0} ^ {M - 1} e _ {j} (w); \tag {10}$$

where $e _ { j } ( w ) = \left( \hat { J } ^ { w } ( x _ { k - j } ) - r ( x _ { k - j } , U _ { k \delta } ) - \hat { J } ^ { w _ { k } } ( x _ { k + 1 - j } ) \right) ^ { 2 }$ and the buffer count is backwards relative to k.

Algorithm 2: CALF as stabilizing actor-critic   
Data: $\varepsilon > 0$ , $\bar{\nu} > 0$ , $\zeta > 0$ Result: $U_0, U_\delta, U_{2\delta}, \ldots$ $w_0 \leftarrow w_\zeta^\#$ ;

for $k = 0 \ldots \infty$ do $x_k \leftarrow \textbf{observe } X_{k\delta}$ $\binom{\theta_k}{w_{-1}} \leftarrow \underset{\text{s.t. } G_{\varepsilon}^{\bar{\nu}}(w, w_k, \Lambda_{(\rho^\theta(x_k))}(x_k), x_k) \leq 0}{\arg \min} \mathbb{J}_a(\theta, w)$ where $\mathbb{J}_a(\theta, w) = r(x_k, \rho^\theta(x_k)) + \mathbb{E}\left[\hat{J}^{w_k}(X_{(k+1)\delta}) | X_{k\delta} = x_k, U_{k\delta} = \rho^\theta(x_k)\right]$ $w_{k+1} \leftarrow \underset{\text{s.t. } G_{\varepsilon}^{\bar{\nu}}(w, w_k, \Lambda_{(\rho^\theta(k)(x_k))}(x_k), x_k) \leq 0}{\arg \min} \mathbb{J}_c(w)$ where $\mathbb{J}_c(w) = \left(\hat{J}^w(x_k) - r(x_k, U_{k\delta}) - \mathbb{E}\left[\hat{J}^{w_k}(X_{\delta(k+1)}) | X_{k\delta} = x_k, U_{k\delta} = \rho_{\theta_k}(x_k)\right]\right)^2$ $U_{k\delta} \leftarrow \rho_{\theta_k}(x_k)$ perform $U_{k\delta}$ end

Remark 9: The above described loss functions are formulated inspired by the Bellman optimality principle for undiscounted problems. Introducing ${ \textbf { a } } \gamma \in ( 0 , 1 )$ in front of the predicted Jˆ would make it to address a discounted problem instead.

Remark 10: Notice that Algorithms 1 and 2 explicitly involve $L ,$ if it is available. Another variant of CALF presented in Algorithm 3 lifts this requirement, although both require knowledge of a stabilizing policy.
