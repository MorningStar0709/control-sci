# II. PRELIMINARIES

$\mathbb { R } : = ( - \infty , + \infty ) ; \mathbb { R } _ { \geq 0 } : = [ 0 , + \infty ) ; \mathbb { R } _ { > 0 } : = ( 0 , + \infty ) ;$ $\mathbb { N } : = \{ 0 , 1 , 2 , . . . \} ; \mathbb { N } _ { + } : = \{ 1 , 2 , . . . \}$ . Given two sets  and $B , B \backslash A : = \{ x : x \in B , x \notin A \} . \ | \cdot \ |$ denotes the Euclidean norm. Given two vectors $x , y \in \mathbb { R } ^ { n } , ( x , y ) : = ( x ^ { \top } , y ^ { \top } ) ^ { \top }$ for simplicity of notation, and $\langle x , y \rangle$ denotes the usual inner product. E denotes the vector with all components being 1, I denotes the identity matrix of appropriate dimension, and diag $\{ A , B \}$ denotes the block diagonal matrix made of the matrices A and B. Given a function $f : \mathbb { R } _ { \geq t _ { 0 } } \to \mathbb { R } ^ { n } , f ( t ^ { + } ) : =$ lim $\mathrm { s u p } _ { s \to 0 ^ { + } } f ( t + s )$ . A function $\alpha : \mathbb { R } _ { \geq 0 }  \mathbb { R } _ { \geq 0 }$ is of class K if it is continuous, $\alpha ( 0 ) = 0$ , and strictly increasing; it is of class $\kappa _ { \infty }$ if it is of class and unbounded; it is of class if it is continuous, strictly decreasing, and $\begin{array} { r } { \operatorname* { l i m } _ { s \to \infty } \alpha ( s ) = 0 } \end{array}$ . A function $\beta : \mathbb { R } _ { > 0 } ^ { 2 }  \mathbb { R } _ { \ge 0 }$ is of class KL if $\beta ( s , t ) \in \mathcal { K }$ for fixed $t \geq 0$ and $\beta ( s , t ) \in \mathcal { L }$ for fixed $s \geq 0 , \mathrm { ~ A ~ }$ function $\beta : \mathbb { R } _ { > 0 } ^ { 3 }  \mathbb { R } _ { \ge 0 }$ is of class $\kappa \mathcal { L L }$ if $\beta ( r , s , t ) \in \mathcal { K L }$ for fixed $s \geq 0$ and $\beta ( \overline { { r } } , s , t ) \in \mathcal { K L }$ for fixed $t \geq 0 .$ .
