# A. Actor-Critic Reinforcement Learning with Physics Informed Critic

In this manuscript, we elevate the concept of the baseline/critic/benchmark by using a physically derived estimate for the state-dependent value function, rather than relying on a NN as in standard physics-agnostic actorcritic RL approaches like [18]. In the following sections, we demonstrate that under certain simplifying assumptions about system dynamics and control, an explicit analytical expression for the baseline, as a function of the state, can be derived for a pair of particles placed in a large-scale turbulent flow.

Table I presents the mapping of general RL notations to the specific problem of controlling the relative separation between two particles. In this context, the agent is represented by the active particle, whose state is the separation distance from the passive particle. The action corresponds to the swimming efforts of the active particle, while the transition probability models the stochastic evolution influenced by relative velocity and Brownian forces.

<table><tr><td>General RL</td><td>PIRL for Particles</td></tr><tr><td>Agent</td><td>Active particle</td></tr><tr><td>State, s</td><td>Separation between active and passive particles, s</td></tr><tr><td>Action, a ~ π(·|s)</td><td>Swimming efforts of the active swim-mer</td></tr><tr><td>Transition proba-ility p(s&#x27; | s, a)</td><td>Stochastic evolution according to rel-ative turbulent velocity between swim-mers and Brownian force, as discussed in Section III A.</td></tr><tr><td>Baseline, b(s)</td><td>State value function, Vφ(s), estimated following assumptions of the BK theory, as discussed in Section III D.</td></tr></table>

TABLE I. Mapping of general RL notations to the specific problem of controlling the relative separation of two particles.

We also present below a joint pseudo-algorithm for the Actor-Critic (AC) and Actor Physicist (AP) where the difference between the two is highlighted:

Require: Number of episodes N. Each episode is split into T intervals. Learning rates $\alpha , \beta > 0$ . Discount factor $\gamma \in [ 0 , 1 ]$ .

initialize NN for the policy function $\pi _ { \theta }$ (θ is the NN’s vector of parameters)

AC: initialize NN for the value function $V _ { w }$

AP: select ϕ for the value function $V _ { \phi }$

Initialize $s _ { 0 }$

for episode = 1 to N do

for t = 1 to T do

$$a _ {t} \sim \pi_ {\theta} (\cdot | s _ {t})$$

Take action $a _ { t } ,$ observe $s _ { t + 1 } , r _ { t }$

$$\mathrm{AC}: A _ {t} \leftarrow r _ {t} + \gamma V _ {w} (s _ {t + 1}) - V _ {w} (s _ {t})\mathrm{AP} \colon A _ {t} \leftarrow r _ {t} + \gamma V _ {\phi} (s _ {t + 1}) - V _ {\phi} (s _ {t})\theta \leftarrow \theta + \frac {\alpha}{t} \sum_ {i = 0} ^ {t} \nabla_ {\theta} \log (\pi_ {\theta} (a _ {t} | s _ {t})) A _ {t}$$

end for

end for
