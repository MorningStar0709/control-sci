# B. Classical Sliding Mode Control

The purpose of this subsection is to design a state feedback controller that can stabilize the origin of the system described by (1) for all uncertainties in δ. The control law is designed based on a standard SMC strategy as outlined in [1].

Definition 1: For the vector fields ${ \pmb b } _ { 1 } ( { \pmb x } ) , \dots , { \pmb b } _ { p } ( { \pmb x } )$ on D, the collection of all vector spaces $\begin{array} { r l } { \pmb { \Pi } ( \pmb { x } ) } & { { } = } \end{array}$ span $\{ b _ { 1 } ( { \pmb x } ) , \ldots , b _ { p } ( { \pmb x } ) \}$ is called a distribution and shown with ⇧ [1].

Definition 2: The distribution ⇧ is involutive on  if for $\pmb { p } _ { 1 } ( \pmb { x } ) \in \pmb { \Pi }$ and $\pmb { p } _ { 2 } ( \pmb { x } ) \in \pmb { \Pi }$ , the Lie bracket $[ { \pmb p } _ { 1 } ( { \pmb x } ) , { \pmb p } _ { 2 } ( { \pmb x } ) ] =$ $\begin{array} { r } { \frac { \partial \bar { \bf p } _ { 2 } ( { \pmb x } ) } { \partial \pmb x } { \pmb p } _ { 1 } ( { \pmb x } ) - \frac { \partial \bar { \bf p } _ { 1 } ( { \pmb x } ) } { \partial \pmb x } { \pmb p } _ { 2 } ( { \pmb x } ) } \end{array}$ belongs to ⇧ [1].

Assuming that the distribution ⇧ associated with the columns of $B ( { \pmb x } )$ is involutive on and since $B ( { \pmb x } )$ has rank p, we can conclude the existence of a diffeomorphism $\pmb { T } : \mathcal { D }  \mathbb { R } ^ { n }$ such that [1]

$$
\frac {\partial \boldsymbol {T} (\boldsymbol {x})}{\partial \boldsymbol {x}} \boldsymbol {B} (\boldsymbol {x}) = \left[ \begin{array}{c} \boldsymbol {0} \\ \boldsymbol {I} \end{array} \right].
$$

This transformation makes it possible to find the variables $\eta \in \mathbb { R } ^ { n - p }$ and $\zeta ~ \in ~  { \mathbb { R } } ^ { p }$ such that $\begin{array} { r l r } { { \pmb T } ( { \pmb x } ) } & { = } & { [ { \pmb \eta } ^ { \mathrm { T } } , \zeta ^ { \mathrm { T } } ] ^ { \mathrm { T } } } \end{array}$ transforms (1) into the following regular form

$$\dot {\boldsymbol {\eta}} = \boldsymbol {f} _ {a} (\boldsymbol {\eta}, \boldsymbol {\zeta}), \tag {2}\dot {\boldsymbol {\zeta}} = \boldsymbol {f} _ {b} (\boldsymbol {\eta}, \boldsymbol {\zeta}) + \boldsymbol {G} (\boldsymbol {x}) \boldsymbol {E} (\boldsymbol {x}) \boldsymbol {u} + \boldsymbol {\delta} (t, \boldsymbol {x}), \tag {3}$$
