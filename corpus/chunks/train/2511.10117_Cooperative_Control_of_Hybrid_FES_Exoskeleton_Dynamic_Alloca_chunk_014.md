$$\boldsymbol {\tau} = \bar {\boldsymbol {\tau}} + \boldsymbol {g} _ {\perp} ^ {*} S \boldsymbol {\zeta}, \tag {11a}\dot {\boldsymbol {\zeta}} = \phi (\boldsymbol {\zeta}, \bar {\boldsymbol {\tau}}, \boldsymbol {\eta}), \tag {11b}$$

where the dynamics in (11b) are adopted from the allocation framework in [31], [42]. The torque input vector τ is composed of a nominal input τ¯ and a null-space contribution given by $g _ { \perp } ^ { * } S \zeta$ , where $\pmb { g } _ { \perp } ^ { * }$ denotes the basis of the null space of the augmented control matrix of the hybrid FES-exoskeleton

$$
\boldsymbol {g} _ {\perp} ^ {*} = \left[ \begin{array}{c c} 1 & 1 \\ 0 & - 1 \\ - 1 & 0 \end{array} \right], \tag {12}
$$

where $l = \operatorname { r a n k } ( \pmb { g } _ { \parallel } ^ { * } ) = 2 . \ S \in \mathbb { R } ^ { 2 }$ is an arbitrary matrix and the vector $\zeta = [ \zeta _ { 1 } , \zeta _ { 2 } ] ^ { \top } \in \mathbb { R } ^ { 2 }$ represents the dynamic input allocator. The parameter set η includes all relevant information affecting the allocator dynamics, such as user preferences (e.g., prioritizing one actuator over another), alongside nominal cooperative gain $\bar { \alpha } ,$ basis of the null space of augmented control matrix $\pmb { g } _ { \perp } ^ { * }$ , and FES-induced torque distributor vector σ.

Assumption 1. The allocation dynamics in (11b) is input-tostate stable with respect to $\bar { \tau } ( t )$ , i.e., for any bounded nominal input $\bar { \tau } ( t )$ , the allocator state ζ(t) remains bounded for all $t \geq 0$ .

Mathematically, $\zeta ( t )$ in (11b) remains bounded for bounded inputs $\bar { \tau } ( t )$ if there exist functions $\mu \in \mathcal { K } _ { \infty }$ and $\delta \in \mathcal { K L }$ such that, for every initial state ζ(0) and every input $\bar { \tau } ( t )$ , the corresponding solution of (11b) satisfies the inequality

$$\left\| \boldsymbol {\zeta} (t) \right\| \leq \delta (\left\| \boldsymbol {\zeta} (0) \right\|, t) + \mu \left(\left\| \bar {\boldsymbol {\tau}} \right\| _ {[ 0, t ]}\right), \forall t \geq 0 \tag {13}$$

where ∥.∥ denotes Euclidean norm, and $\begin{array} { r l } { \| \bar { \boldsymbol { \tau } } \| _ { [ 0 , t ] } } & { { } : = } \end{array}$ ess sup $[ \| { \bar { \tau } } ( p ) \| : p \in [ 0 , t ] \}$ } (supremum norm on [0, t] except for a set of measure zero) [43] [44, Appendix A.6].
