where $a \in \mathbb { R } , b \neq 0 , r > 0 , x _ { i } ( t ) \ ( \mathrm { r e s p . ~ } u _ { i } ( t ) )$ is the state (resp. the control action) of agent $i , u _ { i }$ denotes the strategy of agent $i , u _ { - i }$ denotes the strategies of all other agents except agent i, $w _ { i } ( t )$ is the standard Brownian motion, the initial $x _ { i } ( 0 ) \sim \mathcal { N } ( \mu _ { 0 } , V _ { 0 } )$ is normally distributed with variance $V _ { 0 } \geq 0$ , and the average of the population state is denoted by $\begin{array} { r } { \overline { { x } } ^ { n } ( t ) : = \frac { 1 } { n } \sum _ { i = 1 } ^ { n } x _ { i } ( t ) } \end{array}$ . Let the empirical state distribution for all agents except agent i be denoted by $\mathcal { D } _ { t } ^ { n , - i } ( z ) : =$ $\begin{array} { r } { \frac { 1 } { n - 1 } \sum _ { j = 1 , j \neq i } ^ { n } \mathbf { 1 } _ { ( x _ { i } ( t ) \leq z ) } } \end{array}$ with $\mathbf { 1 } _ { ( \cdot ) }$ as the indicator function. The penalty coefficient $q _ { t } ^ { \alpha } ( i , n )$ in the running cost is given by

$$q _ {t} ^ {\alpha} (i, n) := q \left(1 + e ^ {\mathcal {Q} _ {\mathcal {D} _ {t} ^ {n, - i}} (\alpha)}\right), \quad \text { with } \alpha \in (0, 1), q \geq 0, \tag {4}$$

where the α-quantile of the empirical distribution following the definition in (1) is given by

$$\mathcal {Q} _ {\mathcal {D} _ {t} ^ {n, - i}} (\alpha) = \inf \{z \in \mathbb {R}: \mathcal {D} _ {t} ^ {n, - i} (z) \geq \alpha \}.$$

For a higher quantile (with a larger quantile parameter α), the penalty for deviating from the population average ${ \overline { { x } } } ^ { n }$ is larger and hence agents are encouraged to follow more closely the population average xn.

To identify approximate decentralized solutions for the problem above when the population size is large, we apply the mean field game approach [12] by taking the population limit and obtain approximate solutions via solving the limiting game problem. In the mean field limit, we obtain the corresponding LQG mean field tracking problem with the dynamics

$$d x _ {i} (t) = \left(a x _ {i} (t) + b u _ {i} (t)\right) d t + \sigma d w _ {i} (t), \quad x _ {i} (t), u _ {i} (t), w _ {i} (t) \in \mathbb {R}, \quad t \in [ 0, T ], \tag {5}$$

and the cost

$$J _ {i} (u _ {i}, \overline {{x}}) = \mathbb {E} \int_ {0} ^ {T} \left(\| x _ {i} (t) - \overline {{x}} (t) \| _ {q _ {t} ^ {\alpha}} ^ {2} + \| u _ {i} (t) \| _ {r} ^ {2}\right) d t \tag {6}$$

where the mean state (if exists) is given by

$$\overline {{x}} (t) = \lim _ {n \to \infty} \frac {1}{n} \sum_ {i = 1} ^ {n} x _ {i} (t)$$

and the penalty coefficient in the running cost is given by
