# C. Hierarchical Control Architecture

The high-level and low-level control policies (12) and (17) are assembled into a hierarchical control architecture as outlined in Alg. 1. At the start of each iteration $r \geq 1$ , the

Algorithm 1 Iterative Hierarchical Control   
Require: $\pi^{H,1}$ , $S^{H,1}$ , $\hat{\theta}^{1}$ , $\hat{\Theta}^{1}$ , $\pi^{L,1}$ , $S^{L,1}$ , $x_{0}^{1}=x_{S}$ $r\leftarrow1$ while True do $k\leftarrow0$ , $t\leftarrow0$ $x_{t}^{r}=x_{S}$ , $x_{k}^{H,r}=g(x_{S})$ for $k=0,1,\ldots$ do $u_{k}^{r}=\pi^{H,r}(x_{k}^{H,r},\hat{\theta}^{r},\hat{\Theta}^{r})$ $\triangleright(31)$ $n^{r*}=\sum_{j\in\mathcal{V}}u_{k,n_{k}^{r},j}^{r}j$ $i\leftarrow n_{k}^{r}, j\leftarrow n^{r*}$ while $x_{t}^{r}\notin N_{n^{r*}}$ do $u_{t}^{r}=\pi^{L,r}(x_{t}^{r},i,j)$ $\triangleright(33)$ $x_{t+1}^{r}=f(x_{t}^{r},u_{t}^{r})$ $t\leftarrow t+1$ end while $x_{k+1}^{H,r}=g(x_{t}^{r})$ if $n_{k+1}^{H,r}=D$ then $r\leftarrow r+1$ Construct $\hat{\theta}^{r},\hat{\Theta}^{r}$ $\triangleright(21),(22)$ Construct $S^{H,r},S^{L,r}$ $\triangleright(23),(25)$ Construct $\pi^{H,r},\pi^{L,r}$ $\triangleright(31),(33)$ Break. Iteration r complete.

end if

end for

end while

agent is located at node $v _ { 1 } = D$ (the depot in our example), with the same initial state $x _ { 0 } ^ { r } = x _ { S } \in \mathcal { N } _ { D }$ . The high-level controller (12) assigns the next node for the agent to travel to, and the low-level controller (17) selects the corresponding low-level actuation inputs. Once the agent has reached the assigned node, the high-level controller assigns a new node. This process repeats until the agent has returned to the depot, at which point the policies $\pi ^ { \bar { H } }$ and $\pi ^ { L }$ and their associated parameters $\mathcal { S } ^ { H } , { S ^ { L } } , \hat { \theta } .$ , and $\hat { \Theta }$ are updated using closed-loop trajectory data from iteration r.

After iteration r, we write the agent’s resulting closed-loop trajectory produced by Alg. 1 as
