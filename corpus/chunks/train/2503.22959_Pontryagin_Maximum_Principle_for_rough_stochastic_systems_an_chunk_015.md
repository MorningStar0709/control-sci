# 3. Well-posedness of rough SDEs with affine drivers

In this section, we establish the well-posedness of rough SDEs with affine rough drivers, as well as some key properties that we need to introduce rough and pathwise linear-quadratic control problems. Specifically, we consider the $\mathbb { R } ^ { d _ { X } }$ -valued rough SDE

$$d X _ {t} = b _ {t} (X _ {t}, \boldsymbol {\eta}) d t + \sigma_ {t} (X _ {t}, \boldsymbol {\eta}) d W _ {t} + (F _ {t} (\boldsymbol {\eta}) X _ {t} + f _ {t} (\boldsymbol {\eta})) d \boldsymbol {\eta} _ {t}, \quad X _ {0} = \xi \tag {3.1}$$

where W is a $d _ { W }$ -dimensional Brownian motion, $\xi \in \mathcal { F } _ { 0 }$ , and $\pmb { \eta } = ( \eta , \eta ^ { ( 2 ) } ) \in \mathcal { C } ^ { \alpha } ( [ 0 , T ] , \mathbb { R } ^ { d } )$ . The coefficient processes

$$(b, \sigma) (\boldsymbol {\eta}): [ 0, T ] \times \Omega \times \mathbb {R} ^ {d _ {X}} \rightarrow \mathbb {R} ^ {d _ {X}} \times \mathbb {R} ^ {d _ {X} \times d _ {W}}$$

are assumed to be progressively measurable, and the stochastic controlled rough paths $( F , F ^ { \prime } ) ( \pmb { \eta } )$ and $( f , f ^ { \prime } ) ( \eta )$ are assumed to belong to the spaces $\mathbf { D } _ { \eta } ^ { \beta , \beta ^ { \prime } } L _ { \infty }$ and $\mathbf { D } _ { \eta } ^ { \beta , \beta ^ { \prime } } L _ { m }$ , respectively.

Definition 3.1. We call a continuous adapted process X an m-integrable solution to the rough SDE (3.1) in the space $\mathbf { D } _ { \eta } ^ { \beta , \beta ^ { \prime } } L _ { m } ( [ 0 , T ] ; \mathbb { R } ^ { d _ { X } } )$ , if the following holds:

(a) The integral $\begin{array} { r l } { \int _ { 0 } ^ { T } | b _ { t } ( X _ { t } , \eta ) | d t + \int _ { 0 } ^ { T } | \sigma _ { t } \sigma _ { t } ^ { \mathrm { T } } ( X _ { t } , \eta ) } \end{array}$ |dt is finite a.s.   
(b) The process $\Big ( F ( \eta ) X + f ( \eta ) , F ^ { \prime } ( \eta ) X + F ( \eta ) ( F ( \eta ) X + f ( \eta ) ) + f ^ { \prime } ( \eta ) \Big )$ is a stochastic controlled rough path and belongs to $\mathbf { D } _ { \eta } ^ { \beta , \beta ^ { \prime } } L _ { m }$ for some $m \geq 2 , \beta \in ( 0 , \alpha ]$ and $\beta ^ { \prime } \in ( 0 , \beta ]$ , with

$$\alpha + \beta > \frac {1}{2} \quad a n d \quad \alpha + \beta + \beta^ {\prime} > 1.$$

(c) The following integral equation holds a.s. for all $r \in [ 0 , T ]$ :

$$X _ {r} = \xi + \int_ {0} ^ {r} b _ {t} (X _ {t}, \boldsymbol {\eta}) d t + \int_ {0} ^ {r} \sigma_ {t} (X _ {t}, \boldsymbol {\eta}) d W _ {t} + \int_ {0} ^ {r} \left(F _ {t} (\boldsymbol {\eta}) X _ {t} + f _ {t} (\boldsymbol {\eta})\right) d \boldsymbol {\eta} _ {t}$$
