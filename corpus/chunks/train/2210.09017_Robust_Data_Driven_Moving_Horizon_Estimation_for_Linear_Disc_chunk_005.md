Theorem 1: ([21]) Suppose $\{ u ( k ) \} _ { k = 0 } ^ { N - 1 } , \ \{ y ( k ) \} _ { k = 0 } ^ { N - 1 }$ is an input/output trajectory of a controllable LTI system (1), where {u(k)}N−1k=0 L−1 $\{ u ( k ) \} _ { k = 0 } ^ { N - 1 }$ is persistently exciting of order $L + n$ . Then, $\{ \overline { { u } } ( k ) \} _ { k = 0 } ^ { L - 1 } , \{ \overline { { y } } ( k ) \} _ { k = 0 } ^ { L - 1 }$ is a trajectory of system (1) if and k=0 only if there exists $\mathbf { \Phi } _ { \alpha } ^ { \ast = \mathrm { { R } } ^ { N - L + \bar { 1 } } }$ such that

$$
\left[ \begin{array}{l} H _ {L} (u _ {[ 0, N - 1 ]}) \\ H _ {L} (y _ {[ 0, N - 1 ]}) \end{array} \right] \alpha = \left[ \begin{array}{l} \overline {{u}} _ {[ 0, L - 1 ]} \\ \overline {{y}} _ {[ 0, L - 1 ]} \end{array} \right]. \tag {3}
$$

This important result states that any length L system trajectory of a linear controllable LTI system can be expressed in terms of a single persistently exciting system trajectory. This implies that one does not necessarily need a mathematical model of the treated system to represent system trajectories. Particularly relevant in the context of state estimation is the result summarized in the following remark.

Remark 1: In addition to (3), it holds that [21, Eq. (5)]

$$\overline {{{x}}} _ {[ 0, L - 1 ]} = H _ {L} (x _ {[ 0, N - 1 ]}) \alpha , \tag {4}$$

where $\overline { { x } } _ { [ 0 , L - 1 ] }$ ] is the state trajectory of system (1) corresponding to $\overline { { u } } _ { [ 0 , L - 1 ] } , \overline { { y } } _ { [ 0 , L - 1 ] }$ of (3) and $x _ { [ 0 , N - 1 ] }$ the state trajectory corresponding to $u _ { [ 0 , N - 1 ] } , \ y _ { [ 0 , N - 1 ] }$ .

Throughout this paper, we consider an offline phase and an online phase. In the offline phase, we collect (noise-free) input, (noisy) output and (noisy) state trajectories of length N from the considered system so that an implicit system representation according to the fundamental lemma can be established. That is, we measure the noisy a priori state (denoted by $\tilde { x } ^ { d } )$ and output (denoted by $\tilde { y } ^ { d } )$ data

$$\tilde {y} ^ {d} (t) = y ^ {d} (t) + \varepsilon_ {y} ^ {d} (t) \tag {5}\tilde {x} ^ {d} (t) = x ^ {d} (t) + \varepsilon_ {x} ^ {d} (t), \tag {6}$$
