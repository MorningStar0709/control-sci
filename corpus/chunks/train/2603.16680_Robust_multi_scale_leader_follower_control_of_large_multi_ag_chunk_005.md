$\mathrm { F i g . }$ 1. Block diagram of the multi-scale leader-follower control architecture. An outer loop regulates the followers’ density by comparing the reference $\bar { \rho } ^ { \tilde { F } }$ with the estimated density $\rho ^ { F }$ , generating a reference density $\bar { \rho } ^ { L }$ for the leaders. An inner loop tracks this reference, producing the macroscopic control field $u ( x , t )$ . The macro-to-micro bridge maps $u ( x , t )$ to individual leader inputs ui via spatial sampling. The multi-agent dynamics produce leader and follower positions $\mathbf { X } _ { i } ,$ which the micro-to-macro bridge converts back to estimated densities $\rho ^ { L }$ and $\rho ^ { \dot { F } }$ via density estimation, closing both feedback loops.

$$f (x) = \frac {\operatorname{sgn} (x)}{\mathrm{e} ^ {2 \pi / \ell} - 1} \left[ \mathrm{e} ^ {\frac {2 \pi - | x |}{\ell}} - \mathrm{e} ^ {\frac {| x |}{\ell}} \right]. \tag {2}$$

It describes how leaders influence followers, with ℓ being the characteristic interaction length. The internal dynamics $h _ { i }$ and $g _ { i }$ are unknown and uniformly bounded, that is,

$$\left| h _ {i} (t, X ^ {L} (t)) \right| < r, \forall i \in 1, \dots , N ^ {L}, \forall t \in \mathbb {R} _ {\geq 0}, \tag {3a}\left| g _ {i} (t, X ^ {F} (t)) \right| < k, \forall i \in 1, \dots , N ^ {F}, \forall t \in \mathbb {R} _ {\geq 0}, \tag {3b}$$

with $r , k \geq 0 .$ .
