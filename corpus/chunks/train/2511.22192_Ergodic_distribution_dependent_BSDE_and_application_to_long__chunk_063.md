1.1. First, let us fix ?? and look only for the estimate w.r.t ??, that is to say $\left| w ^ { T } ( 0 , x , [ \theta ] ) - w ^ { T } ( 0 , x ^ { \prime } , [ \theta ] ) \right|$ . Let $\beta ^ { T }$ by given, for all $s \in \mathbb { R } _ { + } , x \in \mathbb { R } ^ { d }$ , by

$$\beta^ {T} (s, x) = \frac {f (x , \left[ X _ {s} ^ {[ \theta ]} \right] , \zeta^ {T} (x , \left[ X _ {s} ^ {[ \theta ]} \right])) - f (x , \left[ X _ {s} ^ {[ \theta ]} \right] , \zeta (x , \left[ X _ {s} ^ {[ \theta ]} \right]))}{\left| (\zeta^ {T} - \zeta) (x , \left[ X _ {s} ^ {[ \theta ]} \right]) \right| ^ {2}} ((\zeta^ {T} - \zeta) (x, \left[ X _ {s} ^ {[ \theta ]} \right])) ^ {\top} {\bf 1} _ {(\zeta^ {T} - \zeta) (x, \left[ X _ {s} ^ {[ \theta ]} \right]) \neq 0}.$$

Due to the Lipschitz property of $f \mathrm { { \ w . r . t } } \ z , \ \beta ^ { T }$ is bounded by $K _ { z } ^ { f } .$ . Consequently, let us set $\mathbb { Q } ^ { T }$ the Girsanov’s probability associated to $\beta ^ { T }$ under which $\mathrm { d } W ^ { T } : = \mathrm { d } W _ { t } - \beta ^ { T } ( s , x ) \mathrm { d }$ ?? is a Brownian motion. Then, $w ^ { T } ( 0 , x , [ \theta ] )$ writes under $\mathbb { Q } ^ { T }$

$$w ^ {T} (0, x, [ \theta ]) = \mathbb {E} ^ {\mathbb {Q} ^ {T}} \bigg [ g (X _ {T} ^ {x, [ \theta ]}, [ X _ {T} ^ {[ \theta ]} ]) - u (X _ {T} ^ {x, [ \theta ]}, [ X _ {T} ^ {[ \theta ]} ]) \bigg ].$$

By using Lemma 3.7, we denote $( \beta _ { n } ^ { T } )$ a sequence of Lipschitz functions, uniformly bounded with respect to ??, approximating $\beta _ { \theta } ^ { \dot { T } } , ( \mathbb { Q } _ { n } ^ { T } )$ the associated probabilities coming from Girsanov’s Theorem, and, for all $n \geq 0$ ,

$$w ^ {T, n} (0, x, [ \theta ]) := \mathbb {E} ^ {\mathbb {Q} _ {n} ^ {T}} \Big [ g (X _ {T} ^ {x, [ \theta ]}, \left[ X _ {T} ^ {[ \theta ]} \right]) - u (X _ {T} ^ {x, [ \theta ]}, \left[ X _ {T} ^ {[ \theta ]} \right]) \Big ].$$

Then, for all $n \geq 0$ , we can introduce $\mathcal P ^ { n }$ the semi-group associated to the following standard SDE

$$
\left\{ \begin{array}{l l} \mathrm{d} X _ {t} ^ {\beta_ {n} ^ {T}, x, [ \theta ]} = \Big (b (X _ {t} ^ {\beta_ {n} ^ {T}, x, [ \theta ]}, [ X _ {t} ^ {[ \theta ]} ]) + \beta_ {n} ^ {T} (t, X _ {t} ^ {\beta_ {n} ^ {T}, x, [ \theta ]}) \sigma (X _ {t} ^ {\beta_ {n} ^ {T}, x, [ \theta ]}, [ X _ {t} ^ {[ \theta ]} ]) \Big) \mathrm{d} t + \sigma (X _ {t} ^ {\beta_ {n} ^ {T}, x, [ \theta ]}, [ X _ {t} ^ {[ \theta ]} ]) \mathrm{d} W _ {t}, & t \in [ 0, T ], \\ X _ {0} ^ {\beta_ {n} ^ {T}, x, [ \theta ]} = x. \end{array} \right.
$$
