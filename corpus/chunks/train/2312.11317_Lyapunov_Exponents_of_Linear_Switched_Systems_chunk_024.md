and denote with $X _ { t , s }$ the solution of (2.2) with control equal to $u _ { t , s }$ . Let $\alpha ( t , s )$ be the greatest eigenvalue of $X _ { t , s } ( t + s )$ , so that the Principal Lyapunov exponent corresponding to the control $u _ { t , s }$ is

$$\ell (t, s) = \frac {1}{t + s} \log (\alpha (t, s)).$$

The following result help us in this task.

Lemma 4.29. With the notation introduced above:

1. $i f \operatorname { t r } \left( [ A , B ] ^ { 2 } \right) > 0 .$ , then $\ell ( \frac { t } { 2 } , \frac { s } { 2 } ) > \ell ( t , s )$ ;   
2. $i f \operatorname { t r } \left( [ A , B ] ^ { 2 } \right) < 0$ , then $\ell ( \textstyle { \frac { t } { 2 } } , \textstyle { \frac { s } { 2 } } ) < \ell ( t , s )$ ;

We first show how to obtain the estimate for the Principal Lyapunov exponent, then we prove the Lemma.

Theorem 4.30. If $\operatorname { t r } \left( [ A , B ] ^ { 2 } \right) > 0$ , then for every $t > 0$ it holds

$$\ell (t, s (t)) < \lim _ {\tau \rightarrow 0} \ell (\tau , s (\tau)) = \frac {1}{2} \sqrt {\frac {\operatorname{tr} ([ A , B ] ^ {2})}{2 \operatorname{tr} (A B) - \operatorname{tr} (A ^ {2}) - \operatorname{tr} (B ^ {2})}} \tag {4.31}$$

If instead $\mathrm { t r } \big ( [ A , B ] ^ { 2 } \big ) < 0 .$ then control with switches are not optimal.

Proof. First, suppose that tr $\left( [ A , B ] ^ { 2 } \right) > 0$ . One can check that this condition implies that the coefficient in (4.26) is smaller than 1:

$$\frac {\mu}{\lambda} \frac {\mathrm{tr} (A B) - \mathrm{tr} (A ^ {2})}{\mathrm{tr} (A B) - \mathrm{tr} (B ^ {2})} < 1.$$

${ \mathrm { S o } } ,$ for every $t > 0$ it makes sense to take the inverse hyperbolic tangent in (4.26) on both sides. Moreover, from point (1) of Lemma 4.29, for any $t > 0$ we have

$$\ell \left(\frac {t}{2}, \frac {s (t)}{2}\right) > \ell (t, s (t)).$$

In particular, we can find $t _ { 1 } < t$ such that $\begin{array} { r } { t _ { 1 } + s ( t _ { 1 } ) = \frac { 1 } { 2 } ( t + s ( t ) ) } \end{array}$ , so that

$$\ell (t _ {1}, s (t _ {1})) > \ell \left(\frac {t}{2}, \frac {s (t)}{2}\right) > \ell (t, s (t)).$$

Moreover, we have that li $\begin{array} { r } { { 1 } _ { t  0 } s ( t ) = 0 . } \end{array}$ , so

$$\ell (t, s (t)) < \lim _ {\tau \to 0} \ell (\tau , s (\tau)).$$

To evaluate the last limit, one can use Taylor expansions. This leads to

$$\lim _ {t \to 0} \ell (t, s (t)) = \frac {1}{2} \sqrt {\frac {\operatorname{tr} ([ A , B ] ^ {2})}{2 \operatorname{tr} (A B) - \operatorname{tr} (A ^ {2}) - \operatorname{tr} (B ^ {2})}}.$$

It is possible to avoid to compute explicitly this limit (see next Subsection).
