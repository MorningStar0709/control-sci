$$
\begin{array}{l} \log p (\Lambda_ {0}, \gamma_ {k} | \mathcal {I} _ {k - 1}) \\ = - \frac {1}{2} (z _ {k} - H _ {k} \hat {x} _ {k | k - 1}) ^ {T} Y _ {k} (z _ {k} - H _ {k} \hat {x} _ {k | k - 1}) - 0. 5 \log | \Phi_ {k | k - 1} | \\ + \sum_ {j = 1} ^ {M} \lambda_ {j, k} \log \mu_ {j, k} - \frac {1}{2} (\phi_ {k} - \hat {\phi} _ {k | k - 1}) ^ {T} \Phi_ {k | k - 1} ^ {- 1} (\phi_ {k} - \hat {\phi} _ {k | k - 1}) \\ + \sum_ {j = 1} ^ {M} \lambda_ {j, k} \left[ 0. 5 \hat {g} _ {j, k | k - 1} \log (| \hat {G} _ {j, k | k - 1} |) - 0. 5 \operatorname{tr} (\hat {G} _ {j, k | k - 1} P _ {k | k - 1} ^ {- 1}) \right. \\ - 0. 5 \left(\hat {g} _ {j, k \mid k - 1} + n + 1\right) \log \left(\left| P _ {k \mid k - 1} \right|\right) - 0. 5 \left(n \hat {g} _ {j, k \mid k - 1}\right) \log 2 \\ \left. - \log \Gamma_ {n} (\hat {g} _ {j, k | k - 1} / 2) \right] + \sum_ {j = 1} ^ {M} \left(\alpha_ {j, k | k - 1}\right) \log \mu_ {j, k} \\ - 0. 5 \left(\hat {s} _ {k \mid k - 1} + m + 1\right) \log | R _ {k} | - 0. 5 \operatorname{tr} \left(\hat {S} _ {k \mid k - 1} R _ {k} ^ {- 1}\right) + c _ {\Lambda_ {0}}, \tag {23} \\ \end{array}
$$

where $c _ { \Lambda _ { 0 } }$ is a constant independent of $\Lambda _ { 0 }$ .

2) Decoupling of $\Phi _ { k | k - 1 } .$ Each element of $\Lambda _ { 0 }$ in (22) will be calculated alternatively, and the corresponding posterior distribution should have the same functional form as the prior distribution according to the variational inference theory [24]. However, $P _ { k | k - 1 }$ and $R _ { k }$ are coupled in $\Phi _ { k \mid k - 1 } ,$ , which poses a challenge in deriving the posterior $x _ { k } , P _ { k | k - 1 }$ , and $R _ { k }$ by using (8). In $( 2 3 ) , - 0 . 5 \mathrm { l o g } \ | \Phi _ { k | k - 1 } |$ and $- \textstyle \frac { 1 } { 2 } \big ( \phi _ { k } - \hat { \phi } _ { k | k - 1 } \big ) ^ { T } \Phi _ { k | k - 1 } ^ { - 1 } \big ( \phi _ { k } - \hat { \phi } _ { k | k - 1 } \big )$ will be decoupled for $P _ { k | k - 1 }$ and $R _ { k }$ . In addition, the expectation of $\Phi _ { k | k - 1 } ^ { - 1 }$ is calculated, as follows.

First, $\Phi _ { k | k - 1 }$ is factorized as

$$
\Phi_ {k \mid k - 1} = \left[ \begin{array}{c c} I & 0 \\ H & I \end{array} \right] \left[ \begin{array}{c c} P _ {k \mid k - 1} & 0 \\ 0 & R _ {k} \end{array} \right] \left[ \begin{array}{c c} I & H ^ {T} \\ 0 & I \end{array} \right]. \tag {24}
$$

Then, the inverse of $\Phi _ { k | k - 1 }$ is computed by

$$
\Phi_ {k \mid k - 1} ^ {- 1} = \left[ \begin{array}{c c} I & - H ^ {T} \\ 0 & I \end{array} \right] \left[ \begin{array}{c c} P _ {k \mid k - 1} ^ {- 1} & 0 \\ 0 & R _ {k} ^ {- 1} \end{array} \right] \left[ \begin{array}{c c} I & 0 \\ - H & I \end{array} \right]. \tag {25}
$$
