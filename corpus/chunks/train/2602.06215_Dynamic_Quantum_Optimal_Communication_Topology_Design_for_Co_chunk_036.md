The overall closed-loop dynamic procedure that combines ADMM-based topology optimization, QITE-based binary updates, and consensus dynamics is summarized

Algorithm 2 Dynamic topology design   
1: Inputs: initial states $\boldsymbol{x}(0)$ (and $\boldsymbol{v}(0)$ ), update times $\{t_{\ell}\}$ , degree bound $\gamma$ , ADMM parameters $\rho, \beta, \mu$ , QITE settings (ansatz, steps, shots).

2: Choose initial connected graph $\mathcal{G}_0$ and Laplacian $L_0$ satisfying Assumption 2.

3: Set $\ell \leftarrow 0$ .

4: while closed-loop operation is active do

5: Consensus evolution on $[t_{\ell}, t_{\ell+1})$ :

6: Simulate consensus with $L_{\ell}$ :

7: First-order: (3).

8: Second-order: (8)-(9).

9: At $t_{\ell+1}$ , measure agent positions (and velocities).

10: Update edge weights $w_{ij}$ via (10).

11: Form MIQP (18) with costs (11), (13) and constraints (18d)-(18g).

12: Initialize ADMM:

13: Choose $(z^{(0)}, f^{(0)}, r^{(0)}, s^{(0)}, \lambda^{(0)})$ , set $k \leftarrow 0$ .

14: while ADMM stopping criterion not met do

15: $k \leftarrow k + 1$ .

16: Block 1 (classical convex subproblem):

17: Given $r^{(k-1)}, s^{(k-1)}, \lambda^{(k-1)}$ , solve (22)

18: for $(z^{(k)}, f^{(k)})$ over $\mathcal{C}$ .

19: Block 2 (quantum binary subproblem):

20: Build QUBO cost $\Phi(r)$ in (42).

21: Map QUBO to Ising $H^{(k)}$ via (44), (45).

22: Run QITE for $H^{(k)}$ :

23: Initialize ansatz $U(\theta)$ (Fig. 2).

24: for each imaginary-time step do

25: Prepare $|\psi(\theta)\rangle = U(\theta)|0\rangle$ .

26: Measure expectations required by QITE.

27: Build and solve the small linear system for $\Delta\theta$ .

28: Update $\theta \leftarrow \theta + \Delta\theta$ .

29: end for

30: Sample bitstrings $r$ from $|\psi(\theta)\rangle$ .

31: Evaluate $\Phi(r)$ and set $r^{(k)}$ to the best sample.

32: Block 3 (auxiliary update):

33: Update $s^{(k)}$ using (25).

34: Dual update:

35: Compute $d^{(k)}$ from (26) and update $\lambda^{(k)}$ via (27).

36: Check convergence as in Theorem 2.

37: end while

38: Laplacian update:

39: Construct $E_{\ell+1}$ and $L_{\ell+1}$ from the ADMM output

40: (e.g., using $r^{(k)}$ or thresholded $z^{(k)}$ ), ensuring (18d)-(18g) hold.

41: Set $\ell \leftarrow \ell + 1$ .

42: end while

in Algorithm 2.
