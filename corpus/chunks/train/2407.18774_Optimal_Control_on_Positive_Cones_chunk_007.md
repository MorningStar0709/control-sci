with domain $\{ x : ( x , v ) \in \mathcal { P }$ for some v} is a linear form (that $i s ,$ the function above takes the form $f ( x ) = \phi ^ { \top } x f o r$ some $\phi \in \mathbb { R } ^ { n } )$ .

(ii) The set ${ \mathcal { C } } _ { \mu }$ has a minimum element (that $i s ,$ there exists a $\bar { \lambda } \in { \mathcal C } _ { \mu }$ such that $\lambda \in \mathcal { C } _ { \mu } \overset { \cdot } { \implies } \left( \lambda - \bar { \lambda } , 0 \right) \in \mathcal { P } ^ { * } )$ .

Furthermore, $i f \bar { \lambda }$ is the minimum element in (ii), then the linear form in $( i ) i s f ( x ) =$ $- \bar { \lambda } ^ { \top } x$ .

Proof. $( i ) \implies ( i i )$ : Under the hypothesis of (i), there exists a $\bar { \lambda }$ such that the function in (6) takes the form $f \left( \dot { x } \right) = - \bar { \lambda } ^ { \top } x .$ . We will first establish that $\left( \bar { \lambda } , \mu \right) \in$ ${ \mathcal { P } } ^ { * }$ . Assume the converse. There therefore exists an

$$
(\bar {x}, \bar {u}) \in \mathcal {P} \text {   such   that   } \left[ \begin{array}{c} \bar {\lambda} \\ \mu \end{array} \right] ^ {\top} \left[ \begin{array}{c} \bar {x} \\ \bar {u} \end{array} \right] <   0,
$$

which implies that $- \bar { \lambda } ^ { \top } \bar { x } > \mu ^ { \top } \bar { u }$ . However from (6) we see that $- \bar { \lambda } ^ { \top } \bar { x } \le \mu ^ { \top } \bar { u }$ , which is a contradiction. We will now show that λ¯ is the minimum element of ${ \mathcal { C } } _ { \mu }$ . The optimization in (6) is a cone program on standard form, with dual program (see for example [8, p.266])

$$\sup _ {(\lambda , \mu) \in \mathcal {P} ^ {*}} - \lambda^ {\top} x. \tag {7}$$

By weak duality this implies that

$$\inf _ {(x, u) \in \mathcal {P}} \mu^ {\top} u \geq \sup _ {(\lambda , \mu) \in \mathcal {P} ^ {*}} - \lambda^ {\top} x,$$

and so for any $( x , u ) \in \mathcal { P }$ ,

$$(\lambda , \mu) \in \mathcal {P} ^ {*} \implies \bar {\lambda} ^ {\top} x \leq \lambda^ {\top} x. \tag {8}$$

This implies that $\left( \lambda - \bar { \lambda } , 0 \right) \in \mathcal { P } ^ { * }$ , which proves the claim.

(ii) =⇒ (i): For all $( x , u ) \in \mathcal { P } .$ ,

$$
\left[ \begin{array}{c} \lambda - \bar {\lambda} \\ 0 \end{array} \right] ^ {\top} \left[ \begin{array}{c} x \\ u \end{array} \right] \geq 0, \Longrightarrow - (\lambda - \bar {\lambda}) ^ {\top} x \leq 0.
$$

Therefore for any such $\lambda ,$

$$- \lambda^ {\top} x = - (\lambda - \bar {\lambda}) ^ {\top} x - \bar {\lambda} ^ {\top} x \leq - \bar {\lambda} ^ {\top} x.$$

It then follows that the solution to (7) equals $- \bar { \lambda } ^ { \top } x$ . Condition (5) ensures that (7) is strictly feasible. Therefore strong duality holds, which proves (6) as required. □
