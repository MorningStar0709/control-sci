# B. Suboptimal MPC

To analyze the suboptimal closed-loop system (6), we look at the combined system-optimizer dynamics (5). The following theorem characterizes the linear convergence rate of PGM.

Theorem 1: [17, Theorem 3.1] For any $x \in \mathbb { R } ^ { n } , \nu \in$ $\mathbb { R } ^ { N m } , \ell \in \mathbb { N } .$ RNm, ℓ ∈ N, and for α = λ+(H)+λ−(H) $\begin{array} { r } { \alpha = \frac { 1 } { \lambda ^ { + } ( H ) + \lambda ^ { - } ( H ) } } \end{array}$ 1

$$\left\| \mathcal {T} ^ {\ell} (x, \boldsymbol {\nu}) - \mu^ {\star} (x) \right\| \leq \eta^ {\ell} \| \boldsymbol {\nu} - \mu^ {\star} (x) \|,$$

where $\eta = ( \lambda ^ { + } ( H ) - \lambda ^ { - } ( H ) ) / ( \lambda ^ { + } ( H ) + \lambda ^ { - } ( H ) )$ .

Remark 1: We take the initial $\begin{array} { r } { \nu = 0 . } \end{array}$

The stability of (5) is assessed by analysing the evolution of the suboptimality disturbance $d ( z _ { k } , x _ { k } )$ and $\psi ( x _ { k } )$ over time.

Lemma 3: Given the dynamics (5), for all $k \geq 0 , x _ { k } \in$ $\Gamma _ { N }$ and $z _ { k } \in \mathcal N$ , the following holds

$$\psi (x _ {k + 1}) \leq \beta \psi (x _ {k}) + \sigma \| d (z _ {k}, x _ {k}) \|,\left\| d \left(z _ {k + 1}, x _ {k + 1}\right) \right\| \leq \eta^ {\ell_ {k}} \kappa \psi \left(x _ {k}\right) + \eta^ {\ell_ {k}} \omega \left\| d \left(z _ {k}, x _ {k}\right) \right\|,$$

where $\omega = 1 + \| H ^ { - \frac { 1 } { 2 } } \| \| H ^ { - \frac { 1 } { 2 } } G \overline { { B } } \| , \sigma = \| W ^ { \frac { 1 } { 2 } } \overline { { B } } \|$ , and

$$\kappa = \left\| H ^ {- \frac {1}{2}} \right\| \left\| H ^ {- \frac {1}{2}} G (A - I) P ^ {- \frac {1}{2}} \right\|+ \| H ^ {- \frac {1}{2}} \| \sqrt {\lambda_ {H} ^ {+} (G \overline {{B}}) (\lambda_ {P} ^ {+} (W) - 1)}.$$

The lemma is a slightly modified version of [12, Lemma 7] with time-varying $\ell _ { k } { \cdot } \mathbf { s } .$ . The proof follows directly from the one found in [12]. The following shows the existence of a Lyapunov function for the augmented state $s _ { k }$ .

Theorem 2: If $\ell _ { k } > \ell ^ { \star }$ for all $k \geq 0$ , where

$$\ell^ {\star} = \frac {\log (1 - \beta) - \log (\sigma \kappa + \omega (1 - \beta))}{\log (\eta)},$$

then the system-optimizer dynamics (5) are asymptotically stable in the forward invariant ROA estimate

$$\Sigma_ {N} = \left\{(x, z) \in \Gamma_ {N} \times \mathcal {N} \mid \psi (x) \leq r _ {N}, \right.\left\| z - \mu^ {\star} (x) \right\| \leq \frac {(1 - \beta) r _ {N}}{\sigma} \Biggr \}.$$

The function

$$\mathcal {L} (x, z) := \psi (x) + \tau \| z - \mu^ {\star} (x) \| \tag {10}$$
