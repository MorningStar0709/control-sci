$$
\begin{array}{l} \tilde {a} = - \beta \left(\xi^ {\top} (\mathcal {D} ^ {\top} \otimes I _ {n}) \left(\nabla^ {2} \tilde {L} (x, t)\right) ^ {- 1} (\mathcal {D} \otimes I _ {n}) \eta\right) \\ + \xi^ {\top} (\mathcal {D} ^ {\top} \otimes I _ {n}) \Psi \\ \leq - \beta \left(\xi^ {\top} (\mathcal {D} ^ {\top} \otimes I _ {n}) \left(\nabla^ {2} \tilde {L} (x, t)\right) ^ {- 1} (\mathcal {D} \otimes I _ {n}) \xi\right) \\ + \xi^ {\top} (\mathcal {D} ^ {\top} \otimes I _ {n}) \Psi \\ \leq - \beta \bar {\lambda} _ {\min} \left((\nabla^ {2} \tilde {L}) ^ {- 1}\right) \| (\mathcal {D} \otimes I _ {n}) \xi \| _ {2} ^ {2} + 2 \bar {\psi} n ^ {2} | \mathcal {E} |. \tag {22} \\ \end{array}
$$

If any two connected nodes $( i _ { 2 } , j _ { 2 } ) \in \mathcal { E }$ have not the same position, $x _ { i _ { 2 } } \neq x _ { j _ { 2 } }$ , then $\| ( \mathcal { D } \otimes I _ { n } ) \xi \| \ge 1$ . Then it can be deduced that

$$\tilde {a} \leq - \beta \bar {\lambda} _ {\min} \left((\nabla^ {2} \tilde {L}) ^ {- 1}\right) + 2 \bar {\psi} n ^ {2} | \mathcal {E} |. \tag {23}$$

$\begin{array} { r } { \mathrm { I f } \beta \geq \frac { 2 \hat { \psi } n ^ { 2 } | \mathcal { E } | } { \operatorname* { m i n } _ { i \in \mathcal { V } } \left\{ \hat { \lambda } _ { \operatorname* { m i n } } \left( ( \nabla ^ { 2 } \tilde { L } _ { i } ) ^ { - 1 } \right) \right\} } + \epsilon = \frac { 2 \hat { \psi } n ^ { 2 } | \mathcal { E } | } { \hat { \lambda } _ { \operatorname* { m i n } } \left( ( \nabla ^ { 2 } \tilde { L } ) ^ { - 1 } \right) } + \epsilon , } \end{array}$ this ensures that if there exists an edge $( i _ { 2 } , j _ { 2 } ) \in \dot { \mathcal { E } }$ such that $x _ { i _ { 2 } } \neq$ $x _ { j _ { 2 } }$ , then $\tilde { a } \le - \epsilon$ . This condition leads to $\dot { V } _ { S 2 } ( t ) \le - \epsilon$ under the same circumstances. According to the Lebesgue’s theory for Riemann integrability, a function on a compact interval is Riemann integrable if and only if it is bounded and the set of its discontinuous points has measure zero [27]. This means that despite some irregularities at certain moments, the derivative $\dot { V } _ { S 2 } ( t )$ can still be integrated over time.

Thus, it follows that

$$V _ {S 2} (t) - V _ {S 2} (0) = \int_ {0} ^ {t} \dot {V} _ {S 2} (\tau) d \tau \leq - \epsilon t \tag {24}$$

When $t > 0$ and there’s a connection $( i _ { 2 } , j _ { 2 } ) \ \in \mathcal { E }$ in the network with different values at both nodes $( x _ { i _ { 2 } } \neq x _ { j _ { 2 } } )$ , then we can deduce that
