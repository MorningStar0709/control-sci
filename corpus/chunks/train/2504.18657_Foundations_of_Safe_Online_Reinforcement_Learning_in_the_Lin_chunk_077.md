The following lemma (Lemma 23 in Schiffer and Janson [2024]) bounds the uncertainty in $\theta ^ { * }$ from regularized least squares estimation.

Lemma 14 (Lemma 23 in Schiffer and Janson [2024], Theorem 1 in Abbasi-Yadkori and Szepesv´ari [2011]). Suppose $x _ { t }$ and $u _ { t }$ are respectively the state and control at time t when using an arbitrary controller C starting at state $x _ { 0 } = 0$ . Define ${ \boldsymbol z } _ { t } = ( x _ { t } , u _ { t } )$ and let $\lambda > 0$ . Let $Z _ { t } \in \mathbb { R } ^ { t \times 2 }$ where the ith row is $z _ { i - 1 }$ , let $X _ { t } \in \mathbb { R } ^ { t \times 1 }$ where the ith element is $x _ { i }$ , and let $I \in \mathbb { R } ^ { 2 \times 2 }$ be the identity matrix. Then under Assumptions $1 { - } 3 ,$ with probability $1 - o _ { T } \left( { \frac { 1 } { T ^ { 2 } } } \right)$ the following holds for all $1 \leq t \leq T - 1$ and for any $S \subseteq [ 0 : t - 1 ]$ :

$$\left\| \theta^ {*} - \left(Z _ {t} ^ {\top} Z _ {t} + \lambda I\right) ^ {- 1} Z _ {t} ^ {\top} X _ {t} \right\| _ {\infty} \leq \sqrt {\frac {\max \left(\left(V _ {t} ^ {S}\right) _ {1 1} , \left(V _ {t} ^ {S}\right) _ {2 2}\right)}{\det \left(V _ {t} ^ {S}\right)}} B _ {t}, \tag {53}$$

where $\begin{array} { r } { V _ { t } ^ { S } ~ = ~ \lambda I + \sum _ { s = 0 } ^ { t - 1 } z _ { s } z _ { s } ^ { \top } 1 _ { s \in S } , ~ B _ { t } ~ = ~ \alpha \sqrt { \log \left( \operatorname* { d e t } \left( V _ { t } ^ { [ 0 : t - 1 ] } \right) \right) + \log ( \lambda ^ { 2 } ) + 2 \log ( T ^ { 2 } ) + \log ( \lambda ^ { 2 } ) } , } \end{array}$ $\sqrt { \lambda } ( \bar { a } ^ { 2 } + \bar { b } ^ { 2 } )$ , and α is from the subgaussian assumption on the noise distribution D, which implies that there exists an α such that $\mathbb { E } _ { w \sim \mathcal { D } } [ \exp ( \gamma w ) ] \le \exp ( \gamma ^ { 2 } \alpha ^ { 2 } / 2 )$ for any $\gamma \in \mathbb { R }$ .

By Lemma 14 we have that with probability $1 - o _ { T } ( 1 / T ^ { 2 } )$ , for all $s , \ \lVert \theta ^ { * } - \hat { \theta } _ { s } ^ { \mathrm { p r e } } \rVert _ { \infty } \ \leq$ $\epsilon _ { s }$ .Therefore,

$$\mathbb {P} (E _ {0}) = 1 - o _ {T} (1 / T ^ {2}).$$

By construction we also have that $\| \hat { \theta } _ { s } - \hat { \theta } _ { s } ^ { \mathrm { p r e } } \| _ { \infty } \leq \epsilon _ { s }$ . This implies by the triangle inequality that under event $E _ { 0 } , \| \hat { \theta } _ { s } - \theta ^ { * } \| _ { \infty } \leq 2 \epsilon _ { s }$ .

We also have the following uncertainty result that is equivalent to Lemma 2 in Schiffer and Janson [2024]:

Lemma 15. Under Assumptions 1–3, there exists a $c _ { \mathrm { L 1 5 } } = \tilde { O } _ { T } ( 1 )$ such that with probability $1 - o _ { T } ( 1 / T ^ { 2 } )$
