As stated in [1], since △ is Hurwitz, there exists a positivedefinite matrix $P _ { 1 }$ such that the following condition holds for any positive number α¯:

$$\triangle^ {\top} P _ {1} + P _ {1} \triangle = - \bar {\alpha} I \tag {28}$$

where $I \in \mathbb { R } ^ { ( n - 1 ) \times ( n - 1 ) }$ .

Lemma $2 \colon \mathrm { H } \ r _ { i } ( t )$ is ultimately bounded, then $e _ { i } ( t )$ is ultimately bounded.

Proof: See [1, Lemma 10.3].

Lemma 3 (Graph Lyapunov Equation): Define

$$q \equiv [ q _ {1}, \dots , q _ {N} ] ^ {\top} := (\nu_ {1} L + \nu_ {2} B) ^ {- 1} \underline {{{{1}}}}, \tag {29}P \equiv \mathrm{diag} \{p _ {i} \} _ {i \in \mathcal {N}} := \mathrm{diag} \{1 / q _ {i} \} _ {i \in \mathcal {N}}, \tag {30}Q := P (\nu_ {1} L + \nu_ {2} B) + (\nu_ {1} L + \nu_ {2} B) ^ {\top} P, \tag {31}$$

where $\underline { { 1 } } = [ 1 , . . . , 1 ] ^ { \top } \in \mathbb { R } ^ { N }$ . then the matrices P and $Q$ are positive definite.

Proof: The proof is similar to [1, Lemma 10.1] by noting that $\nu _ { 1 } L + \nu _ { 2 } B$ is nonsingular by Lemma 1.
