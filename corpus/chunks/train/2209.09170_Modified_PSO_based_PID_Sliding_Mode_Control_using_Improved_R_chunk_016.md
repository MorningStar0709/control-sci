# Switching control

The switching control is the discontinuous control law which forces the system onto the sliding. It allows an easy verification of the sufficiency conditions for the existence and reachability of the sliding mode i.e. $\phantom { 0 } { s ( x ) . \dot { s } ( x ) } < 0$ when $s ( x ) = 0$ .

The switching control $u _ { s w } ( t )$ is defined as (constant gain)

$$u _ {s w} (t) = - \frac {k _ {s c}}{g (\theta , t)} s g n (s (x)) \tag {24}$$

where $k _ { s c }$ is positive constant and $k _ { s c } > d _ { m a x }$ dominates matching uncertainties. Therefore,

$$\dot {s} (t) = - k _ {s c} \operatorname{sign} (s (t)) - d (t) \tag {25}
\operatorname{sign} (s) = \frac {s}{| s |} = \left\{ \begin{array}{c c} 1, & s > 0 \\ 0, & s = 0 \\ - 1, & s <   0 \end{array} \right. \tag {26}
$$

Boundary Layer is the approach used to reduce the effect of chattering by constraining the control, along a boundary on the sliding manifold.

$$B (t) = \{x, | s (x, t) | \leq \varphi \} \quad \varphi > 0 \tag {27}$$

Outside the boundary layer, control law is chosen (following sliding condition) guaranteeing attractiveness. All trajectories starting inside the boundary will remain inside, by using the usual feedback control, B(t) i.e. replacing the term ????????(??) ???? ??/?? inside B(t). This leads to tracking within a guaranteed precision, inside boundary layer width. Saturation function ‘sat(s)’ is adopted instead of sign(s).

$$
s a t (s) = \left\{ \begin{array}{l l} 1 & s > \Delta \\ k s & | s | \leq \Delta , k = 1 / \Delta \\ - 1 & s <   - \Delta \end{array} \right. \tag {28}
$$

Substituting the above equation in (23) will result in:

$$u (t) = - (K _ {d}. g (\theta , t)) ^ {- 1}. (K _ {i} (\theta_ {r} (t) - \theta (t)) + K _ {p} (\dot {\theta} _ {r} (t) - \dot {\theta} (t)) + K _ {d} (\ddot {\theta} _ {r} (t) - f (\theta , \dot {\theta}, t)) + k s +k _ {s c} | s | ^ {\alpha}. \operatorname{sat} (s)) \tag {29}$$
