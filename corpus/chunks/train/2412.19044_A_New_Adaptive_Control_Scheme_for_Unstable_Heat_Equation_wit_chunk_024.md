Lemma 6.2. Suppose that $b \neq 0 , c _ { 0 } > 0$ and $c _ { 1 } > 0$ . Then for any initial state $( \tilde { w } ( \cdot , 0 ) , \breve { w } ( \cdot , 0 ) , \tilde { \zeta } ( 0 ) ) \in$ $\mathcal { H } ^ { 2 } \times \mathbb { R } ,$ , system (6.1) admits a weak solution $( \tilde { w } ( \cdot , t ) , \check { w } ( \cdot , t ) , \tilde { \zeta } ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ such that

$$| \tilde {\zeta} (t) - \zeta_ {*} | + \| (\tilde {w} (\cdot , t), \check {w} (\cdot , t)) \| _ {\mathcal {H} ^ {2}} \rightarrow 0 \text { as } t \rightarrow + \infty , \tag {6.35}$$

where $\zeta _ { * }$ is a constant that may not be 0.

Proof. By Lemma 6.1, system (6.1) admits a weak solution $( \tilde { w } ( \cdot , t ) , \check { w } ( \cdot , t ) , \tilde { \zeta } ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ such that (6.2) holds. If we let $u _ { 0 } ( t ) = - ( q + c _ { 0 } ) \check { w } ( 1 , t )$ , then $u _ { 0 } \in L ^ { 2 } ( 0 , \infty )$ . Since the $\tilde { w } , \tilde { \zeta } -$ subsystem of (6.1) happens to be system (2.7), by exploiting Lemma 5.2, there exists a constant $\zeta _ { * }$ that may not be zero such that

$$\| \tilde {w} (\cdot , t) \| _ {\mathcal {H}} + | \tilde {\zeta} (t) - \zeta_ {*} | \rightarrow 0 \text {as} t \rightarrow + \infty . \tag {6.36}$$

Moreover, $\tilde { w } ( 0 , \cdot ) , \tilde { w } ( 1 , \cdot ) \in L ^ { 2 } ( 0 , \infty )$ . Since the ˇw-subsystem of (6.1) is a well-known exponentially stable heat equation with inhomogeneous terms $q ^ { 2 } e ^ { q x } \tilde { w } ( 0 , t ) , - q \tilde { w } ( 0 , t )$ and $c _ { 1 } \tilde { w } ( 1 , t )$ , we can deduce from [5, Lemma 3.1] that $\| \check { w } ( \cdot , t ) \| _ { \mathcal { H } } \to 0 \mathrm { ~ a s ~ } t \to + \infty$ . □

Proof of Theorem 3.1. By Lemma 6.2, the transformed system (6.1) admits a weak solution $( \tilde { w } ( \cdot , t ) , \check { w } ( \cdot , t ) , \tilde { \zeta } ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ such that (6.35) holds. In order to relate the transformed system (6.1) to the original system (3.3), we define the transformation Π : H → H by

$$(\Pi f) (x) = f (x) + q \int_ {0} ^ {x} e ^ {q (x - s)} f (s) d s, \forall f \in \mathcal {H}. \tag {6.37}$$

Then, $\Pi \in { \mathcal { L } } ( { \mathcal { H } } )$ is invertible and more specifically

$$(\Pi^ {- 1} g) (x) = g (x) - q \int_ {0} ^ {x} g (s) d s, \forall g \in \mathcal {H}. \tag {6.38}$$

In terms of the constant b, we define the transformation $\Upsilon _ { b } : \mathbb { R }  \mathbb { R }$ by

$$\Upsilon_ {b} (s) = \frac {1}{b} - s, \forall s \in \mathbb {R}. \tag {6.39}$$
