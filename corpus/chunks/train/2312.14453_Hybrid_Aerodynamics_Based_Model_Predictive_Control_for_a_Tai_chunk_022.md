# D. Experimental Results

In flight testing, MPC was implemented on the onboard computer mentioned in Section IV-B with ACADOS toolbox to generate a fast C-code solver. The NN model function was generated by MATLAB genF unction syntax and used for HMPC. The MPC ran with a 100-Hz update frequency and 20 × 0.05s horizon, as shown in Table III, which yielded an OCP solving time of less than 15 ms with an approximately 20% CPU load.

The diagonal weighting matrices Q and $\mathbf { Q } _ { N }$ correspond to [x u] and x, respectively. A higher weighting was applied to position than to velocity, to favor position tracking accuracy over transient responsiveness. A relatively high penalty was applied to control inputs to prevent the excitation of innerloop controllers with low position-tracking error.

![](image/02f75d5ba3a23531eeb2389825bfc9b4605b48fcbf3f04219b86790b9a52263e.jpg)  
Fig. 4. Errors in the acceleration predictions generated by the nonlinear and hybrid models from the testing dataset.

TABLE IIIMPC PARAMETERS AND WEIGHTS IN THE COST FUNCTION

<table><tr><td>MPC Parameter</td><td>Value</td></tr><tr><td>Prediction horizon</td><td>20</td></tr><tr><td>Sample time (s)</td><td>0.05</td></tr><tr><td> $\mathbf{Q}$ </td><td>diag [12 12 12 3 3 3 1 1 400 30 30]</td></tr><tr><td> $\mathbf{Q}_{N}$ </td><td>diag [12 12 12 3 3 3 1 1]</td></tr></table>

To investigate how MPC benefited from the NN-augmented model, we compared the tracking performance of MPC for different trajectories. Both NMPC and HMPC use the same set of parameters. The step responses of NMPC and HMPC are shown in Fig. 5 and show that the hybrid model exhibited better tracking performance in the X direction, as the residual dynamics were greater in this direction than in the Y and Z directions. With the application of the hybrid model, the rise time decreased from 3.7 s to 2.0 s and the positional RMSE decreased from 0.106 m to 0.071 m.
