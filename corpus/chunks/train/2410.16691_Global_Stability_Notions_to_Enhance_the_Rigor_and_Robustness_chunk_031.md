Theorem 6 (Lyapunov conditions for p-IOS): Suppose that (36) is forward complete and there exist a function $a \in K _ { \infty }$ , a non-decreasing, continuous function $\varphi : \mathbb { R } _ { + } \to \mathbb { R } _ { + }$ ， functions $V _ { i } \in C ^ { 1 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right) \ i = 1 , . . . , m$ with $V _ { i } ( 0 ) = 0 ~ f o r ~ i = 1 , . . . , m$ and continuous, positive definite functions $\rho _ { i } : \mathbb { R } _ { + } \to \mathbb { R } _ { + } , i = 1 , . . . , m$ for which the following inequality holds for all $x \in \mathbb { R } ^ { n }$

$$a \left(\left| h (x) \right|\right) \leq \max _ {i = 1, \dots , m} \left(V _ {i} (x)\right) \tag {60}$$

and the following implication holds for all $x \in \mathbb { R } ^ { n } , \ d \in D , \ i = 1 , . . . , m$ :

$$V _ {i} (x) \geq \varphi (\left| d \right|) \Rightarrow \dot {V} _ {i} (x, d) = \nabla V _ {i} (x) f (x, d) \leq - \rho_ {i} (V _ {i} (x)) \tag {61}$$

Then system (36), (2) satisfies the p-IOS property with gain function $\gamma ( s ) = a ^ { - 1 } { \bigl ( } \varphi ( s ) { \bigr ) } - a ^ { - 1 } { \bigl ( } \varphi ( 0 ) { \bigr ) }$ and residual constant $\alpha = a ^ { - 1 } \big ( \varphi ( 0 ) \big )$ . Moreover, if $\varphi ( 0 ) = 0$ then system (36), (2) satisfies the IOS property with gain function $\gamma = a ^ { - 1 } \circ \varphi$ .

Example 7: Consider the following system

$$\dot {y} = - \left(z + k + \sigma (\theta + z) ^ {2}\right) y + d\dot {z} = y ^ {2} \tag {62}x = (y, z) ^ {\prime} \in \mathbb {R} ^ {2}, d \in \mathbb {R}$$

where $\theta \in \mathbb { R } , \sigma , k > 0$ are constants that satisfy the inequality $k > \theta + 1 / \left( 4 \sigma \right)$ . The disturbance-free version of system (62) was studied in Example 4. The radially unbounded function

$$H (x) = y ^ {2} / 2 + z ^ {2} / 2 \tag {63}$$

satisfies for all $x \in \mathbb { R } ^ { 2 } , d \in \mathbb { R }$ the equation

$$\dot {H} (x, d) = \nabla H (x) f (x, d) = - \left(k + \sigma (\theta + z) ^ {2}\right) y ^ {2} + y d \leq - \frac {k}{2} y ^ {2} + \frac {1}{2 k} d ^ {2} \tag {64}$$

Inequality (64) implies that all assumptions of Theorem 4 hold for system (62). Therefore, system (62) is forward complete. The function

$$V (x) = y ^ {2} / 2 \tag {65}$$

satisfies for all $\boldsymbol { x } \in \mathbb { R } ^ { 2 } , d \in \mathbb { R }$ the inequality (using the fact that $z + k + \sigma { \big ( } \theta + z { \big ) } ^ { 2 } \geq k - \theta - 1 / ( 4 \sigma )$ for all $z \in \mathbb { R } )$ :
