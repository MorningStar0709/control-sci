Definition 4 $( ( \mathbf { r } , \mathbf { s } )$ -reachable). Given a graph $\begin{array} { r l } { \mathcal { G } ( t ) } & { { } = } \end{array}$ $( \nu , \mathcal { E } ( t ) )$ and a nonempty subset $s \subset \mathcal { V } , \mathcal { S }$ is $( \mathbf { r } , \mathbf { s } )$ - reachable at time t if there are at least $s \in \mathbb { Z } _ { \geq 0 }$ nodes in S with at least $r \in \mathbb { Z } _ { > 0 }$ neighbors outside of S at t; i.e., given $\mathcal { X } _ { S } ^ { r } ( t ) = \{ i \in S : \bar  | \mathcal { N } _ { i } ( t ) \setminus \mathcal { S } | \geq r \} , | \mathcal { X } _ { S } ^ { r } ( t ) | \geq s .$

Definition 5 ((r, s)-robust). A graph $\mathcal { G } ( t ) = ( \nu , \mathcal { E } ( t ) )$ is (r, s)-robust at time t if for every pair of nonempty, disjoint subsets $s _ { 1 } , s _ { 2 } \subset \nu$ , at least one of the following holds at t (recall $\mathcal { X } _ { S _ { k } } ^ { r } ( t ) = \{ i \in S _ { k } : | \mathcal { N } _ { i } ( t ) \setminus \mathcal { S } _ { k } | \geq r \} f o r \ k \in \{ 1 , 2 \} ) .$

• $\begin{array} { r } { | \mathcal { X } _ { S _ { 1 } } ^ { r } ( t ) | = | S _ { 1 } | ; } \end{array}$   
• $\begin{array} { r } { | \mathcal { X } _ { S _ { 2 } } ^ { r } ( t ) | = | S _ { 2 } | ; } \end{array}$   
• $| \mathcal { X } _ { S _ { 1 } } ^ { r ^ { - } } ( t ) | + | \mathcal { X } _ { S _ { 2 } } ^ { r } ( t ) | \geq s .$

A network being $( F + 1 , F + 1 )$ -robust at all times is a sufficient condition for its normal robots to achieve resilient consensus through the W-MSR algorithm under an F -total malicious attack [3, Corollary 1]. Note for a graph of n nodes, $F \ \leq \ \lfloor { \frac { n - 1 } { 2 } } \rfloor$ , which is a direct application of [3, Lemma 4]. Computing (r, s)-robustness of a network is challenging due to its combinatorial nature [8], making its integration into multi-robot systems difficult. We build on results from this notion of resilience to present an alternative sufficient condition based on the minimum degree of normal agents, offering a more computationally efficient approach.
