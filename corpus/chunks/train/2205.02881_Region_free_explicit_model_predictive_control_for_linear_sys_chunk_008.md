# III. WRITING THE OPTIMIZATION STEP AS A PARAMETRIC QUADRATIC PROGRAM (PQP)

Iterating the dynamics $x _ { k + 1 } ^ { \prime } = A x _ { k } ^ { \prime } + B u _ { k } ^ { \prime }$ of the prediction model and using $x _ { 0 } ^ { \prime } = x _ { n }$ , we get the solution formula

$$x _ {k} ^ {\prime} = A ^ {k} x _ {n} + \sum_ {j = 0} ^ {k - 1} A ^ {j} B u _ {k - 1 - j} ^ {\prime}, \quad k \geq 0,$$

and using this, we can derive bounded operators $\widetilde { A }$ and $\widetilde { B }$ with

$$\mathbf {x} ^ {\prime} := (x _ {k} ^ {\prime}) _ {k = 1} ^ {N} = \widetilde {A} x _ {n} + \widetilde {B} \mathbf {u} ^ {\prime}.$$

Note the difference in the indexing: in the vector $\mathbf { x } _ { \mathrm { ~ } } ^ { \prime }$ , the predicted states are at time steps $1 \ldots N$ and the predicted control moves in $\mathbf { u } ^ { \prime }$ are at time steps $0 \ldots N - 1$ .

With $\tilde { Q } _ { P } : = Q _ { 1 } \oplus \cdots \oplus Q _ { N - 1 } \oplus P \geq 0 , \tilde { R } : = R _ { 0 } \oplus \cdots \oplus$ $R _ { N - 1 } \geq 0 , \tilde { M } _ { 0 } : = \big [ M _ { 0 } \quad 0 \quad . . . \quad 0 \big ]$ ,

$$
\widetilde {M} := \left[ \begin{array}{c c c c c} 0 & M _ {1} & 0 & \ldots & 0 \\ 0 & 0 & M _ {2} & \ddots & 0 \\ \vdots & \vdots & \ddots & \ddots & \vdots \\ 0 & 0 & 0 & \ddots & M _ {N - 1} \\ 0 & 0 & 0 & \ldots & 0 \end{array} \right], \qquad \widetilde {V} _ {0} := \left[ \begin{array}{c} - V _ {0} \\ 0 \\ \vdots \\ 0 \end{array} \right],

\widetilde {V} := \left[ \begin{array}{c c c c c} V _ {0} + V _ {1} & - V _ {1} & 0 & \ldots & 0 \\ - V _ {1} & V _ {1} + V _ {2} & - V _ {2} & \ldots & 0 \\ \vdots & \ddots & \ddots & \ddots & \vdots \\ 0 & \ldots & \ddots & \ddots & - V _ {N - 1} \\ 0 & \ldots & 0 & - V _ {N - 1} & V _ {N - 1} + V _ {N} \end{array} \right],
$$

and denoting $\theta _ { n } : = { \left[ \begin{array} { l } { x _ { n } } \\ { u _ { n - 1 } } \end{array} \right] }$ ], the cost functional (3) can be written as
