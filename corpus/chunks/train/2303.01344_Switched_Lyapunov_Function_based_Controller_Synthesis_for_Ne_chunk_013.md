ved with $\begin{array} { r } { \hat { P } ^ { + } = \hat { P } ( \alpha _ { k + 1 } ) = \sum _ { i = 1 } ^ { \delta } \alpha _ { j , k } P _ { j } } \end{array}$ and $0 \leq \gamma < 1$ uniformly asymptotically stable if

$$
\begin{array}{l} - \Delta V (\boldsymbol {\xi} _ {k}, \boldsymbol {\alpha} _ {k}) = V (\boldsymbol {\xi} _ {k}, \boldsymbol {\alpha} _ {k}) - V (\boldsymbol {\xi} _ {k + 1}, \boldsymbol {\alpha} _ {k + 1}) = \\ = \hat {\boldsymbol {P}} - \left(\hat {\boldsymbol {A}} (\boldsymbol {\alpha} _ {k}) - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) ^ {T} \hat {\boldsymbol {P}} ^ {+} \left(\hat {\boldsymbol {A}} (\boldsymbol {\alpha} _ {k}) \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) > 0 \tag {16} \\ \end{array}
$$

holds. Due to the fact that $\hat { P }$ is positive definite, this condition is ensured when (15) is fulfilled. 

The impact of the scalar value $\gamma$ becomes clear from (15) and (16), since increasing its value results in the larger value of $| \Delta V ( \xi _ { k } , \alpha _ { k } ) |$ and therefore leading to the larger lower bound for the transient decay rate of the system states. Hence, the parameter $\gamma$ represents an additional variable which can be used to influence the transient performance and resulting settling time, see [4].

The total number of necessary LMI conditions in the form (13) which should be solved is $\bar { \delta ^ { 2 } }$ . This represent a significant result in reduction of the computational complexity in comparison to the approach presented in [4] and [5], where the number of LMI conditions to be solved is $2 ^ { 2 \bar { \delta } \nu }$ with $\nu \leq n$ depending on the geometric multiplicity of the eigenvalues, see [4].
