= 0. 5 \mathrm{tr} \left(\rho_ {k | k - 1} \rho_ {k | k - 1} ^ {T} \left[ \begin{array}{c c} P _ {k | k - 1} ^ {- 1} & 0 \\ 0 & R _ {k} ^ {- 1} \end{array} \right]\right)
= 0. 5 \operatorname{tr} \left(\left(\rho_ {k \mid k - 1} \rho_ {k \mid k - 1} ^ {T}\right) _ {x x} P _ {k \mid k - 1} ^ {- 1}\right) + 0. 5 \operatorname{tr} \left(\left(\rho_ {k \mid k - 1} \rho_ {k \mid k - 1} ^ {T}\right) _ {z z} R _ {k} ^ {- 1}\right). \tag {31}$$

Hence, according to (27) and (31), $P _ { k | k - 1 }$ and $R _ { k }$ are decoupled. Then, the approximate optimal solutions $q ( P _ { k | k - 1 } )$ and $q ( R _ { k } )$ can be calculated by (8), respectively. Furthermore, the posterior distributions are derived in the same form as the prior distribution to provide a recursive filter.

3) The update of $\phi _ { k } .$ Now, every component in $\Lambda _ { 0 }$ is computed. Let $\theta = \phi _ { k }$ , so that

$$\log q ^ {i + 1} (\phi_ {k})
\begin{array}{l} = - \frac {1}{2} (z _ {k} - H _ {k} \hat {x} _ {k | k - 1}) ^ {T} Y _ {k} (z _ {k} - H _ {k} \hat {x} _ {k | k - 1}) \\ - \frac {1}{2} (\phi_ {k} - \hat {\phi} _ {k | k - 1}) ^ {T} E ^ {i} \{\Phi_ {k | k - 1} ^ {- 1} \} (\phi_ {k} - \hat {\phi} _ {k | k - 1}) + c _ {x _ {k}, z _ {k}} \tag {32} \\ \end{array}
= - \frac {1}{2} (\phi_ {k} - \hat {\phi} _ {k | k - 1}) ^ {T} (\Theta_ {k} ^ {i + 1}) ^ {- 1} (\phi_ {k} - \hat {\phi} _ {k | k - 1}) + c _ {x _ {k}, z _ {k}},$$

where $c _ { x _ { k } , z _ { k } }$ is a constant independent of $x _ { k } , z _ { k }$ , and $\Theta _ { k } ^ { i + 1 }$ is given by

$$
\Theta_ {k} ^ {i + 1} = \left[ \begin{array}{c c} P _ {k \mid k} ^ {i + 1} & P _ {x z, k \mid k} ^ {i + 1} \\ P _ {z x, k \mid k} ^ {i + 1} & P _ {z z, k \mid k} ^ {i + 1} \end{array} \right] \tag {33}

= \left(E ^ {i} \{\Phi_ {k | k - 1} ^ {- 1} \} + \left[ \begin{array}{c c} 0 & 0 \\ 0 & Y _ {k} \end{array} \right]\right) ^ {- 1}.
$$

It follows from (25) that $E ^ { i } \big \{ \Phi _ { k | k - 1 } ^ { - 1 } \big \}$ in (33) can be computed by

$$
\begin{array}{l} E ^ {i} \{\Phi_ {k | k - 1} ^ {- 1} \} = \left[ \begin{array}{c c} I & - H ^ {T} \\ 0 & I \end{array} \right] \left[ \begin{array}{c c} E ^ {i} \{P _ {k | k - 1} ^ {- 1} \} & 0 \\ 0 & E ^ {i} \{R _ {k} ^ {- 1} \} \end{array} \right] \\ \times \left[ \begin{array}{c c} I & 0 \\ - H & I \end{array} \right]. \tag {34} \\ \end{array}
$$
