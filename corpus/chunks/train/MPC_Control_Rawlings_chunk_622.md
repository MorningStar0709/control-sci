$$e ^ {+} = A _ {K} e + \delta_ {x} \quad \delta_ {x} \in \mathbb {A} _ {x}$$

Because $\mathbb { \Delta } _ { x }$ is compact and $\rho ( A _ { K } ) < 1$ , there exists a robust positive invariant set $\mathbb { S }$ for $e ^ { + } = A _ { K } e + \delta _ { x } , \delta _ { x } \in \mathbb { \Delta } _ { x }$ satisfying

$$A _ {K} \mathbb {S} \oplus \mathbb {\Delta} _ {x} = \mathbb {S}$$

Hence $e ( i ) \in \mathbb { S }$ for all $i \in \mathbb { I } _ { \geq 0 }$ if $e ( 0 ) \in \mathbb { S }$ . So, as in Proposition 5.3, the states and controls of the estimator and nominal system satisfy ${ \hat { x } } ( i ) \in$ $\{ z ( i ) \} \oplus \mathbb { S }$ and $u ( i ) \in \{ \nu ( i ) \}$ ⊕ KS for all $i \in \mathbb { I } _ { \geq 0 }$ if the initial states x( ˆ 0) and $z ( 0 )$ satisfy ${ \hat { x } } ( 0 ) \in \{ z ( 0 ) \} \oplus \mathbb { S } .$ . Using the fact established previously that $\tilde { { \boldsymbol { x } } } ( i ) \in \mathbb { \Sigma } _ { \boldsymbol { x } }$ for all i, we can also conclude that $x ( i ) =$ $z ( i ) + e ( i ) + \widetilde { x } ( i ) \in \{ z ( i ) \}$ ⊕Γ and that $u \left( i \right) = \nu \left( i \right) + K e \left( i \right) \in \{ \nu \left( i \right) \} + K \mathbb { S }$ for all i where $\mathbb { T } : = \mathbb { S } \oplus \mathbb { \Sigma } _ { x }$ provided, of course, that $\phi ( 0 ) \in \{ \hat { \phi } ( 0 ) \} \oplus \oplus$ and $x ( 0 ) \in \{ \hat { x } ( 0 ) \} \oplus \mathbb { S }$ . These conditions are equivalent to $\tilde { \phi } ( 0 ) \in \Phi$ and $e ( 0 ) \in \mathbb { S }$ where, for all i, $e ( i ) : = \hat { x } ( i ) - z ( i )$ . Hence x(i) lies in X and u(i) lies in U if $\begin{array} { r } { z ( i ) \in \mathbb { Z } : = \mathbb { X } \ominus \mathbb { T } } \end{array}$ and $\nu \left( i \right) \in \mathbb { V } : = \mathbb { U } \ominus K \mathbb { S }$ .
