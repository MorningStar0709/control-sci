# 2.1 Infinite Horizon LQG-GMFGs

Define the following admissible control space,

$$\mathbb {A} := \{u: \Omega \times [ 0, T ] \mapsto \mathbb {R} \mid u \text { is } \mathbb {F} - \text { progressively }\text { measurable and } \mathbb {E} \int_ {0} ^ {\infty} e ^ {- \rho t} | u (t) | ^ {2} d t < \infty \},$$

and the Linear Quadratic Gaussian Graphon Mean Field Games (LQG-GMFGs) problem:

(1) (Mean Field Inputs) Fix a two-parameter deterministic global flow of mean fields $\{ \bar { z } _ { t } ^ { \alpha } , ~ t \in [ 0 , + \infty )$ , α ∈ $[ 0 , 1 ] \breve  \}$ .

(2) (Control Problems) Find optimal controls, denoted by $u ^ { \alpha , o } : = ( u _ { t } ^ { \alpha , o } ) _ { t \in [ 0 , T ] } \in \mathbb { A }$ , such that

$$J (u ^ {\alpha , o}, z ^ {\alpha}) = \min _ {u ^ {\alpha} \in \mathbb {A}} J (u ^ {\alpha}, z ^ {\alpha}) \tag {7}= \min _ {u ^ {\alpha} \in \mathbb {A}} \mathbb {E} \int_ {0} ^ {\infty} e ^ {- \rho t} \left[ r \left(u _ {t} ^ {\alpha}\right) ^ {2} + \left(x _ {t} ^ {\alpha} - z _ {t} ^ {\alpha}\right) ^ {2} \right] d t$$

where $\forall t \in [ 0 , + \infty ) , \forall \alpha \in [ 0 , 1 ]$

$$d x _ {t} ^ {\alpha} = b u _ {t} ^ {\alpha} d t + \sigma d w _ {t} ^ {\alpha}, x _ {0} ^ {\alpha} \sim \mathcal {N} (m ^ {\alpha}, \nu^ {2}). \tag {8}$$

(3) (Consistency Conditions) Show that the optimal state trajectories $\{ x _ { t } ^ { \alpha , o } , t \in [ 0 , \infty )$ , ∀α $\in [ 0 , 1 ] \}$ , satisfy the consistency conditions, for all $( \alpha , t ) \overset { \cdot } { \in } [ \overset { \cdot } { 0 , 1 } ] \times [ 0 , \infty )$ ;

$$z _ {t} ^ {\alpha} = \int_ {0} ^ {1} g (\alpha , \beta) \mathbb {E} [ x _ {t} ^ {\beta , o} ] d \beta . \tag {9}$$

The control problems can be solved via the following approach described in Huang et al. (2007). Consider the following algebraic Riccati equation:

$$\frac {b ^ {2}}{r} \pi^ {2} + \rho \pi = 1. \tag {10}$$

The Riccati equation has a unique positive solution

$$\pi = \sqrt {\frac {r ^ {2} \rho^ {2}}{4 b ^ {4}} + \frac {r}{b ^ {2}}} - \frac {\rho r}{2 b ^ {2}} > 0. \tag {11}$$

Consider $C _ { b } \left( [ 0 , \infty \right) )$ the set of bounded continuous functions over the domain [0, ∞). This space endowed with the supremum norm, $| x | _ { \infty } : = \operatorname* { s u p } _ { t \in [ 0 , \infty ) } | x ( t ) |$ is a Banach space. Consider $L ^ { 2 } \left( \left[ 0 , 1 \right] \right)$ the set of square integrable functions on the domain [0, 1] with inner product $\langle x , y \rangle =$ $\begin{array} { r } { \int _ { 0 } ^ { 1 } x ( \beta ) y ( \beta ) d \beta } \end{array}$ .
