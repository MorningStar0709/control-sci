Now, define the set-valued mapping $\Psi : { \mathcal { P } } ( X ) \to 2 ^ { \operatorname { I I } } \operatorname { a s } \Psi ( \mu ) = \{ \pi \in \Pi : \pi$ is optimal for $\mu$ and $\mu _ { 0 } =$ $\mu \}$ ; that is, given $\mu ,$ the set $\Psi ( \mu )$ is the set of optimal stationary policies for $\mu$ when the initial distribution is $\mu$ as well.

Conversely, we define another set-valued mapping $\Lambda : \Pi  2 ^ { \mathcal { P } ( X ) }$ as follows: given $\pi \in \Pi$ , the state-measure $\mu _ { \pi }$ is in $\Lambda ( \pi )$ if it is a fixed point of the equation

$$\mu_ {\pi} (\cdot) = \int_ {X \times A} p (\cdot | x, a, \mu_ {\pi}) \pi (d a | x) \mu_ {\pi} (d x).$$

In other words, $\mu _ { \pi }$ is the invariant distribution of the Markov transition probability $P ( \cdot | x ) =$ $\begin{array} { r } { \int _ { A } p ( \cdot | x , a , \mu _ { \pi } ) \pi ( d a | x ) } \end{array}$ .

The notion of equilibrium for mean-field games is defined via these mappings Ψ, Λ as follows.

Definition 1. A pair $( \pi _ { * } , \mu _ { * } ) \in \Pi \times \mathcal { P } ( X )$ is a stationary mean-field equilibrium if $\pi _ { * } \in \Psi ( \mu _ { * } )$ and $\mu _ { * } \in \Lambda ( \pi _ { * } )$ . In other words, π∗ is an optimal stationary policy given the state-measure $\mu _ { * }$ and $\mu _ { * }$ is the state distribution under the policy $\pi _ { * }$ .

In the literature, the existence of mean-field equilibria has been established for the discounted cost in [39].
