one can design ?? to render $( A _ { d } + B _ { d } K ) ^ { i }$ stable such that the norm of $| \lambda ( A _ { d } + B _ { d } k ) | < 1$ . To render $\boldsymbol { \mathcal { Z } } _ { f }$ safe invariant, the constraint (52) will impose that ${ \mathcal { H } } ( z { \dot { } } d ( k + 1 | N ) ) ~ -$ $\mathcal { H } ( z _ { d } ( k | | N ) ) \geq - \gamma \mathcal { H } ( z _ { d } ( k | N ) )$ and as a result with $0 < \gamma \leq$ 1. We have $\mathcal { H } ( z _ { d } ( k + 1 | N ) ) > \mathcal { H } ( z _ { d } ( k | N )$ . From (52), if $z _ { d } ( N | 0 ) \in \mathcal { Z } _ { f }$ such that $\mathcal { H } ( z _ { d } ( N | 0 ) \geq 0$ . One can design ?? such that $\mathcal { H } ( ( A _ { d } + B _ { d } K ) z _ { d } ) > ( 1 - \gamma ) \mathcal { H } ( z _ { d } )$ with $0 < \gamma \leq 1$ proving the safety invariance of $\boldsymbol { \mathcal { Z } } _ { f }$ .

Assumption 1 provides the initial feasibility conditions at $k ~ = ~ 0$ where Theorem 1 guarantees the feasibility for the next time steps. We can define the terminal constraint set as $Z _ { f } ( N _ { c } ) ~ = ~ \{ z _ { d } ~ : ~ \nu _ { \operatorname* { m i n } } ~ \leq ~ K ( A _ { d } + B _ { d } K ) ^ { i } z _ { d } ~ \leq ~ \nu _ { \operatorname* { m a x } } , i ~ =$ $0 , 1 , \dots N _ { c } \}$ . By choosing sufficiently large $N _ { c } .$ , the allowable operating region of the MPC law will be increased satisfying all the constraints. Note that the constraints in (53) and (54) are extra computational burden specially with large $N _ { c }$ which is needed to maintain the feasibility (visit Theorem 1). However, the constraints in (53) and (54) are linear constraints, where the QCQP presented in (48)-(54) can be solved efficiently by offthe-shelf solvers. Algorithm 1 summarizes the implementation steps of the proposed control scheme in Fig.2. Given the known place and shape of the obstacle and for every time step the starts by measuring the states and makes use of the coordinate transformation Φ(??) representing Lemma 3 to compute the equivalent z states.Then the QCQP problem mentioned in (48)-(54) will be solved to compute the optimal value of ?? that will be given the DFL controller to compute the control inputs that will result in an obstacle-free path. The QCQP can be solved by Interior Point OPTimizer (IPOPT) in MATLAB efficiently, and the computational complexity can be estimated by expected number of flops (floating point operations). The worst-case number of flops for the QCQP can be approximated by [39] as follows:

$$\# \text { flops } _ {\mathrm{IP}} = i _ {\mathrm{IP}} \left(2 / 3 (\mathrm{Nm}) ^ {3} + 2 (\mathrm{Nm}) ^ {2}\right) \tag {61}$$
