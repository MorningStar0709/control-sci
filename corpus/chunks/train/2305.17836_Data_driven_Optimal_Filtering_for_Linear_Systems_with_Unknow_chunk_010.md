$$\mathbb {E} \left[ | a ^ {\intercal} x (T) - a ^ {\intercal} \hat {x} _ {\mathcal {L}} (T) | ^ {2} \right] = J _ {T} ^ {L Q R} (a, \mathcal {U} _ {\mathcal {L} ^ {\dagger}} (T)), \tag {8}$$

where $\mathcal { U } _ { \mathcal { L } ^ { \dagger } } ( T ) = \mathcal { L } ^ { \dagger } ( a )$ . In particular, for a Kalman filter with constant gain $L ,$ the output prediction error (5)

$$\mathbb {E} \left[ \| y (T) - \hat {y} _ {L} (T) \| ^ {2} \right] = \sum_ {i = 1} ^ {m} J _ {T} ^ {L Q R} (H _ {i}, \mathcal {U} _ {L ^ {\intercal}} (T)) + \mathrm{tr} \left[ R \right], \tag {9}$$

where $\mathcal { U } _ { L ^ { \boldsymbol { \mathsf { T } } } } ( T ) = \{ L ^ { \boldsymbol { \mathsf { T } } } z ( 1 ) , L ^ { \boldsymbol { \mathsf { T } } } z ( 2 ) , \ldots , L ^ { \boldsymbol { \mathsf { T } } } z ( T ) \}$ , i.e., the feedback control policy with constant gain $L ^ { \mathsf { T } } ,$ , and $\bar { H } _ { i } ^ { \mathsf { T } } \in \mathbb { R } ^ { n }$ is the i-th row of the $m \times n$ matrix $H f o r i = 1 , \dots , m$ .

Remark 2. The duality is also true in the continuous-time setting where the estimation problem is related to a continuous-time LQR. Recent extensions to the nonlinear setting appears in [24] with a comprehensive study in [23]. This duality is distinct from the maximum likelihood approach which involves an optimal control problem over the original dynamics instead of the adjoint system [5].
