# 3.2.2 Statistical error

To bound the statistical error via the offset Rademacher complexities, we employ the covering number [1].

Definition 3.2. Let $( \mathcal { M } , d )$ be a metric space, and the set $\mathcal { F } \subset \mathcal { M }$ . A set $\mathcal { F } _ { \delta } \subset \mathcal { M }$ is called a δ-cover of F if for each $f \in { \mathcal { F } }$ , there exists an $f _ { \delta } \in \mathcal { F } _ { \delta }$ such that $d ( f , f _ { \delta } ) \leq \delta$ . Moreover, $N ( \delta , { \mathcal { F } } , d ) : = \operatorname* { i n f } \{ | { \mathcal { F } } _ { \delta } | :$ $\mathcal { F } _ { \delta }$ is a δ-cover of F} is called the δ-covering number of F.

We also need Hoeffding’s inequality [20].

Lemma 3.4. Let $X _ { 1 } , . . . , X _ { n }$ be independent random variables such that $a _ { i } \leq X _ { i } \leq b _ { i }$ almost surely. Let $S _ { n } = X _ { 1 } + \cdots + X _ { n }$ . Then for all $\begin{array} { r } { t > 0 , \mathbb { P } ( S _ { n } - \mathbb { E } [ S _ { n } ] \geq t ) \leq \exp ( - \frac { 2 t ^ { 2 } } { \sum _ { i = 1 } ^ { n } ( b _ { i } - a _ { i } ) ^ { 2 } } ) } \end{array}$ 2t2 .

Next, we bound the offset Rademacher complexity in terms of the covering number.

Theorem 3.4. Let $\kappa _ { d }$ and $\kappa _ { b }$ be the Lipschitz constants of $g _ { d } \in \mathcal { G } _ { d }$ and $g _ { b } \in \mathcal { G } _ { b }$ in y and p in the $W ^ { 2 , \infty } ( \Omega )$ and $L ^ { \infty } ( \partial \Omega )$ norms, respectively, i.e.,

$$\left| g _ {d} (y, p, X) - g _ {d} (\tilde {y}, \tilde {p}, X) \right| \leq \kappa_ {d} \left(\left\| y - \tilde {y} \right\| _ {W ^ {2, \infty} (\Omega)} + \left\| p - \tilde {p} \right\| _ {W ^ {2, \infty} (\Omega)}\right), \quad \forall (y, p), (\tilde {y}, \tilde {p}) \in \mathcal {Y} \times \mathcal {P}, X \in \Omega ,\left| g _ {b} (y, p, Y) - g _ {b} (\tilde {y}, \tilde {p}, Y) \right| \leq \kappa_ {b} \left(\left\| y - \tilde {y} \right\| _ {L ^ {\infty} (\partial \Omega)} + \left\| p - \tilde {p} \right\| _ {L ^ {\infty} (\partial \Omega)}\right), \quad \forall (y, p), (\tilde {y}, \tilde {p}) \in \mathcal {Y} \times \mathcal {P}, Y \in \partial \Omega .$$

Then for any $\delta > 0$ , the offset Rademacher complexities $\mathcal { R } _ { n } ^ { \mathrm { o f f } } ( \mathcal { G } _ { d } , \beta )$ and $\mathcal { R } _ { n } ^ { \mathrm { o f f } } ( { \mathcal { G } } _ { b } , \beta )$ are bounded by
