which provides an uncertain realization of the input matrix B using the collected data. This equation also depends on the matrix A, which is unknown. To alleviate this issue, as before, we consider the following uncertain input matrix

$$B - A \Upsilon_ {0} M = (Y _ {1} - \Upsilon_ {1} - \Omega_ {0}) M, \tag {26}$$

instead of the nominal matrix B. This reformulation enables the derivation of a data-driven closed-loop system as shown next, where the uncertain dynamics are defined purely in terms of measured data and decision variables G and M as

$$x _ {k + 1} = (A + B K - A \Upsilon_ {0} G) x _ {k} + (B - A \Upsilon_ {0} M) v _ {k} + \omega_ {k},x _ {k + 1} = (Y _ {1} - \Upsilon_ {1} - \Omega_ {0}) (G x _ {k} + M K v _ {k}) + \omega_ {k}. \tag {27}$$

The steady state covariance for the uncertain dynamics in (27)

$$\Sigma = \lim _ {k \to \infty} \mathbb {E} [ x _ {k + 1} x _ {k + 1} ^ {T} ] = \lim _ {k \to \infty} \mathbb {E} [ x _ {k} x _ {k} ^ {T} ], \tag {28}$$

can be computed as

$$
\begin{array}{l} \Sigma = \mathbb {E} [ ((Y _ {1} - \Upsilon_ {1} - \Omega_ {0}) (G x _ {k} + M K v _ {k}) + \omega_ {k}) (*) ^ {T} ] \\ \Sigma = Y _ {1} G \Sigma G ^ {\top} Y _ {1} ^ {\top} + T r (G \Sigma G ^ {\top}) (W + V) + W \\ + Y _ {1} M K V K ^ {\top} M ^ {\top} Y _ {1} ^ {\top} + T r (M K V K ^ {\top} M ^ {\top}) (V + W), \tag {29} \\ \end{array}
$$

where the expectation is taken over all random variables $x _ { k } , v _ { k }$ , and $\omega _ { k }$ and unknown noise matrices $\Upsilon _ { 1 } , \Omega _ { 0 } ,$ , which we treat their columns as random variables. Given this structure, our goal is to compute the optimal control gain K that minimizes the standard infinite-horizon quadratic cost (22) while ensuring MSS and robustness against measurement noise. The following theorem provides a data-driven formulation for the LQR problem under noisy state measurements, using a convex SDP program to determine the optimal feedback gain.
