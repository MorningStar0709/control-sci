$$
\begin{array}{l} \left| a ^ {*} x + b ^ {*} C _ {K} ^ {\theta} (x) - a ^ {*} y - b ^ {*} C _ {K} ^ {\theta} (y) \right| \\ = \left| \frac {b ^ {*}}{b} \left(D _ {L} - D _ {U}\right) + \left(a ^ {*} - \frac {a}{b} b ^ {*}\right) (x - y) \right| \\ \leq \frac {b ^ {*}}{b} \left| D _ {U} - D _ {L} \right| + \left| a ^ {*} - \frac {a}{b} b ^ {*} \right| d \\ \leq \frac {b ^ {*}}{b} | a - b K | d + \left| a ^ {*} - \frac {a}{b} b ^ {*} \right| d \\ \leq \frac {b ^ {*}}{b} | a ^ {*} - b ^ {*} K | d + \frac {b ^ {*}}{b} | a - a ^ {*} + (b ^ {*} - b) K | d + \left| a ^ {*} - \frac {a}{b} b ^ {*} \right| d \\ \leq | a ^ {*} - b ^ {*} K | d + \left| \frac {b ^ {*}}{b} - 1 \right| | a ^ {*} - b ^ {*} K | d + \frac {b ^ {*}}{b} | a - a ^ {*} + (b ^ {*} - b) K | d + \left| a ^ {*} - \frac {a}{b} b ^ {*} \right| d \\ \leq \left(\left| a ^ {*} - b ^ {*} K \right| + O _ {T} (\epsilon)\right) d. \tag {27} \\ \end{array}
$$

we us, that $| a ^ { * } - b ^ { * } K | \le a ^ { * } + b ^ { * } | K | \le \bar { a } + \bar { b } { \frac { \bar { a } + 1 } { b } } = O _ { T } ( 1 )$ that , and $\left| { \frac { b ^ { * } } { b } } \ - \right.$ $1 | \le \frac { \epsilon } { b } = O _ { T } ( \epsilon )$ $\begin{array} { r } { | a - a ^ { * } + ( b ^ { * } - b ) K | \le \epsilon ( 1 + | K | ) \le \epsilon ( 1 + \frac { \bar { a } + 1 } { b } ) = O _ { T } ( \epsilon ) } \end{array}$ $\begin{array} { r } { | a ^ { * } - \bar { \frac { a } { b } } b ^ { * } | \leq \epsilon + \frac { a } { b } \epsilon \leq \epsilon + \frac { \bar { a } } { b } \epsilon = O _ { T } ( \epsilon ) } \end{array}$ .

Case 3 Lemma 4:

$$
\begin{array}{l} \left| C _ {K} ^ {\theta} (y) - C _ {K} ^ {\theta} (x) \right| = \left| \frac {1}{b} \left(D _ {U} - D _ {L}\right) + \frac {a}{b} (x - y) \right| \\ = \frac {1}{b} | D _ {U} - D _ {L} | + \frac {a}{b} | x - y | \\ \leq \frac {1}{b} | a - b K | d + \frac {a}{b} d \\ \leq \frac {1}{\underline {{b}}} d + \frac {\bar {a}}{\underline {{b}}} d \\ = O _ {T} (d). \\ \end{array}
$$

Case 4: $\begin{array} { r } { \mathrm { I f ~ } \frac { D _ { L } } { a - b K } \leq x \leq \frac { D _ { U } } { a - b K } } \end{array}$ and $\begin{array} { r } { y \ge \frac { D _ { U } } { a - b K } } \end{array}$ . Note that by symmetry, this is equivalent to $\begin{array} { r } { \frac { D _ { L } } { a - b K } \leq y \leq \frac { D _ { U } } { a - b K } } \end{array}$ and $\begin{array} { r } { x \le \frac { D _ { L } } { a - b K } } \end{array}$ . We will first assume the former. For Lemma 6, this case is equivalent to Z.

Case 4 Lemma 6:

In this case, K K. Therefore, in this case we have $C _ { K } ^ { \theta } ( x ) = - K x$ and $\begin{array} { r } { C _ { K } ^ { \theta } ( y ) = \frac { D _ { U } - a y } { b } } \end{array}$ b . Furthermore, in this case $| y - x | \geq$ $\begin{array} { r } { | y - \frac { D _ { U } } { a - b K } | } \end{array}$
