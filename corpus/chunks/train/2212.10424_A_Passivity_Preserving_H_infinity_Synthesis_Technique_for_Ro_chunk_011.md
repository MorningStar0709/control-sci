Implicitly linearizing at some equilibrium, that is, $\ddot { z } _ { c } = 0 ,$ , $\dot { z } _ { c } = 0 , q = q ^ { * } , z _ { c } = z ^ { * } , F _ { c } = 0 , F _ { d } = 0$ , and using $\tilde { z } _ { c } \simeq z _ { c } - z ^ { * }$ to denote small displacement variables, we get the linearized operation space dynamics

$$\ddot {\tilde {z}} _ {c} = \Delta (q ^ {*}) ^ {- 1} \left(F _ {c} + J _ {c} (q ^ {*}) ^ {- T} J _ {d} (q ^ {*}) ^ {T} F _ {d}\right). \tag {6}$$

Using the state variable $x = [ z _ { c } - z ^ { * } ; \dot { z } ]$ and the output variable $\boldsymbol { y } = [ \hat { \boldsymbol { z } } _ { d } - W _ { 1 } h _ { d } ( \boldsymbol { q } ^ { * } ) , \hat { \boldsymbol { z } } _ { d d } ]$ from (5), (6) leads to the linearized state-space system

$$\dot {x} (t) = A x (t) + B _ {1} F _ {c} (t) + B _ {2} F _ {d} (t) \tag {7a}y (t) = C x (t) \tag {7b}F _ {c} (t) = - K x (t) \tag {7c}$$

with matrices

$$
A = \left[ \begin{array}{c c} 0 & I \\ 0 & 0 \end{array} \right], \qquad B _ {2} = \left[ \begin{array}{c} 0 \\ \Delta (q ^ {*}) ^ {- 1} J _ {c} (q ^ {*}) ^ {- T} J _ {d} (q ^ {*}) ^ {T} \end{array} \right]

B _ {1} = \left[ \begin{array}{c} 0 \\ \Delta (q ^ {*}) ^ {- 1} \end{array} \right], C = \left[ \begin{array}{c c} W _ {1} J _ {d} (q ^ {*}) J _ {c} (q ^ {*}) ^ {- 1} & 0 \\ 0 & W _ {2} J _ {d} (q ^ {*}) J _ {c} (q ^ {*}) ^ {- 1} \end{array} \right].
$$
