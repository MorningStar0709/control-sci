The expression of $\tau _ { m } ( \sigma )$ in (40) depends on the model via the terms $\| A \|$ and $\| A + B K \|$ . A lower bound of $\tau _ { m } ( \sigma )$ can be derived based on the available data in D. We use for this purpose data-based upper-bounds on }A} and $\parallel A +$ $B K \|$ , as the expression in (40) is monotonically decreasing in $\| A \|$ and $\| A + B K \|$ . We notice for this purpose that $A { + } B K = ( X _ { 1 } { - } D _ { 0 } ) G$ with G satisfying (5), while A satisfies ${ \bigl [ } B \ A { \bigr ] } = { \bigl ( } X _ { 1 } - D _ { 0 } { \bigr ) } { \bigl [ } { \begin{array} { l } { U _ { 0 } } \\ { X _ { 0 } } \end{array} } { \bigr ] } ^ { \dagger }$ where $M ^ { \dagger }$ is the right inverse of the 0matrix M . Partitioning same dimension as A, w $\begin{array} { r } { \dot { [ } _ { X _ { 0 } } ^ { U _ { 0 } } ] ^ { \dagger } = \left[ J _ { 0 } \ { V } _ { 0 } \right] } \end{array}$ $V _ { 0 }$ ing the. Then, $A = ( X _ { 1 } - D _ { 0 } ) V _ { 0 }$ $\| A + B K \| \leqslant \| X _ { 1 } G \| + \| \Delta \| \| G \|$ and $\| A \| \leqslant \| X _ { 1 } V _ { 0 } \| + \| \Delta \| \| V _ { 0 } \|$ , and the upper bounds are both computable from data alone. We then have the next result.

Lemma 5: Let K be any feedback matrix that makes $A +$ BK Hurwitz and

$$\tau_ {d} (\sigma) := \frac {1}{c _ {A}} \log \left(\frac {\sigma}{1 + \sigma} \frac {c _ {A}}{\max \{c _ {\Phi} , 1 \}} + 1\right) \tag {41}$$

with $c _ { A } : = \| X _ { 1 } V _ { 0 } \| + \| \Delta \| \| V _ { 0 } \|$ and $c _ { \Phi } = \| X _ { 1 } G \| + \| \Delta \| \| G \|$ . Given any triggering policy and any solution x to the corresponding closed-loop system (25), (26) with input disturbance d, for any $k \in \mathcal N$ and any $t \in [ t _ { k } , t _ { k } + \tau _ { d } ( \sigma ) ] \cap [ t _ { k } , t _ { k + 1 } ]$ , $| e ( t ) | \leqslant \sigma ( | x ( t ) | + \| d \| _ { [ 0 , t ] } )$ . l

Proof. The result follows from Lemma 4 and the fact that $\tau _ { d } ( \sigma ) \leqslant \tau _ { m } ( \sigma )$ . Specifically, for $A \neq 0$ the inequality $\tau _ { d } ( \sigma ) \leqslant$ $\tau _ { m } ( \sigma )$ follows since $\tau _ { m } ( \sigma )$ decreases monotonically as }A} and/or $\| A + B K \|$ increase, while for $A = 0$ the inequality $\tau _ { d } ( \sigma ) \leqslant \tau _ { m } ( \sigma )$ follows since $\log ( 1 + s ) \leqslant s$ for every $s \geqslant 0$ .

With this result in hands, we derive the next triggering policy: $t _ { 0 } = 0$ and

$$t _ {k + 1} = \inf \left\{t \in \mathbb {R}: t \geqslant t _ {k} + \tau_ {d} (\sigma) \text { and } z (t) ^ {\top} \Psi (\sigma) z (t) \geqslant 0 \right\} \tag {42}$$
