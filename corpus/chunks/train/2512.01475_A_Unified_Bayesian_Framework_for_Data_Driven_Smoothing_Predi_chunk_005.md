(T3) Optimal control. An initial input-output sequence of length $L _ { 0 } \ge l$ is available. We would like to design an control cost input-output sequence $\begin{array} { r } { \sum _ { t = L _ { 0 } + 1 } ^ { L } \left( \left. u _ { t } ^ { 0 } - u _ { t } ^ { \mathrm { r e f } } \right. _ { R } ^ { 2 } + \left. y _ { t } ^ { 0 } - y _ { t } ^ { \mathrm { r e f } } \right. _ { Q } ^ { 2 } \right) } \end{array}$ $( z _ { t } ^ { 0 } ) _ { t = L _ { 0 } + 1 } ^ { L }$ such that the quadratic is minimized, where $u _ { t } ^ { \mathrm { r e f } } \in \mathbb { R } ^ { n _ { u } } , y _ { t } ^ { \mathrm { r e f } } \in \mathbb { R } ^ { n _ { y } }$ are the input t  and output references and $R \in \mathbb { S } _ { + + } ^ { n _ { u } } , Q \in \mathbb { S } _ { + + } ^ { n _ { y } }$ are the input and output cost matrices, respectively. Let $\zeta _ { t } ^ { \mathrm { c t r } } : =$ col $\left( \boldsymbol { u } _ { t } ^ { \mathrm { r e f } } , \boldsymbol { y } _ { t } ^ { \mathrm { r e f } } \right) \dot { \in } \mathbb { R } ^ { n }$ . In this case, we have $p = n L$

$$\Phi = \mathbb {I} _ {n L}, \zeta = \operatorname{col} \left(z _ {1}, \dots , z _ {L _ {0}}, \zeta_ {L _ {0} + 1} ^ {\mathrm{ctr}}, \dots , \zeta_ {L} ^ {\mathrm{ctr}}\right),\boldsymbol {\epsilon} = \operatorname{col} \left(\nu_ {1}, \dots , \nu_ {L _ {0}}, \boldsymbol {\epsilon} ^ {\mathrm{ctr}}\right),$$

where $\epsilon ^ { \mathrm { c t r } }$ denotes the errors from the references.

Then, we are ready to state the main problem of this work. Problem 1. Given the offline data H and the online trajectory observation ζ, obtain an optimal estimate ˆz of $\mathbf { z } ^ { 0 }$ .

The offline data H will be incorporated into estimation as the model surrogate through a MAP framework, which will be detailed in Section 3.
