Proof. First, note that Statement 1 corresponds to the DARE in Lemma 1 with $( A , B _ { u } , C _ { e } , D _ { e u } , Q$ , $R , S )$ replaced by $( \hat { A } , \hat { B } , \hat { C } , \hat { D } , \hat { Q } , \hat { R } , \hat { S } )$ . Thus Statement 1 follows from assumptions (i)-(iv) and Lemma 1.

Next, note that Statement 2 corresponds to the DARE in Lemma 1 with $( A , B _ { u } , C _ { e } , D _ { e u } , Q , R , S )$ replaced by $( \hat { A } ^ { \top } , \hat { K } _ { x } ^ { \top } , 0 , \hat { H } ^ { - \frac { 1 } { 2 } } , 0 , \hat { H } ^ { - 1 } , 0 )$ . Moreover, we have: (a) $\hat { H } ^ { - 1 } \succ 0 , ( \mathbf { b } ) ( \hat { A } ^ { \top } , \hat { K } _ { x } ^ { \top } )$ is stabilizable because $( \hat { A } - \hat { B } \hat { K } _ { x } ) ^ { \top }$ is a Schur matrix, (c) $\hat { A }$ is nonsingular by assumption (vi), and (d) $\left[ \begin{array} { c c } { \hat { A } ^ { \top } - e ^ { j \theta } I } & { \hat { K } _ { x } ^ { \top } } \\ { 0 } & { \hat { H } ^ { - \frac { 1 } { 2 } } } \end{array} \right]$ has full column rank for all $\theta \in [ 0 , 2 \pi ]$ by assumption (v) and $\hat { H } ^ { - \frac { 1 } { 2 } } \succ 0$ . Thus Statement 2 follows from assumptions (i)-(vi) and Lemma 1.

For statement 3, we first show that $\hat { P } ^ { \sim } \hat { P } = F ^ { \sim } F$ . Use the output equation of $\hat { P }$ to express the cost as:

$$
\hat {J} = \langle \hat {e}, \hat {e} \rangle = \sum_ {t = - \infty} ^ {\infty} \left[ \begin{array}{c} \hat {x} _ {t} \\ d _ {t} \end{array} \right] ^ {\top} \left[ \begin{array}{c c} \hat {Q} & \hat {S} \\ \hat {S} ^ {\top} & \hat {R} \end{array} \right] \left[ \begin{array}{c} \hat {x} _ {t} \\ d _ {t} \end{array} \right].
$$

Substitute for $\hat { Q }$ using the DARE (37) to show:

$$
\left[ \begin{array}{c c} \hat {Q} & \hat {S} \\ \hat {S} ^ {\top} & \hat {R} \end{array} \right] = \left[ \begin{array}{c} \hat {K} _ {x} ^ {\top} \\ I \end{array} \right] \hat {H} \left[ \begin{array}{c c} \hat {K} _ {x} & I \end{array} \right] + \left[ \begin{array}{c c} \hat {X} & 0 \\ 0 & 0 \end{array} \right] - \left[ \begin{array}{c} \hat {A} ^ {\top} \\ \hat {B} ^ {\top} \end{array} \right] \hat {X} \left[ \begin{array}{c c} \hat {A} & \hat {B} \end{array} \right].
$$

Thus the cost is equal to:
