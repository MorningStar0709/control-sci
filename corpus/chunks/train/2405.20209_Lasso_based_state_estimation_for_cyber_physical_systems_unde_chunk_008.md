Theorem 1. Let us assume that $( \mathcal { O } \quad I _ { \mathcal { S } } ) \in \mathbb { R } ^ { p \tau , n + h }$ is full rank. Lasso is successful, i.e., by solving it we identify the attack support, if and only $i f$

$$\left\| \mathcal {O} _ {\bar {\mathcal {S}}} ^ {\dagger \top} \mathcal {O} _ {\mathcal {S}} ^ {\top} s i g n (\widetilde {a} _ {\mathcal {S}}) \right\| _ {\infty} < 1 \tag {5}$$

provided that $\lambda > 0$ is sufficiently small. As a consequence, Lasso is successful if

$$\left\| \mathcal {O} _ {\bar {\mathcal {S}}} ^ {\dagger \top} \mathcal {O} _ {\mathcal {S}} ^ {\top} \right\| _ {\infty} < 1. \tag {6}$$

Proof. $( x ^ { \star } , a ^ { \star } )$ is a global minimum of (4) if and only if it fulfills the zerosubgradient condition: there exists $\zeta \in \partial \| a ^ { \star } \| _ { 1 }$ such that

$$\binom {\mathcal {O} ^ {\top}} {I} \left(\mathcal {O} x ^ {\star} + a ^ {\star} - y\right) + \lambda \binom {0} {\zeta} = 0 \tag {7}$$

where $\partial \| a ^ { \star } \| _ { 1 } \subset \mathbb { R } ^ { p \tau }$ is the subdifferential of $\| a ^ { \star } \| _ { 1 }$ : for each $i \in \mathcal { S } , \zeta _ { i } =$ $\mathrm { s i g n } ( a _ { i } ^ { \star } )$ , while $\| \zeta _ { \bar { S } } \| _ { \infty } \leq 1 $ ; see [7] for details. In particular, if $\| \zeta _ { \bar { S } } \| _ { \infty } < 1$ , then $( x ^ { \star } , a ^ { \star } )$ is a strict minimum.

We construct the candidate $( x ^ { \star } , a ^ { \star } ) \in \mathbb { R } ^ { n + p \tau }$ as follows: we set $a _ { \bar { \mathcal { S } } } ^ { \star } = 0$ while

$$\left(x ^ {\star}, a _ {\mathcal {S}} ^ {\star}\right) = \underset {x \in \mathbb {R} ^ {n}, a _ {\mathcal {S}} \in \mathbb {R} ^ {h}} {\operatorname{argmin}} \frac {1}{2} \| y - \mathcal {O} x - I _ {\mathcal {S}} a _ {\mathcal {S}} \| _ {2} ^ {2} + \lambda \| a _ {\mathcal {S}} \| _ {1} \tag {8}$$

which is a Lasso restricted on the non-zero components of the true vector. The so-built $( x ^ { \star } , a ^ { \star } )$ is a solution of Lasso (4) if it satisfies (7); let us verify under which conditions this occurs.

By distinguishing the zero-subgradient equations on $( x ^ { \star } , a _ { S } ^ { \star } )$ and on $a _ { \bar { \mathcal { S } } } ^ { \star }$ and by recalling that $y = \mathcal { O } \widetilde { x } + I _ { S } \widetilde { a } _ { S }$ , we have
