Note that the same bound for the entire graph (including both normal and malicious agents) appears in [7, Corollary 1], but in the context of continuous-time consensus dynamics and a different algorithm. Based on Proposition 1, to guarantee resilient consensus under F -total malicious attacks, it suffices that robots navigate the environment such that normal robots have at least $\begin{array} { r } { F ^ { \prime } = F + \lfloor \frac { n } { 2 } \rfloor } \end{array}$ neighbors $\forall t \geq t _ { 0 }$ . Since the numbers of total robots n and malicious robots F are fixed, each robot $i \in \mathcal V$ can compute $F ^ { \prime }$ locally. Now we design a control law for the physical states of robots so that normal robots always maintain $F ^ { \prime }$ neighbors to achieve resilient consensus without collisions.
