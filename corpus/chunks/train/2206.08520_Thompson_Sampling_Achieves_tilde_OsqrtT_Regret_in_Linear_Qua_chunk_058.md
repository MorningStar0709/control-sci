# D.2. Proof of Lemma 8

The following lemma will be used as the backbone for Lemma $8 .$ .

Lemma 12 Let $V _ { 1 } , V _ { 2 } \in \mathbb { R } ^ { n \times n }$ be symmetric positive semi-definite matrices. Define two ellipsoids as

$$\mathcal {E} _ {1} := \left\{\Theta \in \mathbb {R} ^ {n \times m} \mid \operatorname{tr} \left(\Theta^ {\intercal} V _ {1} \Theta\right) \leq 1 \right\} \quad a n d \quad \mathcal {E} _ {2} := \left\{\Theta \in \mathbb {R} ^ {n \times m} \mid \operatorname{tr} \left(\Theta^ {\intercal} V _ {2} \Theta\right) \leq 1 \right\} \tag {45}$$

Then, ${ \mathcal { E } } _ { 1 } \subseteq { \mathcal { E } } _ { 2 }$ if and only $i f V _ { 1 } \succcurlyeq V _ { 2 } .$ .

Proof For the forward direction, assume $V _ { 1 } - V _ { 2 }$ has a negative eigenvalue, i.e., there exist $\lambda < 0$ and a unit vector $\theta \in \mathbb { R } ^ { n } / \{ 0 \}$ such that $( V _ { 1 } - V _ { 2 } ) \theta = \lambda \theta$ . Construct $\Theta = [ \theta , \theta , \dots , \theta ] \in \mathbb { R } ^ { n \times m }$ . Observe that tr $( \Theta ^ { \mathsf { T } } V _ { 1 } \Theta ) \ = \ m \theta ^ { \mathsf { T } } V _ { 1 } \theta$ and tr $( \Theta ^ { \mathsf { T } } V _ { 2 } \Theta ) \ : = \ : m \theta ^ { \mathsf { T } } V _ { 2 } \theta$ . Therefore, we have the relationship $\operatorname { t r } \left( \Theta ^ { \intercal } V _ { 2 } \Theta \right) = \operatorname { t r } \left( \Theta ^ { \intercal } V _ { 1 } \Theta \right) - m \lambda$ .

If $V _ { 1 } \theta = 0$ , then $\mathrm { t r } ( \Theta ^ { \intercal } V _ { 1 } \Theta ) = 0 \leq 1$ and therefore for any scalar $\alpha > 0 , \alpha \Theta \in \mathcal { E } _ { 1 }$ . On the other hand, tr $( \Theta ^ { \mathsf { T } } V _ { 2 } \Theta ) = - m \lambda > 0$ and therefore, one can find a scalar $\alpha > 0$ such that tr ((αΘ)⊺V2(αΘ)) = −mλα2 > 1, i.e.αΘ ∈ E / 2. If V1θ 6= 0, then define Θ′ = √ 1mθ⊺V θ Θ $\mathrm { t r } \left( ( \alpha \Theta ) ^ { \intercal } V _ { 2 } ( \alpha \Theta ) \right) = - m \lambda \alpha ^ { 2 } > 1$ $V _ { 1 } \theta \neq 0$ $\begin{array} { r } { \Theta ^ { \prime } = \frac { 1 } { \sqrt { m \theta ^ { \intercal } V _ { 1 } \theta } } \Theta } \end{array}$ and observe that tr $( \Theta ^ { \prime \top } V _ { 1 } \Theta ^ { \prime } ) = 1 , i . e . , \Theta ^ { \prime } \in \mathcal { E } _ { 1 }$ . On the other hand, tr $\begin{array} { r } { ( \Theta ^ { \prime \top } V _ { 2 } \Theta ^ { \prime } ) = 1 - \frac { \lambda } { \theta ^ { \top } V _ { 1 } \theta } > 1 } \end{array}$ , $i . e . , \Theta ^ { \prime } \notin \mathcal { E } _ { 2 }$ . Therefore, we have that if ${ \mathcal { E } } _ { 1 } \subseteq { \mathcal { E } } _ { 2 }$ then $V _ { 1 } \succcurlyeq V _ { 2 }$ .
