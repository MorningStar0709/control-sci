where $v : = [ v _ { 1 } ^ { \top } , \ldots , v _ { N } ^ { \top } ] \in \mathbb { R } ^ { 6 N } , M : = M ( q ) : = \mathrm { d i a g } \{ [ M _ { i } ] _ { i \in \mathcal { N } } \} \in \mathbb { R } ^ { 6 N \times 6 N }$ , $C : = C ( q , \dot { q } ) : = \operatorname { d i a g } \{ [ \ddot { C } _ { i } ] _ { i \in N } \} \in \mathbb { R } ^ { 6 N \times 6 N } , h : = [ h _ { 1 } ^ { \top } , \dot { \mathrm { ~ , ~ } } \dot { \mathrm { ~ . ~ . ~ . ~ } } , h _ { N } ^ { \top } ] ^ { \top } , u : = [ u _ { 1 } ^ { \top } , \dot { \mathrm { ~ , ~ . ~ . ~ } } \dot { \mathrm { ~ . ~ } } \dot { \mathrm { ~ ~ } } \dot { \mathrm { ~ ~ } } \dot { \mathrm { ~ ~ } } \dot { \mathrm { ~ ~ } } \dot { \mathrm { ~ ~ } } \dot { \mathrm { ~ ~ } } \dot { \mathrm { ~ ~ } } H ^ { \top } ] ^ { \top } .$ $\begin{array} { r } { u _ { N } ^ { \top } ] ^ { \top } , g : = g ( q ) : = [ g _ { 1 } ^ { \top } , \dotsc , g _ { N } ^ { \top } ] ^ { \top } , d : = d ( q , \dot { q } , t ) : = [ d _ { 1 } ^ { \top } , \dotsc , d _ { N } ^ { \top } ] ^ { \top } \in \dot { \mathbb { R } } ^ { 6 N } } \end{array}$ .

Regarding the object, we denote by $\boldsymbol { x } _ { o } : = [ \boldsymbol { p } _ { o } ^ { \intercal } , \boldsymbol { \eta } _ { o } ^ { \intercal } ] ^ { \intercal } \in \mathbb { M } : = \mathbb { R } ^ { 3 } \times \mathbb { T } ,$ $v _ { O } : = \bar { [ \dot { p } _ { o } ^ { \top } , \omega _ { o } ^ { \top } ] ^ { \top } } \in \mathbb { R } ^ { 6 }$ the pose and generalized velocity of its center of mass; $\eta _ { o }$ here denotes explicitly Euler angles $\eta _ { o } : = [ \phi _ { o } , \theta _ { o } , \psi _ { o } ] ^ { \intercal } \in \mathbb { T } = \mathbb { R } ^ { 3 }$ . We consider the following second order dynamics, which can be derived based on the Newton-Euler formulation:

$$\dot {x} _ {O} = J _ {O} v _ {O}, \tag {2.5a}M _ {o} \dot {v} _ {o} + C _ {o} v _ {o} + g _ {o} + d _ {o} = h _ {o}, \tag {2.5b}$$

where $M _ { o } : = M _ { o } ( \eta _ { o } ) : \mathbb { T } \to \mathbb { R } ^ { 6 \times 6 }$ is the positive definite inertia matrix, $C _ { o } : = C _ { o } ( \eta _ { o } , \omega _ { o } ) : \mathbb { T } \times \mathbb { R } ^ { 6 } \to \mathbb { R } ^ { 6 \times 6 }$ is the Coriolis matrix, $g _ { O } \in \mathbb { R } ^ { 6 }$ is the gravity vector, $d _ { o } : = d _ { o } ( x _ { o } , \dot { x } _ { o } , t ) : \mathbb { M } \times \mathbb { R } ^ { 6 } \times \mathbb { R } _ { > 0 }  \mathbb { R } ^ { 6 }$ a bounded vector representing modeling uncertainties and external disturbances, and $h _ { O } \in \mathbb { R } ^ { 6 }$ is the vector of generalized forces acting on the object’s center of mass.
