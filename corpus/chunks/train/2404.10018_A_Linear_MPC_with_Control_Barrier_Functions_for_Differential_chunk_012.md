where $l _ { k } ( x _ { k } , u _ { k } )$ denotes a cost function to be minimized such that $l _ { k } ( x _ { k } , u _ { k } ) = x _ { k } ^ { \top } Q x _ { k } + u _ { k } ^ { \top } R u _ { k } ^ { \top }$ with $Q \ = \ d i a g ( q _ { 1 } , q _ { 2 } , q _ { 3 } )$ and $R = d i a g ( r _ { 1 } , r _ { 2 } )$ are positive definite symmetric weight matrices and $V _ { N } ( x _ { N } )$ denotes a terminal cost function such that $V _ { N } ( x _ { N } ) = x _ { N } ^ { \top } P x _ { N }$ with $P = d i a g ( p _ { 1 } , p _ { 2 } , p _ { 3 } )$ is a positive definite symmetric weight matrix. The nonlinear dynamics, state, and control input constraints, and terminal constraints are described in (11)-(15). Given the inherent computational demands associated with nonlinear (MPC), we show in the subsequent section how to substitute the nonlinear constraints in (11) with a cascaded scheme with a linear equivalence model.
