$$
\begin{array}{l} \left\| \frac {\partial}{\partial \theta} V ^ {\pi_ {\theta}} \right\| = \frac {1}{1 - \gamma} \sqrt {\sum_ {m = 1} ^ {M} (\pi (m)) ^ {2} \left(\mathfrak {r} (m) - \pi^ {\mathrm{T}} \mathfrak {r}\right) ^ {2}} \\ \geqslant \frac {1}{1 - \gamma} \sqrt {\left(\pi (m ^ {*})\right) ^ {2} \left(\mathfrak {r} (m ^ {*}) - \pi^ {\mathrm{T}} \mathfrak {r}\right) ^ {2}} \\ = \frac {1}{1 - \gamma} (\pi (m ^ {*})) \left(\mathfrak {r} (m ^ {*}) - \pi^ {\mathrm{T}} \mathfrak {r}\right) \\ = \frac {1}{1 - \gamma} (\pi (m ^ {*})) (\pi^ {*} - \pi) ^ {\mathrm{T}} \mathfrak {r} \\ = \left(\pi (m ^ {*})\right) \left[ V ^ {\pi^ {*}} - V ^ {\pi_ {\theta}} \right]. \\ \end{array}
$$

where $\pi ^ { * } = e _ { m ^ { * } }$

![](image/a1b7dd2b611d72dcc1a46d74d28878eff6b3b372345aaf17fc60e182f0c79b9c.jpg)

We will now prove Theorem H.4 and corollary H.2. We restate the result here.

em H.4. With , $\begin{array} { r } { \eta = \frac { 2 ( 1 - \gamma ) } { 5 } } \end{array}$ = 2(1−γ)5 and with θ(1)m = 1/M for all m ∈ [M ], with the availability for true gradient, we have $\theta _ { m } ^ { ( 1 ) } = 1 / M$ $m \in [ M ] ,$ 5 $\forall t \geqslant 1$

$$V ^ {\pi^ {*}} - V ^ {\pi_ {\theta_ {t}}} \leqslant \frac {5}{1 - \gamma} \frac {M ^ {2}}{t}.$$

Proof. First, note that since $V ^ { \pi }$ is smooth we have:

$$
\begin{array}{l} V ^ {\pi_ {\theta_ {t}}} - V ^ {\pi_ {\theta_ {t + 1}}} \leqslant - \left\langle \frac {d}{d \theta_ {t}} V ^ {\pi_ {\theta_ {t}}}, \theta_ {t + 1} - \theta_ {t} \right\rangle + \frac {5}{2 (1 - \gamma)} \| \theta_ {t + 1} - \theta_ {t} \| _ {2} ^ {2} \\ = - \eta \left\| \frac {d}{d \theta_ {t}} V ^ {\pi_ {\theta_ {t}}} \right\| _ {2} ^ {2} + \frac {5}{4 (1 - \gamma)} \eta^ {2} \left\| \frac {d}{d \theta_ {t}} V ^ {\pi_ {\theta_ {t}}} \right\| _ {2} ^ {2} \\ = \left\| \frac {d}{d \theta_ {t}} V ^ {\pi_ {\theta_ {t}}} \right\| _ {2} ^ {2} \left(\frac {5 \eta^ {2}}{4 (1 - \gamma)} - \eta\right) \\ = - \left(\frac {1 - \gamma}{5}\right) \left\| \frac {d}{d \theta_ {t}} V ^ {\pi_ {\theta_ {t}}} \right\| _ {2} ^ {2}. \\ \leqslant - \left(\frac {1 - \gamma}{5}\right) (\pi_ {\theta_ {t}} (m ^ {*})) ^ {2} \left[ V ^ {\pi^ {*}} - V ^ {\pi_ {\theta}} \right] ^ {2} \quad \text { Lemma   H.3 } \\ \leqslant - \left(\frac {1 - \gamma}{5}\right) (\underbrace {\inf _ {1 \leqslant s \leqslant t} \pi_ {\theta_ {t}} (m ^ {*})} _ {=: c _ {t}}) ^ {2} \left[ V ^ {\pi^ {*}} - V ^ {\pi_ {\theta}} \right] ^ {2}. \\ \end{array}
$$
