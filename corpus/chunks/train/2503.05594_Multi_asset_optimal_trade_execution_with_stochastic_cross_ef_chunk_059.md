$$
\begin{array}{l} \lim _ {N \to \infty} E \bigg [ \int_ {0} ^ {T} \bigg (\sum_ {i = 1} ^ {n} | (Y ^ {N} (s)) _ {i} | \bigg) \bigg (\sum_ {j = 1} ^ {n} | (\widetilde {Y} ^ {N} (s)) _ {j} - (\widetilde {Y} (s)) _ {j} | \bigg) d s \bigg ] \\ \leq \lim _ {N \to \infty} n \bigg (E \bigg [ \int_ {0} ^ {T} \| Y ^ {N} (s) \| _ {F} ^ {2} d s \bigg ] \bigg) ^ {\frac {1}{2}} \bigg (E \bigg [ \int_ {0} ^ {T} \| \widetilde {Y} ^ {N} (s) - \widetilde {Y} (s) \| _ {F} ^ {2} d s \bigg ] \bigg) ^ {\frac {1}{2}} = 0, \\ \end{array}
$$

where we have used the assumption lim $\begin{array} { r } { { \bf \Phi } _ { N  \infty } E [ \int _ { 0 } ^ { T } \lVert \widetilde { Y } ( s ) - \widetilde { Y } ^ { N } ( s ) \rVert _ { F } ^ { 2 } d s ] = 0 } \end{array}$ and that $\begin{array} { r } { \operatorname* { s u p } _ { N \in \mathbb { N } } E [ \int _ { 0 } ^ { T } \lVert Y ^ { N } ( s ) \rVert _ { F } ^ { 2 } d s ] < \infty } \end{array}$ due to lim $\begin{array} { r } { { \bf \Phi } _ { N  \infty } E [ \int _ { 0 } ^ { T } \lVert Y ( s ) - Y ^ { N } ( s ) \rVert _ { F } ^ { 2 } d s ] = 0 } \end{array}$ . The second term arising from (74) can be treated similarly. Since A is $d P \times d s { \mathrm { - a . e } }$ . bounded, we thus obtain (73).

(ii) This can be proven similar to part (i).

The following lemma is a variant of [4, Lemma 6.4] and thus relies on Lemma 2.7 in Section 3.2 of Karatzas & Shreve [40]. In the form of Corollary 5.8 below it is used to construct an approximating sequence of finite-variation strategies in the proof of Theorem 3.9.

Lemma 5.7. Let $L = ( L ( s ) ) _ { s \in [ 0 , T ] }$ be the $\mathbb { R } ^ { n \times n }$ -valued process defined $b y$

$$L (r) = \int_ {0} ^ {r} \frac {1}{8} \sum_ {k = 1} ^ {m} \boldsymbol {\sigma} _ {k} (s) \boldsymbol {\sigma} _ {k} (s) d s - \sum_ {k = 1} ^ {m} \int_ {0} ^ {r} \frac {1}{2} \boldsymbol {\sigma} _ {k} (s) d W _ {k} (s), \quad r \in [ 0, T ], \tag {75}$$

and let $K = ( K ( s ) ) _ { s \in [ 0 , T ] }$ be the unique solution of the SDE

$$d K (s) = (d L (s)) K (s), \quad s \in [ 0, T ], \quad K (0) = I _ {n}. \tag {76}$$

Let $u = ( u ( s ) ) _ { s \in [ 0 , T ] } \in \mathcal { L } ^ { 2 }$ . Then there exists a sequence of $\mathbb { R } ^ { n }$ -valued bounded càdlàg finite-variation processes $( v ^ { N } ) _ { N \in \mathbb { N } }$ such that

$$\lim _ {N \to \infty} E \bigg [ \int_ {0} ^ {T} \| u (s) - K (s) v ^ {N} (s) \| _ {F} ^ {2} d s \bigg ] = 0.$$

Moreover, it holds for all $N \in  { \mathbb { N } }$ that $K v ^ { N }$ is an $\mathbb { R } ^ { n }$ -valued càdlàg semimartingale and $\begin{array} { r } { E [ \operatorname* { s u p } _ { s \in [ 0 , T ] } \| K ( s ) v ^ { N } ( s ) \| _ { F } ^ { 2 } ] < \infty . } \end{array}$ .
