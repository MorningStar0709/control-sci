provided that $G _ { \mathbf { d } } \in \Xi$ . However, the set $\mathcal { P }$ is not a singleton in the general case and the problem of identification of the generator $G _ { \mathbf { d } }$ is ill-posed even if $\Xi = \Xi _ { \mathrm { l i n } }$ , where

$$\Xi_ {\mathrm{lin}} = \{G \in \mathbb {R} ^ {n \times n}: G \text { is anti - Hurwitz } \}. \tag {32}$$

Indeed, for example, for any $\gamma > 0 ,$ , the function $h : \mathbb { R } ^ { 2 } \mapsto$ R given by

$$h (x) = x ^ {\top} x, \quad x = (x _ {1}, x _ {2}) ^ {\top} \in \mathbb {R} ^ {2} \tag {33}$$

is ${ \bf d } _ { \gamma }$ -homogeneous of degree 2 with respect to the linear dilation $\mathbf { d } _ { \gamma } ( s ) \ =$ $e ^ { s } \left( \binom { \cos ( \gamma s ) } { \sin ( \gamma s ) } - \sin ( \gamma s ) \right) , s \in \mathbb { R }$ . So, the linear dilation symmetry of the function h given above cannot be uniquely identified.

To reduce the uncertainty in the dilation identification, the cost functional can be modified as follow

$$G _ {\epsilon} = \operatorname{argmin} _ {G \in \Xi} J _ {\epsilon} (G) + \xi \operatorname{trace} (G ^ {\top} G), \tag {34}$$

where $\Xi = \mathbb { R } ^ { n \times n }$ and $\xi > 0$ is a small regularization parameter. The second term in the above formula is a strictly convex function (being a norm) in the space of square matrices. The quadratic optimization problem becomes strictly convex with respect to G, so, for any fixed $\epsilon > 0 .$ , the matrix $G _ { \epsilon }$ is uniquely defined by (34). Another possible way to reduce the uncertainty is to restrict the set of admissible generators $\Xi ,$ for example, to diagonalizable (or simply diagonal) matrices. However, this introduces additional a-priori assumption about the function.

The functional $J _ { \epsilon }$ admits the following approximation

$$J _ {\epsilon} (G) \approx \frac {1}{M L} \sum_ {k = 1} ^ {M} \sum_ {j = 1} ^ {L} \mathcal {J} _ {\epsilon} ^ {2} (x _ {k} + z _ {j}, x _ {i}, G) d z \tag {35}$$

where the points $x _ { k } \in \Omega _ { \delta }$ are uniformly distributed in $\Omega _ { \delta } , z _ { j } \in B _ { \delta }$ are uniformly distributed in $B _ { \delta }$ and $M , L \in$ N. Finally, denoting $f ( x , x ^ { * } , y , G ) = ( \operatorname { t r a c e } ( G ) +$ $\nu ) g _ { \epsilon } ( y ) - \sum _ { i : x _ { i } \neq x _ { i } ^ { * } } \Delta _ { i } ( g _ { \epsilon } , y , x , x ^ { * } , G )$ we derive the following approximation of the i:xi̸=x∗i functional $\mathcal { I } _ { \epsilon } ^ { 2 }$ for a sufficiently small $\delta \in ( 0 , 1 )$ :
