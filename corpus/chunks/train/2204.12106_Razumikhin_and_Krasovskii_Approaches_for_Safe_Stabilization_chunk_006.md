# 2. Problem Formulation

We start with the notation throughout this paper.
Let $\mathbb { R } : =$ $( - \infty , + \infty ) ;
\mathbb { R } ^ { + } : = [ 0 , + \infty ) ;
\mathbb { N } : = \{ 0 , 1 , .
.
.
\}$ and $\mathbb { N } ^ { + } : = \{ 1 , 2 , \ldots \}$ .
Given $a , b \in \mathbb { R } ^ { n } , ( a , b ) : = [ a ^ { \top } , b ^ { \top } ] ^ { \top } .
\mid \cdot \mid$ denotes the Euclidean norm of a real vector, or the induced Euclidean norm of a matrix.
Given $\delta > 0$ and $y \in \mathbb { R } ^ { n }$ , an open ball centered at y with radius of δ is defined as $\mathbf { B } ( y , \delta ) : = \{ x \in \mathbb { R } ^ { n } : | x - y | < \delta \}$ ;
and $\mathbf { B } ( \delta ) : = \mathbf { B } ( 0 , \delta )$ .
Given A, $B \subset \mathbb { R } ^ { n }$ and $\delta > 0 , A - \mathbf { B } ( \delta ) : = \{ x \in$ $A : \mathbf { B } ( x , \delta ) \subset A \}$ } and $B \backslash A : = \{ x \in \mathbb { R } ^ { n } : x \in B , x \not \in A \} .
C ( \mathbb { R } ^ { n } , \mathbb { R } ^ { p } )$ denotes the set of all continuously differentiable functions mapping $\mathbb { R } ^ { n } : \mathbb { R } ^ { p }$ , and $C ( [ a , b ] , \mathbb { R } ^ { n } )$ denotes the set of continuous functions mapping $[ a , b ]$ to Rn, where $a \leq b .
\alpha \circ \beta ( \nu ) : = \alpha ( \beta ( \nu ) )$ for any $\alpha , \beta \in C ( \mathbb { R } ^ { n } , \mathbb { R } ^ { n } )$ .
Given $x \in C ( [ - \Delta , + \infty ) , \mathbb { R } ^ { n } )$ , for any $t \in \mathbb { R } ^ { + }$ , let xt be an element of $C ( [ - \Delta , 0 ]$ , Rn) defined by $x _ { t } ( \theta ) : =$ $x ( t + \theta )$ with $\theta \in [ - \Delta , 0 ]$ .
For $\phi \in C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } )$ with $\Delta > 0$ fixed, we denote kφk $\mathrel { \mathop : } = \operatorname* { s u p } _ { \theta \in [ - \Delta , 0 ] }$ |φ(θ)|.
For any $V \in C ( \mathbb { R } ^ { + } , \mathbb { R } ) .$ , its upper Dini derivative is $D ^ { + } V ( t ) : =$ lim su $_ { s  0 ^ { + } } \frac { V ( t + s ) - V ( t ) } { s }$ .
For any $h : C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } ) \to \mathbb { R } ^ { + }$ , its upper Dini derivative is $\begin{array} { r } { D ^ { + } h ( x _ { t } ) \ = \ \operatorname* { l i m } \operatorname* { s u p } _ { \nu \to 0 ^ { + } } \frac { h ( x _ { t + \nu } ) - h ( x _ { t } ) } \nu } \end{array}$ h(xt+v)−h(xt) .
Id is the identity function, v $\operatorname { s g n } : \mathbb { R } \to \{ - 1 , 0 , 1 \}$ is the sign function, and psgn : $\mathbf { \partial } : \mathbb { R }  \{ 0 , 1 \}$ is defined by psgn(v) = 1 if v  0;
psgn(v) = 0 otherwise.
Given $\alpha \in C ( \mathbb { R } , \mathbb { R } ) , \alpha \geq \operatorname { I d }$ means that for all $\nu \in \mathbb { R } , \alpha ( \nu ) \geq \nu .$ .
