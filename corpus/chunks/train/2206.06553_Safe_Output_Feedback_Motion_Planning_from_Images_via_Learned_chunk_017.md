As (14) still depends on x and xˆ, which are unknown at planning time, extra steps must be taken to obtain a useful bound that is independent of x and xˆ; we achieve this by bounding the first two terms of (14) via a Lipschitz constant. Define $\varDelta k ( \hat { x } , x , x ^ { * } , u ^ { * } ) =$ $\begin{array} { r } { \operatorname* { m a x } _ { s \in [ 0 , 1 ] } \| R _ { c } ( \gamma _ { c } ^ { t } ( s ) ) B ( u ( \hat { x } , x ^ { * } , u ^ { * } ) - u _ { \mathrm { c l o s e s t } } ) \| } \end{array}$ , and $L _ { \varDelta k }$ as its local Lipschitz constant in the first argument, i.e., for all $x ^ { * } \in D , u ^ { * } \in \mathcal { U } , \{ x \mid d _ { c } ( x ^ { * } , x ) \leq \bar { c } \}$ , and $\{ \hat { x } \mid d _ { e } ( x , \hat { x } ) \leq \bar { e } \}$ for predetermined $\bar { c } , \bar { e } > 0$ (adjustable based on the expected error),

$$\left| \Delta k \left(\hat {x} _ {1}, x, x ^ {*}, u ^ {*}\right) - \Delta k \left(\hat {x} _ {2}, x, x ^ {*}, u ^ {*}\right)\right) | \leq L _ {\Delta k} d _ {e} \left(\hat {x} _ {1}, \hat {x} _ {2}\right). \tag {15}$$

See Rem. 1 for details on estimating $L _ { \varDelta k }$ . In estimating $L _ { \varDelta k }$ , we measure input distances w.r.t. $W _ { e } ;$ this reduces conservativeness due to the form of our estimation error bound. Combining (14)-(15) yields Lemma 1; see App. C for the detailed proof.

Bounding estimation error: Now, we provide more details behind Lemma 2. To bound $\begin{array} { r } { \int _ { 0 } ^ { 1 } \| R _ { e } ( \gamma _ { e } ^ { t } ( s ) ) w _ { q } ( t ) \| d s } \end{array}$ , we first note that $\lVert \boldsymbol { w } _ { q } \rVert$ is bounded by the sum of the disturbance magnitudes when $q = x$ and when $q = \hat { x } \ [ 2 6 ]$ . If $q = x , ( 7 \mathrm { b } )$ becomes $\dot { x } = f ( x ) + B u ( \hat { x } , x ^ { * } , u ^ { * } )$ ; relative to the true closed-loop dynamics (12), the disturbance is $B _ { w } ( t ) w _ { x } ( t )$ . If instead $q = \hat { x } , ( 7 \mathbf { b } )$ becomes $\dot { \hat { x } } = f ( \hat { x } ) + B u ( \hat { x } , x ^ { * } , u ^ { * } ) +$ $\begin{array} { r } { \frac { 1 } { 2 } \rho ( \hat { x } ) M _ { e } ( \hat { x } ) C _ { r } ^ { \top } C _ { r } ( x - \hat { x } ) } \end{array}$ ; relative to the true observer (6), the disturbance is

$$w _ {e} (t) \doteq \frac {1}{2} \rho (\hat {x} (t)) M _ {e} (\hat {x} (t)) C _ {r} ^ {\top} (\hat {h} ^ {- 1} (h (x (t), \theta) + B _ {y} w _ {y} (t), \theta) - C _ {r} x (t)). \tag {16}$$
