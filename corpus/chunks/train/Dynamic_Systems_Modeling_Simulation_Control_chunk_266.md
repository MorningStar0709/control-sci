# Conceptual Problems

5.1 Derive the state-variable equations for the system that is modeled by the following ODEs where $\alpha , w ,$ , and z are the dynamic variables and v is the input.

$$0. 4 \dot {\alpha} - 3 w + \alpha = 00. 2 5 \dot {z} + 4 z - 0. 5 z w = 0\ddot {w} + 6 \dot {w} + 0. 3 w ^ {3} - 2 \alpha = 8 v$$

5.2 Given the following system equation

$$4 \ddot {z} + 2 0 \ddot {z} + 8 4 z = 0. 1 2 u$$

a. Obtain a complete SSR with input u and output $y = \dot { z } .$ .   
b. Derive the system transfer function $G ( s ) = Z ( s ) / U ( s )$   
c. Derive the transfer function $Y ( s ) / U ( s )$ where the output is $y = \dot { z } .$

5.3 Obtain a complete SSR for the given system, with input u = v and output variables $y _ { 1 } = \dot { w }$ and $y _ { 2 } = w - z . $ .

$$0. 2 5 \dot {z} + 2 z - 0. 6 w = 00. 5 \ddot {w} + 3 \dot {w} + 2 0 w - 4 z = 2 v$$

5.4 Given the nonlinear first-order system

$$\dot {x} + 0. 4 \sqrt {x} = 2 u$$

Derive the linear model by performing the linearization about the static equilibrium state $x ^ { * }$ that results when the nominal input is $u ^ { * } = 0 . 2 5$ .

5.5 Given the nonlinear second-order system

$$\dot {x} _ {1} - x _ {2} = 0\dot {x} _ {2} + 2 x _ {1} ^ {1 / 4} + 3 x _ {2} = u$$

Derive the linear model by performing the linearization about the static equilibrium state vector $\mathbf { x } ^ { * } =$ $\left[ x _ { 1 } ^ { * } x _ { 2 } ^ { * } \right] ^ { T }$ that results when the nominal input is $u ^ { * } = 1$ . Express the linear model as a matrix-vector state equation.

5.6 Derive the transfer function $G ( s ) = Y ( s ) / U ( s )$ of the given I/O equation

$$2 \ddot {y} + 1 0 \dot {y} + 4 8 y = 0. 8 u$$
