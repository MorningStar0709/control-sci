Without loss of generality, we may assume that $\overline { c } : ( 0 , + \infty )  ( 0 , + \infty )$ is continuous (otherwise replace the function $\overline { { c } }$ by the function $\widetilde c ( r ) : = \int _ { r } ^ { r + 1 } \overline { c } ( l ) d l$ defined for $r > 0 )$ ). Next define for $r \geq 0$ the non-increasing, continuous and positive function

$$C (r) := \overline {{c}} \left(1 + \sigma (r, 0) + \gamma (a r)\right) \tag {87}$$

Moreover, define for $r \geq 0$

$$A (r) := \kappa^ {- 1} \left(\left(\frac {a r}{C (r) \varepsilon} - 1\right) ^ {+}\right), \tag {88}G (r) := \ln \left(1 + \max \left(A (r), \exp (r)\right) + \frac {\Gamma}{C (r)} \left(1 + \frac {a}{C (r)}\right) r\right). \tag {89}$$

Notice that both  A and  G are non-decreasing, continuous functions.

Let arbitrary $\left( \xi _ { 0 } , z _ { 0 } \right) \in \mathbb { R } ^ { n } \times \mathbb { R }$ $d \in L ^ { \infty } \left( \mathbb { R } _ { + } ; D \right)$ , $\theta \in L ^ { \infty } \left( \mathbb { R } _ { + } ; \Theta \right)$ be given. Since the functions $f : \mathbb { R } ^ { n } \times \mathbb { R } \times \Theta \times D \to \mathbb { R } ^ { n }$ and  are locally Lipschitz with respect to $( x , z ) \in \mathbb { R } ^ { n } \times$ there exists an unique solution $\boldsymbol { x } ( t ) = \big ( \xi ( t ) , z ( t ) \big )$ of (77) with $x ( 0 ) = { \bigl ( } \xi ( 0 ) , z ( 0 ) { \bigr ) } = { \bigl ( } \xi _ { 0 } , z _ { 0 } { \bigr ) }$ defined on a maximal interval $[ 0 , t _ { m a x } )$ , where $t _ { m a x } \in ( 0 , + \infty ]$ . For every $t \in \left[ 0 , { t _ { m a x } } \right)$ , we have from (77) that $\dot { z } ( t ) \geq 0$ Therefore, $z ( t )$ is non-decreasing on $[ 0 , t _ { m a x } )$ . Moreover, the mapping $z \mapsto \chi { \big ( } | d | , | \theta | , \exp ( z ) { \big ) }$ is non-increasing. Using (78) we conclude that the following inequalities hold almost everywhere in $\left[ 0 , { t _ { m a x } } \right)$ :

$$\frac {d}{d t} V (\xi (t), z (t)) \leq - \rho (V (\xi (t), z (t))) + \chi (| d (t) |, | \theta (t) |, \exp (z (t))) \tag {90}$$

which implies

$$\frac {d}{d t} V (\xi (t), z (t)) \leq - \rho (V (\xi (t), z (t))) + \chi (\| d \| _ {\infty}, \| \theta \| _ {\infty}, \exp (z _ {0})) \tag {91}$$

It follows from (84) and (85) that the following estimate holds for all $t \in \left[ 0 , { t _ { m a x } } \right)$ :

$$V (\xi (t), z (t)) \leq \sigma \left(V \left(\xi_ {0}, z _ {0}\right), t\right) + \gamma \left(\chi \left(\left\| d \right\| _ {\infty}, \left\| \theta \right\| _ {\infty}, \exp \left(z _ {0}\right)\right)\right) \tag {92}$$
