# 2.5.2 High-order sliding mode control

The control action is in this case a function of higher order time derivatives of the sliding variable. Second order sliding mode approach, used in this thesis, allows the definition of a discontinuous control ?? steering both the sliding variable ?? and its time derivative ??̇ to zero, so that the plant input ?? is a continuous control and thus chattering can be avoided. Problem is that there is no general method for tuning the parameters which characterize the various algorithms.
