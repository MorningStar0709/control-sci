Theorem 1. For the system (1) with a $C L R F \ – I V _ { \complement } \in C ( \mathbb { R } ^ { n } , \mathbb { R } ^ { + } )$ , if there exists $\chi \in \mathbb { R } ^ { + }$ such that for all $\phi \in C ( [ - \Delta , 0 ] , \mathbf { B } ( \varepsilon ) )$ with some $\varepsilon > 0$ and $L _ { g } V _ { \mathsf { c } } ( \phi ) \neq 0 ,$ ,

$$L _ {f} V _ {\mathbf {c}} (\phi) \leq \chi \| L _ {g} V _ {\mathbf {c}} (\phi) \| ^ {2}, \tag {3}$$

then there exists a smooth function $\varphi : \mathbb { R } ^ { + }  \mathbb { R } ^ { + }$ satisfying $\int _ { 0 } ^ { \infty } \varphi ( \nu ) d \nu = \infty$ 0 such that

$$u (\phi) := - \varphi (V _ {\mathrm{c}} (x)) (L _ {g} V _ {\mathrm{c}} (\phi)) ^ {\top} \tag {4}$$

is a continuous controller for the system (1) and ensures the closed-loop system to be GAS.

Proof. We first show the convergence of $V _ { \mathsf { c } } ( x )$ to a region around the origin, and then ensure the convergence of $V _ { \mathsf { c } } ( x )$ in this region. In this way, the system stability can be concluded from these two parts.

Part 1: convergence of $V _ { \mathsf { c } } ( x )$ to a region around the origin. We consider two cases: $L _ { f } V _ { \mathsf { c } } ( \phi ) \leq - \varepsilon \gamma _ { \mathsf { c } } ( V _ { \mathsf { c } } ( x ) )$ and $L _ { f } V _ { \mathsf { c } } ( \phi ) \geq$ $- \varepsilon \gamma _ { \sf c } ( V _ { \sf c } ( x ) )$ , where $ { \varepsilon } \in ( 0 , 1 )$ .

For the first case, we have from (4) that

$$
\begin{array}{l} L _ {f} V _ {\mathrm{c}} (\phi) + L _ {g} V _ {\mathrm{c}} (\phi) u = L _ {f} V _ {\mathrm{c}} (\phi) - \varphi (V _ {\mathrm{c}} (x)) \| L _ {g} V _ {\mathrm{c}} (\phi) \| ^ {2} \\ \leq - \varepsilon \gamma_ {\mathrm{c}} (V _ {\mathrm{c}} (x)) \leq 0, \tag {5} \\ \end{array}
$$

where the first $\ " \leq \ "$ holds due to the positivity of the function $\varphi .$ .

For the second case, from Lemma 1 in $[ 4 ] , L _ { g } V _ { \mathsf { c } } ( \phi ) \neq 0$ and there exists $\omega > 0$ such that $\| L _ { g } V _ { \mathsf { c } } ( \phi ) \| > \omega$ . Define the function $\varphi _ { 1 } : \mathbb { R } ^ { + }  \mathbb { R } ^ { + } ;$ :

$$\varphi_ {1} (V _ {\mathrm{c}} (x)) := \sup _ {\theta \in [ - \Delta , 0 ]} \frac {\varepsilon + \mathfrak {a} _ {\mathrm{c}} (\phi (\theta))}{\| \mathfrak {b} _ {\mathrm{c}} (\phi) \| ^ {2} + \varpi (\mathfrak {b} _ {\mathrm{c}} (\phi))}, \tag {6}$$
