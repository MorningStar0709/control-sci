with $\hat { \pmb { Z } } = \lceil \pmb { Z } \quad \mathbf { 0 } _ { m \times \bar { \delta } } \rceil$ is satisfied for $\forall i , j \in \{ 1 , 2 , \ldots , { \bar { \delta } } \}$ , then the buffered NCS is asymptotically stable. Moreover, the state feedback gain matrix is determined by $\begin{array} { r l } { K _ { x } } & { { } = } \end{array}$ $\pmb { Z } \pmb { X } _ { 1 } ^ { - 1 }$ and the corresponding Lyapunov function is given as $\bar { V ( \xi _ { k } , \pmb { \alpha } _ { k } ) } = \xi _ { k } ^ { T } \hat { P } ( \pmb { \alpha } _ { k } ) \pmb { \xi } _ { k }$ with the parameter dependent Lyapunov matrix $\begin{array} { r } { \hat { P } ( \alpha _ { k } ) = \sum _ { i = 1 } ^ { \delta } \alpha _ { i , k } P _ { i } } \end{array}$ for $P _ { i } = { Y _ { i } ^ { - 1 } }$ , which is positive definite for all values of the indicator function $\alpha _ { k } \ ( 7 )$ .

Proof: The relation between the matrix $\hat { Z }$ from (12) and the control gain is given by $\hat { Z } = \hat { K } X _ { i }$ leading to the LMIs

$$
\left[ \begin{array}{c c} \boldsymbol {X} _ {i} + \boldsymbol {X} _ {i} ^ {T} - \boldsymbol {Y} _ {i} & \left(\left(\hat {\boldsymbol {A}} _ {i} - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) \boldsymbol {X} _ {i}\right) ^ {T} \\ \left(\hat {\boldsymbol {A}} _ {i} - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) \boldsymbol {X} _ {i} & (1 - \gamma) \boldsymbol {Y} _ {j} \end{array} \right] > \mathbf {0}, \tag {13}
$$

where the closed loop dynamic matrices for individual modes of the switched system are contained. For $Y _ { i } ^ { - 1 } = P _ { i } $ this is equivalent to

$$
\left[ \begin{array}{c c} \boldsymbol {P} _ {i} & \left(\hat {\boldsymbol {A}} _ {i} - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) ^ {T} \boldsymbol {P} _ {j} \\ \boldsymbol {P} _ {j} \left(\hat {\boldsymbol {A}} _ {i} - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) & (1 - \gamma) \boldsymbol {P} _ {j} \end{array} \right] = \boldsymbol {Q} _ {i j} > \mathbf {0}, \tag {14}
$$

see [11], [12]. From the Schur complement of $\begin{array} { r } { \sum _ { i = 1 } ^ { \bar { \delta } } \alpha _ { i , k } \sum _ { j = 1 } ^ { \bar { \delta } } \alpha _ { j , k } \boldsymbol { Q } _ { i j } } \end{array}$ δ¯ , the inequality

$$\hat {\boldsymbol {P}} - \left(\hat {\boldsymbol {A}} (\boldsymbol {\alpha} _ {k}) - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) ^ {T} \hat {\boldsymbol {P}} ^ {+} \left(\hat {\boldsymbol {A}} (\boldsymbol {\alpha} _ {k}) - \hat {\boldsymbol {B}} \hat {\boldsymbol {K}}\right) > \gamma \hat {\boldsymbol {P}} (1 5)$$
