Proof. Since the CLRF-II Vr satisfies item (i) in Definition $^ { 2 , }$ we only need to show that (2) can be derived from (8). From the definition of the CLRF-II, there exist $\gamma _ { \mathsf { r } } , \eta _ { \mathsf { r } } \in \mathcal { K }$ such that $\gamma _ { \mathsf { r } } - \eta _ { \mathsf { r } } \in { \mathcal { K } }$ , and (8) holds for any nonzero $\phi \in C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } )$ . From Lemma A.1 in [36], there exists $\rho \in { \mathcal { K } }$ such that $\rho ( s ) > s$ for all $s > 0$ and $\gamma _ { \mathsf { r } } - \eta _ { \mathsf { r } } \circ \rho \in \mathcal { K }$ . In this respect, we have

$$\inf _ {u \in \mathbb {U}} \left\{L _ {f} V _ {\mathrm{r}} (\phi) + L _ {g} V _ {\mathrm{r}} (\phi) u \right\} < - \gamma_ {\mathrm{r}} (V _ {\mathrm{r}} (x)) + \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|)= - (\gamma_ {\mathrm{r}} - \eta_ {\mathrm{r}} \circ \rho) (V _ {\mathrm{r}} (x)) - \eta_ {\mathrm{r}} (\rho (V _ {\mathrm{r}} (x))) + \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|).$$

$\mathrm { I f } \rho ( V _ { \mathsf { r } } ( x ) ) \geq V _ { \mathsf { r } } ( \phi ( \theta ) )$ for all $\theta \in [ - \Delta , 0 ]$ , then

$$\inf _ {u \in \mathbb {U}} \left\{L _ {f} V _ {\mathrm{r}} (\phi) + L _ {g} V _ {\mathrm{r}} (\phi) u \right\} < - (\gamma_ {\mathrm{r}} - \eta_ {\mathrm{r}} \circ \rho) (V _ {\mathrm{r}} (x)).$$

which implies that item (ii) of Definition 2 holds. Hence, $V _ { \mathsf { r } }$ is a CLRF-I with $\gamma _ { \mathtt { c } } : = \gamma _ { \mathtt { r } } - \eta _ { \mathtt { r } } \circ \rho$ . 

We emphasize that the CLRF-II $V _ { \mathsf { r } }$ cannot be deduced from the CLRF-I $V _ { \mathsf { c } } .$ . The reason lies in the case where the Razumikhin condition is not satisfied. To be specific, without the Razumikhin condition, the derivative of $V _ { \mathfrak { r } }$ is still bounded via (8), whereas the bound of the derivative of $V _ { \mathsf { c } }$ is unknown and cannot be derived from (2) directly. Therefore, the estimate of the CLRF-II is bounded consistently via (8), which is not the case for the CLRF-I. In this respect, it is not easy to apply the CLRF-I directly to design the stabilizing controller explicitly (see, e.g., Theorem 1 and [4, 5]). However, with the CLRF-II, we can follow Sontag’s formula to design an explicit controller, which will be further discussed later.

From Definition 3, we define the following set
