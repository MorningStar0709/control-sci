The next two results address an extended version of issues I.1 and I.2 where the choice of resets (impulses) is only allowed to be made from a given compact collection of matrices. Since there is a restriction on available resets (impulses), it is natural to expect certain restrictions on the time constraints so that we are able to stabilize the switched system using resets (impulses) from the given set of compact matrices. We prove that specific time constraints on the signal allow us to choose the set of resets (the set of impulses) from the given collection of matrices, so that the reset (impulsive) switched system is stable.

Theorem 3.9. Consider a switched system (1) governed by the graph G satisfying Hypothesis 3.8, and a compact collection M of real matrices of size n not containing the zero matrix O. Then there exist $\tau ^ { * } \geq 0 , \eta ^ { * } > 0$ , and a collection $\mathcal { R } \subseteq \mathbb { M }$ of nonzero resets such that the reset switched system (2) is stable for all switching signals $\sigma \in \mathcal { S } ^ { \mathcal { G } } [ \tau ^ { * } , \eta ^ { * } ]$ .

Proof. First of all, for each $\eta > 0$ , we prove that, for each $p \in \mathcal P$ , there exist Jordan basis matrix $P _ { p }$ (depending on η) of $A _ { p } ,$ such that $\mathbb { M } \subseteq \cap _ { ( p , q ) \in { \mathcal E } ( { \mathcal G } _ { u } ) } U _ { p , q } ^ { \eta } ,$ . This is where the Hypothesis 3.8 will be used. Fix $\eta > 0$ and let $\begin{array} { r } { \epsilon = \operatorname* { m i n } _ { p \in \mathfrak { u } } \{ e ^ { - \mu _ { p } ^ { * } \eta } \} \in ( 0 , 1 ) } \end{array}$ . By Lemma 3.7, for each $p \in { \mathcal { P } } ,$ , there is a Jordan basis matrix $P _ { p }$ (depending on ϵ, and hence η) of $A _ { p }$ so that max $\left\{ c _ { p } \left\| P _ { q } ^ { - 1 } M P _ { p } \right\| : ( p , q ) \in \mathcal { E } ( \mathcal { G } _ { u } ) \right.$ , M ∈ M	 < ϵ. Hence for each $( p , q ) \in \mathcal { E } ( \mathcal { G } _ { u } )$ and for each $M \in \mathbb { M }$ , we have $\left. P _ { q } ^ { - 1 } M P _ { p } \right. < c _ { p } ^ { - 1 } e ^ { - \mu _ { p } ^ { * } \eta }$ . Thus $\mathbb { M } \subseteq \cap _ { ( p , q ) \in { \mathcal E } ( { \mathcal G } _ { u } ) } U _ { p , q } ^ { \eta } .$ .

In particular, fix a number $\eta _ { \mathrm { m i n } } > 0$ and choose Jordan basis matrix $P _ { p }$ (depending on $\eta _ { \mathrm { m i n } } )$ of $A _ { p } , p \in \mathcal { P }$ , such that M $\mathbb { I } \subseteq \cap _ { ( p , q ) \in { \mathcal { E } } ( { \mathcal { G } } _ { u } ) } U _ { p , q } ^ { \eta _ { \operatorname* { m i n } } }$ . Then define
