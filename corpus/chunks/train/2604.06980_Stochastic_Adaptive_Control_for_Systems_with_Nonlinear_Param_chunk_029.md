The desired leader opinion in this example is calculated as $x _ { L } = 1 . 6 3$ . Additionally, we set $M = 1$ and $B ^ { * } =$ $[ 1 , 0 , 0 , 0 ] ^ { \top }$ , meaning that agent 1 is the pinned node, as shown in Fig. $1 ( b )$ . Let $\Theta = \left\{ \theta \in \mathbb { R } ^ { \left( n + m \right) \times n } : \left\| \theta \right\| \leq 5 \right\}$ and initialize $\hat { \theta } _ { 0 } = 0$ . The sequence $\widehat { \theta } _ { t }$ is generated using Algorithm 1, and the adaptive controller is implemented as $\hat { u } _ { t } = \kappa ( \hat { \theta } _ { t } ) x _ { L } + t ^ { - \frac { 1 } { 8 } } \epsilon _ { t } .$ , where $\kappa ( \cdot )$ is designed according to (Ancona et al., 2023, Section $\overset { \cdot } { I V . A } )$ , and the probing signal $\{ \epsilon _ { t } \} _ { t \ge 0 }$ is i.i.d. and uniformly distributed on [−1, 1]. By Theorem 3 and Corollary 1, the parameter estimates are almost surely consistent, and the trajectory error associated with the sign function satisfies that

$$\operatorname * {l i m s u p} _ {t \to \infty} \frac {1}{t} \sum_ {\tau = 0} ^ {t} \sum_ {i = 1} ^ {4} \| s g n (x _ {i, \tau}) - s g n (x _ {i, \tau} ^ {*}) \| = 0, a. s., \tag {35}$$

where $x _ { i , t }$ and $\boldsymbol { x } _ { i , t } ^ { * }$ denote the opinion trajectory under adaptive control $\hat { u } _ { t }$ and oracle control $\boldsymbol { u } _ { t } ^ { * }$ , respectively.

In this setup, the parameter estimation error converges to zero, as shown in $F i g . 2 ( a )$ . The regret trajectory defined in (35) is also plotted in Fig. 2(b). As can be seen, under the adaptive control $\hat { u } _ { t }$ , the time-averaged opinion of each agent converges to that under the oracle control $\boldsymbol { u } _ { t } ^ { * }$ associated with the true parameter $\theta ^ { * }$ .
