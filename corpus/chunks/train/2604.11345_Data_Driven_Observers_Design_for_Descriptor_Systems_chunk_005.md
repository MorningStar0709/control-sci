The subsequent sections are organized as follows: Section 2 reviews necessary preliminaries on descriptor systems. Section 3 presents the data-driven state observer design and demonstrates the theoretical equivalence with model-based methods. Section 4 addresses observers with unknown inputs, detailing the design of full-order UIO. The application of the proposed framework to ESO design is provided in Section 5. Finally, Section 6 validates the effectiveness of the proposed methods through numerical simulations.

Notations: R and $\mathbb { Z } _ { 0 }$ denote the set of real numbers and the natural numbers with zero. The identity matrix in $\mathbb { R } ^ { n \times n }$ and the zero matrix in $\mathbb { R } ^ { n \times n }$ are denoted by $I _ { n }$ and $0 _ { n \times n } ,$ respectively. For a matrix $A \in \mathbb { R } ^ { m \times n }$ , we denote by Im(A) and $K e r ( A )$ the image and the right kernel of A. In addition, we denote by $A ^ { \dag } \in$ Rn×m its Moore-Penrose inverse and $r k ( A )$ its rank. When $m \ = \ n .$ , det(A) denotes the determinant of A. For a set of matrices $A _ { 1 } , . . . , . .$ with the same column numbers, $[ A _ { 1 } ^ { \top } , A _ { 2 } ^ { \top } , \cdots , A _ { n } ^ { \top } ] ^ { \top }$ is also denoted by col $\{ A _ { 1 } , A _ { 2 } , \cdot \cdot \cdot \stackrel { \cdot } { , } \bar { A _ { n } } \}$ . For a sequence of vectors $\{ f ( k ) \} _ { k = 0 } ^ { \dot { T } - 1 }$ 1 2  , the Hankel matrix of depth L is denoted by $H _ { L } ( f _ { [ 0 , T - 1 ] } )$ and defined as

$$
H _ {L} (f _ {[ 0, T - 1 ]}) \triangleq \left[ \begin{array}{c c c c} f (0) & f (1) \dots & f (T - L) \\ f (1) & f (2) \dots & f (T - L + 1) \\ \vdots & \vdots & \ddots & \vdots \\ f (L - 1) & f (L) \dots & f (T - 1) \end{array} \right]. \text {   If   }
$$

$H _ { L } ( f _ { [ 0 , T - 1 ] } )$ has full row rank, we say $\{ f ( k ) \} _ { k = 0 } ^ { T - 1 }$ is persistently exciting of order L.
