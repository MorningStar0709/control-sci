where we used the Taylor expansion $\begin{array} { r } { \frac { 1 } { 1 - x } = \sum _ { k = 0 } ^ { \infty } x ^ { k } \leq 1 + C x } \end{array}$ for small enough $x \ll 1$ with $C > 0$ being a problem independent constant.

The assumption $\epsilon a ^ { 2 } \gg 1$ can be used to write the asymptotic expansion of incomplete Gamma function $\hat { \Gamma ( k / 2 , \epsilon a ^ { 2 } / 2 ) } = ( \epsilon a ^ { 2 } / 2 ) ^ { k / 2 - 1 } e ^ { - \epsilon a ^ { 2 } / 2 } \left[ 1 + \hat { O } \left( ( \epsilon a ^ { 2 } / 2 ) ^ { - \hat { 1 } } \right) \right]$ . Noting that the Q function is always bounded as $\begin{array} { r } { Q ( 1 + a ) \leq \frac { e ^ { - \frac { ( 1 + a ) ^ { 2 } } { 2 } } } { \sqrt { 2 \pi } ( 1 + a ) } } \end{array}$ , we claim that choosing $\begin{array} { r } { \epsilon = \frac { k } { 2 e a ^ { 1 + 1 / 2 } } } \end{array}$ k2ea1+1/2 , for α ≥ c′′k with $\alpha \geq c ^ { \prime \prime } k$ a constant $c ^ { \prime \prime } > 0$ guarantees that $\begin{array} { r } { \epsilon a = \frac { \dot { k } } { 2 e } a ^ { - 1 / 2 } \ll 1 } \end{array}$ and $\epsilon a ^ { 2 } = \textstyle { \frac { k } { 2 e } } a ^ { 1 - 1 / 2 } \gg 1$ . Therefore, the upper bound (102) is valid for $\alpha \geq c ^ { \prime \prime } k$ . Furthermore, the term ǫa decays slower than both $Q ( 1 + a )$ and Γ(k/2, ǫa2/2) $\scriptstyle { \frac { \Gamma ( k / 2 , \epsilon a ^ { 2 } / 2 ) } { \Gamma ( k / 2 ) } }$ and thus ǫa dominates as

$$\frac {1}{p _ {k} (a)} \leq \frac {1}{Q (1)} \left(1 + \frac {C k}{2 e} a ^ {- 1 / 2}\right)$$

for a problem independent constant $C > 0$ .

Based on Lemma 17, the integrals in (99) and (100) are asymptotically constant if both $a _ { 1 , T }$ and a2,T are asymptotically large enough. This can be achieved if a1,T = √ TwT log T k $a _ { 2 , T }$ $\begin{array} { r } { \dot { a } _ { 1 , T } = \frac { T _ { w } } { \sqrt { T } \log T } \frac { \| \nabla L _ { * } \| _ { F } } { C r _ { * } } = \omega ( 1 ) } \end{array}$ for singular $A _ { c , * }$ and $\begin{array} { r } { a _ { 2 , T } = \sqrt { \frac { T _ { w } } { \log T } } \frac { \operatorname* { m i n } ( \sigma _ { \operatorname* { m i n } , * } , \epsilon _ { * } r _ { * } / 2 ) } { \sqrt { C } r _ { * } } = \omega ( 1 ) } \end{array}$ Tw min(σmin,∗, ǫ∗r∗/2)√ = ω(1) for non-singular Ac, . In other words, $A _ { c , * }$ Cr∗ choosing $T _ { w } ~ = ~ n ^ { 2 } \omega ( \sqrt { T } \log T )$ for singular $A _ { c , * }$ and $T _ { w } ~ = ~ n ^ { 2 } \omega ( \log T )$ for non-singular $A _ { c , * }$ yields the desired bound

$$p _ {t} ^ {\mathrm{opt}} \geq \frac {Q (1)}{1 + o (1)}.$$

for $T _ { r } < t \leq T$ for large enough T . Combined with the upper $T _ { w } = { \cal O } ( ( \sqrt { T } ) ^ { 1 + o ( 1 ) } )$ ), the proposed choices of $T _ { w }$ satisfy the asymptotic conditions.
