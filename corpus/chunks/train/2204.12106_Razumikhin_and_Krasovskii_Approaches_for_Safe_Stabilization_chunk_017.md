$$
u (\phi) := \left\{ \begin{array}{l l} \kappa (\lambda , \mathfrak {a} _ {\mathrm{r}} (\phi), (L _ {\mathrm{g}} V _ {\mathrm{r}} (\phi)) ^ {\top}), & i f \phi \neq 0, \\ 0, & i f \phi = 0, \end{array} \right. \tag {11}
$$

with $\lambda > 0 , \mathfrak { a } _ { \mathsf { r } } ( \phi ) : = L _ { f } V _ { \mathsf { r } } ( \phi ) + \gamma _ { \mathsf { r } } ( V _ { \mathsf { r } } ( x ) ) - \eta _ { \mathsf { r } } ( \| V _ { \mathsf { r } } ( \phi ) \| )$ , and

$$
\kappa (\lambda , p, q) = \left\{ \begin{array}{l l} \frac {p + \sqrt {p ^ {2} + \lambda \| q \| ^ {4}}}{- \| q \| ^ {2}} q, & \text {   if   } q \neq 0, \\ 0, & \text {   if   } q = 0, \end{array} \right. \tag {12}
$$

is continuous and ensures the system (1) to be $G A S .$

Proof. We first prove the stabilization of the system (1) under the controller (11). From (11), if $L _ { g } V _ { \Gamma } ( \phi ) \equiv 0$ , then $u \equiv 0$ and

from (8), we have

$$
\begin{array}{l} L _ {f} V _ {\mathrm{r}} (\phi) + L _ {g} V _ {\mathrm{r}} (\phi) u + \gamma_ {\mathrm{r}} (V _ {\mathrm{r}} (x)) - \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|) \\ = L _ {f} V _ {\mathrm{r}} (\phi) + \gamma_ {\mathrm{r}} (V _ {\mathrm{r}} (x)) - \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|) <   0. \\ \end{array}
$$

For the case $L _ { g } V _ { \Gamma } ( \phi ) \neq 0 .$ , we define $\mathfrak { a } _ { \mathfrak { r } } ( \phi ) : = L _ { f } V _ { \mathfrak { r } } ( \phi ) + \gamma _ { \mathfrak { r } } ( V _ { \mathfrak { r } } ( x ) )$ and $\mathfrak { b } _ { \mathfrak { r } } ( \phi ) : = L _ { g } V _ { \mathfrak { r } } ( \phi )$ . From (11), we have

$$
\begin{array}{l} L _ {f} V _ {\mathrm{r}} (\phi) + L _ {g} V _ {\mathrm{r}} (\phi) u + \gamma_ {\mathrm{r}} (V _ {\mathrm{r}} (x)) - \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|) \\ = \mathfrak {a} _ {\mathrm{r}} (\phi) - \mathfrak {b} _ {\mathrm{r}} (\phi) \frac {\mathfrak {a} _ {\mathrm{r}} (\phi) \mathfrak {b} _ {\mathrm{r}} ^ {\top} (\phi)}{\| \mathfrak {b} _ {\mathrm{r}} (\phi) \| ^ {2}} - \mathfrak {b} _ {\mathrm{r}} (\phi) \frac {\sqrt {\mathfrak {a} _ {\mathrm{r}} ^ {2} (\phi) + \lambda \| \mathfrak {b} _ {\mathrm{r}} (\phi) \| ^ {4}}}{\| \mathfrak {b} _ {\mathrm{r}} (\phi) \| ^ {2}} \mathfrak {b} _ {\mathrm{r}} ^ {\top} (\phi) \\ = - \sqrt {\mathfrak {a} _ {\mathrm{r}} ^ {2} (\phi) + \lambda \| \mathfrak {b} _ {\mathrm{r}} (\phi) \| ^ {4}} <   0, \\ \end{array}
$$

and thus L f Vr(φ) + LgVr(φ)u < −γr(Vr(x(t))) + ηr(kVr(φ)k).

Summarizing the above analysis, we have
