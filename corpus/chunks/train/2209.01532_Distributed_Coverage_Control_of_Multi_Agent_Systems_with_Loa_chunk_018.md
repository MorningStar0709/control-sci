Then it concludes that for all $\varepsilon > 0 .$ , there exists $\begin{array} { r } { \delta = \frac { \omega \varepsilon } { \bar { \omega } } > 0 } \end{array}$ such that $| \xi ( \varphi ) - \xi ( \varphi _ { 0 } ) | \leq \varepsilon$ whenever $| \varphi - \varphi _ { 0 } | \leq \delta$ According to δ -ε definition of limit, one can get $\begin{array} { r } { \operatorname* { l i m } _ { \varphi \to \varphi _ { 0 } } \xi ( \varphi ) = \xi ( \varphi _ { 0 } ) } \end{array}$ , which indicates the continuity of function $\xi : [ 0 , 2 \pi ]  [ 0 , 2 \pi ]$ . Finally, one proves the differentiability of function $\xi : [ 0 , 2 \pi ]  [ 0 , 2 \pi ]$ . For any given $\varphi _ { 0 } \in [ 0 . 2 \pi ]$ , by setting $\varphi = \varphi _ { 0 } + h ,$ , one can get

$$\int_ {\xi (\varphi_ {0})} ^ {\xi (\varphi_ {0} + h)} \omega (\theta) d \theta = \int_ {\varphi_ {0}} ^ {\varphi_ {0} + h} \omega (\theta) d \theta ,$$

which leads to $\left[ \xi \left( \varphi _ { 0 } + h \right) - \xi \left( \varphi _ { 0 } \right) \right] \cdot \omega ( \xi _ { h } ) = h \cdot \omega ( \varphi _ { h } )$ with $\xi _ { h } \in [ \xi ( \varphi _ { 0 } ) , \xi ( \varphi _ { 0 } + h ) ]$ ] and $\varphi _ { h } \in [ \varphi _ { 0 } , \varphi _ { 0 } + h ]$ according to mean value theorems for definite integrals. Thus, it follows from the continuity of functions $\xi ( \varphi )$ and $\omega ( \theta )$ that

$$\lim _ {h \rightarrow 0} \frac {\xi (\varphi_ {0} + h) - \xi (\varphi_ {0})}{h} = \lim _ {h \rightarrow 0} \frac {\omega (\varphi_ {h})}{\omega (\xi_ {h})} = \frac {\omega (\varphi_ {0})}{\omega (\xi (\varphi_ {0}))}.$$

The proof is thus completed.

□

![](image/fbc02673036a050b76c6647dfd55d1c6c70e5c91ddc87937f3ecad5d03ad4c5a.jpg)

<details>
<summary>text_image</summary>

φᵢ*
0 2π/K* 4π/K* 6π/K* 2π(K*-1)/K* 2π
</details>

Figure 2: Equal partition of phase angles on [0, 2π].

Proposition 3.2. There exist optimal solutions to Problem (5).

Proof. Note that there exists a continuous function $\xi : [ 0 , 2 \pi ] \to [ 0 , 2 \pi ]$ such that $\begin{array} { r } { \int _ { \varphi } ^ { \xi ( \varphi ) } \omega ( \theta ) d \theta = \bar { m } , \forall \varphi \in [ 0 , 2 \pi ] } \end{array}$ . Thus, one has $\varphi _ { i } = \xi ( \varphi _ { i - 1 } ) , i \in I _ { N }$ when equitable workload partition is completed. Then the cost function (3) can be rewritten as
