Because the random variables in each disjoint interval are independent, we have that each interval independently satisfies Equation (33) with probability at least $1 / 2$ . Therefore, for sufficiently large $T .$ , the probability that Equation (33) fails to hold for all ⌊ 2 1 ⌈log5(T )⌉+1 ⌋ ≥ $\begin{array} { r } { \big \lfloor \frac { | t _ { 2 } - t _ { 1 } | } { \lceil \log ^ { 5 } ( T ) \rceil + 1 } \big \rfloor \ \ge \ } \end{array}$ $\log ^ { 2 } ( T )$ intervals is at most $( 1 / 2 ) ^ { \lfloor \frac { | t _ { 2 } - t _ { 1 } | } { \lceil \log ^ { 5 } ( T ) \rceil + 1 } \rfloor } \leq 0 . 5 ^ { \log ^ { 2 } ( T ) } = o _ { T } ( 1 / T ^ { 2 2 } )$ . Therefore, we have shown that

$$\mathbb {P} (E _ {\mathrm{L8}} ^ {t _ {1}, t _ {2}}) \geq 1 - o _ {T} (1 / T ^ {2 2}).$$

Since there are less than $T ^ { 2 }$ pairs $( t _ { 1 } , t _ { 2 } )$ and $\mathbb { P } ( E _ { 1 } ^ { t } ) \ge \mathbb { P } ( E _ { 1 } ) = 1 - o _ { T } ( 1 / T ^ { 2 0 } )$ by Equation (160), we have by a union bound that

$$\mathbb {P} (E _ {\mathrm{L8}} (x, K, \theta , W ^ {\prime})) \geq 1 - o _ {T} (T ^ {2} / T ^ {2 2}) - o _ {T} (1 / T ^ {2 0}) = 1 - o _ {T} (1 / T ^ {2 0}).$$

Lemma 10. Using the assumptions and notation of the proof of Lemma $\delta ,$ for all pairs $t _ { 1 } , t _ { 2 }$ such that $t _ { 2 } - t _ { 1 } \geq \log ^ { 8 } ( T )$ , conditional on event $A _ { t _ { 2 } } \cap E _ { \mathrm { L 8 } } ( x , K , \theta , W ^ { \prime } )$ ,

$$\lambda (t _ {2}) - \lambda (t _ {1}) = \Omega_ {T} \left(\frac {| t _ {2} - t _ {1} |}{\log^ {8} (T)}\right). \tag {34}$$

By Lemma 10, conditional on $A _ { t _ { 2 } } \cap E _ { \mathrm { L 8 } } ( x , K , \theta , W ^ { \prime } )$ , we that:
