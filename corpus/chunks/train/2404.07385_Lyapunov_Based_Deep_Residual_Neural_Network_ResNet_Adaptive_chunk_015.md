Remark 2. If $\Phi _ { p }$ suffers from the vanishing gradient problem, i.e., $\Vert \Xi _ { l } \Vert _ { F } ~ \approx ~ 0$ for all $l ~ \in ~ \{ p + 1 , \ldots , m \}$ , then $\Phi _ { p } ^ { \prime } = \left( \prod _ { l = p + 1 } ^ { \curvearrowright } \left( I _ { n } + \Xi _ { l } \right) \right) \Lambda _ { p } \approx \Lambda _ { p }$ . For an equivalent fullyconnected DNN, i.e., in absence of shortcut connections, $\left\| \Phi _ { p } ^ { \prime } \right\| _ { F } = \left\| \left( \bigcap _ { l = p + 1 } ^ { \curvearrowright } \Xi _ { l } \right) \Lambda _ { p } \right\| \approx 0 .$ Λ p . Thus, the shortcut connection circumvents the vanishing gradient problem in the ResNet when $\Phi _ { p }$ has a vanishing gradient.

Based on the subsequent stability analysis, the control input is designed as

$$u \triangleq \dot {x} _ {d} - \Phi^ {\hat {\theta}} (x) - \sigma_ {e} e - \sigma_ {s} \mathrm{sgn} (e), \tag {17}$$

where $\sigma _ { e } , \sigma _ { s } \in \mathbb { R } _ { > 0 }$ are constant control gains, and sgn(·) denotes the vector signum function.
