Conjunction Lemma1, we can get $\begin{array} { r } { \left( \sum _ { o = 1 } ^ { n } \frac { \tilde { \varphi } _ { o } ^ { 2 } } { 2 \varepsilon _ { o } } \right) ^ { p } \leq \sum _ { o = 1 } ^ { n } } \end{array}$ 2εo φ˜2o $\begin{array} { r } { \frac { \tilde { \varphi } _ { o } ^ { 2 } } { 2 \varepsilon _ { o } } + ( 1 - p ) p ^ { \frac { p } { 1 - p } } } \end{array}$ . Bringing this inequality to (27) and simplifying it by calculation yields

$$\dot {V} \leq - a V _ {n} ^ {q} - b V _ {n} ^ {p} + \sum_ {o = 1} ^ {n} \tau_ {o} \left(\frac {\tilde {\varphi} _ {o} ^ {2}}{2 \varepsilon_ {o}}\right) ^ {q} - \sum_ {o = 1} ^ {n} \frac {\tau_ {o}}{2 \varepsilon_ {o}} \tilde {\varphi} _ {o} ^ {2} + c _ {1} \tag {28}$$

$\begin{array} { r } { a = \pi / ( n + 1 ) ^ { q } , b = \pi , c _ { 1 } = \sum _ { o = 1 } ^ { n } ( \frac { u _ { o } ^ { 2 } } { 2 } + \frac { \lambda _ { o } ^ { 2 } } { 2 } + } \end{array}$ o= $\begin{array} { r } { \frac { f _ { o } \tau _ { o } } { \gamma } \tilde { \varphi } _ { o } ^ { 2 } ) + ( 1 - p ) p ^ { \frac { p } { 1 - p } } + \Lambda } \end{array}$ unknown parameter $\gamma _ { m }$ satisfying $| \tilde { \varphi } _ { m } | < \gamma _ { m }$ , then discuss the following two cases:

$\gamma _ { o } < \sqrt { 2 \varepsilon _ { o } } .$ In this case, we have hen (28) is rewritten a $\begin{array} { r } { \sum _ { o = 1 } ^ { n } \tau _ { o } \big ( \frac { \tilde { \varphi } _ { o } ^ { 2 } } { 2 \varepsilon _ { o } } \big ) ^ { q } - } \end{array}$ $\begin{array} { r } { \sum _ { o = 1 } ^ { n } \frac { \tau _ { o } } { 2 \varepsilon _ { o } } \tilde { \varphi } _ { o } ^ { 2 } < 0 . } \end{array}$

$$\dot {V} \leq - a V _ {n} ^ {q} - b V _ {n} ^ {p} + c _ {1} \tag {29}$$

Case 2. γo ≥ √2εo: In this case, we have Pno=1 τo( φ˜2o2εo )q $\gamma _ { o } \geq \sqrt { 2 \varepsilon _ { o } } .$ $\begin{array} { r } { \sum _ { o = 1 } ^ { n } \tau _ { o } \big ( \frac { \tilde { \varphi } _ { o } ^ { 2 } } { 2 \varepsilon _ { o } } \big ) ^ { q } - } \end{array}$ $\begin{array} { r } { \sum _ { o = 1 } ^ { n } \frac { \tau _ { o } } { 2 \varepsilon _ { o } } \tilde { \varphi } _ { o } ^ { 2 } \leq \sum _ { o = 1 } ^ { n } \tau _ { o } ( \frac { \tilde { \gamma } _ { o } ^ { 2 } } { 2 \varepsilon _ { o } } ) ^ { q } - \sum _ { o = 1 } ^ { n } \frac { \tau _ { o } } { 2 \varepsilon _ { o } } \tilde { \gamma } _ { o } ^ { 2 } } \end{array}$ τo . Then (28) is rewritten as:

$$\dot {V} \leq - a V _ {n} ^ {q} - b V _ {n} ^ {p} + c _ {1} + \sum_ {o = 1} ^ {n} \tau_ {o} \left(\frac {\tilde {\gamma} _ {o} ^ {2}}{2 \varepsilon_ {o}}\right) ^ {q} - \sum_ {o = 1} ^ {n} \frac {\tau_ {o} \tilde {\gamma} _ {o} ^ {2}}{2 \varepsilon_ {o}} \tag {30}$$

Combining the two cases above, we get:

$$\dot {V} \leq - a V _ {n} ^ {q} - b V _ {n} ^ {p} + c \tag {31}$$

where
