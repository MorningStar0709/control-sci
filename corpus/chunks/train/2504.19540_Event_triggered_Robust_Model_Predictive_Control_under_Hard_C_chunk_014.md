$$H \tilde {x} (k | t + T) = H x ^ {*} (k + T | t)+ H [ \tilde {x} (k | t + T) - x ^ {*} (k + T | t) ]\leq H x ^ {*} (k + T | t) + | | H | | _ {\infty} | | \tilde {x} (k | t + T) - x ^ {*} (k + T | t) | | \cdot \mathbf {1} _ {\mathrm{H}}\leq (1 - \epsilon_ {N}) \cdot \mathbf {1} _ {\mathrm{H}}+ | | H | | _ {\infty} \sqrt {\rho^ {k} \frac {c _ {\delta , \mathrm{u}}}{c _ {\delta , \ell}}} \tilde {V} _ {\max} (\hat {w}) \cdot \mathbf {1} _ {\mathrm{H}}\leq \left[ \left(1 - \epsilon_ {k + 1}\right) + \sqrt {\rho^ {k}} \epsilon \right] \cdot \mathbf {1} _ {\mathrm{H}} = \left(1 - \epsilon_ {k}\right) \cdot \mathbf {1} _ {\mathrm{H}} \tag {31}$$

Same argumentation can be done for the input constraints, if wˆ ≤ V˜ −1max $\begin{array} { r } { \hat { w } \le \tilde { V } _ { \mathrm { m a x } } ^ { - 1 } \big ( \frac { 1 } { k _ { \mathrm { m a x } } | | L | | _ { \infty } } \sqrt { \frac { c _ { \delta , \ell } } { c _ { \delta , \mathrm { u } } } } \epsilon \big ) . } \end{array}$ cδ,ℓ ϵ). 1 cδ,u

It is $\tilde { x } ( N | t + T ) \in \mathcal { X } _ { \mathrm { f } } ^ { \cdot } ,$ , because

$$\tilde {x} (N | t + T) = \tilde {x} (N | t + T)- x ^ {*} (N + T | t) + x ^ {*} (N + T | t) \tag {32}= \tilde {x} (N | t + T) - x ^ {*} (N + T | t)+ f \left(x ^ {*} (N + T - 1 | t), \kappa_ {\mathrm{f}} \left(x ^ {*} (N + T - 1 | t)\right)\right)$$

and

$$\left| \left| \tilde {x} (N | t + T) - x ^ {*} (N + T | t) \right| \right| \leq \sqrt {\rho^ {N} \frac {c _ {\delta , \mathrm{u}}}{c _ {\delta , \ell}}} \tilde {V} _ {\max} (\hat {w}), \tag {33}$$

and thus $\tilde { x } ( N | t + T ) - x ^ { * } ( N + T | t ) \in \mathcal { W } _ { N }$ .

Remark 2: The bounds on wˆ calculated are equal to the bounds in [9, Proposition 5] transformed with $\tilde { V } _ { \mathrm { m a x } } ( \hat { w } )$ . Hence, if we have already designed a periodic MPC using [9] (plus final state constraints), then, we can reuse it for smaller disturbances in the event-triggered setting.

Remark 3: This lemma also implies that the real states and inputs always satisfy constraints X and U.

We now prove stability. First, we summarize the influence of the disturbances w between t and $t + T$ on the states into the artificial disturbance

$$w _ {\mathrm{art}} (T | t) = x (t + T) - x ^ {*} (T | t). \tag {34}$$

It follows from Lemma 1 that $w _ { \mathrm { a r t } } ( T | t )$ is bounded by

$$\left| \left| w _ {\text { art }} (T | t) \right| \right| \leq \tilde {V} _ {\max} (\hat {w}). \tag {35}$$

With this, we can show the following.
