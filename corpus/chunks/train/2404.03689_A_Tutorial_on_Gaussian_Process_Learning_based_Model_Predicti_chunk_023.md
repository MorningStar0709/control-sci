= \left[ \begin{array}{c c} \nabla f (\mu_ {k} ^ {z}) & B _ {d} \end{array} \right] \left[ \begin{array}{c c} \Sigma_ {k} ^ {z} & \Sigma_ {k} ^ {z d} \\ \Sigma_ {k} ^ {z d ^ {\top}} & \Sigma_ {k} ^ {d} + \Sigma^ {w} \end{array} \right] \left[ \begin{array}{c c} \nabla f (\mu_ {k} ^ {z}) & B _ {d} \end{array} \right] ^ {\top}, \tag {21g}
= \left[ \nabla f (\mu_ {k} ^ {z}) \quad B _ {d} \right] \Sigma_ {k} \left[ \nabla f (\mu_ {k} ^ {z}) \quad B _ {d} \right] ^ {\top}. \tag {21h}$$

It is worth remembering that the posterior predictions of the GP model in the MPC loop are not Gaussian due to multiple prediction steps needed. In order to integrate GP models into the MPC prediction model, it needs to assume the distribution of the states at every time step in the MPC loop is Gaussian distributed as $x _ { k } \sim { \cal N } ( \mu _ { k } ^ { d } , \Sigma _ { k } ^ { d } )$ , thus we can obtain posterior predictions using the GP model. Intuitively, we need to consider the input uncertainty for the GP model at each time step to propagate the uncertainty in the MPC loop cumulatively.

Upon examining the definitive equation for uncertainty propagation (21g), it can be seen that for accurate uncertainty propagation when coupling a GP model $d ( \cdot )$ with a nominal system model $f ( \cdot )$ , both the inherent uncertainty of the GP model $\Sigma _ { k } ^ { d } ,$ , and the covariance between the GP model’s output and the combined state-control inputs $\Sigma _ { k } ^ { z d }$ , play pivotal roles. Comparing (21f) and (21g), we obtain the general equations for $\Sigma _ { k } ^ { d }$ ???? and $\Sigma _ { k } ^ { z d }$ Σ?????? , articulated as:

$$
\left[ \begin{array}{c} \Sigma_ {k} ^ {z d} \\ \Sigma_ {k} ^ {d} \end{array} \right] = \left[ \begin{array}{c} \Sigma_ {k} ^ {z} (\nabla \mu^ {d} (\mu_ {k} ^ {z})) ^ {\top} \\ \Sigma^ {d} (\mu_ {k} ^ {z}) + \nabla \mu^ {d} (\mu_ {k} ^ {z}) \Sigma_ {k} ^ {z} (\nabla \mu^ {d} (\mu_ {k} ^ {z})) ^ {\top} \end{array} \right]. \tag {22}
$$
