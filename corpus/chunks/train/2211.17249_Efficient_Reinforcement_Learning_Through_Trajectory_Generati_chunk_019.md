# Lemma 3 (Fundamental Lemma Willems et al. (2005); Markovsky and Dorfler ¨ (2022) )

If rank $\left\lceil \begin{array} { c } { { U _ { 0 , T , L - T + 1 } } } \\ { { { \bf Y } _ { 0 , L - T + 1 } } } \end{array} \right\rceil = n + T m ,$ Y0,L−T +1 , then any length-T input/output trajectory of system (1) can be expressed as $\begin{array} { r } { \Big [ \pmb { \mathscr { y } } _ { [ 0 , T - 1 ] } ^ { u } \Big ] ^ { - } = \Big [ \frac { \pmb { U } _ { 0 , T , L - T + 1 } } { \pmb { Y } _ { 0 , T , L - T + 1 } } \Big ] } \end{array}$ i g where g ∈ RL−T +1. $\pmb { g } \in \mathbb { R } ^ { L - T + 1 }$

When the rank condition in Lemma 3 holds, linear combination of the columns of the Hankel matrix is an input/output trajectory of the system. We then generate trajectory of length T using

$$\left[ \tilde {\boldsymbol {u}} (0); \dots ; \tilde {\boldsymbol {u}} (T - 1); \tilde {\boldsymbol {y}} (0); \dots ; \tilde {\boldsymbol {y}} (T - 1) \right] = \mathcal {H} \boldsymbol {g}. \tag {14}$$

For convenience, we adopt the notation $\mathcal { H } _ { u } = U _ { 0 , T , L - T + 1 } , \mathcal { H } _ { u } = Y _ { 0 , T , L - T + 1 }$ in the following sections. To represent the lines of blocks starting from the time $k = 0 , \cdots , T - 1 , \mathcal { H } _ { u } ^ { k }$ the same as in (7) and $\mathcal { H } _ { u } ^ { k } : = [ y _ { d } ( k ) \cdot \cdot \cdot y _ { d } ( L - T + k ) ]$ . We also define the stacked blocks starting from $k _ { 1 } , \cdots , k _ { 2 }$ as $\mathcal { H } _ { y } ^ { k _ { 1 } : k _ { 2 } } : = \left[ H _ { y } ^ { k _ { 1 } } ; H _ { y } ^ { k _ { 1 } + 1 } ; \cdot \cdot \cdot ; H _ { y } ^ { k _ { 2 } } \right]$ and $\mathcal { H } _ { u } ^ { k _ { 1 } : k _ { 2 } } : = \left[ H _ { u } ^ { k _ { 1 } } ; H _ { u } ^ { k _ { 1 } + 1 } ; \cdot \cdot \cdot ; H _ { u } ^ { k _ { 2 } } \right]$ 12 .
