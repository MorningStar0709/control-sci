# Suboptimal MPC algorithm.

Data: Integer $N _ { \mathrm { i t e r } }$ .

Initialize: Set current state $ { \boldsymbol { { x } } } \ = \  { \boldsymbol { { x } } } _ { 0 }$ , current control sequence, ${ \textbf { u } } =$ h $. ( x _ { 0 } )$ .

Step 1 (State evolution): Apply control $u = u ( 0 )$ to the system. Obtain state at next sample, $x ^ { + }$ . For the nominal system

$$x ^ {+} = f (x, u (0))$$

Step 2 (Warm start): Denote the warm start for the next sample time as $\widetilde { \mathbf { u } } ^ { + }$ . We use

$$\tilde {\mathbf {u}} ^ {+} = \{u (1), u (2), \dots , u (N - 1), 0 \}$$

in which $x ( N ) \ = \ \phi ( N ; x , { \bf u } )$ . The warm start $\widetilde { \mathbf { u } } ^ { + }$ therefore is a function of (x, u). This warm start is a simplified version of the one considered in Chapter 2, in which the final control move in the warm start was determined by the control law $\kappa _ { f } ( \boldsymbol { x } )$ . In distributed MPC it is simpler to use zero for the final control move in the warm start.

Step 3 (Iteration of an optimization method): The controller performs $N _ { \mathrm { i t e r } }$ iterations of a feasible path optimization algorithm to obtain an improved control sequence using initial state $x ^ { + }$ . The final input sequence ${ \bf u } ^ { + }$ is a function of the state initial condition and the warm start $( x ^ { + } , \tilde { \mathbf { u } } )$ . Noting that $x ^ { + }$ and $\tilde { \mathbf { u } }$ are both functions of $( x , \mathbf { u } )$ , the input sequence ${ \bf u } ^ { + }$ can also be expressed as function of only (x, u)

$$\mathbf {u} ^ {+} = g (x, \mathbf {u})$$

Step 4 (Next time step): Update state and input sequence: $x \gets x ^ { + }$ , u $ \mathbf { u } ^ { + }$ . Go to Step 1.

We establish later in the chapter that the system cost function $V ( x , { \bf u } )$ satisfies the following properties for the form of suboptimal MPC generated by distributed MPC. There exist constants $a , b , c > 0$ such that

$$a \left| (x, \mathbf {u}) \right| ^ {2} \leq V (x, \mathbf {u}) \leq b \left| (x, \mathbf {u}) \right| ^ {2}V \left(x ^ {+}, \mathbf {u} ^ {+}\right) - V (x, \mathbf {u}) \leq - c | (x, u (0)) | ^ {2}$$
