# Adaptive Backstepping

The key idea of backstepping is to derive an error equation and to construct a control law and a parameter adjustment law such that the state of the error equation goes to zero. The idea is illustrated by a simple example.
