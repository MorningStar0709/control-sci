# 3.4 Stability of the Sliding Subgroup

Consider the Lagrangian dynamics (7) with the configuration manifold M. Let $\bar { g } = ( q , \dot { q } ) \overset { \cdot } { \in } \mathcal { M }$ , then the following control law

$$\bar {\tau} = - \lambda (D (q) \left(q _ {0} \dot {q} + \dot {q} _ {0} q\right) + C (q, \dot {q}) \left(q _ {0} q - \bar {1}\right)) - K _ {r} s (\bar {g}) \tag {16}$$

renders the sliding subgroup H¯ attractive, where $K _ { r } =$ ${ K } _ { r } ^ { T } \in \mathrm { R } ^ { 4 \times 4 }$ is a positive definite gain matrix.
