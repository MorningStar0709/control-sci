For each k, the first element of $\mu ^ { \star } ( x _ { k } )$ is applied to the system and the process is repeated in a receding horizon fashion. The optimal state evolution under this optimal MPC policy, starting from some $x _ { 0 } ^ { \star } : = x _ { 0 }$ , is then given by

$$x _ {k + 1} ^ {\star} = A x _ {k} ^ {\star} + \overline {{{B}}} \mu^ {\star} (x _ {k} ^ {\star}) := f (x _ {k} ^ {\star}), \forall k \geq 0, \tag {2}$$

where , $S : = [ I _ { m \times m } \textbf { 0 } . . . \textbf { 0 } ] \in \mathbb { R } ^ { m \times N m }$ and ${ \overline { { B } } } : = B S$

Problem (1) is a parametric quadratic program, and, given $\mathbf { \psi } _ { 1 } \mathbf { \psi } _ { x } \in \mathbb { R } ^ { n }$ , can be represented in an equivalent condensed form $\begin{array} { r } { V ( x ) = \operatorname* { m i n } _ { \pmb { \nu } \in N } \| ( x , \pmb { \nu } ) \| _ { M } ^ { 2 } } \end{array}$ , with $\mathbf { \dot { \mathcal { N } } } = \mathcal { U } ^ { N } \subseteq \mathbb { R } ^ { N m }$

$$
M = \left[ \begin{array}{c c} W & G ^ {\top} \\ G & H \end{array} \right], \tag {3}
$$

with $H \in \mathbb { R } ^ { N m \times N m } , W \in \mathbb { R } ^ { N m \times N m }$ and, $G \in \mathbb { R } ^ { N m \times n }$ defined in Appendix A.

1When clear from the context, we drop the explicit dependence of $V _ { N } ( x )$ on N and use $V ( x )$ .

As computing $\mu ^ { \star } ( x )$ may be prohibitive, one can consider suboptimal solutions computed by using a fixed number of optimization iterations. Specifically, given $x \in \mathbb { R } ^ { n }$ and an input vector $\nu \in \mathbb { R } ^ { N m }$ , consider the operator that performs one step of the projected gradient method (PGM)

$$\mathcal {T} (x, \boldsymbol {\nu}) := \Pi_ {\mathcal {N}} [ \boldsymbol {\nu} - \alpha \nabla \boldsymbol {\nu} J _ {N} (x, \boldsymbol {\nu}) ], \tag {4}$$

where $\alpha \in \mathbb { R }$ is a step size. Applying (4) iteratively for some $\ell _ { k } \ \in \ \mathbb { N }$ times, provides an approximation for the optimal input, and hence the optimal policy. The combined dynamics of the system and the optimizer are given by2

$$z _ {k} = \mathcal {T} ^ {\ell_ {k}} (x _ {k}, z _ {k - 1}), \tag {5a}x _ {k + 1} = A x _ {k} + \overline {{{B}}} z _ {k}, \tag {5b}$$

where for some $l \in \mathbb { N } , x \in \mathbb { R } ^ { n }$ and $\nu \in \mathbb { R } ^ { N m }$ , we define

$$\mathcal {T} ^ {l} (x, \boldsymbol {\nu}) = \mathcal {T} (x, \mathcal {T} ^ {l - 1} (x, \boldsymbol {\nu})),$$

starting with $\mathcal { T } ^ { 0 } ( x , \nu ) = \nu$ . The suboptimal state evolution (5b) is equivalently described by
