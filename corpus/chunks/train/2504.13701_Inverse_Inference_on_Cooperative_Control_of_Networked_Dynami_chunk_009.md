# C. Observation Modeling and Problem of Interest

Consider an external observer can observe the state trajectory of the NDS with a sampling period $\tau > 0$ and zero-order holder. Denote $x _ { i } ( k )$ as node $i \ ' s$ state at instant $k \tau .$ , and the corresponding observation is given by

$$y _ {i} (k) = x _ {i} (k) + v _ {i} (k), \tag {4}$$

where $v _ { i } ( k ) \ \sim \ \mathbf { N } ( 0 , \Gamma )$ is an independent Gaussian noise vector, with $\Gamma = \mathrm { d i a g } \{ \sigma _ { \upsilon , 1 } ^ { 2 } , \cdot \cdot \cdot , \sigma _ { \upsilon , n } ^ { 2 } \}$ being the covariance matrix for n different state dimensions. Then, by integrating (2) and conducting discretization, the corresponding discretetime model that the observer relies on is given by

$$
\left\{ \begin{array}{c} x (k + 1) = e ^ {A _ {c} \tau} x (k) \triangleq A _ {d} x (k) \\ y (k) = x (k) + v (k) \end{array} , \right. \tag {5}
$$

where $y = [ y _ { 1 } ^ { \mathsf { T } } , y _ { 2 } ^ { \mathsf { T } } , \cdot \cdot \cdot , y _ { N } ^ { \mathsf { T } } ] ^ { \mathsf { T } } \in \mathbb { R } ^ { N n } , \upsilon = [ v _ { 1 } ^ { \mathsf { T } } , v _ { 2 } ^ { \mathsf { T } } , \cdot \cdot \cdot , v _ { N } ^ { \mathsf { T } } ] ^ { \mathsf { T } } \in$ $\mathbb { R } ^ { N n }$ , and the matrix exponential $A _ { d }$ represents the discretized closed-loop system matrix. Note that $A _ { d }$ is called stable (including marginally and asymptotically stable) if it belongs to the following set

$ { \boldsymbol { S } } _ { d } = \{  { \boldsymbol { M } } : \rho (  { \boldsymbol { M } } ) < 1$ , or 1 is a semisimple eigenvalue

while other eigenvalue modulus are less than 1}. (6)

Based on the above formulation, the goal of the observer is to infer the cooperative control of the NDS from the noisy observations $\bar { \{ \boldsymbol { y } ( k ) \} } _ { k = 1 } ^ { T }$ . Specifically, the components that needs to be inferred consists of three aspects: i) the nodal dynamics matrices A and B, ii) the topology matrix L among different nodes, and iii) the local control feedback gain K. Mathematically, this goal can be formulated as finding an algorithm mechanism M

$$\mathcal {M} (y (k), k = 1, \dots , T) \mapsto \{A, B, K, L \}. \tag {7}$$
