Now we show sufficiency. Suppose there exists a solution ˆθ to (22) where $S , R , \Lambda \succ 0$ , and $\bigl [ \begin{array} { l } { R I } \\ { I } S  \end{array} \bigr ] \succ 0$ . Note that $S \succ 0$ along with $[ \mathbf { \Sigma } _ { I } ^ { R } \mathbf { \Sigma } _ { S } ^ { I } ] \succ 0$ implies $I - R S$ is invertible. First, construct U and V through SVD of $I - R S$ as follows: $V U ^ { \top } = I - R S$ . Since $I - { \bar { R } } S$ is invertible, so are V and U. Since both R and V are invertible, so is Y . This gives a construction of P by $\begin{array} { r } { \left[ \begin{array} { l l } { I } & { S } \\ { 0 } & { I J ^ { \top } } \end{array} \right] Y ^ { - 1 } } \end{array}$ . Then, reconstruct $D _ { k v y }$ and $D _ { k v w }$ by solving for them in the equations for $\hat { D } _ { k v y }$ and $\hat { D } _ { k v w }$ in (23). Next, the equations for $N _ { A } , \ N _ { B } ,$ and $N _ { C }$ are solved for $( A _ { k } , B _ { k y } , \tilde { C } _ { k u } , D _ { k u y } )$ , $B _ { k w }$ , and $C _ { k v }$ respectively. Due to the invertibility of $Y , ( 2 2 )$ is equivalent to (20). Finally, through the Schur complement, (20) is equivalent to the original dissipativity condition provided by Lemma 1. This completes the proof of sufficiency.

Note that (22) is also affine in $M _ { \Delta _ { p } w w }$ and $X _ { d d } .$ , so these may also be made decision variables. For example, when expressing the supply rate for $L _ { 2 }$ gain as $s ( d , e ) =$ $\gamma \Vert d \Vert ^ { 2 } - \Vert e \Vert ^ { 2 }$ , this allows minimizing γ subject to (22).
