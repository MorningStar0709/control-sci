Notations: $P ^ { \acute { T } }$ represents the transposition of the P matrix. $\mathbb { R } ^ { \times m }$ is the set of all $n \times m$ real matrices. $P > 0$ for $P \in$ $\mathbb { R } ^ { n \times n }$ means that $P$ is a positive definite matrix. $\lambda _ { \operatorname* { m i n } } ( P )$ and $\lambda _ { \operatorname* { m a x } } ( P )$ represent the minimum and maximum eigenvalues of $P \cdot I _ { n }$ denotes the n-dimensional identity matrix. Let $( \Omega , \mathcal { F } , \mathbb { P } )$ denote a complete probability space with a filtration $\{ \mathcal { F } _ { t } \} _ { t \ge 0 }$ satisfying the usual conditions. For $p , q \in R , p \land q$ and $p \vee$ q represent min $\{ p , q \}$ and max $\{ p , q \} . \ \sigma _ { \operatorname* { m a x } } ( A )$ denotes the maximum singular value of a matrix $A \in \mathbb { R } ^ { m \times n }$ . ${ \bf 1 } _ { N }$ denotes the N-dimensional column vector with all ones.
