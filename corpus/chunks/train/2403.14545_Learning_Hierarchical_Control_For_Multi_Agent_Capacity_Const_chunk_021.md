At the beginning of iteration $r ,$ the safe set $S _ { i , j } ^ { L , r }$ Si,j contains the most recently recorded trajectory between node $v _ { i }$ and node $v _ { j }$ . This need not correspond to iteration $r - 1$ , as only edges assigned by the high-level controller are traveled during an iteration. Note that for each state in $S _ { i , j } ^ { L , r }$ , the stored capacity values have been shifted to begin at 0 at the start of the edge trajectory. Non-capacity states are unaltered.

We refer to the low-level stage cost $h ( x , j )$ introduced in (15) and define a low-level value function $Q _ { i , j } ^ { L , r } ( \cdot )$ :

$$
Q _ {i, j} ^ {L, r} (x) = \left\{ \begin{array}{l l} \sum_ {s = t} ^ {\mathcal {I} (\mathbf {x} ^ {p \star}, i, j, - 1)} h (x _ {s} ^ {p \star}, j) & x \in \mathcal {S} _ {i, j} ^ {L, r} \\ + \infty & \text {else} \end{array} \right.
p \star = \operatorname * {a r g m a x} _ {p \in [ 0, r - 1 ]} p$$

s.t. $\boldsymbol { \mathcal { T } } ( \mathbf { x } ^ { p } , i , j )$ not empty.

The function $Q _ { i , j } ^ { L , r } ( \cdot )$ assigns each state in Si,j $S _ { i , j } ^ { L , r }$ the minimum cost to reach the node $v _ { j }$ along the trajectory stored in $S _ { i , j } ^ { L , r }$ .
