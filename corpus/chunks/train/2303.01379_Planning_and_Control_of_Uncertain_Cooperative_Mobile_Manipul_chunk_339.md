which needs to remain positive for all times in order to achieve the collision avoidance property, i.e., $\Delta _ { k } ( x _ { s _ { k _ { 1 } } } ( t ) , x _ { s _ { k _ { 2 } } } ( t ) ) > 0 , \forall t \in \mathbb { R } _ { \ge 0 } , k \in \bar { \mathcal { K } }$ Note that, in view of Proposition 4.2, the agents $k _ { 1 }$ and $k _ { 2 }$ can calculate (4.42) based on $\Delta _ { k _ { 1 } , k _ { 2 } } ^ { s } ( x _ { s _ { k _ { 1 } } } , x _ { s _ { k _ { 2 } } } )$ and $\Delta _ { k _ { 2 } , k _ { 1 } } ^ { s } ( x _ { s _ { k _ { 2 } } } , x _ { s _ { k _ { 1 } } } )$ , respectively, $\forall s \in \{ x y , x z , y z \} , k \in \bar { \mathcal { K } }$ .

We still need to incorporate the fact the that agents have a limited sensing radius, and that agent i does not have access to the functions $\Delta _ { i , j } ^ { s } ( x _ { s _ { i } } , x _ { s _ { j } } )$ , when $j \notin \mathcal { N } _ { i } ( p )$ . To that end, we define first the greatest lower bound of $\Delta _ { k }$ when both agents $k _ { 1 } , k _ { 2 }$ are in each other’s sensing radius, i.e.,

$$
\widetilde{\Delta}_{k}:= \inf_{\substack{(x_{s_{k_{1}}},x_{s_{k_{2}}})\in \mathbb{M}^{2}\\ \| p_{k_{1}} - p_{k_{2}}\| \leq \min \{\varsigma_{k_{1}},\varsigma_{k_{2}}\}}}\{\Delta_{k}(x_{s_{k_{1}}},x_{s_{k_{2}}})\} ,\forall k\in \bar{\mathcal{K}}. \tag{4.43}
$$

Since $\begin{array} { r } { \varsigma _ { i } > \operatorname* { m a x } \{ l _ { x , i } , l _ { y , i } , l _ { z , i } \} + \operatorname* { m a x } _ { j \in \mathcal { N } } \left\{ \operatorname* { m a x } \{ l _ { x , j } , l _ { y , j } , l _ { z , j } \} \right\} + \varepsilon , \forall i \in \mathcal { N } _ { i } } \end{array}$ , it follows that there exists a positive constant $\varepsilon _ { \Delta }$ such that $\widetilde { \Delta } _ { k } \geq \varepsilon _ { \Delta } > 0 , \forall k \in$ $\bar { \kappa }$ . Next, we define the smooth switching functions $\beta _ { k } : \mathbb { R } _ { \ge 0 }  [ 0 , \bar { \beta } _ { m } ]$ , with [209]

$$\beta_ {k} (z) = \bar {\beta} _ {k} \frac {\sigma (z)}{\sigma (z) + \sigma \left(\bar {\Delta} _ {k} - z\right)}, \tag {4.44}$$
