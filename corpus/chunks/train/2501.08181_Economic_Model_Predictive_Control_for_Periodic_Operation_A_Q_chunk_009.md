# IV. GRADIENT-BASED APPROACH

We note that problem (8) is convex but is not, in general, a QP problem—which can make its implementation difficult. In this paper, inspired by the ideas in [23], we present a gradient-based approximated formulation of the E-MPC for changing periodic operation; this approach relies upon a first-order approximation of the economic objective function, allowing to reduce the control problem complexity to that of a QP program, while maintaining the same properties as the original control problem.

Let the approximated single-layer MPC cost function, $\widetilde { V } _ { k }$ : $\mathbb { R } ^ { n + m N } \times \bar { \mathbb { R } } ^ { n + m T } \times \mathbb { R } ^ { n + \breve { m } T } $ R, be defined as:

$$\widetilde {V} _ {k} (\boldsymbol {z}, \boldsymbol {\zeta}, \hat {\boldsymbol {\zeta}}, p) = S (\boldsymbol {z}, \boldsymbol {\zeta}) + \widetilde {O} _ {k} (\boldsymbol {\zeta}, \hat {\boldsymbol {\zeta}}, p), \tag {9}$$

where $\hat { \boldsymbol { \zeta } }$ is a given feasible trajectory about which we take the first-order Taylor approximation of the economic cost, and $\widetilde { O } _ { k } : \mathbb { R } ^ { n + m T } \times \mathbb { R } ^ { n + m T } $ R is the approximated offset cost, given by the following quadratic cost function:

$$
\begin{array}{l} \widetilde {O} _ {k} (\boldsymbol {\zeta}, \hat {\boldsymbol {\zeta}}, p) = \sum_ {j = 0} ^ {T - 1} \left(\ell_ {k + j} ^ {e} (\hat {\xi} _ {j}, \hat {\nu} _ {j}, p) \right. \\ + \nabla_ {(\xi , \nu)} \ell_ {k + j} ^ {e} (\hat {\xi} _ {j}, \hat {\nu} _ {j}, p) \cdot \left[ (\xi_ {j}, \nu_ {j}) - (\hat {\xi} _ {j}, \hat {\nu} _ {j}) \right] \\ \left. + \frac {\rho}{2} \left\| \left(\xi_ {j}, \nu_ {j}\right) - \left(\hat {\xi} _ {j}, \hat {\nu} _ {j}\right) \right\| ^ {2}\right), \tag {10} \\ \end{array}
$$

where $\rho$ is the Lipschitz constant for $\ell _ { k } ^ { e }$ as per Assumption 3, so it holds that

$$\ell_ {k} ^ {e} (\xi , \nu , p) \leq \ell_ {k} ^ {e} (\hat {\xi}, \hat {\nu}, p) + \nabla_ {(\xi , \nu)} \ell_ {k} ^ {e} (\hat {\xi}, \hat {\nu}, p) \cdot \Big [ (\xi , \nu) - (\hat {\xi}, \hat {\nu}) \Big ]+ \frac {\rho}{2} \left\| (\xi , \nu) - (\hat {\xi}, \hat {\nu}) \right\| ^ {2}, \forall k. \tag {11}$$

Note that $\widetilde O$ is an upper-bound of O. Then, replacing O by $\widetilde O$ in P, the approximated MPC problem is obtained. The optimal solution for this is:
