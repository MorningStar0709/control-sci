# Notation

The symbol $I _ { n }$ denotes the $n \times n$ identity matrix, and $0 _ { n }$ is the $n \times n$ matrix of zeros. For a vector a and a symmetric matrix $B , \| a \| _ { B } ^ { 2 } = a ^ { \top } B a$ . The symbol $\nabla _ { ( \cdot ) }$ denotes the gradient with respect to (·), that is $\begin{array} { r } { \nabla _ { x } f \equiv \frac { \partial f } { \partial x } ^ { \top } = } \end{array}$ ≡ ∂f > $\left( { \frac { \partial f } { \partial { x _ { 1 } } } } , { \frac { \partial f } { \partial { x _ { 2 } } } } , \ldots , { \frac { \partial f } { \partial { x _ { n } } } } \right)$ ∂xn with $( x _ { 1 } , x _ { 2 } , \ldots , x _ { n } ) ^ { \top }$ . For a vector x, the symbol $\| { \boldsymbol { x } } \| _ { p }$ denotes a p-norm of x defined by $\| x \| _ { p } = \left( | x _ { 1 } | ^ { p } + \ldots , + | x _ { n } | ^ { p } \right) ^ { \frac { 1 } { p } }$ and, especially, we omit the subscript in the case $p = 2$ , that is, $\| x \| = \| x \| _ { 2 }$ .
