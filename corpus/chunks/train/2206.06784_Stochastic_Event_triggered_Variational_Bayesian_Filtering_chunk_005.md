# A. Preliminaries

Let P be a symmetric positive definite matrix of random variables, and define the inverse Wishart distribution as

$$\mathrm{IW} (P | g, G) = \frac {\left| G \right| ^ {\frac {g}{2}} \left| P \right| ^ {\frac {- (g + n + 1)}{2}} e ^ {- \frac {1}{2} t r (G P ^ {- 1})}}{2 ^ {\frac {g n}{2}} \Gamma_ {n} (\frac {g}{2})}, \tag {1}$$

where $n , g ,$ , and G denote the dimensions of $P ,$ the degree of freedom (dof), a positive definite scale matrix, respectively, and $\Gamma _ { n }$ is the n-th order gamma function.

Lthat dimensional binary variable with . The categorical distribution is $\lambda _ { j } \in \{ 0 , 1 \}$ such $\begin{array} { r } { \sum _ { j = 1 } ^ { M } \lambda _ { j } = 1 } \end{array}$

$$\operatorname{Cat} (\lambda | \mu , M) = \prod_ {j = 1} ^ {M} [ \mu_ {j} ] ^ {\lambda_ {j}}, \tag {2}$$

$\begin{array} { r } { \sum _ { j = 1 } ^ { M } \mu _ { j } = 1 } \end{array}$ $\boldsymbol { \mu } = ( \mu _ { 1 } , . . . , \mu _ { M } )$ is subject to constraints , the Dirichlet distribution $0 \leq \mu _ { j } \leq 1$ an ds

$$\mathrm{Dir} (\mu , \alpha , M) = C (\alpha) \prod_ {j = 1} ^ {M} [ \mu_ {j} ] ^ {\alpha_ {j} - 1}, \tag {3}$$

where $\begin{array} { r } { \alpha = ( \alpha _ { 1 } , . . . , \alpha _ { M } ) , \hat { \alpha } = \sum _ { j = 1 } ^ { M } \alpha _ { j } , C ( \alpha ) = \frac { \Gamma ( \hat { \alpha } ) } { \Gamma ( \alpha _ { 1 } ) \cdot \cdot \cdot \Gamma ( \alpha _ { M } ) } , } \end{array}$ $\begin{array} { r } { E \{ \mu _ { j } \} = \frac { \alpha _ { j } } { \hat { \alpha } } } \end{array}$ , and $\Gamma ( \cdot )$ j=1  Γ(α1)···Γ(αM ) is the gamma function. The above distributions will be used for the estimation of covariances later.

Variational inference is a method based on optimization to estimate unknown probability densities. Denote the set of all latent unknown variables by $\Lambda = \{ \theta _ { 1 } , . . . , \theta _ { N } \}$ and the set of all observed variables by $Z = \{ Z _ { 1 } , . . . , Z _ { M } \}$ . According to the mean field theory [23], the true distribution $p ( \Lambda | Z )$ can be approximated by $\begin{array} { r } { p ( \Lambda | Z ) \approx \prod _ { k = 1 } ^ { N } q ( \theta _ { k } ) } \end{array}$ , where every $\theta _ { k }$ has its own independent distribution $q ( \theta _ { k } )$ . Moreover, $q ( \theta _ { k } )$ can be designed by minimizing the Kullback-Leibler (KL) divergence between the true distribution and the approximation as follows [24]:
