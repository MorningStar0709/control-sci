Now suppose that $\mathbf { M } ( x ) ~ \preceq ~ 0$ must be imposed on all $x \in \sigma .$ . By assumption, $\mathbf { M } ( x _ { j } ) + \mathbf { E } ( x _ { j } ) \ \bar { \preceq } \ 0$ holds for each vertex of $\sigma \ ( x _ { 0 } , \ldots , x _ { n } )$ . The set of negative semi-definite LMIs is a convex cone [2]. By enforcing $\mathbf { M } ( x _ { j } ) + \mathbf { E } ( x _ { j } ) \preceq 0$ on each vertex, the expression $\begin{array} { r } { \sum _ { j = 0 } ^ { \hat { n } ^ { \prime } } \lambda _ { j } ( \mathbf { M } ( x _ { j } ^ { ' } ) + \mathbf { E } ( x _ { j } ) ) \ \preceq \ 0 } \end{array}$ n also holds. The LMI $\mathbf { M } ( x ) + \mathbf { E } ( x ) \preceq 0$ implies ${ \bf M } ( x ) \preceq 0$ , because $\mathbf { E } ( x ) \succeq 0$ . Therefore, $\dot { \mathbf { M } } ( x ) \preceq 0$ for all $x \in \sigma$ . ✷

The weighing matrix Π in (11) of Theorem 10 allows for a user to have some control over the error bound of the off diagonal terms of $\mathbf { M } ( x )$ and can be treated as a design variable in an optimization problem.

In Theorem 10, functions contained within the MI are linearized about an arbitrary point within a simplex $( x \in$ σ). However, a similar process to Lemma 9 can used to linearize the functions about a fixed vertex, x0, resulting in the corollary stated in Appendix B.
