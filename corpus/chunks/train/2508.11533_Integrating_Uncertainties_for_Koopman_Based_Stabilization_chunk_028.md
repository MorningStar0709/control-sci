# 4.2 Main Results for Indirect Data-Driven Control

Now we introduce our main result for indirect datadriven control approach.

Theorem 2 Consider system (46) identified with data sets $Z _ { 0 } , U _ { 0 } , V _ { 0 } ^ { 1 }$ collected from disturbed system $( 1 7 ) \ ( o r$ equivalently (7)) satisfying Assumption 2. The liftedstate feedback controller $\boldsymbol { u } ^ { \flat } = \boldsymbol { L } ^ { \top } \boldsymbol { P } ^ { \flat - 1 } \boldsymbol { z }$ ensures closedloop stability for (17) also (7) with respect to any possible disturbance, if there exist $\mathbf { \bar { \boldsymbol { 0 } } } ^ { \prime } \prec \boldsymbol { P } = \mathbf { \bar { \boldsymbol { P } } } ^ { \top } \in \mathbb { R } ^ { \check { \boldsymbol { N } } \times \boldsymbol { N } } , \boldsymbol { L }$ ∈ $\mathbb { R } ^ { N \times 1 }$ , and positive scalars $\mu , \alpha , \beta , \tau , \nu$ where $\begin{array} { r } { \frac { 1 } { \alpha } + \frac { 1 } { \beta } = 1 } \end{array}$ , such that $\textstyle P \prec { \frac { \rho } { \mu \nu } } I _ { N }$ and

$$
\begin{array}{l} P \hat {A} ^ {\top} + \hat {A} P + L \hat {B} _ {0} ^ {\top} + \hat {B} _ {0} L ^ {\top} + \mathbf {R} + \mu^ {- 1} \Delta \Delta^ {\top} \\ + \frac {\mu}{\rho} \left(P ^ {2} + L L ^ {\top}\right) + \nu \hat {B} _ {1} P \left(I _ {N} - \frac {\mu \nu}{\rho} P\right) ^ {- 1} \hat {B} _ {1} ^ {\top} \prec 0 \tag {49} \\ \end{array}
$$

where $\rho = 1 / \lambda _ { \operatorname* { m i n } } ( W _ { 0 } W _ { 0 } ^ { \top } )$ ). Moreover, the region of attraction contains the set $\mathbb { X } _ { R o A } = \{ x \in \mathbb { X } : z ^ { \top } P ^ { - 1 } z =$ $\Psi ( x ) ^ { \top } P ^ { - 1 } \Psi ( x ) \leq c \}$ .

PROOF. We start with (40), which is equivalent to (29) if $m \ = \ 1$ . The system is identified by (45) via EDMD. But taking the process disturbance into account, the exact solution is

$$[ A B _ {0} B _ {1} \dots B _ {m} ] = \mathbf {Z} ^ {\top} = (Z _ {1} - D _ {0}) W _ {0} ^ {\dagger} = \hat {\mathbf {Z}} ^ {\top} - D _ {0} W _ {0} ^ {\dagger}. \tag {50}$$

Without any knowledge of disturbance, we can only use the least-square solution (45) for nonlinear stabilization. However, we should actually use (50) to stabilize the original nonlinear system. Therefore, if (40) always holds for any disturbance sequence $D _ { 0 } \in \mathcal { D }$ and corresponding $\mathbf { Z } ^ { \top } = \mathbf { \bar { Z } } ^ { \top } - D _ { 0 } W _ { 0 } ^ { \dagger }$ , we can conclude that the controller actually stabilizes (7).

By the Schur complement, (49) is equivalent to
