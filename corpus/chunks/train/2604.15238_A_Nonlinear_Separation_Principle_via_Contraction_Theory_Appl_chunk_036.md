Proof: Let $\begin{array} { r } { U = \left\lceil C \quad \mathbb { 0 } _ { p \times n } \right\rceil } \end{array}$ . Let $V = \left\lceil \mathbb { O } _ { n \times n } \right\rceil _ { { n } } $ . Let $Y = - M ^ { \top }$ . By continuity, the LMI (56) holds strictly for some $c > 0$ if and only if

$$
\underbrace {\left[ \begin{array}{c c} - 2 P & P + W ^ {\top} Q \\ P + Q W & - 2 Q \end{array} \right]} _ {\mathcal {M}} + U ^ {\top} Y V + V ^ {\top} Y ^ {\top} U \prec 0. \tag {58}
$$

An application of the Projection Lemma 38 states that the inequality (58) holds if and only if $U _ { \mathrm { \scriptscriptstyle 1 } } ^ { \top } { \mathcal { M } } U _ { \bot } \ \prec \ 0$ and $V _ { \mathrm { \Omega } _ { | } } ^ { \top } \mathcal { M } V _ { \bot } ~ \prec ~ 0 \quad$ . The second condition always holds, as the (1,1) block of M is negative definite. The first condition is identical to inequality (57) since $U _ { \bot } = { \left[ \begin{array} { l l } { \Pi _ { C } } & { 0 } \\ { 0 } & { I _ { n } } \end{array} \right] }$ .
