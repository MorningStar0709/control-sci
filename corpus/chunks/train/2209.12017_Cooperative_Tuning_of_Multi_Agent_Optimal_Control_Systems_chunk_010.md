# A. Consensus-based Distributed Optimization

We first suppose the gradient $\frac { d L _ { i } ( \pmb { \xi } _ { i } ( \pmb { \theta } _ { i } ) , \pmb { \theta } _ { i } ) } { d \pmb { \theta } _ { i } }$ is available for each agent i (we shall explain in the next subsection how it can be obtained). Then the problem in (2) becomes a standard consensus-based multi-agent optimization as follows:

$$\min _ {\boldsymbol {\theta} _ {1}, \dots , \boldsymbol {\theta} _ {N}} \frac {1}{N} \sum_ {i = 1} ^ {N} L _ {i} (\boldsymbol {\xi} _ {i} (\boldsymbol {\theta} _ {i}), \boldsymbol {\theta} _ {i}) \tag {3}$$

$\mathrm { s . t . } \qquad \pmb { \theta } _ { 1 } = \cdot \cdot \cdot = \pmb { \theta } _ { N } .$

Let $k = 0 , 1 , \cdots$ denote the iteration index for adjustment of tunable parameters. Let $\pmb { \theta } _ { i } ( k ) \in \mathbb { R } ^ { r }$ denote agent i’s tunable parameter at iteration k. At iteration k, the optimal control sequence ${ \pmb u } _ { i , 0 : T - 1 } ^ { * } ( k )$ for agent-i is computed once based on (1) with current parameter $\theta _ { i } ( k )$ . Then an updated value of $\pmb \theta _ { i } ( k + 1 )$ will be computed using $\theta _ { i } ( k )$ and the optimal control sequence ${ \pmb u } _ { i , 0 : T - 1 } ^ { * } ( k )$ together with other information from agent-i’s neighbors.

We in fact employ the following consensus-based gradient-descent update proposed by [17], [18]:

$$\boldsymbol {\theta} _ {i} (k + 1) = \sum_ {j \in \mathcal {N} _ {i} (k)} w _ {i j} (k) \boldsymbol {\theta} _ {j} (k) - \eta (k) \frac {d L _ {i}}{d \boldsymbol {\theta} _ {i} (k)}. \tag {4}$$

Here,

$$\frac {d L _ {i}}{d \pmb {\theta} _ {i} (k)} = \frac {d L _ {i}}{d \pmb {\theta} _ {i}} | _ {\pmb {\theta} _ {i} = \pmb {\theta} _ {i} (k)}$$

is the gradient of agent-i’s local loss $L _ { i }$ with respect to $\pmb \theta _ { i }$ evaluated at $\theta _ { i } = \theta _ { i } ( k ) ;$ ; and the optimal control sequence ${ \pmb u } _ { i , 0 : T - 1 } ^ { * } ( k )$ is used in evaluating the gradient; further, $\eta ( k ) > 0$ is a diminishing step size for agent-i such that

$$\lim _ {k \to \infty} \eta (k) = 0, \sum_ {k = 0} ^ {\infty} \eta (k) = \infty , \sum_ {k = 0} ^ {\infty} \eta (k) ^ {2} < \infty , \tag {5}$$

and wij (k) are non-negative weights. Let W (k) ∈ RN×N $w _ { i j } ( k )$ $W ( k ) \in \mathbb { R } ^ { N \times N }$ be such that the ij-th entry is $w _ { i j } ( k ) { \mathrm { ~ i f ~ } } j \in { \mathcal { N } } _ { i } ( k )$ and $0 ,$ otherwise. As adopted in [17], [18], we make the following assumption
