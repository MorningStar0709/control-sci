Remark 2.8. Thanks to Remark 2.3, this result still holds if we assume that $\eta > K _ { \mathcal { L } } ^ { b }$ and we drop the L-dissipative assumption. This case is already known in the literature, see for instance [12, Proposition $3 . { \widetilde { 1 } } { \widetilde { } }$ by replacing, in the cited reference, ?? by $\eta - K _ { x } ^ { \sigma }$ and by considering our slightly different assumption on the Lipschitz constants of $\sigma .$ .

Corollary 2.9. Assume that $( \mathcal { H } _ { S D E } 1 )$ is fulfilled. Let $( \beta _ { t } ) _ { t \geq 0 }$ be a bounded progressively measurable process such that, by Girsanov’s theorem, $\begin{array} { r } { W ^ { \tilde { \mathbb { Q } } } : = W - \int _ { 0 } ^ { \cdot } \beta _ { s } \mathrm { d } s } \end{array}$ is $a \tilde { \mathbb { Q } } -$ −Brownian motion $f o r \tilde { \mathbb { Q } }$ the associated Girsanov’s probability. Let us assume that $\nu > K _ { x } ^ { \sigma } + \sqrt { 2 K _ { x } ^ { \sigma } } | \beta | _ { \infty }$ and set $\varepsilon , \gamma$ such that $0 < \varepsilon < \nu - K _ { x } ^ { \sigma } - \sqrt { 2 K _ { x } ^ { \sigma } } | \beta | _ { \infty } , 0 < \gamma < \nu - K _ { x } ^ { \sigma } - \sqrt { 2 K _ { x } ^ { \sigma } } | \beta | _ { \infty } - \varepsilon$ and $\gamma < \gamma - ( K _ { x } ^ { \sigma } + K _ { \mathcal { L } } ^ { \sigma } )$ . Then, there exists $C > 0$ such that, for all $x , x ^ { \prime } \in \mathbb { R } ^ { d } , \theta , \theta ^ { \prime } \in L ^ { 2 } ( \Omega ; \mathbb { R } ^ { d } )$ and $t \geq 0$ ,

$$\mathbb {E} ^ {\tilde {\mathbb {Q}}} \left[ \left| X _ {t} ^ {x, [ \theta ]} - X _ {t} ^ {x ^ {\prime}, [ \theta^ {\prime} ]} \right| ^ {2} \right] ^ {1 / 2} \leq C \left(| x - x ^ {\prime} | + \mathcal {W} _ {2} ([ \theta ], [ \theta^ {\prime} ])\right) e ^ {- \gamma t}. \tag {2.9}$$

Proof. We write the equation satisfied by $X ^ { x , [ \theta ] }$ under $\tilde { \mathbb { Q } }$ as

$$\mathrm{d} X _ {t} ^ {x, [ \theta ]} = b (t, X _ {t} ^ {x, [ \theta ]}, [ X _ {t} ^ {\theta} ]) \mathrm{d} t + \sigma (X _ {t} ^ {x, [ \theta ]}, [ X _ {t} ^ {[ \theta ]} ]) \beta_ {t} \mathrm{d} t + \sigma (X _ {t} ^ {x, [ \theta ]}, [ X _ {t} ^ {[ \theta ]} ]) \mathrm{d} W _ {t} ^ {\tilde {\mathbb {Q}}}.$$

Then, by applying Itˆo’s formula to $e ^ { 2 \gamma t } \bigl | \tilde { X } _ { t } \bigr | ^ { 2 }$ , with $\tilde { X } = X ^ { x , [ \theta ] } - X ^ { x ^ { \prime } , [ \theta ^ { \prime } ] }$ , we have by using $( \mathcal { H } _ { S D E } 1 ) – 2$ , Young’s inequality, the

fact that $\nu \leq \eta$ and Theorem 2.7,
