# 复习题

4.1 给出反馈控制的三个优点。

4.2 给出反馈控制的两个缺点。

4.3 已知温度控制系统在常值跟踪输入信号作用下输出误差为零；当跟踪输入信号以 $40^{\circ}C/s$ 的上升速率随时间线性变化时，系统的输出稳态误差为 $0.5^{\circ}C$ 。试计算该控制系统的类型和误差常数 ( $K_{p}$ 或 $K_{v}$ 或 $K_{a}$ )。

4.4 $K_{\mathrm{p}}$ ， $K_{\mathrm{v}}$ 和 $K_{\mathrm{n}}$ 的单位是什么？

4.5 如何用参考输入来定义系统的类型。

4.6 如何用干扰输入来定义系统的类型。

4.7 为什么系统类型取决于外部信号作用于系统的位置？
