It can be seen that the expression of subspace is similar to that of fixed topology, so we can get the following results by using the concept of EP.

Theorem 2. For system (2), if the matrix 𝑄 always exists for all ${ \tilde { L } } _ { i }$ corresponding to $G _ { i } ,$ , the dimension of its controllable state set  satisfies

$$\dim (\mathcal {C}) \leq \operatorname{card} \left(\pi_ {\min}\right) \cdot d$$

PROOF. Since ${ \mathcal { C } } = \mathbb { W } _ { N }$ Tian et al. (2019), the conclusion is equivalent to dim $\left( \mathbb { W } _ { N } \right) \leq \mathrm { c a r d } \left( \pi _ { m i n } \right) \cdot d .$ . According to Lemma 2, if 𝑄 always exists for all ${ \tilde { L } } _ { i }$ corresponding to $G _ { i } ,$ , so for any $\pi _ { i } , i \in \{ 1 , 2 , \ldots , t \} , \operatorname { i m } \left( P _ { i } \right)$ is ${ \tilde { L } } _ { i }$ -invariant, where $P _ { i }$ is the characteristic matrix of $\pi _ { i }$ .

$$
\begin{array}{l} \mathbb {W} _ {1} = \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \operatorname{im} (\tilde {M}) \\ \subseteq \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \operatorname{im} \left(P _ {i}\right) = \operatorname{im} \left(P _ {i}\right) \\ \mathbb {W} _ {2} = \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \mathbb {W} _ {1} \subseteq \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \operatorname{im} \left(P _ {i}\right) = \operatorname{im} \left(P _ {i}\right) \\ \mathbb {W} _ {N} = \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \mathbb {W} _ {N - 1} \\ \subseteq \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \operatorname{im} \left(P _ {i}\right) = \operatorname{im} \left(P _ {i}\right) \\ \end{array}

\begin{array}{l} \mathbb {W} _ {N} = \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \mathbb {W} _ {N - 1} \\ \subseteq \sum_ {i = 1} ^ {t} \sum_ {m = 0} ^ {N - 1} \left(\tilde {L} _ {i}\right) ^ {m} \operatorname{im} \left(P _ {i}\right) = \operatorname{im} \left(P _ {i}\right) \\ \end{array}
$$

That is to say, dim $\left( \mathbb { W } _ { N } \right) \leq \mathrm { d i m } \left( \mathrm { i m } \left( P _ { i } \right) \right) = \mathrm { c a r d } ( P _ { i } ) \cdot d$ , and then

$$\dim \left(\mathbb {W} _ {N}\right) \leq \operatorname{card} \left(\pi_ {\min}\right) \cdot d.$$

This completes the proof.

Remark 8. Theorem 2 shows that the dimension of the controllable subspace is limited by the nontrivial cell contained in each subgraph. This reflects the influence of switching topology on the controllability analysis based on the concept of equitable partition.
