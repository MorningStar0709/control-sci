# 7 Proof of Theorem 4.1

We first consider the following transformed system:

$$
\left\{ \begin{array}{l} \tilde {z} _ {t} (x, t) = \tilde {z} _ {x x} (x, t), \\ \tilde {z} _ {x} (0, t) = 0, \tilde {z} _ {x} (1, t) = - b \tilde {\zeta} (t) u _ {0} (t) - c _ {1} \tilde {z} (1, t), \\ \dot {\tilde {\zeta}} (t) = \operatorname{sgn} (b) \tilde {z} (1, t) [ d (t) - (q + c _ {0}) \tilde {z} (1, t) ], d \in L ^ {\infty} (0, \infty), \\ \check {z} _ {t} (x, t) = \check {z} _ {x x} (x, t), \\ \check {z} _ {x} (0, t) = - q \tilde {z} (0, t), \check {z} _ {x} (1, t) = - c _ {0} \check {z} (1, t) + c _ {1} \tilde {z} (1, t). \end{array} \right. \tag {7.1}
$$

Lemma 7.1. Let $b \neq 0 , c _ { 0 } > 0 , c _ { 1 } > 0$ and d $\in L ^ { \infty } ( 0 , \infty )$ . Then for any initial state $( \tilde { z } ( \cdot , 0 ) , \check { z } ( \cdot , 0 ) , \tilde { \zeta } ( 0 ) ) \in$ $\mathcal { H } ^ { 2 } \times \mathbb { R } ,$ , system (7.1) admits a weak solution $( \tilde { z } ( \cdot , t ) , \check { z } ( \cdot , t ) , \tilde { \zeta } ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ such that

$$\int_ {0} ^ {\infty} \left[ \tilde {z} ^ {2} (0, t) + \tilde {z} ^ {2} (0, t) + \tilde {z} ^ {2} (1, t) + \tilde {z} ^ {2} (1, t) \right] d t < + \infty , \tag {7.2}$$

and

$$\sup _ {t \geq 0} \left[ \| (\tilde {z} (\cdot , t), \check {z} (\cdot , t)) \| _ {\mathcal {H} ^ {2}} + | \tilde {\zeta} (t) | \right] < + \infty . \tag {7.3}$$

If we assume further that there exists a time $\tau > 0$ such that

$$\lim _ {t \to + \infty} \int_ {t} ^ {t + \tau} d (s) d s \neq 0, \tag {7.4}$$

then

$$\tilde {\zeta} (t) \rightarrow 0 \quad a s \quad t \rightarrow + \infty . \tag {7.5}$$

Proof. By the Galerkin approximation, for any initial state $( \tilde { z } ( \cdot , 0 ) , \check { z } ( \cdot , 0 ) , \tilde { \zeta } ( 0 ) ) \in \mathcal { H } ^ { 2 } \times$ R, system (7.1) admits a weak solution $( \tilde { z } ( \cdot , t ) , \check { z } ( \cdot , t ) , \tilde { \zeta } ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ such that

$$\int_ {0} ^ {\infty} \int_ {0} ^ {1} \tilde {z} _ {x} ^ {2} (x, t) + \check {z} _ {x} ^ {2} (x, t) d x + \tilde {z} ^ {2} (1, t) + \check {z} ^ {2} (1, t) d t < + \infty , \tag {7.6}$$

which leads to (7.2) directly due to the Sobolev trace-embedding. Since the proof is almost the same as Lemma 6.1, we omit it due to the space limitation. We only consider the stability.

Let $u _ { 0 } ( t ) = d ( t ) - ( q + c _ { 0 } ) \check { z } ( 1 , t )$ . Since (7.6) and (7.1), we have $d \in L ^ { \infty } ( 0 , \infty )$ and $\check { z } ( 1 , \cdot ) \in$ $L ^ { 2 } ( 0 , \infty )$ . By exploiting Lemma 5.2 that $\tilde { z } ( 0 , \cdot ) \in L ^ { 2 } ( 0 , \infty )$ and
