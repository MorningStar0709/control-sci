# D.1 Proof of Lemma 12

proof. First, we will prove some results about $a ^ { * } , b ^ { * } , K _ { \mathrm { o p t } } ( \theta ^ { * } , t )$ . Because $b , b ^ { * } \geq \underline { { b } }$ and $\lVert \theta -$ $\begin{array} { r } { \theta ^ { * } \| _ { \infty } = \epsilon \le \frac { 1 } { \log ^ { 4 6 } ( T ) } < b / 2 } \end{array}$ for large enough $T$ , we have that

$$\left| \left(\frac {a ^ {*}}{b ^ {*}}\right) ^ {2} - \left(\frac {a}{b}\right) ^ {2} \right| = \left| \frac {(a ^ {*}) ^ {2} b ^ {2} - (b ^ {*}) ^ {2} a ^ {2}}{b ^ {2} (b ^ {*}) ^ {2}} \right| \leq \frac {\epsilon^ {2} b ^ {2} + 2 \epsilon a b ^ {2} + 2 \epsilon b a ^ {2} + \epsilon^ {2} a ^ {2}}{b ^ {2} (b - \epsilon) ^ {2}} = O _ {T} (\epsilon). \tag {41}\left| \frac {a}{b} - \frac {a ^ {*}}{b ^ {*}} \right| = \left| \frac {a ^ {*} b - b ^ {*} a}{b b ^ {*}} \right| \leq \left| \frac {\epsilon b + \epsilon a}{b (b - \epsilon)} \right| = O _ {T} (\epsilon). \tag {42}$$

Let $K ^ { \prime }$ be the solution to $a ^ { * } - b ^ { * } K _ { \mathrm { o p t } } ( \theta ^ { * } , t ) = a - b K ^ { \prime }$ . Then

$$K ^ {\prime} = \frac {(a - a ^ {*}) + b ^ {*} K _ {\mathrm{opt}} (\theta^ {*} , t)}{b} = K _ {\mathrm{opt}} (\theta^ {*}, t) + \frac {(b ^ {*} - b) K _ {\mathrm{opt}} (\theta^ {*} , t)}{b} + \frac {a - a ^ {*}}{b}.$$

Since $\begin{array} { r } { K _ { \mathrm { o p t } } ( \theta ^ { * } , t ) \le \frac { a ^ { * } } { b ^ { * } } } \end{array}$ by definition, we have the following two equations:

$$\left| K ^ {\prime} - K _ {\mathrm{opt}} \left(\theta^ {*}, t\right) \right| = \left| \frac {(b ^ {*} - b) K _ {\mathrm{opt}} \left(\theta^ {*} , t\right)}{b} + \frac {a - a ^ {*}}{b} \right| \leq \left(\frac {a ^ {*}}{b b ^ {*}} + \frac {1}{b}\right) \epsilon = O _ {T} (\epsilon). \tag {43}\left| \left(K ^ {\prime}\right) ^ {2} - \left(K _ {\text {opt}} \left(\theta^ {*}, t\right)\right) ^ {2} \right| \leq \left| K ^ {\prime} - K _ {\text {opt}} \left(\theta^ {*}, t\right) \right| \cdot \left| K ^ {\prime} + K _ {\text {opt}} \left(\theta^ {*}, t\right) \right| = O _ {T} (\epsilon). \tag {44}$$
