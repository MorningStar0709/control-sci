# II. PRELIMINARIES & SETUP

The set of integers in the interval $[ a , b ] \subset \mathbb { R }$ is denoted by $\mathbb { I } _ { [ a , b ] }$ and the set of integers greater than or equal to a by $\mathbb { I } _ { \geq a } .$ .

For a vector $x = [ x _ { 1 } \ldots x _ { n } ] ^ { \top } \in \mathbb { R } ^ { n }$ and a symmetric positive definite matrix P , we write $| x | _ { P } = { \sqrt { x ^ { \top } P x } }$ . The minimal eigenvalue of P is denoted by $\lambda _ { \operatorname* { m i n } } ( P )$ . The identity matrix of dimension n is denoted by $I _ { n }$ . The Euclidean norm $| | x | | _ { 2 }$ is written as |x| and the infinity norm $| | x | | _ { \infty }$ as $| x | _ { \infty } . \mathrm { A }$ stacked window of a sequence $\{ x ( \boldsymbol { k } ) \} _ { k = 0 } ^ { N - 1 }$ is written as $x _ { [ 0 , N - 1 ] } =$ $\begin{array} { r } { \left[ x ( 0 ) ^ { \top } \quad \ldots \quad x ( N - 1 ) ^ { \top } \right] ^ { \top } } \end{array}$ ⊤. A function $\gamma : \mathbb { R } _ { \geq 0 }  \mathbb { R } _ { \geq 0 }$ is of class K, if $\gamma$ is continuous, strictly increasing, and $\gamma ( 0 ) = 0$ . The maximum of two scalars $a , b \in \mathbb { R }$ is denoted by max $\{ a , b \}$ . Furthermore, the Hankel matrix of depth L of a stacked window $x _ { [ 0 , N - 1 ] }$ is defined by

$$
H _ {L} (x _ {[ 0, N - 1 ]}) = \left( \begin{array}{c c c c} x (0) & x (1) & \ldots & x (N - L) \\ x (1) & x (2) & \ldots & x (N - L + 1) \\ \vdots & \vdots & \ddots & \vdots \\ x (L - 1) & x (L) & \ldots & x (N - 1) \end{array} \right).
$$

All of our results are crucially based on Willems’ fundamental lemma [8]. It holds for LTI-DT systems of the form

$$x (t + 1) = A x (t) + B u (t) \tag {1a}y (t) = C x (t) + D u (t), \tag {1b}$$

where $u ( t ) \in \mathbb { R } ^ { m } , x ( t ) \in \mathbb { R } ^ { n }$ , and $\ b { y } ( t ) \in \mathbb { R } ^ { p }$ . The fundamental lemma exploits the notion of persistency of excitation.

Definition 1: An input sequence $\{ u ( k ) \} _ { k = 0 } ^ { N - 1 }$ is persistently exciting of order L if rank $( H _ { L } ( u _ { [ 0 , N - 1 ] } ) ) = m L$ .

Besides the fundamental lemma, the authors in [8] prove the following lemma.

Lemma 1: Suppose $\{ x ( k ) \} _ { k = 0 } ^ { N - 1 } , \{ u ( k ) \} _ { k = 0 } ^ { N - 1 }$ is an in-$\{ u ( k ) \} _ { k = 0 } ^ { N - 1 }$ ajectory of the controllable LTI s is persistently exciting of order $L + n$ (1) where, then the matrix

$$
\left[ \begin{array}{l} H _ {1} (x _ {[ 0, N - L ]}) \\ H _ {L} (u _ {[ 0, N - 1 ]}) \end{array} \right] \tag {2}
$$

has full row rank.

The fundamental lemma can be formulated in the classical state space framework as follows [21], [22].
