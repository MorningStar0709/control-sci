# II. BACKGROUND

Notation: We let $\mathbb { O } _ { n \times m }$ be the $n \times m$ all zero matrix, $I _ { n }$ be the $n \times n$ identity matrix. For symmetric A, we write $A \ \succ \ 0$ (respectively, $A ~ \succeq ~ 0 )$ if A is positive definite (respectively, semidefinite); $A \ \succ \ B$ means $A \ : - \ : B \ : \succ \ : 0 .$ The opposite relations $\prec , \preceq$ are defined analogously. Given an n × n matrix $P \succ 0 ,$ , we let $\| \cdot \| _ { P }$ denote the P -weighted Euclidean norm on $\mathbb { R } ^ { n }$ , defined by $\| x \| _ { P } : = { \sqrt { x ^ { \top } P x } }$ . Given a norm ∥ · ∥X on $\mathbb { R } ^ { n }$ , and $\| \cdot \| _ { y }$ on $\mathbb { R } ^ { m }$ , and a matrix $A \in \mathbb { R } ^ { m \times n }$ , we denote the induced matrix norm $\| A \| _ { \mathcal { X } \to \mathcal { Y } } =$ sup∥x∥X =1 X $\textstyle \operatorname* { s u p } _ { \| x \| _ { \mathcal { X } } = 1 } { \frac { \| A x \| _ { \mathcal { V } } } { \| x \| _ { \mathcal { X } } } }$ ∥Ax∥Y∥x∥ . Given two normed spaces (X , ∥ · ∥X ) and $( { \mathcal { X } } , \| \cdot \| _ { \mathcal { X } } )$ $( { \mathcal { V } } , \parallel \cdot \parallel _ { \mathcal { V } } )$ , a map $F \colon \mathcal { X } \  \ \mathcal { y }$ is Lipschitz with constant $\rho \geq 0 .$ , if for all $x , \tilde { x } \in \mathcal { X } , \| F ( x ) - F ( \tilde { x } ) \| _ { \mathcal { V } } \leq \rho \| x - \tilde { x } \| _ { \mathcal { X } } .$ We denote by $\mathsf { L i p } ( F )$ the (smallest) Lipschitz constant of $F .$ . If a map depends on several variables, e.g., x and u, we write $\mathsf { L i p } _ { x } ( F )$ for the Lipschitz constant with respect to the corresponding variable. We let diag $( A _ { 1 } , A _ { 2 } , \cdots , A _ { n } )$ denote the block-diagonal matrix, where each $A _ { i }$ is either a matrix or a scalar. For each matrix $A \in \mathbb { R } ^ { m \times n }$ , let $A _ { \perp }$ denote any full-column-rank matrix satisfying Im $A _ { \perp } = \operatorname { K e r } ( A )$ , that is, a matrix whose columns form a basis of the kernel of A. Given a matrix $X \in \mathbb { R } ^ { n \times m }$ , we use $\mathbf { v e c } ( X ) \in \mathbb { R } ^ { n m }$ for its columnwise vectorization. We denote the upper right Dini derivative of the function $f \ D ^ { + }$
