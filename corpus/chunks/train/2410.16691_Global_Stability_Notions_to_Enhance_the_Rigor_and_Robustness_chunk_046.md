Theorem 9 (LaSalle-like conditions for the p-OAG property): Suppose that for every $x _ { 0 } \in \mathbb { R } ^ { n }$ and $d \in L ^ { \infty } \left( \mathbb { R } _ { + } ; D \right)$ the solution of (36) with initial condition $x ( 0 ) = x _ { 0 }$ is bounded. Moreover, suppose that there exist a non-decreasing function $b \in C ^ { 0 } \left( \mathbb { R } _ { + } ; \mathbb { R } _ { + } \right)$ and families of functions $H _ { s } \in C ^ { 1 } \left( \mathbb { R } ^ { n } \right) , \ Q _ { s } \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ being locally Lipschitz, parameterized by $s \geq 0$ ， such that the following properties hold for all $s \geq 0 , \ x \in \mathbb { R } ^ { n }$ :

$$Q _ {s} (x) = 0 \Rightarrow | h (x) | \leq b (s) \tag {115}\max \left\{\nabla H _ {s} (x) f (x, d): d \in D, | d | \leq s \right\} \leq - Q _ {s} (x) \tag {116}$$

Then system (36), (2) satisfies the p-OAG property with asymptotic residual constant $\tilde { \alpha } = b ( 0 )$ and asymptotic gain function $\tilde { \gamma } ( s ) = b ( s ) - b ( 0 )$

Before we proceed with the proof of Theorem 9 it is worth noticing some things about Theorem 9. First, the family of functions $H _ { s } \in C ^ { 1 } \left( \mathbb { R } ^ { n } \right)$ parameterized by $s \geq 0$ is not a family of positive definite (or even non-negative) functions. Secondly, we say that $H _ { s } \in C ^ { 1 } \left( \mathbb { R } ^ { n } \right) , \ Q _ { s } \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ are families of functions $H _ { s } \in C ^ { 1 } \left( \mathbb { R } ^ { n } \right) , \ Q _ { s } \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ , parameterized by $s \geq 0$ ，instead of saying that $H , Q$ are functions of $\left( \boldsymbol { x } , \boldsymbol { s } \right) \in \mathbb { R } ^ { n } \times \mathbb { R } .$ because there is no regularity requirement with respect to $s \geq 0$ ; the functions $H _ { s } \in C ^ { 1 } \left( \mathbb { R } ^ { n } \right) , \ Q _ { s } \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ are not assumed to be continuous (or even measurable) with respect to $s \geq 0$ . When (60) and

$$V (x) \geq \varphi (\left| d \right|) \Rightarrow \dot {V} (x, d) = \nabla V (x) f (x, d) \leq - c (x, | d |) \tag {117}$$
