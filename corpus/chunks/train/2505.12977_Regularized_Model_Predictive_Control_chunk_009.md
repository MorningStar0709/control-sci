$$
\mathcal {A} _ {2} = \left[ \begin{array}{c} - I \\ \bar {A} \end{array} \right], \quad \mathcal {B} _ {2} = \left[ \begin{array}{c c} 0 & 0 \\ \bar {B} _ {1} & - \bar {B} _ {2} \end{array} \right], \tag {14}
$$

with $\bar { A } = \mathrm { c o l } \{ A , 0 , \ldots , 0 \} \in \mathbb { R } ^ { \bar { n } \times n } , \bar { B } _ { 2 } = \mathrm { d i a g } \{ B , \ldots , B \} \in$ $\mathbb { R } ^ { \bar { n } \times \bar { m } }$ , and $\begin{array} { r l r } { \bar { B } _ { 1 } } & { { } = } & { I - \tilde { A } \quad \in \quad \mathbb { R } ^ { \bar { n } \times \bar { n } } } \end{array}$ , where $\begin{array} { r l } { \tilde { A } } & { { } = } \end{array}$ $\operatorname { d i a g } \{ A , \dots , A \} S _ { \bar { n } } \in \mathbb { R } ^ { \bar { n } \times \bar { n } }$ in which ${ \bar { n } } = l n .$ , and $\bar { m } = l m$ Also, the augmented matrix $\mathcal { H } = \left( H _ { 1 } \oplus H _ { 2 } \right)$ encodes the weighting matrices as

$$
H _ {1} = \left[ \begin{array}{c c} \bar {Q} _ {k + l} & 0 \\ 0 & \bar {R} \end{array} \right], \quad H _ {2} = \left[ \begin{array}{c c} Q & 0 \\ 0 & \mu I \end{array} \right], \tag {15}
$$

where $\bar { Q } _ { k + l } = \mathrm { d i a g } \{ Q , \ldots , Q , P _ { k + l } \} \in \mathbb { R } ^ { \bar { n } \times \bar { n } }$ and $\bar { R } \ =$ $\mathrm { d i a g } \{ R , \ldots , R \} \in \mathbb { R } ^ { \tilde { M } \times \tilde { M } }$ . Furthermore, the minimum cost associated with the optimal solution is $J ^ { * } = x _ { k | k } ^ { \mathsf { T } } P _ { k + l - 1 } x _ { k | k } .$ where $P _ { k + l - 1 } = K _ { k } ^ { \mathsf { T } } H _ { 1 } K _ { k } + Q + \mu \mathscr { R } ^ { \mathsf { T } } \mathscr { R }$ with $\dot { \mathcal { R } } = \bar { B } _ { 1 } K _ { X _ { k } } -$ $\bar { B } _ { 2 } K _ { U _ { k } } - \bar { A }$ representing the residual term.

Proof: When all inequality constraints are inactive, we can rewrite the problem (5) as

$$\underset {U _ {k}, X _ {k}} {\arg \min} \quad X _ {k} ^ {\top} \bar {Q} _ {k + l} X _ {k} + U _ {k} ^ {\top} \bar {R} U _ {k} + x _ {k | k} ^ {\top} Q x _ {k | k}, \tag {16}\text { s.t. } \quad \bar {B} _ {1} X _ {k} = \bar {A} x _ {k | k} + \bar {B} _ {2} U _ {k},$$

To derive the exact optimal solution (12), we rewrite the constrained optimization problem (16) in an alternative form with a large penalty parameter $\mu ,$ given by
