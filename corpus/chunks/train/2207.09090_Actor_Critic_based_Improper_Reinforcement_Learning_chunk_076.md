Next we observe that the value function of $\pi _ { \theta _ { \alpha } }$ :

$$V ^ {\pi_ {\theta_ {\alpha}}} (s) = \underbrace {\sum_ {a \in \mathcal {A}} \pi_ {\theta_ {\alpha}} (a | s) r (s , a)} _ {r _ {\theta_ {\alpha}}} + \gamma \sum_ {a \in \mathcal {A}} \pi_ {\theta_ {\alpha}} (a | s) \sum_ {s ^ {\prime} \in \mathcal {S}} \mathrm{P} (s ^ {\prime} | s, a) V ^ {\pi_ {\theta_ {\alpha}}} (s ^ {\prime}).$$

In matrix form,

$$
\begin{array}{l} V ^ {\pi_ {\theta_ {\alpha}}} = r _ {\theta_ {\alpha}} + \gamma P (\alpha) V ^ {\pi_ {\theta_ {\alpha}}} \\ \Longrightarrow (I d - \gamma P (\alpha)) V ^ {\pi_ {\theta_ {\alpha}}} = r _ {\theta_ {\alpha}} \\ V ^ {\pi_ {\theta_ {\alpha}}} = \left(I d - \gamma P (\alpha)\right) ^ {- 1} r _ {\theta_ {\alpha}}. \\ \end{array}
$$

Let $M ( \alpha ) : = \left( I d - \gamma P ( \alpha ) \right) ^ { - 1 } = \sum _ { t = 0 } ^ { \infty } \gamma ^ { t } [ P ( \alpha ) ] ^ { t }$ . Also, observe that

$$
\begin{array}{l} \mathbf {1} = \frac {1}{1 - \gamma} (I d - \gamma P (\alpha)) \mathbf {1} \implies M (\alpha) \mathbf {1} = \frac {1}{1 - \gamma} \mathbf {1}. \\ \Longrightarrow \forall i \| [ M (\alpha) ] _ {i,:} \| _ {1} = \frac {1}{1 - \gamma} \\ \end{array}
$$

where $[ M ( \alpha ) ] _ { i , : }$ : is the $i ^ { t h }$ row of $M ( \alpha )$ . Hence for any vector $\begin{array} { r } { x \in \mathbb { R } ^ { S } , \| M ( \alpha ) x \| _ { \infty } \leqslant \frac { 1 } { 1 - \gamma } \| x \| _ { \infty } } \end{array}$ .

By assumption I.6, we have $\left\| r _ { \theta _ { \alpha } } \right\| _ { \infty } = \operatorname* { m a x } _ { s } \left| r _ { \theta _ { \alpha } } ( s ) \right| \leqslant 1$ . Next we find the derivative of $r _ { \theta _ { c } }$ α w.r.t α.
