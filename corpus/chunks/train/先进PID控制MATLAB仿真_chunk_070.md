# 1.2.1 基本的PID控制

以二阶线性传递函数 $\frac{133}{s^2 + 25s}$ 为被控对象，进行模拟PID控制。在信号发生器中选择正弦信号，仿真时取 $k_{\mathrm{p}} = 60$ ， $k_{\mathrm{i}} = 1$ ， $k_{\mathrm{d}} = 3$ ，输入指令为 $y_{\mathrm{d}}(t) = A\sin (2\pi Ft)$ ，其中 $A = 1.0$ ， $F = 0.20\mathrm{Hz}$ 。采用ODE45迭代方法，仿真时间为10s。
