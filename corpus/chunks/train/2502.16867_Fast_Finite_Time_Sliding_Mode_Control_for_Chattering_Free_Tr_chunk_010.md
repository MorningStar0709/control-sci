# 5 Control law design

Due to the similarity of the controller designing principle for all three arms, control designing is only discussed for the first arm’s angular position, and the repetition of similar content is avoided.

and the deviation of equation (9), the following equation is obtained:

$$\dot {S} _ {1} (x) = \ddot {e} _ {1} + \dot {e} _ {1} (\alpha + \beta \frac {p}{q} (e _ {1}) ^ {\frac {p}{q} - 1}) \tag {11}$$

Assuming that the sliding surface equation is defined based on state variables and desired state, the system converges to the desired shape on the sliding surface. So, it can be said that the following relations are established on the sliding surface:

$$
\left\{ \begin{array}{l} S _ {1} = 0 \\ \dot {S} _ {1} = 0 \end{array} \right. \tag {12}
$$

According to the sliding mode theory, the control input u is defined as follows [22], [13], [23]:

$$u = u _ {e q} + u _ {s} \tag {13}$$

So that $u _ { e q }$ is the equivalent control component and maintains the states on the sliding surface, and $u _ { s }$ is the switching and directs the states to the sliding surface, which $u _ { s }$ is responsible for stabilizing the system and is determined by the Lyapunov stability criterion. We adopted the sliding mode control methodology discussed by Samaei et al. [24], which provides an approach to ensuring both asymptotic convergence to the sliding surface and robust maintenance of the system’s trajectory within that surface. By setting $\dot { s _ { 1 } } = 0$ , the equivalent control law is obtained as follows:

$$
\begin{array}{l} \dot {S} _ {1} (x) = \ddot {e} _ {1} + \dot {e} _ {1} \left(\alpha + \beta \frac {p}{q} e _ {1} ^ {\frac {p}{q} - 1}\right) = 0 \Rightarrow \\ f _ {1} (x) + g _ {1} (x) u _ {1 e q} + \dot {e} _ {1} \left(\alpha + \beta \frac {p}{q} e _ {1} ^ {\frac {p}{q} - 1}\right) = 0 \\ \Rightarrow u _ {1 e q} = \frac {- f _ {1} (x) - \dot {e} _ {1} \left(\alpha + \beta \frac {p}{q} e _ {1} ^ {\frac {p}{q} - 1}\right)}{g _ {1} (x)} \tag {14} \\ \end{array}
$$

Thus, a suitable candidate for the switching control law is shown in the following equation:

$$u _ {1 s} = - k _ {1} \operatorname{sgn} \left(S _ {1}\right) \tag {15}$$

where $k _ { 1 }$ is a positive real value. By combining equations (14), (15), (18)-(21) and inserting them into equation (13), the general law of fast terminal sliding mode robust control, which is applied to the first arm of the robot to follow the desired reference angle, is:
