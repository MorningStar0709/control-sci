# 4.1 Online Learning Algorithm

We propose using the online learning algorithm (Algorithm 1), which employs the Online Gradient Descent (OGD) method. Specifically, in line 5 of the algorithm, $\Pi _ { K _ { N } } ( \cdot )$ denotes the projection of a matrix $N \in { \mathbb { R } } ^ { p \times ( p h ) }$ onto the set $\kappa _ { N }$ defined in (19). Noting that $\| N \| _ { F } = \| v e c ( N ) \|$ for any onto the p2h-dimensional ball with radius $N \in \mathbb { R } ^ { p \times ( p h ) }$ , the projection of $N \in \mathbb { R } ^ { p \times ( p h ) }$ $\sqrt { p } \psi \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } }$ onto the set centered around the origin. Therefore, we see that $\kappa _ { N }$ is equivalent to the projection of $v e c ( N )$

the projection $\Pi _ { K _ { N } } ( \cdot )$ in line 5 of Algorithm 1 can be computed as $\begin{array} { r } { \Pi _ { \mathcal { K } _ { N } } ( N ) = \sqrt { p } \psi \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } } N / \left. N \right. _ { F } } \end{array}$ for all $N \in \mathbb { R } ^ { p \times ( p h ) }$ .

We now characterize the regret $\mathtt { R _ { y } } ( T )$ defined in (20) of Algorithm 1 when applied to solve the problem of learning Kalman filtering for output estimation.

Theorem 4.1. Let the step sizes be $\begin{array} { r } { \eta _ { t } = \frac { 2 } { ( \alpha _ { 0 } \log ^ { 2 } T ) t } } \end{array}$ for all $t \geq 1$ and $\eta _ { 0 } = 0$ . Let the length h of the truncated filter $\hat { y } _ { t } ^ { \mathrm { T K } }$ given by (17) be $h = \lfloor \log T / \log ( 1 / \gamma _ { F } ) \rfloor$ , where $\gamma _ { F }$ is defined in (14). Then, for any $T > 1$ and any $\delta \in ( 0 , 1 )$ , it holds that $\mathsf { R } _ { y } ( T ) = \mathcal { O } ( \log ^ { 4 } T )$ , where $\mathcal { O } ( \cdot )$ hides polynomial factors in log 1δ and problem parameters p, n, σ, α ¯ −10 , α1, ψ, κA1−κA , 1log(2¯σ/(2¯σ−α0)) . $\frac { 1 } { \delta }$ $\begin{array} { r } { p , n , \bar { \sigma } , \alpha _ { 0 } ^ { - 1 } , \alpha _ { 1 } , \psi , \frac { \kappa _ { A } } { 1 - \kappa _ { A } } , \frac { 1 } { \log ( 2 \bar { \sigma } / ( 2 \bar { \sigma } - \alpha _ { 0 } ) ) } . } \end{array}$ KA 3
