Equations (18) and (19) together imply that for sufficiently large $T , \ A _ { j + 1 }$ holds conditional on $A _ { j } \cap E _ { 1 } ^ { t } \cap E ^ { * } ( x , K , \theta , W ^ { \prime } )$ . Note that $A _ { 0 }$ always holds by definition because $d _ { 0 } = d .$ . Therefore, we can conclude by induction that $A _ { t }$ must hold conditional on $E _ { 1 } ^ { t } \cap$ $E ^ { * } ( x , K , \theta , W ^ { \prime } )$ for sufficiently large $T$ . Finally, by definition of $A _ { t } .$ , this implies that conditional on $E ^ { * } ( x , K , \theta , W ^ { \prime } ) \cap E _ { 1 } ^ { t }$ for sufficiently large $T .$ , for all $0 \le j \le t$ ,

$$
d _ {j} \leq \left\{ \begin{array}{l l} \left(1 - \frac {1}{\log^ {1 0} (T)}\right) ^ {j} \cdot d, & \text { if } j > \log^ {3 3} (T) \\ d + \tilde {O} _ {T} (\epsilon), & \text { if } j \leq \log^ {3 3} (T). \end{array} \right. \tag {20}
$$

Taking $\begin{array} { r } { E _ { \mathrm { L 5 } } ( x , K , \theta , W ^ { \prime } ) = E ^ { * } ( x , K , \theta , W ^ { \prime } ) \cap E _ { 1 } ^ { t } } \end{array}$ , by a union bound we have that $\mathbb { P } ( E _ { \mathrm { L 5 } } ( x , K , \theta , W ^ { \prime } ) \ge$ $1 - o _ { T } ( 1 / T ^ { 2 0 } )$ . This completes the proof of Lemma 5 for Case 1.

Case 2: $\begin{array} { r } { K \in \left[ \frac { a - 1 } { b } , \frac { a ^ { * } - 1 + \frac { 1 } { \log ^ { 9 } ( T ) } } { b ^ { * } } \right] } \end{array}$

Define

$$\mathcal {W} _ {j} := \left\{\min (x _ {j}, y _ {j}) \geq \frac {D _ {U}}{a - b K} \text {or} \max (x _ {j}, y _ {j}) \leq \frac {D _ {L}}{a - b K} \right\}$$

and

$$\lambda (j) := | \{0 \leq i \leq j: \mathcal {W} _ {i} \} |.$$

For any $\begin{array} { r } { K \in \left\lceil \frac { a - 1 } { b } , \frac { a ^ { * } - 1 + \frac { 1 } { \log ^ { 9 } ( T ) } } { b ^ { * } } \right\rceil } \end{array}$ , we have that $\begin{array} { r } { | a ^ { * } - b ^ { * } K | - 1 \leq O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) } \end{array}$ by Lemma $7 .$ This with the fact that $\begin{array} { r } { \epsilon \leq \frac { \mathbf { \bar { 1 } } } { \log ^ { 4 6 } ( T ) } } \end{array}$ implies that $\displaystyle ( a ^ { * } - b ^ { * } K ) + O _ { T } ( \epsilon ) \le \vert a ^ { * } - b ^ { * } K \vert + O _ { T } ( \epsilon ) \le$ $\begin{array} { r } { 1 + O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) } \end{array}$ . This allows us to bound the $\rho$ in Lemma 6 to be $\begin{array} { r } { 1 + O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) } \end{array}$ . By Lemma 6 and plugging this bound in for $\rho ,$ this gives the following bound.
