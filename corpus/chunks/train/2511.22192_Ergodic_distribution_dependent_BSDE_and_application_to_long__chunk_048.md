$$\lim _ {n \to + \infty} \beta_ {0, n} ^ {\alpha} (x, [ \theta ]) = \beta_ {0} ^ {\alpha} (x, [ \theta ]), \quad \forall (x, \theta) \in \mathbb {R} ^ {d} \times L ^ {2 q + 2} (\Omega ; \mathbb {R} ^ {d}).$$

Proof. By lifting [??] on the Hilbert space $\operatorname { L } ^ { 2 } ( \Omega ; \mathbb { R } ^ { d } ) \supset \operatorname { L } ^ { 2 q + 2 } ( \Omega ; \mathbb { R } ^ { d } )$ we get a function $\check { \beta } ^ { \alpha } : \mathbb { R } ^ { d } \times \mathrm { L } ^ { 2 } ( \Omega ; \mathbb { R } ^ { d } )$ such that for all $\check { \theta } \in \dot { \mathrm { L } } ^ { 2 q + 2 } ( \Omega ; \mathbb { R } ^ { d } ) \sim [ \theta ]$ and $x \in \mathbb { R } ^ { d } , \check { \beta } ^ { \alpha } ( x , \check { \theta } ) = \beta ^ { \alpha } ( x , [ \theta ] )$ . Then, we can apply [9, Lemma 3.5] in order to get the existence of a uniformly bounded sequence of Lipschitz lifted functions $( { \check { \beta } } _ { 0 , n } ^ { \alpha } ) _ { n }$ such that $\check { \beta } _ { 0 , n } ^ { \alpha ^ { \cdot } } ( x , \check { \theta } )  \check { \beta } ^ { \alpha } ( \overset { \cdot } { x } , \check { \theta } )$ when $n \to + \infty$ , which implies that

$$\beta_ {0, n} ^ {\alpha} (x, [ \theta ]) := \check {\beta} _ {0, n} ^ {\alpha} (x, \check {\theta}) \longrightarrow \check {\beta} ^ {\alpha} (x, \check {\theta}) = \beta^ {\alpha} (x, [ \theta ]), \quad \forall (x, \theta) \in \mathbb {R} ^ {d} \times \mathrm{L} ^ {2 q + 2} (\Omega ; \mathbb {R} ^ {d}).$$

Moreover, for all $( x , \check { \theta } ) , ( x ^ { \prime } , \check { \theta } ^ { \prime } ) \in \mathbb { R } ^ { d } \times \mathrm { L } ^ { 2 q + 2 } ( \Omega ; \mathbb { R } ^ { d } )$ with -??ˇ = [??] and $\left[ \check { \theta } ^ { \prime } \right] = \left[ \theta ^ { \prime } \right]$ , we have

$$| \beta_ {0, n} ^ {\alpha} (x, [ \theta ]) - \beta_ {0, n} ^ {\alpha} (x, [ \theta^ {\prime} ]) | = | \check {\beta} _ {0, n} ^ {\alpha} (x, \check {\theta}) - \check {\beta} _ {0, n} ^ {\alpha} (x ^ {\prime}, \check {\theta} ^ {\prime}) | \leq C _ {n} | x - x ^ {\prime} | + C _ {n} \mathbb {E} [ | \check {\theta} - \check {\theta} ^ {\prime} | ^ {2} ] ^ {1 / 2}.$$

Then, it just remains to take for $( \check { \theta } , \check { \theta } ^ { \prime } )$ an optimal coupling of $\left( \left[ \theta \right] , \left[ \theta ^ { \prime } \right] \right)$ for the W2−Wasserstein distance in order to conclude. □
