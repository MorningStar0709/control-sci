A solution satisfying the $\mathcal { H } _ { \infty }$ Riccati inequality corresponding to the controller (6) is shown below.

Lemma 4. Let $\gamma > 0$ . Then, $\forall p \in \mathbb { \mathbb { F } } ]$ , the matrix $P _ { p } = ( I - A _ { p } ) ^ { - 1 }$ associated with the controller gain $K _ { p } : = B ^ { \top } \left( A _ { p } - I \right) ^ { - 1 }$ satisfies the $\mathcal { H } _ { \infty }$ Riccati inequality

$$P _ {p} \succeq I + K _ {p} ^ {\top} K _ {p}+ \left(A _ {p} + B K _ {p}\right) ^ {\top} \left(P _ {p} ^ {- 1} - \gamma^ {- 2} I\right) ^ {- 1} (A _ {p} + B K _ {p}). \tag {27}$$

Proof. Given (8), we see that

$$
0 \preceq \left[ \begin{array}{c c} A _ {p} - A _ {p} ^ {2} - B B ^ {\top} & A _ {p} - A _ {p} ^ {2} - B B ^ {\top} \\ A _ {p} - A _ {p} ^ {2} - B B ^ {\top} & A _ {p} - A _ {p} ^ {2} - B B ^ {\top} \end{array} \right]

\preceq \left[ \begin{array}{c c} I - A _ {p} - \gamma^ {- 2} I & A _ {p} - A _ {p} ^ {2} - B B ^ {\top} \\ A _ {p} - A _ {p} ^ {2} - B B ^ {\top} & A _ {p} - A _ {p} ^ {2} - B B ^ {\top} \end{array} \right],
$$

where we used Lemma 3 to get the second inequality. Now, multiplying the above inequality from left and right by $\left[ \begin{array} { l l } { I } & { 0 } \\ { 0 } & { ( A _ { p } - I ) ^ { - 1 } } \end{array} \right]$ , and substituting $K _ { p } : = B ^ { \top } \left( A _ { p } - I \right) ^ { - 1 } , P _ { p } = ( I - A _ { p } ) ^ { - 1 }$ , we get

$$
\left[ \begin{array}{c c} P _ {p} ^ {- 1} - \gamma^ {- 2} I & - (A _ {p} + B K _ {p}) ^ {\top} \\ - (A _ {p} + B K _ {p}) & P _ {p} - I - K _ {p} ^ {\top} K _ {p} \end{array} \right] \succeq 0.
$$

The result (27) follows by applying Schur complement.

![](image/656236801206da8db1340e082830469c6064d191c75a5c472690e0cd2d6a36b2.jpg)

Theorem 1. When $A = { \overline { { \mathbf { a } } } } I _ { N }$ in (5), Lemma 4 implies that

$$\gamma \geq \left\| \mathbf {G} _ {\infty} ((1 - \overline {{{\mathbf {a}}}}) \mathbf {G} _ {\infty} - \mathbf {F} _ {\infty}) ^ {- 1} \right\| ^ {\frac {1}{2}}, \tag {28}$$

where $\begin{array} { r } { \mathbf { G } _ { \infty } = \frac { 1 } { ( 1 - \overline { { \mathbf { a } } } ) ^ { 2 } } \big ( ( 1 - \overline { { \mathbf { a } } } ) \overline { { \mathbf { a } } } I - B B ^ { \top } \big ) } \end{array}$ , and $\mathbf { F } _ { \infty } = \left( \overline { { \mathbf { a } } } I + \textstyle \frac { 1 } { \mathbf { \overline { { a } } } - 1 } B B ^ { \top } \right) ^ { \top } \left( \overline { { \mathbf { a } } } I + \textstyle \frac { 1 } { \mathbf { \overline { { a } } } - 1 } B B ^ { \top } \right)$
