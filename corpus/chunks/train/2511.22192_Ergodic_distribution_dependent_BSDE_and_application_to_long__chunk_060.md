Without loss of generality, we can take the same ?? in $( \mathcal { H } _ { g } )$ and $\left( \mathcal { H } _ { 0 } \right)$ . We will assume that $\left( \mathcal { H } _ { 1 } \right)$ or that $\left( \mathcal { H } _ { 2 } \right)$ hold: Thus, we can apply Theorem 3.4 in order to get the existence of a triplet $( u , \zeta , \lambda )$ such that $( u ( X _ { t } ^ { x , [ \theta ] } , \left\lceil X _ { t } ^ { [ \theta ] } \right\rceil ) , \zeta ( X _ { t } ^ { x , [ \theta ] } , \left\lceil X _ { t } ^ { [ \theta ] } \right\rceil ) , \lambda )$ is a solution to (Decoupled EBSDE) and ?? has growth and smoothness properties specified in Theorem 3.4. In the remaining of this section, we will always consider this solution of (Decoupled EBSDE). We will now establish three different long-time behaviour for (Decoupled BSDE).

Theorem 4.1 (LTB 1). Assume that $( \mathcal { H } _ { 1 } ) \ – ( \mathcal { H } _ { g } )$ or that $( \mathcal { H } _ { 2 } ) _ { - } ( \mathcal { H } _ { g } )$ hold. Then, there exists $C > 0$ such that, for all $x \in \mathbb { R } ^ { d } , \theta \in L ^ { 2 q + 2 } ( \Omega , \mathbb { R } ^ { d } )$ and all $T > 0$ ,

$$\left| \frac {Y _ {0} ^ {T , x , [ \theta ]}}{T} - \lambda \right| \leq C \frac {1 + | x | ^ {q + 1} + \| \theta \| _ {2 q + 2} ^ {q + 1}}{T}. \tag {4.1}$$

Proof. Let $x \in \mathbb { R } ^ { d } , \theta \in \mathrm { L } ^ { 2 q + 2 } ( \Omega , \mathbb { R } ^ { d } )$ and $T > 0$ . We have

$$\left| \frac {Y _ {0} ^ {T , x , [ \theta ]}}{T} - \lambda \right| \leq \left| \frac {Y _ {0} ^ {T , x , [ \theta ]} - Y _ {0} ^ {x , [ \theta ]} - \lambda T}{T} \right| + \left| \frac {Y _ {0} ^ {x , [ \theta ]}}{T} \right|.$$

First, by the growth property of ??, $\left| Y _ { 0 } ^ { x , [ \theta ] } \right| = | u ( x , [ \theta ] ) | \leq C ( 1 + | x | ^ { q + 1 } + \| \theta \| _ { 2 q + 2 } ^ { q + 1 } )$ . Furthermore, we have, for $\tilde { Z } = Z ^ { T , x , [ \theta ] } - Z ^ { x , [ \theta ] }$

$$
\begin{array}{l} Y _ {0} ^ {T, x, [ \theta ]} - Y _ {0} ^ {x, [ \theta ]} - \lambda T = g (X _ {T} ^ {x, [ \theta ]}, \left[ X _ {T} ^ {[ \theta ]} \right]) - u (X _ {T} ^ {x, [ \theta ]}, \left[ X _ {T} ^ {[ \theta ]} \right]) + \int_ {0} ^ {T} f (X _ {s} ^ {x, [ \theta ]}, \left[ X _ {s} ^ {[ \theta ]} \right], Z _ {s} ^ {T, x, [ \theta ]}) - f (X _ {s} ^ {x, [ \theta ]}, \left[ X _ {s} ^ {[ \theta ]} \right], Z _ {s} ^ {x, [ \theta ]}) \mathrm{d} s \\ - \int_ {0} ^ {T} \tilde {Z} _ {s} \mathrm{d} W _ {s} \\ = g (X _ {T} ^ {x, [ \theta ]}, \left[ X _ {T} ^ {[ \theta ]} \right]) - u (X _ {T} ^ {x, [ \theta ]}, \left[ X _ {T} ^ {[ \theta ]} \right]) - \int_ {0} ^ {T} \tilde {Z} _ {s} \left(\mathrm{d} W _ {s} - \beta_ {s} ^ {T} \mathrm{d} s\right), \\ \end{array}
$$

where
