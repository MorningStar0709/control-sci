$$
\begin{array}{l} \hat {A} := \left[ \begin{array}{c c} \hat {A} _ {1 1} & - B _ {u} K _ {v} \hat {A} _ {1 1} ^ {- \top} \\ 0 & \hat {A} _ {1 1} ^ {- \top} \end{array} \right], \qquad \qquad \qquad \hat {B} := \left[ \begin{array}{c} B _ {d} \\ - X B _ {d} \end{array} \right], \\ \hat {C} := \gamma_ {J} \left[ \begin{array}{c c} C _ {e} - D _ {e u} K _ {x} & - D _ {e u} K _ {v} \hat {A} _ {1 1} ^ {- \top} \\ 0 & 0 \end{array} \right], \qquad \qquad \hat {D} := \gamma_ {d} \left[ \begin{array}{c} 0 \\ I \end{array} \right]. \\ \end{array}
$$

The relation $K _ { d } = K _ { v } X B _ { d }$ is used to obtain these simplified expressions. Let $\hat { P }$ denote the closed-loop dynamics in (14).

The eigenvalues of $\hat { A } _ { 1 1 } ^ { - \top }$ lie outside the unit disk. These eigenvalues are associated with the stable, non-causal dynamics of the controller $K ^ { o }$ . Specifically, an input $d \in \ell _ { 2 }$ generates $v \in \ell _ { 2 }$ when iterating the non-causal controller backward from $v _ { \infty } = 0$ . The plant dynamics then generate $x \in \ell _ { 2 }$ when iterating forward from $x _ { - \infty } = 0$ . Moreover, $v , x \in \ell _ { 2 }$ imply that $v _ { t } \to 0$ as $t \to - \infty$ and $x _ { t } \to 0 \ { \mathrm { a s } } \ t \to \infty$ . Hence the augmented state satisfies the boundary conditions

$$\hat {x} _ {- \infty} = 0 \text { and } \hat {x} _ {\infty} = 0. ^ {\dagger}$$

In summary, the system (14) is stable but with causal and non-causal dynamics. A spectral factorization can be used to re-write $\| \hat { e } \| _ { 2 } ^ { 2 }$ using only stable, causal dynamics.

Lemma 2. Assume that $\left( A , B _ { u } , C _ { e } , D _ { e u } \right)$ satisfy conditions $( i ) - ( i v )$ in Lemma 1 so that the DARE (7) has a stabilizing solution $X ~ \succeq ~ 0$ . This stabilizing solution is used to define the dynamics $\hat { P }$ from $d _ { t }$ to $\hat { e } _ { t }$ in (14).

If $\gamma _ { d } > 0$ and $( \hat { A } _ { 1 1 } ^ { - \top } , X B _ { d } )$ is stabilizable then there exists a square $n _ { d } \times n _ { d }$ LTI system F such that: (i) $\| \hat { e } \| _ { 2 } ^ { 2 } = \| F d \| _ { 2 } ^ { 2 } f o r$ all $d \in \ell _ { 2 }$ , (ii) F is stable, causal, and invertible, and (iii) $F ^ { - 1 }$ is square, stable and causal. Moreover, it is possible to construct F with state dimension $n _ { x } .$ .
