# 2.3 Physics-Guided Recurrent Neural Network

Let a physics-based model $\mathcal { M } _ { p h y }$ with state $\tilde { \pmb { x } } \in \tilde { \mathbb { X } } \subseteq \mathbb { R } ^ { \tilde { n } }$ , output $\tilde { \mathbf { y } } \in \mathbb { Y }$ and parameters $\tilde { \pmb { p } } \in \mathbb { R } ^ { \tilde { q } }$ be given by

$$
\begin{array}{l} \dot {\tilde {\boldsymbol {x}}} _ {t} = \tilde {\boldsymbol {f}} \left(\tilde {\boldsymbol {x}} _ {t}, \boldsymbol {u} _ {t}, t; \tilde {\boldsymbol {p}}\right), \quad \tilde {\boldsymbol {f}}: \tilde {\mathbb {X}} \times \mathbb {U} \times \mathbb {T} \mapsto \tilde {\mathbb {X}}, \\ \tilde {\boldsymbol {u}} _ {t} = \tilde {\boldsymbol {u}} _ {t} (\tilde {\boldsymbol {u}} _ {t}, t; \tilde {\boldsymbol {u}}) \quad \tilde {\boldsymbol {u}} = \tilde {\mathbb {X}} - \mathbb {U} - \mathbb {T} - \mathbb {V}. \end{array} \tag {7}
\tilde {\boldsymbol {y}} _ {t} = \tilde {\boldsymbol {g}} \left(\tilde {\boldsymbol {x}} _ {t}, \boldsymbol {u} _ {t}, t; \tilde {\boldsymbol {p}}\right), \quad \tilde {\boldsymbol {g}}: \tilde {\mathbb {X}} \times \mathbb {U} \times \mathbb {T} \mapsto \mathbb {Y},$$

modeling the reference system S approximately. Then, the model $\bar { \mathcal { M } } _ { P G R N N }$ , relating physics-based dynamics $\dot { \tilde { \mathbf { x } } } _ { t } \in \mathbb { X }$ and outputs $\tilde { \pmb { y } } _ { t } \in \mathbb { Y }$ to $\dot { \hat { \pmb { x } } } _ { t } \in \hat { \mathbb { X } }$ and $\hat { \pmb y } _ { t } \in \mathbb { Y }$ , respectively, is given by two RNNs $\scriptstyle { \frac { } { \mathcal { F } } }$ and $\stackrel { \triangledown } { \boldsymbol { \mathcal { G } } }$ modeling the dynamics and output functions:
