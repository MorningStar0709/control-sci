First, the total flow out of the root node $\hat { \boldsymbol { \imath } } _ { r }$ must equal the number of selected cyber nodes minus one, ensuring that the network forms a spanning tree:

$$\sum_ {\hat {j}: (\hat {i} _ {r}, \hat {j}) \in \mathcal {E} ^ {\prime}} h _ {\hat {i} _ {r} \hat {j}} = \sum_ {\hat {i} \in \mathcal {V} ^ {\prime}} x _ {\hat {i}} - 1 \tag {7}$$

where $h _ { \hat { \imath } _ { r } \hat { \jmath } }$ denotes the flow on link ${ \hat { \imath } } _ { r } { \hat { \jmath } } .$ Eq. (7) ensures that the flow originates from the root node and connects all active nodes in the cyber network.

Then, the total inflow and outflow of every cyber node except the root node, denoted as $\hat { \boldsymbol { \imath } } \in \mathcal { V } _ { \bar { \hat { \boldsymbol { \imath } } } _ { r } } ^ { \prime }$ , must satisfy:

$$\sum_ {\hat {j}: (j, \hat {i}) \in \mathcal {E} ^ {\prime}} h _ {\hat {j} \hat {i}} - \sum_ {\hat {j}: (\hat {i}, \hat {j}) \in \mathcal {E} ^ {\prime}} h _ {\hat {i} \hat {j}} = x _ {\hat {i}}. \quad \forall \hat {i} \in \mathcal {V} _ {\bar {\hat {i}} _ {r}} \tag {8}$$

Eq. (8) ensures that net flows at these nodes must equal the binary indicator $x _ { \hat { \imath } }$ that represents the nodal status, i.e., active (1) and inactive (0).

The flow on each link is constrained further by an activation level of the communication link, defined by:

$$h _ {\hat {i} \hat {j}} \leq M \cdot y _ {\hat {i} \hat {j}}, \quad \forall \hat {i} \hat {j} \in \mathcal {E} ^ {\prime} \tag {9}$$

where M is a large constant representing the maximum possible flow on any edge, e.g., M is the number of nodes.

Moreover, a communication link can be activated only if both endpoints are active, meaning that cyber resources are deployed at both nodes. This is described by the following edge activation and resource deployment constraints:

$$y _ {\hat {i} \hat {j}} \leq \min \{x _ {\hat {i}}, x _ {\hat {j}} \}, \quad \forall \hat {i} \hat {j} \in \mathcal {E} ^ {\prime} \tag {10}$$

which ensures that the flow on link $\hat { \imath } \hat { \jmath }$ is only allowed if cyber resources are deployed at both nodes ˆı and ${ \hat { \jmath } } .$

3) Cyber Resource Optimization: To summarize, the flowbased cyber topology optimization problem is formulated as:

$$\min _ {y _ {i j}, x _ {i}} f _ {\text { cyber }} \tag {11a}\text { s. t. } \quad (7), (8), (9), (1 0). \tag {11b}$$
