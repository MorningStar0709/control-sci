# Problem 1. Distributed state estimation

Consider a group of observer nodes numbered from 1 to $N ,$ , among which the ??th observer node has access to local measured output

$$y _ {i} = C _ {i} x \tag {7}$$

with $C _ { i }$ having full row rank, and has access to some local control input $u _ { i } .$ . Then the input terms in (6) can be rewritten as

$$B u + B _ {v} v = B _ {i} u _ {i} + B _ {- i} u _ {- i} \tag {8}$$

with $B _ { - i }$ having full column rank and $u _ { - i } \in \mathbb { R } ^ { m _ { - i } }$ denoting the input unavailable to the ??th observer node, as in [35, 36]. The communication among observer nodes is mathematically characterized in Section II-C. Suppose that the observer nodes exchange state estimates with each other through communication, we aim to design each ??th observer node $( i = 1 , \cdots , N )$ such that it produces an accurate state estimate ${ \hat { x } } _ { i } ,$ i.e.,

$$\lim _ {t \to \infty} \| \hat {x} _ {i} (t) - x (t) \| = 0.$$
