Notation. R is the set of real numbers and N is the set of natural numbers, $ { \mathbb { N } } _ { 0 } =  { \mathbb { N } } \cup \lbrace 0 \rbrace$ . For two scalars ?? and $b , a \% b$ denotes the remainder of the operation $a / b$ . For a finite set ??, its cardinality is denoted by |??|. For a vector $v , \ \lVert v \rVert$ denotes its Euclidean norm. For symmetric block matrices, F acts as ellipsis for the terms that are introduced by symmetry, diag $Q _ { 1 } , Q _ { 2 } , \ldots , Q _ { n } )$ denotes a block-diagonal matrix with diagonal elements $Q _ { 1 } , Q _ { 2 } , \ldots , Q _ { n } . \ 0 _ { d \times d }$ and $I _ { d \times d }$ denote ??-dimensional 0-matrix and identity matrix, respectively. We will operate in a probabilistic space $( \Omega , { \mathcal { F } } , { \mathbb { P } } )$ , where Ω is the sample space, $\mathcal { F }$ is the ??-algebra of events, and P is the probability measure.
