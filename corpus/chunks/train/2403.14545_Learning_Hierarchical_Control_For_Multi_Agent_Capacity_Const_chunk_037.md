$$
\begin{array}{l} V _ {n, 0 \to N} ^ {H, r} (x _ {k} ^ {H, r}) = \\ = \max _ {\mathbf {x} _ {k: k + N \mid k} ^ {H}} V _ {n} ^ {H} \left(\mathbf {x} _ {k: k + N - 1 \mid k} ^ {H}\right) + Q ^ {H, r} \left(x _ {k + N \mid k} ^ {H}\right) (49) \\ = V _ {n} ^ {H} \left(x _ {k \mid k} ^ {H, r, \star}\right) + V _ {n} ^ {H} \left(\mathbf {x} _ {k + 1: k + N - 1 \mid k} ^ {H, r, \star}\right) + Q ^ {H, r} \left(x _ {k + N \mid k} ^ {H, r, \star}\right) (50) \\ = V _ {n} ^ {H} (x _ {k | k} ^ {H, r, \star}) + V _ {n} ^ {H} (\mathbf {x} _ {k + 1: k + N - 1 | k} ^ {H, r, \star}) + V _ {n} ^ {H} (\mathbf {x} _ {k \star :} ^ {H, p}) (51) \\ = V _ {n} ^ {H} (x _ {k | k} ^ {H, r, \star}) + V _ {n} ^ {H} (\mathbf {x} _ {k + 1: k + N - 1 | k} ^ {H, r, \star}) + V _ {n} ^ {H} (x _ {k \star} ^ {H, p}) + \\ + Q ^ {H, r} (x _ {k \star + 1} ^ {H, p}) \\ \leq V _ {n} ^ {H} \left(x _ {k \mid k} ^ {H, r, \star}\right) + V _ {n, 0 \rightarrow N} ^ {H, r} \left(x _ {k + 1 \mid k} ^ {H, r, \star}\right) (52) \\ \leq V _ {n} ^ {H} (x _ {k | k} ^ {H, r, \star}) + V _ {n, 0 \to N} ^ {H, r} (x _ {k + 1} ^ {H, r}), (53) \\ \end{array}
$$

Where $p ,$ k⋆ in (51) are as defined in (24). Note that we $\mathbf { x } ^ { H }$ break the cost into the sum $V _ { n } ^ { H }$ evaluated on a closed-loop trajectoryosts evaluated on components of the trajectory (as in (49) and (50)) because the high-level optimal control problem enforces in constraint (29b) that planned paths do not contain nodes already traversed in the current iteration. (52) states that the predicted closed-loop cost for iteration r at event $k$ is bounded above by the sum of the current stage cost and the closed-loop cost at the predicted state at event $k + 1$ . This bound is further improved in (53), since (as shown in Thm. 1 and Thm. 2) cH,r,⋆k+1|k $c _ { k + 1 | k } ^ { \check { H } , r , \star } \geq c _ { k + 1 } ^ { \check { H } , r }$ ck+1, potentially allowing for a better route to be planned at event $k + 1$ than was expected at event k. Iteratively applying (53) at event $k = 0$ gives

$$
\begin{array}{l} V _ {n, 0 \rightarrow N} ^ {H, r} (x _ {0} ^ {H, r}) \leq V _ {n} ^ {H} (x _ {0} ^ {H, r}) + V _ {n, 0 \rightarrow N} ^ {H, r} (x _ {1} ^ {H, r}) \\ \leq V _ {n} ^ {H} \left(\mathbf {x} _ {0: 1} ^ {H, r}\right) + V _ {n, 0 \rightarrow N} ^ {H, r} \left(x _ {2} ^ {H, r}\right) \\ \leq \lim _ {k \rightarrow \infty} \left(V _ {n} ^ {H} \left(\mathbf {x} _ {0: k - 1} ^ {H, r}\right) + V _ {n, 0 \rightarrow N} ^ {H, r} \left(x _ {k} ^ {H, r}\right)\right). \tag {54} \\ \end{array}
$$

The agent is constrained to return to the depot (node D) by the end of the task, and thus by definition of (20),
