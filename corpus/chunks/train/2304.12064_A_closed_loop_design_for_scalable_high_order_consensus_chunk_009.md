The number of ordered subsets of the range [1, n] with size $n - k$ is given by the binomial coefficients and therefore the size of $\left| { \mathcal { T } } _ { k } \right| \overset { \cdot } { = } \left( { n - k } \right)$ . Applying Lemma 2 recursively shows that $\begin{array} { r } { A _ { k } \in \mathcal { A } ^ { n - k } ( W , ( \ O _ { n - k } ^ { n } ) c ^ { n - k } ) } \end{array}$ . Clearly, we have that $n - k \leq n$ and $\binom { n } { n - k } c ^ { n - k } \leq \binom { n } { \lceil n / 2 \rceil } \mathrm { m a x } ( c , c ^ { n } ) < \infty$ for all k. Let $c ^ { \prime } = \textstyle { \binom { n } { \lceil n / 2 \rceil } } \textstyle { \operatorname* { m a x } } ( c , c ^ { n } )$ and then we have that $A _ { k } \in \mathcal { A } ^ { n } ( W , c ^ { \prime } )$ for all k.

Example 1: For clarity let us consider the controller for the case $n = 3$ . Then the controller is

$$
\begin{array}{l} U (s) = U _ {\text { ref }} (s) + \left(s ^ {3} I - \prod_ {k = 1} ^ {3} (s I + L _ {k})\right) X (s) \\ = U _ {\text { ref }} (s) - \left(s ^ {2} \left(L _ {1} + L _ {2} + L _ {3}\right) + \right. \\ s \left(L _ {1} L _ {2} + L _ {1} L _ {3} + L _ {2} L _ {3}\right) + \left. L _ {1} L _ {2} L _ {3}\right) X (s) \\ \end{array}
$$

Here, $A _ { 0 } = L _ { 1 } L _ { 2 } L _ { 3 } , A _ { 1 } = L _ { 1 } L _ { 2 } + L _ { 1 } L _ { 3 } + L _ { 2 } L _ { 3 }$ , and $A _ { 2 } = L _ { 1 } L _ { 2 } L _ { 3 }$ . The proposition asserts that if $L _ { 1 } , L _ { 2 }$ , and $L _ { 3 }$ share a sparsity pattern and have bounded gains, then the resulting controller gains $A _ { 0 } , A _ { 1 }$ , and $A _ { 2 }$ will be sparse and have bounded gains.
