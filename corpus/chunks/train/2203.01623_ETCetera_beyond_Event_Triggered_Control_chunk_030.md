# 4.2 Python interface: linear PETC abstractions

Consider the linear PETC system:

$$
\dot {\zeta} (t) = \left[ \begin{array}{c c} - 0. 5 & 0 \\ 0 & 3. 5 \end{array} \right] \zeta (t) + \left[ \begin{array}{l} 1 \\ 1 \end{array} \right] u (\zeta (t _ {i})), \quad t \in [ t _ {i}, t _ {i + 1}) \tag {14}

u (\zeta (t _ {i})) = \left[ \begin{array}{c c} 1. 0 2 & - 5. 6 2 \end{array} \right] \zeta (t _ {i}),
$$

with triggering function ?? (?? (?? ), ?? (???? )) = |?? (?? ) − ?? (???? ) |2 − (1 − ??) |?? (??)|2, where ?? = 0.05 and ℎ = 0.01, ???????? = 20. The PETC traffic model for this system is generated as follows:

```python
import numpy as np
