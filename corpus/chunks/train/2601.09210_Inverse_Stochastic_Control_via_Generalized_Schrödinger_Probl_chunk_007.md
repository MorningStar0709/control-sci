for some $N \in \mathbb { N } , \gamma _ { j } \in \mathbb { R } , \alpha _ { j } > 0 , \xi _ { j } \in [ 0 , T ] \times \mathbb { R } ^ { d } \times U , j = 1 , \dots , N$ , such that $\begin{array} { r } { \sum _ { j = 1 } ^ { N } \{ | \gamma _ { j } | + \alpha _ { j } ( 1 + | \xi _ { j } | ) \} } \end{array}$ is bounded by a given positive constant. Then the class D satisfies (A3) and (A5).

Now we show the existence of a solution to the proposed inverse problem.

Proposition 2.3. Suppose that pA1q–pA5q hold. Then there exists a minimizer of the inverse problem (1.4).

Proof. Let $\{ ( f _ { n } , g _ { n } ) \} _ { n = 1 } ^ { \infty } \subset \mathcal { D }$ be a minimizing sequence, i.e., l $\begin{array} { r } { \operatorname* { i m } _ { n \to \infty } V ( f _ { n } , g _ { n } ) = V ^ { * } } \end{array}$ . By (A3) and (A5), the class $\left\{ \phi _ { f , g } : \left( f , g \right) \in \mathcal { D } \right\}$ is compact in $C ( [ 0 , T ] \times \mathbb { R } ^ { d } \times U , \mathbb { R } ^ { 2 } )$ . It follows from the Ascoli– Arzel\`a theorem that there exist $( f ^ { * } , g ^ { * } ) \in { \mathcal { D } }$ and $\left\{ { { n } _ { k } } \right\}$ such that $\begin{array} { r } { \operatorname* { s u p } _ { ( t , x , u ) \in [ 0 , T ] \times B _ { R } \times U } | f _ { n _ { k } } ( t , x , u ) - } \end{array}$ $\begin{array} { r } { f ^ { * } ( t , x , u ) \vert + \operatorname* { s u p } _ { x \in B _ { R } } \vert g _ { n _ { k } } ( x ) - g ^ { * } ( x ) \vert  0 , k  \infty } \end{array}$ , for any $R > 0$ , where $B _ { R }$ stands for the closed ball in Rd centered at 0 with radius R. Then, the dominated convergence theorem yieldsş ş $\begin{array} { r } { \int _ { 0 } ^ { T } \langle f _ { n _ { k } } ( t , \cdot , \cdot ) , \mu _ { t } \rangle d t \to \int _ { 0 } ^ { T } \langle f ^ { * } ( t , \cdot , \cdot ) , \mu _ { t } \rangle } \end{array}$ dt and $\langle g _ { n _ { k } } , \tilde { \mu } _ { T } \rangle  \langle g ^ { * } , \tilde { \mu } _ { T } \rangle$ as $k  \infty$ . Further, applying Theorem 12 in Section 1 of Chapter 3 in Krylov [11], we get

$$\varepsilon_ {k} := \sup _ {u \in \mathcal {U}} \mathbb {E} \int_ {0} ^ {T} | f _ {n _ {k}} (t, X _ {t} ^ {u}, u _ {t}) - f ^ {*} (t, X _ {t} ^ {u}, u _ {t}) | d t + \mathbb {E} | g _ {n _ {k}} (X _ {T} ^ {u}) - g ^ {*} (X _ {T} ^ {u}) | \to 0,$$

as $k \to \infty$ . Therefore, lim $_ { \substack { k \to \infty } } V ( f _ { n _ { k } } , g _ { n _ { k } } ) = V ( f ^ { * } , g ^ { * } )$ . Thus $( f ^ { * } , g ^ { * } ) \in { \mathcal { D } }$ is optimal.

Example 2.4. Consider the one-dimensional system

$$d X _ {t} ^ {u} = u _ {t} d t + \frac {1}{1 0} d B _ {t}, X _ {0} \sim N (0, 1),$$
