Notation. For a metric space E, we let $C _ { b } ( \mathsf { E } )$ denote the set of all bounded continuous real functions on E, ${ \mathcal { P } } ( \mathsf { E } )$ denote the set of all Borel probability measures on E, and $B ( \mathsf E )$ denote the collection of Borel sets. For any E-valued random element $x , { \mathcal { L } } ( x ) ( \cdot ) \in { \mathcal { P } } ( \mathsf { E } )$ denotes the distribution of x. A sequence $\{ \mu _ { n } \}$ of measures on E is said to converge weakly to a measure $\begin{array} { r } { \mu \operatorname { i f } \int _ { \mathsf E } g ( e ) \mu _ { n } ( d e ) \to \int _ { \mathsf E } g ( e ) \dot { \mu } ( d e ) } \end{array}$ for all $g \in C _ { b } ( \mathsf E )$ . The set of probability measures ${ \mathcal { P } } ( { \mathsf { E } } )$ is endowed with the Borel σ-algebra induced by weak convergence. In this paper, k·k denotes the Euclidean norm and $\| \cdot \| _ { \infty }$ denotes the $l _ { \infty } \mathrm { - n o r m }$ . The notation $v \sim \nu$ means that the random element v has distribution ν. Unless otherwise specified, the term “measurable" will refer to Borel measurability.

2. Preliminary Definitions and Results. In this section, we provide definitions used throughout the paper and present some preliminary lemmas that are necessary to prove the main results. These lemmas are generally easy to prove, but we include their proofs for completeness.

For each $M \geq 1$ , let us define the mean-field term function e from $\mathsf { X } ^ { M }$ to $\mathcal { P } ( \mathsf { X } )$ as follows, where X is the compact state space of the mean-field game

$$\mathrm{e} [ \cdot | \mathbf {x} ] := \frac {1}{N} \sum_ {i = 1} ^ {M} \delta_ {x _ {i}} (\cdot)$$

We also define the following set $\mathcal { P } _ { M } ( \mathsf { X } ) : = \mathsf { e } [ \cdot | \mathsf { X } ^ { M } ]$ , which is the image of $\mathsf { X } ^ { M }$ under e into ${ \mathcal { P } } ( \mathsf { X } )$ . For any set $\{ a _ { 1 } , \ldots , a _ { R } \}$ , let $S _ { \{ a _ { 1 } : a _ { R } \} }$ denote the permutation group of this set;
