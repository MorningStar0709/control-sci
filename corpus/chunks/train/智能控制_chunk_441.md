$$
\| \widetilde {\boldsymbol {\theta}} \| _ {F} ^ {2} = \operatorname{tr} (\widetilde {\boldsymbol {\theta}} ^ {T} \widetilde {\boldsymbol {\theta}})
\begin{array}{l} \dot {V} = \frac {1}{2} \left[ \boldsymbol {x} ^ {\mathrm{T}} \boldsymbol {P} \dot {\boldsymbol {x}} + \dot {\boldsymbol {x}} ^ {\mathrm{T}} \boldsymbol {P} \boldsymbol {x} \right] + \frac {1}{\gamma} \operatorname{tr} (\tilde {\boldsymbol {\theta}} ^ {\mathrm{T}} \tilde {\boldsymbol {\theta}}) \\ = \frac {1}{2} \left[ x ^ {\mathrm{T}} P (A x + B (\tilde {\theta} ^ {\mathrm{T}} \varphi (x) + \eta)) + (x ^ {\mathrm{T}} A ^ {\mathrm{T}} + (\tilde {\theta} ^ {\mathrm{T}} \varphi (x) + \eta) ^ {\mathrm{T}} B ^ {\mathrm{T}}) P x \right] + \frac {1}{\gamma} \operatorname{tr} (\dot {\tilde {\theta}} ^ {\mathrm{T}} \tilde {\theta}) \\ = \frac {1}{2} \left[ x ^ {\mathrm{T}} \left(\boldsymbol {P} \boldsymbol {A} + \boldsymbol {A} ^ {\mathrm{T}} \boldsymbol {P}\right) x + \left(x ^ {\mathrm{T}} \boldsymbol {P} \boldsymbol {B} \bar {\boldsymbol {\theta}} ^ {\mathrm{T}} \boldsymbol {\varphi} (x) + x ^ {\mathrm{T}} \boldsymbol {P} \boldsymbol {B} \boldsymbol {\eta} + \varphi^ {\mathrm{T}} (x) \bar {\boldsymbol {\theta}} \boldsymbol {B} ^ {\mathrm{T}} \boldsymbol {P} x + \boldsymbol {\eta} ^ {\mathrm{T}} \boldsymbol {B} ^ {\mathrm{T}} \boldsymbol {P} x\right) \right] + \frac {1}{\gamma} \operatorname{tr} (\dot {\bar {\boldsymbol {\theta}}} ^ {\mathrm{T}} \bar {\boldsymbol {\theta}}) \\ = - \frac {1}{2} \boldsymbol {x} ^ {\mathrm{T}} \boldsymbol {Q} \boldsymbol {x} + \boldsymbol {\varphi} ^ {\mathrm{T}} (\boldsymbol {x}) \tilde {\boldsymbol {\theta}} \boldsymbol {B} ^ {\mathrm{T}} \boldsymbol {P} \boldsymbol {x} + \boldsymbol {\eta} ^ {\mathrm{T}} \boldsymbol {B} ^ {\mathrm{T}} \boldsymbol {P} \boldsymbol {x} + \frac {1}{\gamma} \operatorname{tr} (\dot {\tilde {\boldsymbol {\theta}}} ^ {\mathrm{T}} \tilde {\boldsymbol {\theta}}) \tag {9.59} \\ \end{array}
$$

其中， $x^{\mathrm{T}}PB\eta = \eta^{\mathrm{T}}B^{\mathrm{T}}Px,x^{\mathrm{T}}PB\widetilde{\theta}^{\mathrm{T}}\varphi (x) = \varphi^{\mathrm{T}}(x)\widetilde{\theta} B^{\mathrm{T}}Px,P^{\mathrm{T}} = P_{\circ}$
