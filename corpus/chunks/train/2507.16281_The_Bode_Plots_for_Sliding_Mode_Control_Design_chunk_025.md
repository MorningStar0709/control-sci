Fig. 9. Bode plots of the sensitivity transfer function (34) over the frequency range $\Omega \in ( \bar { 0 . 0 1 }$ , 100) [rad/s], for perturbation magnitudes $\eta = 1 , 2 ,$ , and 3, respectively.

Table I. The estimation error of the bias component increases with the magnitude of the disturbance. For disturbance levels up to $| \eta | = 2 ,$ , the approximation remains sufficiently accurate, with an error below 15%—provided that Assumption 2-3 holds. Beyond this threshold, not only the estimations of bias level (36) exhibit significant deviation, but the shape of the sliding variable signal also becomes noticeably distorted.

Remark 4: The average control function (13) associated with the relay-type controller is fundamentally governed by an arc-sine function, which exhibits an approximately linear behavior—with a relative error below 10% between both theoretically predicted curves shown in Fig. 6—provided that the ratio between the bias magnitude and the carrier amplitude satisfies $| \sigma _ { 0 } | / A < 2 / 3$ . Accordingly, for a given upper bound $L > 0$ on the constant perturbation (3), the controller parameter $\rho > 0$ must be selected to satisfy the following conditions:

• $\rho \mathrm { ~  ~ { ~ > ~ } ~ } \eta$ guarantees exact disturbance rejection under ideal sliding-mode conditions $( \mu = 0 )$ , as discussed in Section I-A.   
• $\rho > 1 . 5 7 \eta$ ensures the validity of the quasi-linearized model $\begin{array} { r } { u _ { 0 } ^ { * } = \frac { 2 \rho } { \pi } } \end{array}$ arcsin $\left( \frac { \sigma _ { 0 } ^ { * } } { A ^ { * } } \right)$ derived from the linear filter (7)—under practical sliding-mode conditions $( \mu > 0 )$ , as outlined in Section II-A.   
• $\rho > 2 . 3 6 \eta$ justifies the use of the linear approximation $u _ { 0 } ^ { * } ~ = ~ K _ { n } \sigma _ { 0 } ^ { * }$ —based on the EG (33)—for predicting the bias component (36) under practical sliding-mode conditions $( \mu > 0 )$ .
