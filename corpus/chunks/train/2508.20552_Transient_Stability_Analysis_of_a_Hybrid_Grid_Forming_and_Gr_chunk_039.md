subgraph F
        AO["SCHALLENGE 2"] --> AP["Receiving-End System"]
        AP --> AQ["Equivalent Mutual Admittance"]
        AP --> AR["Equivalent Self Admittance"]
    end

subgraph G
        AS["SCHALLENGE 3"] --> AT["Receiving-End System"]
        AT --> AU["Equivalent Mutual Admittance"]
        AT --> AV["Equivalent Self Admittance"]
    end

subgraph H
        AW["SCHALLENGE 1"] --> AX["Synchronization Loop"]
        AX --> AY["Outer Voltage/Current Loop"]
        AY --> AZ["Power Flow Equations"]
    end

subgraph I
        BA["SCHALLENGE 2"] --> BB["Synchronization Loop"]
        BB --> BC["Synchronization Loop"]
        BC --> BD["Power Flow Equations"]
    end

subgraph AJ
        BE["SCHALLENGE 3"] --> BF["Synchronization Loop"]
        BF --> BG["Synchronization Loop"]
        BG --> BH["Power Flow Equations"]
    end

subgraph AK
        BI["SCHALLENGE 1"] --> BJ["Synchronization Loop"]
        BJ --> BK["Synchronization Loop"]
        BK --> BL["Power Flow Equations"]
    end

subgraph AL
        BM["SCHALLENGE 2"] --> BN["Synchronization Loop"]
        BN --> BO["Synchronization Loop"]
        BO --> BP["Power Flow Equations"]
    end

subgraph ME
        BQ["SCHALLENGE 3"] --> BR["Synchronization Loop"]
        BR --> BS["Synchronization Loop"]
        BS --> BT["Power Flow Equations"]
    end

subgraph ME_1
        BU["SCHALLENGE 1"] --> BV["Synchronization Loop"]
        BV --> BW["Synchronization Loop"]
        BW --> BX["Power Flow Equations"]
    end

subgraph ME_2
        BY["SCHALLENGE 2"] --> BZ["Synchronization Loop"]
        BZ --> CA["Synchronization Loop"]
        CA --> CB["Power Flow Equations"]
    end

subgraph ME_3
        CC["SCHALLENGE 3"] --> CD["Synchronization Loop"]
        CD --> CE["Synchronization Loop"]
        CE --> CF["Power Flow Equations"]
    end

subgraph ME_m
        CM["SCHALLENGE 3"] --> CN["Synchronization Loop"]
        CN --> CO["Synchronization Loop"]
        CO --> CP["Power Flow Equations"]
    end

subgraph ME_n
        CU["SCHALLENGE 3"] --> CV["Synchronization Loop"]
        CV --> CW["Synchronization Loop"]
        CW --> CX["Power Flow Equations"]
    end

subgraph ME_y
        CY["SCHALLENGE 3"] --> CZ["Synchronization Loop"]
        CZ --> DA["Synchronization Loop"]
        DA --> DB["Power Flow Equations"]
    end

subgraph ME_x
        DC["SCHALLENGE 3"] --> DD["Synchronization Loop"]
        DD --> DE["Synchronization Loop"]
        DE --> DF["Power Flow Equations"]
    end

subgraph ME_y
        DG["SCHALLENGE 3"] --> DI["Synchronization Loop"]
        DI --> DJ["Synchronization Loop"]
        DJ --> DK["Power Flow Equations"]
    end

subgraph ME_z
        DL["SCHALLENGE 3"] --> DV["Synchronization Loop"]
        DV --> DW["Synchronization Loop"]
        DW --> DX["Power Flow Equations"]
    end
