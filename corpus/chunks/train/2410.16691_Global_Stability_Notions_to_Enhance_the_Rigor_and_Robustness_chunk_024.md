# 6) Proving Stability Properties for Systems with (Disturbance) Inputs

A requirement of all stability properties for systems with inputs is the property of forward completeness. The following theorem is an extension of Theorem 1 and provides the means of proving forward completeness for system (36).

Theorem 4: Let $H \in C ^ { 1 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ be a function for which there exist non-decreasing functions $c , \sigma \in C ^ { 0 } \left( \mathbb { R } _ { + } ; \mathbb { R } _ { + } \right)$ such that the following inequality holds for all $( \boldsymbol { x } , \boldsymbol { d } ) \in \mathbb { R } ^ { n } \times \boldsymbol { D }$ :

$$\dot {H} (x, d) = \nabla H (x) f (x, d) \leq c (\left| d \right|) H (x) + \sigma (\left| d \right|) \tag {42}$$

Furthermore, for each $\left\{ H ( x ) : x \in \mathbb { R } ^ { n } \ \right\}$ define by means of (7) the set $S _ { r }$ as well as the set

$$D _ {r} = \left\{d \in D: | d | \leq r \right\} \tag {43}$$

and suppose that $f \left( { { S } _ { r } } \times { { D } _ { r } } \right)$ is bounded for every $r > \operatorname* { i n f } \left\{ H ( x ) { : } x \in \mathbb { R } ^ { n } \right\}$ . Then system (36) is forward complete.

Proof: Let $x _ { 0 } \in \mathbb { R } ^ { n }$ and $d \in L _ { l o c } ^ { \infty } \left( \mathbb { R } _ { + } ; D \right)$ be given. Since  is locally Lipschitz with respect to $x \in \mathbb { R } ^ { n }$ , the initial-value problem $\dot { x } = f ( x , d )$ with initial condition $x ( 0 ) = x _ { 0 }$ has a unique solution $\ b { x } ( t ) \in \mathbb { R } ^ { n }$ defined for $t \in [ 0 , t _ { \operatorname* { m a x } } )$ , where $t _ { \mathrm { m a x } } \in \left( 0 , + \infty \right]$ . Furthermore, if $t _ { \mathrm { m a x } } < + \infty$ then $\operatorname* { l i m } \operatorname* { s u p } \bigl ( | x ( t ) | \bigr ) = + \infty$ . We show next by means of a contradiction argument that $t _ { \mathrm { m a x } } = + \infty$ ${ t  } t _ { \mathrm { m a x } } ^ { - }$ maxt t→ −

Without loss of generality, we may assume that (42) holds with $\sigma \equiv 0$ (if not then we can replace H by $1 + H$ and  by $c + \sigma )$ . Suppose that $t _ { \mathrm { m a x } } < + \infty$ and define $\left\| d \right\| = \operatorname* { s u p } _ { 0 \leq t \leq t _ { \operatorname* { m a x } } } \left( \left| d ( t ) \right| \right)$ . 0≤t≤tmax
