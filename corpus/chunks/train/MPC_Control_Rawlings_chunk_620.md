If w and ν are zero, $\tilde { \phi }$ decays to zero exponentially fast. Since $\rho (  { \widetilde { A } } _ { L } ) <$ 1 and W is compact, there exists a robust positive invariant set Φ for $\tilde { \boldsymbol { \phi } } ^ { + } = \tilde { \boldsymbol { A } } _ { L } \tilde { \boldsymbol { \phi } } + \tilde { \boldsymbol { w } } , \tilde { \boldsymbol { w } } \in \tilde { \boldsymbol { \mathbb { W } } }$ satisfying

$$\Phi = \tilde {A} _ {L} \Phi \oplus \tilde {\mathbb {W}}$$

Hence $\tilde { \phi } ( i ) \in \Phi$ for all $i \in \mathbb { I } _ { \geq 0 }$ if $\tilde { \phi } ( 0 ) \in \Phi$ . Since $\tilde { \phi } = ( \tilde { { \boldsymbol { x } } } , \tilde { { \boldsymbol { d } } } ) \in \mathbb { R } ^ { n } \times \mathbb { R } ^ { p }$ where $\tilde { x } : = x - \hat { x }$ and $\tilde { \boldsymbol { d } } : = \boldsymbol { d } - \boldsymbol { \hat { d } }$ , we define the sets $\mathbb { \Sigma } _ { x }$ and $\mathbb { \Sigma } _ { d }$ as follows

$$
\mathbb {Z} _ {x} := \left[ \begin{array}{c c} I _ {n} & 0 \end{array} \right] \Phi \qquad \mathbb {Z} _ {d} := \left[ \begin{array}{c c} 0 & I _ {p} \end{array} \right] \Phi
$$

It follows that $\tilde { { \boldsymbol { x } } } ( i ) ~ \in ~ \mathbb { \Sigma } _ { \boldsymbol { x } }$ and $\tilde { \boldsymbol { d } } ( i ) \in \mathbb { Z } \boldsymbol { d }$ for all $i \in \mathbb { I } _ { \geq 0 }$ if $\tilde { \phi } ( 0 ) \ =$ $( \widetilde { x } ( 0 ) , \widetilde { d } ( 0 ) ) \in \Phi$ . That $\tilde { \phi } ( 0 ) \in \Phi$ is a steady-state assumption.
