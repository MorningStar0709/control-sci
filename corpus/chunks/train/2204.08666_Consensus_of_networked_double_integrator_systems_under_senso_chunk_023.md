$$\gamma^ {o} > \frac {\beta T}{\mu_ {1}}; \quad \mu_ {I F} > \frac {\beta^ {2} T}{2 \mu_ {1} \lambda_ {m i n} (M (t))}$$

We can now argue from the convergence of  that $\bar { \mathcal { L } } ( t ) s $ 0 exponentially. Now employing the definition of $s _ { i }$ in (8) and accounting for the fact that $s _ { i } - s _ { j } \to 0$ for all $i , j \in$ $\{ 1 , 2 , \ldots , n \}$ and $\tilde { b } ^ { i } \to 0$ exponentially, we are left with,

$$\frac {\mathrm{d}}{\mathrm{d} t} \left(q _ {i} - q _ {j}\right) = - \lambda \left(q _ {i} - q _ {j}\right) + \Upsilon (t)$$

where $\Upsilon : \mathbb { R } ^ { + }  \mathbb { R } ^ { m }$ denotes an exponentially decaying function. This immediately shows that $\begin{array} { r l } { \operatorname* { i m } _ { t \to \infty } ( q _ { i } - q _ { j } ) \ = } \end{array}$ 0 and the convergence is exponential. This implies that $( \bar { \mathcal { L } } ( t ) q , \bar { \mathcal { L } } ( t ) \dot { q } )  \bar { ( } 0 , 0 )$ exponentially, from the above equation. We use these facts to carry out an asymptotic analysis of the closed-loop by substituting w from (12) in (6). Since, $\tilde { b } , \tilde { b }  0 .$ , we have the dynamics, in the limit, as $\ddot { q } = - \lambda \dot { q }$ , which immediately proves that lim $\iota _ { t  \infty } \dot { q } = 0$ exponentially using similar arguments as before.

For proof of boundedness, we note that in the $\tilde { \theta }$ dynamics of (23), $\mu _ { F } , \mu _ { I F } > 0$ and $\phi _ { F } , \phi _ { I F } , \mathcal { L } \otimes I _ { ( m n + 2 ) }$ are symmetric positive semidefinite matrices at each $t \geqslant 0$ . Therefore we immediately have $\lVert \tilde { { \boldsymbol { \theta } } } \rVert \leqslant \lVert \tilde { { \boldsymbol { \theta } } } ( 0 ) \rVert$ . It is already known that the unforced $( \tilde { \theta } = 0 )$ dynamics of  is exponentially stable ([41], [38]) and from the fact that the forcing term is bounded, we can conclude boundedness of kk irrespective of the collective initial excitation on $Y _ { F _ { i } }$ . Therefore $\| \bar { \mathcal { L } } ( t ) s \| = \| \bar { \mathcal { L } } ( t ) \epsilon \|$ is also uniformly bounded. Therefore, employing the definition of $s _ { i }$ in (8) we obtain,

$$s _ {i} - s _ {j} = \dot {q} _ {i} - \dot {q} _ {j} + \lambda (q _ {i} - q _ {j}) = \psi (t)$$
