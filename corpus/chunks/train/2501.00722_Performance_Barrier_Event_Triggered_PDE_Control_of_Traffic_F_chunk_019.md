Proposition 1 (Well-Posedness between control updates). For given $\left( \bar { w } ( \cdot , t _ { k } ) , \bar { v } ( \cdot , t _ { k } ) \right) ^ { T } \in L ^ { 2 } \left( ( 0 , \ell ) ; \mathbb { R } ^ { 2 } \right)$ , there exists a unique solution $( \bar { w } , \bar { v } ) ^ { T } \in \mathcal { C } ^ { 0 } \left( [ t _ { k } , t _ { k + 1 } ] ; L ^ { 2 } \left( ( 0 , \ell ) ; \mathbb { R } ^ { 2 } \right) \right)$ to the system $( 1 3 ) – ( 1 5 ) , ( 1 7 ) – ( 2 1 ) , ( 3 2 ) , ( 3 3 )$ , between two time instants $t _ { k }$ and $t _ { k + 1 }$ .

Remark 1. This proposition is a straightforward application of Proposition 1 in [7], with the difference up to the scaling factor ℓ, i.e., the road length. Throughout the paper, we establish the well-posedness of the closed-loop system by $\begin{array} { r } { \mathbb { T } = \bigcup _ { k = 0 } ^ { \bar { K } - 1 } \left[ t _ { k } , t _ { k + 1 } \right] \times \{ k \} } \end{array}$ olution where $\mathbb { T } \subset \mathbb { R } _ { \geq 0 } \times \mathbb { N }$ me doand $K$ $t _ { K } = \infty$
