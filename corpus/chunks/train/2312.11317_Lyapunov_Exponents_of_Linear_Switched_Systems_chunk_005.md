We point out that for every $A , B \in \mathfrak { s l } _ { 2 } ( \mathbb { R } )$ such that $a , b \leq 0$ , then $c ^ { 2 } \geq a b$ (see Section 6 for more explanations).

In Figure 1 you can see a graphical representation of Main Theorem.

The whole paper is devoted to prove our Main Theorem and it is structured as follows: in Section 2 we show how computing the Lyapunov exponents $\ell ( A , B )$ can be reduced to an optimal control problem and we will apply Pontryagin Maximum Principle (PMP) to this problem. We stated the precise version of PMP that we used in the Appendix. In Section 3 we will deduce from PMP some general properties of Pontryagin extremals. In particular, we will divide them into two categories: bang-bang extremals and singular extremals. In Section 4 and 5 we study bang-bang and singular extremals respectively. In Section 6 we show which extremal is the optimal one, depending on $\operatorname { t r } ( A ^ { 2 } ) , \operatorname { t r } ( B ^ { 2 } )$ and $\mathrm { t r } ( A B )$ .

![](image/048f6b365ae63dda63aed933924c1e761fd58ab3fe230a924f29ceaaabce8eb3.jpg)  
Figure 1: Graphics of $\ell ( A , B )$ for different ranges of $\operatorname { t r } ( A ^ { 2 } ) , \operatorname { t r } ( B ^ { 2 } ) , \operatorname { t r } ( A B )$ . In the first picture $\operatorname { t r } ( A ^ { 2 } )$ is normalized to 1, while $\operatorname { t r } ( B ^ { 2 } ) \leq 1$ and $\operatorname { t r } ( A B ) \in \mathbb { R } ;$ in the second one $\operatorname { t r } ( A ^ { 2 } ) = 0 , \operatorname { t r } ( B ^ { 2 } ) \leq 0$ and $\operatorname { t r } ( A B ) \in \mathbb { R }$ ; in the third one $\operatorname { t r } ( A ^ { 2 } )$ is normalized $\mathrm { t o } - 1$ , while $\operatorname { t r } ( B ^ { 2 } ) \geq 1$ and $\left| \operatorname { t r } ( A B ) \right| \geq - { \sqrt { \operatorname { t r } ( B ^ { 2 } ) } }$ .
