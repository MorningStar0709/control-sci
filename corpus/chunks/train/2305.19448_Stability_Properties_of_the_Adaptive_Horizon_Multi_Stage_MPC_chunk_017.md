# 4.4. NLP sensitivity

Parametric NLP sensitivities are of our interest because problem (11) is parametric in $x _ { k }$ and ${ \bf { d } } ^ { c }$ , and we will use them in the horizon update algorithm in Section 4.5, and for stability analysis in Section 5. Problem (11) can be written as a general parametric NLP in the form:

$$\min _ {w} J (w, p) \quad \text { s.t. } \quad h (w, p) = 0, \quad g (w, p) \leq 0 \tag {12}$$

where, $w \in \mathbb { R } ^ { n _ { w } }$ is the vector of all optimization variables $( z _ { i } ^ { c } , \nu _ { i } ^ { c } ) .$ , and $p \in \mathbb { R } ^ { n _ { p } }$ is the vector of NLP parameters $( x _ { k }$ , $\mathbf { d } ^ { c } )$ . The problem (12) has a scalar objective function $J \colon \dot { \mathbb { R } ^ { n _ { w } + \dot { n } _ { p } } } \mapsto \mathbb { R }$ , equality constraints $h \colon \mathbb { R } ^ { n _ { w } + n _ { p } } \ \mapsto \ \mathbb { R } ^ { n _ { e } }$ , and inequality constraints $g \colon  { \mathbb { R } } ^ { n _ { w } + n _ { p } } \ \mapsto \  { \mathbb { R } } ^ { n _ { i } }$ i . Then the function $\mathcal { L } ( w , \lambda , \mu , p ) = J ( w , p ) + \lambda ^ { \top } h ( w , p ) + \mu ^ { \top } g ( w , p )$ is the Lagrange function of (12) where,  and $\mu$ , λ, µ, , λ ,are the Lagrange multipliers of appropriate dimension.

A point $w ^ { \star }$ λ µ that (12) satisfies the Karush-Kuhn-Tucker conditions is known as a KKT point [18]. Given a parameter $p ,$ and a point $w ^ { \star }$ that satisfy:

$$\nabla_ {w} \mathcal {L} (w ^ {\star}, \lambda^ {\star}, \mu^ {\star}, p) = 0h (w ^ {\star}, p) = 0g \left(w ^ {\star}, p\right) \leq 0 \tag {13}0 \leq \mu^ {\star} \perp g (w ^ {\star}, p) \leq 0$$

for some multipliers $( \lambda , \mu )$ , where $w ^ { \star }$ is called a KKT point. The set of all multipliers  and $\mu$ that satisfy the KKT λ, µconditions for a certain parameter $p$ is denoted as $M ( p )$ λ. The active constraint set is given by $\begin{array} { r } { \mathcal { A } ( w ^ { \star } ) = \{ j \mid g _ { j } ( w ^ { \star } , p ) = } \end{array}$ ,0}. ⊥ is the complementarity operator, i.e. either  or g (or both) must be zero. Then we define strict complementarity as follows.

Definition 3. (SC, [18]) At the KKT point $w ^ { \star }$ of (12) with multipliers $( \lambda ^ { \star } , \mu ^ { \star } )$ , strict complementarity (SC) condition holds if $\mu _ { j } + g _ { j } ( w ^ { \star } , p ) > 0$ for all $\boldsymbol { j } \in \mathcal { A } ( \boldsymbol { w } ^ { \star } )$ ).
