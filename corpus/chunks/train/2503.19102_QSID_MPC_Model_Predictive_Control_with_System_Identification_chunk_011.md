µRemark 4: Assumption 4 is trivially satisfied if $[ \hat { A } , \hat { B } ]$ is a controllable pair and $\chi _ { f }$ ,can be an arbitrary level set of any valid Lyapunov function under stabilizing control $\mu ( x ) = - K x$ such that $\hat { A } - \hat { B } K$ µhas all eigenvalues within the unit circle.

Assumption 5: Let $V _ { f } ( x ) \triangleq x ^ { \intercal } Q _ { f } x$ and $l ( x , u ) \triangleq x ^ { \top } Q x +$ $u ^ { \top } R u .$ where Q R and $Q _ { f }$ ,are as defined in $( 2 ) . \ V _ { f } ( x )$ is , ,a discrete-time Lyapunov function for $\hat { x } _ { t + 1 } = \hat { A } \hat { x } _ { t } + \hat { B } \hat { u } _ { t }$ with $u _ { t } = \mu ( \hat { x } _ { t } )$ as defined in Assumption 4 within $\chi _ { f }$ . Furthermore, $V _ { f } ( \hat { x } _ { t + 1 } ) - V _ { f } ( \hat { x } _ { t } ) \leq - l ( \hat { x } _ { t } , \mu ( \hat { x } _ { t } ) )$ for all $x _ { t } \in X _ { f }$ .

, µNow we are ready to state the central result concerning the uniform ultimate boundedness of the system dynamics (1) under MPC algorithm 1.

Theorem 3: The system (1) under a control policy computed via Algorithm 1 is uniformly ultimately bounded with an ultimate bound $\delta ( \epsilon )$ as $t \to \infty$ .

δ ϵProof: Let us choose a Lyapunov function $V ( x _ { t } ) \triangleq J ^ { * } ( x _ { t } )$ , i.e., the optimal cost of (2) for computed control sequence $\{ u _ { t | t } ^ { * } , \ldots , u _ { t + T _ { h } - 1 | t } ^ { * } \} .$ . Now, shifting the control sequence by one , . . . ,step and using the feasible control from Assumption 4, we get a new sequence $\{ u _ { t + 1 | t } ^ { * } , \ldots , u _ { t + T _ { h } - 1 | t } ^ { * } , \mu ( \boldsymbol { x } _ { t + T _ { h } | t } ^ { * } ) \}$ . Since this , . . . , , µcontrol sequence is not optimal for (2) starting with $\hat { x } _ { t + 1 } \triangleq$ $x _ { t + 1 | t }$ , we can create a suboptimal cost function

$$J ^ {\prime} (\hat {x} _ {t + 1}) = \sum_ {k = t + 1} ^ {t + T _ {h}} l (x _ {k | t} ^ {*}, u _ {k | t} ^ {*}) + V _ {f} (x _ {t + T _ {h} + 1 | t}),$$

where Howev $u _ { t + T _ { h } | t } ^ { * } \triangleq \mu ( x _ { t + T _ { h } | t } ^ { * } )$ and at th $x _ { t + T _ { h } + 1 | t } \ = \ \hat { A } x _ { t + T _ { h } | t } ^ { * } + \ \hat { B } u _ { t + T _ { h } | t } ^ { * } .$ $\stackrel { \iota } { ( 2 ) } J ^ { * } \stackrel { \iota } { ( \hat { x } _ { t + 1 } ) }$ starting from $\hat { x } _ { t + 1 } = x _ { t + 1 | t }$ should satisfy

$$
\begin{array}{l} J ^ {*} (\hat {x} _ {t + 1}) \leq J ^ {\prime} (\hat {x} _ {t + 1}) \\ = J ^ {*} (x _ {t}) - l (x _ {t} ^ {*}, u _ {t} ^ {*}) - V _ {f} (x _ {t + T _ {h} | t} ^ {*}) \\ + V _ {f} (x _ {t + T _ {h} + 1 | t}) + l (x _ {t + T _ {h}} ^ {*}, \mu (x _ {t + T _ {h} | t} ^ {*})) \\ \end{array}
$$
