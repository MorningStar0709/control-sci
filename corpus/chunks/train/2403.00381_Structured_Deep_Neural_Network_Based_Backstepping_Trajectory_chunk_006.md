# B. Fully Connected Neural Network

Fully Connected Neural Network (FCNN), also known as Multilayer Perceptron (MLP), is a DNN structure. It contains an input layer, one or more hidden layers, and an output layer. Each neuron in each layer is connected to every neuron in the adjacent layer. FCNN has the following expression

$$\boldsymbol {y} _ {i + 1} = \sigma_ {i} \left(\boldsymbol {W} _ {i} \boldsymbol {y} _ {i} + \boldsymbol {b} _ {i}\right), \quad i = 0, \dots , k - 1, \tag {3}f (\boldsymbol {x}; \boldsymbol {\gamma}) = \boldsymbol {y} _ {k},$$

where $\mathbf { \nabla } _ { \mathbf { \psi } _ { 3 } } \mathbf { \psi } _ { 2 } \qquad \mathbf { \psi } _ { 3 } \mathbf { \psi } _ { 4 } \qquad \mathbf { \psi } _ { 3 } \mathbf { \psi } _ { 4 } \qquad \mathbf { \psi } _ { 3 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 3 } \qquad \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 3 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 3 } \qquad \mathbf { \psi } _ { 3 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 3 } \mathbf { \psi } _ { 4 } \mathbf { \psi } _ { 4 }$ are the layer output with ${ \mathbf { } } y _ { 0 } = { \mathbf { } } x$ being the neural network input, σi represents the layer activation function, $\gamma =$ $\left\{ W _ { 0 : k - 1 } , b _ { 0 : k - 1 } \right\}$ are the trainable parameters of FCNN.
