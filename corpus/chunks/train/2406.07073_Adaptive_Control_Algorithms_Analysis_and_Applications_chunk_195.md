$$
\begin{array}{l} \mathbf {E} \{v (t, \hat {\theta}) \tilde {v} (t, \hat {\theta}) \} = \lim _ {N \to \infty} \frac {1}{N} \sum_ {t = 1} ^ {N} v (t, \hat {\theta}) \tilde {v} (t, \hat {\theta}) \\ \geq \lim _ {N \rightarrow \infty} \frac {1}{N} \left[ \frac {1}{2} x (N + 1) ^ {T} P x (N + 1) - \frac {1}{2} x (1) ^ {T} P x (1) \right] \\ + \lim _ {N \rightarrow \infty} \frac {1}{N} \sum_ {t = 1} ^ {N} [ x (t) ^ {T}, v (t, \hat {\theta}) ] M _ {0} \left[\begin{array}{c}x (t)\\v (t, \hat {\theta})\end{array}\right] > 0 \tag {4.51} \\ \end{array}
$$

where $P > 0$ and $M _ { 0 } > 0$ . Therefore, in the case of the output error scheme defined by the predictor of (4.23) and the adaptation algorithm of (4.30) if $\begin{array} { r } { H ( z ^ { - 1 } ) = \frac { 1 } { A ( z ^ { - 1 } ) } } \end{array}$ is a strictly positive real transfer function, global convergence w.p.1 of the estimated parameter vector ${ \hat { \theta } } ( t )$ towards $D _ { c }$ (or $\theta ^ { * }$ under input richness conditions) will be assured.

A similar approach can be used in order to associate an ordinary differential equation for PAA using matrix type adaptation gains of the form:

$$\hat {\theta} (t + 1) = \hat {\theta} (t) + F (t) \phi (t) v (t + 1) \tag {4.52}F (t + 1) ^ {- 1} = F (t) ^ {- 1} + \lambda_ {2} \phi (t) \phi (t) ^ {T}0 < \lambda_ {2} < 2; F (0) > 0 \tag {4.53}$$

In order to use the previous results for the stochastic approximation adaptation gain (4.17), it is convenient to replace $F ( t )$ by:

$$R (t) = \frac {1}{t} F (t) ^ {- 1} \tag {4.54}$$

and therefore (4.52) and(4.53) will take the form:

$$\hat {\theta} (t + 1) = \hat {\theta} (t) + \frac {1}{t} R ^ {- 1} (t) \phi (t) \nu (t + 1) \tag {4.55}R (t + 1) = \hat {R} (t) + \frac {1}{t + 1} [ \lambda_ {2} \phi (t) \phi (t) ^ {T} - R (t) ] \tag {4.56}$$

Following the same type of developments as in the case of stochastic approximation algorithm and using the relation:

$${\frac {1}{N + 1}} \sum_ {i = 0} ^ {N} {\frac {1}{t + i}} R ^ {- 1} (t + i) \approx {\frac {1}{N + 1}} \left(\sum_ {i = 0} ^ {N} {\frac {1}{t + i}}\right) R ^ {- 1} (t)$$

for $t \gg 1 , 1 \ll N \ll t$ one gets the following associated ODE (Ljung 1977a; Ljung and Söderström 1983; Dugard and Landau 1980):

$$\frac {d \hat {\theta}}{d \tau} = R ^ {- 1} (\tau) f (\hat {\theta}) \tag {4.57}\frac {d R}{d \tau} = \lambda_ {2} G (\hat {\theta}) - R (\tau) \tag {4.58}$$

where:
