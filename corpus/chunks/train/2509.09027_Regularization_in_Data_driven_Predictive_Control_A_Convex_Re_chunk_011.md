# 2.2 Direct data-driven predictive control

We here review the notion of persistent excitation (PE), which ensures the offline data is sufficiently rich.

Definition 1 (Persistently Exciting) A sequence of data points $\omega \doteq \mathrm { c o l } ( \omega ( 1 ) , \omega ( 2 ) , \dots , \omega ( \bar { T } ) )$ of the length T is persistently exciting (PE) of order $\overset { \cdot } { L } ( \overset { \cdot } { L } < T )$ if its associated Hankel matrix with depth $L _ { i }$ ,

$$
\mathcal {H} _ {L} (\omega) = \left[ \begin{array}{c c c c} \omega (1) & \omega (2) & \dots & \omega (T - L + 1) \\ \omega (2) & \omega (3) & \dots & \omega (T - L + 2) \\ \vdots & \vdots & \ddots & \vdots \\ \omega (L) & \omega (L + 1) & \dots & \omega (T) \end{array} \right],
$$

has full row rank.

The following result from [15], commonly known as the fundamental lemma, forms the foundation of many recent results of direct data-driven predictive control.

Lemma 1 (Fundamental Lemma [15]) Suppose (1) is controllable. Given a length-T input/output trajectory $u _ { \mathrm { d } } \in \mathbb { R } ^ { m T }$ and gth-L $y _ { \mathrm { d } } \in \mathbb { R } ^ { p T }$ where utput se $u _ { \mathrm { d } }$ is Pence $L + n ,$ $\{ u _ { \mathrm { s } } ( k ) , y _ { \mathrm { s } } ( k ) \} _ { k = 0 } ^ { L - 1 }$ $g \in \mathbb { R } ^ { T - L + 1 }$ such that

$$
\left[ \begin{array}{l} \mathcal {H} _ {L} (u _ {\mathrm{d}}) \\ \mathcal {H} _ {L} (y _ {\mathrm{d}}) \end{array} \right] g = \left[ \begin{array}{l} u _ {\mathrm{s}} \\ y _ {\mathrm{s}} \end{array} \right]. \tag {3}
$$

If L is not smaller than the lag of the system (1), matrix co $| ( \mathcal { H } _ { L } ( u _ { \mathrm { d } } ) , \mathcal { H } _ { L } ( y _ { \mathrm { d } } ) )$ has rank $m L + n$ .

The fundamental lemma gives a parameterization of all finite-dimensional input/output trajectories of (1) using only one offline trajectory {ud, yd}. In particular, the image of the Hankel matrix in (3) is the same as the set of all system trajectories of length L.

With the fundamental lemma, we can use (3) to build a predictive model to replace $\mathrm { ( 2 a ) } - \mathrm { ( 2 c ) }$ . This is the basic idea in DeePC [17]. In particular, we consider the input/output sequence with length L satisfying $L \ =$ $T _ { \mathrm { i n i } } + N$ . The Hankel matrix formed by the offline data in (3) is partitioned as
