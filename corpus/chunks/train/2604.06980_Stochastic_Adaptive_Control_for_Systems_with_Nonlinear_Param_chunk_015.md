# Algorithm 1 Online WLS-type estimator for $\overbrace { \theta ^ { * } }$

Initialization: Set the initial estimate as $\begin{array} { r l } { \hat { \theta } _ { 0 } } & { { } = } \end{array}$ $[ \hat { A } _ { 0 } , \hat { B } _ { 0 } ] ^ { \top } \in \Theta _ { : }$ initialize the matrix $P _ { 0 } = I _ { n + m } ,$ , and choose a positive real $\delta > 0 .$

Update flows: Given the observation $\left( u _ { t } , x _ { t + 1 } \right)$ for each time $t > 0 ,$ , the estimate $\widehat { \theta } _ { t + 1 }$ is recursively updated as follows:

$$\hat {\theta} _ {t + 1} = \Pi_ {\Theta} ^ {t + 1} \{\hat {\theta} _ {t} + \frac {1}{\mu_ {t}} d _ {t} P _ {t + 1} \phi_ {t} [ x _ {t + 1} - f (\hat {\theta} _ {t} ^ {\top} \phi_ {t}) ] ^ {\top} \}, (9)P _ {t + 1} = P _ {t} - a _ {t} d _ {t} ^ {2} P _ {t} \phi_ {t} \phi_ {t} ^ {\top} P _ {t}, \tag {10}a _ {t} = \left(\mu_ {t} + d _ {t} ^ {2} \phi_ {t} ^ {\top} P _ {t} \phi_ {t}\right) ^ {- 1}, \tag {11}$$

where $\begin{array} { r c l c r } { \phi _ { t } } & { = } & { \left[ x _ { t } ^ { \top } , u _ { t } ^ { \top } \right] ^ { \top } , \mu _ { t } } & { = } & { ( 1 + \log r _ { t } ) ^ { 1 + \delta } + } \end{array}$ $\begin{array} { r } { d _ { t } \overline { { g } } _ { t } ^ { 2 } \phi _ { t } ^ { \top } P _ { t } \phi _ { t } , r _ { t } = 1 + \sum _ { \tau = 0 } ^ { t } \| \phi _ { \tau } \| ^ { 2 } , d _ { t } = \frac { 1 } { 2 } \alpha ( \| \hat { \theta } _ { t } ^ { \top } \phi _ { t } \| + } \end{array}$ $\operatorname* { m a x } _ { \theta \in \Theta } \| \theta ^ { \top } \phi _ { t } \| ) , \overline { { g } } _ { t } = \beta ( \| \hat { \theta } _ { t } ^ { \top } \phi _ { t } \| + \operatorname* { m a x } _ { \theta \in \Theta } \| \theta ^ { \top } \phi _ { t } \| )$ , and function operator tions α and $\beta$ $\Pi _ { \Theta } ^ { t + 1 }$ are defined in Assumption 1. The projec- is defined as

$$
\Pi_ {\Theta} ^ {t + 1} (x) = \left\{ \begin{array}{l l} x, & \text {if} x \in \Theta , \\ \underset {y \in \Theta_ {\epsilon}} {\arg \min} \operatorname{tr} [ (x - y) ^ {\top} P _ {t + 1} ^ {- 1} (x - y) ], & \text {if} x \notin \Theta , \end{array} \right.
$$

where $\Theta _ { \epsilon } = \left\{ \theta \in \Theta : \left\| \theta - \theta ^ { * } \right\| \leq \epsilon \right\}$ , and ϵ is given in Assumption 3.

Squaring both sides of (14) and using (13), we obtain
