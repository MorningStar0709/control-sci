# II. THE DISCRETE-TIME MPC FORMULATION

In this section, we describe a rather flexible MPC formulation: We allow time-dependent weights, time-dependent constraints, and a cost on the rate of change of control. We note that, similar to [1, §6], the basic formulation can easily be extended in several ways, in order to account, $\mathrm { e . g . }$ , for reference tracking, measured disturbances, and soft constraints, but we omit those here for the sake of brevity.

Let U and X be real Hilbert spaces and let $A \in { \mathcal { L } } ( X ) , B \in$ ${ \mathcal { L } } ( U , X )$ be bounded linear operators. Consider the discretetime plant dynamics

$$x _ {n + 1} = A x _ {n} + B u _ {n}, \quad n \in \mathbb {N} _ {0}, \tag {1}$$

where $u _ { n } \in U$ is the input at the discrete time step n and $x _ { n } \in X$ is the state at time n. The objective is to steer $x _ { n }$ in (1) to zero in such a way that a quadratic cost functional of the following form is minimized:

$$
\begin{array}{l} \sum_ {n = 0} ^ {\infty} \left(\left\langle \left[ \begin{array}{l l} Q & M \\ M ^ {*} & R \end{array} \right] \left[ \begin{array}{l} x _ {n} \\ u _ {n} \end{array} \right], \left[ \begin{array}{l} x _ {n} \\ u _ {n} \end{array} \right] \right\rangle \right. \tag {2} \\ \left. + \langle V (u _ {n} - u _ {n - 1}), u _ {n} - u _ {n - 1} \rangle\right), \\ \end{array}
$$

for some appropriate weights $Q , M , R , V$ and some arbitrarily fixed $u _ { - 1 } \in U$ , say $u _ { - 1 } = 0$ , while satisfying some affine constraints on the input u and the state $x ,$ which we denote by $\left\lceil \begin{array} { c } { { x _ { n } } } \\ { { u _ { n - 1 } } } \\ { { u _ { n } } } \end{array} \right\rceil \in \mathcal { W }$ for $n = 0 , 1 , \ldots$ . In the optimal control problem un $\left( 1 \right) - \left( 2 \right)$ , the initial state $x _ { 0 }$ is given, and the optimization variable is the control signal $\{ u _ { n } \} _ { n = 0 } ^ { \infty }$ . The optimization problem (3)–(4) below should be interpreted analogously.

One may try to solve the above optimal control problem approximately by approximating the coChoosing a horizon N and denoting $\mathbf { u } ^ { \prime } : = \mathbf { \bar { \Psi } } ( u _ { k } ^ { \prime } ) _ { k = 0 } ^ { N - 1 }$ sum., the control step $u _ { n }$ at time n is chosen as the first element $u _ { * , 0 }$ of the minimizer u∗ = (u∗,k)N−1k=0 ${ \mathbf { u } } _ { * } = ( u _ { * , k } ) _ { k = 0 } ^ { N - 1 }$ of the cost functional
