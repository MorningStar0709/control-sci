Algorithm 1 Computation of $\Gamma ( x _ { o } , \eta )$ for some $\delta \in ( 0 , 1 )$ , some $V _ { \mathrm { m a x } } \in \mathbb { R } _ { > 0 }$ and given $C ( x _ { o } , \eta ) .$ .   
1: $V \leftarrow V(x_o)$ , $C \leftarrow C(x_o, \eta)$ 2: $\bar{h} \leftarrow \delta T_{\max}\left(\gamma_1, L_1 + \frac{\epsilon_1}{2}\right)$ 3: for each $i \in \{2, \ldots, n_p\}$ do
4: $\Lambda_i \leftarrow \max\left\{L_i + \frac{\epsilon_i}{2}, (1 - \delta)\right\}$ 5: if $V_{\max} \geq C \geq V$ then
6: if $-\epsilon_i + \epsilon_{ref} > 0$ then
7: $\bar{h}_i \leftarrow \min\left\{\delta T_{\max}(\gamma_i, \Lambda_i), \frac{\log(C) - \log(V)}{-\epsilon_i + \epsilon_{\text{ref}}}\right\}$ 8: else
9: $\bar{h}_i \leftarrow \delta T_{\max}(\gamma_i, \Lambda_i)$ 10: end if
11: else
12: $\bar{h}_i \leftarrow 0$ 13: end if
14: if $\bar{h}_i > \bar{h}$ then
15: $\bar{h} \leftarrow \bar{h}_i$ 16: end if
17: end for
18: $\Gamma(x_o, \eta) \leftarrow \bar{h}$

$$C (x _ {o}, \eta) = \frac {1}{m} \left(V (x _ {o}) + \sum_ {k = 1} ^ {m - 1} \eta_ {k}\right). \tag {19}$$

The modified procedure to select transmission times is described by Algorithm 1, which is a modified variant of (Hertneck and Allg¨ower, 2021, Algorithm 2). Note that an additional important modification in the algorithm, that is needed for technical reasons to obtain stability guarantees, is that the algorithm selects $\begin{array} { r } { \bar { h } > \delta T _ { \mathrm { m a x } } ( \gamma _ { 1 } , \bar { L } _ { 1 }  + \frac { \epsilon _ { 1 } } { 2 } ) } \end{array}$ only if $C ( x _ { o } ( s _ { j } ) , \bar { \eta ( s _ { j } ) } ) ~ \le ~ V _ { \mathrm { m a x } }$ for the chosen $V _ { \mathrm { m a x } } .$ This means that the algorithm resorts to the fallback strategy if the value of the observed Lyapunov function exceeds the bound $V _ { \mathrm { m a x } } .$ It is needed if $x _ { p }$ and $x _ { o }$ grow faster than $e _ { o }$ converges to limit the maximum increase of $x _ { p }$ and $x _ { o } .$ We obtain the following result for the proposed dynamic STC mechanism with output feedback.
