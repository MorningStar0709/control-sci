For the function $f : \mathbb { R } ^ { n }  \mathbb { R } ^ { n }$ , let $f ^ { ( k ) } : \mathbb { R } ^ { n }  \mathbb { R }$ denote the $k ^ { t h }$ element of $f ,$ and let $\mathbf { H } _ { f ( k ) }$ denote the Hessian for this element, where $k \in \mathbb { Z } _ { 1 } ^ { n }$ . Further, let $f \in \mathcal { C } ^ { r } ( \mathcal { X } )$ denote a real valued function is r-times differentiable over the domain X . The right-hand (left-hand) upper Dini-derivatives for a function, f, is defined $D ^ { + } f ( \mathbf { y } ) : =$ lim supk→0+ $\begin{array} { r } { \operatorname* { s u p } _ { k \to 0 ^ { + } } \frac { f ( x + k g ( x ) ) - f ( x ) } { k } } \end{array}$

$\begin{array} { r } { ( \operatorname* { l i m } \operatorname* { s u p } _ { k \to 0 ^ { - } } \frac { f ( x + k \overset {  } { g } ( x ) ) - f ( x ) } { k } ) } \end{array}$ , where $k \in \mathbb { R }$ and $\scriptstyle { \dot { x } } = g ( x )$ [8].
