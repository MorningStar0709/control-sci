Constant $\hat { \pmb { d } } .$ If $\hat { d }$ remains constant, $\bar { z } ( \hat { d } , \bar { r } )$ is exponentially stable for $z ^ { + } = A z + B _ { d } \hat { d } + B \bar { \kappa } _ { N } ( z , \hat { d } , \bar { r } )$ with a region of attraction $\mathcal { Z } _ { N } ( \hat { d } , \bar { r } )$ . It can be shown, as in the proof of Proposition 5.5, that the set $\mathcal { A } ( \hat { d } , \bar { r } ) : =$ $( \{ \bar { z } ( \hat { d } , \bar { r } ) \} \oplus \mathbb { S } ) \times \{ \bar { z } ( \hat { x } , \bar { r } ) \}$ } is exponentially stable for the composite system $\hat { x } ^ { + } = A \hat { x } + B _ { d } \hat { d } + B \kappa _ { N } ( \hat { x } , z , \hat { d } , \bar { r } ) + \delta _ { x } , z ^ { + } = A z + B _ { d } \hat { d } + B \bar { \kappa } _ { N } ( z , \hat { d } )$ , $\delta _ { x } \in \mathbb { \Delta } _ { x }$ , with a region of attraction $( \mathcal { Z } _ { N } ( \hat { d } , \bar { r } ) \oplus \mathbb { S } ) \times \mathcal { Z } _ { N } ( \hat { d } , \bar { r } )$ . Hence $x ( i ) \in \{ z ( i ) \}$ ⊕ Γ tends to the set $\{ \bar { z } ( \hat { d } , \bar { r } ) \}$ ⊕ Γ exponentially fast. If the external disturbance $w$ is zero, $\mathbb { W } = \{ 0 \}$ . If, in addition, $\mathbb { N } = \left\{ 0 \right\}$ , then $\Delta = \{ 0 \}$ and ${ \mathbb S } = \{ 0 \}$ and $x ( i )  \bar { z } ( \hat { d } , \bar { r } )$ exponentially fast so that the tracking error $\tilde { r } ( i )  0$ as $i  \infty$ .

Slowly varying $\hat { \pmb { d } } .$ If $\hat { d }$ is varying, the inequality for $\Delta V _ { N } ^ { 0 } ( \cdot )$ must be replaced by

$$\Delta V _ {N} ^ {0} (z, \hat {d}, \bar {r}, w _ {d}) = V _ {N} ^ {0} (A z + B _ {d} \hat {d} + B \bar {\kappa} _ {N} (z, \hat {d}, \bar {r}), \hat {d} + \delta_ {d}, \bar {r}) - V _ {N} ^ {0} (z, \hat {d}, \bar {r})\leq - c _ {1} \left| z - \bar {z} (\hat {d}, \bar {r}) \right| ^ {2} + \bar {k} | \delta_ {d} |$$
