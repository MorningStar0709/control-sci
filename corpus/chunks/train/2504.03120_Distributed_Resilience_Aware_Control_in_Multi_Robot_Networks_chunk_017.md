# A. Constraint with Global State Knowledge

1) Constraint for Degree Maintenance: We define the function that encodes maintenance of $F ^ { \prime }$ neighbors for robot $i \in \mathcal { V } ;$ :

$$h _ {i} (\mathbf {x}) = \sum_ {j \in \mathcal {N} _ {i} (t)} a _ {i j} (\mathbf {x}) - F ^ {\prime}. \tag {9}$$

Since $a _ { i j } \mathbf { \left( x \right) } \mathbf { \left( 3 \right) }$ is continuously differentiable, $h _ { i } ( \mathbf { x } )$ is also continuously differentiable. Note that although we define n CBFs so that all robots (both normal and malicious) have the same controller by Assumption 1, as will be shown in Remark 4, the guarantee of maintaining $F ^ { \prime }$ neighbors may apply only to normal agents.

Lemma 2. Let $a _ { i j } ( { \bf x } )$ be computed from (3) with $q _ { 1 } = 2 + \epsilon ,$ where $\textstyle 0 < \epsilon < { \frac { \mathrm { i } } { n - 1 } }$ , and $q _ { 2 } \in \mathbb { R } _ { + }$ at time t. Then, $h _ { i } ( { \bf x } ) \geq 0$ only $i f \delta _ { i } ( { \mathcal { G } } ( t ) ) \stackrel { \cdot \cdot } { = } \tilde { F } ^ { \prime }$ at time t.

Proof. With $q _ { 1 } = 2 + \epsilon$ , where $\textstyle 0 < \epsilon < { \frac { 1 } { n - 1 } }$ , and $q _ { 2 } \in \mathbb { R } _ { + }$ at time $\begin{array} { r } { t , 0 \leq a _ { i j } ( { \bf x } ) < 1 + \frac { 1 } { n - 1 } } \end{array}$ . Then, given $K = \delta _ { i } ( { \mathcal { G } } ( t ) ) \leq$ $\begin{array} { r } { n - 1 , \sum _ { i \in \mathcal { N } _ { i } ( t ) } a _ { i j } ( { \bf x } ) < K + \frac { K } { n - 1 } \le K + 1 \ \forall t \ge t _ { 0 } } \end{array}$ . Thus, $\begin{array} { r } { \sum _ { j \in \mathcal { N } _ { i } ( t ) } \dot { a } _ { i j } \ddot { ( \mathbf { x } ) } \geq F ^ { \prime } } \end{array}$ only if $\begin{array} { r } { \dot { \delta _ { i } } ( \bar { \mathcal { G } } ( t ) ) \geq F ^ { \prime } } \end{array}$ . □

With Lemma $2 , h _ { i } ( { \bf x } ) \ge 0$ only if robot i has at least $F ^ { \prime }$ neighbors. Thus, if $\mathbf { x } \in \mathcal { C } _ { i } = \{ \mathbf { x } \in \mathbb { R } ^ { n m } \mid h _ { i } ( \mathbf { x } ) \geq 0 \}$ at time $t ,$ all robots are positioned in such a way that $\delta _ { i } ( { \mathcal { G } } ( t ) ) \geq F ^ { \prime }$ at t. Since we want $\delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) \geq F ^ { \prime } \forall t \geq t _ { 0 } .$ , the safe set of interest is $\mathcal { C } = \cap _ { i \in \mathcal { L } } \mathcal { C } _ { i } . \mathrm { ~ A l s o , ~ ( 9 ) ~ }$ does not specify which neighbors each robot should connect to, allowing robots the flexibility to form links dynamically. This enables the overall network to be time-varying without fixed topologies.
