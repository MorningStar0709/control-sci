# 3.1 Homogeneous universal approximation theorem

In the case of homogeneous function, the universal approximation theorem can be revisited as follows.

Theorem 3 Let d be a linear continuous dilation in $\mathbb { R } ^ { n }$ . Let $\| \cdot \| _ { \mathbf { d } }$ be an arbitrary (explicit or implicit) d-homogeneous norm in $\mathbb { R } ^ { n }$ . Let $\sigma \in C ( \mathbb { R } , \mathbb { R } )$ be a non-polynomial function.

$I f h \in C ( \mathbb { R } ^ { n } \backslash \{ \mathbf { 0 } \} , \mathbb { R } )$ is a d-homogeneous function of degree $\nu \in \mathbb { R }$ then for any $\epsilon > 0$ there exist $N \in \mathbb { N } , A \in \mathbb { R } ^ { N \times n } , b \in \bar { \mathbb { R } } ^ { N }$ and $C \in \mathbb { R } ^ { 1 \times N }$ such that

$$\left| h (x) - h _ {\varepsilon} (x) \right| \leq \epsilon \| x \| _ {\mathbf {d}} ^ {\nu}, \quad \forall x \in \mathbb {R} ^ {n} \backslash \{\mathbf {0} \}, \tag {12}h _ {\epsilon} (x) = \| x \| _ {\mathbf {d}} ^ {\nu} C \sigma (A \mathbf {d} (- \ln \| x \| _ {\mathbf {d}}) x + b), \tag {13}$$

where the function $\sigma$ in the above formula is applied in the component-wise manner.

Proof. By definition, the d-homogeneous norm $\| \cdot \| _ { \mathbf { d } }$ is a continuous positive definite function satisfying $\| \mathbf { d } ( s ) x \| _ { \mathbf { d } } = e ^ { s } \| x \| _ { \mathbf { d } }$ . Then $\| \cdot \| _ { \mathbf { d } }$ is radially unbounded and the set

$$S _ {\mathbf {d}} = \{x \in \mathbb {R} ^ {n}: \| x \| _ {\mathbf {d}} = 1 \}$$

is a compact such that $\mathbf { 0 } \not \in S _ { \mathbf { d } }$ . Since $\| \mathbf { d ( - l n } \left\| x \right\| _ { \mathbf { d } } ) x \| _ { \mathbf { d } } = e ^ { - \ln { \| x \| _ { \mathbf { d } } } } \| x \| _ { \mathbf { d } } = 1$ for any x $\neq 0$ then $\mathbf { d } ( - \ln \| x \| _ { \mathbf { d } } ) x \in S _ { \mathbf { d } }$ . Let $\| \cdot \|$ be a norm in $\mathbb { R } ^ { n }$ and

$$r _ {1} := \inf _ {x \in \mathbb {R} ^ {n} \setminus \{\mathbf {0} \}} \| \mathbf {d} (- \ln \| x \| _ {\mathbf {d}}) x \| > 0,r _ {2} := \sup _ {x \in \mathbb {R} ^ {n} \setminus \{\mathbf {0} \}} \| \mathbf {d} (- \ln \| x \| _ {\mathbf {d}}) x \| \in [ r _ {1}, + \infty).$$

Let us consider the set

$$\Omega = \{y \in \mathbb {R} ^ {n}: r _ {1} \leq \| y \| \leq r _ {2} \}.$$

By construction, the set Ω is a compact and ${ \textbf { 0 } } \notin { \Omega } .$ Since the function h is continuous on Ω, then, by Theorem 2, for any $\epsilon > 0$ there exists $N \in \mathbb { N }$ , $A \in \mathbb { R } ^ { N \times n } , b \in \mathbb { R } ^ { N }$ and $C \in \mathbb { R } ^ { 1 \times N }$ such that
