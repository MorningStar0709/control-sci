$$\left\| \Phi (t) \right\| ^ {2} \geq \mu_ {1} ^ {2} \| \varsigma (t) \| ^ {2} + \mu_ {2} ^ {2} \left(\left\| \varsigma (t) \right\| ^ {2}\right) ^ {\alpha} + \mu_ {3} ^ {2} (N q) ^ {1 - \beta} \left(\left\| \varsigma (t) \right\| ^ {2}\right) ^ {\beta}. \tag {32}$$

It follows from (28) and (32) that

$$\dot {V} (t) \leq - \frac {\mu_ {1} ^ {2} - \| K \otimes S \|}{2} \| \varsigma (t) \| ^ {2} - \frac {\mu_ {2} ^ {2}}{2} (\| \varsigma (t) \| ^ {2}) ^ {\alpha}- \frac {\mu_ {3} ^ {2}}{2} (\| \varsigma (t) \| ^ {2}) ^ {\beta}\leq - c _ {1} ((\| \varsigma (t) \| ^ {2}) ^ {\beta} + \| \varsigma (t) \| ^ {2} + (\| \varsigma (t) \| ^ {2}) ^ {\alpha}), \tag {33}$$

where $c _ { 1 }$ is defined below (9).

Recall that $0 < \alpha < 1 < \beta$ . Applying Lemma 4 to (27) gives

$$V (t) \leq c _ {2} (\| \varsigma (t) \| ^ {2} + (\| \varsigma (t) \| ^ {2}) ^ {\frac {\alpha + 1}{2}} + (\| \varsigma (t) \| ^ {2}) ^ {\frac {\beta + 1}{2}}), \tag {34}$$

where $c _ { 2 }$ is defined below (9). Since $\textstyle \alpha < { \frac { \alpha + 1 } { 2 } } < 1$ and $1 <$ β+1 < β, one can infer that $\begin{array} { r } { \frac { \beta + 1 } { 2 } < \beta . } \end{array}$ 2

$$\left(\| \varsigma (t) \| ^ {2}\right) ^ {\frac {\alpha + 1}{2}} \leq \| \varsigma (t) \| ^ {2} + \left(\| \varsigma (t) \| ^ {2}\right) ^ {\alpha}, \tag {35}\left(\| \varsigma (t) \| ^ {2}\right) ^ {\frac {\beta + 1}{2}} \leq \| \varsigma (t) \| ^ {2} + \left(\| \varsigma (t) \| ^ {2}\right) ^ {\beta}. \tag {36}$$

Then, substituting (35) and (36) into (34) leads to

$$V (t) \leq c _ {2} ((\| \varsigma (t) \| ^ {2}) ^ {\beta} + (\| \varsigma (t) \| ^ {2}) ^ {\alpha} + 3 \| \varsigma (t) \| ^ {2}). \tag {37}$$

Since $\begin{array} { r } { \frac { 1 } { \alpha } < \beta , } \end{array}$ , one has $\begin{array} { r } { \frac { ( \alpha + 1 ) \beta } { \beta + 1 } > 1 } \end{array}$ and $\begin{array} { r } { \frac { ( \beta + 1 ) \alpha } { \alpha + 1 } > 1 } \end{array}$ > 1. Then, $\begin{array} { r } { \frac { 2 \alpha } { \alpha + 1 } < 1 } \end{array}$ Lem and $0 < \alpha < 1$ 7) along with , yields $\begin{array} { r } { \frac { 2 \beta } { \beta + 1 } > 1 , \beta > 1 , 0 < } \end{array}$ <
