$$\bar {\mathcal {O}} := \sigma_ {1} L _ {3} ^ {\top} \Omega_ {z} L _ {3} + \sigma_ {2} L _ {7} ^ {\top} \Omega_ {z} L _ {7} - (L _ {3} - L _ {7}) ^ {\top} \Omega_ {z} (L _ {3} - L _ {7})\mathcal {D} := \left(L _ {1} + \epsilon L _ {2}\right) ^ {\top}, \mathcal {F} := \left[ L _ {1} ^ {\top} G ^ {\top}, L _ {7} ^ {\top} K _ {c} ^ {\top} \right] ^ {\top}\mathcal {T} _ {1} := \varepsilon \mathcal {V} _ {1} \Theta_ {A B} \mathcal {V} _ {1} ^ {\top}, \mathcal {T} _ {2} := \varepsilon \mathcal {V} _ {1} \Theta_ {A B} \mathcal {V} _ {2} ^ {\top}, \mathcal {T} _ {3} := \varepsilon \mathcal {V} _ {2} \Theta_ {A B} \mathcal {V} _ {2} ^ {\top}
\mathcal {V} _ {1} := \left[ \begin{array}{c c} I & 0 \end{array} \right],   \mathcal {V} _ {2} := \left[ \begin{array}{c c} 0 & \mathcal {D} \end{array} \right].
$$

Moreover, $K = K _ { c } G ^ { - 1 }$ and $\Omega = G ^ { - 1 } { } ^ { \top } \Omega _ { z } G ^ { - 1 }$ are desired the controller gain K and triggering matrix.

Proof. Replacing x of the functional in (12) with the state $z ,$ the following functional is built for the system (20)

$$V (z, t) = V _ {a} (z (t)) + V _ {l} (z (t), t) + \eta (t). \tag {22}$$

Using the descriptor method again, the system (20) can be represented as

$$0 = 2 \xi_ {z} ^ {\top} (t) \mathcal {D} (A G L _ {1} + B K _ {c} L _ {7} - G L _ {2}) \xi_ {z} (t) \tag {23}$$

where  is a given constant and $\xi _ { z } ( t ) : = [ z ^ { \top } ( t ) , z ^ { \top } ( t +$

$$1), z ^ {\top} (\tau_ {j} ^ {k}), z ^ {\top} (\tau_ {j + 1} ^ {k}), \sum_ {i = \tau_ {j} ^ {k}} ^ {t} \frac {z ^ {\top} (i)}{t - \tau_ {j} ^ {k} + 1}, \left. \sum_ {i = t} ^ {\tau_ {j + 1} ^ {k}} \frac {z ^ {\top} (i)}{\tau_ {j + 1} ^ {k} - t + 1}, z ^ {\top} (t _ {k}) \right] ^ {\top}.$$

Triggering condition $\textcircled { 9 }$ and Lemma 2 prove that $\eta ( \tau _ { j + 1 } ^ { k } ) -$ $\eta ( \tau _ { j } ^ { k } ) \leq \xi ^ { \top } ( t ) \mathcal { O } \xi ( t )$ as in (17), which directly ensures the following inequality with $x ( t ) = G z ( t )$

$$\eta (\tau_ {j + 1} ^ {k}) - \eta (\tau_ {j} ^ {k}) \leq \xi_ {z} ^ {\top} (t) \bar {\mathcal {O}} \xi_ {z} (t). \tag {24}$$

Imitating (18), it can be obtained that

$$
\begin{array}{l} \Delta V (z, t) + \eta (\tau_ {j} ^ {k}) - \eta (\tau_ {j + 1} ^ {k}) - \Delta \eta (t) \\ \leq \xi_ {z} ^ {\top} (t) \left[ \frac {t - \tau_ {j} ^ {k}}{h} \bar {\Upsilon} _ {1} (h) + \frac {\tau_ {j + 1} ^ {k} - t}{h} \bar {\Upsilon} _ {2} (h) \right] \xi_ {z} (t) \\ \end{array}
$$
