consistent with the data. Note that the true matrices $\left| \begin{array} { l l } { \boldsymbol { A } ^ { * } } & { \boldsymbol { B } ^ { * } } \end{array} \right|$ are contained within Σ as they satisfy $\left\| x _ { k + 1 } - \left[ A ^ { * } \quad B ^ { * } \right] \left[ { x _ { k } } \right] \right\| _ { 2 } \leq$ $\epsilon , i = 0 , \dots , N { - } 2$ . While the set membership (77) could directly ϵ, , . . . ,be exploited to verify dissipativity following Berberich et al. (2022b) or Martin and Allgöwer (2021b), we will consider here an ellipsoidal outer approximation of (77) given by

$$
\tilde {\Sigma} = \left\{\left[ \begin{array}{l l} A & B \end{array} \right]: \left[ \begin{array}{c c} & I \\ [ A & B ] \end{array} \right] ^ {\top} \Delta \left[ \begin{array}{c c} & I \\ [ A & B ] \end{array} \right] \preceq 0 \right\} \supseteq \Sigma , \tag {78}
$$

as proposed in Martin and Allgöwer (2022a, Proposition 1). The computation of matrix ∆ boils down to an SDP with an LMI constraint because Σ corresponds to an intersection of $N - 1$ quadratic constraints (Boyd et al., 1994). Thereby, Σ˜ is described by a single quadratic matrix inequality. However, this adds an additional conservatism due to a non-tight description of Σ. Since the set of feasible system matrices is characterized by the quadratic uncertainty description (78), we can analyze the set of linear systems with system matrices $( A , B )$ where

$$
x _ {k + 1} = A x _ {k} + B u _ {k}, \quad \left[ \begin{array}{l l} A & B \end{array} \right] \in \tilde {\Sigma}, \tag {79}
$$

using modern robust control techniques. For that purpose, we equivalently write (79) as a linear fractional representation (Scherer and Weiland, 2000)

$$
\left[ \begin{array}{c} x _ {k + 1} \\ q _ {k} \end{array} \right] = \left[ \begin{array}{l l l} 0 & 0 & I \\ \left[ \begin{array}{l} I \\ 0 \end{array} \right] & \left[ \begin{array}{l} 0 \\ I \end{array} \right] & 0 \end{array} \right] \left[ \begin{array}{c} x _ {k} \\ u _ {k} \\ w _ {k} \end{array} \right] \tag {80}
$$

with uncertainty $w _ { k } = \left\lceil A \quad B \right\rceil q _ { k }$ satisfying the quadratic constraint

$$
\left[ \begin{array}{c} q _ {k} \\ w _ {k} \end{array} \right] ^ {\top} \Delta \left[ \begin{array}{c} q _ {k} \\ w _ {k} \end{array} \right] \preceq 0.
$$

Now, all systems of (79) are $( Q , S , R )$ -dissipative for the storage function $V ( x ) = x ^ { \top } X x , X \succeq 0$ ,, on $\chi \times \mathcal { U } = \mathbb { R } ^ { n _ { x } + n _ { u } }$ if
