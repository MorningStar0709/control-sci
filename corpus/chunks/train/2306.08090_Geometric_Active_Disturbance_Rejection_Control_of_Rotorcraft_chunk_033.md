where $e _ { \varphi }$ is updated by the translational ESO error dynamics according to (58). The tracking error dynamics (85) combined with the ESO error dynamics (58) is FFTS at the origin $( ( \tilde { b } , \tilde { v } , e _ { b } , e _ { v } , e _ { \varphi } ) = ( 0 , 0 , 0 , 0 , 0 ) )$ ), when the resultant disturbance force is constant $( \dot { \varphi } _ { D } = 0 )$ , and the gains of the ESO and ADRC schemes are constrained according to Proposition 1 and Proposition 3.

Proof. Simplify (85) as:

$$\dot {\tilde {b}} = \tilde {v}m \dot {\psi} _ {T} = e _ {\varphi} - k _ {T D} L _ {T} \left[ \psi_ {T} + \left(\psi_ {T} ^ {\mathrm{T}} \psi_ {T}\right) ^ {\frac {1 - p}{p}} \psi_ {T} \right] \tag {86}- k _ {T P} L _ {T} \tilde {b}.$$

Next, define the Lyapunov function:

$$V _ {T} = V _ {t} + \frac {1}{2} m \psi_ {T} ^ {\mathrm{T}} \psi_ {T} + \frac {1}{2} k _ {T P} \tilde {b} ^ {\mathrm{T}} \tilde {b}, \tag {87}$$

where Vt is defined by (60) in the proof of Theorem 2. We obtain the time-derivative of this Lyapunov function (87) as:

$$
\begin{array}{l} \dot {V} _ {T} = m \psi_ {T} ^ {\mathrm{T}} \dot {\psi} _ {T} + k _ {T P} \tilde {b} ^ {\mathrm{T}} \dot {\tilde {b}} + \dot {V} _ {t} \\ \leq \psi_ {T} ^ {\mathrm{T}} e _ {\varphi} - k _ {T D} \psi_ {T} ^ {\mathrm{T}} L _ {T} \psi_ {T} \\ - k _ {T D} \left(\psi_ {T} ^ {\mathrm{T}} \psi_ {T}\right) ^ {\frac {1 - p}{p}} \psi_ {T} ^ {\mathrm{T}} L _ {T} \psi_ {T} \\ - k _ {T P} \psi_ {T} ^ {\mathrm{T}} \tilde {b} + k _ {T P} \tilde {b} ^ {\mathrm{T}} \tilde {v} - \Gamma_ {t 1} V _ {t} - \Gamma_ {t 2} V _ {t} ^ {\frac {1}{p}} \\ \leq \frac {1}{2} \psi_ {T} ^ {\mathrm{T}} \psi_ {T} + \frac {1}{2} e _ {\varphi} ^ {\mathrm{T}} e _ {\varphi} \tag {88} \\ - k _ {T D} \lambda_ {\min} \left\{L _ {T} \right\} \psi_ {T} ^ {\mathrm{T}} \psi_ {T} - k _ {T D} \lambda_ {\min} \left\{L _ {T} \right\} \left(\psi_ {T} ^ {\mathrm{T}} \psi_ {T}\right) ^ {\frac {1}{p}} \\ - k _ {T P} \psi_ {T} ^ {\mathbf {T}} \tilde {b} + k _ {T P} \tilde {b} ^ {\mathbf {T}} \Big [ \psi_ {T} - \kappa_ {T} (\tilde {b} + (\tilde {b} ^ {\mathbf {T}} \tilde {b}) ^ {\frac {1 - p}{p}} \tilde {b}) \Big ] \\ - \Gamma_ {t 1} V _ {t} - \Gamma_ {t 2} V _ {t} ^ {\frac {1}{p}} \\ \end{array}
