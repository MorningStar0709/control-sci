From (89), we obtain

$$z (t) = e ^ {A (t - n h)} z (n h) + \int_ {n h} ^ {t} e ^ {A (t - \xi)} v (\xi) d \xi ,$$

for all $t \in [ n h , ( n + 1 ) h )$ and $n \in \ \big [ \tilde { t } _ { j } ^ { p } / h , \tilde { t } _ { j + 1 } ^ { p } / h \big ) \cap \mathbb { N }$ , using which we show

$$\Gamma^ {p *} (t) = C e ^ {A (t - n h)} z (n h) + \int_ {n h} ^ {t} C e ^ {A (t - \xi)} v (\xi) d \xi , \tag {90}$$

where

$$
C = \left[ \begin{array}{c c} 1 & 0 \end{array} \right].
$$

The matrix A has two distinct eigenvalues −η and $1 + \rho _ { 1 }$ . Therefore, using matrix diagonalization, we express the matrix exponential $e ^ { A t }$ as

$$
e ^ {A t} = \frac {\rho}{a} \left[ \begin{array}{c c} - \gamma & - \frac {a + \gamma \rho}{\rho} \\ 1 & 1 \end{array} \right] \left[ \begin{array}{c c} e ^ {- \eta t} & 0 \\ 0 & e ^ {(1 + \rho_ {1}) t} \end{array} \right] \left[ \begin{array}{c c} 1 & \frac {a + \gamma \rho}{\rho} \\ - 1 & - \gamma \end{array} \right].
$$

Moreover, it can be shown that

$$
\begin{array}{l} C e ^ {A (t - \xi)} v (\xi) \\ = - \left(\left(\gamma \beta_ {1} - \alpha_ {1}\right) g _ {1} (t - \xi) - \beta_ {1} g _ {2} (t - \xi)\right) \| u [ \xi ] \| ^ {2} \\ - \left(\left(\gamma \beta_ {2} - \alpha_ {2}\right) g _ {1} (t - \xi) - \beta_ {2} g _ {2} (t - \xi)\right) u ^ {2} (1, \xi) \tag {91} \\ - c \left(\gamma g _ {1} (t - \xi) - g _ {2} (t - \xi)\right) W ^ {p} (\xi) \\ - g _ {1} (t - \xi) \iota (\xi), \\ \end{array}
$$

where

$$g _ {1} (t) = \frac {1}{a} \left(- \gamma \rho + (a + \gamma \rho) e ^ {a t}\right) e ^ {- \eta t},g _ {2} (t) = \frac {\gamma (a + \gamma \rho)}{a} \left(- 1 + e ^ {a t}\right) e ^ {- \eta t}.$$

We can easily observe that $g _ { 1 } ( t ) > 0$ for all $t \geq 0$ . We also obtain that

$$\gamma g _ {1} (t) - g _ {2} (t) = \gamma e ^ {- \eta t} > 0,$$

for all $t \geq 0 .$ Furthermore, noting that $\gamma \beta _ { i } / \alpha _ { i } = 1 / ( 1 -$

σ ), i = 1, 2 from (40), and recalling (33), we show that

$$
\begin{array}{l} \left(\gamma \beta_ {i} - \alpha_ {i}\right) g _ {1} (t - \xi) - \beta_ {i} g _ {2} (t - \xi) \\ = \frac {\alpha_ {i} (a + \gamma \rho)}{a} \left(1 + \frac {\sigma a}{(1 - \sigma) (a + \gamma \rho)} - e ^ {a (t - \xi)}\right) e ^ {- \eta (t - \xi)} \\ = \frac {\alpha_ {i} (a + \gamma \rho)}{a} \left(e ^ {a \tau} - e ^ {a (t - \xi)}\right) e ^ {- \eta (t - \xi)}, \tag {92} \\ \end{array}
$$
