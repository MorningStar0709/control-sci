Proof. First of all, one proves the existence of function $\xi : [ 0 , 2 \pi ]  [ 0 , 2 \pi ]$ with the aforementioned equality constraint. Construct a function $\begin{array} { r } { G ( \xi ) = \int _ { \varphi } ^ { \xi } \omega ( \theta ) d \theta - \bar { m } , \varphi \in [ 0 , 2 \pi ] \quad \quad } \end{array}$ and one gets $G ( \varphi ) = - \bar { m } < 0$ and $G ( \varphi +$ $2 \pi ) = ( N - 1 ) \bar { m } > 0$ . It follows from the intermediate value theorem that there exists a value $\xi _ { \varphi } \in [ \varphi , \varphi + 2 \pi ] =$ [0, 2π] such that $G ( \xi _ { \varphi } ) = 0 $ , which is equivalent t o R ξϕϕ ω $\begin{array} { r } { \int _ { \varphi } ^ { \xi _ { \varphi } } \omega ( \theta ) d \theta = \bar { m } } \end{array}$ . This implies that there exists a function $\xi$ : $[ 0 , 2 \pi ]  [ 0 , 2 \pi ]$ satisfying the equality constraint. Next, one proves the continuity of function $\xi : [ 0 , 2 \pi ] \to [ 0 , 2 \pi ]$ . For any given $\varphi _ { 0 } \in [ 0 . 2 \pi ] .$ one obtains

$$\int_ {\varphi_ {0}} ^ {\xi (\varphi_ {0})} \omega (\theta) d \theta = \int_ {\varphi_ {0}} ^ {\varphi} \omega (\theta) d \theta + \int_ {\varphi} ^ {\xi (\varphi_ {0})} \omega (\theta) d \theta = \int_ {\varphi} ^ {\xi (\varphi)} \omega (\theta) d \theta = \bar {m},$$

which leads to

$$\int_ {\varphi_ {0}} ^ {\varphi} \omega (\theta) d \theta = \int_ {\varphi} ^ {\xi (\varphi)} \omega (\theta) d \theta - \int_ {\varphi} ^ {\xi (\varphi_ {0})} \omega (\theta) d \theta = \int_ {\xi (\varphi_ {0})} ^ {\xi (\varphi)} \omega (\theta) d \theta$$

Thus, one can get

$$\underline {{\omega}} | \xi (\varphi) - \xi (\varphi_ {0}) | \leq \left| \int_ {\xi (\varphi_ {0})} ^ {\xi (\varphi)} \omega (\theta) d \theta \right| = \left| \int_ {\varphi_ {0}} ^ {\varphi} \omega (\theta) d \theta \right| \leq \bar {\omega} | \varphi - \varphi_ {0} |,$$

which implies

$$| \xi (\varphi) - \xi (\varphi_ {0}) | \leq \frac {\bar {\omega}}{\omega} | \varphi - \varphi_ {0} |. \tag {8}$$
