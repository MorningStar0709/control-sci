Last but not least, we show that system (3) does not exhibit Zeno behavior for any $b > 0$ . It has been shown in [1] that the inter-event times $\{ t _ { i } - t _ { i - 1 } \} _ { i \in \mathbb { N } }$ are lower bounded by a positive quantity when $b < \mu - \sigma$ , that is, system (3) does not exhibit Zeno behavior. Hence, we will focus on the scenario of $b \geq \mu - \sigma$ .

It follows from the proof of Theorem 2 in [1] that

$$a e ^ {- b \left(t _ {i + 1} - t _ {0}\right)} \leq \lambda_ {1} \left(e ^ {- \eta \left(t _ {i} - t _ {0}\right)} - e ^ {- \eta \left(t _ {i + 1} - t _ {0}\right)}\right) + \lambda_ {2} \left(t _ {i + 1} - t _ {i}\right) e ^ {- \eta \left(t _ {i} - t _ {0}\right)} \tag {20}$$

where $\lambda _ { 1 } = L _ { 1 } L _ { 2 } M e ^ { \eta \tau } / \eta > 0 , \lambda _ { 2 } = L _ { 1 } L _ { 3 } M > 0 ,$ , and $L _ { 1 }$ is the Lipschitz constant of $\alpha _ { 1 } ^ { - 1 }$ on the interval [0 M]. Let $T _ { i + 1 } = t _ { i + 1 } - t _ { i }$ , then multiplying both sides of (20) by $e ^ { \eta ( t _ { i } - t _ { 0 } ) }$ yields

$$a e ^ {- b T _ {i + 1}} e ^ {(\eta - b) (t _ {i} - t _ {0})} \leq \lambda_ {1} \left(1 - e ^ {- \eta T _ {i + 1}}\right) + \lambda_ {2} T _ {i + 1}. \tag {21}$$

Next we use contradiction argument to show that system (3) is free of Zeno behavior. Suppose that there exists a $\bar { t } < \infty$ such that $\begin{array} { r } { \operatorname* { l i m } _ { i \to \infty } t _ { i } = \bar { t } , } \end{array}$ that is, $t _ { i } < \bar { t }$ for all $i \in \mathbb { N } .$ . The fact $b \geq \mu - \sigma$ and the definition of  imply $\eta < b$ . It then follows from (21) that

$$a e ^ {- b T _ {i + 1}} e ^ {(\eta - b) (\bar {t} - t _ {0})} \leq \lambda_ {1} \left(1 - e ^ {- \eta T _ {i + 1}}\right) + \lambda_ {2} T _ {i + 1}. \tag {22}$$

Define a function

$$g (T) = a e ^ {- b T} e ^ {(\eta - b) (\bar {t} - t _ {0})} - \lambda_ {1} \left(1 - e ^ {- \eta T}\right) - \lambda_ {2} T$$

for $T \geq 0$ . It can be observed that $g ( 0 ) = a e ^ { ( \eta - b ) ( \bar { t } - t _ { 0 } ) } > 0$ , li $\mathfrak { n } _ { T \to \infty } g ( T ) = - \infty$ , and

$$g ^ {\prime} (T) = - b a e ^ {- b T} e ^ {(\eta - b) (\bar {t} - t _ {0})} - \eta \lambda_ {1} e ^ {- \eta T} - \lambda_ {2} < 0.$$
