This is a right coprime factorization of $T _ { 4 }$ , as attested by the Bézout equality (cf. (A.1a))

$$
\begin{array}{l} \left[ \begin{array}{c c} \tilde {M} _ {P} & \tilde {N} _ {P} \\ - Y _ {P} & X _ {P} \end{array} \right] \left[ \begin{array}{c c} M _ {K} & - N _ {P} \\ - N _ {K} & M _ {P} \end{array} \right] \\ + \left[ \begin{array}{c c} X _ {K} - \tilde {M} _ {P} & Y _ {K} + \tilde {N} _ {P} \\ Y _ {P} & X _ {P} \end{array} \right] \left[ \begin{array}{c c} M _ {K} & 0 \\ N _ {K} & 0 \end{array} \right] = I, \\ \end{array}
$$

where $\tilde { M } _ { P } : = \mathrm { d i a g } \{ \tilde { M } _ { i } \}$ and $\tilde { N } _ { P } : = \mathrm { d i a g } \{ \tilde { N } _ { i } \}$ . By Lemma A.1, $T _ { 4 }$ is stable if and only if

$$
\left[ \begin{array}{c c} M _ {K} & - N _ {P} \\ - N _ {K} & M _ {P} \end{array} \right] ^ {- 1} \in H _ {\infty}, \tag {10}
$$

or

$$
\inf _ {s \in \bar {\mathbb {C}} _ {0}} \underline {{{\sigma}}} \left(\left[ \begin{array}{c c} M _ {K} (s) & - N _ {P} (s) \\ - N _ {K} (s) & M _ {P} (s) \end{array} \right]\right) > 0 \tag {11}
$$

by Lemma 3.4. But (7a) implies that there is $v \neq 0$ such that $v ^ { \top } M _ { i } ( \lambda ) = 0$ for all 𝑖 or, equivalently, $( \mathbb { 1 } \otimes v ) ^ { \top } M _ { P } ( \lambda ) = 0$ . Taking into account that (𝟙 ⊗ $v ) ^ { \top } N _ { K } = v ^ { \top } ( \mathbb { 1 } \otimes I _ { m } ) ^ { \top } N _ { K } = 0$ , we end up with

$$
\left[ \begin{array}{c c} 0 & (\mathbb {1} \otimes v) ^ {\top} \end{array} \right] \left[ \begin{array}{c c} M _ {K} (\lambda) & - N _ {P} (\lambda) \\ - N _ {K} (\lambda) & M _ {P} (\lambda) \end{array} \right] = 0, \tag {12}
$$

which violates (11). We thus have that if (7a) holds, then there is no $K _ { \mathrm { e } }$ that internally stabilizes the system in Fig. 2.
