# 5.3.1 Problem Formulation

Like before, consider $N \in \mathbb { N }$ fully actuated agents, with index set ${ \mathcal { N } } .$ , composed by a robotic arm mounted on an omnidirectional mobile base, operating in a static workspace that is bounded by a large sphere in 3D space, i.e. $\mathcal { W } = \mathcal { B } ( p _ { 0 } , r _ { 0 } ) = \{ p \in \mathbb { R } ^ { 3 } \mathrm { ~ s . t . ~ } \| p - p _ { 0 } \| < r _ { 0 } \}$ , where $p _ { 0 } \in \mathbb { R } ^ { 3 }$ is the center of , and $r _ { 0 } \in \mathbb { R } _ { > 0 }$ is its radius. Without loss of generality, we consider that $p _ { 0 } = 0$ , corresponding to an inertial frame of reference. As in the previous section, we consider that within  there exist K disjoint spheres around points of interest, which are described by $\pi _ { k } = \bar { B } ( p _ { \pi _ { k } } , r _ { \pi _ { k } } ) , k \in \mathcal { K } _ { \mathcal { R } }$ , where $p _ { k } \in \mathbb { R } ^ { 3 }$ and $r _ { \pi _ { k } } \in \mathbb { R } _ { > 0 }$ are the center and radius of the kth region, respectively. The regions of interest can be equivalently described by $\pi _ { k } =$ $\{ z \in \mathbb { R } ^ { 4 } \mathrm { ~ s . t . ~ } z ^ { \top } T _ { \pi _ { k } } z \leq 0 \}$ , where $z = [ p ^ { \top } , 1 ] ^ { \top }$ is the vector of homogeneous coordinates of $p \in \mathbb { R } ^ { 3 }$ , and

$$
T _ {\pi_ {k}} = \left[ \begin{array}{c c} I _ {3} & p _ {\pi_ {k}} \\ 0 _ {1 \times 3} & - r _ {\pi_ {k}} ^ {2} \end{array} \right], \forall k \in \mathcal {K} _ {\mathcal {R}}.
$$

The dynamic model of each agent is given by the second-order Lagrangian dynamics (see (2.1) of Chapter 2):

$$B _ {i} (q _ {i}) \ddot {q} _ {i} + C _ {q _ {i}} (q _ {i}, \dot {q} _ {i}) \dot {q} _ {i} + g _ {q _ {i}} (q _ {i}) + f _ {i} (q _ {i}, \dot {q} _ {i}) = \tau_ {i}, \tag {5.5}$$

$\forall i \in \mathcal N$ , where $q _ { i } \in \mathbb { R } ^ { n _ { i } }$ is the vector of generalized coordinates $( \mathrm { e . g . , }$ pose of mobile base and joint coordinates of the arms), with $q : = [ q _ { 1 } ^ { \top } , \ldots , q _ { N } ^ { \top } ] ^ { \top }$ , and the rest of the terms as in (2.1) with a slight change of notation; $f _ { i } ( \cdot )$ here represents friction-like terms. Without loss of generality, we assume that $n _ { i } = n \in \mathbb { N } , \forall i \in \mathcal { N }$ . In addition, we denote by $p _ { B _ { i } } : = p _ { B _ { i } } ( q _ { i } ) : \mathbb { R } ^ { n }  \mathbb { R } ^ { 3 }$ the inertial position of the mobile base of agent i. Moreover, the matrix $\dot { B } _ { i } - 2 C _ { q { i } }$ is skew-symmetric [311], and we further make the following assumption, similar to Assumption 4.1:
