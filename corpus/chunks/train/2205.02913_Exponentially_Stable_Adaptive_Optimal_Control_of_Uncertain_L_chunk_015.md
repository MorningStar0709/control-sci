where $\xi \left( t \right) = \left[ e _ { r e f } ^ { \mathrm { T } } \left( t \right) v e c ^ { \mathrm { T } } \left( \tilde { \theta } \left( t \right) \right) \right] ^ { \mathrm { T } } \in \mathbb { R } ^ { n + m \left( n + m \right) }$ , and $\varepsilon _ { \xi }$ value can be reduced to zero by appropriate choice of the arbitrary parameters values of the proposed method.

Remark 1. Contrary to the conventional model reference adaptive control schemes 3,5, the reference model (10) can not be implemented, and the state vector $x _ { r e f } \left( t \right)$ is unmeasurable.

Remark 2. The control law (11) with the adaptive law to adjust the parameters $\hat { \theta } \left( t \right)$ is suboptimal in terms of criterion (6) as the adjustable law (11) coincides with the optimal one (7) only if $\varepsilon _ { \xi } = 0 , t  \infty$ . The difference between the minimum of (6) when the ideal control law (7) is applied and the value of (6) when the adjustable law (11) is used is written as:

$$
\begin{array}{l} \tilde {J} = \hat {J} - J = \frac {1}{2} \int_ {t _ {r} ^ {+}} ^ {\infty} x ^ {\mathrm{T}} (t) Q x (t) + u ^ {\mathrm{T}} (t) R u (t) d t - \frac {1}{2} \int_ {t _ {r} ^ {+}} ^ {\infty} x _ {r e f} ^ {\mathrm{T}} (t) Q x _ {r e f} (t) + (u ^ {*} (t)) ^ {\mathrm{T}} R u ^ {*} (t) d t = \\ = \frac {1}{2} \int_ {t _ {r} ^ {+}} ^ {\infty} e _ {r e f} ^ {\mathrm{T}} (t) Q e _ {r e f} (t) + e _ {r e f} ^ {\mathrm{T}} (t) Q x _ {r e f} (t) + x _ {r e f} ^ {\mathrm{T}} (t) Q e _ {r e f} (t) d t + \tag {14} \\ + \frac {1}{2} \int_ {t _ {r} ^ {+}} ^ {\infty} \omega^ {\mathrm{T}} (t) \tilde {\theta} (t) R \tilde {\theta} ^ {\mathrm{T}} (t) \omega (t) + \omega^ {\mathrm{T}} (t) \tilde {\theta} (t) R \theta^ {\mathrm{T}} \omega (t) + \omega^ {\mathrm{T}} (t) \theta R \tilde {\theta} ^ {\mathrm{T}} (t) \omega (t) d t \\ \end{array}
$$

and, in the process of the stated problem solution, it can be reduced if the rate of convergence of errors $e _ { r e f }$ and $\tilde { \theta } \left( t \right)$ to zero is improved. Details on how to obtain (14) are given in the supplementary material39.
