I.1 Given time constraints on $\sigma _ { : }$ , is there a collection of reset maps R so that the reset switched system $\Sigma _ { \sigma } ^ { \mathcal { R } }$ is stable.   
I.2 Given time constraints on $\sigma _ { \mathrm { { : } } }$ , is there a collection of impulses I so that the impulsive switched system $\Sigma _ { \sigma , \omega } ^ { \mathcal { Z } }$ is stable for any $\omega$ .   
I.3 Given a set of resets R, finding time constraints on $\sigma$ so that the reset switched system $\Sigma _ { \sigma } ^ { \mathcal { R } }$ is stable.   
I.4 Given a set of impulses I, finding time constraints on $\sigma$ so that the impulsive switched system $\bar { \Sigma _ { \sigma , \omega } ^ { \mathcal { L } } }$ is stable for any $\omega .$ .

In this paper, we will make use of two techniques to study the issues raised above dealing with stability for the switched systems. The first one is by analyzing the Euclidean norm of the flow of switched system, discussed in Section 3. The other one is by constructing multiple Lyapunov functions, discussed in Section 4. We then present, in Section 5, results for the case when the impulses experienced by the system lie in the convex hull of finitely many matrices. In Section 6, we obtain mode dependent constraints for stability of impulsive switched systems, followed by numerical examples in Section 7.

Throughout the paper, the collection ${ \mathfrak { s } } \subseteq { \mathcal { P } }$ will consist of the indices of all stable subsystem matrices and ${ \mathfrak { u } } \subseteq { \mathcal { P } }$ will consist of the indices of all unstable subsystem matrices. The set $M _ { n } ( \mathbb { R } )$ denotes the set of $n \times n$ real matrices, and O denotes the zero matrix of size $n$ . For a matrix Q having all real eigenvalues, $\lambda _ { \operatorname* { m a x } } ( Q )$ and $\lambda _ { \operatorname* { m i n } } ( Q )$ denote the largest and the smallest eigenvalue of Q. The symbol $\lVert v \rVert$ denotes the Euclidean norm if $v \in \mathbb { R } ^ { n }$ , and the spectral norm if $v \in M _ { n } ( \mathbb { R } )$ . If J is a Jordan form of a matrix $A .$ , then any matrix $P$ satisfying $A = P J P ^ { - 1 }$ (Jordan decomposition of $A )$ will be referred to as a Jordan basis matrix of A. We consider complex Jordan decomposition of matrices.
