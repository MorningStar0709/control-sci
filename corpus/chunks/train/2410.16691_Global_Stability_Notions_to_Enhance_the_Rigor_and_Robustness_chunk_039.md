Moreover, if the inequality $\rho { \big ( } s { \big ) } \geq c s$ holds for all $s \geq 0$ then $\gamma \left( s \right) = c ^ { - 1 } s$ for all $s \geq 0$ and $\sigma ( s , t ) = \exp ( - c t ) s$ for all $t , s \geq 0$

It follows from (90), (86), estimate (92) and definition (87) that the following inequality holds for $t \in \left[ 0 , t _ { m a x } \right)$ a.e.:

$$\frac {d}{d t} V (\xi (t), z (t)) \leq - C (s) V (\xi (t), z (t)) + a \frac {\left\| d \right\| _ {\infty} ^ {2} + \left(\left(\left\| \theta \right\| _ {\infty} - b - \lambda \left(\exp \left(z _ {0}\right)\right)\right) ^ {+}\right) ^ {2}}{1 + \kappa (\exp (z (t)))} \tag {93}$$

where

$$s := V \left(\xi_ {0}, z _ {0}\right) + \left| z _ {0} \right| + \left\| \theta \right\| _ {\infty} ^ {2} + \left\| d \right\| _ {\infty} ^ {2} \tag {94}$$

Integrating (93) and using the fact that  is non-decreasing on $[ 0 , t _ { m a x } )$ , we obtain the following estimate for all $t \in \left[ 0 , t _ { m a x } \right)$ :

$$V (\xi (t), z (t)) \leq \exp (- C (s) t) V \left(\xi_ {0}, z _ {0}\right) + \frac {1}{C (s)} \chi \left(\left\| d \right\| _ {\infty}, \left\| \theta \right\| _ {\infty}, \exp \left(z _ {0}\right)\right) \tag {95}$$

Moreover, from estimate (95) and definition (94), we get the following estimate for all $t \in \left[ 0 , { t _ { m a x } } \right)$ :

$$V (\xi (t), z (t)) \leq V \left(\xi_ {0}, z _ {0}\right) + \frac {a}{C (s)} \left(\left\| \theta \right\| _ {\infty} ^ {2} + \left\| d \right\| _ {\infty} ^ {2}\right) \leq s + \frac {a}{C (s)} s \tag {96}$$

Next, we show that $z ( t )$ is bounded. Since $z ( t )$ is non-decreasing on $[ 0 , t _ { m a x } )$ , we have $z ( t ) \geq z _ { 0 }$ for all $t \in \left[ 0 , { t _ { m a x } } \right)$ . We next distinguish the following cases:

Case 1: for all $t \in \left[ 0 , { t _ { m a x } } \right)$

In this case, it follows from (89) that:

$$z (t) \leq G (s), \text { for all } t \in \left[ 0, t _ {\max}\right)$$

Case 2: There exists $T \in \left( 0 , t _ { m a x } \right)$ such that $\exp ( z ( T ) ) > 1 + \operatorname* { m a x } \left( A ( s ) , \exp ( s ) \right)$ . Since $\exp ( z ( 0 ) ) \leq \exp ( s ) < 1 + \operatorname* { m a x } { \big ( } A ( s ) , \exp ( s ) { \big ) }$ , continuity of $z ( t )$ guarantees that there exist $t _ { 0 } \in ( 0 , T )$ such that:

$$\exp \left(z \left(t _ {0}\right)\right) = 1 + \max (A (s), \exp (s)) \tag {97}$$

Since  is non-decreasing, it follows that:

$$\exp (z (t)) \geq 1 + \max (A (s), \exp (s)), \text { for } t \in [ t _ {0}, t _ {\max}) \tag {98}$$
