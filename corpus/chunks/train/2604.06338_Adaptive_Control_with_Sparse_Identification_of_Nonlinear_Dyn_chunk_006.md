A history stack satisfying (7) is called full rank. The constant y quantifies the information richness of the stored data and determines the convergence rate of the parameters, as seen in the subsequent analysis. To guarantee sufficient excitation and improve estimator conditioning and alertness, the history stack H is updated using the strategy in Algorithm 1.

Algorithm 1 ICL Data Selection   
Require: Number of data points N, target $\underline{y} > 0$ , improvement threshold $\delta \geq 1$ 1: Store the first N data: $\{U_{f_i}, Y_{f_i}\}_{i=1}^N$ 2: for each new datum $(U_f(t), Y_f(t))$ do

3: $Y \leftarrow \sum_i Y_{f_i}^\top Y_{f_i}$ $\triangleright$ Fill up memory-regressor

4: if $\lambda_{\min}\{\mathcal{Y}\} \geq \underline{y}$ then $\triangleright$ Favor new data if eigenvalue target is already met

5: for j = 1 to N do

6: $y_j \leftarrow \sum_{i \neq j} Y_{f_i}^\top Y_{f_i} + Y_f^\top(t) Y_f(t)$ 7: if $\lambda_{\min}\{y_j\} \geq \underline{y}$ then $\triangleright$ Search from oldest to newest

8: for k = j to N - 1 do

9: $Y_{f_k} \leftarrow Y_{f(k+1)}, U_{f_k} \leftarrow U_{f(k+1)}$ 10: end for $\triangleright$ Shift data to make room for new datum

11: $Y_{f_N} \leftarrow Y_f(t), U_{f_N} \leftarrow U_f(t)$ 12: break

13: end if

14: end for

15: else $\triangleright$ If the target is not met, then maximize the minimum eigenvalue

16: for j = 1 to N do

17: $y_j \leftarrow \sum_{i \neq j} Y_{f_i}^\top Y_{f_i} + Y_f^\top(t) Y_f(t)$ 18: if $\lambda_{\min}\{y_j\} > \delta \cdot \lambda_{\min}\{\underline{y}\}$ then

19: for k = j to N - 1 do

20: $Y_{f_k} \leftarrow Y_{f(k+1)}, U_{f_k} \leftarrow U_{f(k+1)}$ 21: end for $\triangleright$ Shift data to make room for new datum

22: $Y_{f_N} \leftarrow Y_f(t), U_{f_N} \leftarrow U_f(t)$ 23: break

24: end if

25: end for

26: end if

27: end for

Under Algorithm 1, a candidate data point $( U _ { \mathrm { n e w } } , Y _ { \mathrm { n e w } } )$ replaces the oldest possible entry $( U _ { f _ { j } } , Y _ { f _ { j } } )$ in the history stack H if

$$\lambda_ {\min} (\mathcal {Y} ^ {*}) > \underline {{y}}, \tag {8}$$

where Y∗ = PNi̸=j Yf ⊤i Yf i1+κ∥Yf ∥2 + $\begin{array} { r } { \mathcal { V } ^ { * } = \sum _ { i \neq j } ^ { N } \frac { { Y _ { f } } _ { i } ^ { \top } Y _ { f _ { i } } } { 1 + \kappa \| Y _ { f _ { i } } \| ^ { 2 } } + \frac { Y _ { \mathrm { n e w } } ^ { \top } Y _ { \mathrm { n e w } } } { 1 + \kappa \| Y _ { \mathrm { n e w } } \| ^ { 2 } } } \end{array}$ Y newYnew1+κ∥Ynew∥2 . By construction, (8) guarantees $\lambda _ { \operatorname* { m i n } } ( \mathcal { V } ) > \underline { { y } }$ for all $t \ > \ t _ { s }$ , ensuring the history stack remains full rank and new data replaces old data whenever possible without decreasing the minimum eigenvalue of the memory regressor.
