for $t \in [ 0 , \delta )$ , with $L _ { \varphi }$ the Lipschitz constant of $\varphi$ on a to be defined domain. Assuming that $\begin{array} { r } { \hat { J } ( x _ { k } , w _ { k } ) \le \bar { J } , } \end{array}$ , specifying $R ^ { * } > R$ gives the respective $\delta > 0$ such that

$$\bar {J} + \overline {{{w}}} L _ {\varphi} \delta \bar {\mathcal {F}} \leq q _ {1} (R ^ {*}) \tag {40}$$

with ${ \cal L } _ { \varphi } = { \cal L } _ { \varphi } ( 0 , R ^ { * } )$ , implying that

$$
\begin{array}{l} \mathcal {V} _ {c} (x (t; x _ {k}), \tilde {\theta} _ {k}) \leq \hat {J} (x _ {k}, w _ {k}) + \mathcal {T} (\hat {\theta} _ {k}) + \overline {{w}} L _ {\varphi} \delta \bar {\mathcal {F}} \\ \leq \bar {J} + \mathcal {T} (\hat {\theta} _ {k}) + \overline {{w}} L _ {\varphi} \delta \bar {\mathcal {F}} \\ \leq q _ {1} (R ^ {*}) + q _ {4} (\| \tilde {\theta} _ {k} \|) \\ \end{array}
\stackrel {- q _ {4} (\| \tilde {\theta} _ {k} \|)} {\Rightarrow} \hat {J} (x (t; x _ {k}), w _ {k}) \leq q _ {1} (R ^ {*}) \tag {41}$$

at any $k \in  { \mathbb { N } } _ { 0 }$ and for any $t \in [ 0 , \delta )$ which in turn gives $x ( t ; x _ { k } ) \in B _ { R ^ { * } }$ due to (26). Since $x _ { 0 } ~ \in ~ \boldsymbol { B } _ { R }$ and $w _ { 0 } \in \mathbb { W }$ by constraint, $\hat { J } ( x _ { k } , w _ { k } ) \leq \bar { J }$ holds at $k = 0$ and if $\Delta _ { k } \mathcal { V } _ { c } =$ $\mathcal { V } _ { c } ( x _ { k + 1 } , \tilde { \theta } _ { k + 1 } ) - \mathcal { V } _ { c } ( x _ { k } , \tilde { \theta } _ { k } ) < 0 .$ , the state is bounded to $\boldsymbol { B } _ { R ^ { * } }$ for all $k \in  { \mathbb { N } } _ { 0 }$ and inter sampling times $t \in [ 0 , \delta )$ . Denote $\mathbb { U } =$ $\mathbb { U } _ { B _ { R } }$ ∗ the control set associated to $\boldsymbol { B } _ { R ^ { * } }$ as per Definition 1.

Next, the decay of $\mathcal { V } _ { c }$ is discussed. Consider the following variant of (11)

$$x (t; u) = x _ {k} + \delta \underbrace {\left(\frac {1}{\delta} \int_ {0} ^ {t} \mathcal {F} (x (k \delta + s) , u _ {k} , \theta) \mathrm{d} s\right)} _ {=: \Psi_ {t}}$$

with $t \in [ 0 , \delta )$ . Assume for now that the state remains in a ball $\boldsymbol { B } _ { R ^ { * } }$ with $R ^ { * } > R$ for all times, $\mathbf { i } . \mathbf { e } . , x ( t ; u _ { k } ) \in B _ { R ^ { * } }$ , for all $t \in [ 0 , \delta )$ and $k \in  { \mathbb { N } } _ { 0 }$ , which will be shown to hold using $\mathcal { V } _ { c }$ and a suitable choice of $\delta > 0$ . Let

$$
\bar{\mathcal{F}} := \sup_{\substack{x\in \mathcal{B}_{R^{*}}, u\in \mathbb{U}\\ \theta \in \Theta}}\mathcal{F}(x,u,\theta)
$$
