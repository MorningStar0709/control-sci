with the parameters to be optimized such as weights $W _ { \hat { \Phi } . 1 } \in$ $\mathbb { R } ^ { n _ { l } \times n } , W _ { \hat { \Phi } . 2 } \in \mathbb { R } ^ { n \times n _ { l } }$ and biases $b _ { \hat { \Phi } , 1 } \in \mathbb { R } ^ { n _ { l } } , b _ { \hat { \Phi } , 2 } \in \mathbb { R } ^ { n }$ The complete loss function for training the auto-encoder structure can be synthesized as

$$L = \alpha_ {1} L _ {\text { rec }, x} + \alpha_ {2} L _ {\text { rec }, u} + \alpha_ {3} (L _ {\text { pred }, 1} + L _ {\text { pred }, 2})$$

with the state reconstruction loss (7), the input reconstruction loss (8), the prediction losses (9) and (10), and the hyperparameters $\alpha _ { 1 } , \alpha _ { 2 } , \alpha _ { 3 } \in \mathbb { R }$ .

$$L _ {\text { rec }, x} = \left\| x - \Phi_ {x} ^ {- 1} \circ \Phi_ {x} (x) \right\| _ {\text { MSE }} \tag {7}L _ {\text { rec }, u} = \left\| u - \Phi_ {u} ^ {- 1} \circ (x, \Phi_ {u} (x, u)) \right\| _ {\text { MSE }} \tag {8}L _ {\text { pred }, 1} = \left\| x ^ {+} - \Phi_ {x} ^ {- 1} \circ \sigma (\Phi (x), \Phi_ {u} (x, u)) \right\| _ {\mathrm{MSE}} \tag {9}L _ {\text { pred }, 2} = \left\| \Phi_ {x} (x ^ {+}) - \sigma (\Phi (x), \Phi_ {u} (x, u)) \right\| _ {\text { MSE }} \tag {10}$$
