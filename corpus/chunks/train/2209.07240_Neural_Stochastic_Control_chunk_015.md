# 4 Convergence Time and Energy Cost

The convergence time and the energy cost are the crucial factors to measure the quality of a controller (Yan et al., 2012; Li et al., 2017; Sun et al., 2017). In this section, we provide a comparative study between the traditional stochastic linear control and the ES/AS, the above-articulated neural stochastic control.

To this end, we first present a theorem on the estimations of the convergence time and the energy cost for the stochastic linear control on a general SDE.

Theorem 4.1 Consider the SDE with a stochastic linear controller as:

$$\mathrm{d} \boldsymbol {x} = f (\boldsymbol {x}) \mathrm{d} t + u (\boldsymbol {x}) \mathrm{d} B _ {t}, \quad \boldsymbol {x} (0) = \boldsymbol {x} _ {0} \in \mathbb {R} ^ {d}, \tag {14}$$

where $\langle \pmb { x } , f ( \pmb { x } ) \rangle \leq L \| \pmb { x } \| ^ { 2 }$ and $u ( { \pmb x } ) = k { \pmb x }$ with $| k | > { \sqrt { 2 L } }$ . Then, for $\epsilon < \| \pmb { x } _ { 0 } \|$ , we have

$$
\left\{ \begin{array}{l} \mathbb {E} [ \tau_ {\epsilon} ] \leq T _ {\epsilon} = \frac {2 \log (\| \boldsymbol {x} _ {0} \| / \epsilon)}{k ^ {2} - 2 L}, \\ \mathcal {E} (\tau_ {\epsilon}, T _ {\epsilon}) \leq \frac {k ^ {2} \| \boldsymbol {x} _ {0} \| ^ {2}}{k ^ {2} + 2 L} \left[ \exp \left(\frac {2 (k ^ {2} + 2 L) \log (\| \boldsymbol {x} _ {0} \| / \epsilon)}{k ^ {2} - 2 L}\right) - 1 \right], \end{array} \right.
$$

where, for a sufficiently small $\epsilon > 0 ,$ , we denote the stopping time by $\tau _ { \epsilon } \triangleq$ inf $\{ t > 0 : \| \pmb { x } ( t ) \| = \epsilon \}$ and denote the energy cost by

$$\mathcal {E} (\tau_ {\epsilon}, T _ {\epsilon}) \triangleq \mathbb {E} \left[ \int_ {0} ^ {\tau_ {\epsilon} \wedge T _ {\epsilon}} \| \boldsymbol {u} \| ^ {2} \mathrm{d} t \right] = \mathbb {E} \left[ \int_ {0} ^ {T _ {\epsilon}} \| \boldsymbol {u} \| ^ {2} \mathbb {1} _ {\{t < \tau_ {\epsilon} \}} \mathrm{d} t \right].$$

The proof of this theorem is provided in Appendix ??.

We further consider the case for NN controller ${ \pmb u } ( { \pmb x } )$ . In general, the ${ \pmb u } ( { \pmb x } )$ is Lipschitz continuous with Lipshcitz constant $k _ { u }$ under a suitable activation function such as ReLU in NN (Fazlyab et al., 2019; Pauli et al., 2021). Then we have the following upper bound estimation of the convergence time and the energy cost for ES and AS, whose proofs are provided in Appendix ??, ??.
