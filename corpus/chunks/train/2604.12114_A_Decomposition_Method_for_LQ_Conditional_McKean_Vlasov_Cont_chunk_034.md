and $\left( \ell , \psi ^ { 0 } \right) \in \mathcal { C } _ { \mathcal { F } ^ { 0 } } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right) \times L _ { \mathcal { F } ^ { 0 } } ^ { 2 } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right)$ is the unique solution of the BSDE

$$
\begin{array}{l} d \ell_ {t} = - \left[ \left(A _ {t} + F _ {t} - B _ {t} R _ {t} ^ {- 1} (L _ {t} B _ {t} + \bar {S} _ {t}) ^ {\intercal}\right) ^ {\intercal} \ell_ {t} - \left(L _ {t} B _ {t} + \bar {S} _ {t}\right) R _ {t} ^ {- 1} \varpi_ {t} + \bar {\zeta} _ {t} + L _ {t} b _ {t} + \bar {\Psi} _ {t} ^ {0} D _ {t} ^ {0} \right] d t \\ + \psi_ {t} ^ {0} d W _ {t} ^ {0}, \quad \ell_ {T} = 0. \tag {79} \\ \end{array}
$$

Proof. Adopting the ansatzes $p _ { t } ^ { * } = L _ { t } \bar { x } _ { t } ^ { * } + \ell _ { t }$ and $\lambda _ { t } ^ { * } = \Pi _ { t } \breve { x } _ { t } ^ { * }$ , and applying Itô’s lemma to these expressions, we obtain the BSDEs that they satisfy. We then substitute these expression in (65) and (66) and match them with the corresponding SDEs obtained above, which yields (77)-(79) and

$$q _ {t} ^ {*} = \bar {\Psi} _ {t} ^ {0} \bar {x} _ {t} ^ {*} + L _ {t} D _ {t} ^ {0} + \psi_ {t} ^ {0},\beta_ {t} ^ {*} = \Pi_ {t} D _ {t},\beta_ {t} ^ {0, *} = \Psi_ {t} ^ {0} \breve {x} _ {t} ^ {*},$$

where we can easily verify that $\mathbb { E } [ \beta _ { t } ^ { 0 , * } | \mathcal { F } _ { t } ^ { 0 } ] = 0$ for every $t \in [ 0 , T ]$ , and hence $( \lambda ^ { \ast } , \beta ^ { \ast } , \beta ^ { 0 , \ast } ) \in \mathcal { M } _ { \mathcal { F } } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right)$ . From [32], under Assumption 2, the stochastic Riccati equations (77) and (78) admit respectively a unique solution $\bar { \left( \Pi , \Psi ^ { 0 } \right) } , \left( L , \bar { \Psi } ^ { 0 } \right) \in L _ { \mathcal { F } ^ { 0 } } ^ { \infty } \left( [ 0 , T ] , \mathbb { S } ^ { n } \right) \times L _ { \mathcal { F } ^ { 0 } } ^ { 2 } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right)$ with $\Pi _ { t } , L _ { t } \geq 0 ~ d t \otimes d \mathbb { P } - a . s$ . Subsequently, the BSDE (79) also admits a unique solution $\left( \ell , \psi ^ { 0 } \right) \in \mathcal { C } _ { \mathcal { F } ^ { 0 } } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right) \times L _ { \mathcal { F } ^ { 0 } } ^ { 2 } \left( [ 0 , T ] , \mathbb { R } ^ { n } \right)$ . Finally, substituting these ansatzes in (64) yields the feedback representation given by (74). □
