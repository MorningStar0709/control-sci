# B. Cost Matrices Inference

In this part, we show how the cost matrices $\{ Q , R \}$ can be obtained by leveraging the ARE (76) and the condition (77).

Note that the ARE (76) does not involve $R ,$ and a straightforward method is to replace P with an expression of R. Define auxiliary matrices S ∈ RN2n2×N2n2 a $R .$ $\mathbf { \Psi } _ { S } ^ { \mathsf { ^ { \prime } } } \in \mathbb { R } ^ { N ^ { 2 } n ^ { 2 } \times N ^ { 2 } n ^ { 2 } }$ nd $H \in$ R $\phantom { } _ { \times } N ^ { 2 } n ^ { 2 } \times 2 N ^ { 2 } n ^ { 2 }$ by

$$S = I _ {N n} \otimes \tilde {A} ^ {\intercal} + \tilde {A} ^ {\intercal} \otimes I _ {N n} - \tilde {K} ^ {\intercal} \tilde {B} ^ {\intercal} \otimes I _ {N n}, \tag {82}H = [ S, I _ {N ^ {2} n ^ {2}} ]. \tag {83}$$

Then, we characterize the solution space of $\{ Q , P \}$ .

Theorem 5. Let $\theta = [ \mathrm { v e c } ^ { \mathsf { T } } ( P ) , \mathrm { v e c } ^ { \mathsf { T } } ( Q ) ] ^ { \mathsf { T } } \in \mathbb { R } ^ { 2 N ^ { 2 } n ^ { 2 } }$ . Then, we have

$$\theta \in \ker (H), \tag {84}$$

where ker(H) is the kernel space of H and always exists.

Proof. The key of this proof is to utilize the dependence of R on P . By substituting $\dot { \tilde { K } } = R ^ { - 1 } \tilde { B } ^ { \intercal } P$ into the ARE (76), its equivalent vectorized form is written as

$$
\begin{array}{l} \left(I _ {N n} \otimes A ^ {\intercal}\right) \operatorname{vec} (P) + \left(A ^ {\intercal} \otimes I _ {N n}\right) \operatorname{vec} (P) \\ - \left(\tilde {K} ^ {\intercal} \tilde {B} ^ {\intercal} \otimes I _ {N n}\right) \operatorname{vec} (P) + \operatorname{vec} (Q) = \mathbf {0} \\ \Rightarrow S \operatorname{vec} (P) + \operatorname{vec} (Q) = \mathbf {0}. \tag {85} \\ \end{array}
$$

Clearly, $\{ \widehat { P } , \widehat { Q } \}$ can be directly obtained by solving the following homogeneous equations

$$H \theta = \mathbf {0}, \tag {86}$$

where all feasible solutions of θ in the above equation constitute the kernel of H, i.e., ker(H).

Next, we show that ker(H) always exists. For the formula $H \theta = \mathbf { 0 } .$ , the number of equations is $N ^ { 2 } n ^ { 2 }$ while the number of unknowns is $2 N ^ { 2 } n ^ { 2 }$ . Meanwhile, the equality constraint (77) needs to hold, which implies that there are $2 N n ^ { 2 }$ redundant variables in θ. Hence, the number of nonredundant elements in θ is

$$q = 2 N ^ {2} n ^ {2} - 2 N n ^ {2}. \tag {87}$$

Then, we have that $q > N ^ { 2 } n ^ { 2 }$ holds if and only if $N \ >$ 2. However, in this situation, the difference between q and rank(W ) satisfies
