# 5. EXAMPLE

In this section, we illustrate the proposed STC approach with a numerical example from the literature. We consider the single link robot arm example from Postoyan et al. (2014). The plant is given by

$$x _ {p, 1} = x _ {p, 2}x _ {p, 2} = - a \sin (x _ {p, 1}) + b \hat {u}y = x _ {p, 1}$$

and the static state-feedback controller by $\textit { u } = \ b ^ { - 1 } \left( a \sin ( x _ { o , 1 } ) - x _ { o , 1 } - x _ { o , 2 } \right)$ . We consider an observer given by

$$x _ {o, 1} = x _ {o, 2} + \theta_ {1} (y - x _ {o, 1})x _ {o, 2} = - a \sin (x _ {o, 1}) + b \hat {u} + \theta_ {2} (y - x _ {o, 1})$$

for $\theta _ { 1 } , \theta _ { 2 } \in \mathbb { R }$ . We define $\boldsymbol { x } _ { p } = \left[ x _ { p , 1 } ~ x _ { p , 2 } \right] ^ { \top } , \boldsymbol { e } = \left[ e _ { 1 } ~ e _ { 2 } \right] ^ { \top } =$ $\left[ \hat { x } _ { o , 1 } - x _ { p , 1 } \right. \left. \hat { x } _ { o , 2 } - x _ { p , 2 } \right] _ { \tau } ^ { \top }$ and $\begin{array} { r l r } { e _ { o } } & { { } = } & { \left[ e _ { o , 1 } \ e _ { o , 2 } \right] ^ { 1 } \quad = } \end{array}$ $\left[ x _ { o , 1 } - x _ { p , 1 } \ x _ { o , 2 } - x _ { p , 2 } \right] ^ { ! }$ .

Note again that Assumption 1 is identical as for the state-feedback case and can thus be verified as in statefeedback case based on linear matrix inequalities. Details on the verification procedure for the considered example are omitted for brevity but can be found in (Hertneck and Allg¨ower, 2021, Section VI.A). Using

$$- a \left(\sin (x _ {p, 1}) - \sin (x _ {o, 1})\right)= 2 a \cos \left(\frac {2 x _ {p , 1} + e _ {o , 1}}{2}\right) \sin \left(- \frac {e _ {o , 1}}{2}\right) = \tilde {a} e _ {o, 1}$$

for a varying parameter $\tilde { a } \in [ - a , a ]$ , the dynamics of the observer error can be written as $\dot { \boldsymbol { e } } _ { o } = \left\lceil \begin{array} { c } { - \boldsymbol { \theta } _ { 1 } } \\ { - \boldsymbol { \theta } _ { 2 } + \tilde { \boldsymbol { a } } } \end{array} \right\rceil \boldsymbol { e } _ { o }$ 1 −θ2 + ˜a 0 eo and it can be easily verified that Assumption 2 holds if $\theta _ { 1 } > 0$ and $\theta _ { 2 } + \tilde { a } > 0$ for all $\tilde { a } \in [ - a , a ]$ . Subsequently, we consider $a \ = \ { \frac { 9 . 8 1 } { \mathrm { ~ 2 ~ } } }$ and $b = 2$ . Using the approach from (Hertneck and Allg¨ower, 2021, Section VI.A), we have computed $n _ { p } = 2 3$ different parameter sets that satisfy Assumption 1 with $\epsilon _ { i } ~ \in ~ [ - 2 \bar { 0 } , 0 . 0 1 ]$ . Moreover, we have chosen $\theta _ { 1 } = \theta _ { 2 } = 1 0$ . The maximum sampling interval, for which UGAS can be guaranteed for periodic sampling, is $t _ { \mathrm { m i n } } = 0 . 1 7 5 \mathrm { s }$ .
