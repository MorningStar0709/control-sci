Notation. For a time dependent vector $x _ { t }$ , we denote by $x _ { k \mid t }$ its prediction at time $k + t$ given information at time t. For any two sets A and B, we define their Minkowski sum as the set $A \oplus B : = \{ a + b : a \in A , b \in B \}$ . We also define, for any constant $\lambda \geq 0 ,$ , the scaled set $\lambda A : =$ $\{ \lambda a , \ a \in { \mathcal { A } } \}$ . For any $d \in \mathbb { N } , x \in \mathbb { R } ^ { d }$ , and $\epsilon > 0$ , let $B ( x , \epsilon ) = \{ y : \| y - x \| \le \epsilon \}$ . Let $\boldsymbol { B } = \boldsymbol { B } ( 0 , 1 )$ . For any set ${ \mathcal { S } } .$ , and any $\varepsilon > 0$ , there exists a polytope P that is an outer approximation of ${ \mathcal { S } } , \operatorname { i . e . , } { \mathcal { S } } \subset { \mathcal { P } } \oplus \varepsilon B$ . We refer to this polytope as the outer polyhedral approximation of S. Unless stated otherwise, we use ∥·∥ to denote the Euclidean norm for vectors and the Frobenius norm for matrices.
