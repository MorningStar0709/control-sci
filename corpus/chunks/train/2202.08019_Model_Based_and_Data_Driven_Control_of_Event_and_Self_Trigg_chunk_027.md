$$
\mathcal {W} ^ {s} = \left\{W ^ {s} \in \mathbb {R} ^ {n _ {w} ^ {s} \times \rho} \bigg | \left[ \begin{array}{c} W ^ {s ^ {\top}} \\ I \end{array} \right] ^ {\top} \left[ \begin{array}{c c} Q _ {d} ^ {s} & S _ {d} ^ {s} \\ * & R _ {d} ^ {s} \end{array} \right] \left[ \begin{array}{c} W ^ {s ^ {\top}} \\ I \end{array} \right] \succeq 0 \right\}
$$

for some known matrices $Q _ { d } ^ { s } \prec 0 \in \mathbb { R } ^ { \rho \times \rho } , S _ { d } ^ { s } \in \mathbb { R } ^ { \rho \times n _ { w } ^ { s } }$ , and $R _ { d } ^ { s } = { R _ { d } ^ { s } } ^ { \top } \in \mathbb { R } ^ { n _ { w } ^ { s } \times n _ { w } ^ { s } }$ , where $n _ { w } ^ { 1 } : = n _ { w } , n _ { w } ^ { s } : = n$ for $s > 1$ .

Define the set of all pairs $[ A ^ { s } \underline { { B } } ^ { s } ]$ consistent with the model (33) and Assumption 2 as the same as [25]

$$\Sigma_ {A B} ^ {s} := \left\{\left[ A ^ {s} \underline {{B}} ^ {s} \right] \in \mathbb {R} ^ {n \times (n + s m)} \mid \right.X _ {+} ^ {s} = A ^ {s} X + \underline {{{B}}} ^ {s} U ^ {s} + B _ {w} ^ {s} W ^ {s}, W ^ {s} \in \mathcal {W} ^ {s} \}. \tag {34}$$

Analogously to Lemma 1, we obtain the following equivalent expression of $\Sigma _ { A B } ^ { s }$ in the form of a QMI

$$\Sigma_ {A B} ^ {s} = \left\{\left[ A ^ {s} \underline {{B}} ^ {s} \right] \in \mathbb {R} ^ {n \times (n + s m)} \right|
\left[ \begin{array}{c} {\left[ A ^ {s} \frac {B ^ {s}}{I} \right] ^ {\top}} \\ \end{array} \right] ^ {\top} \Theta_ {A B} ^ {s} \left[ \begin{array}{c} {\left[ A ^ {s} \frac {B ^ {s}}{I} \right] ^ {\top}} \\ \end{array} \right] \succeq 0 \Bigg \} \tag {35}
$$

where

$$
\Theta_ {A B} ^ {s} = \left[ \begin{array}{c c} Q _ {c c} ^ {s} & S _ {c c} ^ {s} \\ * & R _ {c c} ^ {s} \end{array} \right] := \left[ \begin{array}{c c} - X & 0 \\ - U ^ {s} & 0 \\ \hline X _ {+} ^ {s} & B _ {w} ^ {s} \end{array} \right] \left[ \begin{array}{c c} Q _ {d} ^ {s} & S _ {d} ^ {s} \\ * & R _ {d} ^ {s} \end{array} \right] [ \cdot ] ^ {\top}.
$$

Having obtained a data-based representation of system (27), we can now translate the model-based self-triggering function (31) that depends on $[ A ^ { s } \underline { { B } } ^ { s } ]$ to a data-based one. The following technical assumption on the matrix $\Theta _ { A B } ^ { s }$ is required for the subsequent derivation.

Assumption 3. The matrix $\Theta _ { A B } ^ { s }$ is invertible and has $n _ { w }$ positive eigenvalues.

In practice, Assumptions 3 is satisfied when the data are sufficiently rich and $B _ { w }$ is invertible [24]. Based on Assumption 3, we have the following theorem.
