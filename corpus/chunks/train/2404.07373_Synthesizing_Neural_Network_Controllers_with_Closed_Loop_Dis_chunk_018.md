where $\boldsymbol { x } = \left[ \boldsymbol { x } _ { p } ^ { \top } \ \boldsymbol { x } _ { k } ^ { \top } \right] ^ { \top } , \boldsymbol { v } = \left[ \boldsymbol { v } _ { p } ^ { \top } \ \boldsymbol { v } _ { k } ^ { \top } \right] ^ { \top } , \boldsymbol { w } = \left[ \boldsymbol { w } _ { p } ^ { \top } \ \boldsymbol { w } _ { k } ^ { \top } \right] ^ { \top }$ and $\Delta ~ : ~ \pmb { v } \mapsto \left\lceil \Delta _ { p } ( \pmb { v _ { p } } ) ^ { \top } ~ \phi ( \pmb { v _ { k } } ) ^ { \top } \right\rceil ^ { ! }$ . The matrices $A , \ldots , D _ { e d }$ are expressed in terms of the plant and controller matrices, and are affine in the controller matrices. The expansions of these variables can be found in Appendix B. The combined uncertainty ∆ satisfies the static IQC defined by M defined as

$$
M = \left[ \begin{array}{c c} M _ {v v} & M _ {v w} \\ M _ {v w} ^ {\top} & M _ {w w} \end{array} \right] = \left[ \begin{array}{c c c c} M _ {\Delta_ {p} v v} & 0 & M _ {\Delta_ {p} v w} & 0 \\ 0 & 0 & 0 & \Lambda \\ \hline M _ {\Delta_ {p} v w} ^ {\top} & 0 & M _ {\Delta_ {p} w w} & 0 \\ 0 & \Lambda & 0 & - 2 \Lambda \end{array} \right].
$$

This closed-loop system is an uncertain LTI system of the form in (10), with ∆ satisfying a static IQC. Thus, we apply Lemma 1 to get a matrix inequality condition for dissipativity in terms of the closed-loop parameters and M . For computational tractability of controller synthesis, we would like the matrix inequality to be linear in the controller matrices and storage function parameter $P .$ However, this condition is not affine in the controller matrices due to the term involving M being quadratic in controller matrices when $M _ { v v }$ is non-zero, and the term involving X being quadratic when $X _ { e e }$ is non-zero.

In the following we let θ denote the controller parameters:

$$
\theta \triangleq \left[ \begin{array}{c c c} A _ {k} & B _ {k w} & B _ {k y} \\ C _ {k v} & D _ {k v w} & D _ {k v y} \\ C _ {k u} & D _ {k u w} & D _ {k u y} \end{array} \right]. \tag {18}
$$

This section proceeds by first transforming the condition given by Lemma 1 into a bilinear matrix inequality condition for dissipativity, then using a change of variables to arrive at a linear matrix inequality, and finally presenting a projection-based reinforcement learning algorithm for using the LMI dissipativity condition in training the neural network controller to maximize reward.
