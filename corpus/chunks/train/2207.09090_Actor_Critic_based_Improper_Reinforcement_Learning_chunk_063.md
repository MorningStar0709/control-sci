Algorithm 4 Projection-free Policy Gradient (for MABs)   
Input: learning rate $\eta \in (0,1)$ Initialize each $\pi_1(m) = \frac{1}{M}$ , for all $m \in [M]$ .  
for $t = 1$ to $T$ do $m_*(t) \leftarrow \underset{m \in [M]}{\operatorname{argmax}} \pi_t(m)$ Choose controller $m_t \sim \pi_t$ .  
    Play action $a_t \sim K_{m_t}$ .  
    Receive reward $R_{m_t}$ by pulling arm $a_t$ .  
    Update $\forall m \in [M], m \neq m_*(t)$ :

$$\pi_ {t + 1} (m) = \pi_ {t} (m) + \eta \left(\frac {R _ {m} \mathbb {I} _ {m}}{\pi_ {t} (m)} - \frac {R _ {m _ {*} (t)} \mathbb {I} _ {m _ {*} (t)}}{\pi_ {t} (m _ {*} (t))}\right) \tag {18}\operatorname{Set} \pi_ {t + 1} (m _ {*} (t)) = 1 - \sum_ {m \neq m _ {*} (t)} \pi_ {t + 1} (m).$$

end for

Proof. The proof is an extension of that of Theorem 1 of (Denisov & Walton, 2020) for the setting that we have. The proof is divided into three main parts. In the first part we show that the recurrence time of the process $\{ \pi _ { t } ( m ^ { * } ) \} _ { t \geqslant 1 }$ is almost surely finite. Next we bound the expected value of the time taken by the process $\pi _ { t } ( m ^ { * } )$ to reach 1. Finally we show that almost surely, $\operatorname* { l i m } _ { t  \infty } \pi _ { t } ( m ^ { * } )  1$ , in other words the process $\{ \pi _ { t } ( m ^ { * } ) \} _ { t \geqslant 1 }$ is transient. We use all these facts to show a regret bound.

Recall $m _ { * } ( t ) : = \mathrm { a r g m a x } \pi _ { t } ( m )$ . We start by defining the following quantity which will be useful for the analysis of m∈[M ] algorithm 4.

Let τ := min $\begin{array} { r } { \left\{ t \geqslant 1 : \pi _ { t } ( m ^ { * } ) > \frac { 1 } { 2 } \right\} } \end{array}$

Next, let $\begin{array} { r } { S : = \big \{ \pi \in \mathcal { P } ( [ M ] ) : \frac { 1 - \alpha } { 2 } \leqslant \pi ( m ^ { * } ) < \frac { 1 } { 2 } \big \} . } \end{array}$

In addition, we define for any $\begin{array} { r } { a \in \mathbb { R } , S _ { a } : = \left\{ \pi \in \mathcal { P } ( [ M ] ) : \frac { 1 - \alpha } { a } \leqslant \pi ( m ^ { * } ) < \frac { 1 } { x } \right\} } \end{array}$ . Observe that if $\pi _ { 1 } ( m ^ { * } ) \geqslant 1 / a$ and $\pi _ { 2 } ( m ^ { * } ) < 1 / a$ then $\pi _ { 1 } \in { \cal S } _ { a }$ . This fact follows just by the update step of the algorithm 4, and choosing $\eta = \alpha \pi _ { t } ( m )$ for every m 6= m∗.

Lemma H.9. For α > 0 such that $\begin{array} { r } { \alpha < \frac { \Delta _ { m i n } } { \mathfrak { r } ( m ^ { * } ) - \Delta _ { m i n } } } \end{array}$ , we have that

$$\sup _ {\pi \in \mathcal {S}} \mathbb {E} \left[ \tau \mid \pi_ {1} = \pi \right] < \infty .$$
