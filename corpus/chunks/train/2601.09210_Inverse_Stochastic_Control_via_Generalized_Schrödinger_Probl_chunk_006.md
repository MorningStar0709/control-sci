Denote by $P _ { t } ^ { u } \in \mathcal { P } ( \mathbb { R } ^ { d } \times U )$ the probability distribution of $( X _ { t } ^ { u } , u _ { t } )$ under $\mathbb { P }$ for each $t \in [ 0 , T ]$ . Also denote by $\dot { P } _ { t } ^ { u }$ the law of X ut , i.e., $\tilde { P } _ { t } ^ { u } ( A ) = P _ { t } ^ { u } ( A \times U ) , A \in \mathcal { B } ( \mathbb { R } ^ { d } )$ . With these notation,

$$V (f, g) = \int_ {0} ^ {T} \langle f (t, \cdot , \cdot), \mu_ {t} \rangle d t + \langle g, \tilde {\mu} _ {T} \rangle - J ^ {*} (f, g).$$

Proposition 2.1. Let pA1q hold. Suppose that $\mu _ { t }$ is the marginal law $o f \left( X _ { t } ^ { u ^ { * } } , u _ { t } ^ { * } \right)$ for each $t \in [ 0 , T ]$ , where $u ^ { * } = \{ u _ { t } ^ { * } \} _ { 0 \leqslant t \leqslant T } \in \mathcal { U }$ is an optimal policy to the problem (1.1) for some $( f ^ { \ast } , g ^ { \ast } ) \in { \mathcal { D } }$ . Then, this $( f ^ { * } , g ^ { * } )$ is a minimizer for the inverse problem (1.4) and we have

$$V (f ^ {*}, g ^ {*}) = \inf _ {(f, g) \in \mathcal {D}} V (f, g) = 0.$$

In the following analysis, we do not assume optimality of $\mu _ { t }$ . Even more, each $\mu _ { t }$ may not be necessarily a marginal measure of $( X _ { t } ^ { u } , u _ { t } )$ for some $u \in \mathcal { U }$ . However, we do assume the following conditions on $\mu _ { t }$ and D:

(A2) The family $\{ \mu _ { t } \} _ { 0 \leqslant t \leqslant T }$ satisfies

$$\int_ {0} ^ {T} \int_ {\mathbb {R} ^ {d} \times U} (| x | ^ {2} + | u | ^ {2}) \mu_ {t} (d x d u) d t < \infty .$$

(A3) There exists a constant $K > 0$ such that for any $( f , g ) \in \mathcal { D }$ we have

$$| f (t, x, u) | + | g (x) | \leqslant K (1 + | x | ^ {2} + | u | ^ {2}),$$

for $t \in [ 0 , T ] , x \in \mathbb { R } ^ { d } , u \in U .$ .

Under (A1)–(A3), by (2.1) it is straightforward to check that $V ^ { * } = \operatorname* { i n f } _ { ( f , g ) \in { \mathcal { D } } } V ( f , g ) > - \infty$

Further, we assume the following:

(A4) U is compact.

For $( f , g ) \in \mathcal { D }$ , define $\phi : [ 0 , T ] \times \mathbb { R } ^ { d } \times U  \mathbb { R } ^ { 2 }$ by

$$\phi_ {f, g} (t, x, u) = \left(f (t, x, u), g (x)\right) ^ {\mathsf {T}}.$$

Then we assume the following:

(A5) The class $\left\{ \phi _ { f , g } : \left( f , g \right) \in \mathcal { D } \right\}$ is contained in $C ( [ 0 , T ] \times \mathbb { R } ^ { d } \times U , \mathbb { R } ^ { 2 } )$ and is equi-uniformly continuous.

Example 2.2. Assume that each $h = f , g$ is given by

$$h (\xi) = \sum_ {j = 1} ^ {N} \gamma_ {j} e ^ {- \alpha_ {j} | \xi - \xi_ {j} | ^ {2}}, \quad \xi \in [ 0, T ] \times \mathbb {R} ^ {d} \times U,$$
