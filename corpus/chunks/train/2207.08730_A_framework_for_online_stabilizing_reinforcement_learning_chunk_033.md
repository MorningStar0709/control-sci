$$\hat {J} ^ {w _ {k + 1}} (X _ {(k + 1) \delta}) \leq \hat {J} ^ {w _ {k}} (X _ {(k + 1) \delta}) + \varepsilon_ {1} \leq\hat {J} ^ {w _ {k}} (\Lambda_ {U _ {k \delta}} (X _ {k \delta})) + \mathrm{Lip} _ {\hat {J}} \chi_ {1} (\delta) + \varepsilon_ {1} \leq\hat {J} ^ {w _ {k}} (X _ {k \delta}) - \delta \bar {\nu} + \varepsilon + \operatorname{Lip} _ {\hat {J}} \chi_ {1} (\delta) \leq\alpha_ {\mathrm{up}} (\| X _ {0} \|) - \delta \bar {\nu} + \varepsilon + \operatorname{Lip} _ {\hat {J}} \chi_ {1} (\delta) \leq\alpha_ {\mathrm{up}} (\| X _ {0} \|) + \varepsilon + \delta (\operatorname{Lip} _ {\hat {j}} \sigma_ {\max} \operatorname{Lip} _ {Z} - \bar {\nu}) +\delta^ {2} \mathrm{Lip} _ {\hat {J}} \mathrm{Lip} _ {f} (\bar {f} + \sigma_ {\max} \mathrm{Lip} _ {Z}). \tag {25}$$

Now, pick δ to be sufficiently small:

$$\delta < \frac {\bar {\nu}}{2 \operatorname{Lip} _ {\hat {J}} \operatorname{Lip} _ {f} (\bar {f} + \sigma_ {\max} \operatorname{Lip} _ {Z})}. \tag {26}$$

Substituting (26) into (25) yields

$$\hat {J} ^ {w _ {k + 1}} (X _ {(k + 1) \delta}) \leq \alpha_ {\mathrm{up}} (\| X _ {0} \|). \tag {27}$$

Lemma 3: Let $\begin{array} { r l r } { K } & { { } \in } & { \mathbb { N } } \end{array}$ and $\begin{array} { r l r } { w _ { 0 } } & { { } = } & { w ^ { \# } } \end{array}$ . If $\bar { \nu } \mathrm { ~  ~ { ~ \geq ~ } ~ }$ $\mathrm { 2 L i p } _ { \hat { J } } \sigma _ { \mathrm { m a x } } \mathrm { L i p } _ { Z } + 2 { \frac { \varepsilon } { \delta } }$ , then for a sufficiently small δ the following implication holds:

$$
\begin{array}{l} \forall k \leq K G _ {\varepsilon} ^ {\bar {\nu}} (w _ {k}, w _ {k - 1}, \Lambda_ {U _ {k \delta}} (X _ {k \delta}), X _ {k \delta}) \leq 0 \implies \tag {28} \\ \forall t \in [ 0, (K + 1) \delta ] X _ {t} \in \mathcal {B} _ {S ^ {*}}. \\ \end{array}
$$

Proof: Note, that

$$\hat {J} ^ {w _ {0}} (X _ {0}) = L (X _ {0}) \leq \alpha_ {\mathrm{up}} (\| X _ {0} \|). \tag {29}$$

By performing mathematical induction with (29) as its base and Lemma 2 as its step we obtain:

$$\forall k \leq K \hat {J} ^ {w _ {k}} (X _ {k \delta}) \leq \alpha_ {\mathrm{up}} (\| X _ {0} \|). \tag {30}$$

Together with (23) the above yields (28).

The latter lemma justifies the choice of Lipschitz constants and bounds such as for instance (19). The said bounds will have been made heavy use of in the proofs of the presented theorems.

Next, notice that the right-hand side of (1) is continuous in x in each kth sampling step. Noticing that both f and σ are Lipschitz continuous in the first argument furnishes the existence of local (path-wise) solutions. Combining this with the fact that S∗ is not exceeded we conclude existence of global solutions.
