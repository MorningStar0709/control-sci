# C. Cooperative Cyber-Physical Optimization

To ensure the optimal management of cyber and physical resources during power system contingencies, we develop the following resilient cyber-physical optimization model:

$$\text { upper } \quad \min _ {c} \quad \alpha_ {1} f _ {\text { cyber }} \tag {14a}\text { s. t. } \quad (7), (8), (9), (1 0). \tag {14b}\text { lower } \quad \min _ {p, r} \alpha_ {2} f _ {\text { power }} + \alpha_ {3} f _ {\text { res }} \tag {14c}\text { s. t. } \quad (1), (3), (4), (1 3). \tag {14d}$$

where ${ \mathcal { c } } = \{ y _ { \hat { \imath } \hat { \jmath } } , x _ { \hat { \imath } } \} , \rho = \{ P _ { i , g } ^ { \mathrm { g e n } } , Q _ { i , g } ^ { \mathrm { g e n } } \}$ gen , and $\mathcal { F } = \{ \tilde { x } _ { i } , P _ { i } ^ { \mathrm { e s s } } \}$ respectively, and $\alpha _ { 1 } , \ \alpha _ { 1 }$ , and $\alpha _ { 3 }$ are adjustable balancing coefficients. Problem (14) is framed as a bi-level optimization problem that incorporates both the cyber (upper) and cyberphysical (lower) layers into resilient CPPS operation.

To deploy the cooperative cyber-physical optimization model in (14), we propose an adaptive cyber and physical optimization algorithm shown in Algorithm 1. Algorithm 1 is end

Algorithm 1: Adaptive cyber and physical resource optimization.   
Input: Cyber cost coefficients $c_{\hat{i}\hat{j}}^{\prime}, c_{\hat{i}}^{\prime}, \tilde{c}_{\hat{i}}^{\prime}$ , physical cost coefficients $c_{i,g}^{2}, c_{i,g}^{1}, c_{i,g}^{0}, c_{i}^{\mathrm{ess}}$ , balancing coefficients $\alpha_{1}, \alpha_{1}, \alpha_{3}$ , power system parameters.

Initialize: Solve (11) to initialize the cyber topology; if Cyberattack is False then

Solve (5) to control the physical resources based on the cyber topology;

else if Cyberattack is True then

Isolate the corrupted cyber node $\hat{i}_{\tilde{c}}$ ;

for Cyber sources $m \in \mathcal{M}_{\hat{i}_{\tilde{c}}}$ in parallel do

Update the list of critical cyber nodes by replacing $\hat{i}_{\tilde{c}}$ with $m$ ;

Solve (14a) and (14b) to find minimal cyber costs with $\tilde{x}_6$ ;

end

Output updated cyber topology;

Solve (14c) and (14d) to obtain physical actions in response to the cyberattack;

Output: ${ \mathcal { C } } , { \mathcal { R } } , { \mathcal { F } } .$
