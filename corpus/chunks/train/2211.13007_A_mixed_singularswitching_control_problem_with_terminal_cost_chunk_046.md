Therefore $( x , \iota ) \in S _ { \ell , \ell _ { i } } ^ { \varepsilon } \subset \widetilde { S } _ { \ell } ^ { \varepsilon }$ .
□

Now we construct the optimal control $\left( \xi ^ { \varepsilon , * } , \varsigma ^ { \varepsilon , * } \right)$ to the problem (4.4). Let $( x _ { 0 } , \ell _ { 0 } , \iota _ { 0 } ) \in$ $\overline { { \mathcal { O } } } \times \mathbb { M } \times \mathbb { I }$ . The dynamics of the process $( X ^ { \varepsilon , * } , I ^ { * } ) \colon = \{ ( X _ { t } ^ { \varepsilon , * } , I _ { t } ^ { * } ) : t \geq 0 \}$ and $\left( \xi ^ { \varepsilon , * } , \varsigma ^ { \varepsilon , * } \right)$ is given recursively in the following way:

(i) Define $\tau _ { 0 } ^ { * } = 0$ and $\ell _ { 0 } ^ { \ast } = \ell _ { 0 } . \mathrm { ~ H ~ } ( x _ { 0 } , \iota _ { 0 } ) \notin \mathcal { C } _ { \ell _ { 0 } } ^ { \varepsilon } .$ , take $\tau _ { 1 } ^ { * } { : } = 0$ and pass to item (ii) due to Lemma 4.3. Otherwise, the process $( X ^ { \varepsilon , * } , \breve { I } ^ { * } )$ evolves as

$$
\begin{array}{l} X _ {t \wedge \tilde {\tau} _ {1} ^ {*}} ^ {\varepsilon , *} = \tilde {x} - \int_ {0} ^ {t \wedge \tilde {\tau} _ {1} ^ {*}} [ b (X _ {s} ^ {\varepsilon , *}, I _ {s} ^ {*}) + \mathfrak {m} _ {s} ^ {\varepsilon , *} \dot {\zeta} _ {s} ^ {\varepsilon , *} ] \mathrm{d} s + \int_ {0} ^ {t \wedge \tilde {\tau} _ {1} ^ {*}} \sigma (X _ {s} ^ {\varepsilon , *}, I _ {s} ^ {*}) \mathrm{d} W _ {s}, \\ I _ {t \wedge \tilde {\tau} _ {1} ^ {*}} ^ {*} = I _ {t \wedge \tilde {\tau} _ {1} ^ {*}} ^ {(\ell_ {0} ^ {*})}, \end{array} \quad \text { for } t > 0, \tag {4.15}
$$

with $X _ { 0 } ^ { \varepsilon , * } = x _ { 0 } , I _ { 0 } ^ { * } = \iota _ { 0 } , \tau ^ { * } { : = } \operatorname* { i n f } \{ t > 0 : ( X _ { t } ^ { \varepsilon , * } , I _ { t } ^ { * } ) \notin { \mathcal O } \}$ ,

$$\tilde {\tau} _ {1} ^ {*} := \tau_ {1} ^ {*} \wedge \tau^ {*} \quad \text { and } \quad \tau_ {1} ^ {*} := \inf \big \{t \geq 0: (X _ {t} ^ {\varepsilon , *}, I _ {t} ^ {*}) \in \mathcal {S} _ {\ell_ {0} ^ {*}} ^ {\varepsilon} \big \}. \tag {4.16}$$

The control process $\xi ^ { \varepsilon , * } = ( \mathbb { n } ^ { \varepsilon , * } , \zeta ^ { \varepsilon , * } )$ is defined by
