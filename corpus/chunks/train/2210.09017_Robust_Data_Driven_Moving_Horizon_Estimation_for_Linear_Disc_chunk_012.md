In the following, we prove pRES of the data-driven MHE scheme. To simplify the notation, we denote sequences of finite or infinite length by bold face symbols $\begin{array} { r l } { \mathbf { v } } & { { } : = } \end{array}$ $\{ v ( t _ { 1 } ) , \ldots , v ( t _ { 2 } ) \}$ for some $t _ { 1 } , t _ { 2 } \in \mathbb { I } _ { \geq 0 } \mathrm { o r } \mathbf { v } : = \{ v ( t _ { 1 } ) , v ( t _ { 1 } +$ $1 ) , \ldots \}$ . The length of these sequences will be understood from the context. The solution to (8) at time t for initial condition $x _ { 0 }$ and input sequence u is denoted by $x ( t ; x _ { 0 } , { \mathbf { u } } )$ . We denote the corresponding output as $y ( t ) ~ = ~ C x ( t ) ~ +$ $D u ( t ) = : h ( x ( t ; x _ { 0 } , { \bf u } ) , u ( t ) )$ ).

In order to prove RGES, we will assume (compare Assumption 2 below) that the considered system (8) is detectable, which implies “incremental exponential uniform output-tostate stability” (e-UOSS) [32, Corollary 7], [3] as defined below.

Definition 2: The linear system (8) is e-UOSS if there exist constants $p _ { 0 } , r _ { 0 } \ge 1 , \eta \in ( 0 , 1 )$ such that for each pair of initial conditions $x _ { 1 } , x _ { 2 } \in \mathbb { R } ^ { n }$ and any input sequence u generating the states $x ( t ; x _ { 1 } , \mathbf { u } )$ and $x ( t ; x _ { 2 } , { \mathbf { u } } )$ the following

2This prior is typically called “filtering prior” in the MHE literature, compare, e.g., [27].

3For instance, consider the situation, where a good prior estimate and (only) noisy measurements are available, then a suitable choice for the weighting matrices is $R < < P$ .

holds for all $t \in \mathbb { I } _ { \geq 0 } \colon$

$$
\begin{array}{l} \left| x \left(t; x _ {1}, \mathbf {u}\right) - x \left(t; x _ {2}, \mathbf {u}\right) \right| \leq p _ {0} \left| x _ {1} - x _ {2} \right| \eta^ {t} + \\ \sum_ {\tau = 1} ^ {t} r _ {0} | h (x (t - \tau ; x _ {1}, \mathbf {u}), u (t - \tau)) \\ - h (x (t - \tau ; x _ {2}, \mathbf {u}), u (t - \tau)) | \eta^ {\tau}. \tag {12} \\ \end{array}
$$

The notion of “uniformity” is used since the right-hand side of (12) holds uniformly for all u (with the same $p _ { 0 } , r _ { 0 } .$ , and η).
