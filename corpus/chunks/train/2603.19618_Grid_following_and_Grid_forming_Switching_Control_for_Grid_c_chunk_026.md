```mermaid
graph TD
    A["Initialization: Operate the GCI in an active control mode (σ = a ∈ {GFL, GFM})."] --> B["Model Construction: Update the multi-parameter state matrices for both the active (A_a) and the target (A_t) subsystems."]
    B --> C["Boundary Characterization: Describe the detailed SSSRs (Ω_a, Ω_t) with respect to the concerned parameters for both subsystems."]
    C --> D["Index Evaluation: Calculate the CSIs (J_a, J_t) for the current operating point."]
    D --> E{J_t - J_a > ε_h(ω_m + ω_d)}
    E -->|No| C
    E -->|Yes| F["Switching Execution: Switch to the target control mode (σ = t ∈ {GFM, GFL})."]
    F --> B
```
</details>

Fig. 6. Flowchart of the proposed CSI-based switching control strategy. The parameter εh represents the hysteresis threshold, and the sum term $\left( \omega _ { m } + \omega _ { d } \right)$ denotes the theoretical maximum value of the CSI.

• The normalized parameter sensitivity ${ \bar { S } } ( X )$ quantifies the vulnerability of the system’s stability margin to parameter variations. It is evaluated based on the Euclidean norm of the gradient vector $\nabla f (  { \boldsymbol { X } } )$ obtained in (20). This indicator serves as a penalty term within the CSI, where a smaller sensitivity value signifies that the operating point possesses greater robustness against parameter drift.   
• The normalized boundary distance ${ \vec { D } } ( X )$ represents the minimum Euclidean distance from the operating point to the nearest SSSR boundary within the parameter space. This metric explicitly quantifies the fault tolerance of the system under large parameter disturbances, where a greater distance indicates a larger security margin.

Consequently, the proposed CSI effectively identifies the superior operating points that possess high stability margins, exhibit low sensitivity to parameter fluctuations, and reside near the geometric center of the SSSR.
