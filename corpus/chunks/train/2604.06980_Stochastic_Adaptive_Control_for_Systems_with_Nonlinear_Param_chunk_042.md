# C Proof of Theorem 3

For each $t \geq 1 ,$ , we define $\zeta _ { t } = [ w _ { t } ^ { \top } , \kappa _ { t } ^ { \top } + v _ { t } ^ { \top } ] ^ { \top } , \kappa _ { t } \ =$ = $\pi _ { \widehat { \theta } _ { t - 1 } } ( x _ { t } ) - \mathbb { E } [ \pi _ { \widehat { \theta } _ { t - 1 } } ( x _ { t } ) \mid \mathcal { F } _ { t - 1 } ]$ , and $\delta _ { t } \mathbf { \bar { \Gamma } } = \pi _ { \hat { \theta } _ { t - 1 } } \mathbf { \bar { \Gamma } } ( x _ { t } ) \mathbf { \delta } -$ $\pi _ { \hat { \theta } _ { t - 1 } } \left( f ( \theta ^ { * ^ { \top } } \phi _ { t - 1 } ) \right)$ .

We first prove the following lemma.

Lemma 3 Under Assumptions 1-4, there exists a realvalued random variable $\bar { c } > 0$ such that

$$\lambda_ {\min} \left\{\sum_ {\tau = 1} ^ {t} \zeta_ {\tau} \zeta_ {\tau} ^ {\top} \right\} \geq \bar {c} t ^ {1 - 2 b}, a. s., \forall t \geq 0.$$

Proof. Since $\pi _ { \widehat { \theta } _ { t } } ( \cdot )$ is L−Lipschitz, we have

$$\left\| \delta_ {t + 1} \right\| = \left\| \pi_ {\hat {\theta} _ {t}} \left(x _ {t + 1}\right) - \pi_ {\hat {\theta} _ {t}} \left(f \left(\theta^ {* \top} \phi_ {t}\right)\right) \right\| \tag {C.1}\leq L \| x _ {t + 1} - f \left(\theta^ {* ^ {\top}} \phi_ {t}\right) \| \leq L \| w _ {t + 1} \|.$$

Hence, by Assumption 2,

$$\sup _ {t \geq 0} \mathbb {E} \left[ \| \delta_ {t + 1} \| ^ {\gamma} \mid \mathcal {F} _ {t} \right] \leq \sup _ {t \geq 0} L \mathbb {E} \left[ \| w _ {t + 1} \| ^ {\gamma} \mid \mathcal {F} _ {t} \right] < \infty , \text { a.s. } \tag {C.2}$$

Since $\pi _ { \hat { \theta } _ { \star } } ( f ( \theta ^ { * } ^ { \top } \phi _ { t } ) )$ is Ft−measurable, we have $\mathbb { E } \left[ \delta _ { t + 1 } ~ | ~ { \check { \mathcal { F } } } _ { t } \right] = \mathbb { E } [ \pi _ { \hat { \theta } _ { t } } ( x _ { t + 1 } ) ~ | ~ \mathcal { F } _ { t } ] - \pi _ { \hat { \theta } _ { t } } \left( f ( { \theta } ^ { * } { } ^ { \top } \phi _ { t } ) \right)$  . Thus, by the definition of $\kappa _ { t + 1 }$ , we obtain

$$\kappa_ {t + 1} = \delta_ {t + 1} - \mathbb {E} \left[ \delta_ {t + 1} \mid \mathcal {F} _ {t} \right], \forall t \geq 0.$$

From this and (C.2), it follows that

$$\sup _ {t \geq 0} \mathbb {E} \left[ \| \kappa_ {t + 1} \| ^ {\gamma} \mid \mathcal {F} _ {t} \right] < \infty . \tag {C.3}$$

Note that $\{ \| \kappa _ { t + 1 } \| ^ { 2 } - \mathbb { E } [ \| \kappa _ { t + 1 } \| ^ { 2 } \ | \ \mathcal { F } _ { t } ] \} _ { t \geq 0 }$ is a martingale difference sequence. Then by (C.3) and martingale convergence theorem (cf. Theorem 2.8 in Chen and Guo (1991)), we have for any $\eta > 0$ ,
