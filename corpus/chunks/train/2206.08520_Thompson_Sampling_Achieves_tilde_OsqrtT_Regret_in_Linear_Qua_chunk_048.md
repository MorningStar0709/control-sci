# Appendix B. System Identification and Stabilization Guarantees

In this section, we show that improved exploration of TSAC provides persistently exciting inputs, which will be used to enable reaching a stabilizing neighborhood around $\Theta _ { * }$ . From Assumption 2, we have that $\mathbb { E } [ x _ { t + 1 } x _ { t + 1 } ^ { \intercal } \mid \mathcal { F } _ { t } ] \ \succcurlyeq \ \sigma _ { w } ^ { 2 } I$ . Thus, with the input $u _ { t } = K ( \tilde { \Theta } _ { t } ) x _ { t } + \nu _ { t }$ for νt ∼ $\mathcal { N } ( 0 , 2 \kappa ^ { 2 } \sigma _ { w } ^ { 2 } I )$ , we have that $\begin{array} { r } { \mathbb { E } [ z _ { t + 1 } z _ { t + 1 } ^ { \intercal } \mid \mathcal { F } _ { t } ] \succcurlyeq \frac { \sigma _ { w } ^ { 2 } } { 2 } I . } \end{array}$ . Using Lemma 31, we have that $\begin{array} { r } { V _ { t } \succcurlyeq t \frac { \sigma _ { w } ^ { 2 } } { 4 0 } I } \end{array}$ for $t \geq 2 0 0 ( n + d )$ log $\textstyle { \frac { 1 2 } { \delta } }$ with probability at least $1 - \delta$ . Using the RLS estimate error bound given in Section 2.3, i.e., under the event of $\hat { E } _ { t }$ we have

$$\left\| \hat {\Theta} _ {t} - \Theta_ {*} \right\| _ {2} \leq \frac {\beta_ {t}}{\sqrt {\lambda_ {\min} (V _ {t})}} \tag {18}$$

with probability at least 1 − δ. Plugging in the $\lambda _ { \operatorname* { m i n } } ( V _ { t } )$ in its place yields the first result.
