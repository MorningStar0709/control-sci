where the last line used the fact $\ell ( \cdot , \cdot ) \leq B$ . Note that each summand in the above summation can be upper bounded by $\frac { 2 K L _ { \ell } \bar { y } } { t - \tau }$ according to Lemma 1. The equation above then gives $\begin{array} { r } { | \dot { X } _ { i , \tau } - X _ { i , \tau - 1 } | \le T ^ { - 1 } \bigl ( B + 2 K L _ { \ell } \bar { y } \log ( T ) \bigr ) } \end{array}$ . With this martingale difference bound, applying the Azuma-Hoeffding’s inequality to $\{ X _ { i , t } \} _ { t = 0 } ^ { T }$ gives

$$
\begin{array}{l} \mathbb {P} ^ {\prime} \left(\left| X _ {i, T} - X _ {i, 0} \right| \geq \epsilon\right) = \mathbb {P} \left(\left| \hat {\mathcal {L}} _ {i} (\mathrm{TF}) - \mathcal {L} _ {i} (\mathrm{TF}) - \Delta_ {i} \right| \geq \epsilon \mid \mathcal {B} _ {M}\right) \\ \leq 2 e ^ {- \frac {T \epsilon^ {2}}{(B + 2 K L _ {\ell} \bar {y} \log (T)) ^ {2}}}. \\ \end{array}
$$

where $\Delta _ { i } : = \mathcal { L } _ { i } ( \mathrm { T F } ) - \mathbb { E } ^ { \prime } [ \hat { \mathcal { L } } _ { i } ( \mathrm { T F } ) ]$ . Let $Y _ { i } : = \hat { \mathcal { L } } _ { i } ( \mathrm { T F } ) \mathrm { ~ - ~ }$ $\mathcal { L } _ { i } ( \mathrm { T F } ) - \Delta _ { i }$ , then the above equation tells that $Y _ { i }$ is sub-Gaussian conditioning on $\boldsymbol { B } _ { M }$ . Following from sub-Gaussian concentration bound, we have

$$\mathbb {P} \left(M ^ {- 1} \mid \sum_ {i = 1} ^ {M} Y _ {i} \right| \geq \epsilon \left| \mathcal {B} _ {M}\right) \leq e ^ {- \frac {c M T \epsilon^ {2}}{(B + 2 K L _ {\ell} \bar {y} \log (T)) ^ {2}}}, \tag {8}$$

for some absolute constant c. This further translates to, conditioning on $B _ { M }$ , with probability at least 1 − δ,

$$\left| M ^ {- 1} \sum_ {i = 1} ^ {M} Y _ {i} \right| \leq \left(B + 2 K L _ {\ell} \bar {y} \log (T)\right) \sqrt {\log (2 / \delta) / (c M T)}.$$

The definition of Yi gives $\begin{array} { r } { \vert M ^ { - 1 } \sum _ { i = 1 } ^ { M } Y _ { i } \vert = \vert \hat { \mathcal { L } } ( \mathrm { T F } ) - \mathcal { L } ( \mathrm { T F } ) } \end{array}$ $\begin{array} { r } { - M ^ { - 1 } \sum _ { i = 1 } ^ { M } \Delta _ { i } \vert \ge \vert \hat { \mathcal { L } } ( \mathrm { T F } ) - \mathcal { L } ( \overline { { \mathrm { T F } } } ) \vert ^ { - } \vert M ^ { - 1 } \sum _ { i = 1 } ^ { M } \Delta _ { i } \vert . } \end{array}$ This implies, conditioning on $B _ { M }$ , with probability at least $1 - \delta ,$
