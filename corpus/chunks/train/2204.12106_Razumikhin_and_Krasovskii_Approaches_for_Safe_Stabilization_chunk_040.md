# 5.2. Feedback Control Design

With the sliding surface functional (44), we next address the controller design. In the ideal sliding motion case, the system state is to satisfy the manifold invariant condition:

$$\mathbf {U} (\phi) = 0, \tag {48}$$

which can be verified via the functional $\mathbf { W } ( \phi ) : = \mathbf { \phi } 0 . 5 \mathbf { U } ^ { 2 } ( \phi )$ . Specifically, the time derivative of $\mathbf { W } ( \phi )$ is given by

$$
\begin{array}{l} D ^ {+} \mathbf {W} (\phi) = \mathbf {U} (\phi) D ^ {+} \mathbf {U} (\phi) \\ = \mathbf {U} (\phi) (- \mathbf {H} (\phi) \mathbf {J} _ {2} (\phi) \mathbf {H} ^ {\top} (\phi) + \mathbf {H} (\phi) \mathbf {J} _ {3} (\phi) \mathbf {H} ^ {\top} (\phi) \\ + \mathbf {L} (\phi) + \mathbf {G} (\phi) u) \\ = \mathbf {U} (\phi) (\mathbf {H} (\phi) \mathbf {J} _ {3} (\phi) \mathbf {H} ^ {\top} (\phi) + \mathbf {L} (\phi) + \mathbf {G} (\phi) u). \tag {49} \\ \end{array}
$$

By the invariance condition $D ^ { + } \mathbf { W } ( \phi ) = 0$ , the equivalent control law is derived as

$$u _ {\mathrm{e}} (\phi) = \frac {\mathbf {G} ^ {\top} (\phi) (\mathbf {H} (\phi) \mathbf {J} _ {3} (\phi) \mathbf {H} ^ {\top} (\phi) + \mathbf {L} (\phi))}{- \| \mathbf {G} (\phi) \| ^ {2}}.$$

The equivalent control law $u _ { \mathsf { e } }$ neutralizes all working forces which locally affect the magnitude of the sliding surface coordinate. Since the state trajectory of the system (1) may move into the sublevel and superlevel sets of the sliding surface functional, the applied controller is of the following form:

$$u (\phi) = \frac {\mathbf {G} ^ {\top} (\phi) (\mathbf {H} (\phi) \mathbf {J} _ {3} (\phi) \mathbf {H} ^ {\top} (\phi) + \mathbf {L} (\phi) + \mathbf {K} (\phi))}{- \| \mathbf {G} (\phi) \| ^ {2}}, \tag {50}$$

where $\mathbf { K } ( \phi ) > 0$ is the additional item to be designed based on the applied sliding surface functional $\mathbf { U } ( \phi )$ . Obviously, $u ( \phi ) =$ $u _ { \Theta } ( \phi ) - | | \mathbf { G } ( \phi ) | | ^ { - 2 } \mathbf { G } ^ { \top } ( \phi ) \mathbf { K } ( \phi )$ .

From all above discussion, we have the following theorem, which shows the satisfaction of both stabilization and safety objectives via the sliding surface functional U(φ) in (44).

Theorem 8. Consider the system (1) with the safe set $\mathbb { C } \subset \mathbb { R } ^ { n }$ and the initial state $\xi \in C ( [ - \Delta , 0 ]$ , Int(C)). If Assumption 1 holds, and the sliding surface functional U in (44) is such that
