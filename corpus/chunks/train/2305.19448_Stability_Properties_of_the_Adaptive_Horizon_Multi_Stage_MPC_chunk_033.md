# 6.1. Example 1 - Spring damped mass system

This is a simple example adapted from [28] used to demonstrate the performance of an adaptive horizon multistage MPC with a fully branched scenario tree, and therefore suitable terminal conditions that guarantee recursive feasibility and ISpS stability (see Section 5.1). The system model is written as:

$$\dot {x} _ {1} = x _ {2}, \quad \dot {x} _ {2} = - \frac {k _ {0}}{m} \mathrm{e} ^ {- x _ {1}} x _ {1} - \frac {h _ {d}}{m} x _ {2} + \frac {u}{m}$$

where the state variable vector $\boldsymbol { x } = [ x _ { 1 } , ~ x _ { 2 } ] ^ { \top }$ includes the displacement and the velocity of the mass $m \ : = \ : 1$ kg, respectively. $k = k _ { 0 } \mathrm { e } ^ { - x _ { 1 } }$ , is the elastic constant of the spring and $k _ { 0 } = 0 . 3 3 \ \mathrm { N / m }$ . The damping factor $h _ { d }$ is uncertain and can have three different possible values $h _ { d } ^ { r } = \{ 1 . 0 , 2 . 0 , 4 . 0 \}$ . / Ns m with equal probabilities. The damping factor is . , . , .assumed to vary unpredictably between sampling intervals of $\Delta t = 0 . 4 \mathrm { ~ s ~ }$ .

The objective is to control the mass to its equilibrium position $x _ { 1 } = 0$ using an external force u. The stage cost is given by $\ell = x ^ { \top } Q x + u ^ { \top }$ Ru and $h _ { d }$ varies randomly at each time step with the previously defined probabilities.
