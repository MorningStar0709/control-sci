# Proposition 3.1. It holds:

(1) The mapping $x \mapsto v ( x , i ; \theta )$ is decreasing for every $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ .   
(2) The mapping $\theta \mapsto v ( x , i ; \theta )$ is decreasing for every $( x , i ) \in { \mathcal { O } } .$ .

The next result excludes the possibility of empty stopping regions.

Proposition 3.2. It holds ${ S _ { i n f } ^ { \theta } = \varnothing }$ and $\begin{array} { r } { S _ { s u p } ^ { \theta } \neq \varnothing . } \end{array}$

Proof. See Appendix A.

We define the free-boundaries

$$\alpha_ {i} (\theta) := \sup \{x \in \mathcal {I}: v (x, i; \theta) \geq k _ {1} \}, \quad \beta_ {i} (\theta) := \inf \{x \in \mathcal {I}: v (x, i; \theta) \leq k _ {2} \}, \tag {3.7}$$

for any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ , with the conventions sup $\varnothing \ = \ \underline { { x } }$ and inf $\varnothing \ = \ { \overline { { x } } } .$ . Then, thanks to the monotonicity of $v ( \cdot , i ; \theta ) , \ ( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ , we have

$$\mathcal {C} ^ {\theta} = \{(x, i) \in \mathcal {O}: x \in (\alpha_ {i} (\theta), \beta_ {i} (\theta)) \}, \tag {3.8}\mathcal {S} _ {\inf} ^ {\theta} = \{(x, i) \in \mathcal {O}: x \in (\underline {{x}}, \alpha_ {i} (\theta) ] \} \text { and } \mathcal {S} _ {\sup} ^ {\theta} = \{(x, i) \in \mathcal {O}: x \in [ \beta_ {i} (\theta), \overline {{x}}) \}. \tag {3.9}$$

Notice that, due to Assumption 3.1, $x _ { - } ( i , \theta ) < \alpha ( i , \theta )$ and $\beta ( i , \theta ) < x _ { + } ( i , \theta )$ , for any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ .

The next proposition provides regularity results about the value of the Dynkin game (3.6). The proof is in the same spirit as that of Theorem 4.3 in [27].

Proposition 3.3. For any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ we have $v ( \cdot , i ; \theta ) \in C ^ { 1 } ( \mathbb { Z } ) \cap C ^ { 2 } ( \mathbb { Z } \setminus \{ \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) \} )$ .

Proof. See Appendix A.

By Proposition 3.3 and Proposition A.1 (see also Theorem 2.1 in [46]) and using the fact that $\alpha ( i , \theta )$ and $\beta ( i , \theta )$ are finite, we are allowed to conclude that $( v ( x , i ; \theta ) , \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) ) _ { i \in \mathbb { N } }$ solves in the classical sense the following free-boundary problem:
