# 3.6.1 FLIGHT CONTROLLER

A flight controller has been designed which allows the UAV to fly to and follow a stationary or moving desired position in 3D space, and simultaneously yaw to face any desired heading. The design of the flight controller is inspired by Selby [20], Bouabdallah [7], and Sawyer [9]. To determine the required speed of each motor, Equation (10) can be rearranged as:

$$
\left[ \begin{array}{c} u _ {1} \\ u _ {2} \\ u _ {3} \\ u _ {4} \end{array} \right] = \left[ \begin{array}{c c c c} K & K & K & K \\ K L & - K L & 0 & 0 \\ 0 & 0 & K L & - K L \\ K K _ {\psi} & K K _ {\psi} & - K K _ {\psi} & - K K _ {\psi} \end{array} \right] ^ {- 1} \left[ \begin{array}{c} u _ {z} \\ u _ {\theta} \\ u _ {\phi} \\ u _ {\psi} \end{array} \right] \tag {22}
$$

where the ∙ matrix is constant and pre-computed. The controllers for $u _ { z } , u _ { \theta } , u _ { \phi }$ , and $u _ { \psi }$ will now be defined.

For each of the following controller equations,

$$e _ {n} (t) = n _ {d} (t) - n (t) \tag {23}$$

where ??(??) is the value of parameter ?? at time ?? and $n _ { d } ( t )$ is the desired value of that parameter at time ??.

As discussed in Section 2.1, the direction of the quadrotor’s thrust is in the ?? direction of the body-frame. The controller for this thrust in the body-frame is shown in equation ( ):

$$u _ {z _ {b}} (t) \stackrel {\text {def}} {=} K _ {z p} e _ {z} (t) + K _ {z i} \int_ {0} ^ {t} e _ {z} (\tau) d \tau + K _ {z d} \frac {d e _ {z} (t)}{d t} \tag {24}$$

A transformation can then be applied to calculate the thrust force in the ?? direction of the worldframe:

$$u _ {z} (t) = \frac {u _ {z _ {b}} (t)}{\cos \theta (t) \cos \phi (t)} \tag {25}$$

The controllers shown in equations ( 6) and ( 7) determine the desired force in the ?? and ?? world-frame directions:

$$u _ {x} (t) \stackrel {\text { def }} {=} K _ {x p} e _ {x} (t) + K _ {x i} \int_ {0} ^ {t} e _ {x} (\tau) d \tau + K _ {x d} \frac {d e _ {x} (t)}{d t} \tag {26}u _ {y} (t) \stackrel {\mathrm{def}} {=} K _ {y p} e _ {y} (t) + K _ {y i} \int_ {0} ^ {t} e _ {y} (\tau) d \tau + K _ {y d} \frac {d e _ {y} (t)}{d t} \tag {27}$$

A transformation can then be applied to account for the difference between world and bodyframes when the quadrotor is facing some direction other than North, i.e., $\psi \neq 0$ , allowing the desired pitch and roll values to be determined:

$$\theta_ {d} (t) = \left(u _ {x} (t) * \cos \psi (t)\right) + \left(u _ {y} (t) * \sin \psi (t)\right) \tag {28}\phi_ {d} (t) = \left(u _ {x} (t) * \sin \psi (t)\right) + \left(u _ {y} (t) * - \cos \psi (t)\right) \tag {29}$$
