$$
\begin{array}{l} \left| \left| x (t) \right| \right| \leq \left| \left| x \left(k \mid t _ {n}\right) \right| \right| + \tilde {V} _ {\max} (\hat {w}) \tag {52} \\ \leq \frac {1}{\lambda_ {\min} (Q)} \gamma (\beta (| | y _ {1} | |, n) + \delta (\tilde {V} _ {\max} (\hat {w}))) + \tilde {V} _ {\max} (\hat {w}) \\ \leq \frac {1}{\lambda_ {\min} (Q)} \gamma (\beta (\tilde {V} (\hat {w}, | | x (0) | |, p - 1), n) \\ + \delta (\tilde {V} _ {\max} (\hat {w})) + \tilde {V} _ {\max} (\hat {w}). \\ \end{array}
$$

For $t \leq t _ { 1 }$ , we can upper $| | x ( t ) | |$ by Equation (51).

If we have a finite amount of trigger times, we can apply the same arguments as before until the last trigger time. After this, we know, the predicted state converges to zero, and the real state has a difference of at most $\tilde { V } _ { \mathrm { m a x } } ( \hat { w } )$ . ■
