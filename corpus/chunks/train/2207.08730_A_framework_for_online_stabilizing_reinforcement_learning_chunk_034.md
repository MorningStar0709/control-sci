# B. Proof of Theorem 1

Let the following hold:

$$
\begin{array}{l} \forall k \in \mathbb {N} _ {0} G _ {\varepsilon} ^ {\bar {\nu}} (w _ {k}, w _ {k - 1}, \Lambda_ {U _ {k \delta}} (X _ {k \delta}), X _ {k \delta}) \leq 0 \\ \vee X _ {k \delta} \in \mathcal {B} _ {s ^ {*}}. \\ \end{array}
$$

If one assumes (26) together with $\begin{array} { r l r } { \varepsilon } & { { } < } & { \bar { \nu } \mathrm { ~ - ~ } ( 1 + } \end{array}$ $ { \frac { 1 } { 3 } } ) \mathrm { L i p } _ { \hat { J } } \sigma _ { \mathrm { m a x } } \mathrm { L i p } _ { Z }$ , then for $X _ { k \delta } \in B _ { S ^ { * } } \setminus B _ { s }$ ∗ we get

$$
\begin{array}{l} \exists \Delta <   0 \hat {J} ^ {w _ {k + 1}} (X _ {(k + 1) \delta}) - \hat {J} ^ {w _ {k}} (X _ {k \delta}) \leq \\ \leq - \delta \bar {\nu} + \operatorname{Lip} _ {\hat {J}} \operatorname{Lip} _ {f} (\bar {f} + \sigma_ {\max} \operatorname{Lip} _ {Z}) \delta^ {2} + \tag {32} \\ + \operatorname{Lip} _ {\hat {J}} \sigma_ {\max} \operatorname{Lip} _ {Z} \delta + \varepsilon <   \Delta . \\ \end{array}
$$

Now if we assume the opposite of (12) that leads to

$$
\begin{array}{l} \forall k \in \mathbb {N} _ {0} \hat {J} ^ {w _ {k}} (X _ {k \delta}) <   \hat {J} ^ {w _ {0}} (X _ {0}) + k \Delta , \\ \implies \exists k \in \mathbb {N} _ {0} \hat {J} ^ {w _ {k}} (X _ {k \delta}) <   0. \tag {33} \\ \end{array}
$$

which is in turn a contradiction, since $J ^ { w }$ is non-negative.

Thus, it can be concluded that with a sufficiently small $\delta >$ 0, the state of the system will be driven to $\boldsymbol { B } _ { s ^ { * } }$ . The time it takes to reach $B _ { s }$ ∗ can be determined in a uniform way from the decay rate and the value of Jˆ [18], [27], [29], [48].
