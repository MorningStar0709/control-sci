According to [34], given the adaptive control Lyapunov function (32), the controller $\tau _ { d } = \mu$ reads as

$$
\begin{array}{l} \mu (x, \hat {\theta}) = \frac {a _ {1}}{a _ {2}} \omega + \left(\frac {x _ {1}}{a _ {2}} k + \frac {m g}{a _ {2}} h (x _ {2}) \hat {M}\right) \tilde {x} _ {2} \\ - \frac {a _ {3} \omega}{a _ {2} x _ {1}} \hat {\tau} _ {f} + \frac {m g}{a _ {2}} h (x _ {2}) (\hat {\varrho} (1 - e ^ {- \varkappa x _ {2}}) - \hat {c} _ {3} x _ {2}), \tag {33} \\ \end{array}
$$

with $\hat { \theta } = \left[ \hat { \varrho } \hat { c } _ { 3 } \hat { M } \hat { \tau } _ { f } \right] ^ { \top }$ and a controller gain $k > 0 ,$ , and the parameter estimator dynamics (6) are given through

$$\tau (x, \theta) = \tau (x) = \Phi (x) \tilde {x} _ {2},\Phi := \left[ - \frac {a _ {3} \omega}{x _ {1} ^ {2}} \quad \frac {m g}{x _ {1}} h (x _ {2}) \left(1 - e ^ {- \varkappa x _ {2}}\right) - \frac {m g}{x _ {1}} h (x _ {2}) x _ {2} \quad \frac {m g}{x _ {1}} h (x _ {2}) \tilde {x} _ {2} \right] ^ {\top}. \tag {34}$$

Then, a Lyapunov function for system (29)–(31) with (33) and (34) reads as

$$V _ {c} (x, \tilde {\theta}) = \frac {1}{2} \tilde {x} _ {2} ^ {2} + \frac {1}{2} \tilde {\theta} ^ {\top} \Gamma^ {- 1} \tilde {\theta}, \tag {35}$$

which attains a decay rate of $\dot { V } _ { c } = k \tilde { x } _ { 2 } ^ { 2 }$

Note that the decay rate complies with Assumption 1 as it can be lower bounded uniformly.

Regarding Assumption 2 and 3, take $\varphi = \tilde { x } _ { 2 } ^ { 2 }$ for simplicity. Hence $w ^ { \# } ~ = ~ 0 . 5$ and $\mathbb { W } ^ { \prime }$ consist of values $w _ { k } ~ \geq ~ w ^ { \# }$ . Suitable weight norm bounds for W can be selected arbitrarily.

Computation of $w _ { k }$ is done via (15) for $x ^ { s } = x _ { k }$ only. The stage cost is chosen as

$$r (x, u) = 5 \tilde {x} _ {2} ^ {2} + 0. 1 u ^ {2}.$$

The sampling time is set to $\delta = 0 . 0 1$ . The parameter estimate $\hat { \theta }$ is initialized with zeros in all entries, i. e., $\hat { \theta } _ { 0 } = 0$ .

Fig. 2 depicts the tire slip of the car, converging to the target ball, and the associated control signals. As is common for sampled control Lyapunov function based stabilization (refer to, e. g., [9]), the input experiences chattering behavior.
