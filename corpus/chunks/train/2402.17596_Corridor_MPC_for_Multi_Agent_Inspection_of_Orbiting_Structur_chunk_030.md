$$
\zeta_ {\delta \boldsymbol {v}} (\boldsymbol {x}, t, \boldsymbol {u}) \geq \bar {L} _ {\delta \boldsymbol {v}} ^ {l} \Delta t + \bar {c} _ {\delta \boldsymbol {v}} ^ {l} \epsilon_ {d} + q _ {2}, \tag {18d}
$$

over a dense discretization of x and t, yielding a sevendimensional parameter space for problem (18). Note that $\mathbf { \nabla } _ { \mathbf { q } } =$ $[ q _ { 1 } , q _ { 2 } ] ^ { T }$ is only a slack variable that defines how robustly (18c)-(18d) can be satisfied. We propose to lower the dimensionality of the problem by offering a more conservative solution, based on the fact that $\pmb { f } _ { v } ( \tilde { \pmb { x } } , t ) \le \epsilon _ { f } \forall ( \tilde { \pmb { x } } , t ) \in \tilde { \mathbb { X } } \times \mathbb { I }$ . In particular, we note that both $\zeta _ { \delta r }$ and $\zeta _ { \delta \mathbf { \pmb { v } } }$ are directly functions of the x and t through $\pmb { f } _ { v } ( \pmb { x } , t )$ . By upper bounding $\pmb { f } _ { v } ( \pmb { x } , t )$ with $\epsilon _ { f } ,$ , we can assess the feasibility of the CMPC scheme by solving a new QP parameterised over $\mathbf { \nabla } _ { \mathbf { \boldsymbol { { e } } } \delta \mathbf { \boldsymbol { v } } }$ and $e _ { \delta r }$ instead of x and t [12, Eq. 11]

$$
\underset {\boldsymbol {q}, \boldsymbol {u}} {\min} - \mathbf {1} ^ {T} \boldsymbol {q} \tag {19a}
$$

$$
\boldsymbol {I} _ {2} \boldsymbol {q} \geq \mathbf {0}, \| \boldsymbol {u} \| ^ {2} \leq \epsilon_ {u} ^ {2} \tag {19b}
$$

$$
- 2 \| \tilde {\pmb {e}} _ {\delta \pmb {v}} \| ^ {2} - 2 \| \tilde {\pmb {e}} _ {\delta \pmb {r}} \| \epsilon_ {f} + 2 \tilde {\pmb {e}} _ {\delta \pmb {r}} ^ {T} \pmb {u} -
$$

$$
2 (p _ {\delta \boldsymbol {r} 0} + p _ {\delta \boldsymbol {r} 1}) (\tilde {\boldsymbol {e}} _ {\delta \boldsymbol {r}} ^ {T} \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}}) + p _ {\delta \boldsymbol {r} 0} p _ {\delta \boldsymbol {r} 1} (\epsilon_ {\delta \boldsymbol {r}} ^ {2} - \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {r}} \| ^ {2}) \tag {19c}
$$

$$
\geq \bar {L} _ {\delta \boldsymbol {r}} ^ {l} \Delta t + \bar {c} _ {\delta \boldsymbol {r}} ^ {l} \epsilon_ {d} + q _ {1}
$$

$$
- 2 \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} \| \epsilon_ {f} - 2 \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} ^ {T} \boldsymbol {u} + p _ {\delta \boldsymbol {v} 0} \left(\epsilon_ {\delta \boldsymbol {v}} ^ {2} - \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} \| ^ {2}\right) \tag {19d}
$$
