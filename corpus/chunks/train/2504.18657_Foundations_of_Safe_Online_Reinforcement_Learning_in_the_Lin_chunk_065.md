# C.6 Proof of Lemma 10

To show Equation (34), we will show that for all $t _ { 2 } \ \geq \ \lceil \log ^ { 8 } ( T ) \rceil$ , conditional on event $A _ { t _ { 2 } } \cap E _ { \mathrm { L 8 } } ( x , k , \theta , W ^ { \prime } )$ , for every $j \le t _ { 2 } - \lceil \log ^ { 8 } ( T ) \rceil + 1$ there exists some $i \in [ j : j + \lceil \log ^ { 8 } ( T ) \rceil )$ such that $\mathcal { W } _ { i }$ holds, where we recall that

$$\mathcal {W} _ {i} = \left\{\min (x _ {i}, y _ {i}) \geq \frac {D _ {U}}{a - b K} \mathrm{or} \max (x _ {i}, y _ {i}) \leq \frac {D _ {L}}{a - b K} \right\}.$$

This in turn implies Equation (34) because we can divide $[ t _ { 1 } + 1 : t _ { 2 } ]$ into $\Omega _ { T } ( \frac { | t _ { 2 } - t _ { 1 } | } { \log ^ { 8 } ( T ) } )$ disjoint intervals of the form $[ j : j + \lceil \log ^ { 8 } ( T ) \rceil )$ where each interval contains an i such that $\mathcal { W } _ { i }$ holds.

For the rest of the proof, we will prove by contradiction that conditional on event $A _ { t _ { 2 } } \cap$ $E _ { \mathrm { L 8 } } ( x , k , \theta , W ^ { \prime } )$ , for every $j \le t _ { 2 } - \lceil \log ^ { 8 } ( T ) \rceil$ there exists some $i \in [ j : j + \lceil \log ^ { 8 } ( T ) \rceil )$ such that $\mathcal { W } _ { i }$ holds. Assume that this is not the case, and there exists j such that there are no $i \in [ j : j + \lceil \log ^ { 8 } ( T ) \rceil )$ such that $\mathcal { W } _ { i }$ holds.

By definition of $\mathcal { W } _ { i } ,$ , if $\begin{array} { r } { y _ { i } \notin \left[ \frac { D _ { L } } { a - b K } - d _ { i } , \frac { D _ { U } } { a - b K } + d _ { i } \right] } \end{array}$ − d i , , then $\mathcal { W } _ { i }$ must hold. Recall that conditional on event $\begin{array} { r } { A _ { t _ { 2 } } , \ d _ { i } \leq \frac { 3 } { \log ^ { 1 0 } ( T ) } } \end{array}$ for all $i \leq t _ { 2 }$ . Therefore, conditional on event $A _ { t _ { 2 } }$ , if yi $\begin{array} { r } { \notin \left\lceil \frac { D _ { L } } { a - b K } - \frac { 3 } { \log ^ { 1 0 } ( T ) } , \frac { D _ { U } } { a - b K } + \frac { 3 } { \log ^ { 1 0 } ( T ) } \right\rceil } \end{array}$ DU + then $\mathcal { W } _ { i }$ must hold. Because we assumed that there are no $i \bar { \in } [ j : j + \lceil \log ^ { 8 } ( T ) \rceil )$ such that $\mathcal { W } _ { i }$ holds, this implies that for all $i \in [ j : j + \lceil \log ^ { 8 } ( T ) \rceil )$ ,

$$y _ {i} \in \left[ \frac {D _ {L}}{a - b K} - \frac {3}{\log^ {1 0} (T)}, \frac {D _ {U}}{a - b K} + \frac {3}{\log^ {1 0} (T)} \right]. \tag {36}$$

We also have that for sufficiently large $T _ { \ast }$ ,
