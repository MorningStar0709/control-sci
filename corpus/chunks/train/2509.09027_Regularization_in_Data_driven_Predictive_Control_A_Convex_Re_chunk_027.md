# 4.2 Causality-based norm and a Causal SPC

We next consider the following bi-level problem

$$
\min_{\substack{g,\sigma_{y}\in \Gamma ,\\ u\in \mathcal{U},y\in \mathcal{Y}}}\| y\|_{Q}^{2} + \| u\|_{R}^{2} + \lambda_{y}\| \sigma_{y}\|_{2}^{2}
$$

(24)

which considers the SPC constraint (10b) and the causality constraint (10c) for the inner identification. If $H _ { \mathrm { d } }$ has full row rank, the optimal solution $H _ { \mathrm { s c } } ^ { * }$ for the inner problem in (24) is unique [39, Lemma $2 ] .$ , which can be obtained via its optimal solution $K _ { \mathrm { s c } } ^ { * }$ , derived as

$$
K _ {\mathrm{sc}} ^ {*} = \left[ \begin{array}{c c} L _ {3 1} & L _ {3 2} ^ {*} \end{array} \right] \left[ \begin{array}{c c} L _ {1 1} & \emptyset \\ L _ {2 1} & L _ {2 2} \end{array} \right] ^ {- 1},
$$

where $L _ { i j }$ comes from the LQ factorization of $H _ { \mathrm { d } } , i . e .$ ,

$$
H _ {\mathrm{d}} = \left[ \begin{array}{c c c} L _ {1 1} & \mathbb {0} & \mathbb {0} \\ L _ {2 1} & L _ {2 2} & \mathbb {0} \\ L _ {3 1} & L _ {3 2} & L _ {3 3} \end{array} \right] \left[ \begin{array}{c} Q _ {1} \\ Q _ {2} \\ Q _ {3} \end{array} \right]
$$

and $L _ { 3 2 } ^ { * }$ is the lower-block triangular part of $L _ { 3 2 } \in$ RpL×mL. Then, the optimal $H _ { \mathrm { s c } } ^ { * }$ for the inner problem in (24) can be written as

$$
H _ {\mathrm{sc}} ^ {*} = \left[ \begin{array}{c} \operatorname{col} (U _ {\mathrm{P}}, Y _ {\mathrm{P}}) \\ U _ {\mathrm{F}} \\ K ^ {*} \operatorname{col} (U _ {\mathrm{P}}, Y _ {\mathrm{P}}, U _ {\mathrm{F}}) \end{array} \right] = \left[ \begin{array}{l l} L _ {1 1} & \emptyset \\ L _ {2 1} & L _ {2 2} \\ L _ {3 1} & L _ {3 2} ^ {*} \end{array} \right] \left[ \begin{array}{l} Q _ {1} \\ Q _ {2} \end{array} \right]. \tag {25}
$$

We can equivalently formulate (24) as

$$
\min_{\substack{g,\sigma_{y}\in \Gamma ,\\ u\in \mathcal{U},y\in \mathcal{Y}}}\| u\|_{R}^{2} + \| y\|_{Q}^{2} + \lambda_{y}\| \sigma_{y}\|_{2}^{2} \tag{26}
$$

$\mathrm { s u b j e c t ~ t o } \quad H _ { \mathrm { s c } } ^ { * } g = \mathrm { c o l } ( u _ { \mathrm { i n i } } , y _ { \mathrm { i n i } } + \sigma _ { y } , u , y ) .$

Following the discussion in Section 3.3, we next fix H to a new trajectory library and set a suitable constraint on $g$ to obtain an equivalent reformulation for (26). For this, we denote the difference between $L _ { 3 2 }$ and its lowerblock triangular part $L _ { 3 2 } ^ { * }$ as $L _ { 3 2 } ^ { \prime } : = L _ { 3 2 } - L _ { 3 2 } ^ { * }$ and the new trajectory library Hˆ is
