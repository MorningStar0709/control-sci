Remark 1. In [11], the authors introduced the concept of sufficient private information, which is a compression of private information that is sufficient for decision-making purpose. One can replace the space of private information in this model by the space of sufficient private information and the proposed algorithm of this work will still apply.

2) Transformation into Coordinator’s POMDP: Following the CI approach introduced in [1], we transform the problem E into an equivalent POMDP problem, called the coordinator’s POMDP, which can be characterized by a tuple $\bar { \mathcal { E } } = ( \bar { S } , \bar { A } , \mathcal { O } , b _ { 0 } , \bar { \mathbb { P } } , \bar { r } , \beta )$ , where $\mathcal { O } , b _ { 0 } , \beta$ are the same as in the original model; $\bar { \mathcal { S } } = \mathcal { S } \times \mathcal { M }$ is the (augmented) state space; $\begin{array} { r } { \bar { \mathcal { A } } = \prod _ { i \in \mathcal { I } } \bar { \mathcal { A } } ^ { i } } \end{array}$ is the (new) action space, where $\bar { A } ^ { i }$ is the set of mappings (called prescriptions) from $\mathcal { M } ^ { i }$ to ${ \mathcal { A } } ^ { i } ;$ $\begin{array} { r } { \bar { \mathbb { P } } : \bar { \mathcal { S } } \times \bar { \mathcal { A } } \mapsto \Delta ( \bar { \mathcal { S } } \times \mathcal { O } ) } \end{array}$ is the combined state transition and observation kernel; $\bar { r } : \bar { \mathcal { S } } \times \bar { \mathcal { A } } \mapsto$ R is the instantaneous reward function. P¯ and r¯ are respectively defined by

$$
\begin{array}{l} \bar {\mathbb {P}} (\bar {s} ^ {\prime}, o | \bar {s}, \gamma) = \mathbb {P} (\bar {s} ^ {\prime}, o | \bar {s}, a) \quad \forall \bar {s}, \bar {s} ^ {\prime} \in \bar {\mathcal {S}}, o \in \mathcal {O}, \gamma \in \bar {\mathcal {A}} \\ \bar {r} (\bar {s}, \gamma) = r (s, a) \quad \forall \bar {s} \in \bar {\mathcal {S}}, \gamma \in \bar {\mathcal {A}} \\ \end{array}
$$

where ${ \bar { s } } = ( s , m ) , a = ( a ^ { i } ) _ { i \in { \mathcal { I } } }$ and $a ^ { i } = \gamma ^ { i } ( m ^ { i } )$ .
