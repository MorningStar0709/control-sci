Selecting $\gamma ^ { * } > \lambda _ { a } + \lambda _ { b }$ and $\gamma _ { s } ^ { * } > \lambda _ { c }$ guarantees that

$$\dot {V} _ {\varepsilon} - \varepsilon_ {d} ^ {\top} \varepsilon_ {d} \leq (\lambda_ {a} + \lambda_ {b} - \gamma^ {*}) \varepsilon_ {u} ^ {\top} \varepsilon_ {u} \leq 0, \tag {36}$$

which implies that $\begin{array} { r } { V _ { \varepsilon } ( t ) - \int _ { 0 } ^ { t } \varepsilon _ { d } ^ { \top } ( \tau ) \varepsilon _ { d } ( \tau ) \mathrm { d } \tau } \end{array}$ is nonincreasing. Since $\varepsilon _ { d }$ exponentially converges to zero with time, $\begin{array} { r } { \int _ { 0 } ^ { t } \varepsilon _ { d } ^ { \top } ( \tau ) \varepsilon _ { d } ( \tau ) \mathrm { d } \tau } \end{array}$ exists and remains bounded. Consequently, $\ddot { V } _ { \varepsilon } ( t )$ is also bounded, which, together with (34), implies that $\varepsilon _ { u } ( t ) , \gamma _ { i } ( t )$ , and $\gamma _ { i s } ( t )$ are all bounded. It then follows from (33) and Assumption 2 that $\dot { \varepsilon } _ { u } ( t )$ is bounded, and so $\varepsilon _ { u } ^ { \top } ( t ) \varepsilon _ { u } ( t )$ is uniformly continuous. Meanwhile, given that $\begin{array} { r } { V _ { \varepsilon } ( t ) - \int _ { 0 } ^ { t } \varepsilon _ { d } ^ { \top } ( \tau ) \varepsilon _ { d } ( \tau ) \mathrm { d } \tau } \end{array}$ is nonincreasing and has a lower bound, it has a finite limit, i.e.,

$$\lim _ {t \rightarrow \infty} \left(V _ {\varepsilon} (t) - \int_ {0} ^ {t} \varepsilon_ {d} ^ {\top} (\tau) \varepsilon_ {d} (\tau) \mathrm{d} \tau\right) = V _ {\varepsilon^ {\prime}} ^ {\infty}.$$

Integrating both sides of (36) gives

$$\int_ {0} ^ {\infty} \left[ (\gamma^ {*} - \lambda_ {a} - \lambda_ {b}) \varepsilon_ {u} ^ {\top} (\tau) \varepsilon_ {u} (\tau) \right] \mathrm{d} \tau \leq \mathrm{V} _ {\varepsilon} (0) - \mathrm{V} _ {\varepsilon^ {\prime}} ^ {\infty},$$

based on which $\begin{array} { r } { \int _ { 0 } ^ { \infty } \varepsilon _ { u } ^ { \top } ( \tau ) \varepsilon _ { u } ( \tau ) d u } \end{array}$ d?? exists and remains finite. According to Lemma 3, we can arrive at $\begin{array} { r } { \operatorname* { l i m } _ { t \to \infty } \varepsilon _ { u } ( t ) = 0 } \end{array}$ , which completes the proof.
