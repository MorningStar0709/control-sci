# A. Parameter Calculation

We introduce two operators. The indexing operator $\boldsymbol { \mathcal { T } } ( \mathbf { x } ^ { r } , i , j )$ returns the time range during which the agent travels from node $v _ { i }$ to node $v _ { j }$ in the trajectory $\mathbf { x } ^ { r }$ . $\mathcal { T } ( \mathbf { x } ^ { r } , i , j , - 1 )$ returns the time step at which the agent reaches node $v _ { j }$ after departing node $v _ { i }$ . If the agent does not travel from node $v _ { i }$ to node $v _ { j }$ in the trajectory $\mathbf { x } ^ { r } , \mathcal { T } ( \mathbf { x } ^ { r } , i , j )$ is empty.

The capacity change operator $\Omega _ { l } ( \mathbf { x } ^ { r } , i , j )$ calculates how much the agent’s lth capacity state $c _ { l }$ depleted while traveling from node $v _ { i }$ to node $v _ { j }$ in the trajectory $\mathbf { x } ^ { r }$ .

Before beginning iteration r, the capacity depletion estimate ${ \hat { \theta } } ^ { r }$ is calculated as

$$\hat {\theta} _ {i, j, l} ^ {r} = \min _ {p \in [ 0, r - 1 ]} \Omega_ {l} (\mathbf {x} ^ {p}, i, j), \tag {21}$$

where each $\hat { \theta } _ { i , j , l } ^ { r } \in \hat { \theta } _ { i , j } ^ { r }$ ˆθri,j,l describes the smallest amount the lth capacity state depleted along edge $e _ { i , j }$ in any previous recorded iteration. The bound estimate $\hat { \Theta } ^ { r }$ is updated as

$$\hat {\Theta} _ {i, j} ^ {r} = \{\theta \in \mathbb {R} ^ {L} \mid 0 \leq \theta_ {l} \leq \hat {\theta} _ {i, j, l} ^ {r}, l \in [ 1, L ] \}. \tag {22}$$

At the start of iteration r, we define a high-level safe set

$$\mathcal {S} ^ {H, r} = \bigcup_ {p = 0} ^ {r - 1} s (\mathbf {x} ^ {H, p}, \hat {\theta} ^ {p + 1}) \tag {23}$$

where the function s is defined by Alg. 2. Given a node

Algorithm $\textbf { 2 } s : \mathbf { x } ^ { H } , \hat { \boldsymbol { \theta } }  \tilde { \mathbf { x } }$   
$\mathbf{n} \leftarrow \text{nodes in } \mathbf{x}^H, K \leftarrow \text{length}(\mathbf{n})$ $\bar{c}_{l,K} \leftarrow C_l, \bar{c}_{l,0} \leftarrow 0, \quad \forall l \in [1,L]$ $\mathbf{for} k = K - 1 : -1 : 1 \mathbf{do}$ $\bar{c}_{l,k} \leftarrow \bar{c}_{l,k+1} + \hat{\theta}_{n_k,n_{k+1},l} \quad \forall l \in [1,L]$ $\tilde{x}_k = (n_k, \bar{c}_k)$

end for

$$\tilde {x} _ {0} = (n _ {0}, \bar {c} _ {0}), \tilde {x} _ {K} = (n _ {K}, \bar {c} _ {K})$$

return $\tilde { \mathbf { x } }$
