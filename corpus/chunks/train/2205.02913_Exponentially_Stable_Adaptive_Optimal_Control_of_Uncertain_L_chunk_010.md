Notation. |.| is the absolute value, ||.|| is the Euclidean norm of a vector, $\lambda _ { m i n } \left( . \right)$ and $\lambda _ { m a x } \left( . \right)$ are the matrix minimum and maximum eigenvalues respectively, $I _ { n \times n }$ is an identity $n \times n$ matrix, $0 _ { n \times n }$ is a zero ?? × ?? matrix, $1 _ { n \times n }$ is a $. n \times n$ matrix, which is filled with ones, ??????{.} stands for a matrix determinant, $a d j \{ . \}$ represents an adjoint matrix, $L _ { \infty }$ is the space of all essentially bounded functions, ?? (.) means “is ultimately smaller than”. The following definition from1 is used throughout the paper. ??(.) stands for the ’Big ??’ notation to estimate complexity of algorithms.

Definition 1. The regressor $\varphi \left( t \right)$ is finitely exciting $( \varphi \left( t \right) \in \mathrm { F E } )$ over $\left[ t _ { r } ^ { + } ; t _ { e } \right]$ if there exist $t _ { e } \geq t _ { r } ^ { + } > 0$ and $\alpha > 0$ such that the following holds:

$$\int_ {t _ {\tau} ^ {+}} ^ {t _ {e}} \varphi (\tau) \varphi^ {\mathrm{T}} (\tau) d \tau \geq \alpha I _ {n \times n}, \tag {1}$$

where ?? is the excitation level.

The corollary of the Kalman-Yakubovich-Popov lemma is introduced $( \mathrm { s e e } ^ { 3 8 } ,$ , Theorem 9.4).

Corollary 1. For any matrix $D > 0 ,$ , a Hurwitz matrix $A \in \mathbb { R } ^ { n \times n }$ , a scalar $\mu > 0$ , a matrix $B \in \mathbb { R } ^ { n \times m }$ such that a pair (??, ??) is controllable there exists a matrix $M = M ^ { \mathrm { T } } > 0$ and matrices $N \in \mathbb { R } ^ { n \times m } , \ K \in \mathbb { R } ^ { m \times m }$ such that:

$$
\begin{array}{l} \begin{array}{l} A ^ {\mathrm{T}} M + M A = - N N ^ {\mathrm{T}} - \mu M, M B = N K, \\ K ^ {\mathrm{T}} K = D + D ^ {\mathrm{T}} \end{array} \tag {2} \\ \boldsymbol {K} ^ {\mathrm{T}} \boldsymbol {K} = \boldsymbol {D} + \boldsymbol {D} ^ {\mathrm{T}}. \\ \end{array}
$$
