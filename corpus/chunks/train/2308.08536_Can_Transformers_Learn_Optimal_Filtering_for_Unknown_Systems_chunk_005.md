# II. PROBLEM SETUP

To solve the output estimation problem for an unknown system, we will train a transformer model with data trajectories generated by the following M source systems $\{ S _ { i } \} _ { i = 1 } ^ { M }$ drawn from the same distribution $\mathcal { D } _ { s y s }$ :

$$
\mathcal {S} _ {i}: \left\{ \begin{array}{c} \mathbf {x} _ {i, t + 1} = f _ {i} (\mathbf {x} _ {i, t}) + \mathbf {w} _ {i, t + 1} \\ \mathbf {y} _ {i, t} = g _ {i} (\mathbf {x} _ {i, t}) + \mathbf {v} _ {i, t}, \end{array} \right. \tag {1}
$$

where $\mathbf { x } _ { i , t } \in \mathbb { R } ^ { n }$ and $\mathbf { y } _ { i , t } \in \mathbb { R } ^ { m }$ are the state and output at time t in the ith system; $f _ { i } ( \cdot )$ and $g _ { i } ( \cdot )$ are the state dynamics and output functions with $f _ { i } ( 0 ) = 0$ and $g _ { i } ( 0 ) = 0 ; \mathbf { w } _ { i , t } \sim$ $\mathcal { N } ( 0 , \sigma _ { \mathbf { w } , i } ^ { 2 } \mathbf { I } _ { n } )$ and $\mathbf { v } _ { i , t } \sim { \mathcal { N } } ( 0 , \sigma _ { \mathbf { v } , i } ^ { 2 } \mathbf { I } _ { m } )$ are the process and output noise, which are mutually independent for all i and t. For simplicity, it is assumed that the initial state $\mathbf { x } _ { i , 0 } = 0$ . These source systems may be obtained through pre-existing datasets or simulation environments. The target system under evaluation is denoted by $S _ { 0 }$ , which is drawn from the same distribution $\mathcal { D } _ { s y s }$ and does not have to be contained within the source systems.

We assume that there exists a constant $L _ { q } \ > \ 0$ such that for any $i , \ \mathbf { x } , \mathbf { x } ^ { \prime } , \ \| g _ { i } ( \mathbf { x } ) - g _ { i } ( \mathbf { x } ^ { \prime } ) \| \ \leq \ L _ { g } \| \mathbf { x } - \mathbf { x } ^ { \prime } \|$ . Let $\sigma _ { \mathbf { w } : = \operatorname* { m a x } _ { i } \sigma _ { \mathbf { w } , i } }$ i and $\sigma _ { \mathbf { v } : = \operatorname* { m a x } _ { i } \sigma _ { \mathbf { v } , i } }$ . Furthermore, we assume these systems satisfy the following stability condition.

Assumption 1 (Stability): Let $f _ { i } ^ { ( t ) } ( \cdot , \cdot )$ denote the t-step state evolution function such that $f _ { i } ^ { ( t ) } ( \mathbf { x } _ { i , \tau } , \mathbf { w } _ { i , \tau + 1 : \tau + t } ) \ =$ $\mathbf { x } _ { i , \tau + t }$ for all $\tau \in \mathbb { N }$ . Then, there exists constants $\rho \in [ 0 , 1 )$ and $C _ { \rho } > 0$ such that for any system i and time step t, for any $\mathbf { x } , \mathbf { x } ^ { \prime }$ and noise sequence $\mathcal { W } : = \{ \mathbf { w } _ { ( \tau ) } \} _ { \tau \in [ t ] }$ , we have
