Moreover, adaptive gains $\gamma _ { i }$ and $\gamma _ { i s }$ remain bounded, $\forall i \in N .$ .

See Section VII-B for the proof of Theorem 1.

TABLE I COMPARISON BETWEEN DETECTABLE AND STRONGLY DETECTABLE SUBSPACES.

<table><tr><td>System</td><td> $\left\{ \begin{array}{l} \dot{x} = Ax \\ y_i = C_i x \end{array} \right.$ </td><td> $\left\{ \begin{array}{l} \dot{x} = Ax + B_{-i} u_{-i} \\ y_i = C_i x \end{array} \right.$ </td></tr><tr><td>Decomposition</td><td>Detectability decomposition</td><td>Strong detectability decomposition, i.e., Lemma 2</td></tr><tr><td>Result</td><td>Detectable subspace  $\text{Im}T_{id}$ </td><td>Strongly detectable subspace  $\text{Im}T_{id}$ </td></tr><tr><td>Meaning</td><td>Functional  $T_{id}^{\top}x$  can be reconstructed from output  $y_i$ </td><td>Functional  $T_{id}^{\top}x$  can be reconstructed from output  $y_i$ </td></tr><tr><td>Assumption</td><td> $\sum_{i=1}^{N} \text{Im}T_{id} = \mathbb{R}^n \Leftrightarrow (A, \text{col}(C_i)_{i=1}^N)$  is detectable</td><td> $\sum_{i=1}^{N} \text{Im}T_{id} = \mathbb{R}^n \Leftrightarrow (A, B_{-i}, C_i)$  is collectively strongly detectable</td></tr><tr><td>Connection</td><td colspan="2"> $(A, \text{col}(C_i)_{i=1}^N)$  is detectable  $\Leftrightarrow (A, 0, C_i)$  is collectively strongly detectable</td></tr></table>
