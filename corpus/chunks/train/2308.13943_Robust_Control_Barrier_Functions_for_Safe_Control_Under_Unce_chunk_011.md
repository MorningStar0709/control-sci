where xˆ is the estimation of the state $x , { \hat { f } } _ { i }$ is the estimation of the disturbance $f _ { i } , L \in \mathbb { R } ^ { r + 1 }$ is called observer gain. Due to the special structure of $( A , C )$ , the system (9) is observable and the eigenvalues of $A - L C$ can be placed $\mathbf { a t } - \omega _ { o } ,$ , where $\omega _ { o }$ is called observer bandwidth [14].

A discrete version of ESOR-QP will be introduced for the following reasons: 1) it is a supplement to the aforementioned continuous formulation to deliver a comprehensive introduction of our proposed method; 2) it facilitates the comparison with the adaptive robust QP control (aR-QP) in [6], which also uses a discrete formulation; 3) the theoretical estimation error bound can be leveraged from our recent work [15] based on such a discrete formulation. Note that in the remainder of this section, we will show that such an estimation error bound of disturbance can also be directly used in a continuous system without discretization.

The continuous system (8) can be discretized as:

$$
\left\{ \begin{array}{l} x (k + 1) = A _ {0} x (k) + B _ {0} (b (x (k)) + a (x (k)) u _ {i} (k) + d _ {i} (k)) \\ y _ {i} (k) = C _ {0} x (k) \end{array} \right. \tag {11}
$$

where $a ( x ( k ) )$ and $b ( \boldsymbol { x } ( \boldsymbol { k } ) )$ are the discretizations of $a ( x )$ and $b ( x )$ . After including the disturbance for $y _ { i } ( k )$ as an extended state, the system (11) can be augmented into:

$$
\begin{array}{r l} \left[ \begin{array}{l} x (k + 1) \\ f _ {i} (k + 1) \end{array} \right] & = A \left[ \begin{array}{l} x (k) \\ f _ {i} (k) \end{array} \right] + B (b (\hat {x} (k)) \\ & + a (\hat {x} (k)) u _ {i} (k)) + D \Delta f _ {i} (k) \end{array} \tag {12}
y _ {i} (k) = C \left[ x (k) \quad f _ {i} (k) \right] ^ {T}$$

where $A = \left[ \begin{array} { l l } { A _ { 0 } } & { B _ { 0 } } \\ { 0 _ { 1 \times r } } & { 1 } \end{array} \right] _ { ( r + 1 ) \times ( r + 1 ) } , B , C$  A0 , and $D$ are the (r+1)×(r+1) same as those in (9), xˆ(k) is the state vector of the ESO in (13), the actual total disturbance is $f _ { i } ( k ) = b ( x ( k ) ) -$ $b ( \hat { x } ( k ) ) + ( a ( x ( k ) ) - a ( \hat { x } ( k ) ) ) u _ { i } ( k ) + d _ { i } ( k )$ rather than $d _ { i } ( k )$ , and $\Delta f _ { i } ( k ) = f _ { i } ( k + 1 ) - f _ { i } ( k )$ .

Similar to the continuous system, a discrete ESO can be used to estimate the state and the disturbance as follows:
