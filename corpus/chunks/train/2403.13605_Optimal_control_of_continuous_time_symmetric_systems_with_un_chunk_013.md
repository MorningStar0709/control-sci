# 2 Preliminaries

Consider the continuous-time linear time-invariant system

$$\frac {d}{d t} x (t) = A x (t) + B u (t), \quad t \geq 0 \tag {5}y (t) = C x (t) + D u (t)$$

where $x ( t ) \in \mathbb { R } ^ { n } , u ( t ) \in \mathbb { R } ^ { m }$ and $\ b { y } ( t ) \in \mathbb { R } ^ { m }$ . The associated transfer function matrix with (5) is denoted by $G ( s ) \in \mathbb { C } ^ { m \times m }$ , where

$$G (s) = C (s I - A) ^ {- 1} B + D \tag {6}$$

and $g ( t ) = C \exp ( A t ) B \mathbf { 1 } ( t ) + D \delta ( t )$ is the impulse response. Let $\mathcal { L } _ { p } ^ { m } ( 0 , t _ { f } )$ be the space of (equivalence classes of) all vector-valued functions $u : [ 0 , t _ { f } ] \to \mathbb { R } ^ { m }$ with Lebesgue measurable components that have bounded-p norm $\| u \| _ { p , t _ { f } }$ defined by

$$\| u \| _ {p, t _ {f}} := \left(\int_ {0} ^ {t _ {f}} \| u (t) \| _ {p} ^ {p} d t\right) ^ {1 / p} \tag {7}$$

for $t _ { f } ~ \in ~ ( 0 , + \infty ]$ and $p \in \mathbb { N } \cup \{ \infty \}$ . Throughout this paper, we do not distinguish functions that are equal almost everywhere. A linear operator K is called bounded (in the space $\mathcal { L } _ { p } ^ { m } ( 0 , t _ { f } ) )$ ), if there exists some constant $\gamma ( K ) \in ( 0 , + \infty )$ such that

$$\| \mathcal {K} u \| _ {p, t _ {f}} \leq \gamma (\mathcal {K}) \| u \| _ {p, t _ {f}}$$

for all $u \in \mathcal { L } _ { p } ^ { m } ( 0 , t _ { f } )$ . The smallest such constant $\gamma ( \boldsymbol { \cal { K } } )$ is called the operator norm which is denoted by $\| \mathcal { K } \| _ { p , t _ { f } }$ . The identity operator $\mathcal { T }$ and the time-reversal operator $\mathcal { I }$ defined as

$$\mathcal {J} u (t) := \hat {u} (t) = u (t _ {f} - t) \tag {8}$$

are both bounded linear operators with unity norms. The linear-time invariant system (5) constitutes an affine operator as $y = \mathcal { G } u + d _ { x _ { 0 } } ,$ where

$$\mathcal {G} u (t) = \int_ {0} ^ {t} g (t - \tau) u (\tau) d \tau , \quad t \in [ 0, t _ {f} ] \tag {9}$$

and $d _ { x _ { 0 } } ( t ) = C \exp ( A t ) x _ { 0 }$ is the natural response of (5) to the initial condition $x ( 0 ) = x _ { 0 }$ . The input-output gain of the system (5) on the finite time interval $t \in [ 0 , t _ { f } ]$ is defined based on the linear part (9) as follows

$$\| \mathcal {G} \| _ {p, t _ {f}} := \sup _ {u \neq 0} \frac {\| \mathcal {G} u \| _ {p , t _ {f}}}{\| u \| _ {p , t _ {f}}}. \tag {10}$$
