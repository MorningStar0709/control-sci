where the dependence on  indicated in (30) considers the implicit dependence of on it. Given an initial condition $\mu ( 0 , \cdot )$ , the MFG as a coupled system (28)–(30) can be solved at least formally, where (28) is integrated with respect to time in a backward manner, while (29) is integrated in a forward manner.

We want to solve the maximization problem (30). Assume that  is bounded and sufficiently smooth, and that the maximization is achieved by an interior solution wherein $0 < a < 1$ . Then, we have

$$\Phi (t, y) - \Phi (t, x) = F ^ {\prime} (a), \tag {31}$$

where $F ^ { \prime }$ is the derivative of  F . If $F ^ { \prime }$ is strictly increasing except at a finite number of points, then the optimal  should satisfy

$$a ^ {*} = a ^ {*} (t, x, y, \mu) = F ^ {\prime (- 1)} (\Phi (t, y) - \Phi (t, x)). \tag {32}$$

Its right-hand side is a function of the difference of the value function as a “utility” between $x , y \in \Omega$

From this perspective, considering the logit function (7) and identifying  with  , we design  as

$$F ^ {\prime (- 1)} (z) = \frac {1}{1 + e _ {\kappa} (- z \eta^ {- 1})} \text { for any } z \in \mathbb {R}. \tag {33}$$

$z = \eta \ln _ { \kappa } \left( \frac { 1 - u } { u } \right) ( 0 < u < 1 )$

$$F ^ {\prime (- 1)} \left(- \eta \ln_ {\kappa} \left(\frac {1 - u}{u}\right)\right) = u \text {or equivalently} F ^ {\prime} (u) = - \eta \ln_ {\kappa} \left(\frac {1 - u}{u}\right), \tag {34}$$

and hence

$$F (u) = - \eta \int \ln_ {\kappa} \left(\frac {1 - u}{u}\right) d u + F _ {0} \text {for any} 0 < v < 1 \tag {35}$$

with some $F _ { 0 } \in \mathbb { R }$ .ThisFis strictly increasing except at $u = 1 / 2$ ，has a line symmetry $F { \bigl ( } u { \bigr ) } = F { \bigl ( } 1 - u { \bigr ) }$ with respect to $u \in \left( 0 , 1 \right)$ , and is minimized at $u = 1 / 2$ . It is reasonable to assume that there is no cost if the representative player chooses a jump intensity independent from $t , x , y , \mu$ , which occurs if $u = 1 / 2$ . We thus assume $F { \bigl ( } 1 / 2 { \bigr ) } = 0$ in the sequel. Consequently, there will be a cost of controlling the Lévy density if a player wants to design a non-constant one deviating from the uniformly randomcase 1/2.This implies that playing the game considering the other players is costly,and hence the cost here can be interpreted as the information cost.
