# $\begin{array} { r l } { 2 . 2 } & { { } \mathcal { L } _ { \infty } ^ { m } ( 0 , t _ { f } ) } \end{array}$

All the functions in $\mathcal { L } _ { \infty } ^ { m } ( 0 , t _ { f } )$ have (essentially) bounded components. In addition, the system norm in this space admits an explicit expression in the time domain as follows

$$\left\| \mathcal {G} \right\| _ {\infty , t _ {f}} = \left\| \int_ {0} ^ {t _ {f}} | g (t) | d t \right\| _ {\infty}, \tag {12}$$

where the absolute value is understood component-wise. Identity (12) shows that $\| \mathcal { G } \| _ { \infty , t _ { f } }$ is bounded when $t _ { f }$ is finite, even if system (5) is unstable.
