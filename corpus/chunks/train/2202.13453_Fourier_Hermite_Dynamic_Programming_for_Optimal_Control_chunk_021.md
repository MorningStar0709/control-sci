where $\Sigma = \Lambda \Lambda ^ { \top }$ . Substituting to (25) then gives the following Fourier–Hermite series for $g ( y )$ with respect to $\mathcal { N } ( z \mid y , \Sigma )$ :

$$
\begin{array}{l} g (y ^ {\prime}) \approx \mathbb {E} [ g (z) ] + \mathbb {E} [ G _ {z} (z) ] ^ {\top} (y ^ {\prime} - y) \\ + \frac {1}{2} (y ^ {\prime} - y) ^ {\top} \mathbb {E} [ G _ {z z} (z) ] (y ^ {\prime} - y) - \frac {1}{2} \operatorname{tr} \left\{\mathbb {E} [ G _ {z z} (z) ] \Sigma \right\}, \tag {41} \\ \end{array}
$$

where the expectations are over $\mathcal { N } ( z \mid y , \Sigma )$ .

For the Taylor series expansion of (39) we can change variables by $\begin{array} { r } { \int g ( z ) \mathcal { N } ( z \mid y , \Sigma ) d z = \int g ( y + \xi ) \mathcal { N } ( \xi \mid 0 , \Sigma ) d \xi . } \end{array}$ which after differentiation under integral and changing back to z gives

$$\bar {G} _ {y} (y) = \int_ {\mathcal {C}} G _ {z} (z) \mathcal {N} (z \mid y, \Sigma) d z, \tag {42}\bar {G} _ {y y} (y) = \int G _ {z z} (z) \mathcal {N} (z \mid y, \Sigma) d z.$$

In the notation of (41) we have $\bar { g } ( y ) = \mathbb { E } [ g ( z ) ] , \bar { G } _ { y } ( y ) = \mathbb { E } [ G _ { z } ( z ) ]$ , and $\bar { G } _ { y y } ( y ) = \mathbb { E } [ G _ { z z } ( z ) ]$ ], and hence the Taylor series expansion of $\bar { g } ( y ^ { \prime } )$ becomes

$$
\begin{array}{l} \bar {g} (y ^ {\prime}) \approx \mathbb {E} [ g (z) ] + \mathbb {E} [ G _ {z} (z) ] ^ {\top} (y ^ {\prime} - y) \\ + \frac {1}{2} (y ^ {\prime} - y) ^ {\top} \mathbb {E} [ G _ {z z} (z) ] (y ^ {\prime} - y), \tag {43} \\ \end{array}
$$

which is the same as (41) except for the last term which is constant in $y ^ { \prime } .$

Lemma 3 (Equivalent DDP model): Consider a transformation of the problem (1) and (2), where we replace the end-condition with

$$\bar {\ell} _ {T} (x _ {T}) = \int \ell_ {T} (x _ {T} ^ {\prime}) \mathcal {N} (x _ {T} ^ {\prime} \mid x _ {T}, \Sigma_ {T}) d x _ {T} ^ {\prime} \tag {44}$$

and the cost at time step k by

$$\bar {\ell} _ {k} (x _ {k}, u _ {k}) = \bar {Q} _ {k} (x _ {k}, u _ {k}) - V _ {k + 1} (f _ {k} (x _ {k}, u _ {k})), \tag {45}$$

where the transformed action-value function is defined by

$$
\begin{array}{l} \bar {Q} _ {k} \left(x _ {k}, u _ {k}\right) = \iint \left[ \ell_ {k} \left(x _ {k} ^ {\prime}, u _ {k} ^ {\prime}\right) + V _ {k + 1} \left(f _ {k} \left(x _ {k} ^ {\prime}, u _ {k} ^ {\prime}\right)\right) \right] \tag {46} \\ \times \mathcal {N} ([ x _ {k} ^ {\prime}; u _ {k} ^ {\prime} ] \mid [ x _ {k}, u _ {k} ], \Sigma_ {k}) d u _ {k} ^ {\prime} d x _ {k} ^ {\prime}. \\ \end{array}
$$
