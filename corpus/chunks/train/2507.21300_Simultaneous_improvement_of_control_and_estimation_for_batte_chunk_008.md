It is left to show that $\begin{array} { r l } { \mathbb { E } \left\{ \left( q ^ { \top } x _ { k } - r _ { k } \right) ^ { 2 } \mid Z _ { k _ { 0 } } ^ { k } \right\} } & { { } = } \end{array}$ $\left( q ^ { \top } x _ { k | k } - r _ { k } \right) ^ { 2 } + q ^ { \top } \Sigma _ { k | k } q .$ . We first decompose the state $x _ { k }$ into $x _ { k } = x _ { k | k } + \tilde { x } _ { k }$ , where $\tilde { x } _ { k }$ is the estimation error. The quadratic term $\left( q ^ { \top } x _ { k } - r _ { k } \right) ^ { 2 } \ = \ \left( \left( q ^ { \top } ( x _ { k | k } + { \tilde { x } } _ { k } \right) - r _ { k } \right) ^ { 2 }$ , which under the conditional expectation E $\big \{ \cdot \big | Z _ { k _ { 0 } } ^ { k } \big \}$ , and after ignoring the zero mean cross-terms, leads to the righthand-side of (6).

Lemma 2. The double-summation E $\begin{array} { r } { \sum _ { i \leq i \leq n } ( x _ { k } ^ { i } - x _ { k } ^ { j } ) ^ { 2 } } \end{array}$ − x jk ) can be equivalently expressed by

$$
\mathbb{E}\sum_{\substack{1\leq i\leq n\\ i <   j\leq n}}(x_{k}^{i} - x_{k}^{j})^{2} = \sum_{\substack{1\leq i\leq n\\ i <   j\leq n}}\Big\{(\Sigma_{k|k}^{i,i} - 2\Sigma_{k|k}^{i,j} + \Sigma_{k|k}^{j,j})+
\left. \left(x _ {k \mid k} ^ {i} - x _ {k \mid k} ^ {j}\right) ^ {2} \right\}, \tag {9}$$

where $\Sigma _ { k | k } ^ { i , j }$ is the element at the $i ^ { t h }$ row and $j ^ { t h }$ column of the (positive semi-definite symmetric) covariance matrix $\Sigma _ { k | k }$ . Similarly to Lemma 1 in (8), the expectation to the left is with respect to $p ( x _ { k } )$ and the one on the right is with respect to $p ( \bar { Y _ { k _ { 0 } } ^ { k } } )$ .

Proof. Analogously to the proof of Lemma 1, applying the smoothing theorem on each element of the summation

$$\mathbb {E} \left\{\left(x _ {k} ^ {i}\right) ^ {2} - 2 x _ {k} ^ {i} x _ {k} ^ {j} + \left(x _ {k} ^ {j}\right) ^ {2}) \right\} =\mathbb {E} \left\{ \right.\mathbb {E} \left\{ \right.(x _ {k} ^ {i}) ^ {2} - 2 x _ {k} ^ {i} x _ {k} ^ {j} + (x _ {k} ^ {j}) ^ {2}\left. \right) \mid Z _ {k _ {0}} ^ {k} \left. \right\}\left. \right\},$$

then using the decomposition $x _ { k } ^ { i } = x _ { k | k } ^ { i } + \tilde { x } _ { k } ^ { i }$ , we can rewrite the inner conditional expectation in terms of the elements of $x _ { k | k }$ and $\Sigma _ { k | k }$ . □

Corollary 1. The cost function (5) can be equivalently represented by
