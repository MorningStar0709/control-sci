# A PROOF OF PROPOSITION 2.

In order to prove the proposition, it is assumed that the vector $\theta _ { A B }$ and matrices $\Phi _ { 1 1 } \left( \tau _ { \infty } \right) , \Phi _ { 2 1 } \left( \tau _ { \infty } \right)$ are known. It is also taken into consideration that the following equalities hold and $\forall A \in \mathbb { R } ^ { n \times n }$ :

$$
\begin{array}{l} c ^ {n} \det \{A \} = \det \{c A \}, c ^ {n - 1} \text {adj} \{A \} = \text {adj} \{c A \}, \\ \text {adj} \{A \}, A = A, \text {adj} \{A \} = \text {det} \{A \}, I. \end{array} \tag {A1}
a d j \{A \} A = A a d j \{A \} = d e t \{A \} I _ {n \times n}.$$

Then:

a) the definition of the matrix ?? (see (9)) is multiplied by $\Phi _ { 1 1 } \left( \tau _ { \infty } \right) a d j \left\{ \Phi _ { 1 1 } \left( \tau _ { \infty } \right) \right\}$ to obtain the static regression equation with respect to ?? :

$$
\begin{array}{c} y _ {P} = \Delta_ {P} P, \\ y _ {P} := \Phi_ {2 1} (\tau_ {\infty}) a d j \left\{\Phi_ {1 1} (\tau_ {\infty}) \right\}, \Delta_ {P} := d e t \left\{\Phi_ {1 1} (\tau_ {\infty}) \right\}, \end{array} \tag {A2}
$$

where $\Delta _ { P } \neq 0$ as the matrix $\Phi _ { 1 1 } ^ { - 1 } \left( \tau _ { \infty } \right)$ exists in accordance with Proposition 1.

b) the definition of the matrix ?? (see (9)) is multiplied by ?????? $\left\{ A ^ { \mathrm { T } } - P B R ^ { - 1 } B ^ { \mathrm { T } } \right\} \left( A ^ { \mathrm { T } } - P B R ^ { - 1 } B ^ { \mathrm { T } } \right)$ to obtain the static regression equation with respect to ?? :

$$
\begin{array}{c} y _ {V} = \Delta_ {V} V \\ y _ {V} := a d j \left\{A ^ {\mathrm{T}} - P B R ^ {- 1} B ^ {\mathrm{T}} \right\} P B _ {r}, \Delta_ {V} := d e t \left\{A ^ {\mathrm{T}} - P B R ^ {- 1} B ^ {\mathrm{T}} \right\}, \end{array} \tag {A3}
$$

where $\Delta _ { V } \neq 0$ as the matrix $\left( A ^ { \mathrm { T } } - P B R ^ { - 1 } B ^ { \mathrm { T } } \right) ^ { - 1 }$ exists in accordance with Proposition 1.

c) the equation to calculate $K _ { x }$ (see (7)) is multiplied by $\Delta _ { P }$ to obtain the static regression equation with respect to $K _ { x }$ :

$$y _ {K _ {x}} = \Delta_ {P} K _ {x},y _ {K _ {x}} := - R ^ {- 1} B ^ {\mathrm{T}} \Phi_ {2 1} \left(\tau_ {\infty}\right) a d j \left\{\Phi_ {1 1} \left(\tau_ {\infty}\right) \right\}, \tag {A4}$$

d) considering (A2) and (A3), the equation to calculate $K _ { r }$ (see (7)) is multiplied by $\Delta _ { V } \Delta _ { P } ^ { n }$ to obtain the static regression equation with respect to $K _ { r } \mathrm { i }$ :
