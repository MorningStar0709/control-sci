Problem 1. Given a network $\mathcal { G } ( t ) = ( \nu , \mathcal { E } ( t ) )$ under an $F _ { - }$ total attack at time t, let $\begin{array} { r } { \delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) = \operatorname* { m i n } _ { i \in \mathcal { L } } ( \delta _ { i } ( \mathcal { G } ( t ) ) ) } \end{array}$ be the minimum degree of normal agents at time t. We aim to find the lower bound for $\delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) \ \forall t \geq t _ { 0 }$ in which resilient consensus is guaranteed.

Addressing Problem 1 reduces the challenge of ensuring resilient consensus with F malicious agents to ensuring each normal robot maintains its number of neighbors above a threshold $F ^ { \prime } \in \mathbb { Z } _ { + }$ . The value $F ^ { \prime }$ , which depends on $F ,$ is provided in Proposition 1. Given Assumption 1, we solve:

Problem 2. Let $\mathcal { G } ( t _ { 0 } ) ~ = ~ ( \nu , \mathcal { E } ( t _ { 0 } ) )$ be a network under an F -total attack, where $\delta _ { \mathcal { L } } ( \mathcal { G } ( t _ { 0 } ) ) \geq F ^ { \prime }$ . Let $u _ { i , \mathrm { d e s } }$ be the desired controller for agent $i \in \mathcal V .$ . We design a distributed control strategy for all robots that requires only locally available information to ensure $\delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) ~ \geq ~ F ^ { \prime }$ without collisions, while minimally deviating from $u _ { i , \mathrm { d e s } } , \ \forall t \geq t _ { 0 }$ .

We propose a control strategy (given in (20)) such that when all agents implement it using local information, they together ensure that normal robots maintain at least $F ^ { \prime }$ neighbors without collisions under some assumptions, thereby achieving resilient consensus.
