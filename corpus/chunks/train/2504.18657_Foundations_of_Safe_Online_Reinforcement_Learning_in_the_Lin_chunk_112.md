# G.6 Proof of Lemma 24

proof. We will prove the contrapositive, which is that if $F _ { \mathrm { o p t } } ( \theta ) < K _ { D _ { U } } ^ { \theta }$ , then $K _ { \mathrm { o p t } } ( \theta ) < K _ { D _ { U } } ^ { \theta }$ . The first tool we need is the following result about $F _ { \mathrm { o p t } } ( \theta )$ .

Lemma 28. For any $\theta \in \Theta$ and $K \in ( \frac { a - 1 } { b } , \frac { a } { b } ]$ ,

$$\bar {J} (\theta , C _ {K} ^ {\mathrm{unc}}) = \lim _ {T \to \infty} \bar {J} (\theta , C _ {K} ^ {\mathrm{unc}}, T) = \frac {\sigma_ {\mathcal {D}} ^ {2} (q + r K ^ {2})}{1 - (a - b K) ^ {2}}.$$

This function is convex and twice differentiable for $\textstyle K \in ( { \frac { a - 1 } { b } } , { \frac { a } { b } } ]$ . Furthermore, $i f 1 - ( a -$ $b K ) > 0$ , then $\textstyle \left| { \frac { d } { d K } } { \bar { J } } ( \theta , C _ { K } ^ { \mathrm { u n c } } ) \right|$ and $\begin{array} { r } { \left| \frac { d ^ { 2 } } { d K ^ { 2 } } \bar { J } ( \theta , C _ { K } ^ { \mathrm { u n c } } ) \right| } \end{array}$ are finite and $\begin{array} { r } { \frac { d ^ { 2 } } { d K ^ { 2 } } \bar { J } ( \theta , C _ { K } ^ { \mathrm { u n c } } ) > 0 } \end{array}$ .

Finally, if $\begin{array} { r } { K = \frac { a - 1 } { b } } \end{array}$ , then $\bar { J } ( \theta , C _ { K } ^ { \mathrm { u n c } } ) = \infty .$

The proof of Lemma 28 can be found in Appendix G.9.

Lemma 28 implies that the function ${ \bar { J } } ( \theta , C _ { K } ^ { \mathrm { u n c } } )$ has a unique local minimum $( F _ { \mathrm { o p t } } ( \theta ) )$ and is convex. Therefore, if $F _ { \mathrm { o p t } } ( \theta ) < K _ { D _ { U } } ^ { \theta }$ , then for any $K ^ { \prime } > K _ { D _ { U } } ^ { \theta }$ ,

$$\bar {J} (\theta , C _ {K _ {D _ {U}} ^ {\theta}} ^ {\mathrm{unc}}) \leq \bar {J} (\theta , C _ {K ^ {\prime}} ^ {\mathrm{unc}}). \tag {86}$$

For any $K ^ { \prime } \ge K _ { D _ { U } } ^ { \theta }$ , the unconstrained and constrained controllers are the same, i.e. $C _ { K ^ { \prime } } ^ { \mathrm { u n c } } =$ $C _ { K ^ { \prime } } ^ { \theta }$ U. This is because for $K ^ { \prime } \ge K _ { D _ { I } } ^ { \theta }$ the unconstrained controller will always satisfy the state constraints because we assumed WLOG that $D _ { U } \leq | D _ { L } |$ . This implies by Equation (86) that for any $K ^ { \prime } > K _ { D _ { U } } ^ { \theta }$ ,

$$\bar {J} (\theta , C _ {K _ {D _ {U}} ^ {\theta}} ^ {\theta}) \leq \bar {J} (\theta , C _ {K ^ {\prime}} ^ {\theta}).$$

Therefore, to prove that $K _ { \mathrm { o p t } } ( \theta ) < K _ { D _ { U } } ^ { \theta }$ it is sufficient to find some $K ^ { \prime } < K _ { D _ { U } } ^ { \theta }$ such that
