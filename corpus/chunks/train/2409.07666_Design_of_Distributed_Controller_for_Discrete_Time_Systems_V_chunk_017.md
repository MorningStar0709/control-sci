$$\mathcal {K} _ {\mathrm{diag}} ^ {\infty , \gamma} = \left\{K = Z Q ^ {- 1}: \exists Z \in \mathcal {S}, \right.Q = \operatorname{blkdiag} (Q _ {1}, \dots , Q _ {N}) \succ O \text { s.t. }
\left[ \begin{array}{c c c c} - Q & * & * & * \\ (A Q + B Z) ^ {\top} & - Q & * & * \\ B _ {w} ^ {\top} & O & - \gamma I & * \\ O & C Q + D Z & D _ {w} & - \gamma I \end{array} \right] \prec O \}. \tag {25}
$$

Similarly to $\mathcal { K } _ { \mathrm { e x t } }$ and $\mathit { K } _ { S } , \mathit { H } _ { \infty }$ versions of the Extended LMI method and the clique-wise decomposition method can be derived as follows.

Lemma 8 (Distributed version of [11], [13]): Let

$$\mathcal {K} _ {\mathrm{ext}} ^ {\infty , \gamma} = \left\{K = Z G ^ {- 1}:, \exists Z \in \mathcal {S}, Q \succ O, \right.G = \operatorname{blkdiag} (G _ {1}, \dots , G _ {N}) \text { s.t. }
\begin{array}{l} \left[ \begin{array}{c c c c} - Q & * & * & * \\ (A G + B Z) ^ {\top} & Q - G - G ^ {\top} & * & * \\ B _ {w} ^ {\top} & O & - \gamma I & * \\ O & C G + D Z & D _ {w} & - \gamma I \end{array} \right] \\ \prec O \}. \tag {26} \\ \end{array}
$$

Then, K∞,γdiag $K _ { \mathrm { d i a g } } ^ { \infty , \gamma } \subset K _ { \mathrm { e x t } } ^ { \infty , \gamma } \subset K _ { \mathrm { a l l } } ^ { \infty , \gamma }$ holds.

Proof: This can be proved by following the similar procedure to Lemma 1.

Lemma 9: Let

$$\mathcal {K} _ {\mathcal {S}} ^ {\infty , \gamma} = \left\{K = (E ^ {\top} E) ^ {- 1} E ^ {\top} \tilde {Z} \tilde {Q} ^ {- 1} E: \right.\exists \tilde {Z} = \operatorname{blkdiag} \left(\tilde {Z} _ {1}, \dots \tilde {Z} _ {q}\right),\tilde {Q} = \mathrm{blkdiag} (\tilde {Q} _ {1}, \dots , \tilde {Q} _ {q}) \succ O,\rho \in \mathbb {R}, \eta > 0 \text { s.t. }\Gamma_ {\gamma} (\tilde {Q}, \tilde {Z}) + \mathrm{blkdiag} (\rho W, O _ {2 n \times 2 n}) \prec O,\left. U \tilde {Q} + \tilde {Q} U \succeq \eta U \right\} \tag {27}$$

with W , U in (9),

$$
\Gamma_ {\gamma} (\tilde {Q}, \tilde {Z}) = \left[ \begin{array}{c c c c} - \tilde {Q} & * & * & * \\ (\tilde {A} \tilde {Q} + \tilde {B} \tilde {Z}) ^ {\top} & - \tilde {Q} & * & * \\ \tilde {B} _ {v} ^ {\top} & O & - \gamma I & * \\ O & \tilde {C} \tilde {Q} + \tilde {D} \tilde {Z} & D _ {w} & - \gamma I \end{array} \right], \tag {28}
$$

and dilated system matrices

$$\tilde {A} = E A (E ^ {\top} E) ^ {- 1} E ^ {\top}, \tilde {B} = E B (E ^ {\top} E) ^ {- 1} E ^ {\top},\tilde {C} = C (E ^ {\top} E) ^ {- 1} E ^ {\top}, \tilde {D} = D (E ^ {\top} E) ^ {- 1} E ^ {\top}, \tilde {B} _ {v} = E B _ {w}.$$
