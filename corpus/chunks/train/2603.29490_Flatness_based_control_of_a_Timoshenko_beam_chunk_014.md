where $\Delta \tau = \tau _ { 2 } - \tau _ { 1 }$ and $\tilde { a } _ { 0 } ( \tau ) = \bar { a } _ { 0 } ^ { - } ( \Delta \tau - \tau ) h ( \Delta \tau - \tau ) +$ $\bar { a } _ { 0 } ^ { + } ( \tau - \Delta \tau ) h ( \tau - \Delta \tau )$ with the Heaviside function h. Since the HCF consists of two transport equations where the input acts only through the BCs, the control design in this state representation is particularly simple.

Lemma $I \ ( H C F ) { \mathrm { : } }$ There exists an invertible transformation that maps the state ${ \pmb x } ( \cdot , t )$ of (5) into the state $\eta ( \cdot , t )$ o f the HCF in (20).

Proof: The mappings between the HCF state $\eta ( \cdot , t )$ and the state $\bar { \mathbf { x } } ( \cdot , t )$ are provided in the Appendix. The states $\bar { \mathbf { x } } ( \cdot , t )$ and ${ \pmb x } ( \cdot , t )$ are related through the invertible Volterra integral transformation (9).

The HCF (20) for the Timoshenko beam does not constitute a state representation in the classical sense, as it involves positive time shifts of the input. Instead, we refer to (20) as a generalized $H C F$ with input predictions (cf. [4]). Since the system dynamics are equivalently represented by the HCF and the original state representation (5), the corresponding mapping between the associated states also involves the input (see Appendix).
