In (9), $D ( z _ { 2 } ; \theta _ { 2 } , m )$ is a positive definite matrix, constructed from deep neural networks with parameters $\pmb { \theta _ { 2 } }$ , hyperparameter $m ,$ , and input ${ z _ { 2 } }$ . We first use two independent FCNNs to generate the diagonal and off-diagonal elements of a lower triangular matrix $\mathbf { T } .$ The activation function for each FCNN is chosen as tanh(·). We then filter the output of the diagonal elements of $_ { \mathbf { T } }$ through a ReLu function to ensure that the diagonal elements of $_ { \mathbf { T } }$ are nonnegative. In addition, a positive number m is added to the diagonal elements of $\mathbf { T } .$ . Then $D ( z _ { 2 } ; \theta _ { 2 } , m )$ is constructed as $D = T ^ { \top } T$ . Through this design, D is guaranteed to be positive definite.

The structure of the tracking controller is illustrated in Fig. 1, the Jacobian ${ \dot { \phi } } , { \frac { \partial \Phi ( z _ { 1 } ) } { \partial z _ { 1 } } }$ ∂z1 are obtained through automatic differentiation.
