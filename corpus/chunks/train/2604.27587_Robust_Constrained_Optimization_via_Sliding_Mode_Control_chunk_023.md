# 4 Discussion

In this section, we apply the proposed method to various applications and compare it with existing approaches. For practical implementation, parameter tuning is essential; thus, we provide a concise guideline. First, estimate the disturbance bound ¯η and noise level $\delta .$ Then select $K _ { \operatorname* { m i n } } = 2 \sigma _ { \operatorname* { m a x } } ( J _ { h } J _ { h } ^ { \top } ) \bar { \eta }$ , and choose $\epsilon = \tau K _ { \operatorname* { m i n } } + \delta$ to suppress chattering. Verify the Jacobian condition $\underline { { \sigma } } > \delta _ { J } \lVert J _ { h } \rVert$ , and implement the smoothed control $\lambda =$ $- ( J _ { h } J _ { h } ^ { \top } ) ^ { - \dot { 1 } } [ J _ { h } \nabla \phi - \bar { K }$ sat(S/ǫ)].
