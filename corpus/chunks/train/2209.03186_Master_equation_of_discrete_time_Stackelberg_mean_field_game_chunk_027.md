# A. Backward Recursion

In this section, we define an equilibrium generating function $\theta = ( \theta _ { t } ^ { l } , \theta _ { t } ^ { f } ) _ { t \in [ T ] }$ , where $\theta _ { t } ^ { l } : \mathcal { P } ( \mathcal { X } ^ { l } ) \times \mathcal { P } ( \mathcal { X } ^ { f } ) $ $\{ \mathcal { X } ^ { l }  \mathcal { P } ( \mathcal { A } ^ { l } ) \} , \theta _ { t } ^ { f } : \mathcal { P } ( \mathcal { X } ^ { l } ) ) \times \mathcal { P } ( \mathcal { X } ^ { f } )  \{ \mathcal { X } ^ { f }  \mathcal { P } ( \mathcal { A } ^ { f } ) \}$ and a sequence of functions $( V _ { t } ^ { l } , V _ { t } ^ { f } ) _ { t \in \{ 1 , 2 , . . . T + 1 \} }$ , where $V _ { t } ^ { l } : \mathcal { P } ( \mathcal { X } ^ { l } ) \times \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { l }  \mathbb { R } , V _ { t } ^ { f } : \mathcal { P } ( \mathcal { X } ^ { l } ) \times \mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { f }  \mathbb { R }$ , in a backward recursive way, as follows.

1. Initialize $\forall \xi _ { T + 1 } \in \mathcal { P } ( \mathcal { X } ^ { l } ) , z _ { T + 1 } \in \mathcal { P } ( \mathcal { X } ^ { f } ) , x _ { T + 1 } ^ { l } \in \mathcal { X } ^ { l } , x _ { T + 1 } ^ { f } \in \mathcal { X } ^ { f }$ ,

$$V _ {T + 1} ^ {l} \left(\xi_ {T + 1}, z _ {T + 1}, x _ {T + 1} ^ {l}\right) \triangleq 0. \tag {35}V _ {T + 1} ^ {f} \left(\xi_ {T + 1}, z _ {T + 1}, x _ {T + 1} ^ {f}\right) \triangleq 0 \tag {36}$$

2. For $t = T , T - 1 , \ldots 1 , \forall \xi _ { t } \in \mathcal { P } ( \mathcal { X } ^ { l } ) , z _ { t } \in \mathcal { P } ( \mathcal { X } ^ { f } )$ , Set $\tilde { \gamma } _ { t } = \theta _ { t } [ \xi _ { t } , z _ { t } ]$ , where $\tilde { \gamma } _ { t } = ( \tilde { \gamma } _ { t } ^ { l } , \tilde { \gamma } _ { t } ^ { f } )$ is the solution of the following fixed-point equation. For a given $\xi _ { t } , z _ { t } , \gamma _ { t } ^ { l } .$ , define $\hat { B R } _ { t } ^ { f } ( \xi _ { t } , z _ { t } , \gamma _ { t } ^ { l } )$ as follows,

$$\bar {B} R _ {t} ^ {f} (\xi_ {t}, z _ {t}, \gamma_ {t} ^ {l}) := \left\{\tilde {\gamma} _ {t} ^ {f}: \forall x _ {t} ^ {f} \in \mathcal {X} ^ {f}, \tilde {\gamma} _ {t} ^ {f} (\cdot | x _ {t} ^ {f}) \in \arg \max _ {\gamma_ {t} ^ {m, j} (\cdot | x _ {t} ^ {m, j})} \right.\mathbb {E} ^ {\gamma_ {t} ^ {m, j} (\cdot | x _ {t} ^ {m, j}) \gamma_ {t} ^ {l}, z _ {t}, \xi_ {t}} \left\{R _ {t} ^ {f} (z _ {t}, X _ {t} ^ {l, f}, A _ {t}) + \delta V _ {t + 1} ^ {f} (\eta (\xi_ {t}, z _ {t}, \gamma_ {t}), \phi (\xi_ {t}, z _ {t}, \gamma_ {t} ^ {l}, \tilde {\gamma} _ {t} ^ {f}), X _ {t + 1} ^ {f}) | \xi_ {t}, z _ {t}, x _ {t} ^ {f} \right\} \}, \tag {37}$$
