Algorithm 1 Regularized MPC Algorithm.   
1: Input: A, B, l, $P_{k+l}$ , Q, R, $F_x$ , $g_x$ , $F_u$ , $g_u$ , $\mu$ 2: Initialization: $x_{0|0} = x_0$ 3: Construct $\bar{A}$ , $\bar{B}_1$ , $\bar{B}_2$ , $\bar{Q}_{k+l}$ , $\bar{R}$ , $\bar{F}$ , $\bar{g}$ 4: Construct $X_k$ , $U_k$ , and $\bar{U}_k = \text{col}\{X_k, U_k\}$ 5: Construct matrix $H_1$ and $F_{eq} = [\bar{B}_1 - \bar{B}_2]$ 6: for k = 0 to $t_f - 1$ do
7: Compute the MPC gain $K_k$ by (13).
8: Define the function $f(\bar{U}_k) = \bar{U}_k^\top H_1 \bar{U}_k$ .
9: Compute $\bar{U}_k^*$ using (12) (in-active inequalities).
10: Compute $\bar{U}_k^*$ using fmincon (active inequalities).
11: Set $u_k = u_{k|k} = [U_k^*]_1$ and $x_{k+1} = x_{k+1|k} = [X_k^*]_1$ .
12: Compute $P_{k+l-1}$ using (19) backward in time.
13: Update $\bar{Q}_k$ and $H_1$ using (15).
14: end for

algorithm, as the parameter $\mu$ tends to infinity, it effectively vanishes within the algorithm. This procedure ensures that Re-MPC converges and remains stable at each time step, regardless of the value of $\mu$ (see the next section for proof).
