From the definition of IQC and the non-negativity of the Lyapunov function V , it can be clearly seen that these three terms on the left of above inequality are all positive, and (23) becomes

$$\frac {1}{\gamma} \int_ {0} ^ {T} e ^ {\mathrm{T}} (t) e (t) d t < \gamma \int_ {0} ^ {T} d ^ {\mathrm{T}} (t) d (t) d t. \tag {24}$$

Therefore, the augmented closed-loop system (16) with input saturation is stable and performance inequality $\| e \| _ { 2 } < \gamma \| d \| _ { 2 }$ holds for all bounded, time-varying structured uncertainties $\Delta \in \Delta$ and dead-zone nonlinearity.

Furthermore, taking the inequality (20) into consideration and using Schur complement, we have the matrix inequality (18) with the existence of both pairs $( X _ { m _ { k } } , X _ { m _ { k } } ^ { - 1 } )$ for $k \in$

I[1, s] and $( \chi _ { k } , \chi _ { k } ^ { - 1 } )$ for $k \in \mathbf { I } [ s + 1 , n _ { q } ]$ as well as $( \lambda _ { l } , \lambda _ { l } ^ { - 1 } )$ for $l \in { \bf I } [ 1 , N _ { n _ { \psi } } ]$ .

Two facts need to be emphasized here. First, we obtain the scaled bounded real lemma within the framework of integral quadratic constraints, which ensures the robust stabilization of closed-loop system (16) with bounded, time-varying structured uncertainties and dead-zone nonlinearity, also minimizes the $\mathcal { L } _ { 2 }$ gain performance γ. Unlike the proof process previous literature referred to [35], namely, introducing a commutable scaling matrix set D such that $\Gamma \in D$ makes the inequality $q ^ { \mathrm { T } } \Gamma ^ { 1 / 2 } ( I - \Delta ^ { \mathrm { T } } \Delta ) \Gamma ^ { 1 / 2 } q \geq 0$ hold, while in our work the similar results about scaled bounded real lemma are derived using IQC theory. The main diagonal elements of matrix (18) keep consistent, but due to the different realization of closed-loop systems, the explicit form of (18) slightly changed. Second, to capture the input-output behavior of each timevarying structured uncertainty $\Delta \in \Delta$ , we employ a static IQC to realize sBRL (18) where scaling matrix is constructed by $\Gamma = \mathrm { d i a g } \{ X _ { m _ { 1 } } , \ldots , X _ { m _ { s } } , \chi _ { s + 1 } , \ldots , \chi _ { n _ { q } } \} \in \mathbb { S } _ { + } ^ { n _ { q } }$ .

The next objective is to derive an $\mathcal { H } _ { \infty }$ controller synthesis condition based on Lemma 3. After a congruent transformation and an inequality relaxation, the synthesis condition can be expressed as a set of LMIs, as stated below.
