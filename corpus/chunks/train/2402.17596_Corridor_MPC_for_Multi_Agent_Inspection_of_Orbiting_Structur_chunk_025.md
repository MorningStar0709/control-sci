with $\begin{array} { r c l } { \epsilon _ { f } } & { = } & { m a x \quad \| \pmb { f } _ { v } ( \tilde { \pmb { x } } , t ) \| } \\ & { } & { ( \tilde { \pmb { x } } , t ) { \in } \tilde { \mathbb { X } } { \times } \mathbb { I } } \end{array}$ .
Note that in (15) the function g was omitted as it is the identity matrix.
Next, the maximum velocity and position tracking errors are computed assuming that (2) is forced under constant acceleration a¯ during the sampling interval $[ k \Delta t , ( k + 1 ) \Delta t ]$ .
Under these assumptions, we compute the maximum velocity and position tracking errors in first-order approximation for the time interval $[ k \Delta t , ( k + 1 ) \Delta t ]$ as $\lVert \tilde { e } _ { \delta v } ( t ) - \tilde { e } _ { \delta v } ( k \Delta t ) \rVert ~ \le ~ \lVert \delta \tilde { v } ( t ) - \delta \tilde { v } ( k \Delta t ) \rVert + \lVert \delta v _ { r } ( t ) -$ $\delta { \mathbf v } _ { r } ( k \Delta t ) \| \ \le \ \bar { a } \Delta t + \bar { a } _ { r } \Delta t \ \forall t \ \in \ [ k \Delta t , ( k + 1 ) \Delta t ]$ and $\lVert \tilde { e } _ { \delta r } ( t ) - \tilde { e } _ { \delta r } ( k \Delta t ) \rVert _ { - } \le \lVert \delta \tilde { r } ( t ) - \delta \tilde { r } ( k \Delta t ) \rVert + \lVert \delta r _ { r } ( t ) -$ $\begin{array} { r l r } { \delta r _ { r } ( k \Delta t ) \parallel } & { { } \le } & { \bar { a } \frac { \Delta t ^ { 2 } } { 2 } + \epsilon _ { \delta v } \Delta t + \bar { a } _ { r } \frac { \Delta t ^ { 2 } } { 2 } + \bar { v } _ { r } \Delta t \forall t \in } \end{array}$ a¯ ∆t2 $[ k \Delta t , ( k ~ + ~ 1 ) \Delta t ]$ ;
where $\begin{array} { r l r } { \bar { v } _ { r } } & { { } \triangleq } & { \bar { m } a x \| \delta \pmb { v } _ { r } ( t ) \| } \end{array}$ and $\bar { \boldsymbol { a } } _ { r } \triangleq \operatorname* { m a x } _ { t \in \mathbb { T } } \lVert \delta \mathbf { a } _ { r } ( t ) \rVert$ (Tab.
I).
Note that we applied the formulas for linear displacement under constant acceleration to compute the maximum velocity and position error bounds.
Moreover, the constants a¯ and v¯ exist as a PRO is a periodic trajectory.
For completeness, we define $\bar { r } _ { r } \triangleq \operatorname* { m a x } _ { \mathbf { \epsilon } ^ { } } \Vert \delta \mathbf { r } _ { r } ( t ) \Vert$ .
