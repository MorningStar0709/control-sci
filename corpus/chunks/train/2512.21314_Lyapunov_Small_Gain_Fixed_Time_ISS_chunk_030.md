$$
\begin{array}{l} \left| \left[ \begin{array}{c} x (t) - h (\varphi (\theta (t))) \\ u (t) - \varphi (\theta (t)) \end{array} \right] \right| \\ \leq \beta \left(\left| \left[ \begin{array}{c} x (0) - h (\varphi (\theta (0))) \\ u (0) - \varphi (\theta (0)) \end{array} \right] \right|, t\right) + \varrho (| \varepsilon_ {0} \Pi (\theta (t)) | _ {\infty}) \\ \end{array}
$$

for all $t \geq 0 .$

![](image/0c5c2f034db31b73eee8878066ce6c92be8e5305eec328c43431c59bc228a052.jpg)

Proof: Using the change of coordinates $\tilde { x } = x - h ( \varphi ( \theta ) )$ , $\tilde { u } = u - \varphi ( \theta )$ , we arrive at the following system:

$$\dot {\tilde {x}} = f (\tilde {x} + h (\varphi (\theta)), \tilde {u} + \varphi (\theta)) - \varepsilon_ {0} \mathbf {J} _ {h} (\varphi (\theta)) \mathbf {J} _ {\varphi} (\theta) \Pi (\theta) \tag {31a}\dot {\tilde {u}} = - \mathcal {F} _ {\xi} (\mathcal {G} _ {\theta} (\tilde {x} + h (\varphi (\theta)), \tilde {u} + \varphi (\theta))) - \varepsilon_ {0} \mathbf {J} _ {\varphi} (\theta) \Pi (\theta). \tag {31b}$$

For the ${ \tilde { x } } -$ and u˜-subsystems, consider the ${ \mathcal { C } } ^ { 1 }$ FxT-ISS Lyapunov functions $V ( \tilde { x } ) = | \tilde { x } | ^ { 2 }$ and $W ( \tilde { u } ) = | \tilde { u } | ^ { 2 }$ , respectively. Let $M : = \operatorname* { s u p } _ { \theta \in \Theta } | \mathbf { J } _ { \varphi } ( \theta ) |$ |, which is finite since Θ is compact. Then, the derivative of V along the trajectories of the ${ \tilde { x } } -$ subsystem satisfies

$$\dot {V} \leq 2 \tilde {x} ^ {\top} f (\tilde {x} + h (\varphi (\theta)), \tilde {u} + \varphi (\theta)) + 2 M \ell | \varepsilon_ {0} \Pi (\theta) | | \tilde {x} |.$$

By Assumption 3, for $V ( \tilde { x } ) \geq \gamma ^ { 2 } W ( \tilde { u } )$ , we have

$$\dot {V} \leq - a | \tilde {x} | ^ {2 p} - a | \tilde {x} | ^ {2 q} + 2 M \ell | \varepsilon_ {0} \Pi (\theta) | | \tilde {x} |. \tag {32}$$

From (32), if we also have $\begin{array} { r } { V ( \tilde { x } ) \geq \frac { 1 6 M ^ { 2 } \ell ^ { 2 } } { a ^ { 2 } } | \varepsilon _ { 0 } \Pi ( \theta ) | ^ { 2 } } \end{array}$ , then

$$\dot {V} \leq - \frac {a}{2} V ^ {p} (\tilde {x}) - \frac {a}{2} V ^ {q} (\tilde {x}). \tag {33}$$

Hence, if V (˜x) ≥ max{γ2W (˜u), 16M2ℓ2a2 | $\begin{array} { r } { V ( \tilde { x } ) \ge \operatorname* { m a x } \{ \gamma ^ { 2 } W ( \tilde { u } ) , \frac { 1 6 M ^ { 2 } \ell ^ { 2 } } { a ^ { 2 } } | \varepsilon _ { 0 } \Pi ( \theta ) | ^ { 2 } \} } \end{array}$ a2 , then (33) holds. Next, consider the u˜-subsystem, and note that since
