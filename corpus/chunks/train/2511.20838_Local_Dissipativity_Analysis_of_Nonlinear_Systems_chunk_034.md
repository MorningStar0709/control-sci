# 4.2 Analysis via Optimization

The conditions for a CPA or combined quadratic and CPA function to adhere to (5) developed in Theorems 12 and 13, respectively, provide the foundation to formulate optimization problems for local dissipativity analysis. Problem 1 and Problem 2 describe a general optimization problem to determine the dissipativity of a system while simultaneously synthesizing a storage function. Problem 1 considers systems with no linear control affine terms, while Problem 2 considers those with linear control affine terms. In both problems, the generic cost function $J ( \alpha , \beta )$ is used, where α and $\beta$ can be determined based of the desired QSR characterization being analyzed. Common options are given in Table 1.

Problem 1 Consider the mapping $\mathcal { G } _ { \Omega } ( x _ { 0 } ) : \mathcal { U } _ { \Omega } \to \mathcal { L } _ { 2 \epsilon } ^ { p }$ e defined by $\mathbf { y } = \mathcal { G } _ { \Omega } ( x _ { 0 } ) \mathbf { u }$ and (1). Let $\mathbf B = \mathbf 0$ and $\mathbf { D = 0 }$ . Define the triangulation ${ \mathcal { T } } = { \dot { \{ \sigma _ { i } \} } _ { i = 1 } ^ { m \tau } }$ over $\Omega \in \Re ^ { n }$ , the CPA function $\breve { V } = \{ V _ { x } \} _ { x \in \mathbb { E } \tau } \backslash \breve { \in } \mathbb { R } ^ { \bar { n } }$ , and the variables ${ \bf L } = \{ l _ { i } \} _ { i = 1 } ^ { m _ { T } } \in \mathbb { R } , \alpha \stackrel { \cdot } { \in }$ x x∈ T R, and $\beta \in \mathbb { R }$ . Let $\Pi _ { 1 } \in \mathbb { R } ^ { m \times m }$ and $\bar { \Pi } _ { 2 } , \bar { \Pi } _ { 3 } \ \in \mathbb { R } ^ { p \times p }$ be diagonal matrices. The convex optimization problem is defined as

$$\min _ {\alpha , \beta , V, \mathbf {L}, \Pi} J (\alpha , \beta)$$

the constraints from Table 1, (14), and

$$\Pi_ {i} \succeq 0, \quad \forall i \in \mathbb {Z} _ {1} ^ {3}.$$
