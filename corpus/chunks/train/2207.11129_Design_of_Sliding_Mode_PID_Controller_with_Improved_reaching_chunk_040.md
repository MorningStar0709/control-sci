# 2.3.1 Time varying surface

Let the tracking error vector be defined as

$$e = x - x _ {d} = \left[ e \dot {e} \dots \dots e ^ {(n - 1)} \right] \tag {2.5}$$

For a single input system, time varying sliding surface in the state space $R ^ { n }$ is defined by the scalar equation $s ( x , t ) = 0$ according to the desired control bandwidth

$$s (x, t) = \left(\frac {d}{d t} + \lambda\right) ^ {(n - 1)}. e = 0 \tag {2.6}$$

where, ?? is a strictly positive constant which determines the closed loop bandwidth. For example, if $\mathrm { n } { = } 2$

$$s = \dot {e} + \lambda e \tag {2.7}$$

i.e. ?? is simply the weighted sum of the position error and the velocity error; if $\mathtt { n } = 3$

$$s = \ddot {e} + 2 \lambda \dot {e} + \lambda^ {2} e \tag {2.8}$$

Given the initial condition $x _ { d } ( 0 ) = x ( 0 )$ , the problem of tracking $\boldsymbol { x } = \boldsymbol { x } _ { d }$ is equivalent to that of remaining on the surface $s ( t )$ for all $t > 0 \mathrm { i . e }$ . the original $\mathrm { n } ^ { \mathrm { t h } }$ order tracking problem in ?? in effect be replaced by a first order stabilization problem in ??. It can also be seen that the scalar ?? represents a true measure of tracking performance as bounds on ?? can be directly translated into bounds on the tracking error vector representing a true measure of tracking performance.
