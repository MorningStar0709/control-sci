$$\bar {\theta} (t) = \left[ \bar {a} _ {1} (t), \dots , \bar {a} _ {n _ {A}} (t), \bar {b} _ {1} (t), \dots , \bar {b} _ {n _ {B}} (t) \right] \tag {12.142}$$

Since $\| F ( t ) ^ { - 1 / 2 } \tilde { \theta } ( t ) \|$ is bounded and ${ \hat { \theta } } ( t )$ converges, it is possible to compute $\beta ( t )$ as in Sect. 12.2.3 in such a way that $\beta ( t )$ converges and that the Sylvester matrix associated to $\bar { \theta } ( t )$ is nonsingular.

Let us define the modified a posteriori prediction error:

$$
\begin{array}{l} \varepsilon (t) = y (t) - \bar {\theta} (t) \phi (t - 1) \\ = y (t) + \sum_ {i = 1} ^ {n _ {A}} \bar {a} _ {i} (t) y (t - i) - \sum_ {i = 1} ^ {n _ {B}} \bar {b} _ {i} (t) u (t - d - i) \tag {12.143} \\ \end{array}
$$

Using similar notations as in (12.1) through (12.4), (12.143) can also be written as:

$$\bar {A} (t) y (t) = q ^ {- d} \bar {B} (t) u (t) + \varepsilon (t) \tag {12.144}$$

where:

$$\bar {A} (t) = 1 + \bar {a} _ {1} (t) q ^ {- 1} + \dots + \bar {a} _ {n _ {A}} (t) q ^ {- n _ {A}} \tag {12.145}\bar {B} (t) = \bar {b} _ {1} (t) q ^ {- 1} + \dots + \bar {b} _ {n _ {B}} (t) q ^ {- n _ {B}} \tag {12.146}$$

are relatively prime polynomials having coefficients that converge. A bound for $\varepsilon ( t )$ in (12.143), (12.144) which will be required later is given in the following lemma (Lozano 1992).

Lemma 12.2 ε(t) in (12.143), (12.90) satisfies the following inequality:

$$\lim _ {t \to \infty} \sup \left\{\frac {\varepsilon^ {2} (t)}{(1 + \| \phi (t - 1) \|) ^ {2}} - 3 \bar {\delta} ^ {2} (t) \beta_ {\max} ^ {2} \right\} \leq 0 \tag {12.147}$$

where $\beta _ { \mathrm { m a x } }$ is such that:

$$\beta_ {\max} = \max (1, \| \beta (t) \|) \tag {12.148}$$

Proof Introducing (12.141) into (12.143), one gets:

$$
\begin{array}{l} \varepsilon (t) = y (t) - \hat {\theta} ^ {T} (t) \phi (t - 1) - \beta^ {T} (t) F ^ {1 / 2} (t) \phi (t - 1) \\ = [ \bar {y} (t) - \hat {\theta} ^ {T} (t) \bar {\phi} (t - 1) - \beta^ {T} (t) F ^ {1 / 2} (t) \bar {\phi} (t - 1) ] \\ \end{array}
\times [ 1 + \| \phi (t - 1) \| ] \tag {12.149}$$

Taking into account that:

$$\bar {\phi} (t) = \frac {\phi (t)}{1 + \| \phi (t) \|} \tag {12.150}$$

and using (12.86) and (12.93) one obtains:

$$\frac {\varepsilon (t)}{1 + \| \phi (t - 1) \|} = [ \bar {\varepsilon} ^ {0} (t) - \beta^ {T} (t) F ^ {1 / 2} (t - 1) \bar {\phi} (t - 1) + z (t) ] \tag {12.151}$$

where:

$$
\begin{array}{l} z (t) = [ \hat {\theta} (t - 1) - \hat {\theta} (t) ] ^ {T} \bar {\phi} (t - 1) \\ + \beta^ {T} (t) [ F ^ {1 / 2} (t - 1) - F ^ {1 / 2} (t) ] \bar {\phi} (t - 1) \tag {12.152} \\ \end{array}
$$
