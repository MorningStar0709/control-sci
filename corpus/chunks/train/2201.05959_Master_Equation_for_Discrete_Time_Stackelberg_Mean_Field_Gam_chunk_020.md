# A. Algorithm for SMFE computation: Backward Recursion

In this section, we define an equilibrium generating function $\theta = ( \theta _ { t } ^ { f , i } ) _ { i \in \{ l , f \} , t \in [ T ] } .$ , where $\theta _ { t } ^ { f , i } : \mathcal { P } ( \mathcal { X } ^ { f } ) $ $\{ \mathcal { X } ^ { f , i } \  \ \mathcal { P } ( \mathcal { A } ^ { f , i } ) \}$ and a sequence of functions $( V _ { t } ^ { l } , V _ { t } ^ { f } ) _ { t \in \{ 1 , 2 , . . . T + 1 \} }$ , where $V _ { t } ^ { l } : \mathcal { P } ( \mathcal { X } ^ { f } )  \mathbb { R }$ and $V _ { t } ^ { f , i }$ : $\mathcal { P } ( \mathcal { X } ^ { f } ) \times \mathcal { X } ^ { f , i }  \mathbb { R } .$ , in a backward recursive way, as follows.

1. Initialize $\forall z _ { T + 1 } \in \mathcal { P } ( \mathcal { X } ^ { f } ) , x _ { T + 1 } ^ { f } \in \mathcal { X } ^ { f }$ ,

$$V _ {T + 1} ^ {l} (z _ {T + 1}) \triangleq 0. \tag {26}V _ {T + 1} ^ {f} (z _ {T + 1}, x _ {T + 1} ^ {f}) \triangleq 0 \tag {27}$$

2. For $t = T , T - 1 , \dots 1 , \forall z _ { t } \in \mathcal { P } ( \mathcal { X } ^ { f } )$ , let $\theta _ { t } [ z _ { t } ]$ be generated as follows. Set $\tilde { \gamma } _ { t } = \theta _ { t } [ z _ { t } ]$ , where $\tilde { \gamma } _ { t } = ( \tilde { \gamma } _ { t } ^ { l } , \tilde { \gamma } _ { t } ^ { f } )$ is the solution of the following fixed-point equation. For a given $z _ { t } , \gamma _ { t } ^ { l } .$ , define $\hat { B R } _ { t } ^ { f } ( z _ { t } , \gamma _ { t } ^ { l } )$ as follows,

$$\bar {B} R _ {t} ^ {f} (z _ {t}, \gamma_ {t} ^ {l}) = \left\{\tilde {\gamma} _ {t} ^ {f}: \forall x _ {t} ^ {f} \in \mathcal {X} ^ {f}, \tilde {\gamma} _ {t} ^ {f} (\cdot | x _ {t} ^ {f}) \in \arg \max _ {\gamma_ {t} ^ {f} (\cdot | x _ {t} ^ {f})} \right.\mathbb {E} ^ {\gamma_ {t} ^ {f} (\cdot | x _ {t} ^ {f}) \gamma_ {t} ^ {l}, z _ {t}} \left\{R _ {t} ^ {f} (z _ {t}, X _ {t} ^ {f}, A _ {t}) + \delta V _ {t + 1} ^ {f} (\phi (z _ {t}, \gamma_ {t} ^ {l}, \tilde {\gamma} _ {t} ^ {f}), X _ {t + 1} ^ {f}) | z _ {t}, x _ {t} ^ {f} \right\} \}, \tag {28}$$

where expectation in (28) is with respect to random variables $( A _ { t } , X _ { t + 1 } ^ { f } )$ through the measure $\gamma _ { t } ^ { f } ( a _ { t } ^ { f } | x _ { t } ^ { f } ) \gamma _ { t } ^ { l } ( a _ { t } ^ { l } ) ~ Q ( x _ { t + 1 } ^ { f } | x _ { t } ^ { f } , a _ { t } )$ and $\phi$ is defined in (8).

Then let for all $z _ { t } , \theta [ z _ { t } ] = ( \tilde { \gamma } _ { t } ^ { l } , \tilde { \gamma } _ { t } ^ { f } )$ is a solution of the following fixed-point equation (if it exists),

$$\tilde {\gamma} _ {t} ^ {f} \in \bar {B} R _ {t} ^ {f} (z _ {t}, \tilde {\gamma} _ {t} ^ {l}) \tag {29}$$

and
