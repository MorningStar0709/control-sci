# 2.2 Online Optimization Framework and Problem Considered

The key to our algorithm design and analysis of learning the Kalman filter is to view the problem as an online (convex) optimization problem [19, 43]. Specifically, the basic framework of online optimization can be viewed as an interaction between a decision-maker and an environment over a time horizon of length $T \in \mathbb { Z } _ { \geq 1 }$ . At each time step $t \geq 0$ , the decision-maker first chooses $x _ { t } \in \mathcal { X }$ from an action set $\mathcal { X } \subseteq \mathbb { R } ^ { n }$ and incurs a cost given by $f _ { t } ( x _ { t } )$ , where $f _ { t } ( \cdot )$ is a (convex) cost function tregret metric defined as R(T ) = PT −1t=0 ft(xt) − PT −1t=0 minx∈X ft(x), which compares the cost incurred that only becomes available after the action $\begin{array} { r } { \mathtt { R } ( T ) = \sum _ { t = 0 } ^ { T - 1 } f _ { t } ( x _ { t } ) - \sum _ { t = 0 } ^ { T - 1 } \operatorname* { m i n } _ { x \in \mathcal { X } } f _ { t } ( x ) } \end{array}$ $x _ { t } .$ The goal of the decision maker is to minimize a by the decision-maker and the optimal cost in hindsight, which can be obtained when the functions $f _ { 0 } ( \cdot ) , \ldots , f _ { T - 1 } ( \cdot )$ are given a priori. If one can show that $\mathtt { R } ( T ) = o ( T )$ , then $\mathtt { R } ( T ) / T \to 0 \mathrm { ~ a s ~ } T \to \infty$ $\mathrm { i . e . , } x _ { t }$ approaches an optimal solution $x ^ { \star } \in \arg \operatorname* { m i n } _ { x \in \mathcal { X } } f _ { t } ( x )$ as $t \to \infty$ .
