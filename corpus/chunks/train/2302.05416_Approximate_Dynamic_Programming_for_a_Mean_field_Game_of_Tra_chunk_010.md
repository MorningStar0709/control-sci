We could apply dynamic programming to each agent’s optimal control problem, but this would lead to a large system of coupled dynamic programming PDEs, and, assuming that the solution to such a system exists, this would lead to a fully centralized optimal control [4, 17]. Instead, we use mean field games.

2.2. Mean-field Game. — Suppose $N  \infty$ , so we have an infinite sequence of agents’ position-velocity pairs: $\{ ( x _ { t } ^ { i } , v _ { t } ^ { i } ) \} _ { i = 1 } ^ { \infty } \subset Y$ , with $( x _ { ( \cdot ) } ^ { i } , v _ { ( \cdot ) } ^ { i } )$ : $\Omega \times [ 0 , T ]  Y$ . From this sequence of agents, we exclude one anonymous representative agent $i ,$ and drop its indexing. This agent is subject to the reflected Itô SDEs:

$$
\begin{array}{l} d x _ {t} = v _ {t} d t; \\ d v _ {t} = (u _ {t} + w _ {t}) d t + \nu (v _ {t}) d l _ {t} + \sqrt {2 \epsilon} d W _ {t}; \tag {3} \\ \left. \left(x _ {t}, v _ {t}\right) \right| _ {t = 0} (\omega) = \left(x _ {0}, v _ {0}\right) (\omega) \in Y \forall \omega \in \Omega . \\ \end{array}
$$

Assume that

$$u \in \mathcal {U} := \operatorname{Bounded} _ {\mathcal {F}} (\Omega \times [ 0, T ]; \mathbf {U}), w \in \mathcal {W} := \operatorname{Bounded} _ {\mathcal {F}} (\Omega \times [ 0, T ]; \mathbf {W}),$$

where each of the quantities here are anonymized analogues of what appear in (1). Define:

$$\mathcal {F} (t) := \sigma ((x _ {0}, v _ {0}, W _ {s}) | s \leq t).$$

Again, assume $x _ { 0 }$ , v0 are independent of $W _ { ( \cdot ) }$ . Similarly to before, assume (again abusing notation slightly) that we have feedback control and statedependent disturbances:

$$u _ {t} = u (t, x _ {t}, v _ {t}), w _ {t} = w (t, x _ {t}, v _ {t}).$$

If $( u , w ) \in \mathrm { { P W C } } ( [ 0 , T ] ; \mathrm { L i p } ( Y ; \mathbf { U } \times \mathbf { W } ) )$ , we have a $\mathrm { L } _ { \mathcal { F } } ^ { 4 } ( \Omega ; \mathrm { C } ^ { 0 } ( [ 0 , T ] ; Y ) )$ solution $( x _ { ( \cdot ) } , v _ { ( \cdot ) } )$ [25]. Suppose the optimal control taken by all of the exogenous $\dot { \mathbf { \rho } } - \dot { \imath } \mathbf { \rho } \cdot \mathbf { \varepsilon }$ agents exists and is $u ^ { * } : \mathbb { R } _ { 0 } ^ { + } \times Y  \mathbf { U }$ and the worst-case disturbance they encounter exists and is $w ^ { * } : \mathbb { R } _ { 0 } ^ { + } \times Y  \mathbf { W }$ . We will specify for what problems these are optimal shortly in (7). These anonymized exogenous agents each follow independent copies of:
