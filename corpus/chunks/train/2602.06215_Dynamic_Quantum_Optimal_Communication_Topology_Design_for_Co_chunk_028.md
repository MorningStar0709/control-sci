In Block 3, the vector $s ^ { ( k + 1 ) }$ is obtained by minimizing L with respect to s in Rm with $( z , f , r , \lambda )$ fixed at $( z ^ { \ ' ( k + 1 ) } , f ^ { ( k + 1 ) } , r ^ { ( k + 1 ) } , \lambda ^ { ( k ) } )$ . This minimization has the closed-form solution in (25). The dependence of L on s is strictly convex and quadratic, so there exists a constant $\sigma _ { 2 } > 0$ such that

$$
\begin{array}{l} \mathcal {L} \big (z ^ {(k + 1)}, f ^ {(k + 1)}, r ^ {(k + 1)}, s ^ {(k + 1)}, \lambda^ {(k)} \big) \\ \leq \mathcal {L} \left(z ^ {(k + 1)}, f ^ {(k + 1)}, r ^ {(k + 1)}, s ^ {(k)}, \lambda^ {(k)}\right) \\ - \sigma_ {2} \| s ^ {(k + 1)} - s ^ {(k)} \| _ {2} ^ {2}. \tag {37} \\ \end{array}
$$

If we combine (35), (36) and (37), and we identify

$$\mathcal {L} ^ {k} (\lambda^ {(k)}) = \mathcal {L} \big (z ^ {(k)}, f ^ {(k)}, r ^ {(k)}, s ^ {(k)}, \lambda^ {(k)} \big),$$

and

$$\mathcal {L} ^ {k + 1} (\lambda^ {(k)}) = \mathcal {L} \big (z ^ {(k + 1)}, f ^ {(k + 1)}, r ^ {(k + 1)}, s ^ {(k + 1)}, \lambda^ {(k)} \big),$$

we obtain the bound

$$\mathcal {L} ^ {k + 1} (\lambda^ {(k)}) - \mathcal {L} ^ {k} (\lambda^ {(k)}) \leq- \sigma_ {1} \big \| (z ^ {(k + 1)}, f ^ {(k + 1)}) - (z ^ {(k)}, f ^ {(k)}) \big \| _ {2} ^ {2}- \sigma_ {2} \left\| s ^ {(k + 1)} - s ^ {(k)} \right\| _ {2} ^ {2}. \tag {38}$$

Now we substitute (32), (34) and (38) into (31). We get

$$V ^ {(k + 1)} - V ^ {(k)} \leq \rho \left\| d ^ {(k + 1)} \right\| _ {2} ^ {2}- \sigma_ {1} \left\| \left(z ^ {(k + 1)}, f ^ {(k + 1)}\right) - \left(z ^ {(k)}, f ^ {(k)}\right) \right\| _ {2} ^ {2}- \left. \sigma_ {2} \right\| s ^ {(k + 1)} - \left. s ^ {(k)} \right\| _ {2} ^ {2}+ \frac {\kappa}{2} \left(\left\| d ^ {(k + 1)} \right\| _ {2} ^ {2} - \left\| d ^ {(k)} \right\| _ {2} ^ {2}\right). \tag {39}$$

Collecting the terms with $d ^ { ( k + 1 ) }$ and $d ^ { ( k ) }$ , we can rewrite this as

$$
\begin{array}{l} V ^ {(k + 1)} - V ^ {(k)} \leq \left(\rho + \frac {\kappa}{2}\right) \left\| d ^ {(k + 1)} \right\| _ {2} ^ {2} - \frac {\kappa}{2} \left\| d ^ {(k)} \right\| _ {2} ^ {2} \\ - \sigma_ {1} \left\| \left(z ^ {(k + 1)}, f ^ {(k + 1)}\right) - \left(z ^ {(k)}, f ^ {(k)}\right) \right\| _ {2} ^ {2} \\ - \sigma_ {2} \| s ^ {(k + 1)} - s ^ {(k)} \| _ {2} ^ {2}. \tag {40} \\ \end{array}
$$
