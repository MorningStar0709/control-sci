$$\frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} \leq \frac {2 D _ {U}}{c _ {\mathrm{L} 2 5}} = O _ {T} (1). \tag {99}$$

To prove Equation (96), we will need the following result.

Lemma 32. Under Assumptions 1–3 and $\mathit { 4 } ,$ conditional on event $E _ { 2 } ^ { s } \cap \lnot E _ { \mathrm { E 5 7 } }$ and for $s u f f$ ciently large $T$ ,

$$\frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} \leq D _ {U} + \bar {w} - \epsilon^ {*} / 2.$$

The proof of Lemma 32 can be found in Appendix G.11.

Conditional on event $E _ { 2 } ^ { s } \cap \lnot E _ { \mathrm { E 5 7 } }$ , for sufficiently large $T$ , and for any $D _ { L } - \bar { w } \leq x \leq$ $\frac { D _ { U } } { a ^ { * } - b ^ { * } K _ { \mathrm { o p t } } ( \hat { \theta } _ { s } ) } .$ DU ,

$$
\begin{array}{l} (a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})) x + \bar {w} - \epsilon^ {*} / 4 \\ = D _ {U} + \left(x - \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})}\right) (a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})) + \bar {w} - \epsilon^ {*} / 2 + \epsilon^ {*} / 4 \\ \geq \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\text {opt}} (\hat {\theta} _ {s})} + \left(x - \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\text {opt}} (\hat {\theta} _ {s})}\right) (a ^ {*} - b ^ {*} K _ {\text {opt}} (\hat {\theta} _ {s})) + \epsilon^ {*} / 4 \quad \text {Lemma 32} \\ \geq \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} + \left(x - \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})}\right) (1 + \tilde {O} _ {T} (T ^ {- 1 / 4})) + \epsilon^ {*} / 4 \quad \text {Eq (98),} x \leq \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} \\ = x + \tilde {O} _ {T} \left(T ^ {- 1 / 4} \left(x - \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})}\right)\right) + \epsilon^ {*} / 4 \\ \geq x - \tilde {O} _ {T} \left(T ^ {- 1 / 4} \left(| D _ {L} | + \bar {w} + \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})}\right)\right) + \epsilon^ {*} / 4 \quad D _ {L} - \bar {w} \leq x \leq \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} \\ \geq x - \tilde {O} _ {T} \left(T ^ {- 1 / 4}\right) + \epsilon^ {*} / 4 \tag {Eq(99),Assumption4} \\ \geq x + \epsilon^ {*} / 8. \quad \text { For   sufficiently   large } T. \\ \end{array}
$$

This in turn implies the statement containing Equation (96).
