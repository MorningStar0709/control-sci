with $n \leq 0 ,$ is derivable from the axioms, thus proposition p is derivable from the axioms, whenever either the derivation tree t is itself an axiom or the propositions $p _ { 1 } , \ldots , p _ { n }$ are derivable from the axioms.

Definition A.1 (Projection/Composition relation). Relation, $\vdash \subseteq { \mathcal { R } } \times { \mathcal { G } }$ , is defined as:

$$\left\{\mathrm{p} _ {\mathrm{i}}: \text { end } \mid i \in I \right\} \vdash \text { end } \quad \left\{\mathrm{p} _ {\mathrm{i}}: \mathrm{t} \mid i \in I \right\} \vdash \mathrm{t}\frac {S , \mathsf {p} : T , \mathsf {q} : T ^ {\prime} \vdash G}{S , \mathsf {p} : \mathsf {q} _ {!} \cup . T , \mathsf {q} : \mathsf {p} _ {?} \cup . T ^ {\prime} \vdash \mathsf {p} \to \mathsf {q} : \cup . G}\frac {\forall \in I , S , \mathsf {p} : T _ {i} , \mathsf {q} : T _ {i} ^ {\prime} \vdash G _ {i}}{S , \mathsf {p} : \sum_ {i \in I} \mathsf {q} _ {!} \mathsf {U} _ {\mathsf {i}} . T _ {i} , \mathsf {q} : \sum_ {i \in I} \mathsf {p} _ {?} \mathsf {U} _ {\mathsf {i}} . T _ {i} ^ {\prime} \vdash \sum_ {i \in I} \mathsf {p} \to \mathsf {q} : \mathsf {U} _ {\mathsf {i}} . G _ {i}}\frac {\{\mathsf {p} _ {\mathrm{i}} : \mathsf {t} \mid i \in I \} , \{\mathsf {p} _ {\mathrm{j}} : T _ {j} \mid j \in J \} \vdash G \qquad \forall j \in J , T _ {j} \neq \mathsf {t}}{\{\mathsf {p} _ {\mathrm{i}} : \mathsf {e n d} \mid i \in I \} , \{\mathsf {p} _ {\mathrm{j}} : \mathsf {l o o p t .} T _ {j} \mid j \in J \} \vdash \mathsf {l o o p t .} G}$$

Projection/composition is defined inductively on the syntax of global protocols.

1) An inactive local configuration composes an inactive global protocol.   
2) Similarly, a local configuration with local loop variables composes a global loop variable.   
3) The rule for composing the message passing action requires a send action by participant p and a corresponding receive action by participant q on message type U.   
4) The rule for handling choice imposes restrictions on choice interaction. In particular, the rule allows composition for global protocols of the form $\begin{array} { r } { \sum _ { i \in I } \mathsf { p } \to \mathsf { q } : \mathsf { U } _ { \mathrm { i } } . G _ { i } , } \end{array}$

where participant p chooses from a set of messages $\mathsf { U } _ { \mathrm { i } }$ to send to a participant q. Moreover, the rule requires that all roles besides p and q implement the same local protocol in each continuation $G _ { i } ,$ as shown by requirement for local configuration S in condition:
