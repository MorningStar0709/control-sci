\*The non-causal controller provides a lower bound on the performance of any causal controller: √ $J ( K ^ { o } , d ) \le J ( K , d )$ for any causal controller K. Hence the competitive ratio must satisfy $\gamma _ { C } \geq 1$ which further implies $\sqrt { \theta } \cdot \gamma ^ { * } ( \theta ) = \gamma _ { C } \geq 1$ for $\theta = 1$ . Moreover, $\gamma ^ { * } ( \theta )$ is bounded if the plant is stabilizable and observable. Finally, if $\gamma ^ { * } ( \theta )$ is a continuous function then it will cross a value of 1 for some value of $\theta \in [ 0 , 1 ]$ .

Proof. Statements 1)-2) follow from Corollary 21.13 and Theorem 21.7 of [1] (after aligning the notation). Note that the stabilizing solution X , if it exists, is such that $\left( R + B _ { u } ^ { \top } X B _ { u } \right)$ is nonsingular. The matrix inversion lemma can be used to show:

$$A - B _ {u} K _ {x} = \left(I + B _ {u} R ^ {- 1} B _ {u} ^ {\top} X\right) ^ {- 1} \left(A - B _ {u} R ^ {- 1} S ^ {\top}\right).$$

Statement 3) follows from this equation and the assumption that $( A { - } B _ { u } R ^ { - 1 } S ^ { \top } )$ is invertible.

The special case $S ~ = ~ 0$ corresponds to $\begin{array} { r l } { C _ { e } : = } & { { } \left[ { Q } _ { 0 } ^ { 1 / 2 } \right] } \end{array}$ and $\begin{array} { r } { D _ { e u } : = \left[ \begin{array} { c } { 0 } \\ { R ^ { 1 / 2 } } \end{array} \right] } \end{array}$ . In this case the conditions (i)-(iv) of Lemma 1 simplify to: (i) R ≻ 0, (ii) $( A , B _ { u } )$ stabilizable, (iii) A is nonsingular, and (iv) $( A , Q )$ has no unobservable modes on the unit circle. The next theorem constructs the optimal non-causal controller using the stabilizing solution of the DARE (again allowing for $S \neq 0 )$ .

Theorem 1. Assume $\left( A , B _ { u } , C _ { e } , D _ { e u } \right)$ satisfy conditions $( i ) - ( i v )$ in Lemma 1. Let $X \succeq 0$ be the unique stabilizing solution of the DARE with corresponding gain $K _ { x }$ .

Define a non-causal controller $K ^ { o }$ with inputs $( x _ { t } , d _ { t } )$ and output $u _ { t } ^ { o }$ by the following update equations:

$$v _ {t} = \left(A - B _ {u} K _ {x}\right) ^ {\top} (v _ {t + 1} + X B _ {d} d _ {t}), v _ {\infty} = 0 \tag {8}u _ {t} ^ {o} = - K _ {x} x _ {t} - K _ {v} v _ {t + 1} - K _ {d} d _ {t},$$

where

$$K _ {v} := (R + B _ {u} ^ {\top} X B _ {u}) ^ {- 1} B _ {u} ^ {\top},K _ {d} := (R + B _ {u} ^ {\top} X B _ {u}) ^ {- 1} B _ {u} ^ {\top} X B _ {d}.$$

Then $J ( K ^ { o } , d ) \le J ( K , d )$ for any stabilizing controller K and disturbance $d \in \ell _ { 2 }$
