$$
\begin{array}{l} \mathcal {L} (y _ {\theta}, p _ {\sigma}) - \mathcal {L} (\bar {y}, \bar {p}) = \mathcal {L} (y _ {\theta}, p _ {\sigma}) \\ = \| \Delta y _ {\theta} + f - \lambda^ {- 1} p _ {\sigma} \| _ {L ^ {2} (\Omega)} ^ {2} + \alpha_ {i} \| \Delta p _ {\sigma} + y _ {\theta} - y _ {d} \| _ {L ^ {2} (\Omega)} ^ {2} + \alpha_ {b} ^ {y} \| y _ {\theta} \| _ {L ^ {2} (\partial \Omega)} ^ {2} + \alpha_ {b} ^ {p} \| p _ {\sigma} \| _ {L ^ {2} (\partial \Omega)} ^ {2} \\ = \| \Delta (y _ {\theta} - \bar {y}) + \lambda^ {- 1} (\bar {p} - p _ {\sigma}) \| _ {L ^ {2} (\Omega)} ^ {2} + \alpha_ {i} \| \Delta (p _ {\sigma} - \bar {p}) + y _ {\theta} - \bar {y} \| _ {L ^ {2} (\Omega)} ^ {2} + \alpha_ {b} ^ {y} \| y _ {\theta} - \bar {y} \| _ {L ^ {2} (\partial \Omega)} ^ {2} + \alpha_ {b} ^ {p} \| p _ {\sigma} - \bar {p} \| _ {L ^ {2} (\partial \Omega)} ^ {2} \\ \leq c (\boldsymbol {\alpha}, \lambda) \big (\| \bar {y} - y _ {\theta} \| _ {H ^ {2} (\Omega)} ^ {2} + \| \bar {p} - p _ {\sigma} \| _ {H ^ {2} (\Omega)} ^ {2} \big), \\ \end{array}
$$

by the trace inequality $\lVert y _ { \theta } - \bar { y } \rVert _ { L ^ { 2 } ( \partial \Omega ) } \leq c \lVert y _ { \theta } - \bar { y } \rVert _ { H ^ { 2 } ( \Omega ) }$ . With $c _ { y } = \| \bar { y } \| _ { H ^ { s } ( \Omega ) }$ , we have

$$\inf _ {y _ {\theta} \in \mathcal {Y}} \| \bar {y} - y _ {\theta} \| _ {H ^ {2} (\Omega)} ^ {2} = c _ {y} ^ {2} \inf _ {y _ {\theta} \in \mathcal {Y}} \left\| \frac {\bar {y}}{c _ {y}} - \frac {y _ {\theta}}{c _ {y}} \right\| _ {H ^ {2} (\Omega)} ^ {2} = c _ {y} ^ {2} \inf _ {y _ {\theta} \in \mathcal {Y}} \left\| \frac {\bar {y}}{c _ {y}} - y _ {\theta} \right\| _ {H ^ {2} (\Omega)} ^ {2}.$$

By Proposition 3.1, there exists an NN $y _ { \theta } \in \mathcal { Y } = \mathcal { N } _ { \rho } ( c \log ( d + s ) , c ( d , s ) \epsilon ^ { - \frac { d } { s - 2 - \mu } } , c ( d , s ) \epsilon ^ { - \frac { 9 d + 4 s } { 2 ( s - 2 - \mu ) } } )$ 9d+4s such that $\| \bar { \frac { y } { c _ { y } } } - \bar { y _ { \theta } } \| _ { H ^ { 2 } ( \Omega ) } \le \epsilon$ . Similarly, there exists an NN $p _ { \sigma } \in \mathcal { P }$ with the requisite property. Hence,

$$\mathcal {E} _ {a p p} = 3 \inf _ {(y _ {\theta}, p _ {\sigma}) \in \mathcal {Y} \times \mathcal {P}} \mathcal {L} (y _ {\theta}, p _ {\sigma}) \leq c (\boldsymbol {\alpha}, \lambda) \inf _ {(y _ {\theta}, p _ {\sigma}) \in \mathcal {Y} \times \mathcal {P}} (\| \bar {y} - y _ {\theta} \| _ {H ^ {2} (\Omega)} ^ {2} + \| \bar {p} - p _ {\sigma} \| _ {H ^ {2} (\Omega)} ^ {2}) \leq c \epsilon^ {2},$$

where c depends on $\alpha , \lambda ,$ s and the $H ^ { s } ( \Omega )$ norms of ¯y and ¯p. Then the desired assertion follows.

Remark 3.3. The approximation error Eappdepends on the regularity of y¯ and u¯. $I f f , y _ { d } \in H ^ { s - 2 } ( \Omega ) \cap L ^ { \infty } ( \Omega )$ , by standard elliptic regularity theory, we have $\bar { y } , \bar { p } \in H ^ { s } ( \Omega )$ , which satisfies the requirements of Lemma 3.3. In the unconstrained case, the optimal control u¯ has the same regularity as the adjoint state ${ \bar { p } } ,$ but in the constrained case, we have $\bar { u } \in H ^ { 1 } ( \Omega )$ only, due to the presence of the projection operator $P _ { U }$ .
