# B. Weighted Mean Subsequence Reduced Algorithm

At discrete times $t _ { p } = p \tau _ { 2 }$ for $p \in \mathbb { Z } _ { \geq 0 }$ where $\tau _ { 2 } \in \mathbb { R } _ { + }$ (not necessarily the same as $\tau _ { 1 } )$ is the update interval, robot i also shares its consensus state $y _ { i } ( t _ { p } ) \in \mathbb { R }$ with its neighbors. Robot i has $y _ { i } ( t ) = y _ { i } ( t _ { p } ) \forall t \in \overline { { [ t _ { p } , t _ { p + 1 } ) } }$ and an update protocol:

$$y _ {i} (t _ {p + 1}) = \sum_ {j \in \mathcal {B} _ {i} (t _ {p})} w _ {i j} (t _ {p}) y _ {j} (t _ {p}), \tag {6}$$

where $w _ { i j } ( t _ { p } )$ is the weight assigned to $y _ { j } ( t _ { p } )$ by agent i. We assume $\exists \beta \in ( 0 , 1 )$ such that $\forall i \in \mathcal { V } \forall p \in \mathbb { Z } _ { \geq 0 }$ ,

$\bullet w _ { i j } ( t _ { p } ) \geq \beta \ \mathrm { i f } \ j \in B _ { i } ( t _ { p } )$ , or $w _ { i j } ( t _ { p } ) = 0$ otherwise,   
$\textstyle \cdot \sum _ { j = 1 } ^ { n } w _ { i j } ( t _ { p } ) = 1$

Agents are guaranteed to achieve consensus on $y _ { i } ( t )$ through the protocol (6) if there exists a uniformly bounded sequence of contiguous time intervals where the union of $\mathcal { G } ( t )$ in each interval is connected [20, Theorem 3.10]. However, when G has misbehaving and non-misbehaving (normal) agents, the consensus guarantee no longer holds. In this paper, we consider one specific type of misbehaving agents:

Definition 1 (malicious robot). A robot $i \in \nu$ is malicious $i f i t$ sends $c _ { i } ( T _ { 1 } )$ and $y _ { i } ( T _ { 2 } )$ to all its neighbors at times $T _ { 1 }$ and $T _ { 2 } ,$ , respectively, but does not follow the nominal update protocols (5) and/or (6) at some times $T _ { 1 }$ and/or $T _ { 2 }$ .

We define sets of normal and malicious agents as $\mathcal { L } \subseteq \mathcal { V }$ and ${ \mathcal { A } } \subseteq { \mathcal { V } }$ , respectively, such that $\mathcal { L } \cap \mathcal { A } = \emptyset$ and ${ \mathcal { L } } \cup A =$ V. Various threat models describe the number of malicious robots in a network [3], and we consider the following model:

Definition 2 (F-total). A set $s \subset \nu$ is F-total if it contains at most F nodes in the graph $\left( i . e . ~ | S | \leq F \right)$ .

Then resilient consensus is defined as [3]:

Definition 3 (resilient consensus). Under an F -total attack, the normal nodes are said to achieve resilient consensus $i f$ both of the conditions hold for any initial consensus states:
