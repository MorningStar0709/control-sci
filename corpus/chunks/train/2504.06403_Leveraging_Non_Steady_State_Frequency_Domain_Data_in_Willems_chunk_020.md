$$
\tilde {\mathcal {T}} _ {L} := \left[ \begin{array}{c c c c c} \tilde {D} & 0 & \ldots & 0 & 0 \\ C \tilde {B} & \tilde {D} & \ldots & 0 & 0 \\ \vdots & \vdots & \vdots & \ddots & \vdots \\ C A ^ {L - 2} \tilde {B} & C A ^ {L - 3} \tilde {B} & \ldots & C \tilde {B} & \tilde {D} \end{array} \right],
$$

and the observability matrix $\mathcal { O } _ { L _ { 0 } + 1 } .$ , given by (4), is full column rank since $L _ { 0 } \geqslant \ell _ { \Sigma }$ . We exploit (14) in the following algorithm, which is inspired by [24, Algorithm 1].

Algorithm 1 uses the fact that there exists a singular value decomposition in which $\mathcal { U } _ { 1 }$ and $\boldsymbol { { \mathcal { U } } } _ { 2 }$ are real, which is guaranteed by the following lemma.

Lemma 3. Let $\mathcal { A } = \left\lceil \mathcal { A } _ { 0 } \quad \mathcal { A } _ { 1 } \quad \mathcal { A } _ { 1 } ^ { * } \right\rceil$ with $\mathcal { A } _ { 0 } \in \mathbb { R } ^ { n \times m _ { 0 } }$ , $\mathcal { A } _ { 1 } ~ \in ~ \mathbb { C } ^ { n \times m _ { 1 } }$ and $m \ = \ m _ { 0 } + 2 m _ { 1 }$ 1. Then, A admits a singular value decomposition $\mathcal { A } = \mathcal { U } \mathcal { S } \mathcal { V } ^ { \mathsf { H } }$ with $\mathcal { U } \in \mathbb { R } ^ { n \times n }$
