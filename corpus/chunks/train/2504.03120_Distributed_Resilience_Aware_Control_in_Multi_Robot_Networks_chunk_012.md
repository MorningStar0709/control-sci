# III. DEGREE AND RESILIENT CONSENSUS

Consider a graph $\mathcal { G } ( t ) = ( \nu , \mathcal { E } ( t ) )$ formed by n robots’ states x at time $t \in \mathbb { R } _ { > 0 }$ . In this section, we solve Problem 1 by showing that having $\begin{array} { r } { \delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) = \operatorname* { m i n } _ { i \in \mathcal { L } } ( \delta _ { i } ( \mathcal { G } ( t ) ) ) } \end{array}$ above some given threshold $F ^ { \prime } \in \mathbb { Z } _ { + } \ \forall t \geq t _ { 0 }$ guarantees resilient consensus. The main advantage of using $\delta _ { \mathcal { L } } ( \mathcal { G } ( t ) )$ is that normal robots can directly control and observe their influence on it as they navigate at time t. The condition is given in Proposition 1, but before that, we provide a useful result from [21, Property 5.23] that characterizes the $( r , s ) \cdot$ - robustness of $\mathcal { G } ( t )$ in terms of its minimum degree.

Lemma 1. Let $\mathcal { G } ( t ) = ( \nu , \mathcal { E } ( t ) )$ be a graph with the minimum degree of $\delta _ { \mathrm { m i n } } ( \mathcal { G } ( t ) )$ at time t. Then, $i f \delta _ { \operatorname* { m i n } } ( { \mathcal { G } } ( t ) ) \geq$ $\textstyle \left\lfloor { \frac { n } { 2 } } \right\rfloor \ - \ 1 , \ { \mathcal { G } } ( t )$ is $\begin{array} { r } { ( \delta _ { \mathrm { m i n } } ( \mathcal { G } ( t ) ) - \lfloor \frac { n } { 2 } \rfloor + 1 , s ) } \end{array}$ -robust, for all $1 \mathbf { \bar { \le } } s \le n \mathbf { \ } l ^ { 2 } I J .$ .

Lemma 1 provides a sufficient condition for $( r , s ) .$ - robustness of a graph $\mathcal { G } ( t )$ using its minimum degree $\delta _ { \operatorname* { m i n } } ( \mathcal G ( t ) )$ at time t. In the context of Lemma 1, the guarantees of $( r , s )$ -robustness and hence resilient consensus depend on the local connectivity of all nodes, both normal and malicious agents. We relax this dependence to derive a sufficient condition for resilient consensus based solely on the connectivity of the normal agents.

Proposition 1. Let $\mathcal { G } ( t ) ~ = ~ ( \nu , \mathcal { E } ( t ) )$ be a time-varying graph under an F -total malicious attack. $I f ~ \delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) ~ \geq$ $F ^ { \prime } = F + \textstyle \left\lfloor { \frac { n } { 2 } } \right\rfloor \forall t \geq t _ { 0 } $ , normal agents are guaranteed to achieve resilient consensus through the W-MSR algorithm with parameter F .
