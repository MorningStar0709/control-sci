where $\tilde { \mathbf { \Lambda } } ( z ) ~ = ~ \mathrm { d i a g } ( \mathbf { \Lambda } \mathbf { \Lambda } ( z ) , - \mathbf { \Lambda } \mathbf { \Lambda } ( z ) )$ . Notably, (13) differs from the kernel equations commonly used for hyperbolic systems (e.g., [6]), due to the modified coupling in (10b), which involves the boundary value ${ \bar { \pmb x } } ^ { + } ( 0 , t )$ instead of ${ \bar { \pmb x } } ^ { - } ( 0 , t )$ a s

1The kernel ${ K } _ { I } ( z , \zeta ) \in \mathbb { R } ^ { 4 \times 4 }$ of the inverse map x $( z , t ) = \bar { \pmb { x } } ( { z } , t ) +$ $\begin{array} { r l } { \int _ { 0 } ^ { z } K _ { I } ( z , \zeta ) \bar { \mathbf { x } } ( \zeta , t ) \mathrm { d } \zeta } \end{array}$ is obtained from the matrix-valued Volterra integral equation $\begin{array} { r } { \pmb { K } _ { I } ( z , \zeta ) = \pmb { K } ( z , \zeta ) + \int _ { \zeta } ^ { z } \pmb { K } ( z , \sigma ) \pmb { K } _ { I } ( \sigma , \zeta ) \mathrm { d } \sigma } \end{array}$ .

in the standard formulation. Still, it can easily be verified that well-posedness of (13) is guaranteed and the existence of a unique piecewise $C ^ { 1 } ( \mathcal { D } )$ solution $\pmb { K } ( z , \zeta )$ is ensured for $\pmb { \Lambda } \in \bar { ( } C ^ { 1 } \bar { ( } [ 0 , 1 ] ) ) ^ { 2 \times 2 }$ and $\pmb { A } \in ( C ( [ 0 , 1 ] ) ) ^ { 4 \times 4 }$ by [6].
