Lemma (see [5, 9]), we have:

$$\lim _ {t \rightarrow + \infty} \left(\frac {d}{d t} (\exp (z (t)))\right) = \lim _ {t \rightarrow + \infty} \left(\Gamma (V (\xi (t), z (t)) - \varepsilon) ^ {+}\right) = 0 \tag {107}$$

Therefore, estimate (79) holds.

Next, we show estimate (80). Let arbitrary $\epsilon > 0$ be given. Then there exists $\bar { T } > 0$ such that

$$\left| d (t) \right| \leq \operatorname * {l i m s u p} _ {\tau \rightarrow + \infty} \left(\left| d (\tau) \right|\right) + \epsilon \text { for } t \geq \bar {T} \text { a.e. }\left| \theta (t) \right| \leq \operatorname * {l i m s u p} _ {\tau \to + \infty} \left(\left| \theta (\tau) \right|\right) + \epsilon \text { for } t \geq \overline {{T}} \text { a.e. }z (t) \geq \lim _ {\tau \rightarrow + \infty} (z (\tau)) - \epsilon \text { for all } t \geq \bar {T}$$

Define $L _ { d } = \operatorname* { l i m } _ { \tau \to + \infty } \left( \left| d ( \tau ) \right| \right)$ , $L _ { \theta } = \operatorname* { l i m } _ { \tau \to + \infty } \left( \left| \theta ( \tau ) \right| \right)$ and $L _ { z } = \operatorname* { l i m } _ { \tau  + \infty } ( z ( \tau ) )$ . It follows from (90) that the following differential inequality holds for $t \geq \overline { { T } }$ a.e.:

$$\frac {d}{d t} V (\xi (t), z (t)) \leq - \rho (V (\xi (t), z (t))) + \chi (L _ {d} + \epsilon , L _ {\theta} + \epsilon , \exp (L _ {z} - \epsilon)) \tag {108}$$

We get from (84), (85) and (108) for all $t \geq \overline { { T } }$ :

$$V (\xi (t), z (t)) \leq \sigma \left(V (\xi (\bar {T}), z (\bar {T})), t - \bar {T}\right) + \gamma \left(\chi \left(L _ {d} + \epsilon , L _ {\theta} + \epsilon , \exp \left(L _ {z} - \epsilon\right)\right)\right) \tag {109}$$

Consequently, we get from estimate (109):

$$\limsup _ {t \to + \infty} \left(V (\xi (t), z (t))\right) \leq \gamma \left(\chi \left(L _ {d} + \epsilon , L _ {\theta} + \epsilon , \exp \left(L _ {z} - \epsilon\right)\right)\right) \tag {110}$$

Since (110) holds for arbitrary $\epsilon > 0$ , we obtain the required estimate (80).

The proof is complete. A

Example 9: This example conveys the essence of the adaptive robust stability studies in [6, 8]. It covers the closed-loop dynamics for a scalar linear plant. Consider the following system
