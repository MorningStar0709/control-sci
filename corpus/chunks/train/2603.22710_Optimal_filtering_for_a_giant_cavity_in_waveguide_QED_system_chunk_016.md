# 3 Optimal Filter Design

In the quantum system (20), the system state $x ( t )$ is not directly accessible, since any direct projective measurement would irreversibly disturb the quantum state. In practice, the available observation is obtained by continuously monitoring the output field through homodyne detection. This measurement mechanism fundamentally distinguishes quantum filtering from its classical counterpart, as the observation process itself is constrained by quantum measurement back-action [5]. More specifically, in the quadrature representation, the system state consists of canonically conjugate operators satisfying the commutation relation $[ q ( \dot { t } ) , \dot { p } ( t ) ] \dot { = } \dot { 1 }$ . According to the Heisenberg uncertainty principle, such non-commuting observables cannot be simultaneously measured with arbitrary precision. As a consequence, homodyne detection provides access only to a single output quadrature at a given time, while inevitably introducing additional uncertainty into the conjugate quadrature through measurement back-action. This intrinsic limitation has no classical analogue and must be explicitly taken into account in the filter design [8].

Within this framework, the objective of quantum filtering is to construct an optimal estimator that computes the conditional expectation

$$\hat {x} (t) = \mathbb {E} [ x (t) | \mathcal {F} _ {t} ^ {y} ] \tag {21}$$

based on the homodyne measurement record $\{ \mathrm { d } y ( s ) \ :$ : $0 \leq s \leq t \}$ , while respecting both the system dynamics and the fundamental constraints imposed by quantum measurement. Here, $\mathcal { F } _ { t } ^ { y }$ denotes the σ-algebra generated by the homodyne observation process. Let $e ( t ) = x ( t ) -$ ${ \hat { x } } ( t )$ be the estimation error and $\boldsymbol { P } ( t ) = \dot { \mathbb { E } } [ \boldsymbol { e } ( t ) \boldsymbol { e } ^ { \dot { T } } ( t ) ]$ the corresponding error covariance. We then have the following results.

Theorem 3. For the giant cavity system (20), the optimal filter is given as

$$
\begin{array}{l} \mathrm{d} \hat {x} (t) = A \hat {x} (t) \mathrm{d} t + A _ {d} \hat {x} (t - T) \mathrm{d} t + \mathcal {G} (t) (D _ {d} D _ {d} ^ {T}) ^ {- 1} \\ \times \Big (\mathrm{d} y (t) - C \hat {x} (t)   \mathrm{d} t - C _ {d} \hat {x} (t - T)   \mathrm{d} t \Big), \tag {22} \\ \end{array}
$$
