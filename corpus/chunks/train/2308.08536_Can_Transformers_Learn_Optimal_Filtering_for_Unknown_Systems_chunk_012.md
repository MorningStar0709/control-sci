# A. Preliminaries

Definition 1 (Covering Number): Consider a set Q and a distance metric $d ( \cdot , \cdot )$ on Q. For a set $\bar { \mathcal { Q } } _ { N } : = \{ q _ { 1 } , . . . , q _ { N } \}$ , we say it is an ϵ-cover of Q if for any $q \in \mathcal { Q } .$ , there exists $q _ { i } \in \bar { \mathcal { Q } } _ { N }$ such that $d ( q , q _ { i } ) \leq \epsilon$ . The number $\mathcal { N } ( \mathcal { Q } , d , \epsilon )$ is the smallest $N \in \mathbb N$ such that $\bar { \mathcal Q } _ { N }$ is an ϵ-cover of Q.

We will analyze the set A through its ϵ-cover. To do so, we define the following distance on A.

Definition 2 (Distance Metric): Let $\begin{array} { r l r } { \mathcal { Y } _ { T } } & { { } : = } & { \mathrm { ~ \bf ~ y _ { 0 : T } ~ } } \end{array}$ denote a trajectory of some system i under the noise sequence $\left\{ { \bf w } _ { 0 : T } , { \bf v } _ { 0 : T } \right\}$ . For any two transformers $\begin{array} { r } { \mathrm { T F } , \mathrm { T F } ^ { \prime } \in \ A . } \end{array}$ , define the distance metric $\mu ( \mathrm { T F } , \mathrm { T F } ^ { \prime } ) : = $ $\begin{array} { r } { \operatorname* { s u p } _ { t \leq T } \operatorname* { s u p } _ { \mathbf { w } _ { 0 : t } , \mathbf { v } _ { 0 : t } } \frac { \Vert \mathrm { T r } \left\{ \mathbf { y } _ { t - 1 } \right\} - \mathrm { T r } ^ { \prime } \left\{ \mathbf { y } _ { t - 1 } \right\} \Vert } { \operatorname* { m a x } _ { \tau < t } \left. \mathbf { w } _ { \tau - 1 } \right. + \operatorname* { m a x } _ { \tau < t } \left. \mathbf { v } _ { \tau - 1 } \right. } } \end{array}$ .

Though this metric is regarding the transformers $\mathrm { T E } , \mathrm { T E } ^ { \prime }$ in the transformer space A, it can be viewed as a metric between their respective parameters $\theta , \theta ^ { \prime }$ in the parameter set Θ. When the noise is bounded, the distance can also be defined without using the normalization factor in the denominator, e.g. [6]. Next, we quantify the robustness of the transformer in terms of how much the prediction changes with respect to the perturbations of its prompt. This will help us establish generalization bounds for the transformer trained in (3).
