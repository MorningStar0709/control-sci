# B. Fixed-time control based on GPs

A FxT-SM control design approach utilizing GP models is introduced. The control law is formulated directly using the development given in the previous section.

Initially, the model estimates ${ \hat { f } } _ { i }$ are calculated using precollected data before the control design phase. The relevant formulations are provided in (18). Then, the control law (7) is modified by: We propose the following control law:

$$
\begin{array}{l} u _ {i} (z) = - g _ {i} (x) ^ {- 1} \left(\hat {f} _ {i} (x) + \alpha_ {i 1} \exp \left(z _ {i} (t) ^ {2}\right) \left\lfloor z _ {i} (t) \right\rceil^ {\frac {p _ {i}}{q _ {i}}} \right. \tag {19} \\ - \dot {x} _ {i d} (t) + \alpha_ {i 2} \exp (s _ {i} (z _ {i}) ^ {2}) \left\lfloor s _ {i} (z _ {i}) \right\rceil^ {0}) \\ \end{array}
$$

with $\alpha _ { i 2 } > \frac { 2 } { \sqrt { \pi } } \bar { d } _ { i } > 0$ . We need on the following Lemma to complete Theorem 5.

Lemma 4: Consider a Lyapunov function $\begin{array} { r } { V ( z ) ~ = ~ \frac { 1 } { 2 } z ^ { 2 } } \end{array}$ and its derivative satisfying $\dot { V } ( z ) \leq | z | A \mathrm { e } ^ { z ^ { 2 } }$ , where $A < 0$ . Then, the variable z is fixed-time stable and settling time satisfying $T ( z ) < T _ { m a x }$ with, $\begin{array} { r } { T _ { m a x } = - \frac { 2 \sqrt { 2 } + 1 } { 2 A } } \end{array}$ ,

Proof: The proof contains two cases.

Case 1: when $| z | > 1$ , we have $\mathrm { e } ^ { z ^ { 2 } } > z ^ { 2 }$ , therefore,

$$\dot {V} (z) \leq A z ^ {3} = 2. 2 ^ {\frac {1}{2}} A V ^ {\frac {3}{2}},$$

Applying Lemma 1, the settling time is,

$$T _ {1} (z) \leq - \frac {1}{2 A},$$

Case 2: when $| z | \le 1$ , we have min $\ : ( \mathrm { e } ^ { z ^ { 2 } } ) \geq 1 \ :$ , therefore,

$$\dot {V} (z) \leq A | z | = 2 ^ {\frac {1}{2}} A V ^ {\frac {1}{2}},$$

Applying Lemma 1, the settling time in this case is,

$$T _ {2} (z) \leq - \frac {\sqrt {2}}{A},$$

Then, the ultimate settling time is $T ( z ) = T _ { 1 } ( z ) + T _ { 2 } ( z ) \leq$ $T _ { \mathrm { m a x } }$ , which completes the proof.

Theorem 5: Under both Assumptions 1 and 2 and if $\alpha _ { i 2 } >$ $\bar { d } _ { i } + | \Delta f _ { i } ( x ) |$ , the global closed-loop system (4)–(19) is FxT stable with a bounded settling time satisfying

$$T _ {\max} = T (s) + T (z), \tag {20}$$

where

$$T (z) \leq \max \left(T _ {1} (z _ {1}), T _ {2} (z _ {2}), \dots , T _ {n} (z _ {n})\right). \tag {21}$$

with

$$T _ {i} (z _ {i}) \leq \frac {2}{\alpha_ {i 1}}. \frac {1}{1 - \frac {p _ {i} ^ {2}}{q _ {i} ^ {2}}} \tag {22}$$

and

$$T (s) \leq \max \left(T _ {1} (s _ {1}), T _ {2} (s _ {2}), \dots , T _ {n} (s _ {n})\right), \tag {23}$$

with
