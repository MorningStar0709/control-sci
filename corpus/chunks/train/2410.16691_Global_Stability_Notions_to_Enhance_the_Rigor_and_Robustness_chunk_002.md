# Table of Contents

Notation.. 2

1) Introduction... /   
2) Global Stability Notions for Input-Free Systems .....   
3) Proving Stability Properties for Input-Free Systems .... . 7   
4) The Need of Output Stability Properties for Disturbance-Free Systems in Adaptive Control...... 15   
5) Global Stability Notions for Systems with (Disturbance) Inputs . . 17   
6) Proving Stability Properties for Systems with (Disturbance) Inputs.. . 19   
7) The Need of Output Stability Properties for Systems with Disturbances in Adaptive Control .... 38   
Notes and Comments ... . 46   
Acknowledgements.... .. 46   
References... . 47

Notation. Throughout this document, we adopt the following notation.

  for all $x \in \mathbb { R }$

 Let $S \subseteq \mathbb { R } ^ { n }$ be an open set and let $A \subseteq \mathbb { R } ^ { n }$ be a set that satisfies $S \subseteq A \subseteq c l ( S )$ , where $c l ( S )$ is the closure of $S \subseteq \mathbb { R } ^ { n }$ . By $C ^ { 0 } ( A ; \Omega )$ , we denote the class of continuous functions on , which take values in $\Omega \subseteq \mathbb { R } ^ { m } . \mathbf { B y } \ C ^ { k } ( A ; \Omega )$ , where $k \geq 1$ is an integer, we denote the class of functions on $A \subseteq \Re ^ { n }$ , which takes values in $\Omega \subseteq \mathbb { R } ^ { m }$ and has continuous derivatives of order  . In other words, the functions of class $C ^ { k } ( A ; \Omega )$ are the functions which have continuous derivatives of order  in $S = \operatorname { i n t } ( A )$ that can be continued continuously to all points in $\partial S \cap A$ . When $\Omega = \mathbb { R }$ then we write $C ^ { 0 } ( A ) { \mathrm { ~ o r ~ } } C ^ { k } ( A )$

By  we denote the class of strictly increasing $C ^ { 0 }$ functions $a : \mathbb { R } _ { + } \to \mathbb { R } _ { + }$ with  . By $K _ { \infty }$ we denote the class of strictly increasing $C ^ { 0 }$ functions $a : \mathbb { R } _ { + } \to \mathbb { R }$ + with $a ( 0 ) = 0$ and lim $a ( s ) = + \infty$ . By we denote the set of all continuous functions $\sigma : \mathbb { R } _ { + } \times \mathbb { R } _ { + } \to \mathbb { R }$ with the s-→+8

properties: (i) for each $t \geq 0$ the mapping $\sigma ( \cdot , t )$ is of class $K ; ( \mathrm { i i } )$ for each $s \geq 0$ , the mapping $\sigma ( s , \cdot )$ is non-increasing with $\operatorname* { l i m } _ { s \to + \infty } \sigma ( s , t ) = 0$ .

For a vector $x \in \mathbb { R } ^ { n } , \left| x \right|$ denotes its Euclidean norm and $x ^ { \prime }$ denotes its transpose.
