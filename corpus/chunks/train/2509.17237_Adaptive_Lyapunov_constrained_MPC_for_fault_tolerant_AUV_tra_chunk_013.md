$$\mathbf {s} ^ {\top} (\dot {\mathbf {M}} ^ {*} (\psi) - 2 \mathbf {C} ^ {*} (\mathbf {v}, \psi)) \mathbf {s} = 0 \quad \forall \mathbf {v}, \psi , \mathbf {s} \tag {12}$$

Using standard Euler–Lagrange identities, the auxiliary generalized-force law [27], [34], [35]

$$\boldsymbol {\tau} _ {b} = \boldsymbol {M} \dot {\boldsymbol {v}} _ {r} + \boldsymbol {C} (\boldsymbol {v}) \boldsymbol {v} _ {r} + \boldsymbol {D} (\boldsymbol {v}) \boldsymbol {v} _ {r} - \boldsymbol {R} ^ {\top} (\psi) \left(\boldsymbol {K} _ {p} \tilde {\boldsymbol {\eta}} + \boldsymbol {K} _ {d} \boldsymbol {s}\right), \tag {13}$$

with $K _ { d } = K _ { d } ^ { \top } \succ 0$ , yields the decay

$$\dot {V} _ {2} = - \boldsymbol {s} ^ {\top} \left[ \boldsymbol {D} ^ {*} (\boldsymbol {v}, \psi) + \boldsymbol {K} _ {d} \right] \boldsymbol {s} - \tilde {\boldsymbol {\eta}} ^ {\top} \boldsymbol {K} _ {p} \tilde {\boldsymbol {\eta}} \leq 0, \tag {14}$$

To capture slow-varying mismatch, we augment the generalized force with a bias d as in (7), and define the composite Lyapunov function

$$V (\tilde {\boldsymbol {\eta}}, \boldsymbol {s}, \boldsymbol {d}) = V _ {2} (\tilde {\boldsymbol {\eta}}, \boldsymbol {s}) + \frac {1}{2} \boldsymbol {d} ^ {\top} \boldsymbol {P} _ {d} \boldsymbol {d}, \quad \boldsymbol {P} _ {d} = \boldsymbol {P} _ {d} ^ {\top} \succ 0, \tag {15}$$

where $\dot { d } = - \Lambda d$ with $\Lambda \succ 0$ contributes $- d ^ { \top } P _ { d } \Lambda d$ to $\dot { V } .$ .

Let $\pmb { \xi } = [ \tilde { \pmb { \eta } } ^ { \top } , \pmb { s } ^ { \top } , \pmb { d } ^ { \top } ] ^ { \top }$ and sampling period ∆t. The MPC predictor uses the explicit Euler discretization [36]

$$\boldsymbol {\xi} _ {k + 1} = \boldsymbol {\xi} _ {k} + \Delta t \tilde {\boldsymbol {f}} (\boldsymbol {\xi} _ {k}, \boldsymbol {u} _ {k}; \boldsymbol {T} (\boldsymbol {\theta} _ {k}), \boldsymbol {\Gamma} _ {k}), \tag {16}$$

where $\tilde { \pmb f }$ collects the error dynamics implied by (8) and the allocation (7).

Using V as terminal penalty, the Optimal Control Problem (OCP) at time k is
