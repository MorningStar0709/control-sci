Equation (7) shows that the safety-filtered closed loop is a continuous two-mode piecewise-affine system. Its activemode matrix A˜ differs from the nominal closed-loop matrix $A _ { 0 }$ by the rank-one term $- U V ^ { \top }$ , a structure underlying the scalar loop representation and robustness analysis developed next. We also recall the following stability result from [21], which will be used later to translate mode-wise stability into stability of the full closed loop (7).

Proposition 2 ([21]). If A˜ is Hurwitz, then the origin is globally exponentially stable (GES) for (7).

The goal of the paper is to characterize robustness margins of the active-mode loop induced by the safety filter and to derive design conditions on K that enlarge these margins while preserving stability of the closed loop (7). Although robustness of (7) depends, in principle, on both modes, the inactive mode is exactly the nominal closed loop $A _ { 0 } ,$ , whose robustness margin is assumed satisfactory by design. Since the safety filter modifies the dynamics only through the correction term $u _ { \mathrm { c o r r } } ( x )$ when active, the relevant robustness degradation arises in the active mode.
