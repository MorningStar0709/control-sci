The matched uncertainty $h ( s ) : = - R$ represents unknown rolling resistance forces. For the purpose of simulation, we have generated ?? by using the expression $R ^ { ( 1 ) } = - 2$ cos ?? + $\omega y - v \theta + \omega$ and $R ^ { ( 2 ) } = 2 ( 1 - \sin \theta ) + x v - y - 0 . 5 y ^ { 2 }$ as in [2]. The control objective is to steer the states of robot $s _ { t }$ from $s _ { 0 } = \left[ - 1 \quad - 0 . { \overset { \cdot } { 2 } } 5 \quad \pi / 4 \quad 0 \quad - \pi / 8 \right] ^ { \intercal }$ to the equilibrium $s _ { e } =$ $\left[ 0 \quad \dot { 0 } \quad 0 \quad - v _ { r } \quad 0 \right]$ .

To design the deep MPC, we used a neural network with three hidden layers of sizes $\{ 8 , 1 2 , n _ { 2 } \}$ , where $n _ { 2 } = 4$ represents the outermost layer. We used ??????ℎ as a bounded activation function in the last hidden layer and Rectified Linear Unit (ReLU) in the remaining hidden layers; see Fig. 2. The weights of the linear output layer are updated by using (3) and (4) with learning rate $\theta = 0 . 5$ and bounds ${ \bar { W } } _ { i }$ in (4) is obtained by

$$\bar {W} _ {i} = \frac {u _ {\max} ^ {a}}{\sqrt {n _ {u} (1 + 0 . 2 5 n _ {2})}} \text { for } i = 1, \dots , n _ {u} + 1. \tag {12}$$

The hidden layers of Deep MPC are trained at each 20 time steps using SGD with learning rate 0.01. We choose the optimization horizon $N = 1 0$ , the state penalty matrix ?? as a diagonal matrix with entries $\{ 0 . 5 , 2 , 1 , 0 . 5 , 5 \}$ , the control penalty matrix $R = I$ and the terminal cost matrix $Q _ { f } = 1 0 ^ { 5 } I .$ . We have implemented the reference governor (2) by using the same parameters $Q , R$ as the online MPC, $N = 1 0 0$ and heuristically tightened the constraints by multiplying them with a factor of 0.9. We follow the approach of [23] for the inclusion and removal of data pairs based on the singular value maximization criterion. We carried out simulations by using a MATLAB-based software package MPCTools [27].
