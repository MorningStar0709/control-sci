for $w ,$ for all $x \in \mathbb { R } ^ { n }$ with, ideally, $\hat { \theta } = \theta .$ In practice, this may be approached by the least-squares solution as per

$$w = \underset {v \in \mathbb {R} ^ {l}} {\arg \inf} \sum_ {j = 1} ^ {M} \left(v ^ {\top} \varphi (x ^ {j}) - \mathbb {J} (x ^ {j}, u (x ^ {j}), v, \hat {\theta})\right) ^ {2} \tag {15}$$

over M samples $x ^ { j } \in \mathbb { R } ^ { n }$ . To ensure existence of w, $v \in$ $\mathbb { W } : = \{ w \in \mathbb { R } ^ { l } : { \underline { { w } } } \leq \| w \| \leq \overline { { w } } \} , { \underline { { w } } } , \overline { { w } } > 0 ,$ , may be employed as constraint. Another common option is to adapt the critic weights via gradient rules roughly in the following form (refer to, e. g., [29], [40])

$$w _ {k} = w _ {k - 1} - l _ {w} \nabla_ {w} (\hat {J} - \mathbb {J}), k \in \mathbb {N} _ {0}, \tag {16}$$

with suitably chosen learning rates $l _ { w } > 0 .$ .

Subsequently, $\| w _ { k } - w _ { k - 1 } \| \leq \varepsilon _ { w }$ for some $\varepsilon _ { w } \geq 0 .$ .

Based on (14), the approximate optimal control may be found by gradient methods analogously, adjusting weights of a parameterized $u _ { k } = u _ { k } ( \varsigma _ { k } )$ (cf. policy gradient methods [39]) in the general form [40]

$$\varsigma_ {k} = \varsigma_ {k - 1} - l _ {\varsigma} \nabla_ {\varsigma} \mathbb {J}, \tag {17}$$

with $l _ { \varsigma } > 0$ or via optimization as per

$$\mathcal {P} (x _ {k}): \quad u _ {k} = \underset {u \in \mathbb {U} _ {\mathbb {X}}} {\arg \min} \quad \mathbb {J} (x _ {k}, u, w _ {k}, \hat {\theta} _ {k}). \tag {18}$$

Here $\mathbb { U } _ { \mathbb { X } }$ is the control set associated with the adaptive control Lyapunov function.

Subsequently, $\| u _ { k } - u _ { k - 1 } \| \leq \varepsilon _ { u }$ for some $\varepsilon _ { u } \geq 0$ associated to $\varepsilon _ { w } .$ .

Remark 4: Note that in online reinforcement learning, repeated runs are not available and thus finding w at any time t can either depend only on x(t) or a history of M state samples. The use of data has impact on the performance of uˆ which may be estimated empirically. However, the focus herein lies in ensuring that uˆ (practically) stabilizes the system despite errors in $\lVert \boldsymbol { w } ^ { * } - \boldsymbol { w } \rVert \mathrm { ~ o r ~ } \lVert \boldsymbol { \theta } - \boldsymbol { \hat { \theta } } \rVert$ .

Consider the user-defined ϕ to satisfy the following:
