Input: D, D, Θ, $C^{init}$ , T, λ
for $t \leftarrow 0$ to $\sqrt{T} - 1$ do $\phi_t \sim \text{Rademacher}(0.5)$ Use control $u_t = C^{\text{init}}(x_t) + \frac{\phi_t}{\log(T)}$ end

3 $\hat{\theta}_{wu} \leftarrow (Z_{\sqrt{T}}^\top Z_{\sqrt{T}} + \lambda I)^{-1} Z_{\sqrt{T}}^\top X_{\sqrt{T}}$ for $s \leftarrow 0$ to $\log_2(\sqrt{T}) - 1$ do
4 $T_s \leftarrow 2^s \sqrt{T}$ 5 $\epsilon_s \leftarrow B_{T_s} \sqrt{\frac{\max\left(V_{T_s}^{22}, V_{T_s}^{11}\right)}{V_{T_s}^{11} V_{T_s}^{22} - (V_{T_s}^{12})^2}}$ 6 $\hat{\theta}_s^{\text{pre}} \leftarrow (Z_{T_s}^\top Z_{T_s} + \lambda I)^{-1} Z_{T_s}^\top X_{T_s}$ 7 $\hat{\theta}_s \leftarrow \arg\max_{\|\theta - \hat{\theta}_s^{\text{pre}}\| \leq \epsilon_s} a - b K_{\text{opt}}(\theta)$ 8 $C_s^{\text{alg}} \leftarrow \begin{cases} C_{F_{\text{opt}}(\hat{\theta}_{wu})}^{\text{unc}} & \text{if } \bar{w} + D_U - \frac{D_U}{\hat{a}_{wu} - \hat{b}_{wu} F_{\text{opt}}(\hat{\theta}_{wu})} \leq C_{\text{switch}} T^{-1/4} \\ C_{K_{\text{opt}}(\hat{\theta}_s)}^{\hat{\theta}_s} & \text{otherwise} \end{cases}$ 9    for $t \leftarrow T_s$ to $2T_s - 1$ do
    if $\bar{w} + D_U - \frac{D_U}{\hat{a}_{wu} - \hat{b}_{wu} F_{\text{opt}}(\hat{\theta}_{wu})} \leq C_{\text{switch}} T^{-1/4}$ then $u_t^{\text{safeU}} \leftarrow \max\left\{ u : \max_{\|\theta - \hat{\theta}_{wu}\|_\infty \leq \epsilon_0} ax_t + bu \leq D_U \right\}$ $u_t^{\text{safeL}} \leftarrow \min\left\{ u : \min_{\|\theta - \hat{\theta}_{wu}\|_\infty \leq \epsilon_0} ax_t + bu \geq D_L \right\}$ end
    else
7 $u_t^{\text{safeU}} \leftarrow \max\left\{ u : \max_{\|\theta - \hat{\theta}_s\|_\infty \leq \epsilon_s} ax_t + bu \leq D_U \right\}$ 8 $u_t^{\text{safeL}} \leftarrow \min\left\{ u : \min_{\|\theta - \hat{\theta}_s\|_\infty \leq \epsilon_s} ax_t + bu \geq D_L \right\}$ 9    Use control $u_t = \max\left( min\left( C_s^{\text{alg}}(x_t), u_t^{\text{safeU}} \right), u_t^{\text{safeL}} \right)$ end
    end
end
