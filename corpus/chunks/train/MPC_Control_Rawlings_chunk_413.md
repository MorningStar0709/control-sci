# 3.6.1 The Central Path

The central path is a feasible trajectory for the nominal system that is sufficiently far from the boundaries of the original constraints to enable the ancillary controller for the uncertain system to satisfy these constraints. It is generated by the solution to a nominal optimal control problem $\bar { \mathbb { P } } _ { N } ( z )$ where z is the state of the nominal system. The cost function $\bar { V } _ { N } ( \cdot )$ for the nominal optimal control problem is defined by

$$\bar {V} _ {N} (z, \mathbf {v}) := \sum_ {k = 0} ^ {N - 1} \ell (z (k), v (k)) + V _ {f} (z (N)) \tag {3.47}$$

in which $z ( k ) = \bar { \phi } ( k ; z , { \bf v } )$ and z is the initial state. The function $\ell ( \cdot )$ is defined by

$$\ell (z, \nu) := (1 / 2) \big (| z | _ {Q} ^ {2} + | \nu | _ {R} ^ {2} \big)$$

in which Q and R are positive definite, $| z | _ { O } ^ { 2 } : = z ^ { T } Q z$ , and $| \nu | _ { R } ^ { 2 } : =$ $\nu ^ { T } Q \nu$ . We impose the following state and control constraints on the nominal system

$$z \in \mathbb {Z} \qquad v \in \mathbb {V}$$

in which $\mathbb { Z } \subset \mathbb { X }$ and $\mathbb { V } \subset \mathbb { U }$ . The choice of Z and V is more difficult than in the linear case because it is difficult to bound the deviation $e = x - z$ of the state x of the uncertain system from the state z of the nominal system. We assume that these two constraint sets are compact. The terminal cost function $V _ { f } ( \cdot )$ together with the terminal constraint set $\mathbb { Z } _ { f } \subseteq \mathbb { X }$ for the nominal system are chosen as described in Chapter 2 and Section 3.4 to satisfy the usual “stability axioms.” The state and control constraints, and the terminal constraint $z ( N ) \in \mathbb { Z } _ { f }$ impose a parametric constraint $\mathbf { v } \in \mathcal { V } _ { N } ( z )$ on the nominal control sequence in which $\gamma _ { N } ( z )$ is defined by

$$
\begin{array}{l} \mathcal {V} _ {N} (z) := \{\mathbf {v} \mid v (k) \in \mathbb {V}, \quad \bar {\phi} (k; z, \mathbf {v}) \in \mathbb {Z} \quad \forall k \in \mathbb {I} _ {0: N - 1}, \\ \bar {\phi} (N; z, \mathbf {v}) \in \mathbb {Z} _ {f} \} \\ \end{array}
$$

For each z, the set $\gamma _ { N } ( z )$ is compact; it is bounded because of the assumptions on V, and closed because of the continuity of $\bar { \phi } ( \cdot )$ . The nominal optimal control problem $\bar { \mathbb { P } } _ { N } ( z )$ is defined by
