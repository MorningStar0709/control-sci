along the solution of (34)-(38) for $t \in \left( \check { t } _ { k } ^ { p } , \check { t } _ { k + 1 } ^ { p } \right) , k \in \mathbb { N } .$ , $W ^ { p } ( t )$ is the performance residual given by $( 7 9 ) – ( 8 1 )$ , and H(t) is given by (73).

Next we present Lemma 6 which provides bounds on $d ^ { 2 } ( t )$ , $m ^ { p } ( t )$ , and $W ^ { p } ( t )$ required for proving the main results of P-STC.

Lemma 6. Consider the P-STC approach (119)-(123), which generates an increasing set of event times $\{ \check { t } _ { k } ^ { p } \} _ { k \in \mathbb { N } }$ with $\check { t } _ { k } ^ { p } =$ 0. Then, for the input holding error error $d ( t )$ given by (34), the following estimate holds

$$d ^ {2} (t) \leq H (\check {t} _ {k} ^ {p}) e ^ {\varrho^ {\star} (t - \check {t} _ {k} ^ {p})}, \tag {124}$$

where ${ \varrho ^ { \star } } > 0$ is given by (70), and $H ( t )$ is given by (73). Further, if the event-trigger parameters $\theta , \theta _ { m } , \kappa _ { 1 } , \kappa _ { 2 } , \kappa _ { 3 } > 0$ are chosen as in Assumption $2 , \ c > 0 ,$ , and $\eta > 0$ is chosen as in (95), then $W ^ { p } ( t )$ given by (79)-(81) satisfies

$$W ^ {p} (t) \geq e ^ {- (b ^ {\star} + c) (t - \check {t} _ {k} ^ {p})} W ^ {p} (\check {t} _ {k} ^ {p}) \text { with } b ^ {\star} = \eta , \tag {125}$$

for all $t \in \big [ \check { t } _ { k } ^ { p } , \check { t } _ { k + 1 } ^ { p } \big ) , k \in \mathbb { N } ,$ and

$$W ^ {p} (t) \geq 0, i. e., V ^ {p} (t) \leq e ^ {- b ^ {\star} t} V _ {0} w i t h b ^ {\star} = \eta , \tag {126}$$

for all $t > 0$ whereas $m ^ { p } ( t )$ governed by (78) satisfies

$$
\begin{array}{l} m ^ {p} (t) \geq m ^ {p} (\check {t} _ {k} ^ {p}) e ^ {- \eta (t - \check {t} _ {k} ^ {p})} \\ - \frac {\theta_ {m} H (\check {t} _ {k} ^ {p})}{\varrho^ {\star} + \eta} e ^ {- \eta (t - \check {t} _ {k} ^ {p})} \left(e ^ {(\varrho^ {\star} + \eta) (t - \check {t} _ {k} ^ {p})} - 1\right), \tag {127} \\ \end{array}
$$

for all $t \in [ \check { t } _ { k } ^ { p } , \check { t } _ { k + 1 } ^ { p } ) , j \in \mathbb { N } .$

Proof. Let us consider the following positive definite function

$$\bar {V} (t) := \int_ {0} ^ {\ell} \left(\frac {1}{v ^ {\star}} \alpha^ {2} (x, t) e ^ {- \frac {\mu x}{v ^ {\star}}} + \frac {r _ {0} ^ {2}}{(\gamma p ^ {\star} - v ^ {\star})} \beta^ {2} (x, t) e ^ {\frac {\mu x}{(\gamma p ^ {\star} - v ^ {\star})}}\right) d x, \tag {128}$$

where $\mu > 0$ . Taking the time derivative of (128), integrating by parts, and recalling (38), we can obtain
