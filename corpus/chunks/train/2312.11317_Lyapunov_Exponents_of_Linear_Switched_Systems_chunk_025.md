Suppose now that tr $\left( [ A , B ] ^ { 2 } \right) < 0$ . Notice that this can happen only if $\operatorname { t r } ( A ^ { 2 } ) , \operatorname { t r } ( B ^ { 2 } ) > 0$ . Similarly to the previous case, this inequality implies

$$\frac {\mu}{\lambda} \frac {\mathrm{tr} (A B) - \mathrm{tr} (A ^ {2})}{\mathrm{tr} (A B) - \mathrm{tr} (B ^ {2})} > 1.$$

So, we can solve equation (4.26) only for $t < \bar { t } ,$ where

$$\bar {t} = \frac {1}{\lambda} \mathrm{arctanh} \left(\frac {\lambda}{\mu} \frac {\mathrm{tr} (A B) - \mathrm{tr} (A ^ {2})}{\mathrm{tr} (A B) - \mathrm{tr} (B ^ {2})}\right).$$

Moreover, lim $1 _ { t  \bar { t } } s ( t ) = + \infty$ . Then, from point (2) of Lemma 4.29, we have

$$\ell (2 t, 2 s (t)) > \ell (t, s (t)).$$

So, in particular we can find $t _ { 1 } \in ( t , \bar { t } )$ such that $t _ { 1 } + s ( t _ { 1 } ) = 2 ( t + s ( t ) )$ . So, this implies

$$\ell (t _ {1}, s (t _ {1})) > \ell (2 t, 2 s (t)) > \ell (t, s (t)).$$

So, we obtain that

$$\ell (t, s (t)) < \lim _ {\tau \to \bar {t}} \ell (\tau , s (\tau)).$$

But as $t  \bar { t } ,$ the trajectory spends more and more time with control equal to 0. So, this strategy is for sure worse than spending all the time with control equal to 1, which is always admissible if $\operatorname { t r } \left( [ A , B ] ^ { 2 } \right) < 0$ . Hence, in this case switches are not optimal. □

We now prove Lemma 4.29.

Proof of Lemma 4.29. Recall that

$$l (t, s) = \frac {1}{t + s} \log (\alpha (t, s)).$$

Hence, we can write

$$\ell \left(\frac {t}{2}, \frac {s}{2}\right) - \ell (t, s) = \frac {2}{t + s} \log \left(\alpha \left(\frac {t}{2}, \frac {s}{2}\right)\right) - \frac {1}{t + s} \log (\alpha (t, s)) == \frac {1}{t + s} \log \left(\frac {\alpha \left(\frac {t}{2} , \frac {s}{2}\right) ^ {2}}{\alpha (t , s)}\right).$$

So, we can see that this difference is positive if and only if

$$\frac {\alpha \left(\frac {t}{2} , \frac {s}{2}\right) ^ {2}}{\alpha (t , s)} > 1.$$

Since $\alpha ( t , s ) > 1$ for any $t , s > 0 .$ , we have that

$$\alpha \left(\frac {t}{2}, \frac {s}{2}\right) ^ {2} > \alpha (t, s) \quad \mathrm{iff} \quad \alpha \left(\frac {t}{2}, \frac {s}{2}\right) ^ {2} + \frac {1}{\alpha \left(\frac {t}{2} , \frac {s}{2}\right) ^ {2}} > \alpha (t, s) + \frac {1}{\alpha (t , s)},$$

which is the same as

$$\operatorname{tr} \left(X \left(\frac {t}{2}, \frac {s}{2}\right) ^ {2}\right) > \operatorname{tr} \bigl (X (t, s) \bigr).$$

The term in the left hand side can be rewritten as
