# 3.6.3 Controller Algorithm

Suppose Z and V have been chosen. In the following algorithm, $\mathbf { v } ^ { * }$ denotes the control sequence $\{ \nu ^ { * } ( 0 ) , \nu ^ { * } ( 1 ) , \ldots , \nu ^ { * } ( N - 1 ) \}$ , and $\mathbf { z } ^ { \ast }$ denotes the state sequence $\{ z ^ { * } ( 0 ) , z ^ { * } ( 1 ) , \ldots , z ^ { * } ( N ) \}$ . The controller algorithm is:
