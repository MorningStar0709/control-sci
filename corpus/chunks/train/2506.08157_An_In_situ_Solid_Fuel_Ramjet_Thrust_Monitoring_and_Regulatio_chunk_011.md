and the scalars $K _ { \mathrm { P } , k } , K _ { \mathrm { I } , k }$ and $K _ { \mathrm { D } , k }$ are the proportional, integral and derivative gains optimized by the RCAC algorithm at step k. The integral signal $\gamma _ { k }$ is computed recursively as $\gamma _ { k + 1 } = \gamma _ { k } + z _ { k + 1 }$ . Note that the PID control law (11) can be written in the regressor form as $u _ { k } = \Phi _ { k } \theta _ { k }$ , where

$$
\Phi_ {k} \triangleq \left[ z _ {k} \quad \gamma_ {k} \quad z _ {k} - z _ {k - 1} \right], \quad \theta_ {k} \triangleq \left[ \begin{array}{c} K _ {\mathrm{P}, k} \\ K _ {\mathrm{I}, k} \\ K _ {\mathrm{D}, k} \end{array} \right], \tag {13}
$$

where the regressor matrix $\Phi _ { k }$ contains the measured data and the controller gain vector $\theta _ { k }$ is optimized by the RCAC algorithm. To determine the controller gains $\theta _ { k }$ , let $\boldsymbol { \theta } \in \mathbb { R } ^ { l _ { \theta } }$ , and consider the retrospective performance variable defined by

$$\hat {z} _ {k} (\theta) \triangleq z _ {k} + G _ {\mathrm{f}} (\mathbf {q}) (\Phi_ {k} \theta - u _ {k}), \tag {14}$$

where $\begin{array} { r } { G _ { \mathrm { f } } ( \mathbf { q } ) \overset { \triangle } { = } \sum _ { i = 1 } ^ { n _ { \mathrm { f } } } \frac { N _ { i } } { \mathbf { q } ^ { i } } } \end{array}$ Pnfi=1 Niqi . is a finite-impulse response filter. Note that $N _ { i } \in \dot { \mathbb { R } } ^ { l _ { z } \times l _ { u } }$ . Furthermore, define the retrospective cost function $J _ { k } \colon \mathbb { R } ^ { l _ { \theta } }  [ 0 , \infty )$ b y

$$J _ {k} (\theta) \triangleq \sum_ {i = 0} ^ {k} \hat {z} _ {i} (\theta) ^ {\mathrm{T}} R _ {z} \hat {z} _ {i} (\theta) + \left(\theta - \theta_ {0}\right) ^ {\mathrm{T}} P _ {0} ^ {- 1} \left(\theta - \theta_ {0}\right), \tag {15}$$

where $R _ { z } \in \mathbb { R } ^ { l _ { z } \times l _ { z } } , R _ { u } \in \mathbb { R } ^ { l _ { u } \times l _ { u } }$ , and $P _ { 0 } \in \mathbb { R } ^ { l _ { \theta } \times l _ { \theta } }$ are positive definite; and $\theta _ { 0 } \in \mathbb { R } ^ { l _ { \theta } }$ is the initial vector of controller gains.

Proposition 3.1: Consider (15), where $\theta _ { 0 } \in \mathbb { R } ^ { l _ { \theta } }$ and $P _ { 0 } \in \mathbb { R } ^ { l _ { \theta } \times l _ { \theta } }$ is positive definite. For all $k \geq 0 ,$ , denote the minimizer of $J _ { k }$ given by (15) by

$$\theta_ {k + 1} \stackrel {\triangle} {=} \underset {\theta \in \mathbb {R} ^ {n}} {\operatorname{argmin}} J _ {k} (\theta). \tag {16}$$

Then, for all $k \geq 0 , \theta _ { k + 1 }$ is given by
