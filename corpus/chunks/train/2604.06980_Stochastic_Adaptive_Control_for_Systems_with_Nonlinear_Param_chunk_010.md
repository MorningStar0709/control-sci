# 2.1 Examples

In the following, we present three representative examples of the nonlinear system (1) in which f satisfies Assumption 1.

Example 1 (Nonlinear Opinion Dynamics) Consider a nonlinear opinion dynamical system with external noise(see, $e . g .$ , Durlauf and Ioannides $( 2 0 1 0 ) ;$ Ancona et al. (2023); Bizyaeva et al. (2022) for background on nonlinear opinion formation)

$$x _ {t + 1} = a \tanh (A ^ {*} x _ {t} + B ^ {*} u _ {t}) + w _ {t + 1}, \tag {5}$$

where $x _ { t } = [ x _ { 1 , t } , \cdot \cdot \cdot , x _ { n , t } ] \in \mathbb { R } ^ { n }$ represents the opinion states of the n agents at time t; $A ^ { * } \in \mathbb { R } ^ { n \times n }$ and $B ^ { * } \in \mathbb { R } ^ { n }$ denote the unknown adjacency matrices and the connections of influencers of the underlying social network; $a > 0$ is the maximum sensitivity of agents to social influence; $u _ { t } \in \mathbb { R } ^ { m }$ is a pinning (or influence) input, potentially generated by a leader, that drives the opinion dynamics of the entire network. The hyperbolic tangent function tanh(·) is defined as tanh $\begin{array} { r } { ( z ) = \frac { e ^ { z } - e ^ { - z } } { e ^ { z } + e ^ { - z } } , ~ z \in \mathbb { R } } \end{array}$ e −e− ez+e−z , z ∈ R, and is applied elementwise to vectors in (5). This nonlinear function captures the saturation effects in opinion updates arising from interactions among social peers. By Case (2) of Lemma 1, the function a tanh(·) satisfies Assumption 1 with the following functions α(·) and $\beta ( \cdot )$ :

$$\alpha (x) = a \inf _ {| z | \leq x} \frac {d}{d z} \tanh (z) = \frac {4 a}{(e ^ {x} + e ^ {- x}) ^ {2}},\beta (x) = a \sup _ {| z | \leq x} \frac {d}{d z} t a n h (z) = a, \forall x \geq 0.$$

Example 2 (RNN Dynamics) Consider the classical Elman recurrent neural networks $( R N N )$ state equation with external noise (See., e.g., Equation (14) of Mienye et al. (2024), p. 634 of Schäfer and Zimmermann (2006), Elman (1990)),

$$x _ {t + 1} = \sigma (A ^ {*} x _ {t} + B ^ {*} u _ {t}) + w _ {t + 1}, \tag {6}$$

where $x _ { t }$ and ut are the state vector and the input data at timestamp t. $A ^ { * } \in \mathbb { R } ^ { n \times n }$ and $B ^ { * } \in \mathbb { R } ^ { n \times m }$ are unknown state weight matrix and input weight matrix to be trained. The nonlinear function $\sigma : \mathbb { R } ^ { n } \to \mathbb { \Lambda }$ Rn is an activation function of the form $\tau ( \tau _ { 1 } , \dots , \tau _ { n } ) \ =$ $[ \sigma _ { 1 } ( \tau _ { 1 } ) , \dots , \sigma _ { n } ( \tau _ { n } ) ] ^ { \top }$ . Classical examples of activation functions include:
