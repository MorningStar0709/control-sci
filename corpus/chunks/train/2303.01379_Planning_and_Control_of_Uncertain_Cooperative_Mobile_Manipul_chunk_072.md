where $\widetilde { Y } : = \widetilde { Y } ( q , \dot { q } , v _ { o } , \dot { v } _ { o } ) : = \mathrm { d i a g } \{ [ Y _ { i } ] _ { i \in \mathcal { N } } \} \in \mathbb { R } ^ { 6 N \times N \ell } , \vartheta : = [ \vartheta _ { 1 } ^ { \top } , \ldots , \vartheta _ { N } ^ { \top } ] ^ { \top } \in \mathbb { R } ^ { 6 N \times N \ell } ,$ $\mathbb { R } ^ { N \ell } , \widetilde { \delta } : = \widetilde { \delta } ( q , \dot { q } , t ) : = \mathrm { d i a g } \{ [ \delta _ { i } ] _ { i \in \mathcal { N } } \} \in \mathbb { R } ^ { 6 N \times N \mu }$ , and $\bar { d } : = [ \bar { d } _ { 1 } ^ { \top } , \ldots , \bar { d } _ { N } ^ { \top } ] ^ { \top } \in$ $\mathbb { R } ^ { N \mu }$ .

Let us now introduce the states $\hat { \vartheta } _ { o } \in \mathbb { R } ^ { \ell _ { o } }$ and $\hat { \vartheta } _ { i } \in \mathbb { R } ^ { \ell }$ which represent the estimates of $\vartheta _ { o }$ and $\vartheta _ { i }$ , respectively, by agent $i \in \mathcal N$ , and the corresponding stack vector $\hat { \vartheta } : = [ \hat { \vartheta } _ { 1 } ^ { \top } , \ldots , \hat { \vartheta } _ { N } ^ { \top } ] ^ { \top } \in \mathring { \mathbb { R } } ^ { N \ell }$ , for which we formulate the associated errors as

$$e _ {\vartheta_ {O}} := \vartheta_ {O} - \hat {\vartheta} _ {O} \tag {2.19a}
e _ {\vartheta} := \left[ \begin{array}{c} e _ {\vartheta_ {1}} \\ \vdots \\ e _ {\vartheta_ {N}} \end{array} \right] := \left[ \begin{array}{c} \vartheta_ {1} - \hat {\vartheta} _ {1} \\ \vdots \\ \vartheta_ {N} - \hat {\vartheta} _ {N} \end{array} \right] = \vartheta - \hat {\vartheta}. \tag {2.19b}
$$

In the same vein, we introduce the states $\hat { d } _ { o } \in \mathbb { R } ^ { \mu o }$ and $\hat { d } _ { i } \in \mathbb { R } ^ { \mu }$ that correspond to the estimates of $\bar { d } _ { o }$ and ${ \bar { d } } _ { i }$ , respectively, by agent $i \in \mathcal N$ , and the corresponding stack vector $\hat { d } : = [ \hat { d } _ { 1 } ^ { \top } , \ldots , \hat { d } _ { N } ^ { \top } ] ^ { \top } \in \mathbb { R } ^ { N \mu }$ , for which we also formulate the associated errors as

$$e _ {d _ {O}} := \bar {d} _ {O} - \hat {d} _ {O} \in \mathbb {R} ^ {\mu_ {O}} \tag {2.20a}
e _ {d} := \left[ \begin{array}{c} e _ {d _ {1}} \\ \vdots \\ e _ {d _ {N}} \end{array} \right] := \left[ \begin{array}{c} \bar {d} _ {1} - \hat {d} _ {1} \\ \vdots \\ \bar {d} _ {N} - \hat {d} _ {N} \end{array} \right] = \bar {d} - \hat {d} \in \mathbb {R} ^ {N \mu}. \tag {2.20b}
$$

Next, we design the reference velocity

$$
v _ {f} := v _ {\mathrm{d}} - K _ {f} e = \left[ \begin{array}{l} \dot {p} _ {\mathrm{d}} - k _ {p} e _ {p} \\ \omega_ {\mathrm{d}} + k _ {\zeta} e _ {\epsilon} \end{array} \right] \tag {2.21}
$$
