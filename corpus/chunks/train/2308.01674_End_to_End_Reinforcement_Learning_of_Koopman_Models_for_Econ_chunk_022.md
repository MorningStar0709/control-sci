# 3.2 Data sampling and system identification

Given randomly sampled control inputs u within their respective bounds, the product concentration c typically quickly moves away from its feasible range in eNMPC and also away from its target value in NMPC (see Table 2). Therefore, such a sampling strategy would not produce a data set particularly suitable for learning a good dynamic surrogate model, as most data would be in irrelevant parts of the state space. Instead of actuating the mechanistic model by randomly sampling both control inputs $\rho$ and F , we, therefore, take a different approach: We generate multiple trajectories using the mechanistic model, each trajectory starting from the target state of the NMPC, which is also at the center of the feasible range of the eNMPC. For each trajectory, we randomly generate a series of control inputs for either ρ or F . Given this series of inputs, we then solve an OCP using the mechanistic model to determine a corresponding series of values for the respective other control variable, which leads to c not diverging much from its feasible range in eNMPC. As in NMPC and eNMPC, we use control steps of length $\Delta t _ { \mathrm { c t r l } } = 6 0 \operatorname* { m i n }$ , i.e., $\rho$ and F change every four time steps.

Given an OCP horizon of $t _ { f }$ time steps with the corresponding set $\mathrm { T } = \{ 0 , \ldots , t _ { f } - 1 \}$ , and a randomly generated trajectory for the production rate $\rho _ { \mathrm { t a r g e t } , t } \forall t \in \mathrm { T }$ , the OCP minimizes

$$\sum_ {t \in \mathrm{T}} (c _ {\text { target }} - c _ {t}) ^ {2} + w _ {\rho} (\rho_ {\text { target }, t} - \rho_ {t}) ^ {2}, \tag {7}$$

subject to the mechanistic model (Equations (6a) - (6b)), and subject to the given bounds on control variables (see Table 2), with $c _ { \mathrm { t a r g e t } }$ being the target value for c in NMPC (see Table 2) and $w _ { \rho } = 1 0$ . Equivalently, given a sampled trajectory for the coolant flow rate $F _ { \mathrm { t a r g e t } , t } \forall t \in \mathrm { ~ T ~ }$ , the OCP mini-

Tab. 2. Lower (lb) and upper (ub) bounds of system states and control inputs, target values of system states in NMPC, and steady-state (ss) values used to evaluate the economic benefit of flexible production in eNMPC.
