In the remainder of this section, $\{ ( \tilde { \mathbf { x } } ( t ) , \tilde { \mathbf { a } } ( t ) ) \} _ { t \geq 0 }$ denotes the state-action vectors under the joint policy $( \gamma ^ { * , N } , \pi ^ { * } , \dots , \pi ^ { * } )$ and initial distribution $\mu ^ { * , \otimes ^ { N } }$ for the N -agent game. Similarly, $\{ ( \hat { x } ( t ) , \hat { a } ( t ) ) \} _ { t \geq 0 }$ denotes the state-action pairs under the policy $\gamma ^ { * , N }$ and initial distribution $\mu ^ { * }$ of a generic agent in the infinite population limit. Note that in the infinite population limit, a generic agent applies the best-response policy $\gamma ^ { * , N } : \mathcal { P } ( \mathsf X ) \times \mathsf X \to \mathsf { A }$ as follows: $\hat { a } ( t ) = \gamma ^ { * , \bar { N } } ( \mu ^ { * } , \hat { x } ( t ) )$ for all $t \geq 0$ .

LEMMA 4.3. Given any $\varepsilon > 0 ,$ for all $t \geq 0 ,$ , we have

$$\mathbb {E} \left[ \sup _ {g \in \mathcal {F}} \left| \int_ {\mathsf {X}} g (x) \mathsf {e} [ d x | \tilde {\mathbf {x}} (t) ] - \int_ {\mathsf {X}} g (x) \mu^ {*} (d x) \right| \right] = \mathbb {E} [ W _ {1} (\mathsf {e} [ \cdot | \tilde {\mathbf {x}} (t) ], \mu^ {*}) ] \leq \tilde {\alpha} _ {t} (N, \varepsilon)$$

where error bounds $\{ \tilde { \alpha } _ { t } ( N , \varepsilon ) \} _ { t \geq 0 }$ are recursively defined as follows

$$\tilde {\alpha} _ {0} (N, \varepsilon) := \alpha (N)
\begin{array}{l} \tilde {\alpha} _ {t + 1} (N, \varepsilon) := (\mathsf {K} _ {1} + \mathsf {K} _ {2} \mathsf {L} ^ {*} + \mathsf {K} _ {3})   \tilde {\alpha} _ {t} (N, \varepsilon) + \sqrt {\frac {2}{N - 1}}   \mathrm{diam} (\mathsf {X}) \left\{\mathbb {N} (\varepsilon , \mathcal {F}) + \sqrt {\frac {8}{N - 1}} \right\} + 2 \varepsilon \\ =: \kappa_ {1} \tilde {\alpha} _ {t} (N, \varepsilon) + \kappa_ {2} ^ {N} \sqrt {\frac {2}{N - 1}} + 2 \varepsilon \\ \end{array}
$$

Therefore, for any $t \geq 1$ , we have

$$\tilde {\alpha} _ {t} (N, \varepsilon) = \kappa_ {1} ^ {t} \alpha (N) + \kappa_ {2} ^ {N} \sqrt {\frac {2}{N - 1}} \sum_ {i = 0} ^ {t - 1} \kappa_ {1} ^ {i} + 2 \varepsilon \sum_ {i = 0} ^ {t - 1} \kappa_ {1} ^ {i}$$

PROOF. Since $( \widetilde { x } _ { 1 } ( 0 ) , \dots , \widetilde { x } _ { N } ( 0 ) ) \sim \mu ^ { * , \otimes ^ { N } }$ , the statement is true for $t = 0$ . Suppose that it is true for some $t \geq 0$ and consider $t + 1$ . Fix any $g \in { \mathcal { F } }$ . Then we can write
