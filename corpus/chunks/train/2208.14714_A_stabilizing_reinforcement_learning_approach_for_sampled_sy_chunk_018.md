# Do:

- select regressor $\varphi$ satisfying Assumption. 2;   
- define critic weight sets $\mathbb { W } , \mathbb { W } ^ { \prime } .$ , weight $w ^ { \# } \in \mathbb { W }$ of Assumption 2 and set $w _ { 0 } \in \mathbb { W } ^ { \prime }$ ;   
- define critic weight update (15) or (16) with rate $l _ { w } \mathbf { ; }$   
- define control input update (17) or (18);   
- select sampling time δ as per (49);   
- determine bounds $\bar { \varepsilon } _ { 1 , w }$ as per (47);

while $k \geq 0$ do

$$
\begin{array}{l} \text {   get   } \hat {\theta} _ {k + 1} \text {   from   (21);   } \\ \text {   get   } w _ {k} \text {   from   (15)   or   (16);   } \\ \text {   get   } u _ {k} \text {   using   (17)   or   from   (18);   } \\ \text { if   if } (u _ {k}, w _ {k}) \text { satisfy } i) (2 0) \text { with } \varepsilon_ {1} \leq \bar {\varepsilon} _ {1}, i i) \\ \begin{array}{l} \| w _ {k} - w _ {k - 1} \| \leq \varepsilon_ {w} \leq \bar {\varepsilon} _ {w} \text {   and   iii)   } w _ {k} \in \mathbb {W} ^ {\prime} \text {   then } \\ \text { take   } (u _ {k}, w _ {k}) \text {   as   computed; } \end{array} \\ \text { set } (u _ {k}, w _ {k}) = (\mu (x _ {k}, \hat {\theta} _ {k}), w ^ {\#}); \\ k \mapsto k + 1; \\ \end{array}
$$
