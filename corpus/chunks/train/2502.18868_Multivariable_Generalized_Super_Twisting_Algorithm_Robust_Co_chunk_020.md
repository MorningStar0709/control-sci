for $i \in \mathbb { K }$ . As it can be easily verified, the inequalities (52)-(54) are LMIs with respect to the matrix variables Q, X , Y , W and $Z _ { d }$ provided that the pair of positive variables $( \alpha , \rho )$ is fixed. Finally, keeping in mind that the maximum of two elements equals the minimum upper bound, the LMIs

$$
\left[ \begin{array}{c c} \theta & \bullet \\ \zeta_ {0} & Q \end{array} \right] > 0, \quad \left[ \begin{array}{c c} \theta & \bullet \\ R (\sigma_ {0}) x _ {0} & X \end{array} \right] > 0 \tag {55}
$$

depending on the scalar variable $\theta > 0$ , enable us to determine the Lyapunov function evaluated at the initial condition $\left( \zeta _ { 0 } , x _ { 0 } \right)$ where $x _ { 0 } = [ \sigma _ { 0 } ^ { T } \ v _ { 0 } ^ { T } ] ^ { T }$ whenever the exogenous disturbance satisfies $f ( 0 ) = 0$ , see (2). The case of $f ( 0 ) \neq 0$ can be addressed following the same steps presented in [17]. Moreover, note that $R ( \sigma _ { 0 } )$ relies on the positive scalar $\overset { \cdot } { \alpha } > 0$ . These finds are summarized in the next theorem.

Theorem 3 Let the scalars $\gamma > 0 , \alpha > 0 , \rho > 0$ and a non null initial condition $\left( \zeta _ { 0 } , x _ { 0 } \right)$ be given. The optimal solution of the convex programming problem

$$\inf _ {\theta , Q, X, Y, W, Z _ {d}} \left\{\theta : (5 2) - (5 5) \right\} \tag {56}$$

provides the state feedback matrix gains $K _ { 0 } = W Q ^ { - 1 }$ and $\mathbf { \bar { \boldsymbol { K } } } = \boldsymbol { Y } \boldsymbol { X } ^ { - 1 }$ such that the closed-loop system is globally asymptotically stable with guaranteed performance index $\theta ,$ , whenever the exogenous disturbance satisfying (2) and the parametric uncertainty defined by the polyhedral set M is concerned.

Proof: The proof is a consequence of Theorem 1. Whenever (56) is feasible the corresponding state feedback gain ensure global robust asymptotic stability of the closed-loop system. Moreover, since $\alpha > 0$ and $\rho > 0$ are fixed (but not necessarily optimal) the value of θ is an upper bound to the guaranteed performance index, because

$$
\begin{array}{l} \nu (\zeta_ {0}, x _ {0}) = \max \{v (\zeta_ {0}), V (x _ {0}) \} \\ = \inf _ {\theta} \{\theta : (5 5) \} \tag {57} \\ \end{array}
$$

concluding thus the proof.

![](image/0275163c6bd3783193cea9aa184733d2c86ca54e60aaf64aaab4c25cd0d097d0.jpg)
