Thus, if the weights $c _ { k } \mathrm { ^ { \circ } s }$ and the basis functions $\phi _ { k } \mathbf { ^ { \prime } s }$ are written in a vector form, $\Phi ( { \boldsymbol x } ( t ) u ( t ) )$ can be expressed as

$$\Phi (x (t), u (t)) = c ^ {T} \phi (x (t), u (t)), \tag {9}$$

where $\begin{array} { c c c c c c } { \boldsymbol { c } } & { = } & { \left[ c _ { 1 } } & { c _ { 2 } } & { \ldots } & { c _ { r } \right] ^ { T } } \end{array}$ and $\begin{array} { r l } { \phi ( x ( t ) , u ( t ) ) } & { { } = } \end{array}$ $\begin{array} { r l } { \left[ \phi _ { 1 } ( x ( t ) , u ( t ) ) \right. } & { { } \phi _ { 2 } ( x ( t ) , u ( t ) ) \quad \ldots \quad \phi _ { r } ( x ( t ) , u ( t ) ) \big ] ^ { T } . } \end{array}$ .

Using Assumption $^ { 6 , }$ the optimal control problem (7) becomes

$$
\begin{array}{l} \underset {u} {\text { minimize }} \int_ {t _ {0}} ^ {t _ {f}} c ^ {T} \phi (x (t), u (t)) d t, \tag {10} \\ \text { subject   to } \quad \dot {x} (t) = f (x (t), u (t)), \\ \end{array}
x (t _ {0}) = x _ {0},$$

Hence, the goal of the IOC problem now changes to finding the unknown weight vector c instead of the cost function $\Phi ( x ( t ) , u ( t ) )$ . The IOC problem statement is now refined in the following way [38].

Problem Statement 2: Given the system dynamics ${ \dot { x } } ( t ) =$ $f ( x ( t ) , u ( t ) )$ , basis function vector $\phi ( { \boldsymbol { x } } ( t ) , { \boldsymbol { u } } ( t )$ and a specific system trajectory $x ( t )$ and control signal $u ( t ) , \forall t _ { 0 } \leq t \leq t _ { f } ,$ measured from the primary optimal control problem $( 1 0 ) .$ , the objective of the IOC problem is to find the weight vector c.
