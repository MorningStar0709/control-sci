Remark 4: Considering the auxiliary state $\bar { x } _ { i }$ is approaching the average state ${ \bar { x } } ^ { * }$ due to $( 4 ) , { \bar { x } } _ { i }$ can be regarded as the estimated average state for each agent.

Next, we prove the boundness of the consensus error related to $\pmb { \tilde { x } } = [ \tilde { x } _ { 1 } , \cdots , \tilde { x } _ { N } ] ^ { T }$ , which is shown as follows.

Lemma 3: Let all assumptions in Theorem 1 hold and choose $\delta \in \mathsf { \Gamma } ( 0 , 1 / N )$ , then it holds lim $\begin{array} { r } { \mathbf { \widehat { l } } _ { t  \infty } \| \tilde { \mathbf { x } } ( t ) \| \ \leq } \end{array}$ $2 c ^ { - 1 } N \eta$ with probability of at least $1 - N \delta$ .

Proof: Apply the control law (3) with (4) into (1), the concatenated dynamics of the controlled system denotes

$$\dot {\tilde {\boldsymbol {x}}} = \dot {\boldsymbol {x}} - \dot {\bar {\boldsymbol {x}}} = - c (\boldsymbol {\mathcal {L}} + \boldsymbol {I} _ {N}) \tilde {\boldsymbol {x}} + \boldsymbol {f} (\boldsymbol {x}) - \hat {\boldsymbol {\mu}} (\boldsymbol {x}), \tag {18}$$

where the concatenated unknown function and its prediction denote ${ \pmb f } ( { \pmb x } ) ~ = ~ [ f ( x _ { 1 } ) , \cdot \cdot \cdot , f ( x _ { N } ) ] ^ { T }$ and ${ \hat { \pmb { \mu } } } ( { \pmb x } ) \ =$ $[ \hat { \mu } _ { 1 } ( x _ { 1 } ) , \cdot \cdot \cdot , \hat { \mu } _ { N } ( x _ { N } ) ] ^ { T }$ respectively with $\hat { \mu } _ { i } ( \cdot )$ defined in Section III-B. Consider the controlled system with eventtriggered learning as a switching system, and then the control performance is proven using the common Lyapunov theory. Choose the common Lyapunov candidate $V _ { c }$ as

$$V _ {c} = \tilde {\boldsymbol {x}} ^ {T} \tilde {\boldsymbol {x}}, \tag {19}$$

whose time derivative using (18) is written as

$$\dot {V} _ {c} = - 2 c \tilde {\boldsymbol {x}} ^ {T} (\boldsymbol {\mathcal {L}} + \boldsymbol {I} _ {N}) \tilde {\boldsymbol {x}} + 2 \tilde {\boldsymbol {x}} ^ {T} (\boldsymbol {f} (\boldsymbol {x}) - \hat {\boldsymbol {\mu}} (\boldsymbol {x})). \tag {20}$$

Consider the fact that $\underline { { \lambda } } ( \pmb { { \mathscr { L } } } { + } \pmb { { I } } _ { N } ) = 1 \left[ 3 1 \right]$ , where $\underline { { \lambda } } ( \cdot )$ returns the minimal eigenvalue of the matrix. Then, the derivative $\dot { V } _ { c }$ is bounded by
