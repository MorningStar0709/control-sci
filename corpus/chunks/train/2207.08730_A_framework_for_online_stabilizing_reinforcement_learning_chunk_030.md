# A. Preliminaries

First note that (1) is in fact equivalent to

$$
\begin{array}{l} \mathrm{d} X _ {t} = f \left(X _ {t}, U _ {t}\right) \mathrm{d} t + \sigma \left(X _ {t}, U _ {t}\right) Z _ {t} ^ {\prime} \mathrm{d} t, \\ \mathbb {P} [ X _ {t}, U _ {t} ] = 1 \end{array} \tag {18}
\mathbb {P} \left[ X _ {0} = \text { const } \right] = 1,$$

where $Z ^ { \prime }$ is a measurable bounded random process, such that ∀t $\| Z _ { t } ^ { \prime } \| \leq \mathrm { L i p } _ { Z }$ . The latter is motivated by the fact that due to absolute continuity of $Z ,$ , there always exists such $Z ^ { \prime }$ that $\begin{array} { r } { Z _ { t } = Z _ { 0 } + \int _ { 0 } ^ { t } Z _ { \tau } ^ { \prime } } \end{array}$ dτ , with the last identity implying $\mathrm { d } Z _ { t } =$ $Z _ { t } ^ { \prime }$ dt.

Recall that the statements of the presented theorems concern an arbitrary Lyapunov function L. If a function L is already known to be a Lyapunov function, then the same applies to $L ^ { \prime } : = \zeta ^ { - 1 } L$ , which makes $\zeta L = L ^ { \prime }$ . Thus without loss of generality let’s assume $\zeta = 1$ and denote $w ^ { \# } : = w _ { 1 } ^ { \# }$ .

Once the system’s state $X _ { k \delta }$ is in $B _ { s ^ { \ast } } \subset B _ { \| X _ { 0 } \| }$ , which is referred to as the core ball, the setting of $( U _ { k } , w _ { k } )$ is arbitrary. This is dictated by the fact that the optimization problem may become infeasible in a small vicinity of the origin due to noise and S&H behavior. Nor is one interested in what happens there as far as S&H-setting is concerned.

Let $v ^ { * } = \hat { \alpha } _ { \mathrm { l o w } } ( r )$ . At this point, note that for any $w \in \mathbb { W } .$

$$\hat {\alpha} _ {\text { low }} (\| x \|) \leq \hat {J} ^ {w} (x) \leq v ^ {*} \implies \| x \| \leq r$$

and also

$$\hat {\alpha} _ {\mathrm{up}} (s ^ {*}) \leq \hat {J} ^ {w} (x) \leq \hat {\alpha} _ {\mathrm{up}} (\| x \|) \implies \| x \| \geq s ^ {*},$$

which relate the value of $\hat { J }$ to the facts that $x \in B _ { r }$ r or $s \notin B _ { s ^ { * } }$ , respectively. It can be seen that, among other factors to be detailed later, the bounding functions $\hat { \alpha } _ { \mathrm { l o w , u p } }$ contribute to the radius of the target ball.

Finally, call an actor-critic sequence $( U _ { k \delta } , w _ { k } ) _ { k \in \mathbb { N } _ { \mathrm { C } } }$ admissible if, for any $k \in  { \mathbb { N } } _ { 0 } , (  { \mathbf { C } } 1 ) – (  { \mathbf { C } } 4 )$ are satisfied along the corresponding trajectory as long as $X _ { k \delta } \notin \mathcal { B } _ { s ^ { * } }$ .
