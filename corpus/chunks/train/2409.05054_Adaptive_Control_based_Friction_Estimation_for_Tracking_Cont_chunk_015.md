$$\min _ {a _ {j, i}, b _ {j, i}} \operatorname{cond} (\mathbf {Y} (\mathbf {q}, \dot {\mathbf {q}}, \ddot {\mathbf {q}})) \tag {3}\text { s.t. } q _ {j} (t) = \sum_ {i = 1} ^ {N} \frac {a _ {j , i}}{i w} \sin (i \omega t) - \frac {b _ {j , i}}{i w} \cos (i \omega t) + q _ {j} (0),q _ {j} ^ {-} (t) = q _ {j, \text { fixed }} ^ {-}, q _ {\min} < q _ {j} < q _ {\max}, \dot {q} _ {\min} < \dot {q} _ {j} < \dot {q} _ {\max},$$

with $j \in [ 1 , J ]$ , the joint index of the robot, $i \in [ 1 , N ]$ , the order for the fourier series, $\mathring { q _ { j } }$ the joints that stay at fixed positions to decouple the system dynamics during estimation, qmin, qmax, ˙qmin, $\dot { q } _ { \mathrm { m a x } } ,$ the joint position and the velocity limits, cond, the condition number, cond $( \mathbf { Y } ) = \frac { \sigma _ { \operatorname* { m a x } } ( \mathbf { Y } ) } { \sigma _ { \operatorname* { m i n } } ( \mathbf { Y } ) }$ σmax(Y) σmin(Y) , where $\sigma _ { \mathrm { m i n } }$ and $\sigma _ { \mathrm { m a x } }$ are the minimum and maximum singular values of the regressor matrix. Instead of minimizing the induced norm of the regressor matrix, we minimize the condition number of the regressor matrix, which has a similar effect. The optimization problem can be solved by sequential least square quadratic programming (SLSQP) or trust-region algorithms for constrained optimization (trust-constr).

Passivity is an inherent property of friction [19], to guarantee the passivity of the friction model, all parameters in the proposed friction model $\pi _ { f }$ must be positive. In this paper we simply clip the parameters $\hat { \pi } _ { f } = \mathrm { c l i p } ( \hat { \pi } _ { f } , 0 , + \infty )$ after each update to guarantee the passivity of the friction model.
