# 4.1. Continuity of the free-boundaries w.r.t. the mean-field term. In this subsection we establish some regularity of the free-boundaries with respect to the variable θ. For the subsequent analysis we introduce the following assumptions.

Assumption 4.1. The following hold.

(1) There exists κ : $\mathcal { T } \to \mathbb { R } _ { + }$ such that

$$\lim _ {\theta \downarrow 0} \pi_ {x} (x, \theta) = \infty , \quad \lim _ {\theta \uparrow \infty} \pi_ {x} (x, \theta) = \kappa (x), f o r a n y x \in \mathcal {I}$$

and, for some $\widehat { x } _ { - } : = ( \widehat { x } _ { - } ( 1 ) , . . . , \widehat { x } _ { - } ( d ) )$ and $\widehat { x } _ { + } : = ( \widehat { x } _ { + } ( 1 ) , . . . , \widehat { x } _ { + } ( d ) )$ , we have

$$
\kappa (x) + k _ {1} b _ {x} (x, i) \left\{ \begin{array}{l l} > 0, & x <   \widehat {x} _ {-} (i), \\ = 0, & x = \widehat {x} _ {-} (i), \\ <   0, & x <   \widehat {x} _ {-} (i), \end{array} \right. \quad \kappa (x) + k _ {2} b _ {x} (x, i) \left\{ \begin{array}{l l} > 0, & x <   \widehat {x} _ {+} (i), \\ = 0, & x = \widehat {x} _ {+} (i), \\ <   0, & x <   \widehat {x} _ {+} (i). \end{array} \right.
$$

(2) It holds that $b _ { x x } ( x , i ) \leq 0$ for any $( x , i ) \in \mathcal { O }$ , and, for any compact $\mathcal { X } \times \Theta \subset \mathcal { T } \times \mathbb { R } _ { + }$ , there exists a constant $C _ { ( \mathcal { X } , \Theta ) } > 0$ such that

$$\widehat {\mathbb {E}} _ {(x, i)} \left[ \int_ {0} ^ {\infty} e ^ {- c t} \big | \pi_ {x \theta} (\widehat {X} _ {t}, \theta) \big | d t \right] < C _ {(\mathcal {X}, \Theta)} < \infty ,$$

for any $( x , i , \theta ) \in \mathcal { X } \times \mathbb { Y } \times \Theta$ , with c as in Assumption 2.1.

(3) For any $i \in \mathbb { Y }$ we have $\textstyle \sum _ { j \in \mathbb { Y } } q _ { j i } \leq 0$ .

Remark 4.1. As an example, as in Remark 2.2 consider a profit function $\pi ( x , \theta ) = x ^ { \beta } ( \theta ^ { - ( 1 + \beta ) } + \kappa _ { * } )$ , for $\kappa _ { * } > 0 , \beta \in ( 0 , 1 )$ , and dynamics $b ( x , i ) = - \delta _ { i } x$ and $\sigma ( x , i ) = \sigma _ { i } x$ , for constants $\delta _ { i } , \sigma _ { i } > 0$ for any $i \in \mathbb { Y } .$ . By choosing $\kappa ( x ) = \kappa _ { * } \beta x ^ { \beta - 1 }$ , both Assumptions 2.2 and 4.1 are satisfied (see also Remark 2.2). Notice that the case $\kappa _ { * } = 0 f u l f i l s$ Assumption 3.1, but it does not satisfy Condition 1 in Assumption 4.1.

Lemma 4.1. For any $i \in \mathbb { Y } ,$ the maps $\theta \mapsto \alpha _ { i } ( \theta )$ and $\theta \mapsto \beta _ { i } ( \theta )$ are nonincreasing.
