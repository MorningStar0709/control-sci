Theorem 2. Consider a heterogeneous multi-agent system (19), where $( \breve { A } _ { i } , \breve { C } _ { i } )$ is detectable and $u _ { i }$ is bounded, $\forall i \in N .$ If the ??th agent implements observer dynamics (20), then it can produce an accurate state estimate for multi-agent system (19), i.e.,

$$\lim _ {t \to \infty} \| \hat {x} _ {i} (t) - x (t) \| = 0, \forall i \in \mathcal {N}.$$

Moreover, adaptive gains $\gamma _ { i }$ and $\gamma _ { i s }$ remain bounded, $\forall i \in N .$

This result implies that, in a heterogeneous multi-agent system, each agent is able to reconstruct not only its own state but also the states of all other agents, without requiring access to their control inputs or output measurements. Based on others’ real-time state information, each agent is omniscient and so can autonomously plan its own actions. Such capability can help a group of agents engage in collaborative tasks that are more complex than consensus-seeking.
