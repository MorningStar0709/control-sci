$$\tilde {\boldsymbol {T}} = \boldsymbol {T} (\boldsymbol {\theta}) - \boldsymbol {T} (\hat {\boldsymbol {\theta}}),
\tilde {\boldsymbol {N}} = \boldsymbol {N} \hat {\boldsymbol {N}} ^ {+} - \boldsymbol {I} = \left[ \begin{array}{l l} \boldsymbol {0} & \tilde {\boldsymbol {T}} ^ {T} \end{array} \right] \hat {\boldsymbol {N}} ^ {+} \tag {39}
\tilde {\boldsymbol {h}} = \bar {\boldsymbol {h}} - \hat {\boldsymbol {h}}= \tilde {\boldsymbol {T}} ^ {T} (\boldsymbol {h} _ {2} + \boldsymbol {M} _ {2} \hat {\boldsymbol {D}} \boldsymbol {\Lambda} \dot {\boldsymbol {x}}) + \boldsymbol {T} ^ {T} \boldsymbol {M} _ {2} \tilde {\boldsymbol {D}} \boldsymbol {\Lambda} \dot {\boldsymbol {x}},\tilde {M} = \bar {M} - \hat {M}= \left(\tilde {\boldsymbol {T}} ^ {T} \boldsymbol {M} _ {2} \boldsymbol {L} _ {2} \boldsymbol {T} + \hat {\boldsymbol {T}} ^ {T} \boldsymbol {M} _ {2} \boldsymbol {L} _ {2} \tilde {\boldsymbol {T}}\right) \boldsymbol {L} _ {1} ^ {- 1} \boldsymbol {\Lambda}.$$

If the velocity and acceleration commands are assumed bounded, i $\cdot \mathrm { e } .$ ,

$$\| \dot {\boldsymbol {x}} _ {d} \| \leq c _ {v} \quad \text { and } \quad \| \ddot {\boldsymbol {x}} _ {d} \| \leq c _ {a}, \tag {40}$$

and the estimators are convergent, then the perturbation term satisfies the following quadratic growth bound

$$\left\| \boldsymbol {g} (\boldsymbol {z}, t) \right\| \leq \left(\kappa_ {0} + \kappa_ {1} \| \boldsymbol {z} \| + \kappa_ {2} \| \boldsymbol {z} \| ^ {2}\right) \| \tilde {\boldsymbol {\theta}} (t) \|, \tag {41}$$

for some positive constants $\kappa _ { 2 } , \kappa _ { 1 }$ , and $\kappa _ { 0 } ;$ see the Appendix 3 for detailed derivations. Moreover, for convergent estimator it is reasonable to assume bounded estimation error, i.e.,

$$\sup _ {\tau \geq 0} \| \tilde {\boldsymbol {\theta}} (\tau) \| = r _ {\theta} \tag {42}$$

for $r _ { \theta } ~ < ~ \infty$ . Since a small initial parameter error $\tilde { \pmb { \theta } } ( 0 )$ results in small parameter error all the time [40, 45], there must exist scalar $\alpha > 0$ such that $\lVert \tilde { { \pmb \theta } } ( \tau ) \rVert \leq \alpha \lVert \tilde { { \pmb \theta } } ( 0 ) \rVert$ for all $\tau \geq 0$ . The remainder of this section proves that $z ( t )$ is ultimately bounded under the assumption on $r _ { \theta }$ being sufficiently small. Furthermore, if $\lVert \tilde { \pmb { \theta } } \rVert$ converges to zero, then so does $\| z ( t ) \|$ for sufficiently large t.
