# 3.2.2 P-CETC

Let $I ^ { p } = \{ t _ { 0 } ^ { p } , t _ { 1 } ^ { p } , t _ { 2 } ^ { p } , . . . \}$ denote the sequence of eventtimes associated with the $\mathrm { P \mathrm { - } C E T C ^ { 1 } }$ . Let the event-trigger parameters $\eta , \gamma , c , \beta _ { 1 } , \beta _ { 2 } , \rho > 0$ be selected as outlined in Section 3.2.1. The proposed P-CETC strategy consists of two components:

(1) An event-triggered boundary control input $U _ { j } ^ { p }$

$$U _ {j} ^ {p} = \int_ {0} ^ {1} k (y) u (y, t _ {j} ^ {p}) d y, \tag {46}$$

valid for all $t \in [ t _ { i } ^ { p } , t _ { i + 1 } ^ { p } ) , j \in \mathbb { N }$ . Accordingly, the boundary condition (3) becomes

$$u _ {x} (1, t) + q u (1, t) = U _ {j} ^ {p}. \tag {47}$$

(2) A dynamic event-trigger determining the event-times

$$t _ {j + 1} ^ {p} = \inf \left\{t \in \mathbb {R} _ {+} | t > t _ {j} ^ {p}, \Gamma^ {p} (t) > 0 \right\}, \tag {48}$$

with $t _ { 0 } ^ { p } = 0$ and $\Gamma ^ { p } ( t )$ defined as

$$\Gamma^ {p} (t) \triangleq d ^ {2} (t) - \gamma m ^ {p} (t) - \frac {c}{\rho} W ^ {p} (t). \tag {49}$$

Here $m ^ { p } ( t )$ satisfies

$$
\begin{array}{l} \dot {m} ^ {p} (t) = - \eta m ^ {p} (t) - \rho d ^ {2} (t) + \beta_ {1} \| u [ t ] \| ^ {2} + \beta_ {2} u ^ {2} (1, t) \\ + c W ^ {p} (t), \tag {50} \\ \end{array}
$$

for $t \in ( t _ { j } ^ { p } , t _ { j + 1 } ^ { p } ) , j \in \mathbb { N }$ , where $m ^ { p } ( t _ { 0 } ^ { p } ) = m ^ { r } ( t _ { 0 } ^ { r } ) > 0 $ , $m ^ { p } ( t _ { i } ^ { p - } ) = \dot { m ^ { p } } ( t _ { i } ^ { p } ) = m ^ { p } ( t _ { i } ^ { p + } )$ , d(t) is given by (25) for $t \in [ \bar { t _ { j } ^ { p } } , t _ { j + 1 } ^ { p } ) , j \in \mathbb { N } ,$ , and $\dot { W } ^ { p } ( t )$ is defined as

$$W ^ {p} (t) \triangleq e ^ {- b ^ {*} t} V _ {0} - V ^ {p} (t). \tag {51}$$

In (51), $V ^ { p } ( t )$ is defined as

$$V ^ {p} (t) \triangleq \frac {B}{2} \| w [ t ] \| ^ {2} + m ^ {p} (t), \tag {52}$$

with

$$V ^ {p} (0) = V ^ {r} (0) = V _ {0}, \tag {53}$$

w satisfying the target PDE (26)-(28) for t ∈ $[ t _ { j } ^ { p } , t _ { j + 1 } ^ { p } ) , \bar { { j } } \in \mathbb { N }$ , and $B \bar { > } 0$ chosen to satisfy (44). The exponential decay b∗ in (51) is defined as

$$b ^ {*} \triangleq \min \left\{\frac {2 b}{B}, \eta \right\} > 0, \tag {54}$$

with $b > 0$ given by

$$b = \frac {\varepsilon B}{4} - \beta_ {1} \tilde {L} ^ {2} - 2 \beta_ {2} \int_ {0} ^ {1} L ^ {2} (1, y) d y. \tag {55}$$

Here, $L ( x , y )$ ) satisfies (20)-(22), and L˜ is given by (45). Note from (44) that $b > 0$ .
