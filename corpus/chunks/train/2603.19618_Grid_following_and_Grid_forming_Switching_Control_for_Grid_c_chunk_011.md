$$
\left\{ \begin{array}{l} \dot {\boldsymbol {x}} _ {\sigma} = \boldsymbol {A} _ {\sigma} \boldsymbol {x} _ {\sigma} + \boldsymbol {B} _ {\sigma} \boldsymbol {u} _ {\sigma}, \sigma \in \{1, 2 \} \\ \left\{\boldsymbol {A} _ {1}, \boldsymbol {x} _ {1}, \boldsymbol {B} _ {1}, \boldsymbol {u} _ {1} \right\} \xrightarrow {\text { Switching   Laws }} \left\{\boldsymbol {A} _ {2}, \boldsymbol {x} _ {2}, \boldsymbol {B} _ {2}, \boldsymbol {u} _ {2} \right\} \end{array} \right. \tag {11}
$$

where $\sigma ~ \in ~ \{ 1 , 2 \}$ denotes the switching signal indicating the activated subsystem, which is governed by the designed switching strategy. For brevity, the detailed expressions for the system matrices $A _ { 1 } , B _ { 1 } , \mathrm { ~ \ r ~ { ~ A _ { 2 } ~ } ~ }$ and $B _ { 2 }$ are provided in our open-source document [29].
