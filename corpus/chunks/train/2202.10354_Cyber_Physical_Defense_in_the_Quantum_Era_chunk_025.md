The example is simple enough so that a certain number of cases can be highlighted. When probabilities $p$ and q tend to 0 one, it means that the adversary is more likely to behave as the agent. Inversely, when executed. In the tth execution instance, random action $p$ ands c $q$ tend to null, the adversary issen from set A. In curren likely to make a different choice from that of the agent. Such a bias can be explained by the existence of an insider that leakscausing the world to make a transition. World state s0 is observed, using a, s and s0 Bel information to the adversary when the agent makes its choice at point 0. In the former case, the agent is trapped in a riskyQt (s, a) is calculated. Using s, a, Θt and pt,s,a the variational circuit is updated and yields condition. In the latter case, the adversary is applying its worst possible strategy. WhenThe variational circuit is trained such that under input $p$ and q are both close to 50%, thete s , the measured output in adversary is behaving arbitrarily. On the long term, the most rewarding action for the agent is to take the loop. It is of courseprobability p . Training of the circuit can be done with a gradient descent optimizer. Th possible to update the policy according to a varying adversarial behavior, i.e., changing values for $p$ and $q .$ In following, we address this RL problem with a quantum approach.
