where $\Gamma ( \mu _ { 1 } , \dots , \mu _ { k } ) : = \left\{ \gamma \in { \mathcal { P } } ( X ) | ( \mathrm { p r o j } _ { X _ { i } } ^ { X } ) _ { \# } \gamma = \mu _ { i } , i \in \{ 1 , \dots , k \} \right\}$ . In general, the infima in (2.1) and (2.2) are not attained, unless mild conditions on the transportation cost hold true $( \mathrm { e . g . }$ , c lower semicontinuous in (2.1) [54, §4]). A transport plan $\gamma ^ { \varepsilon } \in$ $\Gamma ( \mu _ { 1 } , \dots , \mu _ { k } )$ is ε-optimal when

$$\int_ {X} c (x _ {1}, \ldots , x _ {k}) \mathrm{d} \boldsymbol {\gamma} ^ {\varepsilon} (x _ {1}, \ldots , x _ {k}) \leq \mathcal {K} [ c ] (\mu_ {1} \ldots , \mu_ {k}) + \varepsilon . \tag {2.3}$$

The formulation in (2.1) and (2.2) is the Kantorovich formulation of the optimal transport problem, whereby one optimizes over transport plans γ. The (stricter) Monge formulation1 considers only transport plans $\gamma = ( \operatorname { i d } _ { X _ { 1 } } , T ) _ { \# } \mu _ { 1 } \in \Gamma ( \mu _ { 1 } , \dots , \mu _ { k } )$ induced by a transport map $T : X _ { 1 } \to X _ { 2 } \times \dots X _ { k } , T _ { \# } \mu _ { 1 } = ( \mu _ { 2 } , \dots , \mu _ { k } )$ .

3. Problem Statement. Let $X _ { k } , ~ U _ { k } ,$ , and $R _ { k }$ be Polish spaces, representing the state space, the input space, and the space of references in the ground space, respectively (often, $R _ { k } \ = \ X _ { k } )$ . We consider dynamical systems whose state is a probability measure over $X _ { k }$ . This approach encompasses continuous approximations of multi-agent systems and systems with uncertain initial conditions (usually captured by absolutely continuous probability measures), as well as finite settings (captured by empirical probability measures).

Example 3.1 (Robots in a grid). Consider M robots in a grid of three cells; i.e., $X _ { k } = \{ \pm 1 , 0 \}$ . Suppose that the $i ^ { \mathrm { t h } }$ robot is located at $x _ { k } ^ { \left( i \right) } \in X _ { k } \ \mathrm { ~ ( i . e ~ }$ ., has state $x _ { k } ^ { ( i ) } )$ Then the state of the system is µk = 1M PMi=1 $\begin{array} { r } { \mu _ { k } = \frac { 1 } { M } \sum _ { i = 1 } ^ { M } \delta _ { x _ { k } ^ { ( i ) } } } \end{array}$ xk . The same modeling approach applies to M robots in the two-dimensional plane, simply setting $X _ { k } = \mathbb { R } ^ { 2 }$ .

In this setting, we focus on the following optimal control problem.
