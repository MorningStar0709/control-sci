$$
e _ {i} [ k ] = \left\{ \begin{array}{l l} 0 & \text { if } k = t _ {\ell} ^ {i} \text { for } \ell \in \mathbb {Z} _ {\geq 0}, \\ \bar {e} _ {i} [ k ] & \text { otherwise }, \end{array} \right.
$$

which implies that

$$e _ {i} ^ {\top} [ k ] \Omega_ {i} e _ {i} [ k ] \leq \sigma \hat {\phi} _ {i} [ k - 1 ], \quad \forall k \in \mathbb {Z} _ {\geq 1}, i \in \{1, \dots , N \}.$$

We also have

$$\sum_ {i = 1} ^ {N} \hat {\phi} _ {i} [ k ] = g _ {\mathrm{all}} (\hat {x} [ k ]), \tag {20}$$

where $g _ { \mathrm { a l l } } ( x ) : = x ^ { \top } .$ Sx and $\begin{array} { r } { S : = L \otimes Q + c ^ { 2 } L ^ { 2 } \otimes F ^ { \top } R F } \end{array}$

For $i \in \{ 2 , \ldots , N \}$ , define a matrix

$$
\begin{array}{l} \Gamma_ {i} := c ^ {2} \lambda_ {i} (L) ^ {2} F ^ {\top} B ^ {\top} P _ {i} B F \\ + \frac {1}{\varepsilon} c ^ {2} \lambda_ {i} (L) ^ {2} F ^ {\top} B ^ {\top} P _ {i} (A - c \lambda_ {i} (L) B F) \\ \times W _ {i} ^ {- 1} (A - c \lambda_ {i} (L) B F) ^ {\top} P _ {i} B F, \\ \end{array}
$$

and $\Gamma _ { U } : = ( U \otimes I _ { n } ) \mathrm { d i a g } ( 0 , \Gamma _ { 2 } , . . . , \Gamma _ { N } ) ( U ^ { \top } \otimes I _ { n } )$ . We also define

$$
\begin{array}{l} \alpha_ {S} := \lambda_ {\max} \left(\hat {\Omega} ^ {- 1 / 2} S \hat {\Omega} ^ {- 1 / 2}\right), \\ \alpha_ {S _ {u}} := \lambda_ {\max} \left(\hat {\Omega} ^ {- 1 / 2} S _ {u} \hat {\Omega} ^ {- 1 / 2}\right), \\ \alpha_ {\Gamma_ {U}} := \lambda_ {\max} \left(\hat {\Omega} ^ {- 1 / 2} \Gamma_ {U} \hat {\Omega} ^ {- 1 / 2}\right), \\ \end{array}
$$

where $\hat { \Omega } : = \mathrm { d i a g } ( \Omega _ { 1 } , \ldots , \Omega _ { N } )$ and $S _ { u } : = c ^ { 2 } L ^ { 2 } \otimes F ^ { \top } R F$

Then the following lemmas hold, which are used to establish the main results.

Lemma 2: Suppose that σ satisfies

$$0 < \sigma < \frac {1}{\alpha_ {S}}, \tag {21}$$

and choose a constant $\eta > 0$ such that

$$\eta > \frac {\sigma \alpha_ {S}}{1 - \sigma \alpha_ {S}}. \tag {22}$$

Then, it holds that

$$\sum_ {k = 0} ^ {\infty} e ^ {\top} [ k ] \hat {\Omega} e [ k ] \leq \beta \sum_ {k = 0} ^ {\infty} g _ {\text { all }} (x [ k ]),$$

where

$$\beta := \frac {\sigma (1 + \eta)}{1 - \sigma (1 + \eta^ {- 1}) \alpha_ {S}}. \tag {23}$$

Proof: For any $x , e \in \dot { \mathbb { R } } ^ { N n }$ , Young’s inequality gives

$$
\begin{array}{l} g _ {\text { all }} (x + e) = (x + e) ^ {\top} S (x + e) \\ \leq (1 + \eta) x ^ {\top} S x + \left(1 + \eta^ {- 1}\right) e ^ {\top} S e. \tag {24} \\ \end{array}
$$

By the definition of $\alpha _ { S }$ , and since $\hat { \Omega } ^ { - 1 / 2 } S \hat { \Omega } ^ { - 1 / 2 }$ is symmetric, it follows that $S \preceq \alpha _ { S } \hat { \Omega }$ . Hence,
