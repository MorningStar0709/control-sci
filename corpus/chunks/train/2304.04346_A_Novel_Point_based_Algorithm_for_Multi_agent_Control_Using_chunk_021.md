Building upon this idea, we make use of other types of linear constraints other than value function upper bounds, $\mathbf { e . g . , } \alpha ( s ) \ge v _ { \operatorname* { m i n } }$ where $\begin{array} { r } { v _ { \operatorname* { m i n } } : = \operatorname* { m i n } _ { s \in \mathcal { S } , a \in \mathcal { A } } r ( s , a ) / ( 1 - } \end{array}$ $\beta )$ . Adding this to (15), we have

$$U (b) = \max \{b ^ {T} y: y \in \mathbb {R} ^ {\mathcal {S}}, \mathbf {B} ^ {T} y \leq \bar {v}, y \geq v _ {\min} \mathbf {1} \}. \tag {16}$$

to be an upper bound no worse than the original one used in HSVI. In contrast to (15), the upper bound (16) can facilitate more cross learning: By bounding the $L _ { \infty }$ diameter of the constraint set, it can be shown that (16) is L1-Lipschitz continuous with constant $v _ { \mathrm { m a x } } - v _ { \mathrm { m i n } }$ (where $v _ { \operatorname* { m a x } } : = \operatorname* { m a x } _ { s \in \mathcal { S } , a \in \mathcal { A } } r ( s , a ) / ( 1 - \beta ) )$ , hence ensuring that if b is close to ${ \tilde { b } } ,$ then their upper bounds are relatively close as well, no matter whether ˜b is expressible with b or not. See Appendix B for more types of α-constraints.

In summary, any valid linear inequalities that are satisfied by all vectors in $\nu ^ { * }$ can be added to the set of α- constraints. This provides us a lot of flexibility compared to the convex hull-based bounds. We next describe the update of α-constraint based upper bound in extended coordinator’s POMDPs. The update method described in the next section is independent of the specific choice of α-constraints.
