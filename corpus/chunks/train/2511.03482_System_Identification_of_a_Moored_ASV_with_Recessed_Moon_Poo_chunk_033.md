# 1. Preprocessing.

• Identify the training window of length $l _ { t r }$ within the reference time series;   
• (optional) subsample time series;   
• apply z-scoring to ${ \bf { \bar { x } } } ( t )$ and ${ \bf \delta u } ( t )$
