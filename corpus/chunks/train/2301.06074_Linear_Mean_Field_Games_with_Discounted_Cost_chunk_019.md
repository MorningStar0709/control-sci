(e) Let $\begin{array} { r } { \mathsf { L } _ { F } : = \mathbb { L } _ { 1 } \left( \frac { \mathsf { L } _ { 1 } } { 1 - \beta \mathsf { K } _ { 1 } } , 0 , 0 \right) } \end{array}$ . Then define $\begin{array} { r } { \mathsf { L } ^ { * } : = \frac { 2 \mathsf { L } _ { F } } { \rho } } \end{array}$ . We assume that $\beta ( \mathsf { K } _ { 1 } + \mathsf { K } _ { 2 } \mathsf { L } ^ { * } +$ $\mathsf { K } _ { 3 } ) < 1$ . Moreover, we also assume that $\beta ( \mathsf { K } _ { 1 } + \mathsf { K } _ { 2 } \mathsf { L } _ { 2 } ^ { * } ) < 1$ , where

$$\mathsf {L} _ {2} ^ {*} := \frac {2 \mathbb {L} _ {2} (\mathsf {R} _ {1} , \mathsf {R} _ {2} , 0)}{\rho}, \mathsf {R} _ {1} := \frac {\mathsf {L} _ {1}}{1 - \beta \mathsf {K} _ {1}}, \mathsf {R} _ {2} := \frac {\mathsf {L} _ {3} (\mathsf {K} _ {1} + \mathsf {K} _ {2} \mathsf {L} ^ {*} + \mathsf {K} _ {3})}{1 - \beta (\mathsf {K} _ {1} + \mathsf {K} _ {2} \mathsf {L} ^ {*} + \mathsf {K} _ {3})}$$

3.1.2. Sufficient Conditions for Assumption (d). Assumption (d) is the most difficult assumption to verify. In this section, we provide some easy-to-check sufficient conditions in terms of p¯ and c¯ that imply assumption (d). To this end, we first assume that for any $( x , a , z ) \in { \mathsf { X } } \times { \mathsf { A } } \times { \mathsf { X } }$ , the transition probability $\bar { p } ( \cdot | x , a , z )$ is absolutely continuous with respect to $\mathsf { m } \in \mathscr { P } ( \mathsf { X } )$ . Let $\eta ( y , x , a , z )$ be the density function of $\bar { p } ( d y | x , a , z )$ with respect to m; that is

$$\bar {p} (d y | x, a, z) = \int_ {\{y \in d y \}} \eta (y, x, a, z) \mathsf {m} (d y)$$

for any $( x , a , z ) \in \mathsf { X } \times \mathsf { A } \times \mathsf { X }$ . We also assume that for any $( x , z ) \in \mathsf X \times \mathsf X , \bar { c } ( x , \cdot , z )$ is ρ-strongly convex and for any $( y , x , z ) \in \mathsf X \times \mathsf X \times \mathsf X , \eta ( y , x , \cdot , z )$ is convex. Moreover, $\nabla _ { a } \bar { c } ( x , a , z )$ is Lipschitz continuous in $( x , z )$ for all $a \in \mathsf { A }$ with Lipschitz constants $( \mathsf { L } _ { 1 } ^ { \mathsf { g } } , \mathsf { L } _ { 2 } ^ { \mathsf { g } } )$ and $\nabla _ { a } \eta ( y , x , a , z )$ is Lipschitz continuous in $( x , z )$ for all $( y , a ) \in \mathsf { X } \times \mathsf { A }$ with Lipschitz constants $( { \sf K } _ { 1 } ^ { \sf g } , { \sf K } _ { 2 } ^ { \sf g } )$ . Then, assumption (d) holds with the following function L

$$\mathbb {L} \left(R _ {1}, R _ {2}, R _ {3}\right) := \left(\mathbb {L} _ {1} \left(R _ {1}, R _ {2}, R _ {3}\right), \mathbb {L} _ {2} \left(R _ {1}, R _ {2}, R _ {3}\right)\right)$$

where
