# 1.5 Notation

R is the field of reals; $\mathbb { R } _ { \mathbf { 0 } } ^ { n } = \mathbb { R } ^ { n } \backslash \{ \mathbf { 0 } \}$ , where 0 is the zero element of a vector space $( \mathrm { e . g . } , \mathbf { 0 } \in \mathbb { R } ^ { n }$ means that 0 is the zero vector); ∥·∥ is a norm in $\mathbb { R } ^ { n }$ (to be specified later); the matrix norm for $A \in \mathbb { R } ^ { n \times n }$ is defined as $\begin{array} { r } { \| A \| = \operatorname* { s u p } _ { x \neq \mathbf { 0 } } \frac { \| A x \| } { \| x \| } ; \lambda _ { \operatorname* { m i n } } ( P ) } \end{array}$ ∥Ax∥∥x∥ ; λmin(P ) denote a minimal eigenvalue of a symmetric matrix $P = P ^ { \top } \in$ Rn×n; $P \succ 0$ means that the symmetric matrix P is positive definite; $C ( \Omega _ { 1 } , \Omega _ { 2 } )$ denotes the set of continuous functions $\Omega _ { 1 } \subset \mathbb { R } ^ { n } \mapsto \Omega _ { 2 } \subset \mathbb { R } ^ { m } ; C _ { c } ( \mathbb { R } ^ { n } , \mathbb { R } )$ denotes the set of compactly supported continuous functions; $L ^ { p } ( \mathbb { R } ^ { n } , \mathbb { R } )$ is the Lebesgue space.
