# 9.2.3 通过非线性逆的线性化

把非线性引入控制设计的最简单的情况就是求逆非线性。有时将某些非线性的作用反过来是可能的。比如，假设有一个系统的输出为我们所关注信号的平方：

$$y = x ^ {2} \tag {9.18}$$

一种聪明且显然的方法就是在此物理非线性之前串联一个平方根非线性来抵消它，即

$$x = \sqrt {(\bullet)} \tag {9.19}$$

正如下面的例子所示。整个级联系统将会变为线性的。
