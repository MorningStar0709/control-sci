Fig. 5. Overall simulation block diagram, which contains the high fidelity nonlinear model of quarter car and actuator (‘QC SAVGS’ and ‘Single-link actuator’, respectively, where $\omega _ { S L } ^ { * }$ is the reference of $\omega _ { S L }$ tracked by the actuator) and the proposed coupled closed-loop control scheme. The ‘Online RMPC Scheme’ uses the linear equivalent uncertain quarter car model and initial feasible sets $( Y ^ { * } , \tilde { Y } ^ { * } )$ based on offline created lookup tables. The first four elements of the state x as defined in the subsection II-B are measurable at the output of the high-fidelity model and the last state component $( \Delta z _ { l i n } ) .$ which is also used as input to the PI controller, is computed through the function β and the measurable output $\Delta \theta _ { S L }$ of the high fidelity model, as explained in subsection $\mathrm { { I V } } { \cdot } \mathrm { { C } } .$ .
