$$\mathrm{d} \big (e (t) e ^ {T} (t) \big) = (\mathrm{de} (t)) e ^ {T} (t) + e (t) (\mathrm{de} (t)) ^ {T} + (\mathrm{de} (t)) (\mathrm{de} (t)) ^ {T}. \tag {30}$$

Taking $\mathbb { E } [ \cdot \mid \mathcal { F } _ { t } ^ { y } ]$ on both sides yields

$$\mathrm{d} P (t) = \mathrm{d} R _ {t} + \mathrm{d} S _ {t} + \mathrm{d} Q _ {t}, \tag {31}$$

where $\mathrm { d } R _ { t } = \mathbb { E } [ ( \mathrm { d } e ( t ) ) e ^ { T } ( t ) ~ | \mathcal { F } _ { t } ^ { y } ] , \mathrm { d } S _ { t } = \mathbb { E } [ e ( t ) ( \mathrm { d } e ( t ) ) ^ { T }$ $| \mathbf { \nabla } \mathcal { F } _ { t } ^ { y } ] , \mathrm { d } Q _ { t } = \bar { \mathbb { E } } \big [ ( \mathrm { d } e ( t ) ) ( \mathrm { d } e ( t ) ) ^ { T } | \mathcal { F } _ { t } ^ { y } \big ]$ . Using (29) and $\mathbb { E } [ \mathrm { d } W ( \cdot ) \mid \mathcal { F } _ { t } ^ { y } ] = \mathbf { \bar { 0 } }$ ,

$$\mathrm{d} R _ {t} = \left((A - \mathcal {G} (t) C) P (t) + \left(A _ {d} - \mathcal {G} (t) C _ {d}\right) P _ {1} ^ {T} (t)\right) \mathrm{d} t, \tag {32}\mathrm{d} S _ {t} = \left(P (t) \left(A - \mathcal {G} (t) C\right) ^ {T} + P _ {1} (t) \left(A _ {d} - \mathcal {G} (t) C _ {d}\right) ^ {T}\right) \mathrm{d} t. \tag {33}$$

Since Wiener increments satisfy $\mathrm { d } W ( t ) \mathrm { d } W ^ { T } ( t ) = I _ { m }$ dt and $\mathrm { d } W ( t )$ is independent of $\mathrm { d } \mathsf { \check { W } } ( t - \mathsf { \check { T } } )$ for $\dot { T } > 0 .$ , the cross-variation between the two noise channels vanishes. Therefore,

$$\mathrm{d} Q _ {t} = \left(B B ^ {T} + \left(B _ {d} - \mathcal {G} (t) D _ {d}\right) \left(B _ {d} - \mathcal {G} (t) D _ {d}\right) ^ {T}\right) \mathrm{d} t. \tag {34}$$

Combining $\mathrm { d } R _ { t } , \mathrm { d } S _ { t }$ , and $\mathrm { d } Q _ { t }$ in (31) gives (28).

Remark 5. From Theorem 4, it can be observed that when the current time t is less than the time delay T , d $| P ( t )$ can be expressed directly. However, when t exceeds $T ,$ the expression of d $P ( \dot { t } )$ depends both on the information of $\bar { P } _ { 1 } ( t )$ and $P ( t )$ . Consequently, determining how to compute $P _ { 1 } ( t )$ for $t \geq T$ , and then successively derive $P _ { j } ( t )$ for $t \geq j T$ , becomes a crucial step in designing the filter.

Next, we present the derivation for calculating the delay state covariance matrix.

Theorem 5. Given the linear quantum stochastic delay system (20), the filter (22), $\dot { P ( 0 ) } = \mathbb { E } [ e ( 0 ) e ^ { T } ( 0 ) ]$ and $P _ { j } ( t ) = \dot { 0 }$ for $t < j T$ , define for $j \in \mathbb { N } ^ { + }$
