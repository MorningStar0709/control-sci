$$\max _ {s \in [ 0: s _ {e} ]} \epsilon_ {s} \leq c _ {\mathrm{L15}} T ^ {- 1 / 4} = \tilde {O} _ {T} (T ^ {- 1 / 4}).$$

The proof of Lemma 2 in Schiffer and Janson [2024] relies only on the first $1 / \nu _ { T } ^ { 2 }$ steps and is written agnostic to the choice of $\nu _ { T }$ , and therefore the result of Lemma 15 follows directly from that proof. Note that we explicitly named the constant in Lemma 15 as we will use this constant later in the proof. For the rest of this section, define

$$E _ {2} := E _ {0} \bigcap \left\{\max _ {s \in [ 0: s _ {e} ]} \epsilon_ {s} \leq c _ {\mathrm{L} 1 5} T ^ {- 1 / 4} = \tilde {O} _ {T} (T ^ {- 1 / 4}) \right\}. \tag {54}$$

Lemma 15 implies that we have

$$\mathbb {P} (E _ {2}) = 1 - o _ {T} (1 / T ^ {2}).$$

Define

$$E _ {2} ^ {0} := \{\epsilon_ {0} \leq c _ {\mathrm{L15}} T ^ {- 1 / 4} \} \cap \{\| \theta^ {*} - \hat {\theta} _ {0} ^ {\mathrm{pre}} \| _ {\infty} \leq \epsilon_ {0} \} \subseteq E _ {2}.$$

Recall $\hat { \theta } _ { \mathrm { w u } }$ , which is defined in Line 3 of Algorithm 2. Because $\widehat { \theta } _ { \mathrm { w u } } = \widehat { \theta } _ { 0 } ^ { \mathrm { p r e } }$ , by the same logic as above, under $E _ { 2 } ^ { 0 }$ we have that $\lVert \boldsymbol { \theta } ^ { * } - \hat { { \boldsymbol { \theta } } } _ { \mathrm { w u } } \rVert _ { \infty } \leq 2 \epsilon _ { 0 } \leq 2 c _ { \mathrm { L 1 5 } } T ^ { - 1 / 4 }$ .

Define $E _ { 1 }$ as

$$E _ {1} = \left\{\forall t < T: | w _ {t} | \leq \log^ {2} (T) \right\}. \tag {55}$$

and $E _ { \mathrm { s a f e } }$ as the following, where $\boldsymbol { x } _ { t } ^ { \prime }$ and $u _ { t } ^ { \prime }$ are the states and controls respectively of the algorithm:

$$E _ {\text { safe }} = \left\{\forall t < T: D _ {\mathrm{L}} \leq a ^ {*} x _ {t} ^ {\prime} + b ^ {*} u _ {t} ^ {\prime} \leq D _ {\mathrm{U}} \right\}, \tag {56}$$

Finally, we define the event

$$E = E _ {1} \cap E _ {2} \cap E _ {\mathrm{safe}}.$$

By a union bound we have that $\mathbb { P } ( E ) = 1 - o _ { T } ( 1 / T ^ { 2 } )$ . Using this new notation and Lemma 15, we can proceed to the main proof.

The desired safety of $C ^ { \mathrm { a l g } }$ follows from the following lemma:

Lemma 16. Under Assumptions 1–3 , Algorithm 2 is safe for T steps for dynamics $\theta ^ { * }$ with probability $1 - o _ { T } ( 1 / T ^ { 2 } )$ .
