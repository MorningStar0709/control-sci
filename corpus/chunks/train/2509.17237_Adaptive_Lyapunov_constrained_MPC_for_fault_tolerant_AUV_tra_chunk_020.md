$$V \left(F \left(\boldsymbol {\xi} _ {k}, \boldsymbol {u} ^ {\text { blend }}\right)\right) - V \left(\boldsymbol {\xi} _ {k}\right) \leq - \alpha \| \tilde {\boldsymbol {x}} _ {k} \| ^ {2} + L _ {u} \| \boldsymbol {u} ^ {\text { blend }} - \hat {\boldsymbol {u}} \|, \tag {35}$$

for some $L _ { u } \ > \ 0$ depending on local model bounds. Since $\Pi _ { \mathcal { U } }$ is a nonexpansive projection and rate limits bound $\lVert \boldsymbol { \mathbf { u } } ^ { \mathrm { b l e n d } } - \boldsymbol { \hat { \boldsymbol { u } } } \rVert .$ , the contraction margin remains negative provided $L _ { u } \parallel \mathbf { \boldsymbol { u } } ^ { \mathrm { b l e n d } } - \hat { \mathbf { \boldsymbol { u } } } \parallel \leq \alpha ^ { \prime } \| \tilde { \mathbf { \boldsymbol { x } } } _ { k } \| ^ { 2 }$ for a chosen $\alpha ^ { \prime } \in ( 0 , \alpha )$ . In practice, this is ensured by the shared auxiliary backstepping rate embedded in (32), and conservative rate limits, so recursive feasibility and closed-loop stability are maintained during probability evolution. To reduce computation without affecting stability, one may temporarily switch to the most–probable controller when $p _ { m } ( k ) \geq p _ { \mathrm { o n } }$ for $N _ { \mathrm { o n } }$ samples, and resume blending only if $p _ { m } ( k ) ~ \leq ~ p _ { \mathrm { o f f } } ~ < ~ p _ { \mathrm { o n } }$ for $N _ { \mathrm { o f f } }$ samples. Anomaly-triggered “unlocking” can be used to avoid probability lock-in under new faults.
