$$
\begin{array}{l} O \prec \left[ \begin{array}{c c} G ^ {- \top} + G ^ {- 1} - G ^ {- \top} Q G ^ {- 1} & * \\ G ^ {- \top} (A + B K) & G ^ {- \top} Q G ^ {- 1} \end{array} \right] \\ = \left[ \begin{array}{c c} E ^ {\top} (\tilde {G} ^ {- \top} + \tilde {G} ^ {- 1} - \tilde {G} ^ {- \top} \tilde {Q} \tilde {G} ^ {- 1}) E & * \\ E ^ {\top} \tilde {G} ^ {- \top} E (A + B K) & E ^ {\top} \tilde {G} ^ {- \top} \tilde {Q} \tilde {G} ^ {- 1} E \end{array} \right] \\ = \left[ \begin{array}{c c} E ^ {\top} O \\ O & E ^ {\top} \end{array} \right] \left[ \begin{array}{c c} \tilde {G} ^ {- \top} + \tilde {G} ^ {- 1} - \tilde {G} ^ {- \top} \tilde {Q} \tilde {G} ^ {- 1} & * \\ \tilde {G} ^ {- \top} (\tilde {A} + \tilde {B} \tilde {K}) & \tilde {G} ^ {- \top} \tilde {Q} \tilde {G} ^ {- 1} \end{array} \right] \left[ \begin{array}{c c} E & O \\ O & E \end{array} \right] \\ \end{array}
$$

By using Finsler’s lemma, we obtain

$$
O \prec \left[ \begin{array}{c c} \tilde {G} ^ {- \top} + \tilde {G} ^ {- 1} - \tilde {G} ^ {- \top} \tilde {Q} \tilde {G} ^ {- 1} & * \\ \tilde {G} ^ {- \top} (\tilde {A} + \tilde {B} \tilde {K}) & \tilde {G} ^ {- \top} \tilde {Q} \tilde {G} ^ {- 1} \end{array} \right] + \nu W \tag {20}
$$

for some $\nu \in \mathbb { R }$ . Since $\tilde { G } U = U \tilde { G } \Leftrightarrow \tilde { H } W = W \tilde { H }$ and $W = W ^ { 2 }$ , we obtain

$$
\begin{array}{l} (2 0) \Leftrightarrow O \prec \Psi (\tilde {G}, \tilde {Q}, \tilde {Z}) + \nu \tilde {H} ^ {\top} W ^ {2} \tilde {H} \\ = \Psi (\tilde {G}, \tilde {Q}, \tilde {Z}) + \nu W \tilde {H} ^ {\top} \tilde {H} W \\ \preceq \Psi (\tilde {G}, \tilde {Q}, \tilde {Z}) + \rho W, \\ \end{array}
$$

for $\rho ~ =$ max(ν $\mathrm { e i g } ( \tilde { H } ^ { \top } \tilde { H } ) )$ . Moreover, $\tilde { G } ^ { \top } U + U \tilde { G } \ =$ $\tilde { G } ^ { \top } \dot { U } ^ { 2 } \ : + \ : U ^ { 2 } \tilde { G } = \ : \tilde { U } ( \tilde { G } ^ { \top } \ : + \ : \tilde { G } ) U \ : \succeq \ : \mu U$ holds, where $\mu$ is the minimum or maximum eigenvalue of $\tilde { G } ^ { \top } + \tilde { G }$ . This satisfies the LMI in (18). Therefore, we obtain $K \in \mathcal { K } _ { S , \mathrm { e x t } }$ and $\mathcal { K } _ { \mathrm { e x t } } \subset \mathcal { K } _ { S , \mathrm { e x t } }$ .
