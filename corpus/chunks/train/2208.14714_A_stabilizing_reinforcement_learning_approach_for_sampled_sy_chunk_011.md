# C. Sample-and-Hold Framework

Consider the following sample-and-hold (SH) form of (1)

$$
\begin{array}{l} \dot {x} = \mathcal {F} (x, u _ {k}, \theta), \quad x (0), \\ \text {i.e.} [ h f _ {1} (h + 1) f) \end{array} \tag {10}
t \in [ k \delta , (k + 1) \delta), u _ {k} = \kappa (x (k \delta)), k \in \mathbb {N} _ {0}.$$

At any $k \in  { \mathbb { N } } _ { 0 }$ and sampling period $\delta > 0$ , the state trajectory $x ( t ; u ) , t \in [ 0 , \delta )$ , under any $u \in \mathbb { R } ^ { m }$ is defined as

$$x (t; u) = x _ {k} + \int_ {0} ^ {t} \mathcal {F} (x (k \delta + s), u, \theta) d s. \tag {11}$$

Denote $x _ { k } : = x ( k \delta )$ and $x _ { k + 1 } ( u ) : = x ( ( k { + } 1 ) \delta ; u )$ , for brevity and the corresponding trajectory to (10) will be referred to as the SH-trajectory.

Definition 2: A control policy $\kappa ( \cdot )$ is said to practically semi-globally stabilize (1) if, given $0 < r < R < \infty$ , there exists a $\bar { \delta } \ > \ 0$ such that any SH-trajectory of (10) with sampling time $0 < \delta \leq \bar { \delta } .$ , starting in $B _ { R } .$ is bounded, enters $\boldsymbol { B } _ { r }$ after a time T , which depends uniformly on $R , r ,$ and remains there for all $t \geq T$ .

In the sampled setting, the next section introduces the model-based learning approach to be utilized herein.
