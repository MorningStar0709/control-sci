# A. Notation

For a vector ?? and a matrix ??, k??k and k?? k denote the Euclidean norm and the induced 2-norm, respectively. Let Im?? denote the range or image of ??. $\mathrm { R e } \lambda ( X ) < 0$ means that all eigenvalues of ?? lie in the open left half of the complex plane. The annihilator of ?? is a real matrix, whose row vectors are a basis of the left null space of ??. ?? and 0 denote respectively the identity matrix and zero matrix of appropriate dimensions, whose subscripts are omitted when it causes no ambiguity. $1 _ { r }$ is a column vector with all ?? entries equal to one. For a set of scalars or matrices $\{ X _ { i } | \ i = 1 , 2 , \cdots , N \}$ , define diag $( X _ { i } ) _ { i = 1 } ^ { N }$ as a matrix formed by arranging the matrices in a block diagonal fashion, and col $( X _ { i } ) _ { i = 1 } ^ { N }$ as a matrix formed by stacking them (i.e., $\Big [ X _ { 1 } ^ { \top } X _ { 2 } ^ { \top } \cdot \cdot \cdot X _ { N } ^ { \top } \Big ] ^ { \top } )$ if the dimensions match.
