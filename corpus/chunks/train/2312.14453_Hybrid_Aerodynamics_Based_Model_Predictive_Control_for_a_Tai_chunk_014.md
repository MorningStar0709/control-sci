where ${ \bf \delta u } ( t )$ and ${ \bf x } ( t )$ denote control inputs and states at timestep $t ,$ respectively; $t _ { 0 }$ and $t _ { N }$ denote the first and last timesteps of the prediction horizon, respectively; $\mathbf { y } _ { \mathrm { r e f } }$ and $\mathbf { y } _ { N , \mathrm { r e f } }$ denote the reference state for the prediction horizon and the terminal time-step, including desired position, velocity, attitude and control inputs; Q and $\mathbf { Q } _ { N }$ represent weighting matrices for states and terminal states, respectively; $f ( \cdot )$ denotes the prediction function, and $h ( \cdot )$ denotes the system output function; and U and X denote the input constraint and state constraint, respectively.

The OCP presented in (7) solved using multiple shooting method. A boundary value problem (BVP) is formulated with the discretized system and solved by employing the active set method in qpOASES solver [12], [26].
