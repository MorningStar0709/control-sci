2) Load characteristics: The load behavior in real systems is often complex, with some dynamic loads introducing rotational dynamics (e.g., induction motor loads), which further complicates overall system analysis. When the system frequency is close to the steady-state value, the phasor-based analytical model proposed in this paper can capture the characteristics of loads at both power flow and dynamic levels.   
3) Dimensionality of multi-machine systems: As the number of devices increases, the dimensionality of the system’s state space also expands. For example, in a system with m sources, the operating region of relative power angles becomes (m-1)-dimensional. Furthermore, if each device is equipped with k control strategies, the total number of control combination regions reaches k × m. In this paper, control combination regions and stable/unstable equilibrium points are modeled as sets, and through high-dimensional mappings among these sets, the proposed method remains applicable to multi-machine systems to a certain extent.

![](image/d5673c5ed6e57f65057bad690e0af8330dd418e004a67d1c90b8e887ddf8b4c5.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    A["SENDING-END SYSTEM"] --> B["HVDC"]
    B --> C["GFM/GFL CONTROL STRATEGIES"]
    C --> D["RECEIVING-END SYSTEM"]

subgraph A
        E["HYDRO-ELECTRIC POWER PLANT"] --> F["Power Limiting"]
        F --> G["Current Saturation"]
        G --> H["Virtual Impedance"]
        H --> I["Droop"]
        I --> J["VSG"]
        J --> K["dVOC"]
        K --> L["Current Magnitude Control"]
        L --> M["Current Phase Control"]
    end

subgraph B
        N["ENERGY STORAGE SYSTEM"] --> O["Power Limiting"]
        O --> P["Current Saturation"]
        P --> Q["Virtual Impedance"]
        Q --> R["Droop"]
        R --> S["VSG"]
        S --> T["dVOC"]
        T --> U["Current Magnitude Control"]
        U --> V["Current Phase Control"]
    end

subgraph C
        W["HELVDC Power Limiting"] --> X["Power Limiting"]
        X --> Y["Current Saturation"]
        Y --> Z["Virtual Impedance"]
        Z --> AA["Droop"]
        AA --> AB["VSG"]
        AB --> AC["dVOC"]
        AC --> AD["Current Magnitude Control"]
        AD --> AE["Current Phase Control"]
    end

subgraph D
        AF["RECEIVING-END SYSTEM"] --> AG["GFM-RESs"]
        AG --> AH["LOAD"]
        AH --> AI["Equivalent Mutual Admittance"]
        AH --> AJ["Equivalent Self Admittance"]
    end

subgraph E
        AK["SCHALLENGE 1"] --> AL["Synchronization Loop"]
        AL --> AM["Outer Voltage/Current Loop"]
        AM --> AN["Power Flow Equations"]
    end
