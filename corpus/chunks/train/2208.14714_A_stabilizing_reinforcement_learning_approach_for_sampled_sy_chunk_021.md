Proposition 1: Let $I \subset \{ 1 , \ldots , n \} , \bar { d } = n - I \in [ 0 , n ]$ and $x _ { \{ I \} }$ be the state to an I-dimensional subsystem of (1) such that x can be written as $x = [ x _ { \{ I \} } x _ { \{ \bar { d } \} } ] ^ { \top }$ . Let Assumption 1–3 hold and $\bar { d } > 0$ . If the { ¯d}-subsystem is ISS w. r. t. to the {I}- subsystem, then Theorem 1 applies to the {I}-subsystem, i. e., the set $\underline { { \boldsymbol { B } } } _ { r } : = \{ \boldsymbol { x } \in \mathbb { R } ^ { n } : \| \boldsymbol { x } _ { \{ I \} } \| \leq r \} \subset \mathrm { \bar { R } } ^ { \bar { n } - \bar { d } }$ is practically stabilized, while $x _ { \{ \bar { d } \} }$ remains bounded.
