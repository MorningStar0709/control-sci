# 6 Numerical experiments

The proposed approach is evaluated by numerical examples in this section. The first example considers a finitehorizon optimal control problem, while the second example considers an infinite-horizon problem with noisy measurements.
