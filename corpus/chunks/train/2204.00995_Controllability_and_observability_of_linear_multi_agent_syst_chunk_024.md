Continue to expand the above matrix and add some column vectors, then

$$\left[ \tilde {M}, \tilde {L} _ {1} \tilde {M} + \dots + \tilde {L} _ {t} \tilde {M}, \tilde {L} _ {2} \tilde {M}, \ldots , \tilde {L} _ {t} \tilde {M}, \ldots , \tilde {L} _ {1} ^ {d N - 1} \tilde {M} + \tilde {L} _ {1} ^ {d N - 2} \tilde {L} _ {2} \tilde {M} + \dots + \tilde {L} _ {t} ^ {d N - 1} \tilde {M}, \tilde {L} _ {1} ^ {d N - 1} \tilde {L} _ {2} \tilde {M}, \ldots , \tilde {L} _ {t} ^ {d N - 1} \tilde {M} \right]$$

Because the basic transformation of the matrix does not change the rank of the matrix, the above matrix can be transformed into

$$\left[ \tilde {M}, \tilde {L} _ {1} \tilde {M}, \dots , \tilde {L} _ {t} \tilde {M}, \tilde {L} _ {1} ^ {2} \tilde {M}, \dots , \tilde {L} _ {1} ^ {d N - 1} \tilde {M}, \tilde {L} _ {1} ^ {d N - 1} \tilde {L} _ {2} \tilde {M}, \dots , \tilde {L} _ {t} ^ {d N - 1} \tilde {M} \right]$$

which is full row rank. According to Lemma 4, system (2) is controllable.

Remark 10. Theorem 4 is only a sufficient condition, that is, when the system (4) is uncontrollable, the system (2) can also be controllable. See example 4 in Section 7 for more details.

From Theorem 4 and its examples, the controllability of system (2) cannot be determined when system (4) is uncontrollable only by above results. Therefore, we need to further analyze more specific cases. Inspired by Theorem 2, the following results are obtained

Theorem 5. Suppose the matrix 𝑄 always exists for all 𝐿̃ corresponding to 𝐺 , then the union graph is uncontrollable if any subgraph in the switching sequence contains nontrivial cells.

PROOF. If a proposition holds, so does its converse negative proposition. The converse negative proposition of Theorem 4 is that system (4) is uncontrollable if system (2) is uncontrollable. Theorem 2 shows that the dimension of the controllable subspace of system (2) is less than 𝑑𝑁 if any subgraph in the switching sequence contains nontrivial cells. It means that the system (2) is uncontrollable, so we can get that the union graph is also uncontrollable.

Remark 11. Theorem 4 gives a special class of graphs. For this kind of graph, when system (4) is uncontrollable, system (2) is also uncontrollable. It avoids the uncertainty of previous results. At the same time, a new method to identify the controllability of the union graph is also given, which simplifies the process of identifying the controllability of the union graphs.
