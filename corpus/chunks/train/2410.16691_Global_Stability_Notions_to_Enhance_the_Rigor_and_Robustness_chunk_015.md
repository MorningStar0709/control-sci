where $K : = \sqrt { \xi _ { 0 } ^ { 2 } + ( k + z _ { 0 } ) ^ { 2 } }$ . It is clearly shown by the above formulas that the convergence rate is state-dependent. Notice that convergence can happen at a rate that may slow towards zero rate. Indeed, when $z _ { 0 } = - k$ then $\xi ( t ) = \frac { 2 \xi _ { 0 } \exp \left( - | \xi _ { 0 } | t \right) } { 1 + \exp \left( - 2 | \xi _ { 0 } | t \right) }$ , which shows that the convergence rate tends to zero as $\xi _ { 0 }$ tends to zero. Moreover, following the arguments in [17], we are able to show that system (24) with $\sigma = 0$ is only GAOS, and not UGAOS. We repeat the argument here: when $\xi _ { 0 } > 0$ and $z _ { 0 } + k < 0$ , it holds that $\xi \left( t ^ { * } \right) = \operatorname* { m a x } _ { t \ge 0 } \left( \xi ( t ) \right) = \sqrt { \xi _ { 0 } ^ { 2 } + { ( k + z _ { 0 } ) } ^ { 2 } }$ for $t ^ { \ast } = \frac { 1 } { 2 \sqrt { \xi _ { 0 } ^ { 2 } + \left( k + z _ { 0 } \right) ^ { 2 } } } \ln \left( \frac { \sqrt { \xi _ { 0 } ^ { 2 } + \left( k + z _ { 0 } \right) ^ { 2 } } - z _ { 0 } - k } { \sqrt { \xi _ { 0 } ^ { 2 } + \left( k + z _ { 0 } \right) ^ { 2 } } + z _ { 0 } + k } \right) > 0$ . If system (24) with $\sigma = 0$ were UGAOS, then there would exist $T > 0$ such that for all $\boldsymbol { x } _ { 0 } = \left( \xi _ { 0 } , z _ { 0 } \right) \in \mathbb { R } ^ { 2 }$ with $\left. x _ { 0 } \right. \leq \sqrt { 1 + \left( k + 1 \right) ^ { 2 } }$ it holds that $\left| y ( t , x _ { 0 } ) \right| \leq \frac { 1 } { 2 }$ for all $t \geq T$ $x _ { 0 , i } = \left( { \frac { 1 } { i } } , - k - 1 \right) \in \mathbb { R } ^ { 2 }$ with $i \geq 1$ being an integer, satisfies $\left| x _ { 0 , i } \right| \leq \sqrt { 1 + \left( k + 1 \right) ^ { 2 } }$ and $y \left( t _ { i } ^ { * } , x _ { 0 , i } \right) = \xi \left( t _ { i } ^ { * } \right) = \sqrt { 1 + i ^ { - 2 } } > \frac { 1 } { 2 }$ for $t _ { i } ^ { * } = \frac { 1 } { 2 \sqrt { 1 + i ^ { - 2 } } } \ln \left( 1 + 2 i ^ { 2 } \left( \sqrt { 1 + i ^ { - 2 } } + 1 \right) \right) \geq \frac { \ln \left( 1 + 4 i ^ { 2 } \right) } { 2 \sqrt { 2 } }$ 2√+ . Consequently, we would get $T \geq t _ { i } ^ { * } \geq \frac { \ln \left( 1 + 4 i ^ { 2 } \right) } { 2 \sqrt { 2 } }$ for all integers $i \geq 1$ ; a contradiction. Thus, system (24) with $\sigma = 0$ is not UGAOS.

The example is continued below.

The counterexample in [17] showed that the existence of functions $V \in C ^ { 1 } ( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } )$ with $V ( 0 ) = 0$ $a , c \in K _ { \infty }$ for which (15) holds and for which the following inequality holds for all $x \in \mathbb { R } ^ { n }$

$$\dot {V} (x) = \nabla V (x) f (x) \leq - c \left(\left| h (x) \right|\right) \tag {27}$$
