Estimating the computational complexity of the proposed method, it should be noted that its upper bound is defined by the complexity of the most computationally intensive operations. They include the calculation of the ?????? $\left\{ \overline { { \varphi } } _ { f } ( t ) \right\}$ and ?????? $\left\{ \overline { { \varphi } } _ { f } ( t ) \right\}$ (this regressor has the highest dimension), as well as matrix multiplication (the matrices $z _ { D } ( t )$ and $z _ { \Phi } ( t )$ have the highest dimension of $2 n \times 2 n )$ . Considering the first two operations, their complexity is $O \left( ( n + m + 1 ) ^ { 3 } \right)$ , for square matrices multiplication $- O \left( ( 2 n ) ^ { 3 } \right)$ . For non-square matrices such estimation is a bit different, but without losing generality, we can assume that 2?? is the greatest value among all dimensions, then the estimation $O \left( ( 2 n ) ^ { 3 } \right)$ can be used for non-square matrices as a majorant. Thus, the upper bound of the computational complexity of the proposed identification algorithm depends on whether $n + m + 1 > 2 n$ or not. In the first case it is $O \left( ( n + m + 1 ) ^ { 3 } \right)$ , in the second one $- O \left( ( 2 n ) ^ { 3 } \right)$ . Considering ALQ-STR methods, only authors $\mathrm { o f } ^ { 2 7 }$ made such estimations and stated that the complexity of their method is $O \left( n ^ { 6 } \right)$ .

The block diagram of the developed adaptive control system is shown in Fig. 1.

![](image/d506ecec0ec1539c0b6f9314bacd6caa1060107cf32e9f1d80385f84b350d5a3.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    A["Controller adaptation (32)"] -->|θ̂(t)| B["Control Law (7)"]
    B -->|u(t)| C["Plant"]
    C -->|x(t)| D["Filtrations (20)-(23)"]
    D -->|z̄(t)| E["Extension and Mixing (24) + (25)"]
    E -->|φ(t)| F["Parametrizations (19), (30)"]
    F -->|Δ(t)| D
    D -->|yθ(t)| G["Filtration (31)"]
    G -->|Ω(t)| B
    B -->|r(t)| A
    D -->|z_A(t)| E
    E -->|z_B(t)| F
```
</details>

FIGURE 1 Block diagram of the proposed adaptive control system
