where L is the graph Laplacian and $\begin{array} { r l r l } { G ( x ) } & { { } : = } & { } \end{array}$ $[ g ( x _ { 1 } ) , \ldots , g ( x _ { n } ) ] ^ { T }$ .

Next, we introduce stochastic consensus notions for stochastic multi-agent systems which are adapted from [20].

Definition 2.1 (Consensus in expectation): The agents in the stochastic multi-agent system (3) are said to reach consensus in expectation if the following holds

$$\lim _ {t \to \infty} \mathbb {E} [ \overline {{{x}}} (t) ^ {T} \overline {{{x}}} (t) ] = 0, \tag {4}$$

where x is the stack vector of relative positions.

Definition 2.2 (Almost sure consensus): The agents in the stochastic multi-agent system (3) are said to reach almost sure (a.s.) consensus if the following holds lim $\mathbf { \rho } _ { 1 } \mathbf { \rho } _ {  \infty } \overline { { x } } ( t ) = 0$ , almost surely (i.e., $\mathbb { P } [ \operatorname* { l i m } _ { t \to \infty } \overline { { x } } ( t ) = 0 ] = 1 )$ .

For later use, we recall the infinitesimal generator (denoted by the operator L) for a stochastic system $S ~ : ~ \mathrm { d } x ~ =$ $f ( x ) \operatorname { d } t + g ( x ) \operatorname { d } W _ { t } , x \in \mathbb { R } ^ { n }$ using Ito’s differentiation [19]. ˆ Let $V : \mathbb { R } ^ { n } \to \mathbb { R } _ { 0 } ^ { + }$ be a twice differentiable continuous function. The infinitesimal generator of V associated with a stochastic system S is an operator, denoted by ${ \mathcal { L } } V$ , and given by

$$\mathcal {L} V (x) = \frac {\partial V}{\partial x} f (x) + \frac {1}{2} \operatorname{Tr} \left(g (x) ^ {T} \frac {\partial^ {2} V}{\partial x ^ {2}} g (x)\right), \tag {5}$$

for all $x \in \mathbb { R } ^ { n }$ .

Lemma $2 . 3 \ ( l { } l { } 9 ] ) .$ Consider a stochastic system $S : \mathbf { \Omega }$ d $\begin{array} { r } { x = f ( x ) \mathrm { d } t + g ( x ) \mathrm { d } W _ { t } , } \end{array}$ a twice differentiable continuous function $V : \mathbb { R } ^ { n }  \mathbb { R } _ { 0 } ^ { + }$ , and a constant $q \in \mathbb { R } ^ { + }$ . If there exists a constant $\kappa \in \mathbb R ^ { + }$ , a function $\overline { { \psi } } \in \mathcal { K } _ { \infty } ,$ , and a convex function $\psi \in \kappa _ { \infty }$ such that for all $x \in \mathbb { R } ^ { n }$ the following hold

$$\underline {{\psi}} (\| x \| ^ {q}) \leq V (x) \leq \overline {{\psi}} (\| x \| ^ {q}), \tag {6}\mathcal {L} V (x) \leq - \kappa V (x), \tag {7}$$

then the solution of S satisfies

$$\mathbb {E} [ \| x (t) \| ^ {q} ] \leq \underline {{\psi}} ^ {- 1} (\overline {{\psi}} (\| x (0) \| ^ {q}) \mathrm{e} ^ {- \kappa t}) \tag {8}$$

for all $t \in \mathbb { R } _ { 0 } ^ { + }$

Lemma $2 . { \overset { \cdot } { 4 } } ( I 2 I J ) .$ For a stochastic system $S : \mathrm { d } x \ =$ $f ( x ) \mathrm { d } t + g ( x ) \mathrm { d } W _ { t }$ , if there exist a twice differentiable continuous function $V : \mathbb { R } ^ { n }  \mathbb { R } _ { 0 } ^ { + }$ satisfying (6) and

$$\mathcal {L} V (x) \leq - \beta (x),$$

where $\beta : \mathbb { R } ^ { n }  \mathbb { R } _ { 0 } ^ { + }$ is continuous and nonnegative. Then, for each $x _ { 0 } \in \mathbb { R } ^ { n } , \operatorname* { l i m } _ { t \to \infty } \beta ( x ( t ) ) = 0 \mathrm { ~ a . s . }$ .
