$$
\begin{array}{l} \alpha \mathcal {V} + H (y, \mathbf {m}, \nabla_ {y} \mathcal {V}) + \epsilon \partial_ {y _ {2}} ^ {2} \mathcal {V} = 0 i n i n t Y; \\ \alpha (P - \rho_ {0}) + \mathfrak {A} (u ^ {*}, w ^ {*}) P = 0 i n i n t Y; \\ H (y, \mathbf {m}, p) := \min _ {u \in \mathbf {U}} \max _ {w \in \mathbf {W}} \mathcal {H} (y, u, w, \mathbf {m}, p); \\ w ^ {*} (u) = \arg \max _ {w \in \mathbf {W}} \mathcal {H} (y, u, w, \mathbf {m}, \nabla_ {y} \mathcal {V}); \\ u ^ {*} = \arg \min _ {u \in \mathbf {U}} \mathcal {H} (y, u, w ^ {*} (u), \mathbf {m}, \nabla_ {y} \mathcal {V}); \tag {10} \\ \mathcal {V} (y _ {1}, y _ {2}) = \mathcal {V} (y _ {1} + \mathfrak {L}, y _ {2}) i n Y; \\ \partial_ {y _ {2}} \mathcal {V} (y _ {1}, 0) = \partial_ {y _ {2}} \mathcal {V} (y _ {1}, s _ {m a x}) = 0 i n \mathbb {T}; \\ P (y _ {1}, y _ {2}) = P (y _ {1} + \mathfrak {L}, y _ {2}) i n Y; \\ \partial_ {y _ {2}} P (y _ {1}, 0) = \partial_ {y _ {2}} P (y _ {1}, s _ {m a x}) = 0 i n \mathbb {T}, \\ \end{array}
$$

assuming a solution exists and is regular-enough.

Proof. — (sketch) For details, see [6]. In short, one assumes that a solution to a finite-horizon version of the given mean-field game can be extended to arbitrarily long times, and P is defined by:

$$P (\cdot) := \alpha \int_ {0} ^ {\infty} e ^ {- \alpha t} \rho (t, \cdot) d t$$

as in Chapter 7 of [6]. Then, standard dynamic programming approaches to the discounted infinite-horizon optimal control problem are used.

As we obtained in [36] by solving the optimization problem on the pre-Hamiltonian, the optimal control and worst-case disturbance are the ramp functions with cutoff:

$$
u ^ {*} (p _ {2}) := \left\{ \begin{array}{l l} u _ {\max} & \text { if } p _ {2} <   - u _ {\max} \\ - p _ {2} & \text { if } - u _ {\max} \leq p _ {2} \leq u _ {\max} \\ - u _ {\max} & \text { if } u _ {\max} <   p _ {2} \end{array} \right. \tag {11}

w ^ {*} (p _ {2}) := \left\{ \begin{array}{l l} - w _ {\max} & \text { if   } \gamma^ {2} p _ {2} <   - w _ {\max} \\ \gamma^ {2} p _ {2} & \text { if   } - w _ {\max} \leq \gamma^ {2} p _ {2} \leq w _ {\max} \\ w _ {\max} & \text { if   } w _ {\max} <   \gamma^ {2} p _ {2}. \end{array} \right. \tag {12}
$$

These constrained solutions are found by checking if the unconstrained solution is in the interior of the feasible sets, and if not, one then compares the values at the boundary points. The solutions are unique as the respective problems are concave and convex. □
