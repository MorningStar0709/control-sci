• estimates (83), (82) guarantee the Lagrange stability property,   
• the set $\left\{ ( 0 , z ) ^ { \prime } \in \mathbb { R } ^ { 2 } , z \in \mathbb { R } \right\}$ is invariant for (111); however, it should be noticed that (depending on the value of $\theta \in \mathbb { R } )$ there may be equilibrium points of system (111) out of the set $\left\{ ( 0 , z ) ^ { \prime } \in \mathbb { R } ^ { 2 } , z \in \mathbb { R } \right\}$   
• estimate (113) guarantees the p-UGAOS property and residual constant $\alpha { = } \left( \left| \theta \right| { - } 1 \right) ^ { + } \sqrt { 2 a / c }$   
• estimates (112), (113) show that system (111) with output $y = \xi$ is p-GOA with asymptotic residual constant equal to $\tilde { \alpha } = \operatorname* { m i n } \left( \sqrt { 2 \varepsilon } , \left( \left| \theta \right| - 1 \right) ^ { + } \sqrt { 2 a / c } \right) \leq \alpha$ ,   
• estimates (112) and (114) guarantee the estimate lim sup $\left( \left| \xi ( t ) \right| \right) \leq \operatorname* { m i n } \left( \sqrt { 2 \varepsilon } , \sqrt { \frac { 2 a } { c \left( 1 + l \right) } } \big ( \left| \theta \right| - 1 - l \big ) ^ { + } \right)$ , where $l = \exp { ( \operatorname* { l i m } _ { t  + \infty } { ( z ( t ) ) } ) }$ ; thus, we can t→+8 obtain an explicit condition for the convergence of the output to zero, namely, lim $\textstyle { \left( \xi ( t ) \right) = 0 }$ when $\left| \theta \right| \leq 1 + l$ . Consequently, we get the following result: t→+80

“At least one of the following holds:

$$\text {(i)} \quad \lim _ {t \rightarrow + \infty} (\xi (t)) = 0,\text {(ii)} \left| \theta \right| > 1 \text { and } z (t) \leq \lim _ {s \rightarrow + \infty} (z (s)) < \ln (| \theta | - 1), \text { for all } t \geq 0.$$

It is clear that system (111) with output $y = \xi$ is rich of stability properties.

We end this section by providing sufficient conditions for the p-OAG property. The following theorem is a novel result which is close in spirit to LaSalle’s theorem (see [9]) which is not applicable to systems with inputs. In order to overcome the presence of inputs the following result exploits a family of functions (instead of a single function).
