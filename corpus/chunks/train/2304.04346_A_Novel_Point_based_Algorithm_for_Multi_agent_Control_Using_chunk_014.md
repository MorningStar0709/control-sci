We first describe the extended form of coordinator’s POMDP. For the ease of illustration, consider $\mathcal { T } = \{ 1 , 2 \}$ though the idea can naturally extend to more than two players. We derive an extended POMDP ˆ from the coordinator’s POMDP $\bar { \mathcal { E } } = \{ \mathcal { S } , \bar { \mathcal { A } } , \mathcal { O } , b _ { 0 } , \mathbb { P } , r , \beta \}$ by extending each time t into three stages: $( t , 0 ) , ( t , 1 ) , ( t , 2 )$ . The state space is $\scriptstyle S ^ { 0 } : = S$ at $( t , 0 ) , S ^ { 1 } : = S \times \mathcal { A } ^ { 1 }$ at $( t , 1 )$ , and $S ^ { 2 } : = S \times A$ at (t, 2). The common observation space is {∅} at both (t, 0) and (t, 1), and O at (t, 2). Only stage (t, 2) features an instantaneous reward, which is $r ( s , a )$ for $( s , a ) \in S ^ { 2 }$ . The system evolves as follows:

1) At (t, 0), the coordinator chooses prescription $\gamma ^ { 1 } \in \bar { \mathcal { A } } ^ { 1 }$ for agent 1 only. The state deterministically transits from $s \in S$ to $( s , \gamma ^ { 1 } ( m _ { s } ^ { 1 } ) ) \in \mathcal { S } \times \mathcal { A } ^ { 1 }$ .   
2) At (t, 1), the coordinator chooses prescription $\gamma ^ { 2 } \in { \bar { \mathcal { A } } } ^ { 2 }$ for agent 2 only. The state deterministically transits from $( s , a ^ { 1 } ) \in S \times \mathcal { A } ^ { 1 }$ to $( s , a ^ { 1 } , \gamma ^ { 2 } ( m _ { s } ^ { 2 } ) ) \in S \times A ^ { 1 } \times$ $\mathcal { A } ^ { 2 }$ .   
3) At $( t , 2 )$ , the coordinator has no action to take. The state-information joint transition kernel is simply the same $\mathbb { P } ( s ^ { \prime } , o | s , a )$ as defined in the initial model .

The total reward for the new POMDP is given by $\begin{array} { r } { \sum _ { t = 0 } ^ { \infty } \beta _ { - } ^ { t } r \big ( s _ { ( t , 2 ) } , a _ { ( t , 2 ) } \big ) , } \end{array}$ . With some abuse of notation, for $\gamma ^ { i } \in \bar { \mathcal { A } } ^ { i }$ , we define $\acute { \gamma } ^ { i } ( a ^ { i } | m ^ { i } ) : = \mathbf { 1 } _ { \{ \gamma ^ { i } ( m ^ { i } ) = a ^ { i } \} }$ . The belief update functions for the three stages are given by

$$[ \tau^ {0} (b, \gamma^ {1}) ] (s, a ^ {1}) = b (s) \gamma^ {1} (a ^ {1} | m _ {s} ^ {1})[ \tau^ {1} (b, \gamma^ {2}) ] (s, a ^ {1}, a ^ {2}) = b (s, a ^ {1}) \gamma^ {2} (a ^ {2} | m _ {s} ^ {2})[ \tau^ {2} (b, o) ] (s ^ {\prime}) = \frac {\sum_ {(s , a) \in \mathcal {S} \times \mathcal {A}} \mathbb {P} (s ^ {\prime} , o | s , a) b (s , a)}{\sum_ {\tilde {s} \in \mathcal {S}} \sum_ {(s , a) \in \mathcal {S} \times \mathcal {A}} \mathbb {P} (\tilde {s} , o | s , a) b (s , a)}$$
