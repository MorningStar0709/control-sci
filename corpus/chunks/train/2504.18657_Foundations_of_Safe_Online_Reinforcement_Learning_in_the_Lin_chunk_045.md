Now, consider any $| x | , | y | \leq 4 \log ^ { 2 } ( T )$ such that $\begin{array} { r } { | x - y | \le \frac { 1 } { \log ^ { 1 0 } ( T ) } } \end{array}$ log . Then there must exist some g ∈ G such that max (|x − g|, |y − g|) ≤ 1log10(T ) . $g \in G$ $( | x - g | , | y - g | ) \leq \frac { 1 } { \log ^ { 1 0 } ( T ) }$ For this $g _ { ; }$ , let $d _ { 0 } ^ { x } , d _ { 1 } ^ { x } , \ldots$ . be the sequence of differences of states when starting at state g versus x and using controller $C _ { K } ^ { \theta }$ with noise $W ^ { \prime }$ , and likewise let $d _ { 0 } ^ { y } , d _ { 1 } ^ { y } , \ldots$ , be the sequence of absolute differences of states when starting at state g versus y and using controller $C _ { K } ^ { \theta }$ with noise $W ^ { \prime }$ . Conditional on event $E _ { \mathrm { L 3 } } ( K , \theta , W ^ { \prime } )$ , we have by Lemma 5 that $\{ d _ { i } ^ { x } \}$ and $\{ d _ { i } ^ { y } \}$ both satisfy Equation (10). Since $\{ d _ { i } ^ { x } \}$ and $\{ d _ { i } ^ { y } \}$ are both distances comparing to the same set of states starting at state $^ { g , }$ we have by the triangle inequality that

$$d _ {i} \leq d _ {i} ^ {x} + d _ {i} ^ {y}.$$

Therefore, for $i \leq t$ we have the following, where L is from Lemma 5:

$$
d _ {i} \leq \left\{ \begin{array}{l l} 2 \left(1 - \frac {1}{\log^ {1 0} (T)}\right) ^ {i} \cdot d, & \text { if   } i > L \\ 4 d + \tilde {O} _ {T} (\epsilon) & \text { if   } i \leq L. \end{array} \right. \tag {11}
$$

This is exactly the desired result, and therefore we are done.
