Consequently, if the event-trigger parameters $\eta , \gamma , c , \beta _ { 1 } , \beta _ { 2 } ,$ $\rho > 0$ are chosen as outlined in Section 3.2.1, then the $f o l -$ - lowing results hold:

$R l { : }$ : The set of event-times I p generates an increasing sequence. Specifically, it holds that $t _ { j + 1 } ^ { p } - t _ { j } ^ { p } \geq \bar { \tau } > 0$ where τ is given by (33). Thus $t _ { j } ^ { p } $ ∞ as $j $ ∞ excluding Zeno behavior.

R2: For $e \nu e r y u [ 0 ] \in L ^ { 2 } ( 0 , 1 )$ , there exists a unique solution $u : \mathbb { R } _ { + } \times [ 0 , 1 ] \to \mathbb { R }$ such that $u \in C ^ { 0 } ( \mathbb { R } _ { + } ; L ^ { 2 } ( 0 , 1 ) \cap$ $C ^ { 1 } ( J ^ { p } \times \left[ 0 , 1 \right] )$ with $u [ t ] \in C ^ { 2 } ( [ 0 , 1 ] )$ which satisfy $( 2 ) , ( 4 6 ) , ( \bar { 4 } 7 )$ for all $t > 0$ and (1) for all $t > 0 , x \in ( 0 , \dot { 1 } )$ , where $\begin{array} { r } { J ^ { p } = \check { \mathbb { R } } _ { + } \backslash I ^ { p } . } \end{array}$ .

R3: The dynamic variable $m ^ { p } ( t )$ governed by (50)-(55) with $m ^ { p } ( 0 ) = m ^ { r } ( 0 ) > 0$ satisfies $m ^ { p } ( t ) > 0 .$ for all $t > 0$ .

R4: As a result of R1-R3, the Lyapunov candidate $V ^ { p } ( t )$ given by (52),(53) satisfies

$$\dot {V} ^ {p} (t) \leq - b ^ {*} V ^ {p} (t) + c \big (e ^ {- b ^ {*} t} V _ {0} - V ^ {p} (t) \big), \tag {63}$$

for all $t \in ( t _ { j } ^ { p } , t _ { j + 1 } ^ { p } ) , j \in \mathbb { N } ,$ , and consequently,

$$V ^ {p} (t) \leq e ^ {- b ^ {*} t} V _ {0}, \tag {64}$$

for all $t > 0 ,$ , where $b ^ { * } > 0$ is given by (54).

R5: The closed-loop solution of (1),(2),(46)-(55) globally exponentially converges to zero in the spatial ${ \check { L } } ^ { 2 }$ norm satisfying

$$\left\| u [ t ] \right\| \leq M e ^ {- \frac {b ^ {*}}{2} t} \sqrt {\left\| u [ 0 ] \right\| ^ {2} + m ^ {p} (0)}, \tag {65}$$

where $b ^ { * }$ is given by (54) and

$$M = \sqrt {\frac {2 \tilde {L} ^ {2}}{B} \max \left\{\frac {B \tilde {K} ^ {2}}{2} , 1 \right\}}. \tag {66}$$

Here, K˜ = 1 +  R 10 R x0 K2(x, y)dydx1/2 w $\begin{array} { r l } & { H e r e , \tilde { K } = 1 + \left( \int _ { 0 } ^ { 1 } \int _ { 0 } ^ { x } K ^ { 2 } ( x , y ) d y d x \right) ^ { 1 / 2 } w h e r e K ( x , y ) } \\ & { s a t i s f i e s ( 1 0 ) \ – ( 1 2 ) . } \end{array}$ satisfies (10)-(12).
