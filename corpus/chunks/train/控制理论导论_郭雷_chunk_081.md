如果要求 $f_{c}(\lambda) = f(\lambda)$ ，可以通过比较系数得出

$$\beta_ {i} = \alpha_ {i} + \overline {{{{k}}}} _ {i}, \quad i = 0, 1, \dots , n - 1,$$

从而有

$$\overline {{{k}}} _ {i} = \beta_ {i} - \alpha_ {i}, \quad i = 0, 1, \dots , n - 1.$$

由此可见，系统(1.6.8)通过状态反馈控制规律(1.6.9)，使得闭环系统(1.6.10)的极点集合为 $\Lambda_0$

再取坐标反变换 $x = T^{-1}\overline{x}$ 这时闭环系统(1.6.10)变为

$$
\left\{ \begin{array}{l} {\dot {x} (t) = (\pmb {A} - \pmb {b k} ^ {\mathrm{T}}) \pmb {x} (t),} \\ {y (t) = \pmb {C x} (t),} \end{array} \right. \tag {1.6.11}
$$

其中 $k^{\mathrm{T}} = \overline{k}^{\mathrm{T}}T$ 。由于坐标变换不改变系统的极点，因此系统(1.6.11)与系统(1.6.10)有相同的极点，于是有 $\sigma (A - bk^{\mathrm{T}}) = \Lambda_0$ 。这就说明，对单输入系统(1.6.1)，对任意给定的复对称集合 $\Lambda_0$ ，都能找到一个 $k^{\mathrm{T}}$ ，使得 $A - bk^{\mathrm{T}}$ 的特征值集合为 $\Lambda_0$ ，即系统(1.6.1)能任意极点配置。

b) 假设系统 (1.6.1) 是多输入的。此时希望把它变成一个能控的单输入系统。取反馈规律

$$u (t) = K _ {1} x (t) + e _ {1} v (t), \tag {1.6.12}$$

其中 $K_{1}$ 为一个 $r \times n$ 阶常值矩阵， $e_{1}^{T} = [1, 0, \cdots, 0]$ ， $v(t)$ 为一个标量。于是将这个控制规律 (1.6.12) 作用于系统 (1.6.1) 中得到单输入系统

$$
\left\{ \begin{array}{l} \dot {x} (t) = (A + B K _ {1}) x (t) + b _ {1} v (t), \\ y (t) = C x (t), \end{array} \right. \tag {1.6.13}
$$

其中 $b_{1}$ 是矩阵 $B$ 的第一列组成的向量. 如果存在某 $\pmb{K}_{1}$ . 使得 $(A + BK_{1}, b_{1})$ 完全能控, 那么系统 (1.6.13) 就是一个单输入能控系统. 用已证明的第一部分得出的结论, 若任意给定复对称集合 $\Lambda_{0}$ , 其个数为 $n$ , 则必存在某向量 $\pmb{k}_{2}$ , 使得 $A + BK_{1} + b_{1}k_{2}^{\mathrm{T}}$ 的特征值集合为 $\Lambda_{0}$ . 注意 $b_{1} = Be_{1}$ , 我们有 $A + BK_{1} + b_{1}k_{2}^{\mathrm{T}} = A + B(K_{1} + e_{1}k_{2}^{\mathrm{T}})$ . 令

$$\boldsymbol {K} = \boldsymbol {K} _ {1} + e _ {1} \boldsymbol {k} _ {2} ^ {\mathrm{T}},$$

那么 $A + BK$ 的特征值集合为 $\Lambda_{0}$ ，这说明系统 (1.6.1) 能任意极点配置。

因此，问题的关键就在于证明，是否存在矩阵 $K_{1}$ ，使得 $(A + BK_{1}, b_{1})$ 完全能控。下面用构造性方法将 $K_{1}$ 找出来。

设 $b_{1}$ 相对 $\pmb{A}$ 的最小多项式为

$$f _ {1} (\lambda) = \lambda^ {k _ {1}} + \alpha_ {k _ {1} - 1} \lambda^ {k _ {1} - 1} + \dots + \alpha_ {1} \lambda + \alpha_ {0},$$

那么 $k_{1}$ 个向量

$$b _ {1}, A b _ {1}, \dots , A ^ {k _ {1} - 1} b _ {1}$$

是线性独立的. 如果 $k_{1} = n$ , 则 $\pmb{A}$ 是循环矩阵, $b_{1}$ 是它的生成元, 这时只要取 $K_{1} = 0$ 就是所求. 假设 $k_{1} < n$ 并取

$$
\begin{array}{l} x _ {1} = b _ {1}, \\ x _ {i} = A x _ {i - 1} + b _ {1}, \quad i = 2, 3, \dots , k _ {1}. \\ \end{array}
$$

从 $b_{1}, Ab_{1}, \cdots, A^{k_{1}-1}b_{1}$ 的线性独立性容易推知， $x_{1}, x_{2}, \cdots, x_{k_{1}}$ 也是线性独立的.

由于 $(A,B)$ 能控，因此在B的列元中存在某列向量，比如说是 $b_{2}$ ，使得它与 $x_{1},x_{2},\cdots,x_{k_{1}}$ 线性独立。设 $k_{2}$ 是使
