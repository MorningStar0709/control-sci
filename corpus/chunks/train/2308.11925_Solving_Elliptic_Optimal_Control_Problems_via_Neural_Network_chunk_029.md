By Theorem 3.2, the generalization error can be decomposed into the approximation error $\mathcal { E } _ { a p p } =$ $3 \operatorname* { i n f } _ { ( y , p ) \in \mathcal { V } \times \mathcal { P } } \mathcal { L } ( y , p )$ and statistical error $\mathcal { E } _ { s t a t } : = 4 \mathcal { R } _ { n _ { d } } ^ { \mathrm { o f f } } ( \mathcal { G } _ { d } , ( 2 b _ { d } ) ^ { - 1 } ) + 4 \mathcal { R } _ { n _ { h } } ^ { \mathrm { o f f } } ( \mathcal { G } _ { b } , ( 2 b _ { b } ) ^ { - 1 } )$ . The former arises from restricting the trial spaces of $( y , p )$ from the space $( H _ { 0 } ^ { 1 } ( \Omega ) \cap H ^ { 2 } ( \Omega ) ) ^ { 2 }$ to $\mathcal { V } \times \mathcal { P }$ , and the latter is due to approximating the integrals with Monte Carlo methods. Throughout, the optimization error is ignored.
