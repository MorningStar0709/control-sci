# 4.1 SGD with biased gradient and stability constraint

First, we characterize the “robustness” of a policy at which we aim to estimate the gradient. This is formalized in the following lemma which is a consequence of [43, Lemma IV.1].

Lemma 4. Consider any $L \in S$ and let Z be the unique solution of $Z = A _ { L } Z A _ { L } ^ { \intercal } + \Lambda$ for any $\Lambda \succ 0$ . Then, $L + \Delta \in S$ for any $\Delta \in \mathbb { R } ^ { n \times m }$ satisfying $0 \leq \| \Delta \| _ { F } \leq \lambda ( \Lambda ) / \big [ 2 \overline { { \lambda } } ( Z ) \| H \| \big ]$ .

Second, we provide a uniform lowerbound for the stepsize of gradient descent for an approximated direction “close” to the true gradient direction.

Lemma 5 (Uniform Lower Bound on Stepsize). Let $L _ { 0 } \in S _ { \alpha }$ for some $\alpha \geq \alpha ^ { * } : = J ( L ^ { * } )$ , and choose any finite $\beta \geq \alpha$ . Consider any direction E such that $\| E - \nabla J ( L _ { 0 } ) \| _ { F } \leq \gamma \| \nabla J ( L _ { 0 } ) \| _ { F } f o r$ some $\gamma \in [ 0 , 1 ]$ , then we have $J ( L _ { 0 } - \eta E ) \le \beta$ for any η satisfying:

$$0 \leq \eta \leq \frac {1 - \gamma}{(\gamma + 1) ^ {2}} \cdot \frac {1}{\ell (\beta)} + \frac {c _ {3} (\alpha)}{\ell (\alpha) [ \alpha - \alpha^ {*} ]} \sqrt {\frac {\beta - \alpha}{2 \ell (\beta)}}.$$

Remark 6. Note that for the case of exact gradient direction, i.e. when $E = \nabla J ( L _ { 0 } )$ , we have $\gamma = 0$ and choosing $\beta = \alpha$ implies the known uniform bound of $\begin{array} { r } { \eta \le \frac { 1 } { \ell ( \beta ) } } \end{array}$ for feasible stepsizes as expected. Also, by this choice of $\beta ,$ , this guarantees that the next iterate remains in sublevel set $S _ { \alpha }$ . This lemma generalizes this uniform bound for general directions and (potentially) larger sublevel set.

The next proposition provides a decay guarantee for one iteration of gradient descent with an approximate direction which will be used later for convergence of SGD with a biased gradient estimate.
