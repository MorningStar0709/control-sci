Equivalently, we write

$$\sup _ {(x, i) \in \mathcal {O}} \mathbb {E} _ {(x, i)} [ \eta (\alpha_ {i} (\theta)) ] \leq \frac {1}{\rho} \max _ {i \in \mathbb {Y}} \mathbb {E} _ {i} [ \tau ] < \infty , \tag {4.9}$$

where, in the last inequality, we have used that the process $( Y _ { t } ) _ { t \geq 0 }$ is irreducible and positive recurrent.

Finally, plugging (4.9) into (4.5), we conclude that $\mathbb { E } _ { ( x , i ) } [ \eta ] \ < \ \infty$ , which in turn implies that (X ξ∗(θ)t , $( X _ { t } ^ { \xi ^ { \ast } ( \theta ) } , Y _ { t } ) _ { t \geq 0 }$ is a regenerative process with finite mean regenerative epochs. Hence, by Theorem 2.1 in [49], there exists a unique stationary distribution, concluding the proof. 

Next, we characterize the stationary distribution and we study its stability with respect to the changes in the parameter θ.

Theorem 4.1. Let $\nu ^ { \theta } \in \mathcal { P } ( \mathcal { O } )$ be the stationary distribution of $( X _ { t } ^ { \xi ^ { * } ( \theta ) } , Y _ { t } ) _ { t \geq 0 }$ . The following hold:

(1) For any $i \in \mathbb { Y } ,$ , the cumulative distribution function $\mu ^ { \theta } ( x , i ) \ : = \ \nu ^ { \theta } ( ( \underline { { x } } , x ] , i ) , \ x \ \in \ \mathcal { T }$ is $C ^ { 1 } ( [ \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) ] ) \cap C ^ { 2 } ( ( \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) ) \setminus \bigcup _ { j \in \mathbb { Y } } \{ \alpha _ { j } ( \theta ) , \beta _ { j } ( \theta ) \} )$ and it is the unique nondecreasing solution of the equation

$$\frac {1}{2} \sigma^ {2} (x, i) \mu_ {x x} ^ {\theta} (x, i) - (b (x, i) - \sigma \sigma_ {x} (x, i)) \mu_ {x} ^ {\theta} (x, i) + \sum_ {j \in \mathbb {Y}} q _ {j i} \mu^ {\theta} (x, j) = 0, \tag {4.10}$$

for $x \ \in \ ( \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) )$ , satisfying the boundary conditions $\mu ^ { \theta } ( x , i ) \ = \ 0 , x \ \leq \ \alpha _ { i } ( \theta )$ and $\mu ^ { \theta } ( x , i ) = p ( i ) , x \geq \beta _ { i } ( \theta ) .$ .

(2) The $m a p \nu : \mathbb { R } _ { + } \to \mathcal { P } ( \mathcal { O } ) , \theta \mapsto \nu ^ { \theta }$ is continuous.

Proof. For the first claim see Appendix C. We only provide here a proof of Claim 2.
