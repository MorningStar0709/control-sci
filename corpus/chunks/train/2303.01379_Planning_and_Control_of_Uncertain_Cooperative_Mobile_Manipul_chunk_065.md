and x is the overall state $\begin{array} { r } { x : = [ q ^ { \top } , \dot { q } ^ { \top } , \eta _ { o } ^ { \top } , \omega _ { o } ^ { \top } ] ^ { \top } \in \mathbb { X } : = 5 \times \mathbb { R } ^ { n + 3 } \times \mathbb { T } _ { 1 } } \end{array}$ ${ \mathsf S } : = { \mathsf S } _ { 1 } \times \cdots \times { \mathsf S } _ { N }$ . Moreover, the following Lemma is necessary for the following analysis.

Lemma 2.1. The matrix $\widetilde { M } ( \boldsymbol { x } )$ is symmetric and positive definite and the matrix $\dot { \widetilde { M } } ( x ) - 2 \widetilde { C } ( x )$ is skew symmetric, $i . e . _ { \cdot }$ ,

$$\left[ \dot {\widetilde {M}} (x) - 2 \widetilde {C} (x) \right] ^ {\top} = - \left[ \dot {\widetilde {M}} (x) - 2 \widetilde {C} (x) \right], \forall x \in \mathbb {X}y ^ {\top} \left[ \dot {\widetilde {M}} (x) - 2 \widetilde {C} (x) \right] y = 0, \forall x \in \mathbb {X}, y \in \mathbb {R} ^ {6}.$$

Proof. The matrices $M _ { o }$ and $M _ { i }$ are symmetric and positive definite, $\forall i \in \mathcal N$ and the matrices $\dot { M } _ { i } - 2 C _ { i } , M _ { o } - 2 C _ { O }$ are skew-symmetric, $\forall i \in \mathcal N$ [85], which leads to the skew-symmetry of $\dot { M } - 2 C$ . Therefore, since G is full row-rank, we can conclude the symmetry and positive definiteness of $\widetilde { M }$ . Regarding the skew symmetry of $\stackrel { \cdot } { \widetilde { M } } - 2 \stackrel { \sim } { C }$ , we define first $A : = A ( x ) : = { \dot { G } } M G ^ { \top }$ , and we have from (2.15b):

$$\dot {\widetilde {M}} - 2 \widetilde {C} = \dot {M} _ {o} - 2 C _ {o} + G (\dot {M} - 2 C) G ^ {\top} + A - A ^ {\top},$$

which, by employing the skew-symmetry of $M _ { o } - 2 C _ { o }$ and $\dot { M } - 2 C$ , leads to $[ \dot { \widetilde { M } } - 2 \widetilde { C } ] ^ { \top } = - [ \dot { \widetilde { M } } - 2 \widetilde { C } ]$ , which completes the proof.

The positive definiteness of $\widetilde { M } ( \boldsymbol { x } )$ leads to the property

$$\underline {{{m}}} I _ {6} \leq \widetilde {M} (x) \leq \bar {m} I _ {6}, \tag {2.16}$$

$\forall x \in \mathbb { X }$ , where $\underline { m }$ and ¯m are positive unknown constants.
