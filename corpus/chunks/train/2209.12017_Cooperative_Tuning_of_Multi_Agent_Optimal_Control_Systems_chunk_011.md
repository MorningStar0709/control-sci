Assumption 1. $W ( k )$ is doubly stochastic for all $k \_ =$ $0 , 1 , \cdot \cdot \cdot$ . There exists positive integers τ and l such that the union of graphs $\mathbb { G } _ { k l + 1 + \tau } , \mathbb { G } _ { k l + 2 + \tau } , . . . , \mathbb { G } _ { ( k + 1 ) l + \tau }$ is strongly connected.

By analytical proofs and results in [17], [18], one directly has the following lemma

Lemma 2. [18] Assume that the gradient $\frac { d L _ { i } ( \pmb { \xi } _ { i } ( \pmb { \theta } _ { i } ) , \pmb { \theta } _ { i } ) } { d \pmb { \theta } _ { i } } i s$ known for each agent i, and each $L _ { i } ( \pmb { \xi } _ { i } ( \pmb { \theta } _ { i } ) , \pmb { \theta } _ { i } )$ is convex in $\theta _ { i } .$ . Then the distributed update (4) with Assumption 1 and step size (5) drives $\theta _ { i } ( k ) \to \theta ^ { * }$ as k → ∞ for all $i \in \nu$ and $\pmb { \theta } ^ { * }$ minimizes the global average, i.e.

$$\{\boldsymbol {\theta} ^ {*} \} _ {i = 1} ^ {N} = \underset {\boldsymbol {\theta} _ {1}, \dots , \boldsymbol {\theta} _ {N}} {\arg \min} \quad \frac {1}{N} \sum_ {i = 1} ^ {N} L _ {i} (\boldsymbol {\xi} _ {i} (\boldsymbol {\theta} _ {i}), \boldsymbol {\theta} _ {i}). \tag {6}$$

Note that [18] proves Lemma 2 when the step size satisfies (5) and [17] proves Lemma 2 when the step size is a positive constant. Lemma 2 hypothesises that the gradient dLi(ξi(θi),θi)dθ is available to each agent i for all iterations $k = 0 , 1 , \cdots$ $\underline { { { \bf { \dot { \varepsilon } } } } } d L _ { i } ( \pmb { \xi } _ { i } ( \pmb { \theta } _ { i } ) , \pmb { \theta } _ { i } )$ i . From the chain rule, one has

$$\frac {d L _ {i} (\boldsymbol {\xi} _ {i} , \boldsymbol {\theta} _ {i})}{d \boldsymbol {\theta} _ {i}} = \frac {\partial L _ {i} (\boldsymbol {\xi} _ {i} , \boldsymbol {\theta} _ {i})}{\partial \boldsymbol {\xi} _ {i}} \frac {\partial \boldsymbol {\xi} _ {i} (\boldsymbol {\theta} _ {i})}{\partial \boldsymbol {\theta} _ {i}} + \frac {\partial L _ {i} (\boldsymbol {\xi} _ {i} , \boldsymbol {\theta} _ {i})}{\partial \boldsymbol {\theta} _ {i}}, \tag {7}$$
