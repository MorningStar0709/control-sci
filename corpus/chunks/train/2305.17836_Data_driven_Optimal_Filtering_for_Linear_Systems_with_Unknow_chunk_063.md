# F.1 Preliminary lemmas and their proofs

First, we provide the proof for the complete version of Lemma $6 { : } ^ { 4 }$

Lemma ${ \bf 6 ^ { \prime } }$ (Uniform Bounds for Stable Systems). Suppose $L \in S _ { : }$ , then there exit a constants $C _ { L } > 0$ such that

$$\left\| A _ {L} ^ {k} \right\| \leq C _ {L} \left(\sqrt {\rho (A _ {L})}\right) ^ {k + 1}, \quad \forall k \geq 0,$$

whenever $\rho ( A _ { L } ) > 0$ , and otherwise $\sqrt { \rho ( A _ { L } ) }$ is replaced with any arbitrarily small $r \in ( 0 , 1 )$ Additionally,

$$\sum_ {i = 0} ^ {\infty} \| A _ {L} ^ {i} \| \leq \frac {C _ {L}}{1 - \sqrt {\rho (A _ {L})}}\sum_ {i = 0} ^ {\infty} \| M _ {i} [ E ] \| \leq \frac {1 + 2 C _ {L} ^ {2} \rho (A _ {L}) ^ {3 / 2}}{[ 1 - \sqrt {\rho (A _ {L})} ] ^ {2}} \| E H \|$$

Furthermore, consider $S _ { \alpha }$ for some $\alpha > 0 _ { ; }$ , then there exist constants $D _ { \alpha } > 0 , C _ { \alpha } > 0$ and $\rho _ { \alpha } \in ( 0 , 1 )$ such that $\| L \| \leq D _ { \alpha } , C _ { L } \leq C _ { \alpha }$ and $\rho ( A _ { L } ) \leq \rho _ { \alpha } , \forall L \in { \mathcal { S } } _ { \alpha }$ .

Proof of Lemma 6. Recall the Cauchy Integral formula for matrix functions [16, Theorem 1.12]: for any matrix M ∈ Cn×n, $M \in \mathbb { C } ^ { n \times r }$

$$f (M) = \frac {1}{2 \pi i} \oint_ {\Gamma} f (z) (z I - M) ^ {- 1} d z,$$

whenever $f$ is real analytic on and inside a closed contour Γ that encloses spectrum of $M .$ . Note that $L \in S$ implying that $\rho ( A _ { L } ) < 1$ . Now, fix some $r \in ( \rho ( A _ { L } ) , 1 )$ and define $\Gamma ( \theta ) = r e ^ { i \theta }$ with θ ranging on [0, 2π]. Therefore, Cauchy Integral formula applies to $f ( z ) = z ^ { k }$ for any positive integer k and the contour Γ defined above. ${ \mathrm { S o } } ,$ for matrix $A _ { L }$ , we obtain

$$A _ {L} ^ {k} = \frac {1}{2 \pi i} \oint_ {\Gamma} z ^ {k} (z I - A _ {L}) ^ {- 1} d z = \frac {1}{2 \pi i} \int_ {0} ^ {2 \pi} r ^ {k} e ^ {i k \theta} (r e ^ {i \theta} I - A) ^ {- 1} r d e ^ {i \theta},$$

implying that

$$\| A _ {L} ^ {k} \| \leq \frac {r ^ {k + 1}}{2 \pi} \int_ {0} ^ {2 \pi} \| (r e ^ {i \theta} I - A _ {L}) ^ {- 1} \| d \theta \leq r ^ {k + 1} \max _ {\theta \in [ 0, 2 \pi ]} \| (r e ^ {i \theta} I - A _ {L}) ^ {- 1} \|.$$

Finally, the first claim follows by choosing $r = \sqrt { \rho ( A _ { L } ) }$ (whenever $\rho ( A _ { L } ) > 0$ , otherwise $r \in ( 0 , 1 )$ can be chosen arbitrarily small) and defining

$$C _ {L} := \max _ {\theta \in [ 0, 2 \pi ]} \| (\sqrt {\rho (A _ {L})} e ^ {i \theta} I - A _ {L}) ^ {- 1} \|$$

which is attained and bounded.

Next, by applying the first claim, we have
