equality constraints are irrelevant here. Regarding equality constraints are irrelevant here. Regarding tquality constraints are irrelevant here. Regarding theNow, given the layout (4) of the global variable, we can specsecond ADMM step (5b), we findsecond ADMM step (5b), we findecond ADMM step (5b), we findify the local averaging (7). In fact, a generic setup then is to let agent i perform the averaging for all entries k of $\zeta$ associated with $\alpha _ { i }$ . We collect all these entries in the index set $\mathcal { A } _ { i } \subseteq \mathcal { V }$ and find, by construction,

$$\mathcal {A} _ {i} \cap \mathcal {K} _ {j} \neq \varnothing \quad \Longrightarrow \quad j \in \mathcal {N} _ {i}$$

for all $( i , j ) \in \mathcal { M } ^ { 2 }$ with $i \neq j$ . During iteration step τ , agent i then evaluates the following algorithm, where we emphasize the communication with neighbors as a preparation for the encrypted implementation in Section 4.
