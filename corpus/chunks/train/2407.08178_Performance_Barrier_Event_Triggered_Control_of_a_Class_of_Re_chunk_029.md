Lemma 3 Consider the P-PETC approach $( 7 2 ) \AA - ( 7 6 )$ which generates an increasing set of event-times $\tilde { I } ^ { p } = \{ \tilde { t } _ { j } ^ { p } \} _ { j \in \mathbb { N } }$ with $\tilde { t } _ { 0 } ^ { p } = 0$ . For $d ( t )$ given by (25), it holds that

$$\left(\dot {d} (t)\right) ^ {2} \leq \rho_ {1} d ^ {2} (t) + \alpha_ {1} \| u [ t ] \| ^ {2} + \alpha_ {2} | u (1, t) | ^ {2}, \tag {77}$$

along the solution of $( 1 ) , ( 2 ) , ( 7 2 ) , ( 7 3 )$ for all $t \in ( n h , ( n +$ $1 ) h )$ and any $n \in \left[ \tilde { t } _ { i } ^ { p } / h , \tilde { t } _ { i + 1 } ^ { p } / h \right) \cap \mathbb { N } .$ . Here, $\rho _ { 1 } , \alpha _ { 1 }$ and $\alpha _ { 2 }$ are give by $( 3 5 ) , ( 4 1 )$ , and (42), respectively.

The proof is very similar to that of Lemma 2 in [29], and hence omitted.

Lemma 4 Consider the P-PETC approach (72)-(76) under Assumption 1, which generates an increasing set of eventtimes $\hat { \tilde { I } } ^ { p } = \{ \tilde { t } _ { i } ^ { p } \} _ { j \in \mathbb { N } }$ with $\tilde { t } _ { 0 } ^ { p } = 0$ . If the event-trigger parameters $\gamma , c , \beta _ { 1 } , \bar { \beta _ { 2 } } , \rho > 0$ are chosen as outlined in Section 3.2.1, and $\eta > 0$ is chosen such that

$$\eta \leq \frac {2 b}{B}, \tag {78}$$

with $b > 0$ given by (55) and B satisfying (44), then for the residual $W ^ { p } ( t )$ given by (51), the followings hold along the solution of $( \dot { 1 } ) , \check { ( } 2 ) , ( 2 6 ) { \cdot } ( 2 8 ) , ( 5 0 ) { \cdot } \check { ( } 5 5 ) , ( 7 2 ) { \cdot } ( 7 6 )$ :

$$W ^ {p} (t) \geq e ^ {- (b ^ {*} + c) (t - n h)} W ^ {p} (n h) \text { with } b ^ {*} = \eta , \tag {79}$$

for all $t \in \left[ n h , ( n + 1 ) h \right)$ and any $n \in \big [ \tilde { t } _ { j } ^ { p } / h , \tilde { t } _ { j + 1 } ^ { p } / h \big )$ ∩ N and

$$W ^ {p} (t) \geq 0, i. e., V ^ {p} (t) \leq e ^ {- b ^ {*} t} V _ {0} w i t h b ^ {*} = \eta , \tag {80}$$

for all $t > 0 .$ .

Proof By selecting the event-trigger parameters $\gamma , c , \beta _ { 1 } , \beta _ { 2 } , \rho >$ 0 as outlined in Section 3.2.1 with $\eta > 0$ and differentiating (52) along the solutions of $( 2 \dot { 6 } ) – ( 2 8 ) , ( 5 0 ) – ( 5 5 )$ i n $t \in ( n h , ( n + 1 ) h )$ and $n \in \big [ \tilde { t } _ { j } ^ { p } / h , \tilde { t } _ { j + 1 } ^ { p } / h \big )$ ∩ N, one can obtain that

$$\dot {V} ^ {p} (t) \leq - \left(\frac {2 b}{B}\right) \frac {B}{2} \| w [ t ] \| ^ {2} - \eta m ^ {p} (t) + c W ^ {p} (t). \tag {81}$$
