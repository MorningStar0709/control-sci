When multiple executions of the algorithm overlap in time, the values of j(tl) from each execution must be accumulated to obtain the total number of iterations required at each time step across all executions $\begin{array} { r } { ( \mathrm { i .
e .
, } \sum j ( t _ { l } ) ) } \end{array}$ .
As a result, $\sum j ( t _ { l } )$ may exceed the iteration limit $J _ { m } .$ .

Algorithm 1 The Parametrization Method   
Input: An optimal control problem as stated in Prob. 1 with initial states $\boldsymbol{x}(t_0)$ , initial hyperparameters including $a_{i,w}(t_0), i \in \{1, \dots, m_a\}$ , a sampling interval $\triangle t$ , an iteration limit $J_m > 0$ , a rollback horizon $N_c > 0$ , a threshold $\varepsilon > 0$ , a learning rate $\gamma > 0$ and the ending time $T > 0$ .

Output: The optimal hyperparameters $a_{i,w}^*(t_l)$ and number of iterations $j(t_l)$ at some time steps $t_l, l \leq \frac{T}{\triangle t}$ .

1: Discretize time into intervals $[t_k, t_{k+1})$ where $t_N = T, k \in \{0, \dots, N-1\}$ , set $a_{i,w}(t_{k+1}) = a_{i,w}(t_0)$ .

2: Solve (30) with constraints (10b),(10c),(25) and (26) (CBF-CLF-QP) at each time step until $\psi_{m_a-1}(\mathbf{Z}(t_f)) > \varepsilon$ is no longer satisfied for the first time.

3: Revert to the time step $t_k$ where $t_k = t_f - N_c \triangle t$ (if $t_f - N_c \triangle t < 0, t_k = 0$ ), set iteration $j = 1, j(t_l) = 0, l \in \{0, \dots, f\}$ .

4: for $j \leq J_m$ do

5: while $t_{k+1} \leq t_f$ do

6: Solve CBF-CLF-QP with $a_{i,w}(t_l), l \in \{k, \dots, f\}$ , get the minimum value of $\psi_{m_a-1}(\mathbf{Z}(t_l))$ as $\psi_{\min}$ .

7: If $\psi_{\min} > \varepsilon$ , jump to $13^{\text{th}}$ step of Alg. 1.

8: Evaluate $\frac{\partial\psi_{\min}}{\partial a_{i,w}(t_k)}, a_{i,w}(t_k) \leftarrow a_{i,w}(t_k) + \gamma\frac{\partial\psi_{\min}}{\partial a_{i,w}(t_k)}$ .

9: $j(t_k) = j, k = k + 1$ .

10: end while

11: $k = k - N_c, j = j + 1$ .

12: end for

13: $a_{i,w}^*(t_l) = a_{i,w}(t_l), l \in \{0, \dots, f\}$ .

14: return $a_{i,w}^*(t_l), j(t_l), l \in \{0, \dots, f\}$ .

Remark 3. In $6 ^ { t h }$ step of Alg. 1, we solve the $C B F { \cdot } C L F { \cdot } Q P$ with $a _ { i , w } ( t _ { l } )$ , where $l \in \{ k , \ldots , f \}$ , to obtain the minimum value of $\psi _ { m _ { a } - 1 } ( Z ( t _ { l } ) )$ at corresponding time steps. A general approach is to use the min-operator to determine the minimum value as:
