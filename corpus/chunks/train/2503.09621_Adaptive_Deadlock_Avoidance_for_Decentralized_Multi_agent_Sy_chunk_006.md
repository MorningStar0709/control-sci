Denote $\mathbf { x } ~ = ~ [ \bar { \mathbf { x } _ { 1 } } , \bar { \mathbf { \xi } } \cdot \cdot ~ , \mathbf { x } _ { N } ] ^ { T } ~ \in ~ \bar { \mathcal { X } } ~ \subset ~ \bar { \mathbb { R } ^ { d N } }$ as the joint multi-agent states. For any pairwise inter-agent collision avoidance between agent $i , j \in \mathcal { I }$ , the safety condition of x can be defined as follows.

$$h _ {i j} (\mathbf {x}) = \| \pi (\mathbf {x} _ {i}) - \pi (\mathbf {x} _ {j}) \| ^ {2} - (r _ {i} + r _ {j}) ^ {2}, \forall j \neq i \tag {3}\mathcal {H} _ {i j} = \{\mathbf {x} \in \mathbb {R} ^ {d N} | h _ {i j} (\mathbf {x}) \geq 0 \}, \forall j \neq i \tag {4}$$

where $r _ { i } , r _ { j } \in \mathbb { R }$ are the safety radius of the agent i and j, and $\pi : \mathbb { R } ^ { d } \mapsto \mathbb { R } ^ { s }$ is the mapping from the agent’s state to its position in the Cartesian coordinate system. To guarantee the safety of the decentralized multi-agent system, Control Barrier Functions (CBFs) [6], [18], [19] can be used to derive the set of controls that render the forward invariance of the safe set for pair-wise agents, as discussed below.

Lemma 2. [Summarized from [18], [20]] Given a decentralized multi-agent system with each agent’s dynamics defined by Eq. (1) and a safe set $\mathcal { H } _ { i j }$ as the 0-super level set of a continuously differentiable function $h _ { i j } ( { \bf x } ) : \mathcal { X } \mapsto \mathbb { R }$ for pair-wise agents i and $\mathbf { \chi } _ { i f } ^ { j , \dag }$ the function $\mathsf { \bar { h } } _ { i j } ( \mathbf { x } )$ is called a control barrier function there exists an extended class-K function $\alpha ( \cdot )$ such that s $\mathfrak { u p } _ { \mathbf { u } _ { i } \in \mathbb { R } ^ { m } } \{ \dot { h } _ { i j } ( \mathbf { x } , \mathbf { u } ) \} \geq - \alpha ( h _ { i j } ( \mathbf { x } ) )$ . iThe admissible control space for any Lipschitz continuous controller $\mathbf { u } _ { i } \in \mathcal { U } _ { i }$ enforcing the safe set forward invariant thus becomes:

$$
\begin{array}{l} K _ {\mathrm{cbf}} (\mathbf {x} _ {i}) = \left\{\mathbf {u} _ {i} \in \mathcal {U} _ {i} \mid L _ {f} h _ {i j} (\mathbf {x}) + L _ {g} h _ {i j} (\mathbf {x}) \mathbf {u} _ {i} \geq \right. \\ - W _ {i} \alpha (h _ {i j} (\mathbf {x})), \forall j \neq i \} \tag {5} \\ \end{array}
$$
