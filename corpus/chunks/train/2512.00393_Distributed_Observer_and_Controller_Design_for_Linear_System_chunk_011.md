# B. Problem Formulation

Consider a linear time-invariant system

$$\dot {x} = A x + B u + B _ {v} v, \tag {6}$$

where $x \in \mathbb { R } ^ { n } , \ u \in \mathbb { R } ^ { m }$ , and $\nu \in \mathbb { R } ^ { m _ { \nu } }$ are the state, control input, and unknown input vectors, respectively. While ?? is unknown, matrix $B _ { \nu }$ is known. This paper is concerned with the following problems.
