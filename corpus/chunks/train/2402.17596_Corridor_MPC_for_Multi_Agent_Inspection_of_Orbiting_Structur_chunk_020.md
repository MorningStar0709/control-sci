Furthermore consider a $H O C B F \ h : \mathbb { D } \times \mathbb { I } \to \mathbb { R }$ of relative degree r as in Def.
2 that is at least $C ^ { r + 1 }$ on $\mathbb { D } \times \mathbb { I }$ and where $\alpha _ { j }$ is $C ^ { r - j } \forall j = 0 , \ldots r - 1$ .
Let $\cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) \subset \mathbb { D }$ be the safe set as in (5).
Given that at time instant $k \Delta t ,$ $\tilde { { \pmb x } } ( k \Delta t ) \in \cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) , \ { \pmb x } ( k \Delta t ) \triangleq \tilde { { \pmb x } } ( k \Delta t )$ and that the constant feedback control input $\pmb { u } ( k \Delta t ) \in \mathbb { U }$ respects the condition $\begin{array} { r l } { \zeta ( \pmb { x } ( k \Delta t ) , \pmb { u } ( k \Delta t ) , k \Delta t ) - } & { { } L _ { w } ^ { l } ( \tilde { \pmb { x } } ( k \Delta t ) , \Delta t ) \Delta t - } \end{array}$ $c _ { w } ^ { l } ( \tilde { { \pmb x } } ( k \Delta t ) , \Delta t ) { \epsilon } _ { d } \geq 0$ where $L _ { w } ^ { l } ( \tilde { \mathbf { x } } ( k \Delta t ) , \Delta t )$ is defined over the $\ L _ { S e t } \Delta t \dot { \mathcal { Q } } ( \Omega _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) ) \triangleq \Delta ^ { \mathrm { i n } } \mathcal { R } ( \dot { \Omega } _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) ) \times \mathbb { U } \times \mathbb { W } \times$ $[ k \Delta t , ( k + 1 ) \Delta t ]$ as

$$
L_{w}^{l}(\tilde{\boldsymbol{x}} (k\Delta t),\Delta t) = \max_{\substack{(\tilde{\boldsymbol{x}},\boldsymbol {u},\boldsymbol {d},t)\in \\ \Delta^{t}\mathcal{Q}(\cap_{r - 1}^{0}\mathcal{C}_{S_{i}}(k\Delta t))}}|\dot{\zeta} (\tilde{\boldsymbol{x}},\boldsymbol {u}(k\Delta t),\delta \boldsymbol {d},t)|,
$$

and the constant $c _ { w } ^ { l } ( \tilde { { \pmb x } } ( k \Delta t ) , \Delta t )$ is defined as

$$
c _ {w} ^ {l} (\tilde {\boldsymbol {x}} (k \Delta t), \Delta t) = \max _ {\Delta^ {t} \mathcal {R} (\cap_ {r - 1} ^ {0} \mathcal {C} _ {S _ {i}} (k \Delta t)) \times \mathbb {I}} | | \mathcal {L} _ {g} \mathcal {L} _ {f} ^ {r - 1} h (\tilde {\boldsymbol {x}}, t) | |.
$$

Then, $\forall t \in [ k \Delta t , ( k + 1 ) \Delta t ] _ { \cdot }$ , it holds that

$$
\tilde {\boldsymbol {x}} (k \Delta t) \in \cap_ {r - 1} ^ {0} \mathcal {C} _ {S _ {i}} (k \Delta t) \Rightarrow \tilde {\boldsymbol {x}} (t) \in \cap_ {r - 1} ^ {0} \mathcal {C} _ {S _ {i}} (t). \tag {11}
$$
