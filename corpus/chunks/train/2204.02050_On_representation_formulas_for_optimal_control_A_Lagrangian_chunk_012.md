$$H ^ {*} (t, \boldsymbol {x}, b) = S (t, \boldsymbol {x}) + H _ {R} ^ {*} (t, \boldsymbol {x}, b).$$

Now using $B ( { \pmb x } , t ) = \{ b : b = - A { \pmb x } + h ( t , a ) , a \in U \}$ , we further have

$$H ^ {*} (t, \boldsymbol {x}, b) = S (t, \boldsymbol {x}) + H _ {f} ^ {*} (t, b + A \boldsymbol {x}) \tag {3.1}$$

where $H _ { f } ^ { * } ( t , b ) : = \operatorname* { s u p } _ { p } \{ p \cdot b - H _ { h } ( t , p ) \}$ and $\begin{array} { r } { H _ { h } ( t , p ) : = \operatorname* { s u p } _ { a \in U } \{ - p \cdot h ( t , a ) - R ( t , a ) \} } \end{array}$

In the following lemma, we state the relaxation theorem [22] that guarantees the existence of an admissible control such that the controlled trajectory can accurately approximate any state trajectory $- \dot { x } ( s ) \in \operatorname { C o n v } ( B ( x ( s ) , s ) )$ with same initial condition.

Lemma 1. Let $x : [ t , T ] \to \mathbb { R } ^ { n }$ satisfy $- \dot { x } ( s ) \in \operatorname { C o n v } ( B ( x ( s ) , s ) )$ . Then, for any $\epsilon > 0$ , there exists an admissible control $\tilde { u } \in \mathcal { U }$ and a state trajectory $\tilde { x } : [ t , T ]  \mathbb { R } ^ { n }$ satisfying $\dot { \tilde { x } } = f ( s , \tilde { x } ( s ) , \tilde { u } ( s ) )$ for $s \in [ t , T ]$ and $\tilde { { \boldsymbol { x } } } ( t ) = { \boldsymbol { x } } ( t )$ such that

$$| \tilde {x} (s) - x (s) | < \epsilon , \quad s \in [ t, T ].$$

We can then show that the value function $V ( { \pmb x } , t )$ can be approximated with an arbitrary precision using an optimal solution of (2.7) or (2.10) although an optimal $\beta$ lies in Conv $\left( B ( x ( s ) , s ) \right)$ rather than $B ( { \boldsymbol { x } } ( s ) , s )$ .

Theorem 3. Suppose $H _ { f } ^ { * } ( t , \cdot )$ is linear and let $( \boldsymbol { x } ( \cdot ) , \boldsymbol { \beta } ( \cdot ) )$ be an optimal solution of (2.7) (or $\left( 2 . 1 0 \right) { }$ . Then, for any $\dot { \epsilon } > 0$ , there exists an admissible control $\tilde { u } \in \mathcal { U }$ and a state trajectory $\tilde { x } : [ t , T ] \to \mathbb { R } ^ { n }$ satisfying $\dot { \tilde { x } } = f ( s , \tilde { x } ( s ) , \tilde { u } ( s ) ) \ f o r \ s \in [ t , T ]$ and $\tilde { { \boldsymbol { x } } } ( t ) = { \boldsymbol { \mathscr { x } } }$ such that

$$\left| \underbrace {\left\{\int_ {t} ^ {T} H ^ {*} (s , x (s) , \beta (s)) \mathrm{d} s + g (x (T)) \right\}} _ {= V (\pmb {x}, t)} - \left\{\underbrace {\int_ {t} ^ {T} H ^ {*} (s , \tilde {x} (s) , \tilde {\beta} (s)) \mathrm{d} s - g (\tilde {x} (T))} _ {\mathrm{approximation}} \right\} \right| < \epsilon ,$$

where $\tilde { \beta } ( s ) = - f ( s , \tilde { x } ( s ) , \tilde { u } ( s ) )$ .
