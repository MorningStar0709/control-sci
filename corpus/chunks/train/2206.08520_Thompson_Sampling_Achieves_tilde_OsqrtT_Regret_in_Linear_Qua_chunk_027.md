# 5.2. Reformulation in Terms of Closed-Loop Matrix

In the second step, we reformulate the probability of sampling optimistic parameters in terms of closed-loop system matrix $\tilde { A } _ { c } : = \tilde { \Theta } ^ { \intercal } \tilde { H _ { \ast } } = \tilde { A } + \tilde { B } K ( \Theta _ { \ast } ) \overline { { { \partial } } }$ of the sampled system $\tilde { \Theta } = ( \tilde { A } , \tilde { B } ) ^ { \intercal }$ driven by the policy $K ( \Theta _ { * } )$ . Transitioning to the closed-loop formulation allows tighter bounds on the optimistic probability. To complete this reformulation, we need to construct an estimation confidence set for the closed-loop system matrix $\hat { A } _ { c } : = \hat { \Theta } ^ { \intercal } H _ { \ast } = \hat { A } + \hat { B } K ( \Theta _ { \ast } )$ of the RLS-estimated system $\hat { \Theta } = ( \hat { A } , \hat { B } ) ^ { \intercal }$ and show that the constructed confidence set is a super set to $\mathcal { E } _ { t } ^ { \mathrm { R L S } }$ .

Lemma 8 (Closed-loop confidence) Let $F _ { t } ( \delta ) : = \beta _ { t } ^ { 2 } ( \delta ) H _ { * } ^ { \intercal } V _ { t } ^ { - 1 } H _ { * }$ . For any $t \geq 0 ,$ , define by

$$\mathcal {E} _ {t} ^ {c l} (\delta) := \left\{\hat {\Theta} \in \mathbb {R} ^ {(n + d) \times n} \mid \operatorname{tr} \left[ \left(\hat {\Theta} ^ {\intercal} H _ {*} - \Theta_ {*} ^ {\intercal} H _ {*}\right) F _ {t} ^ {- 1} (\delta) \left(\hat {\Theta} ^ {\intercal} H _ {*} - \Theta_ {*} ^ {\intercal} H _ {*}\right) ^ {\intercal} \right] \leq 1 \right\}. \tag {7}$$

the closed-loop confidence set. Then, for all times $t \geq 0 a n d \delta \in ( 0 , 1 )$ , we have that $\mathcal { E } _ { t } ^ { R L S } ( \delta ) \subseteq \mathcal { E } _ { t } ^ { c l } ( \delta )$ . Note that the definition of $\mathscr { E } _ { t } ^ { \mathrm { c l } } ( \delta )$ only involves closed-loop matrices $\hat { A } _ { c } \mathrel { \mathop : } = \hat { \Theta } ^ { \intercal } H _ { \ast }$ and $A _ { c , * } : = \Theta _ { * } ^ { \intercal } H _ { * }$ . We can use the result of Lemma 8 to reformulate the probability of sampling optimistic parameters, $\tilde { \Theta } = ( \tilde { A } , \tilde { B } )$ , as sampling optimistic closed-loop system matrices, $\tilde { A } _ { c }$ . We bound $p _ { t } ^ { \mathrm { { o p t } } }$ from below as
