2For the case of DNNs with nonsmooth activation functions $( { \mathrm { e . g . } }$ , rectified linear unit (ReLU), leaky ReLU, maxout etc.), the reader is referred to [12] where a switched analysis is provided to account for the nonsmooth nature of activation functions. To better focus on our main contribution without loss of generality, we restrict our attention to smooth activation functions. 3Bias terms are omitted for simplicity of the notation.

$L _ { m , 0 } = L _ { m , k _ { m } + 1 } = n$ . To facilitate the subsequent development, the following assumption is made.

Assumption 1. The function space of ResNets given by (6) is dense in C(Ω) with respect to the supremum norm, where ${ \mathcal { C } } ( \Omega )$ denotes the space of functions continuous over the compact set $\Omega \subset \mathbb { R } ^ { n }$ .

Remark 1. Assumption 1 implies that ResNets satisfy the universal function approximation property that is well-known for various DNN architectures [1]. The universal function approximation property of ResNets is a common assumption that is widely used in the deep learning literature, and has been rigorously established for ResNets with specific activation functions in [24] and [25].

Consider any vector field $f \in { \mathcal { C } } ( \Omega )$ and a prescribed accuracy $\overline { { \varepsilon } } \in \mathbb { R } _ { > 0 }$ . Then by Assumption 1, there exists a ResNet Φ with sufficiently large $m , k _ { p } , L _ { p , j } \ \forall ( p , j ) \in \{ 1 , \ldots , m \} \times$ $\mathbb { R } ^ { \Sigma _ { p = 1 } ^ { m } \Sigma _ { j = 0 } ^ { k _ { p } } L _ { p , j } L _ { p , j + 1 } }$ $\{ 0 , \ldots , k _ { p } \}$ orrespondi such that $\begin{array} { r } { \operatorname* { s u p } _ { x \in \Omega } \left\| f ( x ) - \Phi ^ { \theta ^ { * } } ( x ) \right\| \leq \overline { { \varepsilon } } . } \end{array}$ $\theta ^ { \ast } \in$ Therefore, the drift vector field $x \mapsto f ( x )$ can be modeled $\mathrm { a s ^ { 4 } }$

$$f (x) = \Phi^ {\theta^ {*}} (x) + \varepsilon (x), \tag {8}$$

when $x \in \Omega$ , where $\varepsilon ~ : ~ \mathbb { R } ^ { n } ~ \to ~ \mathbb { R } ^ { n }$ denotes an unknown function reconstruction error that can be bounded as $\begin{array} { r } { \operatorname* { s u p } _ { x \in \Omega } \| \boldsymbol { \varepsilon } ( x ) \| \leq \overline { { \boldsymbol { \varepsilon } } } } \end{array}$ .

To facilitate the subsequent analysis, the following assumption is made (cf., [26, Assumption 1]).

Assumption 2. There exists a known constant $\overline { { \theta } } \in \mathbb { R } _ { > 0 }$ such that the unknown ideal ResNet weights can be bounded as $\| \theta ^ { * } \| \leq \overline { { \theta } }$ .
