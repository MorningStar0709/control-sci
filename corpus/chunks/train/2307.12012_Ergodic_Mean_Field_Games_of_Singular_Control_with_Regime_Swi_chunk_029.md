Proof. Take arbitrary $i \in \mathbb { Y }$ and let $\theta _ { 1 } , \theta _ { 2 } \in \mathbb { R } _ { + }$ such that $\theta _ { 1 } \leq \theta _ { 2 }$ . Then, by Proposition 3.1, we have that the map $\theta \mapsto v ( x , i ; \theta )$ is decreasing, for any $( x , i ) \in \mathcal { O }$ . Hence,

$$\{x \in \mathcal {I}: v (x, i; \theta_ {2}) \geq k _ {1} \} \subseteq \{x \in \mathcal {I}: v (x, i; \theta_ {1}) \geq k _ {1} \},$$

which, by (3.7), implies that

$$\alpha_ {i} (\theta_ {2}) \leq \alpha_ {i} (\theta_ {1}).$$

The monotonicity of $\theta \mapsto \beta _ { i } ( \theta ) , \ i \in \mathbb { Y }$ , can be proved similarly.

Lemma 4.2. The following hold.

(1) For any compact X , the map $\theta \mapsto v ( x , i ; \theta )$ is locally Lipschtiz continuous, uniformly for $( x , i ) \in \mathcal { X } \times \mathbb { Y }$ .   
(2) The function $v _ { x } ( x , i ; \theta )$ is nonpositive for any $( x , i , \theta ) \in \mathcal { O } \times \mathbb { R } _ { + }$ , and, for fixed $\theta \in \mathbb { R } _ { + }$ , it is strictly negative for any $( x , i ) \in { \mathcal { C } } ^ { \theta }$ , where ${ \mathcal { C } } ^ { \theta }$ is the continuation region as in Theorem 3.1.

Proof. Take a compact $\mathcal { X } \times \Theta \subset \mathcal { T } \times \mathbb { R } _ { + }$ and generic $( x , i , \theta ) \in \mathcal { X } \times \mathbb { Y } \times \Theta$ . Following the proof of Proposition 3.3 (cf. Appendix $\mathbf { A } )$ , for $\epsilon > 0$ small enough, by mean value theorem, we have that

$$
\begin{array}{l} \left| v (x, i; \theta + \epsilon) - v (x, i; \theta) \right| \leq \widehat {\mathbb {E}} _ {(x, i)} \bigg [ \int_ {0} ^ {\infty} e ^ {\int_ {0} ^ {t} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} \big | \pi_ {x} (\widehat {X} _ {t}, \theta + \epsilon) - \pi_ {x} (\widehat {X} _ {t}, \theta) \big | d t \bigg ] \\ = \epsilon \widehat {\mathbb {E}} _ {(x, i)} \bigg [ \int_ {0} ^ {\infty} e ^ {\int_ {0} ^ {t} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} \big | \pi_ {x \theta} \big (\widehat {X} _ {t}, \tilde {\theta} \big) \big | d t \bigg ] \\ \leq \epsilon \widehat {\mathbb {E}} _ {(x, i)} \left[ \int_ {0} ^ {\infty} e ^ {- c t} \big | \pi_ {x \theta} (\widehat {X} _ {t}, \tilde {\theta}) \big | d t \right] \leq \epsilon C _ {(\mathcal {X}, \Theta^ {*})}, \\ \end{array}
$$
