subgraph Environment
        D["Agent 1 π₁^c"] -->|m₁| E["Actuator"]
        E --> F["..."]
        F --> G["Agent N π₂^c"]
        G --> H["Pr(s'| s, m)"]
        H --> I["o₁"]
        I --> D
        H --> J["o₂"]
    end

A -->|c₁| D
    B -->|m₁| D
    C -->|c_N| G
    D -->|o₁| H
    E -->|m₂| H
    G -->|m₂| H
    style Central_Controller fill:#f9f,stroke:#333
    style Environment fill:#ccf,stroke:#333
```
</details>

Figure 3. Illustration of the interactions of the CC and agents for the control of the environment. The red link shows the communication channels that are bit-budgeted - implying the local (and not global) observability of the CC. The orange dashed box is detailing the same box in Fig. 1 and Fig. 2 .

that is described by the tuple $M = \{ \mathcal { S } , \mathcal { M } ^ { N } , r ( \cdot ) , \gamma , T ( \cdot ) \}$ , where $\begin{array} { r } { r ( \cdot ) : \mathcal { S } \times \mathcal { M } ^ { N }  \mathcal { } ^ { \cdot } } \end{array}$ R is the per-stage reward function and the scalar $0 \leq \gamma \leq 1$ is the discount factor. The function $T ( \cdot ) : \mathcal { S } \times \mathcal { M } ^ { N } \times \overline { { \mathcal { S } } } \stackrel { \left. } { \right. } \left\lceil 0 , 1 \right\rceil$ is a conditional probability mass function (pmf) which represents state transitions such that $T \big ( \mathsf { s } ( t + 1 ) , \mathsf { s } ( t ) , \mathsf { m } ( t ) \big ) \mathsf { \Lambda } = \mathsf { P r } \big ( \mathsf { s } ( t + 1 ) | \mathsf { s } ( t ) , \mathsf { m } ( t ) \big )$ . According to the per-stage reward signals, the system’s return within the time horizon $\scriptstyle { \overline { { T } } } ^ { \prime }$ is denoted by

$$\mathbf {g} (t ^ {'}) = \sum_ {t = t ^ {\prime}} ^ {T ^ {\prime}} \gamma^ {t - 1} r \big (\mathbf {o} _ {1} (t),..., \mathbf {o} _ {N} (t), \mathbf {m} _ {1} (t),..., \mathbf {m} _ {N} (t) \big). \tag {1}$$
