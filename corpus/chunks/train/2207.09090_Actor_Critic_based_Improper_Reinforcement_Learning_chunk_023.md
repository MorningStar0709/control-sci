Assumption 5.2 (Uniform Ergodicity). For any $\theta \in \mathbb { R } ^ { M }$ , consider the Markov Chain induced by the policy $\pi _ { \theta } .$ and following the transition kernel $\bar { P } ( . | s , m )$ . Let $\xi _ { \pi _ { \theta } }$ be the stationary distribution of this Markov Chain. We assume that there exists constants $\kappa > 0$ and $\xi \in ( 0 , 1 )$ such that

$$\sup _ {s \in \mathcal {S}} \| \mathbb {P} (s _ {t} \in \cdot | s _ {0} = s, \pi_ {\theta}) - \xi_ {\pi_ {\theta}} (\cdot) \| _ {T V} \leqslant \kappa \xi^ {t}.$$

Further, let $L _ { \pi } : = \mathbb { E } _ { \nu _ { \pi } } [ \varphi ( s ) ( \gamma \varphi ( s ^ { \prime } ) - \varphi ( s ) ) ^ { \top } ]$ and $v _ { \pi } : =$ $\mathbb { E } _ { \nu _ { \pi } } [ r ( s , m , s ^ { \prime } ) \varphi ( s ) ]$ . The optimal solution to the critic’s TD learning is now $\stackrel { \cdot } { w ^ { \ast } } : = - L _ { \pi } ^ { - 1 } v _ { \pi }$ .

Assumption 5.3. There exists a positive constant $\Gamma _ { L }$ such that for all $w \in \mathbb { R } ^ { d }$ , we have $\langle w - w ^ { * } , L _ { \pi } ( w - w ^ { * } ) \rangle \leqslant$ $- \Gamma _ { L } \left\| \boldsymbol { w } - \boldsymbol { w } ^ { * } \right\| _ { 2 } ^ { 2 }$ .

Based on the above two assumptions, let LV := 2 2Cκξ+11−γ , $\begin{array} { r } { L _ { V } : = \frac { 2 \sqrt { 2 } C _ { \kappa \xi } + 1 } { 1 - \gamma } } \end{array}$ 1- ， where $\begin{array} { r } { C _ { \kappa \xi } = \left( 1 + \left\lceil \log _ { \xi } \frac { 1 } { \kappa } \right\rceil + \frac { 1 } { 1 - \xi } \right) } \end{array}$ .

Theorem 5.4. Consider the Actor-Critic improper learning algorithm ACIL (Alg 2). Assume $\mathrm { s u p } _ { s \in \mathcal { S } } \| \varphi ( s ) \| _ { 2 } \leqslant 1$ . Under Assumptions 5.2 and 5.3 with step-sizes chosen as $\begin{array} { r } { \alpha = \bigg ( \frac { 1 } { 4 L _ { V } \sqrt { M } } \bigg ) , \beta = \operatorname* { m i n } \big \{ \mathcal { O } \left( \Gamma _ { L } \right) , \mathcal { O } \left( 1 / \Gamma _ { L } \right) \big \} } \end{array}$  14L √M , β = min {O (ΓL) , O (1/ΓL)}, batchsizes $\begin{array} { r } { H = \mathcal { O } \left( \frac { 1 } { \varepsilon } \right) , B = \mathcal { O } \left( 1 / \varepsilon \right) , T _ { c } = \mathcal { O } \left( \frac { \sqrt { M } } { \Gamma _ { L } } \log ( 1 / \varepsilon ) \right) } \end{array}$ , $\begin{array} { r l r } { T } & { { } = } & { { \mathcal O } \left( \frac { \sqrt { M } } { ( 1 - \gamma ) ^ { 2 } \varepsilon } \right) } \end{array}$  M(1−γ )2 ε , we have E[

∇θ V (θT )
