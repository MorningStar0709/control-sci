To proceed, recalling the regret $\mathtt { R _ { x } } ( T )$ for the state estimation problem defined in (21), we know that the expected regret of any deterministic algorithm satisfies that

$$\mathbb {E} \left[ \mathrm{R} _ {\mathbf {x}} (T) \right] = \mathbb {E} \left[ \sum_ {t = 0} ^ {T - 1} \left((\hat {x} _ {t} - x _ {t}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}} - x _ {t}) ^ {2}\right) \right], \tag {D.3}$$

where $\mathbb { E } [ \cdot ]$ denotes the expectation with respect to $\{ w _ { t } \} _ { t \ge 0 } , \{ v _ { t } \} _ { t \ge 0 }$ and $r , \hat { x } _ { t }$ is the state estimate returned by the algorithm, and $\hat { x } _ { t } ^ { \mathrm { K F } }$ is the Kalman filter given by (2). For our analysis in the sequel, let $\mathbb { E } _ { w , v } [ \cdot ]$ denote the expectation with respect to $\{ w _ { t } \} _ { t \ge 0 }$ and $\{ v _ { t } \} _ { t \geq 0 }$ , and let $\mathbb { E } _ { r } [ \cdot ]$ denote the expectation with respect to r. Moreover, let $\hat { x } _ { t } ^ { \mathbf { s ( i ) } }$ and $\hat { x } _ { t } ^ { \bar { \mathrm { K F } } , \mathbf { s } ( \mathrm { i } ) }$ respectively denote the state estimate returned by the deterministic algorithm and the Kalman filter, conditioned on $r = r _ { i } ,$ for all $i \in \{ 1 , 2 , 3 \}$ , which are initialized as $\hat { x } _ { 0 } ^ { \mathrm { K F , s ( i ) } } = \hat { x } _ { 0 } ^ { \mathbf { s ( i ) } } = 0$ = xˆs(0 . We then deduce that for any $t \in \{ 0 , \ldots , T - 1 \}$ , the corresponding term within the summation in (D.3) satisfies

$$
\begin{array}{l} \mathbb {E} \big [ (\hat {x} _ {t} - x _ {t}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}} - x _ {t}) ^ {2} \big ] = \mathbb {E} _ {w, v} \Big [ \mathbb {E} _ {r} \big [ (\hat {x} _ {t} - x _ {t}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}} - x _ {t}) ^ {2} \big ] \Big ] \\ = \mathbb {E} _ {w, v} \Big [ \sum_ {i = 1} ^ {3} \frac {1}{3} \big ((\hat {x} _ {t} ^ {\mathbf {s (i)}} - x _ {t} ^ {\mathbf {s (i)}}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}, \mathbf {s (i)}} - x _ {t} ^ {\mathbf {s (i)}}) ^ {2} \big) \Big ] \\ = \sum_ {i = 1} ^ {3} \frac {1}{3} \underbrace {\mathbb {E} _ {w , v} \left[ (\hat {x} _ {t} ^ {\mathbf {s} (\mathbf {i})} - x _ {t} ^ {\mathbf {s} (\mathbf {i})}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF} , \mathbf {s} (\mathbf {i})} - x _ {t} ^ {\mathbf {s} (\mathbf {i})}) ^ {2} \right]} _ {R _ {t} ^ {\mathbf {s} (\mathbf {i})}}, \tag {D.4} \\ \end{array}
$$

where the first equality follows from the fact that r is independent of $\{ w _ { t } \} _ { t \ge 0 }$ and $\{ v _ { t } \} _ { t \geq 0 }$ .
