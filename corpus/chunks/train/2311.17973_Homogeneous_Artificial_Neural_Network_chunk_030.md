where $\Xi \subset \mathbb { R } ^ { n \times n }$ is a set of admissible $n \times n$ matrices and

$$J _ {\epsilon} (G) = \frac {1}{M _ {\delta}} \int_ {\Omega_ {\delta}} \int_ {B _ {\delta}} \mathcal {J} _ {\epsilon} ^ {2} (x ^ {*} + z, x ^ {*}, G) d z d x ^ {*}, \tag {31}\mathcal {J} _ {\epsilon} (x, x ^ {*}, G) = \int_ {x ^ {*}} ^ {x} (\mathrm{trace} (G) + \nu) g _ {\epsilon} (y) - \sum_ {i: x _ {i} \neq x _ {i} ^ {*}} \Delta_ {i} (g _ {\epsilon}, y, x, x ^ {*}, G _ {\mathbf {d}}) d y,$$

and $\begin{array} { r } { M _ { \delta } = \int _ { \Omega _ { \delta } } d x \cdot \int _ { B _ { \delta } } } \end{array}$ dz. If $G _ { \epsilon }$ is anti-Hurwitz then, in the view of the following theorem, it can be interpreted as a generator of the practical dilation in $\mathbb { R } ^ { n }$ .

Theorem 4 Let $\Xi \subset \mathbb { R } ^ { n \times n }$ be a class of admissible generators of linear $d i -$ lations. Let $g _ { \epsilon }$ be an ANN (11) approximating a function $h : \mathbb { R } ^ { n } \mapsto \mathbb { R }$ on a compact $\Omega \subset \mathbb { R } ^ { n }$ with an error $\epsilon > 0$ , i.e., the inequality (10) is fulfilled for g = h. If h is locally Lipschitz continuous on $\mathbb { R } ^ { n } \backslash \{ \mathbf { 0 } \}$ and d-homogeneous of degree $\nu \in \mathbb { R }$ with respect to a linear dilation $\mathbf { d } ( s ) = e ^ { G _ { \mathbf { d } } s } , s \in \mathbb { R } ^ { n }$ with $G _ { \mathbf { d } } \in \Xi$ , then

$$\min _ {G \in \Xi} J _ {\epsilon} (G) \to 0 a s \epsilon \to 0.$$

Proof. $\mathrm { B y }$ Theorem 1, we have

$$\int_ {x ^ {*}} ^ {x} (\mathrm{trace} (G _ {\mathbf {d}}) + \nu) h (y) - \sum_ {i: x _ {i} \neq x _ {i} ^ {*}} \Delta_ {i} (h, y, x, x ^ {*}, G _ {\mathbf {d}}) d y = 0$$

for all $x , x ^ { * } \in \mathbb { R } ^ { n } \backslash \{ { \bf 0 } \}$ such that $R ( x , x ^ { * } ) \in \mathbb R ^ { n } \backslash \{ \mathbf { 0 } \}$ . Since $g _ { \epsilon } ( x )  h ( x )$ as $\epsilon  0$ uniformly on compacts from Ω then

$$\int_ {x ^ {*}} ^ {x} (\mathrm{trace} (G _ {\mathbf {d}}) + \nu) g _ {\epsilon} (y) - \sum_ {i: x _ {i} \neq x _ {i} ^ {*}} \Delta_ {i} (g _ {\epsilon}, y, x, x ^ {*}, G _ {\mathbf {d}}) d y \to 0$$

as $\epsilon  0$ for all $x , x ^ { * } \in \Omega _ { \delta }$ . This means that

$$\min _ {G \in \Xi} J _ {\epsilon} (G) \to 0 \quad \mathrm{as} \quad \epsilon \to 0.$$

The proof is complete

Notice that the above theorem implies that

$$G _ {\mathbf {d}} \in \mathcal {P} := \lim _ {\epsilon \to 0} \operatorname{argmin} _ {G \in \Xi} J _ {\epsilon} (G).$$
