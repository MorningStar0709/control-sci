# §3. Main results

§ 3.1. Stabilizing scheduling logics. We first present our solution to Problem 1. We will operate under the following assumption:

Assumption 3. The total number of plants, ?? and the capacity of the shared communication network, ?? together satisfy $N ^ { \mathrm { q } } \mathrm { o } M = 0$ .

Assumption 3 ensures that the total number of plants, ??, in the NCS is divisible by the capacity of the shared network, ??. In other words, the ?? plants can be divided into an integer number of chunks of ?? plants. Let $v = N / M$ . Towards designing a scheduling logic, we rely on disjoint sets $c _ { 1 } , c _ { 2 } , \ldots , c _ { v } \in S$ and scalars $p _ { c _ { 1 } } , p _ { c _ { 2 } } , . . . , p _ { c _ { v } } \in ] 0 , 1 [$ [ that satisfy $\sum _ { j = 1 } ^ { v } p _ { c _ { j } } = 1$ .

Suppose that $c _ { 1 } , c _ { 2 } , \ldots , c _ { v }$ and $p _ { c _ { 1 } } , p _ { c _ { 2 } } , . . . , p _ { c _ { v } }$ are fixed. A scheduling logic, ??, is generated as follows: at each time instant $t = 0 , 1 , 2 , . . . ,$ we allocate the shared network to the plants in $c _ { j }$ with probability $p _ { c _ { j } } , { } ~ j \in \{ 1 , 2 , \ldots , v \}$ . This procedure is summarized in Algorithm $1 . ^ { 2 }$ The following theorem provides necessary and sufficient conditions on the matrices, $A _ { i } , B _ { i } , K _ { i } , i = 1 , 2 , . . . , N$ and the capacity of the network, ??, under which a scheduling logic, ??, obtained from Algorithm 1 ensures stochastic stability of each plant in the NCS.

Algorithm 1 Design of a scheduling logic   
1: Set $v = N / M$ .
2: Pick $c_1, c_2, \ldots, c_v \in S$ such that $c_j \cap c_k = \emptyset$ for all $j, k = 1, 2, \ldots, v, j \neq k$ .
3: Pick $p_{c_1}, p_{c_2}, \ldots, p_{c_v} \in ]0, 1[$ such that $\sum_{j=1}^{v} p_{c_j} = 1$ .
4: for $t = 0, 1, 2, \ldots$ do
5: Set $\gamma(t) = c_j$ with probability $p_{c_j}$ .
6: end for

Theorem 1. Consider an NCS described in §2. Suppose that Assumption 3 holds. Let ?? be a scheduling logic obtained from Algorithm 1. Each plant ?? in (1) is stochastically stable under ?? if and only if the following conditions hold: for each $i \in c _ { j } , j = 1 , 2 , \ldots , v ,$ , there exist symmetric and positive definite matrices $P _ { k } \in \mathbb { R } ^ { d _ { i } \times d _ { i } } , k = i _ { s } , i _ { u } ,$ , such that

$$A _ {k} ^ {\top} \mathcal {P} ^ {i} A _ {k} - P _ {k} < 0, \tag {2}$$

where $\mathcal P ^ { i } = p _ { c _ { j } } P _ { i _ { s } } + ( 1 - p _ { c _ { j } } ) P _ { i _ { u } } .$
