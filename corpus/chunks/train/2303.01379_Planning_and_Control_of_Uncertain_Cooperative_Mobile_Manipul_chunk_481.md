# 6.2.3 Simulation Results

To demonstrate the proposed scheme, we consider a task and motion planning problem for a robot operating in a planar office environment. In particular, we consider three points of interest and therefore have $\Pi = \{ c _ { \pi _ { k } } \} _ { k \in \mathcal { K } _ { \mathcal { R } } } .$ where $\mathcal { K } _ { \mathcal { R } } = \{ 1 , 2 , 3 \}$ . The corresponding regions of interest $\pi _ { k } = \bar { B } ( c _ { \pi _ { k } } , r )$ , where $r _ { \pi _ { k } } = 0 . 2$ for $k \in \mathcal { K } _ { \mathcal { R } }$ , define the set $\Pi = \left\{ \pi _ { k } \right\} _ { k \in \mathcal { K } _ { \mathcal { R } } }$ . The set of atomic prepositions is $\Psi = \tilde { \Pi }$ and the labeling function $\mathcal { L } : \Pi  2 ^ { \Psi }$ is defined as $\pi _ { k } \mapsto \{ \pi _ { k } \} , k \in \mathcal { K } _ { \mathcal { R } }$ . The scenario setting is illustrated in Figure 6.1.

We require that the robot “always visits each region of interest at least once every 120 time units” which is equivalent to the MITL formula $\Phi = \Lambda _ { k \in \mathcal { K } _ { \mathcal { R } } } \left( \boxed { - } 0 , 1 2 0 \middle ] \pi _ { k } \right)$ . The robot is initially located at $c _ { \pi _ { 1 } } \in \pi _ { 1 }$ and, therefore, the infinitely repeating cycle of transitions $\pi _ { 1 } \xrightarrow { t _ { 1 , 2 } ( 1 ) } \pi _ { 2 } \xrightarrow { t _ { 2 , 3 } ( 1 ) }$ t1,2(1) π $\pi _ { 3 } \xrightarrow { t _ { 3 , 1 } ( 1 ) } \pi _ { 1 } \xrightarrow { t _ { 1 , 2 } ( 2 ) }$ π t1,2(2)−−−−→ . . . with appropriately assigned transition times is an accepting run. Let $t : 0 , 1 , . . . \to \mathbb { Q } _ { > 0 } ^ { 3 } , \kappa \mapsto [ t _ { 1 , 2 } ( \kappa ) , t _ { 2 , 3 } ( \kappa ) , t _ { 3 , 1 } ( \kappa ) ] ^ { \intercal }$ which is defined recursively as follows: $t ( 0 ) : = [ 0 , 0 , 0 ] ^ { \top }$ , then assuming t(κ) is defined

for some $\kappa \in  { \mathbb { N } } _ { 0 }$

$$
t (\kappa + 1) := (U _ {3} ^ {\top}) ^ {\kappa} \left[ \begin{array}{c c c} 1 & 0 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{array} \right] U _ {3} ^ {\kappa} \hat {t} (\kappa) + (U _ {3} ^ {\top}) ^ {\kappa} \left[ \begin{array}{c c c} 0 & 0 & 0 \\ 0 & 1 & 0 \\ 0 & 0 & 1 \end{array} \right] U _ {3} ^ {\kappa} t (\kappa),
$$

where $U _ { 3 } : = { \left[ \begin{array} { l l l } { 0 } & { 1 } & { 0 } \\ { 0 } & { 0 } & { 1 } \\ { 1 } & { 0 } & { 0 } \end{array} \right] }$ is the upper shift matrix and $\hat { t } ( \kappa )$ is the solution of the optimization problem (6.2) under the following constraints:
