# Exercise 2.18: Unreachable setpoint MPC and evolution in a compact set

Prove the following lemma, which is useful for analyzing the stability of MPC with an unreachable setpoint.

Lemma 2.46 (Evolution in a compact set). Suppose $x ( 0 ) = x$ lies in the set $\mathcal { X } _ { N }$ . Then the state trajectory {x(i)} where, for each i, $x ( i ) = \phi _ { f } ( i ; x )$ of the controlled system $x ^ { + } = f ( x )$ evolves in a compact set.
