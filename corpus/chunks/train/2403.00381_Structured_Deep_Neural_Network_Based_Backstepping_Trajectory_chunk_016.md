Theorem 3. Consider the system (2) with a bounded disturbance $\| \tau ^ { d } \| ^ { 2 } \leq d .$ If $D ( z _ { 2 } ) \geq { \frac { 1 } { 2 } } I$ and $\begin{array} { r } { \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } \geq \alpha I , } \end{array}$ , then the tracking error under the controller (9) will converge to the set $\begin{array} { r } { \{ z _ { \mathbf { 1 } } | \| z _ { \mathbf { 1 } } \| ^ { 2 } \leq \frac { 1 } { 2 } \frac { 1 } { \alpha ^ { 2 } } d \} } \end{array}$ .

Proof. The Taylor expansion of $\frac { \partial \Phi } { \partial z _ { 1 } }$ ∂z1 at $z _ { 1 } = 0$ is

$$\frac {\partial \Phi}{\partial z _ {1}} = \left. \frac {\partial^ {2} \Phi}{\partial z _ {1} ^ {2}} \right| _ {z _ {1} = 0} z _ {1} + \delta \tag {12}$$

where δ contains high-order terms. We can approximate $\frac { \partial \Phi } { \partial z _ { 1 } }$ ∂z1 with $\begin{array} { r } { \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } z _ { 1 } } \end{array}$ . Then from $\begin{array} { r } { \frac { \partial \Phi } { \partial z _ { 1 } } ^ { \top } \frac { \partial \Phi } { \partial z _ { 1 } } \ \leq \ \frac { 1 } { 2 } d , } \end{array}$ we have 2 $\begin{array} { r } { z _ { 1 } ^ { \top } \left( \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } \right) ^ { z } z _ { 1 } \leq \frac { 1 } { 2 } d . } \end{array}$ z⊤1 . Since $\begin{array} { r } { \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } \geq \alpha I , } \end{array}$ , therefore $\begin{array} { r } { \alpha ^ { 2 } \| \dot { z } _ { 1 } \| ^ { 2 } \leq \frac { 1 } { 2 } d , } \end{array}$ that is, $\begin{array} { r } { \| z _ { 1 } \| ^ { 2 } \leq \frac { 1 } { 2 } \frac { 1 } { \alpha ^ { 2 } } d . } \end{array}$ 12 1α2 d.

These analyses imply that we can constrain the Hessian matrix of Φ at $z _ { 1 } = 0$ to improve the tracking error performance under disturbances. This can be achieved by adding a regularizer ReLu $\begin{array} { r } { \bigg ( \lambda _ { \operatorname* { m a x } } \left( \alpha I - \left. \frac { \partial ^ { 2 } \Phi } { \partial z _ { 1 } ^ { 2 } } \right| _ { z _ { 1 } = 0 } \right) \bigg ) } \end{array}$ λmax during training or by letting the hyperparameter S satisfy $\textit { \textbf { S } } \geq \ \alpha I$ in $\Phi ( z _ { 1 } ; \theta _ { 1 } , S )$ .
