In fact, from (86)∼(88) in the proof of Theorem 3.1, due to $k _ { c } > L _ { d }$ and $k _ { 2 } > L _ { d }$ in the sliding mode, the influence of bounded disturbances or uncertainties can be completely rejected.
Furthermore, for this sliding mode, the condition on the stochastic disturbance $d \left( t \right)$ is relax, and $d \left( t \right)$ is only required to be bounded.
3) Bounded gain of the sliding mode.
From (20), the selection of $e _ { 1 c } \in ( 0 , k _ { 2 M } - L _ { d } ) { \mathrm { ~ ( i .
e .
, ~ } } e _ { 1 c } =$ $\left| e _ { 1 } \left( t _ { c } \right) \right| )$ and $e _ { 2 c } \in ( e _ { 1 c } , \sqrt { ( k _ { 2 M } - L _ { d } ) e _ { 1 c } } \Big | \mathrm { ~ ( i .
e .
, ~ } e _ { 2 c } = | e _ { 2 } \mathrm { ~ ( } t _ { c } ) | )$ makes $| e _ { 1 } \left( t _ { c } \right) | , | e _ { 2 } \left( t _ { c } \right) |$ and $\frac { e _ { 2 } ^ { 2 } ( t _ { c } ) } { 2 | e _ { 1 } ( t _ { c } ) | }$ （24号 all bounded.
Therefore, k2 is bounded, and the non-overshooting performance is guaranteed.
$\it 4 )$ Chattering phenomenon in the sliding variable $e _ { 2 } \left( t \right)$ .
For the sliding mode (18), due to the switching functions in the $e _ { 2 } \left( t \right)$ dynamic equation, chattering may exist in $e _ { 2 } \left( t \right)$ .

Remark 3.2 (Parameter conditions that cause slow convergence): For the selection of $k _ { 1 }$ and $k _ { 2 }$

we can use the partitioning $e _ { 1 } \left( t _ { c } \right) e _ { 2 } \left( t _ { c } \right) < 0$ and $e _ { 1 } \left( t _ { c } \right) e _ { 2 } \left( t _ { c } \right) \geq 0$ , and we get

$$
k _ {1} \in \left\{ \begin{array}{l} \left(0, \frac {\left| e _ {2} (t _ {c}) \right|}{\left| e _ {1} (t _ {c}) \right|}\right), \text {   if   } e _ {1} \left(t _ {c}\right) e _ {2} \left(t _ {c}\right) <   0; \\ (0, \infty), \text {   if   others } \end{array} \right. \tag {22}

k _ {2} > \left\{ \begin{array}{l} \max \left\{k _ {1} \left| e _ {2} \left(t _ {c}\right) \right| + L _ {d}, \frac {e _ {2} ^ {2} \left(t _ {c}\right)}{2 \left| e _ {1} \left(t _ {c}\right) \right|} + L _ {d} \right\}, \text {   if   } e _ {1} \left(t _ {c}\right) e _ {2} \left(t _ {c}\right) <   0; \\ \max \left\{k _ {1} \left| e _ {2} \left(t _ {c}\right) \right| + L _ {d}, \frac {k _ {1} ^ {2}}{3} \left(\left| e _ {1} \left(t _ {c}\right) \right| + \sqrt {e _ {1} ^ {2} \left(t _ {c}\right) + 3 \left(\frac {e _ {2} \left(t _ {c}\right)}{k _ {1}}\right) ^ {2}}\right) + L _ {d} \right\}, \text {   if   others } \end{array} \right. \tag {23}
$$

Thus, the system (18) is also globally exponentially stable, and no overshoot exists in $e _ { 1 } \left( t \right)$
