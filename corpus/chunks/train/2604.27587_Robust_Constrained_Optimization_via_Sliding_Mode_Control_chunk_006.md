# 2.1 Notations

Let Rn denote the set of real column vectors of dimension n. The p-norm $\| { \boldsymbol { x } } \| _ { p }$ for $p \in \{ 1 , 2 , \infty \}$ is defined by: $\begin{array} { r } { \| \boldsymbol { x } \| _ { 1 } = \sum | x _ { i } | , \| \boldsymbol { x } \| _ { 2 } } \end{array}$ (Euclidean norm), and $\| x \| _ { \infty } =$ max xi . The absolute value of a scalar is denoted by $| \cdot | .$ . For a matrix $A \in \mathbb { R } ^ { m \times n }$ , the range is range $( A ) =$ $\{ A x \}$ , and the kernel is $\ker ( A ) = \{ x : A x = 0 \}$ . The Jacobian of a function $\begin{array} { r } { \begin{array} { l } { \displaystyle h ~ : ~ \mathbb { R } ^ { n } ~  } \end{array} } \end{array}$ Rm is the matrix Jh(x) ∈ Rm×n with entries ∂hi∂xj . $J _ { h } ( x ) \in \mathbb { R } ^ { m \times n }$ The closed ball of ra- $\frac { \partial h _ { i } } { \partial x _ { i } }$ dius $\delta > 0$ is $B _ { \delta } ( 0 ) = \{ x : \| x \| _ { 2 } \leq \delta \}$ . The sign function is sign(x) = x/ x for $x \neq 0$ . The set-valued signum function is defined as $\operatorname { S G N } ( \dot { S } ( x ) _ { i } ) = \{ + 1 \} \mathrm { i f } \ S ( x ) _ { i } > 0 ,$ , $[ - 1 , 1 ] \mathrm { i f } S ( x ) _ { i } = 0$ , and 1 if $S ( x ) _ { i } < 0 . \mathrm { ~ A ~ }$ differential inclusion ${ \dot { x } } \in { \mathcal { F } } ( x )$ becomes an equality ${ \dot { x } } = { \mathcal { F } } ( x )$ when $\mathcal F ( x )$ is a singleton. A function $\mathsf { \bar { \phi } } \in \mathsf { \bar { C } } ^ { 1 } ( \mathbb { R } ^ { n } , \mathbb { R } )$ is µ-strongly convex $( \bar { \mu } > 0 ) \mathrm { i f } \phi ( y ) \geq \phi ( x ) + \nabla \phi ( x ) ^ { \top } ( \bar { y } -$ $\begin{array} { r } { \dot { \boldsymbol { x } } ) + \frac { \mu } { 2 } \lVert \boldsymbol { \breve { y } } - \boldsymbol { x } \rVert ^ { 2 } , \quad \boldsymbol { \ddot { \forall } } \boldsymbol { x } , \boldsymbol { y } \in \mathbb { R } ^ { n } } \end{array}$ .
