$$
c _ {i} (A - B K) ^ {d _ {i} + 1} = \mathbf {0} (\text { 见 式 } (5. 1 1 0) \text { 的 推 导 })
\begin{array}{l} \mathbf {c} _ {i} (A - B K) ^ {d _ {i} + 2} = \mathbf {c} _ {i} (A - B K) ^ {d _ {i} + 1} (A - B K) = \mathbf {0} \\ \dots \dots \end{array} \tag {5.113}
\mathbf {c} _ {i} (A - B K) ^ {d _ {i} + n} = \mathbf {0}
$$

而 $\mathbf{c}_i(A - BK)^{d_i} \neq 0$ ，故得

$$\bar {a} _ {0} = 0 \tag {5.114}$$

类似地，将(5.112)等式两边乘以 $c_{i}(A - BK)^{d_{i} - 1}$ ，又可导出

$$\bar {a} _ {1} = 0 \tag {5.115}$$

照此步骤,还可证得

$$\bar {a} _ {2} = 0, \dots , \bar {a} _ {d _ {i}} = 0 \tag {5.116}$$

从而，将(5.114)—(5.116)代入 $\alpha(s)$ 中，可进而把 $\alpha(s)$ 表示为：

$$
\begin{array}{l} \bar {a} (s) = s ^ {n} + \bar {a} _ {n - 1} s ^ {n - 1} + \dots + \bar {a} _ {d _ {i} + 1} s ^ {d _ {i} + 1} \\ = s ^ {d _ {i} + 1} \left(s ^ {n - d _ {i} - 1} + \bar {a} _ {n - 1} s ^ {n - d _ {i} - 2} + \dots + \bar {a} _ {d _ {i} + 1}\right) \tag {5.117} \\ \end{array}
$$

这样，由(5.111)和(5.117)，就可把 $g_{KLi}(s)$ 表示为：

$$
\begin{array}{l} \mathbf {g} _ {K L i} (s) = E _ {i} L \left(\frac {1}{s ^ {d _ {i} + 1}}\right) = (0 \dots 0, 1, 0 \dots 0) E E ^ {- 1} \left(\frac {1}{s ^ {d _ {i} + 1}}\right) \\ = \left(0 \dots 0, \frac {1}{s ^ {d _ {i} + 1}}, 0 \dots 0\right) \tag {5.118} \\ \end{array}
$$

这表明，在(5.104)的 $\{L, K\}$ 的选取下，闭环系统的传递函数矩阵

$$
G _ {K L} (s) = \left[ \begin{array}{c c c} \frac {1}{s ^ {d _ {i} + 1}} & & \\ & \ddots & \\ & & \frac {1}{s ^ {d _ {p} + 1}} \end{array} \right] \tag {5.119}
$$

为对角阵且非奇异，也即实现了解耦。于是，充分性得证。至此，证明完成。

进一步，从上述结论及其证明过程中，还可以直接引伸出如下的几点推论：

（1）此结论表明，受控系统（5.72）能否可采用状态反馈和输入变换来实现解耦，唯一地决定于其传递函数矩阵 $G(s)$ 的两组特征量 $d_{i}$ 和 $E_{i}(i=1,2,\cdots,p)$ 。从表面上看，系统的能控性或能镇定性在这里是无关重要的。但是，从解耦后的系统要能正常地运行并具有良好的动态性能而言，仍需要求受控系统是能控的，或至少是能镇定的。否则，甚至不能保证闭环系统是渐近稳定的，此时解耦控制也就失去了意义。  
(2) 为了判断受控系统 (5.72) 能否可采用状态反馈和输入变换来实现解耦, 既可从系统的传递函数矩阵描述来组成判别矩阵 $E$ , 也可从系统的状态空间描述来组成判别矩阵 $E$ 。  
(3) 从结论的证明过程可以看出, 对于一个可解耦的受控系统, 当选取 $\{L, K\}$ 为:

$$L = E ^ {- 1}, \quad K = E ^ {- 1} F \tag {5.120}$$

时必可使系统实现解耦，且解耦控制系统的传递函数矩阵为

$$
\mathcal {G} _ {K L} (s) = \left[ \begin{array}{c c c} \frac {1}{s ^ {d _ {1} + 1}} & & \\ & \ddots & \\ & & \frac {1}{s ^ {d _ {p} + 1}} \end{array} \right] \tag {5.121}
$$

从物理上看,解耦后每个单输入-单输出闭环控制系统的传递函数均具有多重积分器的特性,因此常称这类形式的解耦为积分型解耦。积分型解耦系统虽然因其不能令人满意的动态性能,导致其本身没有实际应用价值,但它常常是综合性能满意的解耦控制系统中的一个中间性步骤,因而仍有研究的价值。

确定解耦控制矩阵对 $\{L, K\}$ 的算法 给定受控系统为

$$\dot {\boldsymbol {x}} = \boldsymbol {A} \boldsymbol {x} + \boldsymbol {B} \boldsymbol {u} \tag {5.122}\mathbf {y} = C \mathbf {x}$$
