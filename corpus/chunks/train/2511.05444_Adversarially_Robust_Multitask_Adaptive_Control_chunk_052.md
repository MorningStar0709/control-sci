$$S - \Sigma_ {z _ {t}} ^ {(\ell)} = \Sigma_ {t} ^ {1 / 2} \left(\frac {1}{\tau_ {k , 1}} \sum_ {t} y _ {t} y _ {t} ^ {\top} - I _ {d}\right) \Sigma_ {t} ^ {1 / 2}, \text {and thus} \| S - \Sigma_ {z _ {t}} ^ {(\ell)} \| \leq \| \Sigma_ {z _ {t}} ^ {(\ell)} \| \left\| \frac {1}{\tau_ {k , 1}} \sum_ {t} y _ {t} y _ {t} ^ {\top} - I _ {d} \right\|.$$

As $\{ z _ { t } \}$ t are dependent over time $t ,$ we use a blocking technique to construct “independent blocks” of data in $\mathcal { T } _ { 1 }$ . Therefore, let us partition $\mathcal { T } _ { 1 }$ into q kept blocks of length $m _ { 1 }$ separated by gaps of length $m _ { 2 }$ . In addition, we denote m = m1 + m2, $q = \lfloor \tau _ { k , 1 } / m \rfloor$ . For the kept blocks, we define the block averages $\begin{array} { r } { \overline { { Y } } _ { j } : = \frac { 1 } { m _ { 1 } } \sum _ { t \in \mathcal { B } _ { j } } ( y _ { t } y _ { t } ^ { \top } - I _ { d } ) } \end{array}$ . Therefore, there exist i.i.d. copies $\{ \overline { { Y } } _ { j } ^ { \prime } \} _ { j = 1 } ^ { q }$ with the same marginals such that $\mathbb { P } \{ \exists j : \overline { { Y } } _ { j } \neq \overline { { Y } } _ { j } ^ { \prime } \} \leq q \beta ( m _ { 2 } ) \leq q C _ { \beta } \rho ^ { m _ { 2 } }$ . This implies that, with probability at least $1 - q C _ { \beta } \rho ^ { m _ { 2 } }$ , the kept blocks behave as independent.

Conditional on the coupling event, we apply an effective-rank covariance deviation bound to $\textstyle { \frac { 1 } { q } } \sum _ { j = 1 } ^ { q } { \overline { { Y } } } _ { j } ^ { \prime }$ , where each $\overline { { Y } } _ { j } ^ { \prime }$ is an average of $m _ { 1 }$ i.i.d. variables, where its sub-exponential norm is uniformly bounded, and its second moment has effective rank at most $r _ { \mathrm { e f f } } ^ { ( \ell ) } = \mathrm { t r } ( \Sigma _ { t } ) / \| \Sigma _ { t } \|$ . A standard Gaussian covariance concentration $\left( \mathrm { e . g . } \right.$ , matrix Bernstein [Vershynin, 2018]) yields, for all $s > 0$ ,

$$\mathbb {P} \left\{\left\| \frac {1}{q} \sum_ {j = 1} ^ {q} \overline {{Y}} _ {j} ^ {\prime} \right\| \geq C _ {Y} \left(\sqrt {\frac {r _ {\mathrm{eff}} ^ {(\ell)} + s}{q m _ {1}}} + \frac {r _ {\mathrm{eff}} ^ {(\ell)} + u}{q m _ {1}}\right) \right\} \leq 2 e ^ {- s}.$$

As the contribution of discarded intervals of samples dilutes the kept sample size by at most a factor $\frac { m _ { 1 } } { m _ { 1 } + m _ { 2 } }$ , we conclude that
