# IV. BODE PLOTS FOR LIPSCHITZ CONTROL DESIGN

The control design strategy considered here involves shaping the control signal through its time derivative. In this formulation, the control input is obtained as the integral of a high-frequency switching function, resulting in a continuous control signal despite the discontinuity in its derivative [3]. Let the tracking error be denoted by $\sigma \in \mathbb { R } ,$ , whose dynamics are governed by the first-order system of equation (1). The sliding variable is now defined as

$$S (t) = \dot {\sigma} (t) + b \sigma (t). \tag {39}$$

where $b > 0$ is a design parameter. Implementation of the socalled Lipschitz continuous sliding-mode controller requires differentiation of the error signal and takes the form

$$\dot {u} (t) = \rho \operatorname{sign} (S (t)), \tag {40}$$

with $\rho \mathrm { ~  ~ { ~ \gamma ~ } ~ } > \mathrm { ~  ~ { ~ \theta ~ } ~ }$ as the controller parameter. Finite-time convergence of the sliding variable (39) to the origin can be demonstrated using the Lyapunov function $V ( t ) = S ^ { 2 } ( t ) / 2$ , which yields the inequality $\dot { V } \leq - \alpha | \sigma |$ , where $\alpha = \rho - \bar { L } - b L > 0$ . Here, the perturbation f (t) and its derivative are assumed to be uniformly bounded, satisfying $| f | \le L$ and $| { \dot { f } } | \leq { \bar { L } }$ . Thus, once the zero dynamics $( S \equiv 0 )$ is achieved in finite time $t \geq t _ { r } ,$ the exponential convergence of tracking error satisfy $\sigma ( t ) = \sigma ( 0 ) \exp ( - b t )$ . The necessary and sufficient condition for robust stabilization is that the controller gain satisfies $\rho > \bar { L } + b L$ [2]. Under this condition, the convergence time is bounded by $t _ { r } \le \sqrt { 2 V ( 0 ) } / \alpha$ . It is important to note that the initial value of the sliding variable S(0) in (39) depends not only on the initial tracking error $\sigma ( 0 )$ , but also on the instantaneous values of the perturbation signal f (0) and its derivative ${ \dot { f } } ( 0 )$ .

According to [45], the linear block is constructed as the cascade of the critically damped second-order actuator dynamics (4), the nominal (undisturbed) tracking error dynamics (1), and the sensor-based reconstruct TF is given by

$$W (s) = \frac {s + b}{s ^ {2} (\mu s + 1) ^ {2}}, \tag {41}$$
