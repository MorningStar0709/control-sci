# 4.2.2. Terminal ingredients for a fully branched scenario tree

Let us consider the case when the scenario tree is fully branched i.e. $N _ { R } = N$ . The stabilizing control law for each parameter realization inside the terminal region cannot be determined independently due to the NACs at the leaf node. Therefore, a common terminal control law must exist $( { \mathrm { i . e . ~ } } h _ { { \mathrm { \scriptsize ~ { \scriptsize ~ { \mathit { \varepsilon } } } } } } ^ { r } ( x ) = \kappa _ { f } ( x )$ , for all r).

κAfter linearizing (1) as outlined in Section 4.2.1, the following steps are used to determine the terminal ingredients for the adaptive horizon multi-stage MPC with a fully branched scenario tree:

1. Select matrices $Q ,$ , and R, for the LQR and choose a K such that the common terminal control law $\kappa _ { f } ( x ) = - K .$ x is stabilizing for all parameter realizations r, and $- K x \in \mathbb { U } , \forall x \in \mathbb { X }$ ,   
2. Solve for $P _ { r }$ in the discrete Lyapunov equation $A _ { r } ^ { \top } P _ { r } A _ { r } + Q = P _ { r } ^ { \top }$ , for each parameter realization r,   
3. Use $Q , R , A _ { K }$ to obtain the terminal radii for each parameter realization r by evaluating (10) in Lemma 2 where $A _ { K _ { r } } = A _ { r } - B _ { r } K$ . Then pick the smallest as the common terminal region’s radius,   
4. Obtain the terminal cost function as the weighted sum $\begin{array} { r } { \psi ( x ) = \sum _ { r } x ^ { r ^ { \top } } P _ { r } x ^ { r } . } \end{array}$

Steps 3 and 4 above are similar procedures as in Section 4.2.1 only that K is common for all realizations r.
