# A.1 A technical lemma

We here introduce Hoffman’s error bound for a system of linear equalities.

Theorem 5 (Hoffman’s error bound [57]) Let the system of linear equalities $A x \ = \ \bar { b }$ with $\boldsymbol { x } ~ \in ~ \mathbb { R } ^ { n _ { x } }$ be consistent and $\bar { \mathcal F }$ be its feasible solution set. Fix any $p \in [ 1 , \infty ]$ . Then, there exists a constant ${ \bar { L } } ,$ such that $f o r \ a n y \ x \ \in \ \mathbb { R } ^ { n }$ there exists a point $x ^ { \ast } \in \bar { \mathcal { F } }$ satisfying $\| x - x ^ { * } \| _ { p } \leq \bar { L } \| A x - \bar { b } \| _ { p }$ .

This theorem shows that if a vector does not satisfy the linear equality constraints, then its distance to the feasible set can be bounded by the size of the constraint violation. We now state a key technical lemma that will be used in the proof of Theorem 1.

Lemma 2 (Linear perturbation) Let $A _ { 1 } ~ \in ~ \mathbb { R } ^ { m \times n _ { 1 } }$ , $A _ { 2 } \in \mathbb { R } ^ { m \times n _ { 2 } } , D \in \mathbb { R } ^ { \bar { s } \times n _ { 2 } }$ , and $b \in \mathbb { R } ^ { m }$ . Suppose the set

$$\mathcal {F} = \left\{\left(x _ {1}, x _ {2}\right) \mid A _ {1} x _ {1} + A _ {2} x _ {2} = b, D x _ {2} = 0 \right\}$$

is non-empty. Fix any $p \in [ 1 , \infty ]$ . Then there exists a finite constant $L ~ > ~ 0$ such that for any pair $( x _ { 1 } , x _ { 2 } )$ satisfying $A _ { 1 } x _ { 1 } + A _ { 2 } x _ { 2 } = b$ , there exists a pair $( \tilde { x } _ { 1 } , \tilde { x } _ { 2 } )$ satisfying $( { \tilde { x } } _ { 1 } , { \tilde { x } } _ { 2 } ) \in \mathcal { F } , \| x _ { 1 } - { \tilde { x } } _ { 1 } \| _ { p } \leq L \| D { \hat { x } } _ { 2 } \| _ { p }$ .

Proof. We can construct $A : = { \left[ \begin{array} { l l } { A _ { 1 } } & { A _ { 2 } } \\ { 0 } & { D } \end{array} \right] } , { \bar { b } } : = { \left[ \begin{array} { l } { } \\ { 0 } \end{array} \right] }$ and then F is the set of feasible solutions for constraint $A x =$ ¯b. From Theorem 5, there exists L such that, for any $x : = \mathrm { c o l } ( x _ { 1 } , x _ { 2 } )$ there exists $\tilde { x } : = ( \tilde { x } _ { 1 } , \tilde { x } _ { 2 } ) \in \mathcal { F }$ with

$$\left\| x _ {1} - \tilde {x} _ {1} \right\| _ {p} \leq \left\| x - \tilde {x} \right\| _ {p}
\leq L \| \left[ \begin{array}{c} A _ {1} x _ {1} + A _ {2} x _ {2} - b \\ D x _ {2} \end{array} \right] \| _ {p} = L \| D x _ {2} \| _ {p},
$$

where the last equality comes from $A _ { 1 } x _ { 1 } + A _ { 2 } x _ { 2 } = b .$ This completes the proof. □
