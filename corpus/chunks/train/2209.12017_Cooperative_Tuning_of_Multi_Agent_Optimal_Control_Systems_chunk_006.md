# II. PROBLEM FORMULATION

Consider a multi-agent system consisting of a number of $N$ agents labeled as $\mathcal { V } = \{ 1 , \cdots , N \}$ . Each agent i can receive information from its neighbor set, which is denoted by $\mathcal { N } _ { i \cdot } \mathbb { G } _ { k } = \{ \mathcal { V } , \mathcal { E } _ { k } \}$ denotes the directed graph such that a directed edge from j to i is in $\mathcal { E } _ { k }$ if and only if $j \in \mathcal { N } _ { i } ( k )$ .

Suppose each agent i is an optimal control system with a tunable parameter $\pmb \theta _ { i } \in \mathbb { R } ^ { r }$ denoted by $\pmb { S } _ { i } ( \pmb \theta _ { i } )$ . The openloop system dynamics of agent i, i.e. $\pmb { S } _ { i } ( \pmb { \theta } _ { i } )$ , are described by

$$\boldsymbol {x} _ {i, t + 1} = \boldsymbol {f} _ {i} (\boldsymbol {x} _ {i, t}, \boldsymbol {u} _ {i, t}, \boldsymbol {\theta} _ {i}),$$

where $t = 0 , \cdots , T$ denotes the discrete time index, $\mathbf { \mathscr { x } } _ { i , t } \in$ $\mathbb { R } ^ { n }$ denotes agent-i’s state at time t, $\pmb { u } _ { i , t } \in \mathbb { R } ^ { m }$ denotes agent-i’s optimal control input 1 at time t, and $\pmb { f } _ { i } : \mathbb { R } ^ { n } \times \mathbb { \Lambda }$ $\mathbb { R } ^ { m } \times \mathbb { R } ^ { r } \mapsto \mathbb { R } ^ { n }$ denotes the nonlinear dynamics of agent-i, which is assumed to be twice-differentiable. The open-loop control $\mathbf { \Delta } \mathbf { u } _ { i , t }$ could be replaced or determined by an optimal control determined in the following way. Associated with agent i is an objective function denoted by

$$J _ {i} = \sum_ {t = 0} ^ {T - 1} c _ {i, t} (\boldsymbol {x} _ {i, t}, \boldsymbol {u} _ {i, t}, \boldsymbol {\theta} _ {i}) + h _ {i} (\boldsymbol {x} _ {i, T}, \boldsymbol {\theta} _ {i}),$$

where $c _ { i , t } : \mathbb { R } ^ { n } \times \mathbb { R } ^ { m } \times \mathbb { R } ^ { r } \mapsto \mathbb { R }$ and $h _ { i } : \mathbb { R } ^ { n } \times \mathbb { R } ^ { r } \mapsto \mathbb { R }$ denoting the running and final cost, respectively. Then under a given initial condition $\mathbf { \Delta x } _ { i , 0 }$ , the optimal control for agent i can be determined by
