# B. Safeguarding Control Law

Let us first present the following lemmas which are used to design the safeguarding controller. Hereafter, $X = [ x ^ { \mathrm { T } } , z ] ^ { \mathrm { T } } \in$ $\mathbb { R } ^ { n + \bar { 1 } }$ stands for the augmented state.

Lemma 2: For any continuously differentiable function $\Upsilon ( z ) : \mathbb { R }  ( a , b )$ with $a > 0$ and $a < b < \infty$ , if the set $\mathcal { C } _ { \Upsilon } = \{ X \in \mathbb { R } ^ { n + 1 } \mid h _ { \Upsilon } ( X ) = \Upsilon ( z ) h ( { \pmb x } ) \geq 0 \}$ is forward invariant, then is also forward invariant.

Proof 1: From Nagumo’s theorem [23] and since $\mathcal { C } _ { \Upsilon }$ is forward invariant, we can deduce that $\dot { h } _ { \Upsilon } ( { \pmb X } ) ~ > ~ 0$ for $\pmb { X } \in \partial \mathcal { C } _ { \Upsilon }$ . In other words, when $h \Upsilon \left( X \right)$ approaches zero, ${ \dot { h } } \mathbf { \gamma } _ { \mathbf { Y } } ( { \mathbf { \cal { X } } } )$ is strictly positive. Additionally, as $\Upsilon ( z ) > 0$ for $z \in \mathbb { R } , h ( \pmb { x } )$ tends to zero if $h \Upsilon ( \pmb { X } )$ tends to zero. Hence, from ${ \dot { h } } \Upsilon ( X ) = \Upsilon ( z ) { \dot { h } } ( { \pmb x } ) + \dot { \Upsilon } ( z ) { \dot { h } } ( { \pmb x } ) > 0$ and $h \Upsilon ( \pmb { X } )  0$ , we can conclude that $\dot { h } ( { \pmb x } ) > 0$ when $h ( { \pmb x } )  0$ . By applying Nagumo’s theorem [23], we can therefore conclude that C is a forward invariant set.

In the rest of this paper, $\Upsilon ( z ) = h _ { 1 } + h _ { 2 } \arctan ( h _ { 3 } z ) :$ : $\mathbb { R }  ( h _ { 1 } - \pi h _ { 2 } / 2 , h _ { 1 } + \pi h _ { 2 } / 2 )$ is considered where $h _ { 1 } , h _ { 2 } .$ , and $h _ { 3 }$ are strictly positive constant. For $h _ { 1 } > ( \pi / 2 ) h _ { 2 }$ , this function has the conditions stated in Lemma 2.

Assume $\alpha ( h _ { \mathrm { Y } } )$ is a class K function. Computing ${ \dot { h } } \mathbf { \mathbf { \dot { r } } } ( \mathbf { X } )$ and according to Lemmas 1 and 2, the safe set is forward invariant if the following inequality is satisfied:

$$\frac {h _ {2} h _ {3}}{1 + h _ {3} ^ {2} z ^ {2}} \dot {z} h + \Upsilon \left(L _ {\boldsymbol {f}} h + L _ {\boldsymbol {B G E}} h \boldsymbol {u} + L _ {\boldsymbol {B}} h \delta\right) + \alpha \geq 0. \tag {19}$$

The inequality (19) is robustly met for all $\pmb { x } \in \mathcal { C }$ and $t \geq 0$ despite uncertainties in $G ( \pmb { x } )$ and $\delta ( t , { \boldsymbol { x } } )$ if
