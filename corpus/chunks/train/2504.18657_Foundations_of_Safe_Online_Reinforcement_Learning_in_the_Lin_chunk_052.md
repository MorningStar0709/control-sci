$$
d _ {j + 1} \leq \left\{ \begin{array}{l l} O _ {T} (\epsilon) \left(1 + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)\right) d _ {j} & \text {   If   } \mathcal {W} _ {j} \\ \min \left(2 \left(1 + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)\right) d _ {j}, \left(1 + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)\right) (d _ {j} + O _ {T} (\epsilon) (| x _ {j} | + \| D \| _ {\infty}))\right) & \text {   If   } \mathcal {Z} _ {j} \\ \left(1 + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)\right) d _ {j} & \text {   Otherwise   } \end{array} \right. \tag {21}
$$

Similar to in the proof of Case 1 above, by Lemma 42 and the assumption that $\| D \| _ { \infty } \leq$ $\log ^ { 2 } ( T )$ , we have that conditional on event $E _ { 1 } ^ { t }$ , Equation (21) implies the following two relationships. The first relationship comes from using the first term in the min of Equation (21) and recursing.

$$d _ {j + 1} \leq \left(1 + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)\right) ^ {j + 1} \cdot 2 ^ {\kappa (j)} \cdot O _ {T} (\epsilon) ^ {\lambda (j)} \cdot d. \tag {22}$$

The second relationship comes from using the second term in the min of Equation (21) and bounding $\begin{array} { r } { \left( 1 + O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) \right) \left( | x _ { j } | + \| D \| _ { \infty } \right) = O _ { T } ( \log ^ { 2 } ( T ) ) } \end{array}$ under event $E _ { 1 } ^ { t }$ . This gives the recursive relationship of

$$d _ {j + 1} \leq \left(1 + O _ {T} \left(\frac {1}{\log^ {1 0} (T)}\right)\right) (O _ {T} (\epsilon)) ^ {1 _ {\mathcal {W} _ {j}}} \cdot d _ {j} + O _ {T} (\epsilon \log^ {2} (T)) 1 _ {\mathcal {Z} _ {j}}. \tag {23}$$

In other words, at every step there is a multiplicative factor of $\begin{array} { r } { \left( 1 + O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) \right) } \end{array}$ . When $\mathcal { W } _ { j }$ holds, there is an additional multiplicative factor of $O _ { T } ( \epsilon )$ . When ${ \mathcal { Z } } _ { j }$ holds, there is an additive factor of $O _ { T } ( \epsilon \log ^ { 2 } ( T ) )$ . Unwrapping Equation (23) gives that, at time $j + 1$ , any additive factor contributed at time $i \leq j$ will be scaled by $\begin{array} { r } { O _ { T } ( \epsilon ) ^ { \lambda ( j ) - \lambda ( i ) } \left( 1 + O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) \right) ^ { j - i } } \end{array}$ . This gives that
