$$
\begin{array}{l} J _ {k + 1} (x) = \min _ {x, u) \in \mathcal {P}} \left[ s ^ {\top} x + r ^ {\top} u + J _ {k} (A x + B u) \right] \\ = \min _ {(x, u) \in \mathcal {P}} \left[ s ^ {\top} x + r ^ {\top} u + \lambda_ {k} ^ {\top} (A x + B u) \right] \\ = s ^ {\top} x + \lambda_ {k} ^ {\top} A x + \min _ {(x, u) \in \mathcal {P}} \left[ \mu_ {k} ^ {\top} u \right] \\ = \left[ s + A ^ {\top} \lambda_ {k} + \phi (\mu_ {k}) \right] ^ {\top} x = \lambda_ {k + 1} ^ {\top} x \\ \end{array}
$$

and $\lambda _ { k + 1 } ^ { \top } x \ge 0 ,$ so $( \lambda _ { k + 1 } , 0 ) \in \mathcal { P } ^ { * }$ and the induction assumption holds also for $k + 1 .$ The assumption is trivial for $k = 0 .$ , so it follows by induction for $k = 1 , 2 , 3 , \dots$ .

$( i i ) \longleftrightarrow ( i )$ : If (i) holds, then the Bellman recursion from $J _ { 0 } = 0$ gives $0 \ : =$ $J _ { 0 } \leq J _ { 1 } \leq J _ { 2 } \leq . .$ . with upper limit $\lambda _ { * } ^ { \top }$ x where λ∗ satisfies the condition in (ii). Conversely, if (ii), then $\lambda _ { 0 } = 0$ gives $\left( \lambda _ { * } - \lambda _ { k } , 0 \right) \in \mathcal { P } ^ { * }$ for all k. This is trivial for $k = 0$ and follows by induction for all other k since

$$
\begin{array}{l} \lambda_ {k + 1} ^ {\top} x = \min _ {(x, u) \in \mathcal {P}} \left[ s ^ {\top} x + r ^ {\top} u + \lambda_ {k} ^ {\top} (A x + B u) \right] \\ \leq \min _ {(x, u) \in \mathcal {P}} \left[ s ^ {\top} x + r ^ {\top} u + \lambda_ {*} ^ {\top} (A x + B u) \right] = \lambda_ {*} ^ {\top} x. \\ \end{array}
$$

Hence the Jk-sequence has an upper limit and (i) holds too. This proves the implication from (ii) to (i), as well as the formula $\lambda _ { * } ^ { \top } x _ { 0 }$ for the minimal value in (i). Given x, the optimal control is u is obtained by the minimization

$$\arg \min _ {(x, u) \in \mathcal {P}} \left[ s ^ {\top} x + r ^ {\top} u + \lambda_ {*} ^ {\top} (A x + B u) \right]$$

or equivalently arg min $\cdot ( x , u ) \in \mathcal { P } \left[ ( r + B ^ { \top } \lambda _ { * } ) ^ { \top } u \right]$ .

$( i i ) \implies ( i i i ) ;$ : Consider $\lambda _ { * }$ satisfying (ii) and let λ be a feasible point for the optimization problem in (iii). In particular,

$$\lambda^ {\top} x \leq s ^ {\top} x + r ^ {\top} u + \lambda^ {\top} (A x + B u) \quad \text { for } (x, u) \in \mathcal {P}. \tag {4}$$

The assumption that $( s , r )$ is an interior point of ${ \mathcal { P } } ^ { * }$ gives existence of $\gamma , \delta > 0$ such that
