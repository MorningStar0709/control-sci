Hence, for all $\widetilde { { \boldsymbol x } } \in \mathbb { Z } , \widetilde { { \boldsymbol x } } ^ { + } = A _ { L } \widetilde { { \boldsymbol x } } + \widetilde { { \boldsymbol w } } \in \mathbb { Z }$ for all $\tilde { w } \in \tilde { \mathbb { W } } ;$ the term robust in the description of Σ refers to this property. In fact, Σ is the minimal robust, positive invariant set for $\widetilde { \boldsymbol { x } } ^ { + } = \boldsymbol { A _ { L } } \widetilde { \boldsymbol { x } } + \widetilde { \boldsymbol { w } } , \widetilde { \boldsymbol { w } } \in \widetilde { \mathbb { W } }$ , i.e., a set that is a subset of all robust positive invariant sets. There exist techniques (Rakovi´c, Kerrigan, Kouramas, and Mayne, 2005) for obtaining, for every $\epsilon > 0$ , a polytopic, nonminimal, robust, positive invariant set $\Sigma ^ { 0 }$ that satisfies $d _ { H } ( \mathbb { Z } , \mathbb { Z } ^ { 0 } ) \leq \epsilon$ where $d _ { H } ( \cdot , \cdot )$ is the Hausdorff metric. An immediate consequence of (5.13) is:

Proposition 5.2 (Proximity of state and state estimate). If the initial system and observer states, x(0) and x( ˆ 0) respectively, satisfy $\{ x ( 0 ) \} \in$ $\{ \hat { x } ( 0 ) \} \oplus \Sigma$ , then $x ( i ) \in \{ \hat { x } ( i ) \}$ } ⊕ Σ for all $i \in \mathbb { I } _ { \geq 0 }$ , and all admissible disturbance sequences w and ν.

The assumption that $\tilde { { \boldsymbol { x } } } ( i ) \in \mathbb { \Sigma }$ for all i is a steady-state assumption; if $\tilde { { \boldsymbol { x } } } ( 0 ) \in \mathbb { \Sigma }$ , then $\tilde { { \boldsymbol { x } } } ( i ) \in \mathbb { \Sigma }$ for all i. If, on the other hand, $\tilde { { \boldsymbol { x } } } ( 0 ) \in \Sigma ( 0 )$ ewhere $\mathbb { \Sigma } ( 0 ) \supseteq \mathbb { \Sigma }$ e, then it is possible to show that $\tilde { { \boldsymbol { x } } } ( i ) \in \mathbb { \Sigma } ( i )$ for all $i \in \mathbb { I } _ { \geq 0 }$ where $\mathbb { \Sigma } ( i )  \mathbb { \Sigma }$ in the Hausdorff metric as $i  \infty ;$ the sequence $\{ \mathbb {  } ( i ) \}$ satisfies $\Sigma ( 0 ) \supseteq \Sigma ( 1 ) \supseteq \Sigma ( 2 ) \supseteq \cdots \supseteq \mathbb { Z }$ . Hence, it is reasonable to assume that if the estimator has been running for a “long” time, it is in steady state.

Hence we have obtained a state estimator, with “state” (x, ˆ Σ) satisfying

$$
\begin{array}{l} \hat {x} ^ {+} = A \hat {x} + B u + L (y - \hat {y}) \tag {5.14} \\ \mathbb {Z} ^ {+} = \mathbb {Z} \\ \end{array}
$$

and ${ \boldsymbol { x } } ( i ) \in \hat { { \boldsymbol { x } } } ( i ) \oplus \mathbb { \delta }$ for all $i \in \mathbb { I } _ { \geq 0 }$ , thus meeting the requirements specified in Section 5.2. Knowing this, our remaining task is to control $\hat { x } ( i )$ so that the resultant closed-loop system is stable and satisfies all constraints.
