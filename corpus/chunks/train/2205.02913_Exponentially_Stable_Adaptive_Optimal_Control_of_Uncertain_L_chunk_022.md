Assumption 1. The parameter ?? is chosen in such a way that $\overline { { \Psi } } \left( t \right) \in \mathrm { F E } \Rightarrow \overline { { \varphi } } \left( t \right) \in \mathrm { F E } \mathrm { ~ h o l d s ~ }$ .

The linear minimum-phase filter $\mathfrak { H } \left[ . \right] : = 1 / \left( p + k _ { 0 } \right)$ [.] is introduced, and the DREM procedure 4 is applied to (23):

$$z (t) = \varphi (t) \overline {{\theta}} _ {A B},z (t) := \frac {k _ {1} \operatorname{adj} \left\{\bar {\varphi} _ {f} (t) \right\} \bar {z} _ {f} (t)}{1 + k _ {1} \det \left\{\bar {\varphi} _ {f} (t) \right\}}, \varphi (t) := \frac {k _ {1} \det \left\{\bar {\varphi} _ {f} (t) \right\}}{1 + k _ {1} \det \left\{\bar {\varphi} _ {f} (t) \right\}}, \tag {24}\dot {\overline {{z}}} _ {f} (t) = - k _ {0} \overline {{z}} _ {f} (t) + \overline {{\varphi}} (t) \overline {{z}} ^ {\mathrm{T}} (t), \overline {{z}} _ {f} (t _ {r} ^ {+}) = 0 _ {(n + m + 1) \times n},\dot {\overline {{\varphi}}} _ {f} (t) = - k _ {0} \overline {{\varphi}} _ {f} (t) + \overline {{\varphi}} (t) \overline {{\varphi}} ^ {\mathrm{T}} (t), \overline {{\varphi}} _ {f} (t _ {r} ^ {+}) = 0 _ {(n + m + 1) \times (n + m + 1)},$$

where $k _ { 0 } > 0 , \ k _ { 1 } > 0 , \ \varphi \left( t \right) \in \left[ 0 ; \ 1 \right) \subset \mathbb { R } , \ z \left( t \right) \in \mathbb { R } ^ { \left( n + m + 1 \right) \times n }$ .

It is easy to obtain the regression equations (17) from (24):

$$\boldsymbol {z} _ {A} (t) = \boldsymbol {z} ^ {\mathrm{T}} (t) \boldsymbol {L} = \varphi (t) \boldsymbol {A},\boldsymbol {z} _ {B} (t) = \boldsymbol {z} ^ {\mathrm{T}} (t) \boldsymbol {e} _ {n + m + 1} = \varphi (t) \boldsymbol {B},
\begin{array}{l} L = \left[ I _ {n \times n} 0 _ {n \times (m + 1)} \right] ^ {\mathrm{T}} \in \mathbb {R} ^ {(n + m + 1) \times n}, \\ _ {n + 1} = \left[ 0 _ {m \times n} 1 _ {m \times m} 0 _ {m \times 1} \right] ^ {\mathrm{T}} \in \mathbb {R} ^ {(n + m + 1) \times m}, \end{array} \tag {25}
$$

where $z _ { A } \left( t \right) \in \mathbb { R } ^ { n \times n } , ~ z _ { B } \left( t \right) \in \mathbb { R } ^ { n \times m }$ are the measurable functions.

Thus, the equation (17), which, following Proposition 1, is necessary to form the regression equation (16), (19), is obtained by filtration of the plant equations (4) with the help of (20), (21) and application of the DREM procedure (24) to the resulting equation (23).

Remark 5. According to Lemma $6 . 8 ^ { 4 0 }$ , as the filter (21) is stable, if $\Psi ( t ) \in \mathrm { F E }$ , then $\overline { { \boldsymbol { \Psi } } } \left( t \right) \in \mathrm { F E }$ . So, considering Assumption 1, it follows that $\overline { { \varphi } } \left( t \right) \in \mathrm { F E . } \mathrm { I n } ^ { 4 1 }$ it is proved that the implication $\overline { { \varphi } } \left( t \right) \in \mathrm { F E } \Rightarrow \varphi \left( t \right) \in \mathrm { F E }$ holds for the DREM procedure (24).

The next step is to obtain the regression equations (18) on the basis of (25).
