# IV. STABILITY OF MODEL PREDICTIVE CONTROL (MPC) UNDER IDENTIFICATION ERROR

In this section, we seek to prove uniform ultimate boundedness or practical asymptotic stability of the LTI system (1) under the QSID-MPC framework of Algorithm 1. To this end, we first state an assumption on the identified system $[ \hat { A } , \hat { B } ]$ .

Assumption 2: [Aˆ Bˆ ] form a controllable pair.

,Our goal is to show that, under Algorithm 1, xt tracks the reference ${ \bar { \mathbf { \chi } } } _ { x } ^ { \mathrm { r e f } }$ with a uniformly ultimately bounded error even in the presence of model error $[ \epsilon ^ { 2 } \Delta _ { A } , \epsilon ^ { 2 } \Delta _ { B } ]$ . The MPC problem (2) ϵ , ϵfocuses on tracking an arbitrary time-varying trajectory $x ^ { \mathrm { r e f } }$ However, the ultimate boundedness of the tracking error can be established only if $x ^ { \mathrm { r e f } }$ is constant or asymptotically converging to a constant. Without loss of generality, we assume $x ^ { \mathrm { r e f } } \equiv \bar { 0 }$ in this section. With the origin as a reference, we now define the uniform ultimate boundedness of the MPC Algorithm 1.

Definition 1: Let the MPC optimization problem (2) be feasible for all $x _ { t } \in { \mathcal { M } }$ . Then the solution of the system (1) under MPC policy with $x ^ { \mathrm { r e f } } \equiv 0$ from algorithm 1 is called uniformly ultimately bounded with an ultimate bound if there exists a class KL function $\beta ,$ δ and for every initial state x0 satisfying $\lvert | \boldsymbol { x } _ { 0 } \rvert | \leq r$ with $r > \delta ,$ , there exists $T \geq 0$ such that

$$\left\| x _ {t} \right\| \leq \beta \left(\left\| x _ {0} \right\|, t\right), \forall t \in \{0, \dots , T \} \text { and } \left\| x _ {t} \right\| \leq \delta , \forall t > T.$$

β , , , . . . , δ, >This is also called practical asymptotic stability [16].

Assumption 3: The cost weight matrices are positive definite, $\mathrm { i . e , ~ } Q , R \succ 0$ . More specifically $x ^ { \top } Q x + u ^ { \top } R u \geq \alpha _ { 1 } \| x \| ^ { 2 } +$ $\alpha _ { 2 } \| u \| ^ { 2 }$ .

αNote that, this is stronger than $Q \succeq 0$ prevalent in standard LQ problems and required for ultimate boundedness of the error.

Assumption 4: Feasibility–There exists a feasible control policy (·) for which $\chi _ { f }$ is invariant, i.e., $\mu ( \hat { x } ) \in \mathcal { U } \ \forall \ \hat { x } \in \mathcal { X } _ { f }$ and $\hat { A } \hat { x } + \hat { B } \mu ( \hat { x } ) \in X _ { f }$ .
