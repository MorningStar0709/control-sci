Problem 7.7 Suppose $P$ and $Q$ are the controllability and observability Gramians of $G ( s ) = C ( s I - A ) ^ { - 1 } B \in \mathcal { R } \mathcal { H } _ { \infty }$ . Let $G _ { d } ( z ) = G ( s ) | _ { s = \frac { z + 1 } { \cdot } } = C _ { d } ( z I - A _ { d } ) ^ { - 1 } B _ { d } + D _ { d }$ . Compute the controllability and observability Gramians $P _ { d }$ and $Q _ { d }$ and compare $P Q$ and $P _ { d } Q _ { d }$ .

Problem 7.8 Note that a delay can be approximated as

$$e ^ {- \tau s} \approx \left(\frac {1 - \frac {\tau}{2 n} s}{1 + \frac {\tau}{2 n} s}\right) ^ {n}$$

for a sufficiently large n. Let a process model $\frac { e ^ { - s } } { 1 + T s }$ e−s be approximated by

$$G (s) = \left(\frac {1 - 0 . 0 5 s}{1 + 0 . 0 5 s}\right) ^ {1 0} \frac {1}{1 + s T}$$

For each $T = 0 , 0 . 0 1 , 0 . 1 , 1 , 1 0$ , find a reduced-order model, if possible, using balanced truncation such that the approximation error is no greater than 0.1.
