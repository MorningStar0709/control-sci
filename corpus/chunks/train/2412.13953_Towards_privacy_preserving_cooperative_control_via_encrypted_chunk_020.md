# Algorithm 3. Encrypted ADMM iteration τ at agent i.

1. Compute $[ [ z _ { i } ^ { \tau + 1 } ] ] _ { 0 }$ according to (16) for given $\mathbb { I } ( \zeta ^ { \tau } ) _ { K _ { i } } ]$ 0 , $\mathbb { [ \lambda _ { i } ^ { \tau } ] ] _ { 0 } } .$ , and $[ { p } _ { i } ] _ { 0 }$ .   
2. For each $k \in { \mathcal { K } } _ { i } \setminus { \mathcal { A } } _ { i }$ and each $j \in \mathcal { N } _ { i }$ , if k $\in \mathcal { A } _ { j }$ , send $\mathbb { [ ( z _ { i } ^ { \tau + 1 } ) } _ { k _ { i } }$ ⟧0 to agent j secured by AES.   
3. For each $k \in \mathcal A _ { i }$ and each $j \in \mathcal { N } _ { i }$ , if $k \in \mathcal { K } _ { j }$ , receive $\mathbb { [ ( z _ { j } ^ { \tau + 1 } ) _ { k _ { j } } ] _ { 0 } }$ from agent j.   
4. Compute $\mathbb { I } ( \zeta ^ { \tau + 1 } ) _ { A _ { i } }$ ⟧0 according to (12).   
5. For each $j \in \mathcal { N } _ { i }$ , send $\mathbb { [ ( \zeta ^ { \tau + 1 } ) _ { A _ { i } \cap \mathcal { K } _ { j } } }$ ⟧0 to agent j secured by AES.   
6. For each $j \in \mathcal { N } _ { i }$ , receive $\mathbb { [ ( \zeta ^ { \tau + 1 } ) _ { A _ { j } \cap \mathcal { K } _ { i } } }$ ⟧0 from agent $j .$ .   
7. Compute $[ \lambda _ { i } ^ { \tau + 1 } ] _ { 0 }$ according to (13).

An encrypted version of the initialization routine in $\mathrm { { A l g o - } }$ - rithm 2 can be obtained analogously:
