# Box 5.1: System identification using HDMDc workflow

Inputs. Training state and input time series $\mathbf { } \mathbf { } \mathbf { } \mathbf { x } ( t ) \in \mathbb { R } ^ { N } , \mathbf { u } ( t ) \in \mathbb { R } ^ { Q } ,$ ; training window length $l _ { t r } ;$ delay-embedding length $l _ { d } ;$ Testing input time series ${ \bf u } ( t ) \in \mathbb { R } ^ { Q }$ for the forecast horizon $l _ { t e } .$ .
