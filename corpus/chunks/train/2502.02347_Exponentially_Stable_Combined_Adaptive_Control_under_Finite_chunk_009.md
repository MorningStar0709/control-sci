the Lyapunov function satisfies, min $\{ \lambda _ { \operatorname* { m i n } } \left( \mathbf { P } \right) , | k _ { p } | \} \| x \| ^ { 2 }$ $\leq V \leq \operatorname* { m a x } \left\{ \lambda _ { \operatorname* { m a x } } \left( \mathbf { P } \right) , \left| k _ { p } \right| \right\} \left\| x \right\| ^ { 2 }$ , hence V is positive definite and decrescent (Slotine, 1991), also $\dot { V } \leq 0 .$ , that is, V˙ is negative semi-definite, therefore using [Theorem 4.1, (Slotine, 1991)], the origin of the closed-loop system is uniformly stable. Further using Barbalat’s Lemma, the origin of the tracking error is asymptotically stable (Slotine, 1991), this completes the proof. 

In combined adaptive control, the adaptation laws are driven by the tracking error and the system parameters’ ideal values. Since the ideal values are unknown, we propose an algorithm that extracts the ideal values under the FE condition.
