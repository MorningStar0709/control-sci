# 3.1 Modeling Cyber Physical Systems

Let a CPS be defined as the tuple {E,C,M}. E denotes the physical shared environment, which is a set of k properties $p _ { k }$ such that $E = \left\{ p _ { 0 } , p _ { 1 } , . . . p _ { k - 2 } , p _ { k - 1 } \right\}$ . C denotes the set of n cyber agents operating on the environment, such that C = $\left\{ c _ { 0 } , c _ { 1 } , . . . c _ { n - 2 } , c _ { n - 1 } \right\}$ . The internal behavior of each cyber agent is not relevant for this work; we point interested readers to the body of work on modeling embedded systems18. M denotes the message channels exchanging between the environment E and the cyber agents C, such that ${ \cal M } = \{ \{ m _ { c _ { 0 } } ^ { i } , m _ { c _ { 0 } } ^ { o } \} , \{ m _ { c _ { 1 } } ^ { i } , m _ { c _ { 1 } } ^ { o } \} , . . . \{ m _ { c _ { n - 2 } } ^ { i } , m _ { c _ { n - 2 } } ^ { o } \} , \{ m _ { c _ { n - 1 } } ^ { i } , m _ { c _ { n - 1 } } ^ { o } \} \}$ , where $m _ { c _ { x } } ^ { i }$ denotes an input message channel for cyber agent x, i.e., sensing the values of the environment, and $m _ { c _ { x } } ^ { o }$ denotes an output message channel from cyber agent x, i.e., acting on the environment. Messages conceptually represent all possible interactions between each agent and features of the environment, which we notate using the following notation: $m _ { c _ { x } } ^ { i } \gets p _ { \gimel }$ y denotes sensing by the cyber agent of environment property $p _ { y } ,$ several properties may be sensed at once; e.g., $m _ { c _ { x } } ^ { i }  p _ { y } , p _ { z } . p _ { y }  m _ { c _ { x } } ^ { o } = z$ denotes updating property $p _ { y }$ by agent $c _ { x } ,$ with a value of z.

Let Mo denote the set of all output message channels (acting on the environment) and $M ^ { i }$ denote the set of all input message channels (sensing the environment), such that $M ^ { o } \subset M , M ^ { i } \subset M , M ^ { o } \cap M ^ { i } = \emptyset , M ^ { o } \cup M ^ { i } = M$ . To keep notation simpler, instead of adding additional message channels for inter cyber agent communication, we consider the communication link to be a hidden part of the environment $E ,$ and we notate direct messages with content s from cyber agent x to y through $m _ { c _ { v } } ^ { i } \gets ( s ) \gets m _ { c _ { x } } ^ { o }$ .
