# Algorithm 1 Local linearization of Problem 2

At current time $k _ { 0 } ,$ , given $X _ { k _ { 0 } } ~ = ~ \left( x _ { k _ { 0 } | k _ { 0 } } , \Sigma _ { k _ { 0 } | k _ { 0 } } \right)$ from the running eKF;

$( x _ { k _ { 0 } } ^ { \breve { p } } , \Sigma _ { k _ { 0 } | k _ { 0 } } ^ { p } )  ( x _ { k _ { 0 } | k _ { 0 } } , \Sigma _ { k _ { 0 } | k _ { 0 } } )$

Linearize (15) about

$$
\xi_ {k _ {0}} = \left[ \begin{array}{c} x _ {k _ {0}} ^ {p} \\ \text { up\_vec } (\Sigma_ {k _ {0} | k _ {0}} ^ {p}) \end{array} \right], \quad I _ {k _ {0}} = 0,
$$

(up vec is the vectorized upper-triangle; to avoid repitition due to symmetry) to find A, B in

$$\xi_ {k + 1} = \mathcal {A} \xi_ {k} + \mathcal {B} I _ {k}; \tag {18}$$

Solve:

$$\min _ {U _ {k _ {0}} ^ {k _ {0} + N}, \{x _ {k} \} _ {k = k _ {0}} ^ {k _ {0} + N}} \hat {J} _ {k _ {0}}$$

subject to (17), (18) and

$$\Sigma_ {k | k} ^ {p} \succeq 0, \quad k = k _ {0}, \dots , k _ {0} + N;$$

Apply the optimal value of $I _ { k _ { 0 } } .$
