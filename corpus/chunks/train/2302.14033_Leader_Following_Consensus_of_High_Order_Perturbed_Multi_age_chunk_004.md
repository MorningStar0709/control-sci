# II. PRELIMINARIES

The set of natural, real, and strictly positive real numbers are denoted by N, R and $\mathbb { R } { > } 0$ , respectively. For $d \in \mathbb { N }$ and a column vector of $x \in \mathbb { R } ^ { d }$ , let $x ^ { \prime }$ be its transpose and kxk2 the corresponding 2-norm. Then, define $\mathcal { G } = ( \nu , \mathcal { E } )$ as a directed graph (digraph), where $\pmb { \nu } = \{ 1 , \ldots , N \}$ is a set of nodes (i.e. agents) and ${ \pmb { \mathcal { E } } } \subseteq \{ { \pmb { \mathcal { V } } } \times { \pmb { \mathcal { V } } } \}$ is the set of edges. $\pmb { \mathcal { A } } = [ \alpha _ { i j } ] \in \mathbb { R } ^ { N \times N }$ is the adjacency matrix of G, with non-zero weight if i communicates with $j \left( i ; j \right) \subseteq \mathcal { E } , \alpha _ { i j } = 0$ otherwise. Let $\pmb { \mathscr { N } } _ { i } = \{ j \in \pmb { \mathscr { V } } : ( i , j ) \in \pmb { \mathscr { E } } \}$ be the set of neighbors around agent i, showing those of agents that share an edge with agent i. Next, let agent $ { { } ^ { 6 6 } } 0 ^ { 9 }$ be an additional virtual object in the augmented graph $\mathcal { G } _ { N + 1 }$ , while the remainder $ { { } ^ { 6 6 } } 0 ^ { 9 }$ is considered as the virtual leader for the proposed protocol assuming to be globally reachable in $\mathcal { G } _ { N + 1 }$ .

In the remainder of the paper the following useful properties are exploited:

Theorem 1. Suppose that $f : \mathbb { R } \times C \in ( [ - h \quad 0 ] , \mathbb { R } ^ { n } ) \longrightarrow \mathbb { R } ^ { n }$ in ${ \dot { x } } ( t ) = f ( t , x _ { t } )$ maps R× (bounded sets $o f C )$ into bounded sets of $\mathbb { R } ^ { n }$ and also that $u , \nu , w : \mathbb { R } _ { > 0 } \longrightarrow \mathbb { R } _ { > 0 }$ are continuous non-decreasing functions, $u ( s )$ and $\nu ( s )$ are positive $f o r s > 0 ,$ , $u ( 0 ) = \nu ( 0 ) = 0$ and v is always increasing. If there exists a continuously differentiable function $V : \mathbb { R } \times \mathbb { R } ^ { n } \longrightarrow$ R such that

$$u (\| x \|) \leq V (t, x (t)) \leq v (\| x \|), \quad t \in \mathbb {R}, \quad x \in \mathbb {R} ^ {n}, \tag {1}$$

and the derivative of $V ( t , x ( t ) )$ along the solution, $x ( t )$ , of ${ \dot { x } } ( t ) = f ( t , x _ { t } )$ satisfies

$$V (t, x (t)) \leq - w (\| x \|) \tag {2}$$

whenever

$$V (t + \theta , x (t + \theta)) \leq V (t, x (t)) \tag {3}$$

for $\theta \in [ - h \quad 0 ]$ where $h > 0$ is the delay, then the trivial solution of ${ \dot { x } } ( t ) = f ( t , x _ { t } )$ is uniformly stable. 
