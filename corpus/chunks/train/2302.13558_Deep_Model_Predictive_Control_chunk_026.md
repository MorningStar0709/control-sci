# B Stability

We recall the following definition:

Definition 1 ([54], page 117). The vector sequence $\left( s _ { t } \right) _ { t \in \mathbb { N } _ { 0 } }$ is called $\mu$ small in mean square sense if it satisfies 0 $\begin{array} { r } { \sum _ { t = k } ^ { k + N - 1 } \| s _ { t } \| ^ { 2 } \leqslant N c _ { 0 } \mu + c _ { 0 } ^ { \prime } } \end{array}$ for all $k \in \mathbb { Z } _ { + }$ , a given constant $\mu \geqslant 0$ and some $N \in \mathbb { Z } _ { + }$ , where $c _ { 0 } , c _ { 0 } ^ { \prime } \geqslant \dot { 0 }$ .

Some straightforward arguments as in [54, §4.11.3] give us the following result:

Lemma 1. Consider the dynamical system (1), weight update law (20) and the projection method (21). Let the Assumption 2 hold and define $\begin{array} { r } { V _ { a } ( K _ { t } ) : = \frac { 1 } { \theta } \operatorname { t r } ( \tilde { K } _ { t } ^ { \top } \tilde { K } _ { t } ) } \end{array}$ . Then for all t,

(i) $\begin{array} { r } { V _ { a } ( K _ { t } ) \leqslant \frac { 4 } { \theta } \bar { W } , } \end{array}$ ,

(ii) $\begin{array} { r } { V _ { a } ( K _ { t + 1 } ) - V _ { a } ( K _ { t } ) \leqslant - \frac { 1 - \theta } { \sigma ^ { 2 } } \| \tilde { u } _ { t } \| ^ { 2 } + \| \varepsilon ( x _ { t } ) \| ^ { 2 } , } \end{array}$

(iii) $\tilde { u } _ { t } \ i s \ \bar { \varepsilon } ^ { 2 }$ small in mean square sense with $\begin{array} { r } { c _ { 0 } = \frac { \sigma ^ { 2 } } { 1 - \theta } } \end{array}$ and $c _ { 0 } ^ { \prime } = \frac { 4 c _ { 0 } } { \theta } \hat { W }$ as per the Definition 1.

We provide a proof of Lemma 1 in the appendix. Let $X _ { c } ( x _ { t } ^ { r } )$ be the level set around $x _ { t } ^ { r }$ of radius c generated by $V _ { m } ( x _ { t } )$ and $X _ { c }$ be their union. In particular,

$$X _ {c} \left(x _ {t} ^ {r}\right) := \left\{x _ {t} \in \mathbb {R} ^ {d} \mid V _ {m} \left(x _ {t}\right) \leqslant c; c > 0 \right\}, \tag {23}X _ {c} := \cup_ {t = 0} ^ {N} X _ {c} (x _ {t} ^ {r}).$$

Properties of the value function are summarized in the following Lemma. These results are standard in the literature [55]. We provide their proofs in the appendix for completeness.

Lemma 2. (i) If α > c then $x _ { t + N | t } \in \mathcal X _ { f }$ for every $x _ { t } \in X _ { c } ( x _ { t } ^ { r } )$ .

(ii) [34, Lemma 3] There exist $c _ { 2 } > c _ { 1 } > 0$ such that

$$c _ {1} \left\| x _ {t} - x _ {t} ^ {r} \right\| ^ {2} \leqslant c _ {\mathrm{s}} (x _ {t}, u _ {t | t} ^ {*}) \leqslant V _ {m} (x _ {t}) \leqslant c _ {2} \left\| x _ {t} - x _ {t} ^ {r} \right\| ^ {2}.$$
