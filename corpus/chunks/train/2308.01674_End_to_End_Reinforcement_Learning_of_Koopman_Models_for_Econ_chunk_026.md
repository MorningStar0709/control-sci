# 3.3 NMPC

Here, the controller task is to stabilize the product concentration c and temperature T at constant target values (see Table 2). Every eight hours, the production rate ρ is set to a random value within its bounds for the next eight hours. The controller can respond by varying the coolant flow rate F within its bounds to stabilize c and T . Thus, the production rate becomes an external input $\rho _ { \mathrm { { e x t } } }$ and the coolant flow rate F is the control input.

During RL, we define the reward $r _ { t }$ for each control action as proportional to the negative squared relative deviation between $c , T .$ , and their respective

steady-state values:

$$r _ {t} = - \left(\left(c _ {t} - c _ {\text {target}}\right) / \left(c _ {\mathrm{ub}} - c _ {\mathrm{lb}}\right)\right) ^ {2} \tag {9}- \left(\left(T _ {t} - T _ {\text { target }}\right) / \left(T _ {\mathrm{ub}} - T _ {\mathrm{lb}}\right)\right) ^ {2}$$

Relative deviations are achieved by normalizing each variable using the size of its feasible range (see Table 2). Aptly, using the feasible range of each variable, we also normalize the inputs and outputs of the Koopman models to values between zero and one. At each control step, the MPC controller solves an OCP in Koopman space, minimizing the squared distance from the target state $\begin{array} { r l } {  { x _ { \mathrm { t a r g e t } } \colon } } & { { } } \end{array}$

$$\min _ {(F _ {t}) _ {t \in \mathrm{T}}} \sum_ {t \in \mathrm{T} _ {+ 1}} | | C _ {\boldsymbol {\theta}} \boldsymbol {z} _ {t} - \boldsymbol {x} _ {\mathrm{ss}} | |, \tag {10a}\text { s.t. } \boldsymbol {z} _ {0} = \boldsymbol {\psi} _ {\boldsymbol {\theta}} (\boldsymbol {x} _ {0}), \tag {10b}\boldsymbol {z} _ {t + 1} = A _ {\boldsymbol {\theta}} \boldsymbol {z} _ {t} + B _ {\boldsymbol {\theta}} \binom{\rho_ {\mathrm{ext}}}{F _ {t}} \quad \forall t \in \mathrm{T}, \tag {10c}\underline {{F}} _ {t} \leq F _ {t} \leq \bar {F} _ {t} \quad \forall t \in \mathrm{T}. \tag {10d}$$

We use an MPC horizon of $t _ { f } = 1 2$ quarter hours, i.e., three hours, and episodes with a duration of 288 time steps, i.e., three days. As the production rate $\rho _ { \mathrm { { e x t } } }$ is adjusted every eight hours, each episode includes eight step changes.
