it can be verified that inequality (30) is satisfied for all $t \geq t _ { s } .$ , which in turn implies that

$$\dot {V} (\bar {x}) \leq - c (\sigma) \| R (\sigma) \bar {x} \| ^ {2}, \forall t \geq t _ {s} \tag {68}$$

Taking into account that $c ( \sigma ) \geq 1 / { \sqrt { \| \sigma \| } }$ for all $\sigma \neq 0$ and $\| R ( \sigma ) \bar { x } \| \geq \sqrt { \| \sigma \| }$ , there exist $\theta _ { N } > 0$ and $\varepsilon _ { N } > 0$ such that

$$\dot {V} (\bar {x}) \leq - \frac {\varepsilon_ {N}}{\sqrt {\theta_ {N}}} V (\bar {x}) ^ {1 / 2}, \forall t \geq t _ {s} \tag {69}$$

allowing, from usual arguments, see [4], that the equilibrium point $\bar { x } = 0$ is globally reached after some finite elapsed time. The next section is entirely devoted to the application of the theoretical results presented so far in a practical motivated control problem.
