# 2.1 Linear Dilation

Let us recall that a family of operators $\mathbf { d } ( s ) : \mathbb { R } ^ { n } \mapsto \mathbb { R } ^ { n }$ with $s \in \mathbb R$ is a oneparameter group i $\begin{array} { r } { \boldsymbol { \mathrm { \textbf { f } } } \boldsymbol { \mathbf { d } } ( 0 ) x = x , \boldsymbol { \mathbf { d } } ( s ) \boldsymbol { \mathbf { d } } ( t ) x = \boldsymbol { \mathbf { d } } ( s + t ) x , \forall x \in \mathbb { R } ^ { n } , \forall s , t \in \mathbb { R } . } \end{array}$ A group d is a) continuous if the mapping $s \mapsto \mathbf { d } ( s ) x$ is continuous, $\forall x \in \mathbb { R } ^ { n } ; \mathrm { b } )$ linear if $\mathbf { d } ( s )$ is a linear mapping $( { \mathrm { i . e . , ~ } } \mathbf { d } ( s ) \in \mathbb { R } ^ { n \times n } )$ , $\forall s \in \mathbb { R } ;$ c) a dilation [12] in $\mathbb { R } ^ { n }$ if lim inf $\| \mathbf { d } ( s ) x \| = + \infty$ and lim sup $\| \mathbf { d } ( s ) x \| = 0 , \forall x \neq \mathbf { 0 }$ . Any linear continuous s→+∞ s→−∞ group in $\mathbb { R } ^ { n }$ admits the representation [19]

$$\mathbf {d} (s) = e ^ {s G _ {\mathbf {d}}} = \sum_ {j = 1} ^ {\infty} \frac {s ^ {j} G _ {\mathbf {d}} ^ {j}}{j !}, \quad s \in \mathbb {R}, \tag {2}$$

where $G _ { \mathbf { d } } ~ \in ~ \mathbb { R } ^ { n \times n }$ is a generator of d. A continuous linear group (2) is a dilation in $\mathbb { R } ^ { n }$ if and only if $G _ { \mathbf { d } }$ is an anti-Hurwitz matrix. In this paper we deal only with continuous linear dilations. A dilation d in $\mathbb { R } ^ { n } { \mathrm { ~ i s ~ i ) } }$ monotone if the function $s \mapsto \| \mathbf { d } ( s ) x \|$ is strictly increasing, ∀x $\neq \mathbf { 0 } ; { \mathrm { i i } } )$ strictly monotone if $\exists \beta > 0$ such that $\| \mathbf { d } ( s ) x \| \leq e ^ { \beta s } \| x \| , \forall s \leq 0 , \forall x \in \mathbb { R } ^ { n }$ .

The following result is the straightforward consequence of the existence of the quadratic Lyapunov function for stable linear system [17].

Corollary 1 A linear continuous dilation in √ $\mathbb { R } ^ { n }$ is strictly monotone with respect to the weighted Euclidean norm $\| x \| = { \sqrt { x ^ { \top } P x } }$ with $0 \prec P \in \mathbb { R } ^ { n \times n }$ if and only if $P G _ { \bf d } + G _ { \bf d } ^ { \top } P \succ 0 , P \succ 0$ .

The standard dilation corresponds to $G _ { \mathbf { d } _ { 1 } } = I _ { n }$ and $\mathbf { d } _ { 1 } ( s ) = e ^ { s } I _ { n }$ . The generator of the weighted dilation [24] is $G _ { \mathbf { d } _ { 2 } } { = } \mathrm { d i a g } ( r _ { 1 } , . . . , r _ { n } )$ .
