# 2 PROBLEM STATEMENT

The optimal control problem for a class of linear time-invariant plants with parameter uncertainty is considered:

$$
\forall t \geq t _ {r} ^ {+}, \left\{ \begin{array}{l} \dot {x} _ {p} (t) = A _ {p} x _ {p} (t) + B _ {p} u (t), x _ {p} \left(t _ {r} ^ {+}\right) = x _ {p 0}, \\ z (t) = C _ {p} ^ {\mathrm{T}} x _ {p} (t), \end{array} \right. \tag {3}
$$

where $x _ { p } \left( t \right) \in \mathbb { R } ^ { n _ { p } }$ is a measurable plant state vector with an unknown initial conditions $\boldsymbol { x } _ { p 0 } , \boldsymbol { u } ( t ) \in \mathbb { R } ^ { m }$ is the control vector, $z \left( t \right) \in \mathbb { R } ^ { m }$ is the performance output, $A _ { p } \in \mathbb R ^ { n _ { p } \times n _ { p } }$ is an unknown state matrix, $B _ { p } \in \mathbb { R } ^ { n _ { p } \times m }$ is an unknown input matrix of the plant, $C _ { p } \in \mathbb { R } ^ { n _ { p } \times m }$ is a known output matrix to form the output ?? (??), the pair $\left( A _ { p } , B _ { p } \right)$ is fully controllable.

The objective is to derive an optimal control law that allows the output ?? (??) to track a given bounded piecewise constant command $r \in L _ { \infty } \cap \mathbb { R } ^ { m }$ . For this purpose, a weighted integral error state $e _ { z _ { I } } \left( t \right) = \vartheta \intop _ { t _ { * } ^ { + } } \left( z \left( \tau \right) - r \right) d \tau \left( \vartheta \neq 0 \right)$ is introduced to denote the integrated output tracking error. The system (3) is augmented with $e _ { z _ { I } } \left( t \right) \mathrm { t o }$ yield the extended system as follows:

$$
\forall t \geq t _ {r} ^ {+}, \left\{ \begin{array}{l} \dot {x} (t) = \theta_ {A B} ^ {\mathrm{T}} \Psi (t) - B _ {r} r = A x (t) + B u (t) - B _ {r} r, x \left(t _ {r} ^ {+}\right) = x _ {0}, \\ z (t) = C ^ {\mathrm{T}} x (t), \end{array} \right. \tag {4}
$$

where $x \left( t \right) = \left[ x _ { p } \left( t \right) e _ { z _ { I } } \left( t \right) \right] ^ { \mathrm { T } } \in \mathbb { R } ^ { n }$ is an augmented state vector $\left( n = n _ { p } + m \right)$ ,
