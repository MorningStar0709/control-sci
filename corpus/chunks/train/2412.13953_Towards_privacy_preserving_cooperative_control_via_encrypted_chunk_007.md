# 1. Only agent ?? should have access to plaintext value. Only agent ?? should have access to plaintext valuesOnly agent ?? should have access to plaintext values o3 Distributed solution via ADMM

2. Only the operator should have access to plaintex Only the operator should have access to plaintextOnly the operator should have access to plaintextBefore focussing on a privacy-preserving implementation in values of ????.values of ????.values of ????.Section 4, we briefly present a distributed (plaintext) solution 3. Neither the agents nor the operator should have accesNeither the agents nor the operator should have accessNeither the agents nor the operator should have accessof (1) via ADMM. The largely standard procedure builds on to plaintext values oto plaintext values of to plaintext values of athe augmented Lagrangian

$$L _ {\rho} (z _ {1}, \dots , z _ {M}, \zeta , \lambda_ {1}, \dots , \lambda_ {M}) :=\sum_ {i = 1} ^ {M} f _ {i} (z _ {i}, p _ {i}) + \lambda_ {i} ^ {\top} (z _ {i} - (\zeta) _ {\mathcal {K} _ {i}}) + \frac {\rho}{2} \| z _ {i} - (\zeta) _ {\mathcal {K} _ {i}} \| _ {2} ^ {2}$$

In other words, while with the dual variables $\lambda _ { i } \in \mathbb { R } ^ { v _ { i } }$ (1) in a distributed manne and the positive weighting factor $\rho \in \mathbb { R }$ . According to [6, p. 55], the three ADMM steps per iteration $\tau \in \mathbb { N }$ are

$$z _ {i} ^ {\tau + 1} := \arg \min _ {z _ {i}} L _ {\rho} (z _ {1}, \dots , z _ {M}, \zeta^ {\tau}, \lambda_ {1} ^ {\tau}, \dots , \lambda_ {M} ^ {\tau}), \tag {5a}\zeta^ {\tau + 1} := \arg \min _ {\zeta} L _ {\rho} (z _ {1} ^ {\tau + 1}, \ldots , z _ {M} ^ {\tau + 1}, \zeta , \lambda_ {1} ^ {\tau}, \ldots , \lambda_ {M} ^ {\tau}), \tag {5b}\lambda_ {i} ^ {\tau + 1} := \lambda_ {i} ^ {\tau} + \rho \left(z _ {i} ^ {\tau + 1} - \left(\zeta^ {\tau + 1}\right) \kappa_ {i}\right). \tag {5c}$$
