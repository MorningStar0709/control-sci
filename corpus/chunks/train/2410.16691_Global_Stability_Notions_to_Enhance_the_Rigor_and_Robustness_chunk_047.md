hold for certain functions $a \in K _ { \infty }$ , $V \in C ^ { 1 } ( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } )$ , a non-decreasing, continuous function $\varphi : \mathbb { R } _ { + } \to \mathbb { R } .$ and a locally Lipschitz function $c : \mathbb { R } ^ { n } \times \mathbb { R } _ { + } \to \mathbb { R }$ with $c ( x , s ) > 0$ for all $\left( \boldsymbol { x } , \boldsymbol { s } \right) \in \mathbb { R } ^ { n } \times \mathbb { R } .$ + with $V ( x ) > \varphi { \bigl ( } s { \bigr ) }$ ,then (115), (116) hold with

$$H _ {s} (x) = \frac {1}{2} \left(\left(V (x) - \varphi (s)\right) ^ {+}\right) ^ {2}, Q _ {s} (x) = \left(V (x) - \varphi (s)\right) ^ {+} \min \left\{c (x, l): 0 \leq l \leq s \right\} \text { and } b (s) = a ^ {- 1} (\varphi (s))$$

Proof of Theorem 9: Let arbitrary $x _ { 0 } \in \mathbb { R } ^ { n }$ , $d \in L ^ { \infty } \left( \mathbb { R } _ { + } ; D \right)$ be given and consider the bounded solution $\ b { x } ( t ) \in \mathbb { R } ^ { n }$ of (36) with initial condition $x ( 0 ) = x _ { 0 }$ . Define $s = \| d \| _ { \infty }$ . Since the solution is bounded, there exists $R \in \mathbb R$ such that $H _ { s } \left( x ( t ) \right) \geq R$ for all $t \geq 0$ . Exploiting (116) and the fact that $s = \left\| d \right\| _ { \infty }$ we get for all $t \geq 0$ :

$$\int_ {0} ^ {t} Q _ {s} (x (\tau)) d \tau \leq H _ {s} (x (0)) - H _ {s} (x (t)) \leq H _ {s} (x (0)) - R \tag {118}$$

Moreover, the facts that $d \in L ^ { \infty } \left( \mathbb { R } _ { + } ; D \right) , \ x ( t ) \in \mathbb { R } ^ { n }$ is bounded and (36) imply that  is of class $L ^ { \infty } \left( \mathbb { R } _ { + } \right)$ . Since $Q _ { s } \in C ^ { 0 } \left( \mathbb { R } ^ { n } ; \mathbb { R } _ { + } \right)$ is locally Lipschitz, it follows that the function $t \mapsto Q _ { s } \left( x ( t ) \right)$ is uniformly continuous. From Barbălat’s Lemma (see [5, 9]), we have:

$$\lim _ {t \rightarrow + \infty} \left(Q _ {s} (x (t))\right) = 0 \tag {119}$$

We next claim that

$$\operatorname * {l i m s u p} _ {t \to + \infty} \left(\left| y (t) \right|\right) \leq b \left(\left\| d \right\| _ {\infty}\right) \tag {120}$$
