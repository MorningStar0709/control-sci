Proof: Let $\begin{array} { r l } { U = \left\lceil \mathbb { 0 } _ { m \times n } \quad B ^ { \top } \right\rceil } \end{array}$ . Let $V = \left[ I _ { n } \quad \mathbb { 0 } _ { n \times n } \right]$ . By continuity, the LMI (52) holds for some $c > 0$ if and only if

$$
\underbrace {\left[ \begin{array}{c c} - 2 X & D + X W ^ {\top} \\ D + W X & - 2 D \end{array} \right]} _ {\mathcal {M}} + U ^ {\top} Y V + V ^ {\top} Y ^ {\top} U \prec 0. \tag {54}
$$

An application of the Projection Lemma (Lemma 38) states that the inequality (54) holds if and only if $U _ { \perp } ^ { \top } \mathcal { M } U _ { \perp } \preccurlyeq 0$ and $V _ { \perp } ^ { \top } \mathcal { M } \bar { V _ { \perp } } \prec \bar { 0 } .$ . The second condition always holds, as the (2,2) block of M is negative definite. The first condition is identical to inequality (53).
