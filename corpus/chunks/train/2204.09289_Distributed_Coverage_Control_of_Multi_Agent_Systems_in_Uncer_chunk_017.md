Through the above analysis, it is concluded that the right side of the equation is less than 0 and $\alpha \Delta T _ { i } ( x _ { 0 } , t )$ is less than 0, we can get $h _ { i } ( x _ { 0 } , t ) > 0$ , which completes the proof. □

Lemma 4.2. Each agent can reach the triangular area with local maximum in finite time using Maximal Upgrade Method.

Proof. In Maximal Upgrade Method, before the update, the temperature field remains unchanged. $T _ { i } ( x _ { 0 } )$ is the temperature value of local maximum point in region $D _ { i } , T _ { i } ( \mathbf { s _ { i } ( t ) } )$ is the temperature value of each point on the trajectory of agent i. Contruct the following Lyapunov-Krasovskii function:

$$V _ {i} (t) = T _ {i} (x _ {0}) - T _ {i} (\mathbf {s _ {i}} (\mathbf {t})).$$

Calculating the time derivative of $V _ { i } ( t )$ yields

$$
\begin{array}{l} \dot {V} _ {i} (t) = - \bigtriangledown T _ {i} (\mathbf {s _ {i}} (\mathbf {t})) \dot {\mathbf {l _ {i}}} ^ {\mathbf {T}} (\mathbf {t}) \\ = - \bigtriangledown T _ {i} (\mathbf {s _ {i}} (\mathbf {t})) \mathbf {V _ {i}} \frac {\bigtriangledown \mathbf {T _ {i} ^ {T}} (\mathbf {s _ {i}} (\mathbf {t}))}{| | \bigtriangledown \mathbf {T _ {i}} (\mathbf {s _ {i}} (\mathbf {t})) | |} \\ = - V _ {i} | | \bigtriangledown T _ {i} (\mathbf {s _ {i}} (\mathbf {t})) | |. \\ \end{array}
$$

where ${ \dot { s } } _ { i } ( t )$ is defined in (13). We can easily get $V _ { i } ( t ) > 0$ and $\dot { V } _ { i } ( t ) < 0$ . Hence, the system (12) is asymptotical stable. In other words, the agent can reach the local maximum point when $t \longrightarrow \infty$ . According to Lyapunov asymptotical stability and the definition of limit, $\forall \epsilon > 0 .$ , $\exists T > 0$ , when $t > T$ , one has $| | \mathbf { s _ { i } ( t ) } - \mathbf { x _ { 0 } } | | \leq \epsilon$ . Take  equals the radius of minimum covering circle $r _ { m i n }$ , which completes the proof. □

Lemma 4.3. Delaunay triangulation enables each agent to complete workload on its subregion in finite time.
