# V. IMPLEMENTATION STEPS

The implementation process of the proposed UAV controller is presented step by step in this section as follows:

Step 1. Define the initial values $X _ { 0 } = [ x _ { p } ( 0 ) , y _ { p } ( 0 ) , z _ { p } ( 0 ) ]$ ], $\begin{array} { r l r } { Q _ { 0 } } & { { } \ = \ } & { [ q _ { 0 } ( 0 ) , q _ { 1 } ( 0 ) , q _ { 2 } ( 0 ) , q _ { 3 } ( 0 ) ] ^ { \top } } \end{array}$ , and $\begin{array} { r l r l } { \Omega _ { B _ { 0 } } } & { { } } & { = } & { { } } \end{array}$ $[ p _ { v } ( 0 ) , q _ { v } ( 0 ) , r _ { v } ( 0 ) ]$ . Calculate for R and $R _ { d }$ as follows:

$$
\begin{array}{l} R = \left(q _ {0} ^ {2} - \boldsymbol {q} ^ {\top} \boldsymbol {q}\right) I _ {3} + 2 \boldsymbol {q} \boldsymbol {q} ^ {\top} + 2 q _ {0} [ \mathbf {q} ] _ {\times} \tag {54} \\ R _ {d} = \left(q _ {d 0} ^ {2} - \boldsymbol {q} _ {d} ^ {\top} \boldsymbol {q} _ {d}\right) I _ {3} + 2 \boldsymbol {q} _ {d} \boldsymbol {q} _ {d} ^ {\top} + 2 q _ {d 0} [ \mathbf {q} _ {d} ] _ {\times} \\ \end{array}
$$

Step 2. Set UAV’s desired position trajectory: $\begin{array} { r l } { X _ { d } } & { { } = } \end{array}$ $[ x _ { d } , y _ { d } , z _ { d } ] ^ { \top }$ ⊤

Step 3. Calculate position errors using (9):

$$e _ {x p} = x _ {p} - x _ {d}e _ {y p} = y _ {p} - y _ {d}e _ {z p} = z _ {p} - z _ {d}$$

and follow (13), (14) to design virtual $v _ { 1 _ { d } } , v _ { 2 _ { d } }$ , and $v _ { 3 _ { d } } \colon$

$$v _ {1 d} = - m _ {x p} e _ {x p} + \dot {x} _ {d}v _ {2 d} = - m _ {y p} e _ {y p} + \dot {y} _ {d}v _ {3 d} = - m _ {z p} e _ {z p} + \dot {z} _ {d}$$

and find:

$$e _ {v 1} = v _ {1} - v _ {1 d}e _ {v 2} = v _ {2} - v _ {2 d}e _ {v 3} = v _ {3} - v _ {3 d}$$

Step 4. Design and implement the ABC control for translation dynamics with the update laws in (19) and (20):

$$U _ {x} = - m _ {x p} ^ {2} e _ {x p} - \hat {n} _ {x 2} e _ {v 1} + \ddot {x} _ {d}U _ {y} = - m _ {y p} ^ {2} e _ {y p} - \hat {n} _ {y 2} e _ {v 2} + \ddot {y} _ {d}U _ {z} = - m _ {z p} ^ {2} e _ {z p} - \hat {n} _ {z 2} e _ {v 3} + \ddot {z} _ {d}$$

where the control parameter adaptation to compensate for uncertainties are given by:

$$
\text { Adaptive   update } \left\{ \begin{array}{l l} \dot {\hat {n}} _ {x 2} & = \eta_ {1} e _ {v 1} ^ {2} \\ \dot {\hat {n}} _ {y 2} & = \eta_ {2} e _ {v 2} ^ {2} \\ \dot {\hat {n}} _ {z 2} & = \eta_ {3} e _ {v 3} ^ {2} \end{array} \right.
$$

Step 5. Find the actual thrust using the previous step as follows:

$$F _ {t h} = m \sqrt {U _ {x} ^ {2} + U _ {y} ^ {2} + (U _ {z} + g) ^ {2}}$$

Step 6. Generate the desired attitude and angular velocity using (30) and (34) as follows:

$$q _ {0 d} = \sqrt {\frac {m (g + U _ {z})}{2 F _ {t h}} + \frac {1}{2}}q _ {1 d} = - \frac {m U _ {y}}{2 F _ {t h} q _ {0 d}}q _ {2 d} = \frac {m U _ {x}}{2 F _ {t h} q _ {0 d}}q _ {3 d} = 0$$
