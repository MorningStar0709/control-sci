$$
\varphi_ {p, j} \triangleq \left\{ \begin{array}{l l} \phi_ {p, j} \left(V _ {p, j - 1} ^ {T} \varphi_ {p, j - 1}\right), & j \in \{1, \dots , k _ {p} \}, \\ \eta_ {p}, & j = 0. \end{array} \right. \tag {5}
$$

The arguments of $\varphi _ { p , j }$ are suppressed for notational brevity. Let ${ \phi _ { p , j } ^ { \prime } } \ : \ \mathbb { R } ^ { L _ { p , j } } \ \to \ \mathbb { R } ^ { L _ { p , j } \times \overleftarrow { L } _ { p , j } }$ be defined as $\phi _ { p , i } ^ { \prime } ( y ) \ \triangleq$ $\begin{array} { r } { \frac { \partial } { \partial u } \phi _ { p , j } ( y ) \ \forall y \in \mathbb { R } ^ { L _ { p , j } } } \end{array}$ . The short-hand notation $\Phi _ { p } ^ { \tilde { \theta } _ { p } } ( \eta _ { p } ) \triangleq$ $\Phi _ { p } ^ { ' } ( \eta _ { p } , V _ { p , 0 } , V _ { p , 1 } , . . . , V _ { p , k _ { p } } )$ is defined for notational brevity in the subsequent development. Then the output of the $p ^ { t { \dot { h } } }$ building block is given by $\eta _ { p } + \Phi _ { p } ^ { \theta _ { p } } ( \eta _ { p } )$ , where the addition of the input term $\eta _ { p }$ represents the shortcut connec-$\mathbb { R } ^ { n } \times \mathbb { R } ^ { \Sigma _ { p = 1 } ^ { m } \Sigma _ { j = 0 } ^ { k _ { p } } L _ { p , j } L _ { p , j + 1 } }  \mathbb { R } ^ { n }$ $\Phi _ { p } . \mathrm { \bf ~ A s }$ Figure 1, the ResNet Φ :, which defines the mapping $\left( \eta _ { 1 } , \theta \right) \mapsto \Phi ^ { \theta } \left( \eta _ { 1 } \right)$ , is modeled as [14]

$$\Phi^ {\theta} \left(\eta_ {1}\right) \triangleq \eta_ {m} + \Phi_ {m} ^ {\theta_ {m}} \left(\eta_ {m}\right), \tag {6}$$

where ${ \boldsymbol { \theta } } ~ \triangleq ~ \left\lceil ~ { \boldsymbol { \theta } } _ { 1 } ^ { T } ~ \dots ~ { \boldsymbol { \theta } } _ { m } ^ { T } ~ \right\rceil ^ { T } ~ \in ~ \mathbb { R } ^ { \Sigma _ { p = 1 } ^ { m } \Sigma _ { j = 0 } ^ { k _ { p } } L _ { p , j } L _ { p , j + 1 } } ~ \mathrm { d e } - \Sigma _ { p = 1 } ^ { m } ~ .$ notes the vector of weights for the entire ResNet, and $\eta _ { m }$ is evaluated using the recursive relation

$$
\eta_ {p} = \left\{ \begin{array}{l l} \eta_ {p - 1} + \Phi_ {p - 1} ^ {\theta_ {p - 1}} (\eta_ {p - 1}), & p \in \{2, \dots , m \}, \\ x, & p = 1. \end{array} \right. \tag {7}
$$

The recursive relation in (7) has valid dimensions under the constraint $L _ { 1 , 0 } ~ = ~ L _ { 1 , k _ { 1 } + 1 } ~ = ~ L _ { 2 , 0 } ~ = ~ L _ { 2 , k _ { 2 } + 1 } ~ = ~ . . . ~ =$
