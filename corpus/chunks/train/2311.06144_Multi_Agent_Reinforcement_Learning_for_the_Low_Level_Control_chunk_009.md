# C. Quadrotor Dynamics

Consider a quadrotor UAV, illustrated at Figure 1, where $\{ \vec { e } _ { 1 } , \vec { e } _ { 2 } , \vec { e } _ { 3 } \}$ corresponds to the inertial frame, and $\{ \vec { b } _ { 1 } , \vec { b } _ { 2 } , \vec { b } _ { 3 } \}$ denotes the body-fixed frame located at the mass center of the quadrotor. The position, velocity, and angular velocity of the quadrotor are represented by $x \in \mathbb { R } ^ { 3 } , v \in \mathbb { R } ^ { 3 }$ , and $\Omega =$ $[ \Omega _ { 1 } , \bar { \Omega } _ { 2 } , \Omega _ { 3 } ] ^ { T } \in \mathbb { R } ^ { 3 }$ , respectively, where x and v are resolved in the inertial frame, and Ω is in the body-fixed frame. The attitude is defined by the rotation matrix $R = [ b _ { 1 } , b _ { 2 } , b _ { 3 } ] \in$ ${ \mathsf { S O } } ( 3 ) = \{ R \in \mathbb { R } ^ { 3 \times 3 } | R ^ { T } R = I _ { 3 \times 3 } , \ \det [ R ] \stackrel { \cdot } { = } 1 \}$ , where $b _ { i } \in \mathbb { R } ^ { 3 }$ is the representation of $\vec { b } _ { i }$ resolved in the inertial frame for $i \in \{ 1 , 2 , 3 \}$ .

From [5], the equations of motion are given by

$$\dot {x} = v, \tag {7}m \dot {v} = m g e _ {3} - f R e _ {3}, \tag {8}\dot {R} = R \hat {\Omega}, \tag {9}J \dot {\Omega} + \Omega \times J \Omega = M, \tag {10}$$

where the hat map $\hat { \mathbf { \eta } } : \mathbb { R } ^ { 3 } \to \mathfrak { s o } ( 3 ) = \{ S \in \mathbb { R } ^ { 3 \times 3 } | S ^ { T } =$ $- S \}$ is defined by the condition that ${ \hat { x } } y \ = \ x \times y .$ , and xˆ is skew-symmetric for any $x , y \ \in \ \mathbb { R } ^ { 3 }$ . The inverse of the hat map is denoted by the vee map $\mathsf { V } : \mathsf { s o } ( 3 ) \to \mathbb { R } ^ { 3 }$ . Next, $m \in \mathbb { R }$ and $J \in \mathbb { R } ^ { 3 \times 3 }$ are the mass and the inertia matrix of the quadrotor with respect to the body-fixed frame, respectively, and $g \in$ R is the gravitational acceleration. From the total thrust $\begin{array} { r } { f = \sum _ { i = 1 } ^ { 4 } \bar { T } _ { i } \in \mathbb { R } } \end{array}$ and the moment $M = [ M _ { 1 } , M _ { 2 } , M _ { 3 } ] ^ { T } \in \mathbb { R } ^ { 3 }$ resolved in the body-fixed frame, the thrust of each motor can be computed by

$$
\left[ \begin{array}{l} T _ {1} \\ T _ {2} \\ T _ {3} \\ T _ {4} \end{array} \right] = \frac {1}{4} \left[ \begin{array}{c c c c} 1 & 0 & 2 / d & - 1 / c _ {\tau f} \\ 1 & - 2 / d & 0 & 1 / c _ {\tau f} \\ 1 & 0 & - 2 / d & - 1 / c _ {\tau f} \\ 1 & 2 / d & 0 & 1 / c _ {\tau f} \end{array} \right] \left[ \begin{array}{l} f \\ M _ {1} \\ M _ {2} \\ M _ {3} \end{array} \right]. \tag {11}
$$

where $d \in \mathbb { R }$ is the distance between the center of the rotor and the third body-fixed axis, and $c _ { \tau f } \in \mathbb { R }$ is a coefficient relating the thrust and the reactive torque.
