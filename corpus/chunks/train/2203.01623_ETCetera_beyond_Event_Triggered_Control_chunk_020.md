$$\mathcal {R} _ {k} := \mathcal {Q} _ {k} \setminus \left(\bigcap_ {j = 1} ^ {k - 1} \mathcal {Q} _ {j}\right) = \mathcal {Q} _ {k} \cap \bigcap_ {j = 1} ^ {k - 1} \bar {\mathcal {Q}} _ {j},
Q _ {k} := \left\{ \begin{array}{l l} \{\boldsymbol {x} \in \mathbb {R} ^ {n _ {\zeta}} \mid \boldsymbol {x} ^ {\mathrm{T}} \boldsymbol {N} (h k) \boldsymbol {x} > 0 \}, & k <   k _ {\max}, \\ \mathbb {R} ^ {n _ {\zeta}}, & k = k _ {\max}, \end{array} , \right. \tag {8}

\boldsymbol {N} (h k) := \left[ \begin{array}{c} \boldsymbol {M} (h k) \\ \mathbf {I} \end{array} \right] ^ {\mathsf {T}} \boldsymbol {Q} \left[ \begin{array}{c} \boldsymbol {M} (h k) \\ \mathbf {I} \end{array} \right].
$$

In (8), the set $Q _ { k }$ is the set of all points in the state-space that certainly triggered by time ℎ??; then the set $\mathcal { R } _ { k }$ is the isochronous subset obtained by removing from $Q _ { k }$ all points that have triggered before, i.e., $Q _ { j } , j < l$ ??. The sets $\mathcal { R } _ { k }$ can be checked for non-emptiness exactly via nonlinear satisfiability-modulo-theories (SMT) solvers, e.g., Z3 [17], or approximately through a semi-definite relaxation (SDR) as proposed in [13]. The non-empty sets form the finite state-space of a quotient system [40], which gives formally a (exact) simulation relation to the original PETC traffic. Effectively, the sets $\mathcal { R } _ { k }$ form a partition of $\mathbb { R } ^ { n _ { \zeta } }$ , and each $\mathcal { R } _ { k }$ is a conjunction of finitely many quadratic cones, as depicted in Fig. 3.

The transition relation is as well a problem of checking nonemptiness of an intersection of quadratic inequalities:

$$\exists x \in \mathcal {R} _ {i}: M (h k) x \in \mathcal {R} _ {j} \implies \mathcal {R} _ {i} \xrightarrow {k} \mathcal {R} _ {j}. \tag {9}$$
