# E.2 Proof of Proposition 4

Informally, $E _ { \mathrm { E 5 7 } }$ implies that the optimal linear controller for $\theta ^ { * }$ is close to satisfying the constraints. Therefore, we will bound the regret by approximating both the best constrained controller and the controller of Algorithm 2 by the optimal unconstrained linear controller.

We will decompose the regret as follows. Define $C ^ { \mathrm { a l g ^ { \prime } } }$ to be the controller of Algorithm 2 after the warm-up period, i.e. starting at time $t = T _ { 0 }$ . Therefore , C alg′t $C _ { t } ^ { \mathrm { a l g ^ { \prime } } } = C _ { t + T _ { 0 } } ^ { \mathrm { a l g } }$ . Define $x _ { 0 } ^ { \prime } , x _ { 1 } ^ { \prime } , \ldots$ . as the series of states when using algorithm $C ^ { \mathrm { a l g } }$ . Define $W ^ { \prime } = \{ w _ { i } \} _ { i = T _ { 0 } } ^ { T - 1 }$ . Recall that $C _ { K } ^ { \mathrm { u n c } }$ is the linear controller such that $C _ { K } ^ { \mathrm { u n c } } ( x ) = - K x$ . We can decompose the regret as follows:
