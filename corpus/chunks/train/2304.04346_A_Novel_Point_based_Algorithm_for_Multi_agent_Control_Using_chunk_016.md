# A. Lower Bound Update

In CHSVI, we maintain three lower bound functions (sets of α-vectors) corresponding to the three stages of t. Let $L ^ { \ell }$ denote the lower bound function and $\mathcal { V } ^ { \ell }$ denote the corresponding set of α-vectors for stage \` (of some time t). Let $T ^ { \ell }$ be the Bellman operator for stage \`. We now describe the update steps in detail.

Stage $\ell { \bf \Phi } = { \bf \Phi } 0 , 1 ;$ In this stage, the coordinator picks a prescription for agent $i = \ell + 1$ . For $b \in \Delta (  { \mathcal { S } } ^ { \ell } )$ , the Bellman update of the lower bound at b is given by

$$[ T ^ {\ell} L ^ {\ell + 1} ] (b) = \max _ {\gamma^ {i} \in \bar {\mathcal {A}} ^ {i}} L ^ {\ell + 1} (\tau^ {\ell} (b, \gamma^ {i}))= \max _ {\gamma^ {i} \in \bar {\mathcal {A}} ^ {i}} \max _ {\alpha \in \mathcal {V} ^ {i}} \sum_ {(s ^ {\ell}, a ^ {i}) \in \mathcal {S} ^ {\ell} \times \mathcal {A} ^ {i}} b (s ^ {\ell}) \gamma^ {i} (a ^ {i} | m _ {s ^ {\ell}} ^ {i}) \alpha (s ^ {\ell}, a ^ {i}) \tag {7}$$

The optimization problem (7) can be solved via the following steps:

$$\gamma^ {i, \alpha} (m ^ {i}) := \underset {a ^ {i} \in \mathcal {A} ^ {i}} {\arg \max} \sum_ {s ^ {\ell} \in \mathcal {S} ^ {\ell}: m _ {s ^ {\ell}} ^ {i} = m ^ {i}} b (s ^ {\ell}) \alpha (s ^ {\ell}, a ^ {i}) \tag {8}\forall m ^ {i} \in \mathcal {M} ^ {i}, \alpha \in \mathcal {V} ^ {i}J (\alpha) := \sum_ {s ^ {\ell} \in \mathcal {S} ^ {\ell}} b (s ^ {\ell}) \alpha (s ^ {\ell}, \gamma^ {i, \alpha} (m _ {s ^ {\ell}} ^ {i})) \quad \forall \alpha \in \mathcal {V} ^ {i} \tag {9}\alpha^ {*} := \underset {\alpha \in \mathcal {V} ^ {i}} {\arg \max} J (\alpha) \tag {10}\gamma^ {i, *} := \gamma^ {i, \alpha^ {*}}, \tag {11}$$

Function CHSVI:   
Input: A coordinator's POMDP model $(\mathcal{S},\bar{\mathcal{A}},\mathcal{O},b_0,\mathbb{P},r,\beta)$

Output: A belief based coordination policy $\pi$ ; an upper bound for $V^{*}(b_{0})$ and lower bound for $V^{\pi}(b_{0})$ .

// $\zeta \in (0,1)$ is a hyperparameter.

Initialize $U$ and $L$ ;

```txt
while Stopping criterion not satisfied do 
```

$\begin{array}{l}\left|\begin{array}{l}\epsilon = \zeta [U(b_0) - L(b_0)];\\ \operatorname {Explore}(U,L,b_0,\epsilon); \end{array}\right.\\ \pi = \operatorname {DirectControlPolicy}(L);$ return $\pi ,U(b_0),L(b_0)$
