where $\bar { \Upsilon } _ { \varsigma } ( h ) : = \mathrm { S y m } \{ \mathcal { D } ( A G L _ { 1 } + B K _ { c } L _ { 7 } ) \} + \Xi _ { 0 } + h \Xi _ { \varsigma } +$ $\bar { \Psi } + \bar { \mathcal { O } } + h N _ { \varsigma } \mathcal { R } _ { \varsigma } ^ { - 1 } N _ { \varsigma } ^ { \top }$ for $\varsigma = 1 , 2 $ . Then, the terms ${ \bar { \Upsilon } } _ { 1 } ( h )$ and ${ \bar { \Upsilon } } _ { 2 } ( h )$ are restructured as follows

$$
\bar {\Upsilon} _ {\varsigma} (h) := \left[ \begin{array}{c} [ \mathcal {D} A   \mathcal {D} B ] ^ {\top} \\ I \end{array} \right] ^ {\top}

\times \left[ \begin{array}{c c} 0 & \mathcal {F} \\ * & \Xi_ {0} + h \Xi_ {\varsigma} + \bar {\Psi} + \bar {\mathcal {O}} + h N _ {\varsigma} \mathcal {R} _ {\varsigma} ^ {- 1} N _ {\varsigma} ^ {\top} \end{array} \right] [ \cdot ].
$$

According to the data-based representation in Lemma 1, it holds for any $[ A B ] \in \Sigma _ { A B }$ that

$$
\left[ \begin{array}{c} {[ A B ] ^ {\top}} \\ I \end{array} \right] ^ {\top} \Theta_ {A B} \left[ \begin{array}{c} {[ A B ] ^ {\top}} \\ I \end{array} \right] \succeq 0. \tag {25}
$$

By the full-block S-procedure [34], we have $\bar { \Upsilon } _ { 1 } ( h ) \prec 0$ and $\bar { \Upsilon } _ { 2 } ( h ) \prec 0$ for any $[ A B ] \in \Sigma _ { A B }$ if there exists a scalar $\varepsilon > 0$ such that for $\varsigma = 1 , 2$

$$
\left[ \begin{array}{c c} 0 & \mathcal {F} \\ * & \Xi_ {0} + h \Xi_ {\varsigma} + \bar {\Psi} + \bar {\mathcal {O}} + h N _ {\varsigma} \mathcal {R} _ {\varsigma} ^ {- 1} N _ {\varsigma} ^ {\top} \end{array} \right]

+ \varepsilon \left[ \begin{array}{c c} \mathcal {V} _ {1} \Theta_ {A B} \mathcal {V} _ {1} ^ {\top} & \mathcal {V} _ {1} \Theta_ {A B} \mathcal {V} _ {2} ^ {\top} \\ * & \mathcal {V} _ {2} \Theta_ {A B} \mathcal {V} _ {2} ^ {\top} \end{array} \right] \prec 0. \tag {26}
$$

Finally, the Schur Complement Lemma ensures that the inequalities in (26) are equivalent to LMIs in (21). Similar to Theorem 1, we have a conclusion that LMIs (21) at the vertices of $h \in [ \underline { { h } } , \bar { h } ]$ are sufficient stability conditions for system (20) under the triggering condition (9) for any $[ A B ] \in \Sigma _ { A B }$ , and that $\eta ( \tau _ { j } ^ { k } )$ converges to the origin. Since G is nonsingular, system (20) exhibits the same stability behavior as (5).

Remark 5 (Model-based design under ETS). A model-based co-design method under the ETS (9) can be derived by replacing (21) with the condition $\bar { \Upsilon } _ { \varsigma } ( h ) \prec 0$ for $\varsigma = 1 , 2$ .
