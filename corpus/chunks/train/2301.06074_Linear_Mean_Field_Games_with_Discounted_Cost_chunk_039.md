Recall that $\mathsf { S } ^ { N }$ is the set of all reachable points by $( \mathsf { e } [ \cdot | \mathbf { x } ( t ) ] , x _ { 1 } ( t ) )$ for any $t \geq 0$ under any policy $\pi \in \Pi$ . Since $\gamma _ { \mathrm { o p t } } ^ { N }$ depends only on $( \mathbf { e } [ \cdot | \mathbf { x } ( t ) ] , x _ { 1 } ( t ) )$ , it is sufficient to define $\gamma _ { \mathrm { o p t } } ^ { N }$ only on $\mathsf { S } ^ { N }$ for the N -agent game problem. However, for the approximation analysis in the sequel, we need to extend the definition of $\gamma _ { \mathrm { o p t } } ^ { N }$ to the whole $\mathcal { P } ( \mathsf { X } ) \times \mathsf { X }$ . To do this, we first extend the definition of $T ^ { N } : \mathsf { X } \times \mathsf { S } ^ { N } \to [ 0 , \infty )$ to ${ \sf X } \times \mathcal { P } ( { \sf X } ) \times { \sf X }$ as follows

$$H ^ {N} (y, \mu , x) := \inf _ {(z, \nu , r) \in \mathsf {X} \times \mathsf {S} ^ {N}} \left\{T ^ {N} (z, \nu , r) + \mathsf {R} _ {1} ^ {N} d _ {\mathsf {X}} (y, z) + \mathsf {R} _ {2} ^ {N} W _ {1} (\mu , \nu) + \mathsf {R} _ {3} ^ {N} d _ {\mathsf {X}} (x, r) \right\}$$

One can prove that $H ^ { N } = T ^ { N }$ on ${ \mathsf { X } } \times { \mathsf { S } } ^ { N }$ and $H ^ { N }$ is $( \mathsf { R } _ { 1 } ^ { N } , \mathsf { R } _ { 2 } ^ { N } , \mathsf { R } _ { 3 } ^ { N } )$ )-Lipschitz continuous on ${ \sf X } \times \mathcal { P } ( { \sf X } ) \times { \sf X }$ . These properties can be established easily, and so, we omit the details. Now, for any $( \mu , x ) \in \mathcal P ( \mathsf X ) \times \mathsf X .$ , let us define the following policy

$$\gamma^ {*, N} (\mu , x) := \underset {a \in \mathsf {A}} {\arg \min} \left[ c (x, a, \mu) + \beta \int_ {\mathsf {X}} H ^ {N} (y, \mu , x) p (d y | x, a, \mu) \right]$$

Since $H ^ { N } = T ^ { N }$ on ${ \mathsf { X } } \times { \mathsf { S } } ^ { N }$ , we have $\gamma ^ { \ast , N } = \gamma _ { \mathrm { o p t } } ^ { N }$ on $\mathsf { S } ^ { N }$ . Hence, without loss of generality, we can take $\gamma ^ { \ast , N } \in \Pi _ { \mathrm { e } }$ as the best response policy in place of $\gamma _ { \mathrm { o p t } } ^ { N }$ as they have the same behavior on the reachable set $\mathsf { S } ^ { N }$ , and so, have the same cost function in response to MFE policy $\pi ^ { * }$ .

Now, we define

$$F _ {N} (x, a, \mu) := c (x, a, \mu) + \beta \int_ {\mathsf {X}} H ^ {N} (y, \mu , x) p (d y | x, a, \mu)$$

Since $H ^ { N }$ is $( \mathsf { R } _ { 1 } ^ { N } , \mathsf { R } _ { 2 } ^ { N } , \mathsf { R } _ { 3 } ^ { N } )$ )-Lipschitz continuous, by assumption (d), $F _ { N }$ has the following properties
