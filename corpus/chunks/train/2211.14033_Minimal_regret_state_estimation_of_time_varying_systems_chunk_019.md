Boyd et al. (1994) also show that the problem (28) is convex and can be solved with any convex optimization methods, which makes it very convenient in practice. Note that the γ-suboptimal estimators presented in Sabag and Hassibi (2021) are cheaper to compute, but do not provide an exact solution. For time-invariant systems, the problems (23a) and (28) can be solved once offline, yielding the observer $\begin{array} { r } { \mathcal { L } _ { r } = \Phi _ { w , r } ^ { - 1 } \Phi _ { v , r } . } \end{array}$ , which can be applied using (3). For time-varying systems, both problems must be solved every time the parameters change.
