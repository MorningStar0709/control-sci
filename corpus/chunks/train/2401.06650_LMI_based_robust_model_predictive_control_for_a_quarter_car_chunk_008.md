# A. Notation

R denotes the set of real numbers, Rn denotes the space of n-dimensional real (column) vector. $\mathbb { R } ^ { n \times m }$ denotes the space of $n \times m$ real matrices and $\mathbb { D } ^ { n }$ denotes the space of diagonal matrices in $\mathbb { R } ^ { n \times n } . \mathcal { H } ( A ) { : = } A { + } A ^ { T }$ for $A \in \mathbb { R } ^ { n \times n }$ and $A ^ { T }$ denotes the transpose of A. If $\pmb { V } \subseteq \mathbb { R } ^ { p \times q }$ is a subspace, then ${ \mathcal { B } } \pmb { V } = \{ V \in$ $\pmb { V } : \boldsymbol { V } \boldsymbol { V } ^ { T } \preceq 1 \}$ denotes the unit ball of V .
