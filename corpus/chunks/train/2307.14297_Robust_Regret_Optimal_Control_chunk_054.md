\bar {e} _ {t} = \left[ (I - \hat {B} ^ {\top} \hat {A} ^ {- \top} \hat {K} _ {x} ^ {\top}) \hat {H} \hat {K} _ {x} \quad \hat {B} ^ {\top} \hat {A} ^ {- \top} \right] \left[ \begin{array}{l} \hat {x} _ {t} \\ \bar {x} _ {t} \end{array} \right]
+ (I - \hat {B} ^ {\top} \hat {A} ^ {- \top} \hat {K} _ {x} ^ {\top}) \hat {H} d _ {t}.$$

Next, apply the following coordinate transformation:

$$
\left[ \begin{array}{c} \tilde {x} _ {t} \\ \bar {x} _ {t} \end{array} \right] = \left[ \begin{array}{c c} I & \hat {Y} \\ 0 & I \end{array} \right] \left[ \begin{array}{c} \hat {x} _ {t} \\ \bar {x} _ {t} \end{array} \right] := T \left[ \begin{array}{c} \hat {x} _ {t} \\ \bar {x} _ {t} \end{array} \right].
$$

Note that $\begin{array} { r } { T ^ { - 1 } = \left[ { \begin{array} { l l } { I } & { - \hat { Y } } \\ { 0 } & { I } \end{array} } \right] } \end{array}$ . We can simplify the results of the coordinate transformation by using the DARE (38) to show:

$$
\begin{array}{l} \hat {Y} \hat {A} ^ {- \top} \hat {K} _ {x} ^ {\top} \hat {H} = (\hat {A} \hat {Y} - \hat {A} \hat {Y} \hat {K} _ {x} ^ {\top} \hat {W} ^ {- 1} \hat {K} _ {x} \hat {Y}) \hat {K} _ {x} ^ {\top} \hat {H} \\ = \hat {A} \hat {Y} \hat {K} _ {x} ^ {\top} \hat {W} ^ {- 1} (\hat {W} - \hat {K} _ {x} \hat {Y} \hat {K} _ {x} ^ {\top}) \hat {H} = \hat {K} _ {y} ^ {\top}. \\ \end{array}
$$

Thus $\hat { A } - \hat { Y } \hat { A } ^ { - \top } \hat { K } _ { x } ^ { \top } \hat { H } \hat { K } _ { x }$ is equal to $A _ { F } = \hat { A } - \hat { K } _ { y } ^ { \top } \hat { K } _ { x }$ . Similarly, we can use the DARE (38) to show $\hat { Y } \hat { A } ^ { - \top } = A _ { F } \hat { Y }$ . Apply the transformation T to the state matrix of $\hat { F } ^ { \sim } \hat { F }$ using these relations:

$$
\begin{array}{l} T \left[ \begin{array}{c c} \hat {A} & 0 \\ - \hat {A} ^ {- \top} \hat {K} _ {x} ^ {\top} \hat {H} \hat {K} _ {x} & \hat {A} ^ {- \top} \end{array} \right] T ^ {- 1} = \left[ \begin{array}{c c} A _ {F} & A _ {F} \hat {Y} \\ - \hat {A} ^ {- \top} \hat {K} _ {x} ^ {\top} \hat {H} \hat {K} _ {x} & \hat {A} ^ {- \top} \end{array} \right] T ^ {- 1} \\ = \left[ \begin{array}{c c} A _ {F} & 0 \\ - \hat {A} ^ {- \top} \hat {K} _ {x} ^ {\top} \hat {H} \hat {K} _ {x} & \hat {A} ^ {- \top} (I + \hat {K} _ {x} ^ {\top} \hat {H} \hat {K} _ {x} \hat {Y}) \end{array} \right]. \\ \end{array}
$$
