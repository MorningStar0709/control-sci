Figure 1: Sensing unsafe region: sik is closest point on kth unsafe region from agent i’s position.

To ensure safe invariance, we want (23) to hold true for our given ECBF, which results in the following inequality

$$
\begin{array}{l} 2 v _ {i} ^ {T} v _ {i} + 2 (p _ {i} - s _ {i k}) ^ {T} u _ {i} + 2 k _ {1} (p _ {i} - s _ {i k}) ^ {T} v _ {i} \\ + k _ {0} (\| p _ {i} - s _ {i k} \| ^ {2} - \delta_ {e x} ^ {2}) \geq 0. \\ \end{array}
$$

This can be simplified and expressed as

$$A _ {i k} + B _ {i k} ^ {T} u _ {i} \geq 0, \tag {25}$$

whe and $\begin{array} { r l } & { \mathrm { \bf ~ r e ~ } A _ { i k } = 2 v _ { i } ^ { T } v _ { i } + 2 k _ { 1 } ( p _ { i } - s _ { i k } ) ^ { T } v _ { i } + k _ { 0 } ( \| p _ { i } - s _ { i k } \| ^ { 2 } - \delta _ { e x } ^ { 2 } ) } \\ & { B _ { i k } = 2 ( p _ { i } - s _ { i k } ) . } \end{array}$

A similar construction of ECBF is implemented to ensure internal safety, i.e. for collision avoidance within the agents. Consider the interaction between agent i and j, then with the required safe distance to be $\bar { \delta _ { i n } } \left( \ll r \right)$ , we define the ECBF as

$$h _ {i j} = \left\| p _ {i} - p _ {j} \right\| ^ {2} - \delta_ {i n} ^ {2} \forall j \in \mathcal {N} _ {i}; i \in \mathcal {V}.$$

As earlier, using equation (23) we get the inequality as

$$\bar {A} _ {i j} + \bar {B} _ {i j} ^ {T} (u _ {i} - u _ {j}) \geq 0 \forall j \in \mathcal {N} _ {i}; j \neq i, \tag {26}$$

where $\bar { A } _ { i j } = 2 ( v _ { i } - v _ { j } ) ^ { T } ( v _ { i } - v _ { j } ) + 2 k _ { 1 } ( p _ { i } - p _ { j } ) ^ { T } ( v _ { i } - v _ { j } ) +$ $k _ { 0 } ( \| p _ { i } - \dot { p } _ { j } \| ^ { 2 } - \delta _ { i n } ^ { 2 } )$ and $\bar { B } _ { i j } = 2 ( p _ { i } - p _ { j } )$ .

in  One can define a pairwise safe set $\mathcal { C } _ { i j }$ in the space $\mathcal { D } \in$ $\mathbb { R } ^ { n }$ as

$$\mathcal {C} _ {i j} = \left\{\left(p _ {i}, v _ {i}\right) \mid h _ {i k} > 0; h _ {i j} > 0 \right\} \forall i \neq j.$$

We note that direct implementation of the constraints (26) poses two difficulties, it depends on the velocity and control input of both agents which are not shared during the communication. For the velocity dependence, we utilize the velocity estimate $\hat { v } _ { i j }$ defined in (7) by taking into account the error bound of this estimate.

Lemma 3. For the dynamics defines by (1), the error bound, e, between $( v _ { i } - v _ { j } )$ and the relative velocity estimate vˆij defined by (7) is given by

$$\| e \| \leq \frac {2 u _ {\max}}{\eta}. \tag {27}$$

Using Lemma 3, we observe that relative velocity can be bounded using

$$\| v _ {i} - v _ {j} \| \geq \| \hat {v} _ {i j} \| - \| e \|.$$
