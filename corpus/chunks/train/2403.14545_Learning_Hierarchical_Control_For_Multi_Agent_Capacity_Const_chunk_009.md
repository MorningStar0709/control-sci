# IV. ITERATIVE DATA-DRIVEN HIERARCHICAL CONTROL

This section introduces our proposed hierarchical, iterative approach to solving (8). First, the two-level hierarchy is introduced. At each iteration, the high-level controller assigns a subset of tasks to an agent, and the low-level controller chooses actuation inputs for the agent. We then formalize desiderata for the resulting closed-loop system.
