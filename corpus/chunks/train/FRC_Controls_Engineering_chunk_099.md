# 4.5 Differential equations

A differential equation is an equation containing derivatives.

Let y be an unknown function of x and $f$ be a given function of x. An ordinary differential equation (ODE) contains an unknown function of one variable x, the unknown function’s derivatives, and given functions of x. For example,

$${\frac {d y}{d x}} = f (x)$$

Let y be an unknown function of $x _ { 1 }$ and $x _ { 2 }$ . A partial differential equation (PDE) contains unknown multivariable functions and their partial derivatives. For example,

$$y (x _ {1}, x _ {2}) = x _ {1} \frac {\partial y}{\partial x _ {1}} + x _ {2} \frac {\partial y}{\partial x _ {2}}$$

Some ODEs and PDEs have closed form solutions $( { \bf e . g . } , y ( x ) = y ( 0 ) e ^ { a x }$ for the ODE $\begin{array} { r } { \frac { d y } { d x } = a y ) } \end{array}$ , but most must be solved numerically with a computer instead.

This page intentionally left blank
