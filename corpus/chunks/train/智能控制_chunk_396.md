$$\frac {\mathrm{d} U _ {x i}}{\mathrm{d} t} = - \frac {\partial E}{\partial V _ {x i}} = - A \left(\sum_ {i = 1} ^ {N} V _ {x i} - 1\right) - A \left(\sum_ {y = 1} ^ {N} V _ {y i} - 1\right) - D \sum_ {y = 1} ^ {N} d _ {x y} V _ {y, i + 1} \tag {8.23}$$

按式(8.23)求 $U_{xi}(t)$ ，则可保证E逐渐减小，并最终达到极小值。

采用 Hopfield 网络求解 TSP 问题的算法描述如下：

(1) 置初值: t = 0, A = 1.5, D = 1.0, $\mu = 50$ ;

(2) 计算 N 个城市之间的距离 $d_{xy}(x, y = 1, 2, \cdots, N)$ ;

(3) 神经网络输入 $U_{n}(t)$ 的初始化在 0 附近产生；

(4) 利用动态方程式(8.23) 计算 $\frac{dU_{xi}}{dt}$ ;

(5) 根据一阶欧拉法离散化式(8.23)，求 $U_{ri}(t+1)$

$$U _ {x i} (t + 1) = U _ {x i} (t) + \frac {\mathrm{d} U _ {x i}}{\mathrm{d} t} \Delta T \tag {8.24}$$

(6) 为了保证收敛于正确解, 即矩阵 V 各行各列只有一个元素为 1, 其余为 0, 采用单调上升的 Sigmoid 函数计算 $V_{xi}(t)$

$$V _ {x i} (t) = \frac {1}{1 + \mathrm{e} ^ {- \mu U _ {x i} (t)}} \tag {8.25}$$

式中， $\mu>0,\mu$ 值的大小决定了 Sigmoid 函数的形状。

(7) 根据式(8.22)，计算能量函数 E:

(8) 检查路径的合法性, 判断迭代次数是否结束, 如果结束, 则终止, 否则返回第(4)步:

（9）显示输出迭代次数、最优路径、最优能量函数、路径长度的值，并作出能量函数随时间变化的曲线图。
