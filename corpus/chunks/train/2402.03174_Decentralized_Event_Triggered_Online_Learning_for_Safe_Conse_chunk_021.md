Theorem 1 shows the ultimate boundness of the consensus error with high probability, where the consensus error bound ǫ is increasing with the number of agents N . This relationship is intuitive, because in the worst case the prediction errors of the individual Gaussian process model on each body are superimposed, leading to a larger prediction error $\hat { \pmb { \eta } } ( \pmb { x } )$ with more agents. Moreover, the error ǫ is also related to the quality of the collected data reflected by $\underline { { \eta } } ,$ which is proportional to the variance of measurement noise $\sigma _ { n }$ considering Lemma 1 and (13). While using more accurate sensors or processing methods reduces measurement noise, the variance $\sigma _ { n }$ will not vanish in practice. To achieve arbitrary small consensus error ǫ, a high gain c is necessary. The size of the ultimate bound ǫ only depends on the control gain c, while the auxiliary gain c¯ determines the convergence speed [31]. This means larger c¯ results in faster convergence, which is obtained by considering the convergence speed of the auxiliary state in Lemma 2 with [31] and the fact that Lemma 3 only considers $\tilde { x } _ { i } = x _ { i } - \bar { x } _ { i }$ . The decoupled effects of c and c¯ provide us more flexibility to design the controller for desired performance in the steady state, i.e., for smaller ultimate average consensus errors, and the transition phase, i.e., for faster convergence, separately.

Remark 6: Note that only with $x _ { i } ( t ) \in \mathbb { X } , \forall t \in \mathbb { R } _ { 0 , + }$ , the boundness of the prediction error in Lemma 1 and thus in Theorem 1 holds. Moreover, it is easily to prove the trajectories to ${ \bar { x } } ^ { * }$ for each agent are in X due to the convex domain of $\mathbb { X }$ and $x _ { ( 0 ) } \in \mathbb { X }$ for $\forall i \in \nu$ . Therefore, Theorem 1 holds with safety guarantee for the controlled system, if the neighbor domain of trajectories to ${ \bar { x } } ^ { * }$ is a subset of $\mathbb { X } .$ This can be easily realized by restraining the initial condition $x _ { i } ( 0 )$ to a smaller subset $\mathbb { X } _ { 0 } \subset \mathbb { X }$ such that the domain of $x ( \infty )$ , i.e., $\{ x \in \mathbb { R } : | x - { \bar { x } } ^ { * } | \leq \epsilon \}$ , is a subset of $\mathbb { X }$ with $\epsilon = 2 c ^ { - 1 } N \bar { \eta }$ defined in Theorem 1 and $\bar { \eta } = \operatorname* { m a x } _ { i \in \mathcal { V } , x _ { i } \in \mathbb { X } } \ \eta _ { i } ( x _ { i } )$ from [12].
