$$J = \int_ {0} ^ {\infty} \left[ x _ {(t)} ^ {T} Q x _ {(t)} + u _ {(t)} ^ {T} R u _ {(t)} \right] d t \tag {24}u = - K x = - R ^ {- 1} B ^ {T} P x \tag {25}A ^ {T} P + P A + Q - P B R ^ {- 1} B ^ {T} P = 0 \tag {26}$$

The use of LQR control will be optimal in the case of a state regulator towards an equilibrium point in a system, but for the tracking case, the LQR controller is not capable of optimally, especially in systems that have high levels of nonlinearity and system dynamic changes such as inertial disturbances. Therefore, this paper proposes the design of LQG control, which is expected to overcome system dynamic changes, in this case, in the form of inertial disturbances to the Bicopter when carrying a payload. The LQG controller design that will be applied to the Bicopter flight simulation is described in Fig. 5 as follows:

• Requires a Bicopter dynamics model in the form of state space   
• Doing the process of linearization of the model   
• Check the controlability and observability of the system   
• Adding the noise process covariance Matrix, w and noise measurement covariance, v   
• Determine the Q Matrix and R Matrix   
• Calculate the feedback gain L   
• Calculate the feedback gain K   
• After the feedback gain K is obtained, then solve the differential equation of the Bicopter model with "ode45".

Figure 5 presents the Bicopter state space in Eq. (27) and the state observer in Eq. (28). In the LQG controller, there is an observer whose purpose is to determine the estimate of x˜ so that the estimated value approaches the actual value $\tilde { x } $ x along with t → ∞. Remember that the value of $x \big ( t _ { 0 } \big )$ is uncertain. Calculating the initial value of $\tilde { x } \big ( t _ { 0 } \big )$ to the observer is necessary by defining the estimation error as in Eq. (29).

$$\dot {x} = A x + B u; y = C x \tag {27}\dot {\tilde {x}} = A \tilde {x} + B u + L (y - \tilde {y}); \tilde {y} = C \tilde {x} \tag {28}e (t) = x (t) - \tilde {x} (t) \tag {29}$$

The observer is designed with the property $e ( t ) = x ( t ) -$ $\tilde { { \boldsymbol { x } } } ( t )$ along with $t  \infty$ . If the system has fully observable properties, then the L Matrix will always be obtained so the tracking error is asymptotically stable as desired. By deriving Eq. (29) against time, Eq. (30) is obtained.
