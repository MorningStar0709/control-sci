For any metric space E, let ${ \mathcal { M } } ( { \sf E } )$ denote the set of finite signed measures on E and $B ( \mathsf E )$ （2 denotes the set of bounded measurable real functions. Consider the vector spaces $( \mathcal { M } ( \mathsf { X } \times$ $\mathsf { A } ) , B ( \mathsf { X } \times \mathsf { A } ) \big )$  and $\left( \mathcal { M } ( \mathsf { X } ) , B ( \mathsf { X } ) \right)$ . Let us define bilinear forms on $( \mathcal { M } ( \mathsf { X } \times \mathsf { A } ) , B ( \mathsf { X } \times \mathsf { A } ) )$ and on $\left( \mathcal { M } ( \mathsf { X } ) , B ( \mathsf { X } ) \right)$ as follows

$$\langle \zeta , v \rangle := \int_ {\mathsf {X} \times \mathsf {A}} v (x, a) \zeta (d x, d a) \tag {55}\langle \nu , u \rangle := \int_ {X} u (x) \nu (d x) \tag {56}$$

where $\zeta \in \mathcal { M } ( \mathsf { X } \times \mathsf { A } ) , v \in B ( \mathsf { X } \times \mathsf { A } ) , \nu \in \mathcal { M } ( \mathsf { X } )$ , and $u \in B ( \mathsf { X } )$ . The bilinear form in (55) constitutes duality between $\mathcal { M } ( \mathsf { X } \times \mathsf { A } )$ and $B ( { \sf X } \times { \sf A } )$ , and the bilinear form in (56) constitutes duality between $\mathcal { M } ( \mathsf { X } )$ and $B ( { \sf X } )$ . For any $\zeta \in \mathcal { M } ( \mathsf { X } \times \mathsf { A } )$ , let $\hat { \zeta } \in \mathcal { M } ( \times )$ denote the marginal of $\zeta$ on $\mathsf { X } ,$ i.e.,

$$\hat {\zeta} (\cdot) = \zeta (\cdot \times \mathsf {A})$$

We define the linear map $\mathrm { T } _ { \mu } : \mathcal { M } ( \mathsf { X } \times \mathsf { A } ) \to \mathcal { M } ( \mathsf { X } )$ b y

$$\mathrm{T} _ {\mu} \zeta (\cdot) = \hat {\zeta} (\cdot) - \beta \int_ {\mathsf {X} \times \mathsf {A}} p _ {\mu} (\cdot | x, a) \zeta (d x, d a) =: \hat {\zeta} - \beta \zeta p _ {\mu}$$

which depends on $\mu .$

Recall that $\mathrm { M D P } _ { \mu }$ has the following components

$$\{\mathsf {X}, \mathsf {A}, c _ {\mu}, p _ {\mu}, \mu \}$$

where

$$c _ {\mu} (x, a) := c (x, a, \mu) = \int_ {X} \bar {c} (x, a, z) \mu (d z)p _ {\mu} (\cdot \mid x, a) := p (\cdot \mid x, a, \mu) = \int_ {\mathsf {X}} \bar {p} (\cdot \mid x, a, z) \mu (d z)$$

It is indeed equivalent to the following equality constrained linear program [19, Lemma 3.3 and Section 4]:

$$\text { minimize } _ {\zeta \in \mathcal {M} _ {+} (\mathrm{X} \times \mathrm{A})} \langle \zeta , c _ {\mu} \rangle\text { subject to } \mathrm{T} _ {\mu} (\zeta) = (1 - \beta) \mu \tag {57}$$

Indeed, for any policy π, define the $\beta .$ -discount expected occupation measure as
