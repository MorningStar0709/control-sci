The first equality is by smoothness, second inequality is by the update equation in algorithm 1.

Next, let $\delta _ { t } : = V ^ { \pi ^ { * } } - V ^ { \pi _ { \theta _ { t } } }$ . We have,

$$\delta_ {t + 1} - \delta_ {t} \leqslant - \frac {(1 - \gamma)}{5} c _ {t} ^ {2} \delta_ {t} ^ {2}. \tag {17}$$

Claim: $\begin{array} { r } { \forall t \geqslant 1 , \delta _ { t } \leqslant \frac { 5 } { c _ { t } ^ { 2 } ( 1 - \gamma ) } \frac { 1 } { t } } \end{array}$ c 2t (1 − γ ) t .

We prove the claim by using induction on $t \geqslant 1$ .

Base case. Since $\begin{array} { r } { \delta _ { t } \leqslant \frac { 1 } { 1 - \gamma } } \end{array}$ , the claim is true for all $t \leqslant 5$

Induction step: Let ϕt := c2(1−γ) . $\begin{array} { r } { \varphi _ { t } : = \frac { 5 } { c _ { t } ^ { 2 } ( 1 - \gamma ) } } \end{array}$ 5 Fix a $\iota t \geqslant 2 .$ , assume $\begin{array} { r } { \delta _ { t } \leqslant \frac { \varphi _ { t } } { t } } \end{array}$

Let $g : \mathbb { R } $ R be a function defined as $\textstyle g ( x ) = x - { \frac { 1 } { \varphi t } } x ^ { 2 }$ . One can verify easily that g is monotonically increasing in $\begin{array} { r } { [ 0 , \frac { \varphi _ { t } } { 2 } ] } \end{array}$ . Next with equation 19, we have

$$
\begin{array}{l} \delta_ {t + 1} \leqslant \delta_ {t} - \frac {1}{\varphi_ {t}} \delta_ {t} ^ {2} \\ = g (\delta_ {t}) \\ \leqslant g \left(\frac {\varphi_ {t}}{t}\right) \\ \leqslant \frac {\varphi_ {t}}{t} - \frac {\varphi_ {t}}{t ^ {2}} \\ = \varphi_ {t} \left(\frac {1}{t} - \frac {1}{t ^ {2}}\right) \\ \leqslant \varphi_ {t} \left(\frac {1}{t + 1}\right). \\ \end{array}
$$

This completes the proof of the claim. We will show that $c _ { t } \geqslant 1 / M$ in the next lemma. We first complete the proof of the corollary assuming this.

We fix a T > 1. Observe that, δt 6 5(1−γ)c2 1t $T \geqslant 1$ $\begin{array} { r } { \delta _ { t } \leqslant \frac { 5 } { ( 1 - \gamma ) c _ { t } ^ { 2 } } \frac { 1 } { t } \leqslant \frac { 5 } { ( 1 - \gamma ) c _ { T } ^ { 2 } } \frac { 1 } { t } } \end{array}$ (1−γ )c2T .

$$\sum_ {t = 1} ^ {T} V ^ {\pi^ {*}} - V ^ {\pi_ {\theta_ {t}}} = \frac {1}{1 - \gamma} \sum_ {t = 1} ^ {T} (\pi^ {*} - \pi_ {\theta_ {t}}) ^ {\mathrm{T}} \mathfrak {r} \leqslant \frac {5 \log T}{(1 - \gamma) c _ {T} ^ {2}} + 1.$$

Also we have that,
