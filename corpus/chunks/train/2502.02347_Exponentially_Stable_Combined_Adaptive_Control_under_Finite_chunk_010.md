# 4.1 Extraction of System Parameters

The system dynamics (3) can be written in linear-inparameter form,

$$\dot {\mathbf {x}} = \underbrace {\left[ \mathbf {A} \mathbf {b} k _ {p} \mathbf {b} k _ {p} \boldsymbol {\theta} ^ {T} \right]} _ {\mathbf {W} ^ {T}} \varphi , \tag {18}$$

where $\mathbf { W } \in \mathbb { R } ^ { q \times n } , q = n + 1 + p $ , and $\varphi \in \mathbb { R } ^ { q }$ is defined in (11). The regressor vector $\varphi$ is measurable however x˙ is not, hence both the signals are passed through a stable filter $( f / \left( s + f \right) , s : = \breve { d / } d t )$ . Let $\mathbf { y } _ { f }$ and $\varphi _ { f }$ denote the filtered signals of x˙ and $\varphi ,$ respectively, and their dynamics are as follows,

$$\dot {\mathbf {x}} _ {f} = - f \mathbf {x} _ {f} + f \mathbf {x},\mathbf {y} _ {f} = f \mathbf {x} - e ^ {- f (t - t _ {0})} f \mathbf {x} (t _ {0}) - f \mathbf {x} _ {f}, \tag {19}\dot {\varphi} _ {f} = - f \varphi_ {f} + f \varphi ,$$

where $f \in \mathbb { R } ^ { + }$ is the cut-off frequency of the filter and initial condition of the signals $\mathbf { x } _ { f }$ and $\varphi _ { f }$ are considered to be zero; hence, we can write

$$\mathbf {y} _ {f} = \mathbf {W} ^ {T} \boldsymbol {\varphi} _ {f}. \tag {20}$$

Let the regressor signal ϕ satisfies FE condition, [Lemma 6.8, (Narendra and Annaswamy, 2012)] infers $\varphi _ { f }$ also satisfies FE condition, then there exists a full rank matrix (12), that is converted to an orthogonal matrix $\Phi _ { b } \in \mathbb { R } ^ { q \times q }$ using Modified Gram-Schmidt (MGS) (Johnson and Horn, 1985), and a corresponding transformation is done to obtain another matrix $\mathbf { Y } _ { b } \in \mathbb { R } ^ { n \times q }$ . Let the matrices $\mathbf { Y } _ { b }$ and $\Phi _ { b }$ are

$$
\mathbf {Y} _ {b} = \left[ \begin{array}{c c c} \mathbf {Y} _ {b _ {1}} & \dots & \mathbf {Y} _ {b _ {q}} \end{array} \right], \quad \boldsymbol {\Phi} _ {b} = \left[ \begin{array}{c c c} \boldsymbol {\Phi} _ {b _ {1}} & \dots & \boldsymbol {\Phi} _ {b _ {q}} \end{array} \right], \tag {21}
$$

where $\mathbf { Y } _ { b _ { i } } \in \mathbb { R } ^ { n } , \Phi _ { b _ { i } } \in \mathbb { R } ^ { q } , i = \{ 1 , 2 , . . . , q \}$ , are computed using the Algorithm 1, that uses the following recursive calculations,
