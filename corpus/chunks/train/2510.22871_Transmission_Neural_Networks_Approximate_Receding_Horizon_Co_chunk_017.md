# C. Receding Horizon Control based on TransNNs

In the receding horizon control formulation, we use TransNNs to predict the approximate probability of infection in the future, and update the current state and the current control each time an observation of the current state $p ( t ) =$ $p ^ { 0 } ( t ) ~ ( \mathrm { o r } ~ s ( t ) ~ = ~ s ^ { 0 } ( t )$ with $s _ { i } ^ { 0 } ( t ) ~ = ~ - \log ( 1 - p _ { i } ^ { 0 } ( t ) ) )$ becomes available at time t. The corresponding receding horizon optimization problem (with shrinking horizons) is then formulated as follows:

$$J _ {3} (p ^ {0} (t), t) = \sum_ {k = t} ^ {T - 1} \mathbf {1} _ {n} ^ {\intercal} (c p (k) + u (k)),$$

subject to the dynamics given by (7) with $p ( t ) = p ^ { 0 } ( t )$ . At each time step, the optimal control problem over the horizon $\{ t , t + 1 , \ldots , T \}$ is solved (via the Minimum Principle) and only the resulting control action $u ( t )$ at time t is applied. The state for node i at time t satisfies that $p _ { i } ( t ) = 1$ if infected and $p _ { i } ( t ) = 0$ otherwise.

Equivalently, we can use the TransNN model (8) as the model for dynamics

$$s _ {i} (k + 1) = \sum_ {j \in N _ {i} ^ {\circ k}} \Psi (m _ {i j} ^ {k} (u _ {i} (k)), s _ {j} (k)), \quad i \in [ n ], \tag {24}$$

with $\Psi ( w , x ) = - \log ( 1 - w + w e ^ { - x } )$ and $s ( t ) = s ^ { 0 } ( t )$ and $s _ { i } ^ { 0 } ( t ) = - \log ( 1 - p _ { i } ^ { 0 } ( t ) ) )$ for $i \in [ n ]$ . Then the corresponding receding horizon cost is equivalently given by

$$J _ {3} (s ^ {0} (t), t) = \sum_ {k = t} ^ {T - 1} \mathbf {1} _ {n} ^ {\intercal} (c (\mathbf {1} _ {n} - \exp_ {\circ} (- s (k))) + u (k)),$$

where $\exp _ { \circ } ( \cdot )$ denotes the element-wise exponential function. The solution involves solving the following joint equations over the horizon $\{ t , t + 1 , \ldots , T \}$ for all $i \in [ n ]$

$$s _ {i} (k + 1) = \sum_ {j \in N _ {i} ^ {\circ k}} \Psi (m _ {i j} ^ {k} (u _ {i} ^ {*} (k)), s _ {j} (k)) \tag {25}$$

with $s _ { i } ( t ) = s _ { i } ^ { 0 } ( t )$ and

$$
\begin{array}{l} \lambda_ {i} (k) = c e ^ {- s _ {i} (k)} \\ + \sum_ {\ell \in \mathcal {N} _ {i -} ^ {\circ k}} \lambda_ {\ell} (k + 1) \frac {\partial}{\partial s _ {i}} \Psi \left(m _ {\ell i} ^ {k} \left(u _ {\ell} (k)\right), s _ {i} (k)\right) \tag {26} \\ \end{array}
$$

with $\lambda _ { i } ( T ) = 0 .$ , where $\mathcal { N } _ { i - } ^ { \circ k } : = \{ \ell : ( \ell , i ) \in E ^ { k } \} \cup i$ denotes the outgoing neighborhood of i with itself included. The optimal control rule used in solving the joint equations is given by
