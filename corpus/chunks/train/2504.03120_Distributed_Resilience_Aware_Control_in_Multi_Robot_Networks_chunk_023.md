# C. Controller Design

Now, we present our controller. Let $u _ { i , \mathrm { d e s } } ( x _ { i } )$ be the desired controller of robot i. Our CBF-based Quadratic Program (CBF-QP) controller for robot $i \in \nu$ is:

$$u _ {i} ^ {*} (\mathbf {x}) = \arg \min _ {u _ {i} \in \mathcal {U} _ {i}} | | u _ {i, \text {des}} (x _ {i}) - u _ {i} | | ^ {2} \tag {20}$$

s.t. (15) is satisfied.

Assumption 2. $u _ { i } ^ { * } ( \mathbf { x } )$ (20) is locally Lipschitz continuous with respect to x.

Remark 2. In general, CBF-QPs with input constraints do not yield locally Lipschitz continuous solutions, as the input constraints effectively act as additional constraints. The local Lipschitz continuity of QPs with multiple constraints have been studied in [27]–[29].

Now, we prove that our controller ensures both resilient consensus and physical safety under a specific class of malicious attacks on $c _ { i } ( t )$ :

Theorem 1. Let all conditions in Lemma 2-3 and Assumption 1-2 hold. Suppose all misbehaving agents $i \in \mathcal { A }$ update $c _ { i } ( t )$ according to $\begin{array} { r } { c _ { i } ( t ) = \sum _ { j \in \mathcal { N } _ { i } ( t ) } a _ { i j } ( \mathbf { x } ( t ) ) + \epsilon _ { i } , \forall t \geq t _ { 0 } , } \end{array}$ , where $\epsilon _ { i } \in \mathbb { R } ,$ , such that $\mathbf { x } ( t _ { 0 } ) \in \tilde { \mathcal { Z } } ( \mathbf { w } )$ . If each robot $i \in \nu$ computes its control input $u _ { i }$ using (20), then $\forall t \geq t _ { 0 } ,$ , all robots move such that (a) $\delta _ { \mathcal { L } } ( \mathcal { G } ( t ) ) \geq F ^ { \prime } , ( b )$ no collisions occur, and (c) resilient consensus is guaranteed.

Proof. (Part 1) By Assumption 1, all robots compute $u _ { i } ( t )$ from (20) $\forall t \geq t _ { 0 }$ , which means ${ \bf \delta u } ( t )$ from $( 2 0 ) \ \forall i \ \in \ \mathcal { V }$ satisfies (12) $\forall t \geq t _ { 0 }$ by Lemma 3. Since ${ \bf { ( a ) } } \ u _ { i } ^ { * } ( { \bf { x } } ) \ \forall i \in \mathcal { V }$ are locally Lipschitz by Assumption 2 and feasible $\forall \mathbf { x } \in \mathbf { \partial }$ $\tilde { \mathcal { Z } } ( \mathbf { w } )$ by Lemma $3 , ( \mathbf { b } ) \mathbf { x } ( t _ { 0 } ) \in \tilde { \mathcal { Z } } ( \mathbf { w } )$ , and (c) $H _ { i } ( \mathbf { x } ) \neq 0$ $\forall \mathbf { x } \in \partial \tilde { \mathcal { Z } } _ { i } ( \mathbf { w } )$ , the controllers $u _ { i } ^ { * } ( \mathbf { x } ) \ \forall i \in \mathcal { V }$ together render $\tilde { \mathcal { Z } } ( \mathbf { w } )$ forward invariant.
