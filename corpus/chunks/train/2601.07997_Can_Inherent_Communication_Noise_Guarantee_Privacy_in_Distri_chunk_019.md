# 3.3 Privacy Analysis

In this subsection, the privacy-preserving performance of Algorithm 1 is analysed. To distinguish from (ǫ, δ) privacy defined over the infinite horizon, let us introduce $( \epsilon _ { t } , \delta _ { t } )$ as the instantaneous privacy parameters at step t. Here, $\delta _ { t }$ quantifies the probability that differential privacy fails and is chosen to balance the trade-off between privacy protection and control performance.

Theorem 2. Suppose Assumptions 1 and 2 hold, and $c ( t )$ and $\delta _ { t }$ are designed such that

$$c (t) > 0, c (t) \in \ell_ {2}, \delta_ {t} \in \ell_ {1}, c (t) \mathcal {Q} ^ {- 1} (\delta_ {t}) \in \ell_ {1}. \tag {20}$$

Algorithm 1 can ensure bounded $( \epsilon , \delta ) { \mathrm { - D P } }$ even as the number of iterations approaches infinity.

Proof. The privacy property of the LQR mechanism $\mathcal { M } _ { t }$ at time t is first analyzed. Let D and D′ be two adjacent databases as defined in Definition 1. The corresponding state vectors generated by the mechanism are $\mathcal M _ { t } ( \bar  D ) = \left[ x _ { 1 } ^ { \top } ( t + 1 ) , x _ { 2 } ^ { \top } ( t + 1 ) , \cdots , x _ { N } ^ { \top } ( t + 1 ) \right] ^ { \top }$ a nd $\mathcal { M } _ { t } ( D ^ { \prime } ) ~ = ~ \left[ x _ { 1 } ^ { \prime \top } ( t + 1 ) , x _ { 2 } ^ { \prime \top } ( t + 1 ) , \cdot \cdot \cdot , x _ { N } ^ { \prime \top } ( t + 1 ) \right] ^ { \top }$ . For any pair of adjacent databases, only the states of the two agents corresponding to the differing edge $( i , j ) = l _ { k }$ are affected, $. . . \bar { x } _ { q } ( t { + } 1 \bar { ) = } x _ { q } ^ { \prime } ( t { + } 1 )$ for all $q \neq i , j$ . Hence, the $\ell _ { 2 } { \mathrm { - s e n s i t i v i t y } }$ of $\mathcal { M } _ { t }$ is

$$
\begin{array}{l} \Delta_ {2, M, t} = \max _ {D, D ^ {\prime}} \| M _ {t} (D) - M _ {t} (D ^ {\prime}) \| \\ = \max _ {D, D ^ {\prime}} \sqrt {\| x _ {i , t + 1} - x _ {i , t + 1} ^ {\prime} \| ^ {2} + \| x _ {j , t + 1} - x _ {i , t + 1} ^ {\prime} \| ^ {2}} \\ \leq 2 c (t) \rho_ {K, t} \theta \\ \end{array}
$$

where $\rho _ { K , t } = \operatorname* { m a x } \{ \| K _ { 1 , t } \| , \dots , \| K _ { N , t } \| \}$ and θ is the adjacency bound defined in (7). Note that based on Lemma 2, the instantaneous privacy budget $\epsilon _ { t }$ of $\mathcal { M } _ { t }$ given the channel noise in (6) satisfies
