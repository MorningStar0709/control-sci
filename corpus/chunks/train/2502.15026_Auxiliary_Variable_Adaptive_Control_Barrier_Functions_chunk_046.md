Since these AVCBFs include two auxiliary functions, the above method is referred to as AVCBF-2. Based on Rem. 5, for simplicity, we remove the second auxiliary function $\boldsymbol { A } _ { 2 }$ , which means that (54) and (55) are no longer needed, and the third term in the cost function (57) is also eliminated. Since this approach includes only auxiliary function $\mathcal { A } _ { 1 }$ (with manually tuned parameters), we refer to it as AVCBF-1. Additionally, we use the parametrization method from Alg. 1 to tune the parameter $_ { a _ { 1 , w } }$ in AVCBF-1, and we denote this method as AVCBF-P. Other parameters are set as $a _ { 1 } ( 0 ) \ = \ a _ { 2 } ( 0 ) \ =$ $\pi _ { 1 , 2 } ( 0 ) = 0 . 1 , c _ { 3 } = 1 0 , W _ { 1 } = W _ { 2 } = 1 0 0 0 , Q = 1 0 ^ { 5 } , \epsilon _ { 1 } =$ $\epsilon _ { 2 } = 1 0 ^ { - 1 0 } , J _ { m } = 1 0 , N _ { c } = 8 , \varepsilon = 0 . 1 , \lambda = 1 0 , l _ { 1 , 1 } = l _ { 1 , 2 } =$ $l _ { 2 , 1 } = 0 . 1$ .

4) Implementation with HOCBFs: Similar to AVCBFs, we define $\bar { b ( } { \pmb x } ( t ) ) = ( { \ b x } - { \ b x } _ { o } ) ^ { 2 } + ( { \pmb y } - { \pmb y } _ { o } ) ^ { 2 } - r _ { o } ^ { 2 }$ for HOCBF candidate. The HOCBFs are then defined as

$$\psi_ {0} (\boldsymbol {x}) := b (\boldsymbol {x}),\psi_ {1} (\boldsymbol {x}) := \dot {\psi} _ {0} (\boldsymbol {x}) + k _ {1} \psi_ {0} (\boldsymbol {x}), \tag {58}\psi_ {2} (\boldsymbol {x}) := \dot {\psi} _ {1} (\boldsymbol {x}) + k _ {2} \psi_ {1} (\boldsymbol {x}),$$

where $\alpha _ { 1 } ( \cdot ) , \alpha _ { 2 } ( \cdot )$ are set as linear functions. By formulating constraints from HOCBFs (58), CLF (51) and control input (50), we can define cost function for QP the same as (9). Other parameters are set as $c _ { 3 } = 1 0 , Q = 1 0 ^ { 5 }$ .
