# 7.1.1 Discrete system stability

Eigenvalues of a system that are within the unit circle are stable. To demonstrate this, consider the discrete system $x _ { k + 1 } = a x _ { k }$ where a is a complex number. $| a | < 1$ will make $x _ { k + 1 }$ converge to zero.
