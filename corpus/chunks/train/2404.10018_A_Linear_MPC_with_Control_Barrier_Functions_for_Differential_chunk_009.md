is nonsingular at $x = x _ { 0 }$

Lemma 2. [32] The input-to-state feedback linearization of the system dynamics in (2) is solvable at $x _ { 0 }$ using the control

input $u = A ^ { - 1 } ( x ) \left( \left[ \begin{array} { c } { { \mathcal { L } _ { f } ^ { r _ { 1 } } h _ { 1 } ( x ) } } \\ { { \vdots } } \\ { { \mathcal { L } _ { f } ^ { r _ { m } } h _ { m } ( x ) } } \end{array} \right] + \left[ \begin{array} { c } { { \nu _ { 1 } } } \\ { { \vdots } } \\ { { \nu _ { m } } } \end{array} \right] \right)$ ,where ?? is an ????

«   external reference input to be defined, $\begin{array} { r } { i f \sum _ { r = 1 } ^ { m } r = n } \end{array}$ and the decoupling input matrix ??(??) in (3) is full rank.

Throughout this paper H (??) represents control barrier function with ${ \mathcal { H } } ( x ) : D \subset \mathbb { R } ^ { n } \to$ R describing a safety metric. The class of ${ \mathcal K } _ { \infty }$ extended function is donated by ℵ such that it is a continuous function with the mapping $[ 0 , \infty )  [ 0 , \infty )$ and $l i m _ { r  \infty } \aleph ( r ) = \infty$ .

Definition 1. Let $C = \{ x \in D \subset \mathbb { R } ^ { n } : \mathcal H ( x ) \geq 0 \}$ be the Safe set where $I n t ( C ) = \{ x \in D \subset \mathbb { R } ^ { n } : \mathcal H ( x ) > 0 \}$ describes the interior of the set and $\partial C = \{ x \in D \subset \mathbb { R } ^ { n } : \mathcal H ( x ) = 0 \}$ refers to the set boundary.
