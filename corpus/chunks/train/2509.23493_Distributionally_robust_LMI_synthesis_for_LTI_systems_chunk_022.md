in terms of the parameters $X ~ = ~ X ^ { \top } ~ \in ~ \mathbb { R } ^ { n _ { x } \times n _ { x } } , ~ Y ~ = ~ Y ^ { \top } ~ \in$ $\mathbb { R } ^ { n _ { x } \times n _ { x } } , K \in \mathbb { R } ^ { n _ { x } \times n _ { x } } , L \in \mathbb { R } ^ { n _ { x } \times n _ { y } } , M \in \mathbb { R } ^ { n _ { u } \times n _ { x } } , N \in \mathbb { R } ^ { n _ { u } \times n _ { y } }$ . These matrices shall be related to the controller parameters $A _ { c } , B _ { c } , C _ { c }$ , and $D _ { c }$ and the multiplier matrix $\mathcal { P }$ through the equations

$$
\left( \begin{array}{c c} \mathcal {T} ^ {\top} \mathcal {P A T} & \mathcal {T} ^ {\top} \mathcal {P B} \\ \mathcal {C T} & \mathcal {D} \end{array} \right) = \left( \begin{array}{c c} \boldsymbol {A} & \boldsymbol {B} \\ \boldsymbol {C} & \boldsymbol {D} \end{array} \right), \quad \mathcal {T} ^ {\top} \mathcal {P T} = \boldsymbol {P}. \tag {15}
$$

The following facts are true:

For any $\mathcal { P } \ \succ \ 0 , A _ { c } , B _ { c } , C _ { c } ,$ , and $D _ { c } ,$ there exist a congruence transformation via nonsingular matrix $\tau$ and parameters $( X , Y , K , L , M , N )$ such that (15) holds.   
For any parameters $( X , Y , K , L , M , N )$ with $P \ \succ \ 0 .$ , there exist a congruence transformation via nonsingular matrix $\tau ,$ , a matrix P, and controller parameters $A _ { c } , B _ { c }$ , $C _ { c } ,$ and $D _ { c }$ such that (15) holds.

With the aid of Theorem 6.3, we can easily extend the results of Proposition 6.1 and Proposition 6.2 to convex controller synthesis.

Theorem 6.4 (Robust synthesis 1): The optimal value of the optimization problem (3) for $\mathcal { W } = \mathcal { W } _ { \mathrm { c o r } }$ is equal to the optimal value of the convex program

$$
\begin{array}{l} \underset {\lambda \geq 0, Q, X, Y, K, L, M, N} {\text { minimize }} \quad \operatorname{tr} \left(Q \Sigma_ {\text { nom }}\right) + \lambda \gamma^ {2} \\ \text {s.t.} \left( \begin{array}{c c c c c} - \boldsymbol {P} & 0 & 0 & \boldsymbol {A} ^ {\top} & \boldsymbol {C} ^ {\top} \\ 0 & - \lambda I & \lambda I & \boldsymbol {B} ^ {\top} & \boldsymbol {D} ^ {\top} \\ 0 & \lambda I & - Q - \lambda I & 0 & 0 \\ \boldsymbol {A} & \boldsymbol {B} & 0 & - \boldsymbol {P} & 0 \\ \boldsymbol {C} & \boldsymbol {D} & 0 & 0 & - I \end{array} \right) <   0, \quad \boldsymbol {P} > 0. \\ \end{array}
$$
