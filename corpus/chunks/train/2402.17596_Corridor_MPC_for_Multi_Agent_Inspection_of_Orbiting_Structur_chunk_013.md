Given that at time instant $k \Delta t , \tilde { \mathbf { x } } ( k \Delta t ) \in \cap _ { r = 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) , \mathbf { x } ( k \Delta t ) \triangleq \tilde { \mathbf { x } } ( k \Delta t )$ and that the constant feedback control input $\pmb { u } ( k \Delta t ) \in \mathbb { U }$ satisfies

$$
\zeta (\boldsymbol {x} (k \Delta t), \boldsymbol {u} (k \Delta t), k \Delta t) - L _ {w} \Delta t - c _ {w} \epsilon_ {d} \geq 0 \tag {8}
$$

where $L _ { w }$ is defined over the set $\mathcal { Q } \triangleq \mathbb { D } \times \mathbb { U } \times \mathbb { W } \times \mathbb { I }$ as $L _ { w } = \underset { ( \tilde { \mathbf { x } } , \mathbf { u } , \delta \mathbf { d } , t ) \in \mathcal { Q } } { m a x } | \tilde { \zeta } ( \tilde { \mathbf { x } } , \mathbf { u } , \delta \mathbf { d } , t ) | ,$ ,, and $c _ { w }$ is defined as $c _ { w } = \underset { ( \tilde { \pmb { x } } , t ) \in \mathbb { D } \times \mathbb { I } } { m a x } | | \mathcal { L } _ { g } H _ { r - 1 } ( \tilde { \pmb { x } } , t ) | |$ ,. Then it holds that $\tilde { \pmb { x } } ( k \Delta t ) \in$ (x˜,t)∈D×I $\cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( k \Delta t ) \Rightarrow \tilde { \mathbf { x } } ( t ) \in \cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) , \forall t \in [ k \Delta t , ( k + 1 ) \Delta t ] .$ .

Proof.
