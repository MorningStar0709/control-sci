The usage of a gradient ascent scheme on the free energy curve for the estimation of s is motivated by the proof for the existence of a unique maximum for F under practical bounds as follows.

Proposition V.1. The free energy F defined by Equation 9 has a unique maximum with respect to noise smoothness s, under the practical range of noise smoothness $( 0 < s < 1 )$ and sampling time $( d t < s )$ .

Proof. Consider all the smoothness values of s with zero free energy gradient s ( ∂ F | $\big ( \frac { \partial F } { \partial s } | _ { s = s _ { o } } = 0 \big )$ s=so . Substituting Equation 11 in 10 and using $\frac { \partial F } { \partial s } | _ { s = s _ { o } } = 0$ yields the condition satisfied by all maximum and minimum points:

$$\frac {1}{2} (\tilde {\varepsilon} ^ {T} \tilde {\Pi} _ {s} \tilde {\varepsilon}) | _ {s = s _ {o}} = \frac {2 1 (n + m)}{s _ {o}} - s _ {o} \tag {12}$$

where we use the shorthand $\begin{array} { r } { \tilde { \Pi } _ { s } = \frac { \partial \tilde { \Pi } } { \partial s } } \end{array}$ . Since $0 < s < 1$ , we have from Equation 12 that:

$$\left. \left(\tilde {\varepsilon} ^ {T} \tilde {\Pi} _ {s} \tilde {\varepsilon}\right) \right| _ {s = s _ {o}} > 0. \tag {13}$$

The proof for the existence of a unique maximum is complete if we prove that $\frac { \partial ^ { 2 } F } { \partial s ^ { 2 } } | _ { s = s _ { o } } < 0 .$ ∂ s2 |s=so , for all $s _ { o }$ satisfying Equation 13. The curvature of F at $s = s _ { o }$ is calculated from Equation 10 using Equation 11 as:

$$\left. F _ {s s} \right| _ {s = s _ {o}} = - \frac {1}{2} \left(\left(\tilde {\varepsilon} ^ {T} \tilde {\Pi} _ {s s} \tilde {\varepsilon}\right) | _ {s = s _ {o}} + 4 2 (n + m) \frac {1}{s _ {o} ^ {2}} + 1\right) \tag {14}$$

Since $\tilde { \Pi } \succ 0 ,$ from definition $\tilde { \varepsilon } ^ { T } \tilde { \Pi } \tilde { \varepsilon } > 0 .$ , and since $\big ( \tilde { \varepsilon } ^ { T } \tilde { \Pi } _ { s } \tilde { \varepsilon } \big ) \big | _ { s = s _ { o } } > 0$ , we can conclude that $( \tilde { \varepsilon } ^ { T } \tilde { \Pi } _ { s s } \tilde { \varepsilon } ) | _ { s = s _ { o } } > 0 .$ , even though $\tilde { \Pi } _ { s } \ * \forall $ and $\tilde { \Pi } _ { s s } \nsucc 0$ (refer Appendix B for numerical analysis). From Equation 14, $( \bar { \varepsilon } ^ { \bar { T } } \tilde { \Pi } _ { s s } \tilde { \varepsilon } ) | _ { s = s _ { o } } >$ $0 \implies F _ { s s } | _ { s = s _ { o } } < 0$ , completing the proof for the existence of a unique maximum of free energy at $s = s _ { o }$ . □
