# C.2 Proof of Lemma 5

proof. The main tool we will use for this proof is the following lemma that bounds the difference in future states in three different cases.

Lemma 6. For alarge T , suppose $x , y ,$ $d = | y - x |$ . Innd nd for sufficiently. Then for some $\textstyle \theta \ \in \ \Theta , \ K \ \in \ \left[ { \frac { a - 1 } { b } } , { \frac { a } { b } } \right]$ $\begin{array} { r } { \| { \boldsymbol { \theta } } - { \boldsymbol { \theta } } ^ { * } \| _ { \infty } = { \epsilon } \leq \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$ $\rho : = | a ^ { * } - b ^ { * } K | + O _ { T } ( \epsilon )$ ,

$$
| a ^ {*} x + b ^ {*} C _ {K} ^ {\theta} (x) - a ^ {*} y - b ^ {*} C _ {K} ^ {\theta} (y) | \leq \left\{ \begin{array}{l l} \min {(2 \rho d, \rho d + O _ {T} (\epsilon) (| x | + \| D \| _ {\infty}))} & i f \mathcal {Z} \\ O _ {T} (\epsilon) d & i f \mathcal {W} \\ \rho d & o t h e r w i s e \end{array} \right. \tag {12}
\mathcal {Z} := \left\{\min (x, y) \leq \frac {D _ {L}}{a - b K} \leq \max (x, y) \leq \frac {D _ {U}}{a - b K} o r \frac {D _ {L}}{a - b K} \leq \min (x, y) \leq \frac {D _ {U}}{a - b K} \leq \max (x, y) \right\}\mathcal {W} := \left\{\max (x, y) \leq \frac {D _ {L}}{a - b K} o r \frac {D _ {U}}{a - b K} \leq \min (x, y) \right\}.$$

The proof of Lemma 6 can be found in Appendix C.3.

The rest of this proof will be structured as follows. First, we will introduce some additional definitions that we will use to construct event $E _ { \mathrm { L 5 } } ( x , K , \theta , W ^ { \prime } )$ . Then, we will prove Lemma 5 in two cases.

Define $x _ { 0 } , x _ { 1 } , . . . , x _ { T }$ as the sequence of states when starting at state $x _ { 0 } = x$ and using controller $C _ { K } ^ { \theta }$ with noise $W ^ { \prime }$ . For $i \leq t$ , define the event

$$X (i, x, K, \theta , W ^ {\prime}) := \left\{\min \left(\left| x _ {i} - \frac {D _ {L}}{a - b K} \right|, \left| x _ {i} - \frac {D _ {U}}{a - b K} \right|\right) \leq \frac {3}{\log^ {1 0} (T)} \right\}.$$

Note that whether the event $X ( i , x , K , \theta , W ^ { \prime } )$ occurs depends on $w _ { 0 } , . . . , w _ { i - 1 }$ . For $0 \le j \le t$ and $x \in \mathbb { R }$ , define the event $H ( j , x , K , \theta , W ^ { \prime } )$ as

$$H (j, x, K, \theta , W ^ {\prime}) := \left\{| \{0 \leq i \leq j: X (i, x, K \theta , W ^ {\prime}) \} | \leq \log^ {2 3} (T) + \frac {2 4 B _ {P} j}{\log^ {1 0} (T)} \right\}.$$

Define

$$E ^ {*} (x, K, \theta , W ^ {\prime}) := \bigcap_ {0 \leq j \leq t} H (j, x, K, \theta , W ^ {\prime}).$$
