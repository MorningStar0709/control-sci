Assumption 3: For each $\zeta ~ > ~ 0$ there exists $w _ { \zeta } ^ { \# } \ \in \ \mathbb { W }$ $\mathrm { s . t . } \zeta L ( x ) = \hat { J } ^ { w _ { \zeta } ^ { \# } } ( x )$ , for all $x \in \mathbb { X }$ .

Note that if $\zeta > 0$ then $\zeta L ( x )$ is also a control-Lyapunov function that corresponds to the same stabilizing policy as $L ( x )$ .

Remark 4: The simplest way to satisfy Assumption 3 is to choose $\hat { J } ^ { w } ( x ) = w _ { 0 } \varphi ( x , w _ { [ 2 : N _ { c } + 1 ] } ) + w _ { 1 } L ( x )$ , where $\varphi$ is a (deep) neural network with some $\dot { N } _ { c }$ weights $\boldsymbol { w } _ { [ 2 : N _ { c } + 1 ] } .$ , and $w _ { 0 } , w _ { 1 } \in [ 0 , \infty )$ .

Remark 5: Assumption 3 means that the critic structure be rich enough so as to capture the structure of the Lyapunov function as per Assumption 1. We use it in such an exact format only for brevity. A relaxed form with some discrepancy between $\hat { J } ^ { w _ { \zeta } ^ { \# } } ( x )$ and $\zeta L ( x )$ would also do the job, one would only to account for it in the constraints $( \mathbf { C } 1 ) \mathbf { - } ( \mathbf { C } 4 ) . \ \mathbf { A }$ weaker, local condition of the kind ∀ compact $\mathbb { X } ^ { \prime } \exists w _ { \zeta } ^ { \# }$ ∀x $\in$ $\mathbb { X } ^ { \prime } \ \hat { J } ^ { w _ { \zeta } ^ { \# } } ( x ) = \zeta L ( x )$ would also fit, whereas one would just need to determine such an $\mathbb { X } ^ { \prime }$ that the current and predicted state lie in. Again, we choose the simplest form for the ease of exposition.

Assumption 4: The critic $\hat { J } ^ { w } ( \cdot )$ is locally Lipschitz continuous for every $w \in \mathbb { W } .$ . Thus the notation Lip ˆ implies $\forall w \in \mathbb { W } | \hat { J } ^ { w } ( x ) - \hat { J } ^ { w } ( x ^ { \prime } ) | \leq \mathrm { L i p } _ { \hat { J } } \| x - x ^ { \prime } \|$ . Though, since ${ \hat { J } } ( \cdot )$ is locally Lipschitz continuous, its Lipschitz constant $\mathrm { L i p } _ { \hat { J } }$ is ambiguous until a bounded domain is specified.
