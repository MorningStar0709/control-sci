# B. Prescribed-time coordination control with disturbances

In this subsection, a novel prescribed-time sliding mode control protocol is developed to settle the robust PTCC problems of the CAN (2). The following sliding variables are proposed:

$$
\begin{array}{l} \sigma_ {k} = x _ {k} + \varsigma_ {k}, \\ \therefore \quad n o m, \quad 1, \quad \angle \end{array} \tag {46}
\dot {\varsigma} _ {k} = - u _ {k} ^ {n o m}, \quad k \in \mathcal {I} _ {N},$$

with $\begin{array} { r } { u _ { k } ^ { n o m } = \left( \rho _ { 1 } + \rho _ { 2 } \frac { \dot { \varphi } ( t , T ) } { \varphi ( t , T ) } \right) \sum _ { l = 1 } ^ { N } w _ { k l } \left[ x _ { l } - \mathrm { s i g n } \left( w _ { k l } \right) x _ { k } \right] } \end{array}$ ρ1 + ρ2 ϕ˙ (t,T )ϕ(t,T )  , where $T = T _ { r } + T _ { s }$ with $\smash { \vec { T _ { r } } > 0 }$ and $T _ { s } ~ > ~ 0 .$ , and other variables are as stated previously. The sliding mode control protocol is designed as

$$u _ {k} = u _ {k} ^ {\text { dis }} + u _ {k} ^ {\text { nom }}, \quad k \in \mathscr {I} _ {N}, \tag {47}$$

where $\begin{array} { r } { u _ { k } ^ { d i s } = - \mu _ { 1 } \mathrm { s i g n } \left( \sigma _ { k } \right) - \left( \mu _ { 2 } + \mu _ { 3 } \frac { \dot { \varphi } ( t , T ) } { \varphi ( t , T ) } \right) \sigma _ { k } } \end{array}$ with $T =$ $T _ { r } , \mu _ { 1 } > \delta , \mu _ { 2 } > 0$ , and $\mu _ { 3 } > 0$ .

Remark 5. The first component of $u _ { k } ^ { d i s }$ is designed for compensating the bounded perturbation $d _ { k } ,$ , while the second term $\begin{array} { r } { - \left( \mu _ { 2 } + \mu _ { 3 } \frac { \dot { \varphi } ( t , T _ { r } ) } { \varphi ( t , T _ { r } ) } \right) \sigma _ { k } } \end{array}$ is responsible for driving the sliding variables to zero in the prescribed finite time $T _ { r }$ .

The following proposition shows that the sliding mode control protocol (47) can drive the states of all agents in (2) with any initial values towards the sliding manifold ${ \mathcal { S } } \triangleq$ $\left\{ \left( x _ { 1 } , \ldots , x _ { N } \right) \big | \sigma _ { k } = 0 , k \in \mathcal { I } _ { N } \right\}$ in the prescribed finite time $T _ { r }$ and keeps them on it thereafter in spite of the disturbances $d _ { k } , k \in \mathcal I _ { N }$ .

Proposition 1. Consider the CAN (2) under the sliding mode control protocol (47) with its sliding variables as designed in (46). Then, the closed-loop CAN (2) can reach the sliding manifold S in the prescribed finite time $T _ { r }$ for arbitrary initial conditions.

Proof. Using (2), (46), and (47), one can obtain
