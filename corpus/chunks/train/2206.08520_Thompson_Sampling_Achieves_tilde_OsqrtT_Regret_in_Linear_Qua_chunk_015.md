# 4. Theoretical Analysis

In this section, we study the theoretical guarantees of TSAC. The following states the first orderoptimal frequentist regret bound for TS in multidimensional stabilizable LQRs, our main result.

Theorem 3 (Regret of TSAC) Suppose Assumptions 1 and 2 hold and set $\tau _ { 0 } = 2 \gamma ^ { - 1 } \log ( 2 \kappa \sqrt { 2 } )$ and $T _ { 0 } = \mathrm { p o l y } ( \log ( 1 / \delta ) , \sigma _ { w } ^ { - 1 } , n , d , \bar { \alpha } , \gamma ^ { - 1 } , \kappa )$ . Then, for long enough T , TSAC achieves the regret $R _ { T } = \tilde { O } \left( ( n + d ) ^ { ( n + d ) } \sqrt { T \log ( 1 / \delta ) } \right)$ w.p. at least 1−10δ, if Tw = max $\Big ( T _ { 0 } , c _ { 1 } ( \sqrt { T } \log T ) ^ { 1 + o ( 1 ) } \Big )$ for a constant $c _ { 1 } > 0$ . Furthermore, if the closed loop matrix of the optimally controlled underlying system, $A _ { c , * } : = A _ { * } + B _ { * } K _ { * }$ , is non-singular, w.p. at least 1 − 10δ, TSAC achieves the regret $R _ { T } = \tilde { O } \left( \mathrm { p o l y } ( n , d ) \sqrt { T \log ( 1 / \delta ) } \right) i f T _ { w } = \operatorname* { m a x } \left( T _ { 0 } , { c } _ { 2 } ( \log T ) ^ { 1 + o ( 1 ) } \right)$ for a constant $c _ { 2 } > 0 .$ .
