$$\hat {\mathbf {g}} (a _ {k - 1}) = \hat {\mathbf {x}} _ {k} - \mathbf {f} (\hat {\mathbf {x}} _ {k - 1}, \mathbf {u} _ {k - 1}). \tag {43}$$

Here, $\hat { \mathbf { x } } _ { k }$ and $\hat { \mathbf { x } } _ { k - 1 }$ represent the robot poses from the on-board navigation system at time ?? and $k - 1$ respectively, $\mathbf { u } _ { k - 1 }$ is the control input at time ?? − 1. In the disturbance query state $a _ { k - 1 } = \left( \hat { \mathbf { x } } _ { k - 1 } , \hat { \mathbf { v } } _ { k - 2 } , \mathbf { u } _ { k - 1 } , \mathbf { u } _ { k - 2 } \right)$ , the $\hat { \mathbf { v } } _ { k - 2 }$ is calculated by the estimated robot poses $\hat { \mathbf { x } } _ { k - 1 }$ and $\hat { \mathbf { x } } _ { k - 2 }$ which indicates the disturbance query state $a _ { k }$ requires historic states data. In one experiment for data collection, the observed disturbance datasets are collected and prepared by following (43) as:

![](image/91b3e81ccf65022d3deb2d7cdcac5c30b55a984b4adc527e26ca52c6f5faf98a.jpg)  
Fig. 6. Illustration of the trained GP models for the robot pose variables ??, ??, and $\theta ( d ^ { x } ( \mathbf { a } ) , d ^ { y } ( \mathbf { a } )$ , and $d ^ { \theta } ( \mathbf { a } )$ respectively), based on a 60 m field experiment dataset (44). These models, developed under the assumption of uncorrelated disturbances, demonstrate the application of (12) in creating separate GP models for each pose variable [24].

$$
\begin{array}{l} \mathcal {D} _ {m} ^ {x} = \left\{\mathbf {a} = \left[ a _ {1}, \dots , a _ {k - 1}, \dots , a _ {m} \right] ^ {\top}, \mathbf {g} ^ {x} (\mathbf {a}) = \left[ g ^ {x} (a _ {1}), \dots , g ^ {x} (a _ {k}), \dots , g ^ {x} (a _ {m}) \right] ^ {\top} \right\}, \\ \mathcal {D} _ {m} ^ {y} = \left\{\mathbf {a} = \left[ a _ {1}, \dots , a _ {k - 1}, \dots , a _ {m} \right] ^ {\top}, \mathbf {g} ^ {y} (\mathbf {a}) = \left[ g ^ {y} (a _ {1}), \dots , g ^ {y} (a _ {k}), \dots , g ^ {y} (a _ {m}) \right] ^ {\top} \right\}, \\ \mathcal {D} _ {m} ^ {\theta} = \left\{\mathbf {a} = \left[ a _ {1}, \dots , a _ {k - 1}, \dots , a _ {m} \right] ^ {\top}, \mathbf {g} ^ {\theta} (\mathbf {a}) = \left[ g ^ {\theta} (a _ {1}), \dots , g ^ {\theta} (a _ {k}), \dots , g ^ {\theta} (a _ {m}) \right] ^ {\top} \right\}, \tag {44} \\ \end{array}
$$
