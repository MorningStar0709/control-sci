# A. Technical preliminaries

Due to the SH nature and given a sampling period $\delta ,$ feasibility of (18) cannot be ensured inside a small enough vicinity of the origin, in general. Let $B _ { r }$ ∗ denote a core ball for some $0 < r ^ { * } < r .$ which can be used to tackle the aforementioned. That is, once $x _ { k } \in B _ { r ^ { * } }$ , the setting $\left\{ u _ { k } , w _ { k } \right\} =$ $\{ \mu ( x _ { k } , \hat { \theta } _ { k } ) , w ^ { \# } \}$ is used, where $w ^ { \# }$ is the adaptive control Lyapunov function recovering weight of Assumption 2.

The following Lipschitz condition is satisfied for the system $\mathcal { F }$ due to differentiability: for any $0 < r < \infty$ , any compact Input: pair $( V , \nu )$ as per Assumption 1, balls $( B _ { R } , B _ { r } )$ , stage cost r, initial parameter guess $ { \hat { \theta } } _ { 0 }$ , weigth matrix Γ, initial state $x _ { 0 } \in B _ { R } .$ , membership set Θ;
