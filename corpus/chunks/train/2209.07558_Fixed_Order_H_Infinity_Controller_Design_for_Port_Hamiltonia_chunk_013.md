# 5. Our Approach: Structured Optimization-Based Synthesis

Our approach for pH $\mathcal { H } _ { \infty }$ synthesis is an adaptation of the model order reduction method developed in Schwerdtner and Voigt (2020, 2021). Following a similar strategy, we only make use of samples of the closedloop transfer function to avoid the computation of the $\mathcal { H } _ { \infty }$ norm of $P \star K$ and impose the $\mathrm { p H }$ structure of the controller directly in our parameterization such that no constraints have to be enforced during optimization. The parameterization is described in Lemma 1 and our optimization method is given in detail in Algorithm 1. We call our method SOBSYN (Structured Optimization-Based SYNthesis).

Lemma 1. [Schwerdtner and Voigt (2020)] Let $\theta \in \mathbb { R } ^ { n _ { \theta } }$ be a parameter vector that is partitioned $\begin{array} { r l } { a s } & { { } \theta = \big [ \theta _ { J } ^ { \mathsf { T } } , \theta _ { W } ^ { \mathsf { T } } , \theta _ { G } ^ { \mathsf { T } } , \theta _ { Q } ^ { \mathsf { T } } , \theta _ { N } ^ { \mathsf { T } } \big ] ^ { \mathsf { T } } , \qquad w i t h \ \quad \theta _ { J } \in \mathbb { R } ^ { k ( k - 1 ) / 2 } } \end{array}$ , $\theta _ { W } \in \mathbb { R } ^ { ( k + p _ { 2 } ) ( k + p _ { 2 } + 1 ) / 2 } , \theta _ { Q } \in \mathbb { R } ^ { k ( k + 1 ) / 2 } , \theta _ { G } \in \mathbb { R } ^ { k p _ { 2 } }$ , and $\theta _ { N } \in \mathbb { R } ^ { p _ { 2 } ( p _ { 2 } - 1 ) }$ . We define the matrices

$$J (\theta) = \operatorname{vtsu} \left(\theta_ {J}\right) ^ {\mathsf {T}} - \operatorname{vtsu} \left(\theta_ {J}\right), \tag {8a}W (\theta) = \mathrm{vtu} \left(\theta_ {W}\right) ^ {\mathsf {T}} \mathrm{vtu} (\theta_ {W}), \tag {8b}Q (\theta) = \mathrm{vtu} \left(\theta_ {Q}\right) ^ {\mathsf {T}} \mathrm{vtu} (\theta_ {Q}), \tag {8c}G (\theta) = \operatorname{vtf} _ {k, m _ {2}} \left(\theta_ {G}\right), \tag {8d}N (\theta) = \operatorname{vtsu} \left(\theta_ {N}\right) ^ {\mathsf {T}} - \operatorname{vtsu} \left(\theta_ {N}\right), \tag {8e}$$

where th $\begin{array} { r c c c c c l } { { } } & { { \it f u n c t i o n s } } & { { \mathrm { v t u } } } & { { : } } & { { \mathbb { R } ^ { k ( k + 1 ) / 2 } } } & { {  } } & { { \mathbb { R } ^ { k \times k } } } & { { \it \Delta / o r  } } \end{array}$ vtu $: \quad \mathbb { R } ^ { ( \check { k } + p _ { 2 } ) ( k + p _ { 2 } + 1 ) / 2 } \quad \to \quad \mathbb { R } ^ { ( k + p _ { 2 } ) \times ( k + p _ { 2 } ) } \boldsymbol { J } \quad ( r e s p .$
