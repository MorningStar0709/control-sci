Remark 7. By combining Theorem 1 and the PL property of the cost in (11a), we immediately obtain a sample complexity for our algorithm; e.g., choose $\gamma = 1 / 2$ , then in order to guarantee an ε error on the cost $J ( L _ { k } ) - J ( L ^ { * } ) \leq \varepsilon$ , it suffices to have a small enough bias term $\begin{array} { r } { s _ { 0 } \leq \frac { \sqrt { c _ { 1 } ( \alpha ) \varepsilon } } { 4 } } \end{array}$ and variance coefficient $\begin{array} { r } { s \leq \frac { 1 } { 4 } } \end{array}$ for the oracle, and run the SGD algorithm for $\begin{array} { r } { k > \ln ( \frac { \varepsilon } { \alpha } ) / \ln ( 1 - \frac { c _ { 1 } ( \alpha ) } { 1 8 \ell ( \alpha ) } ) ) } \end{array}$ steps.
