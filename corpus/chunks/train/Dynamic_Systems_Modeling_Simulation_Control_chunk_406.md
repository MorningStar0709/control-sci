# 7.14 Given the system I/O equation

$$2 \ddot {y} + 8 \dot {y} + 6 y = 3 u$$

a. Compute the characteristic roots.   
b. Compute the poles of the transfer function.   
c. Compute the eigenvalues of the system matrix from an SSR.   
d. Qualitatively describe the complete system response when initial conditions are $y ( 0 ) = 5 , { \dot { y } } ( 0 ) = 0 .$ , and u(t) = 0 (no input).
