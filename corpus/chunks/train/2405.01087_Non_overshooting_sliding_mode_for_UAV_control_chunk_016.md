1) The first subsystem (with robust non-overshooting reachability): its gain is bounded; it makes $e _ { 1 } \left( t \right)$ reach the given range $| e _ { 1 } ( t ) | \le e _ { 1 c }$ without overshoot, and $e _ { 2 } \left( t \right)$ approaches to the assigned $- e _ { 2 c } \mathrm { s i g n } ( e _ { 1 } \left( t \right) )$ which is opposite to $e _ { 1 } \left( t \right)$ ; the bounded disturbance $d ( t )$ can be rejected completely; and then the first subsystem switches to the second subsystem, which it has the initial values $e _ { 1 } \left( t _ { c } \right)$ and $e _ { 2 } \left( t _ { c } \right)$ .   
2) The second subsystem (with robust non-overshooting $s t a b i l i t y )$ : its bounded gain is determined from the initial values $e _ { 1 } \left( t _ { c } \right)$ and $e _ { 2 } \left( t _ { c } \right)$ ; the sliding variables $e _ { 1 } \left( t \right)$ and $e _ { 2 } \left( t \right)$ converge to zero, and no overshoot exists for $e _ { 1 } \left( t \right)$ ; moreover, the bounded disturbance $d ( t )$ can be rejected completely.

Two Theorems on the explicit forms of robust non-overshooting 2-sliding mode are presented as follows.
