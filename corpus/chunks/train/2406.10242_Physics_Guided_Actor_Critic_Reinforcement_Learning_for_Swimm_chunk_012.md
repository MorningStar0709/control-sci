where ${ \pmb W } ( t ^ { \prime } ; t ) ~ \in ~ \mathbb { R } ^ { d \times d }$ , with $\pmb { W } ( t ; t ) ~ = ~ \mathbb { I } _ { d \times d } .$ , representing the time-ordered exponential of $\pmb { \sigma } ( t )$ , denoted as $\begin{array} { r } { W ( t ^ { \prime } ; t ) = \mathcal { T } \big [ \exp \big ( \int _ { t } ^ { t ^ { \prime } } \mathrm { d } t ^ { \prime \prime } \pmb \sigma ( t ^ { \prime \prime } ) \big ) \big ] } \end{array}$ .

According to the Oseledets theorem $[ 2 5 - 2 7 ]$ , at sufficiently large times, the matrix log $W ^ { \top } ( t ^ { \prime } ; t ) \mathbf { \bar { W } } ( t ^ { \prime } ; t ) )$ $/ \left( t ^ { \prime } - t \right)$ stabilizes, with eigenvectors tending to d fixed orthonormal eigenvectors, $f _ { i }$ , and the eigenvalues $\lambda _ { i } ( t ^ { \prime } ; t ) ~ = ~ \log | W ( t ^ { \prime } ; t ) { \bf f } _ { i } | / ( t ^ { \prime } - t )$ stabilizing to their mean values. The finite-time statistics of $\lambda _ { i }$ is given by:

$$P \left(\lambda_ {1} \left(t ^ {\prime}; t\right), \dots , \lambda_ {d} \left(t ^ {\prime}; t\right) \mid t ^ {\prime} - t\right) \tag {9}\propto \exp \left[ - (t ^ {\prime} - t) S \big (\lambda_ {1} (t ^ {\prime}; t), \dots , \lambda_ {d} (t ^ {\prime}; t) \big) \right],$$

where $S ( \cdot )$ , called the Cram´er function, is convex, and the Lyapunov exponents are ordered: $\lambda _ { 1 } \geq \lambda _ { 2 } \geq \cdot \cdot \cdot \geq \lambda _ { d } .$ Of particular interest are the finite-time statistics of the leading Lyapunov exponent:

$$P (\lambda_ {1} (t ^ {\prime}; t) \mid t ^ {\prime} - t) \propto \exp \Big [ - (t ^ {\prime} - t) S _ {1} (\lambda_ {1} (t ^ {\prime}; t)) \Big ]. \tag {10}$$
