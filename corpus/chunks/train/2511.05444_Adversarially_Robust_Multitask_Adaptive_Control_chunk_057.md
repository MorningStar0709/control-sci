$$\| \widehat {\Theta} _ {N} ^ {(i)} - \Theta_ {\star} ^ {(i)} \| _ {F} \leq \rho^ {N} \| \widehat {\Theta} _ {0} ^ {(i)} - \Theta_ {\star} ^ {(i)} \| _ {F} + \frac {C _ {\mathrm{stat}} \sigma_ {w} \sqrt {(d _ {x} ^ {2} + d _ {x} d _ {u}) \log (1 / \delta)}}{\sigma_ {k} \sqrt {M _ {\hat {j}} \tau_ {k , 1}}} + C _ {\mathrm{mis,1}} \exp \left(- C _ {\mathrm{mis,2}} \sigma_ {k} ^ {2} \tau_ {k}\right),$$

with $\rho = 1 - \eta$ . The proof is complete by setting N ≥ log $\left( C _ { \alpha } \Delta _ { \operatorname* { m i n } } \tau _ { 1 } ^ { 2 } \right) / \log ( 1 / \rho )$ , and noting that for a sufficiently large initial epoch length, $M _ { \hat { i } } \approx M _ { j }$ , which guarantees that the systems are correctly classified and the error bound decays with the true total number of systems inside the cluster. Finally, we also note that $\tau _ { k , 1 } = \mathcal { O } ( \tau _ { k } )$ . In addition, we omit the contraction term of order $\mathcal { O } ( 1 / \tau _ { k } ^ { 2 } )$ by setting the total number of iterations N as before, since this term becomes negligible in the subsequent regret analysis. □
