At time $t _ { 0 } ,$ the neural network is initialized with random weights on both machines, and for a given x as input, φ0(x) denote the output of the last activation layer at $t _ { 0 } .$ Let $( t _ { j } ) _ { j \in \mathbb { Z } _ { + } }$ + denote the instants when the weights of hidden layers are updated on main machine after the completion of the $j ^ { \mathrm { t h } }$ training. Let $\phi _ { j } ( x )$ be the output of the last activation layer after the $j ^ { \mathrm { t h } }$ training for a given x as input. We can use bounded neurons in the last activation layer, which results in bounded $\phi ^ { * }$ and $\phi _ { j }$ . Due to the universal approximation property of DNN, $\phi ^ { * }$ exists with bounded $\varepsilon ^ { * }$ . We can assume that there exists $\pmb { \varepsilon } _ { \phi _ { i } } : \mathbb { R } ^ { d }  \mathbb { R } ^ { m }$ for each $\phi _ { j }$ such that $\phi ^ { * } ( x ) = \phi _ { j } ( x ) + \varepsilon _ { \phi _ { j } } ( x )$ for each $x \in \mathcal X$ . The boundedness of both $\phi ^ { * }$ and $\phi _ { j }$ ensures boundedness of $\varepsilon _ { \phi _ { j } }$ j . We need not to compute their bounds for the controller design. For $t \in \{ t _ { j } , t _ { j } + 1 , \dots , t _ { j + 1 } - 1 \}$ , (17) becomes

$$h (x _ {t}) = W ^ {* \top} \phi_ {j} (x _ {t}) + \varepsilon_ {j} (x _ {t}), \tag {18}$$

where $\pmb { \varepsilon } _ { j } ( x _ { t } ) = \pmb { \varepsilon } ^ { * } ( x _ { t } ) + W ^ { * } ^ { \top } \pmb { \varepsilon } _ { \phi _ { j } } ( x _ { t } )$ is the overall reconstruction error.

Notice that even when the weights of hidden layers are randomly assigned as in ELM [49], the universal approximation property of the neural network allows us to make the overall reconstruction error $\varepsilon _ { 0 } ( \cdot )$ as small as desired by increasing the width of the network. However, a network with trained hidden layers can capture several useful features, which in turn results in performance improvement [32].

![](image/b507bdf7c6c70b44f7cc416414138e991dc0e548e0e8e19d1fd92aff931a74c5.jpg)

<details>
<summary>flowchart</summary>
