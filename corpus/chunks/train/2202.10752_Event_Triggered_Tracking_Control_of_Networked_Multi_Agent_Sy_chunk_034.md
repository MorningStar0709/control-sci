Remark 5: From Theorem 1, the conservatism is from the ISS gains $\varphi _ { 1 } , \varphi _ { 2 }$ in (37). Since the ZOH mechanism is applied, the bounds on $\left\| e _ { \mathrm { f } } \right\| _ { ( t , j ) } , \left\| e _ { \mathrm { r } } \right\| _ { ( t , j ) }$ can be obtained via a step-bystep sampling approach [23], and the upper bounds on ϕ1, ϕ2 can be derived. From [16], [17], ϕ1, ϕ2 can be the functions of the MASP, the MAD and $\varepsilon _ { i } .$ . Since $\varepsilon _ { i }$ is from the hardware constraints of the networks, the effects of $\varepsilon _ { i }$ are inevitable but can be limited by choosing appropriate networks, thereby leading to the lower bounds on $\varphi _ { 1 } , \varphi _ { 2 }$ . 

Next, the special case of a single network is addressed, that is, $N = 1$ and only a common network is implemented for all agents. The following theorem shows how to ensure the tracking performance under the designed centralized ETM.

Theorem 2: Consider the system (18) and let Assumptions 1-3 hold. If the following holds:

i) for all $e \in \mathbb { R } ^ { n _ { e } } \times \mathbb { R } ^ { l } \times \mathbb { R } ^ { n _ { e } } \times \mathbb { R } ^ { l } \times \mathbb { N } \times \{ 0 , 1 \}$ ,

$$\alpha_ {1 W} (| e _ {\mathrm{a}} |) \leq W (e, m, \kappa , b) \leq \alpha_ {2 W} (| e |), \tag {38}
\begin{array}{l} W (e, h (\kappa , e) - e, \kappa + 1, 1) \\ \leq \lambda W (e, m, \kappa , 0) + \alpha_ {3 W} (| e _ {\mathrm{f}} |) + \alpha_ {4 W} (| e _ {\mathrm{r}} |), \tag {39} \\ \end{array}
W (e + m, - e - m, \kappa , 0)\leq W (e, m, \kappa , 1) + \alpha_ {5 W} (| e _ {\mathrm{f}} |) + \alpha_ {6 W} (| e _ {\mathrm{r}} |). \tag {40}$$

ii) for all $( x , \kappa ) \in \mathbb { R } ^ { n _ { x } } \times$ N and almost all $e \in \mathbb { R } ^ { n _ { e } }$ ,

$$\left\langle \frac {\partial W (e , m , \kappa , b)}{\partial e}, g _ {e} (\delta , x, e, w) \right\rangle \leq L _ {b} W (e, m, \kappa , b)+ H (x, e) + \sigma_ {1 b} (| e _ {\mathrm{f}} |) + \sigma_ {2 b} (| e _ {\mathrm{r}} |) + \sigma_ {3 b} (| w |); \tag {41}$$

iii) for all $( e , m , \kappa ) \in \mathbb { R } ^ { n _ { e } } \times \mathbb { R } ^ { n _ { e } } \times$ N and almost all $x \in \mathbb { R } ^ { n _ { x } }$ ,

$$\langle \nabla V (x), f (\delta , x, e, w) \rangle \leq - \mu V (x) - H _ {b} ^ {2} (x, e)+ (\gamma_ {b} ^ {2} - \theta) W ^ {2} (e, m, \kappa , b) + \zeta_ {1 b} (| e _ {\mathrm{f}} |)+ \zeta_ {2 b} (| e _ {\mathrm{r}} |) + \zeta_ {3 b} (| w |); \tag {42}$$

iv) consider the following equation

$$\dot {\phi} _ {b} = - 2 L _ {b} \phi_ {b} - \gamma_ {b} [ (1 + \varrho_ {b}) \phi_ {b} ^ {2} + 1 ], \quad b \in \{0, 1 \}, \tag {43}$$
