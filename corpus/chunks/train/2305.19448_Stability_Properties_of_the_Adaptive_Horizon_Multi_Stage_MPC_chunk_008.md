where $z _ { i } ^ { c } , \nu _ { i } ^ { c }$ , and $d _ { i } ^ { c }$ represent the state, input, and disturbance vectors at stage i and scenario c. The set C is the , νscenario index set, and the nominal scenario has a zero index. The objective function (3a) is the weighted sum of the stage and terminal costs across all the scenarios with $\omega _ { c }$ being the probability for each scenario. Non-anticipativity ωconstraints (NACs) ensuring that inputs from a common parent node in the scenario tree (Fig. 1) must be equal are given by (3d). This is because it is only possible to make a single control action $u _ { k } = \nu _ { 0 } ^ { \star }$ on the plant. (3e) is included νto show that parameter realizations are constant after the robust horizon. The state and control inputs inequality constraints across all scenarios are given in (3f) and D is the set of the discrete parameter realizations.

Problem (3) is a parametric optimization problem in $x _ { k }$ and dc, where dc denotes $\mathbf { d } _ { i = 0 , \dots , N - 1 } ^ { c = 1 , \dots , \mathrm { c a r d } ( \mathbb { C } ) }$ ) which is the con-,...,catenated vector of all parameter realizations in the scenario tree (across all scenarios and across all time steps in the prediction horizon).
