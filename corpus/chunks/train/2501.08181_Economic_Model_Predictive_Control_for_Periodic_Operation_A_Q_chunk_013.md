# A. Recursive feasibility:

In [18], it is proved that P is recursively feasible. Since the constraints in P are the same as those in P, this is recursively feasible irrespective of the selected values of $\hat { \zeta } .$ .
