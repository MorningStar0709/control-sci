research employed a Q weighting matrix with a range of five variations to assess the value of variation. Fig. 6 presents the altitude response of the UAV Bicopter with variations in the Q Matrix weighting. This altitude response graph shows that the 5th Q Matrix weighting parameter produces the smallest root mean square error (RMSE) compared to the others and does not experience overshoot as shown in Table 4.

Furthermore, testing was carried out by adding inertial disturbances to the Bicopter UAV dynamics system. This inertial disturbance is in the form of a payload that will be carried by the Bicopter in following a given trajectory. Based on Eq. (21) and Table 1 related to the mass and dimensions of the payload to be carried are described in Table 5.

![](image/ba7d4960dc5a1d85a458e39511a997a3a10bf5adbbb37fd3a2c80a181e6ac574.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    subgraph_Plant_Bicopter["Plant Bicopter"]
        W["W"] --> process_noise["process noise"]
        V["V"] --> measurement_noise["measurement noise"]
        u["u [4 x 1"]] --> B["B [12 x 4"]]
        Bu["Bu [12 x 1"]] --> Σ[Σ]
        Σ --> ÷x["ẋ [12 x 1"]]
        ∫.dt["∫.dt"] --> x["x [12 x 1"]]
        C["C [12 x 12"]] --> y=Cx["y = Cx [12 x 1"]]
        A["A [12 x 12"]] --> Σ
        Ax["Ax [12 x 1"]] --> Σ
        Σ --> y["y = ỹ"]
        y --> L["L [12 x 12"]]
        Σ --> sum["Σ"]
        sum --> y
        sum --> L
        L --> y
        sum --> A["A [12 x 12"]]
        A --> sum
    end

subgraph_State_observer["State observer"]
        B["B [12 x 4"]] --> Σ[Σ]
        Σ --> ÷x["ẋ [12 x 1"]]
        ÷xt["∫.dt"] --> x["x̃ [12 x 1"]]
        C["C [12 x 12"]] --> ỹ=Cx["x̃ [12 x 1"]]
        ỹ --> L
        y --> sum
        sum --> y
        sum --> A
    end

-K["-K [4 x 12"]]
    style Plant_Bicopter fill:#f9f,stroke:#333
    style State_observer fill:#bbf,stroke:#333
```
</details>

FIGURE 5: LQG controller design on UAV Bicopter.

TABLE 3: Variation of weighting of the Q Matrix.

<table><tr><td>No</td><td>Q weighting matrix</td></tr><tr><td>1</td><td> $Q = [C^{T}C]$ </td></tr><tr><td>2</td><td> $Q = [eye(12) \times 0,1]$ </td></tr><tr><td>3</td><td> $Q = [eye(12) \times 10]$ </td></tr><tr><td>4</td><td> $Q = [eye(12) \times 300]$ </td></tr><tr><td>5</td><td> $Q = [eye(12) \times 700]$ </td></tr></table>

TABLE 4: Response characteristics at altitude Bicopter with LQR control.
