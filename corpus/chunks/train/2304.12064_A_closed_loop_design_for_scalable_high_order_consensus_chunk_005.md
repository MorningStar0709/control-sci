# B. $n ^ { t h }$ order consensus

Let the system be modeled as N agents with identical $n ^ { \mathrm { t h } }$ order integrator dynamics, i.e.

$$\frac {\mathrm{d} ^ {n} x _ {i} (t)}{\mathrm{d} t ^ {n}} = u _ {i} (t), \tag {3}$$

for and $i \in \mathcal V$ use the convention to denote time de $x _ { i } ^ { ( 0 ) } ( t ) = x _ { i } ( t )$ x(k)i (t $\begin{array} { r } { x _ { i } ^ { ( k ) } ( t ) \ = \ \frac { \mathrm { d } ^ { k } } { \mathrm { d } t ^ { k } } x _ { i } ( t ) } \end{array}$ clear we may omit the time argument for brevity.

In this paper we will consider the problem of synchronizing the agents and thus achieve a state of consensus.

Definition $\textit { I } ( n ^ { t h }$ order consensus): The multi-agent system (3) is said to achieve $( n ^ { \mathrm { t h } }$ order) consensus if $\begin{array} { r } { \operatorname* { l i m } _ { t  \infty } | x _ { i } ^ { ( k ) } ( t ) - x _ { j } ^ { ( k ) } ( t ) | = 0 , } \end{array}$ , for all $i , j \in \mathcal { V }$ and $k \in$ $\{ 0 , 1 \ldots , n - 1 \}$ .
