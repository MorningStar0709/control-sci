Proof: First, observe that $\begin{array} { r l r } { \hat { J } ^ { w _ { k } } ( X _ { k \delta } ) } & { \le } & { \alpha _ { \mathrm { u p } } ( \| X _ { 0 } \| ) } \end{array}$ implies

$$\hat {\alpha} _ {\text { low }} (\| X _ {k \delta} \|) \leq \hat {J} ^ {w _ {k}} (X _ {k \delta}) \leq \alpha_ {\text { up }} (\| X _ {0} \|),\left\| X _ {k \delta} \right\| \leq \hat {\alpha} _ {\text { low }} ^ {- 1} (\alpha_ {\text { up }} (\left\| X _ {0} \right\|)). \tag {22}$$

Together with Lemma 1 the above yields

$$\forall t \in [ k \delta , (k + 1) \delta ] X _ {t} \in \mathcal {B} _ {S ^ {*}}. \tag {23}$$

The latter justifies the usage of earlier mentioned bounds and Lipschitz constants $\bar { f } , \mathrm { L i p } _ { \hat { J } } , \mathrm { L i p } _ { f }$ that assume $X _ { t } \in B _ { S ^ { * } }$ . This enables us to derive

$$
\begin{array}{l} \left\| X _ {(k + 1) \delta} - \Lambda_ {U _ {k \delta}} (X _ {k \delta}) \right\| \leq \\ \leq \left\| \int_ {k \delta} ^ {(k + 1) \delta} f (X _ {t}, U _ {k \delta}) d t - f (X _ {k \delta}, U _ {k \delta}) \delta \right\| + \\ \left\| \int_ {k \delta} ^ {(k + 1) \delta} \sigma (X _ {t}, U _ {k \delta}) Z _ {t} ^ {\prime} \mathrm{d} t \right\| \\ \leq \left\| \int_ {k \delta} ^ {(k + 1) \delta} f (X _ {t}, U _ {k \delta}) - f (X _ {k \delta}, U _ {k \delta}) d t \right\| + \\ \left\| \int_ {k \delta} ^ {(k + 1) \delta} \sigma (X _ {t}, U _ {k \delta}) Z _ {t} ^ {\prime} d t \right\| \\ \leq \int_ {k \delta} ^ {(k + 1) \delta} \| f (X _ {t}, U _ {k \delta}) - f (X _ {k \delta}, U _ {k \delta}) \| d t + \\ \int_ {k \delta} ^ {(k + 1) \delta} \| \sigma (X _ {t}, U _ {k \delta}) Z _ {t} ^ {\prime} \| d t \\ \leq \int_ {k \delta} ^ {(k + 1) \delta} \operatorname{Lip} _ {f} (\bar {f} + \sigma_ {\max} \operatorname{Lip} _ {Z}) \delta d t + \\ \int_ {k \delta} ^ {(k + 1) \delta} \sigma_ {\max} \mathrm{Lip} _ {Z} \mathrm{d} t \\ = \underbrace {\operatorname{Lip} _ {f} (\bar {f} + \sigma_ {\max} \operatorname{Lip} _ {Z}) \delta^ {2} + \sigma_ {\max} \operatorname{Lip} _ {Z} \delta} _ {\chi_ {1} (\delta) :=} \tag {24} \\ \end{array}
$$

Due to $G _ { \varepsilon } ^ { \bar { \nu } } ( \dots ) \leq 0$ the latter implies
