# C. Learning Optimization Formulation

Since the controller (9) is stable for all compatible DNN parameters, we can further optimize the performance by optimizing the DNN parameters. The optimization we are solving is the following.

$$
\begin{array}{l} \min _ {\boldsymbol {\theta_ {1}}, \boldsymbol {\theta_ {2}}} \quad \int_ {t = 0} ^ {T} l _ {t} (\boldsymbol {z _ {1}}, \boldsymbol {u}) d t \\ + \operatorname{ReLu} \left(\lambda_ {\max} \left(\alpha I - \left. \frac {\partial^ {2} \Phi}{\partial z _ {1} ^ {2}} \right| _ {z _ {1} = 0}\right)\right) \tag {13} \\ \end{array}
$$

s.t. dynamics $M ( q ) { \ddot { q } } + C ( q , { \dot { q } } ) { \dot { q } } + G ( q ) = { \pmb u } _ { \mathrm { : } }$

errors (8), controller (9),

initial state q(0), q˙(0),

where $l _ { t }$ denotes the stage cost at time t. The above optimization problem can be solved by discretizing the dynamics and the cost function first and then numerically solving the discrete-time counterpart.
