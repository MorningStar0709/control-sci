# Exercise 1.54: Where is the steady state?

Consider the two-input, two-output system

$$
A = \left[ \begin{array}{c c c c} 0. 5 & 0 & 0 & 0 \\ 0 & 0. 6 & 0 & 0 \\ 0 & 0 & 0. 5 & 0 \\ 0 & 0 & 0 & 0. 6 \end{array} \right] \qquad B = \left[ \begin{array}{c c} 0. 5 & 0 \\ 0 & 0. 4 \\ 0. 2 5 & 0 \\ 0 & 0. 6 \end{array} \right] \qquad C = \left[ \begin{array}{c c c c} 1 & 1 & 0 & 0 \\ 0 & 0 & 1 & 1 \end{array} \right]
$$

(a) The output setpoint is $\begin{array} { r l } { y _ { \mathrm { s p } } = \lceil 1  } & { { } - 1 \rceil ^ { \prime } } \end{array}$ and the input setpoint is $u _ { \mathrm { s p } } = \left\lceil 0 \quad 0 \right\rceil ^ { \prime }$ . Calculate the target triple $( x _ { s } , u _ { s } , y _ { s } )$ . Is the output setpoint feasible, i.e., does $y _ { s } = y _ { \mathrm { s p } } ?$

(b) Assume only input one $u _ { 1 }$ is available for control. Is the output setpoint feasible? What is the target in this case using $Q _ { s } = I ?$

(c) Assume both inputs are available for control but only the first output has a setpoint, $y _ { 1 t } = 1$ . What is the solution to the target problem for $R _ { S } = I ?$
