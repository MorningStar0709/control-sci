$$\boldsymbol {M} _ {i} \ddot {\boldsymbol {x}} _ {i} + \boldsymbol {h} _ {i} (\boldsymbol {q} _ {i}, \dot {\boldsymbol {q}} _ {i}) = \boldsymbol {u} _ {i} - \boldsymbol {f} _ {i}, \quad \forall i = 1, 2 \tag {27}$$

where

$$\pmb {u} _ {i} = \pmb {J} _ {i} ^ {- T} \pmb {\tau} _ {i},$$

see Appendix 3. Here, $\tau _ { i }$ is the vector of joint torques, $f _ { i }$ is the vector of generalized force exerted at the end-effector, $M _ { i }$ is the Cartesian mass matrix, and nonlinear vector $\boldsymbol { h } _ { i }$ contains the Coriolis, centrifugal, and gravity terms. Assuming kinematic singularity does not occur, there is a one-to-one correspondence with the torque vector $\tau _ { i }$ and the corresponding auxiliary input $\mathbf { { \mathscr { u } } } _ { i } \ [ 4 2 - 4 4 ]$ . Therefore, in the following derivation, we will take $\mathbf { \Delta } \mathbf { u } _ { i }$ as the control input for the sake of simplicity. Moreover, writing the balance of the forces on the common object yields

$$\boldsymbol {M} _ {o} \ddot {\boldsymbol {x}} + \boldsymbol {h} _ {o} = \boldsymbol {f} _ {1} + \boldsymbol {T} ^ {T} \boldsymbol {f} _ {2} \tag {28}$$

where x is the Cartesian coordinate of the object, $M _ { o }$ and $h _ { o }$ are the object inertia matrix and the associated Coriolis, centrifugal, and gravity terms, and $\pmb { T } ^ { T }$ is the corresponding force transformation. Define the augmented control input and generalized coordinates, respectively, as

$$
\boldsymbol {u} = \left[ \begin{array}{c} \boldsymbol {u} _ {1} \\ \boldsymbol {u} _ {2} \end{array} \right] \quad \text {and} \quad \boldsymbol {q} = \left[ \begin{array}{c} \boldsymbol {q} _ {1} \\ \boldsymbol {q} _ {2} \end{array} \right].
$$

The velocity of the object can be related to one of the involved end-effectors, say the first one, by ${ \dot { \pmb x } } _ { 1 } = \Lambda { \dot { \pmb x } }$ , where constant matrix Λ represents the corresponding velocity transformation. Thus, upon substitution of $f _ { 1 } , f _ { 2 } ,$ and ${ \ddot { x } } _ { 2 }$ from (26) and (27) into(28), the latter equation can be arranged in the following form

$$\bar {M} \ddot {\boldsymbol {x}} + \bar {\boldsymbol {h}} = \bar {\boldsymbol {u}} \tag {29}$$

where
