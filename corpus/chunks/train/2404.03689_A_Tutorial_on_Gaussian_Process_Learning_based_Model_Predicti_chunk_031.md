where $H _ { x , k + b }$ and $H _ { u , k + b }$ are matrices capturing the combined effects of the linearized nominal model and the GP mean derivative as:

$$H _ {x, k + b} = \left. \frac {\partial f (x , u)}{\partial x} \right| _ {\bar {x} _ {k + b}, \bar {u} _ {k + b}} + \left. \frac {\partial \mu^ {d} (x , u)}{\partial x} \right| _ {\bar {x} _ {k + b}, \bar {u} _ {k + b}}, \tag {36a}H _ {x, k + b} = \left. \frac {\partial f (x , u)}{\partial u} \right| _ {\bar {x} _ {k + b}, \bar {u} _ {k + b}} + \left. \frac {\partial \mu^ {d} (x , u)}{\partial u} \right| _ {\bar {x} _ {k + b}, \bar {u} _ {k + b}}. \tag {36b}$$

Defining the deviations in state and control input vectors as $\delta \mathbf { x } _ { k + 1 } = ( \delta x _ { k + 1 } , \cdot \cdot \cdot , \delta x _ { k + N } ) , \delta \mathbf { x } _ { k } =$ $( \delta x _ { k } , \cdots , \delta x _ { k + N - 1 } )$ and $\delta { \bf u } _ { k } = ( \delta u _ { k } , \cdot \cdot \cdot , \delta u _ { k + N - 1 } )$ yields a compact matrix formulation for the system dynamics as follows:

$$
\begin{array}{l} \delta \mathbf {x} _ {k + 1} = \mathbf {H} _ {\mathbf {x}} \delta \mathbf {x} _ {k} + \mathbf {H} _ {\mathbf {u}} \delta \mathbf {u} _ {k}, \\ = \left(\mathbf {I} - \mathbf {H _ {x}}\right) ^ {- 1} \mathbf {H _ {u}} \delta \mathbf {u} _ {k}, \\ = \mathbf {H} ^ {\prime} \delta \mathbf {u} _ {k}, \tag {37} \\ \end{array}
$$

where I denotes the identity matrix, and $\mathbf { H _ { u } } = \mathrm { d i a g } ( H _ { u , k } , \cdot \cdot \cdot , H _ { u , k + N - 1 } )$ encapsulates the control dynamics across the prediction horizon. The matrices $\mathrm { H } _ { \mathbf { x } }$ and $\mathbf { H } _ { \mathbf { u } }$ reflect the response to state and control perturbations of the system and GP model, respectively with

$$
\Lambda_ {x, k + 1} = \left[ \begin{array}{c c} 0 & 0 \\ H _ {x, k + 1} & 0 \end{array} \right], \quad \mathbf {H} _ {\mathbf {x}} = \operatorname{diag} \left(\Lambda_ {x, k + 1}, \dots , \Lambda_ {x, k + N - 1}\right). \tag {38}
$$

Substituting $\delta \mathbf { x } _ { k + 1 } = \mathbf { x } _ { k + 1 } - { \bar { \mathbf { x } } } _ { k + 1 } , \delta \mathbf { u } _ { k } = \mathbf { u } _ { k } - { \bar { \mathbf { u } } } _ { k }$ and (37) to (30), results in an approximated cost function:
