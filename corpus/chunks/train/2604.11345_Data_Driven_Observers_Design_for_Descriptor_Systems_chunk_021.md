$$
\left[ \begin{array}{c} x (k) \\ x (k + 1) \\ u (k) \\ y (k) \\ y (k + 1) \end{array} \right] = \mathcal {H} \bar {\mathcal {M}} \left[ \begin{array}{c} z _ {1} (k) \\ u (k) \\ \eta (k) \\ \vdots \\ u (k + s) \\ \eta (k + s) \end{array} \right], \tag {39}
$$

which confirms that the trajectory $( \{ u ( k ) \} , \{ y ( k ) \} , \{ x ( k ) \} )$ is generated by system (34). Hence, the trajectory belongs to $\mathbb { T } _ { \Sigma _ { 2 } }$ , proving the inclusion $\mathbb { T } _ { c } ( u _ { d } , y _ { d } , x _ { d } ) \subseteq \mathbb { T } _ { \Sigma _ { 2 } }$ .

Conversely, arbitrarily select a trajectory $( \{ u ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } ,$ $\{ y ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } , \{ x ( k ) \} _ { k \in \mathbb { Z } _ { 0 } } ) \in \mathbb { T } _ { \Sigma _ { 2 } }$ . This trajectory intrinsically satisfies the system dynamics formulated in (39) for some $\{ \eta ( k ) \} _ { k \in \mathbb { Z } _ { 0 } }$ . Under Assumption 3, there exists a vector $g _ { 2 } \in \mathbb { R } ^ { T }$ such that (38) holds. Substituting (37) and (38) backwards into (39), it follows directly that the trajectory satisfies the data compatibility condition (36). This implies the reverse inclusion $\mathbb { T } _ { \Sigma _ { 2 } } \subseteq \mathbb { T } _ { c } ( u _ { d } , y _ { d } , x _ { d } ) .$ . Consequently, $\mathbb { T } _ { \Sigma _ { 2 } } = \mathbb { T } _ { c } ( u _ { d } , y _ { d } , x _ { d } )$ is established. 

According to Lemma 2, given any compatible trajectory $[ x ( k ) ^ { \top } , x ( \overline { { k + 1 } } ) ^ { \top } , u ( k ) ^ { \top } , y ( k ) ^ { \top } , y ( k \mathring { + } 1 ) ^ { \top } ] ^ { \top }$ of system (34), there exists a vector $g _ { 2 } \in \mathbb { R } ^ { T }$ such that

$$
\begin{array}{l} \operatorname{col} \{x (k), x (k + 1), u (k), y (k), y (k + 1) \} \tag {40} \\ = \mathbf {c o l} \{X _ {p}, X _ {f}, U _ {p}, Y _ {p}, Y _ {f} \} g _ {2} \\ \end{array}
$$

To predict $x ( k + 1 )$ via $x ( k + 1 ) = X _ { f } g _ { 2 }$ , we first determine the coefficient vector g2 by solving the remaining equations, whose general solution is given by

$$
g _ {2} = D _ {2} ^ {\dagger} \left[ \begin{array}{c} x (k) \\ u (k) \\ y (k) \\ y (k + 1) \end{array} \right] + \Omega_ {2} \beta_ {2}, \tag {41}
$$

where $D _ { 2 } = [ X _ { p } ^ { \top } , U _ { p } ^ { \top } , Y _ { p } ^ { \top } , Y _ { f } ^ { \top } ] ^ { \top }$ , D†2 denotes the Moore-Penrose pseudoinverse of matrix $D _ { 2 } .$ the columns of matrix $\Omega _ { 2 }$ form a basis for $K e r ( D _ { 2 } )$ , and $\beta _ { 2 }$ is an arbitrary matrix of appropriate dimensions.
