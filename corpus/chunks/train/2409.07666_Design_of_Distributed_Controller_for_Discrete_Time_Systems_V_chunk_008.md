Next, we provide the explicit formulation of $\kappa _ { s }$ , the solution set for the discrete-time version of the clique-wise decomposition method [15]. This formulation constrains the Lyapunov matrix to satisfy a sparsity pattern, yet it yields a larger solution set than $\kappa _ { \mathrm { d i a g } }$ in (5). Note that there is no inclusion relationship between this set and $\mathcal { K } _ { \mathrm { e x t } }$ in (6).

Lemma 5: Assume Assumption 1 holds. Let

$$
\begin{array}{l} \mathcal {K} _ {\mathcal {S}} = \{K = (E ^ {\top} E) ^ {- 1} E ^ {\top} \tilde {Z} \tilde {Q} ^ {- 1} E \\ = (E ^ {\top} E) ^ {- 1} \sum_ {k = 1} ^ {q} E _ {\mathcal {C} _ {k}} ^ {\top} (\tilde {Z} _ {k} \tilde {Q} _ {k} ^ {- 1}) E _ {\mathcal {C} _ {k}} \in \mathcal {S}: \\ \exists \tilde {Z} = \operatorname{blkdiag} \left(\tilde {Z} _ {1}, \dots \tilde {Z} _ {q}\right), \\ \tilde {Q} = \operatorname{blkdiag} \left(\tilde {Q} _ {1}, \dots \tilde {Q} _ {q}\right) \succ O, \rho \in \mathbb {R}, \eta > 0, \\ \text { s.t. } \left[ \begin{array}{c c} \tilde {Q} & * \\ \tilde {A} \tilde {Q} + \tilde {B} \tilde {Z} & \tilde {Q} \end{array} \right] + \rho W \succ O, \\ U \tilde {Q} + \tilde {Q} U \succeq \eta U \}, \tag {8} \\ \end{array}
$$

where

$$W = \text { blkdiag } (U, U), U = I - E ^ {\top} (E ^ {\top} E) ^ {- 1} E, \tag {9}\tilde {A} = E A (E ^ {\top} E) ^ {- 1} E ^ {\top}, \tilde {B} = E B (E ^ {\top} E) ^ {- 1} E ^ {\top}. \tag {10}$$

Then ${  { \mathcal { K } } _ { \mathrm { d i a g } } } \subset {  { \mathcal { K } } _ { S } } \subset {  { \mathcal { K } } _ { \mathrm { a l l } } }$ holds.

Proof: The proof follows a similar procedure as in [15]; see Appendix B. for the full proof.

Here, the Lyapunov matrix $\boldsymbol { P } = \boldsymbol { E } ^ { \intercal } \boldsymbol { \tilde { Q } } ^ { - 1 } \boldsymbol { E }$ is structurally constrained in sparsity, $P \in S .$ , as established in Lemma 2. In our main result, we aim to remove structural constraints on ${ \tilde { Q } } ,$ , and consequently on the Lyapunov matrix $P .$
