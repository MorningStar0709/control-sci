$$\dot {V} _ {\mathrm{r}} (x (t)) < - \gamma_ {\mathrm{r}} (V _ {\mathrm{r}} (x (t))) + \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|), \quad \forall t \in \mathbb {R} ^ {+}. \tag {13}$$

From Corollary 1 in [36] and item (i) in Definition 2, there exists $\beta \in \mathcal { K L }$ such that $| x ( t ) | \leq \beta ( \| \xi \| , t )$ for all $t \in \mathbb { R } ^ { + }$ , which implies that the system (1) is GAS.

Next, we show the continuity of the controller. From $V _ { \mathsf { r } } \in$ $C ( \mathbb { R } ^ { n } , \mathbb { R } ^ { + } )$ and the continuity of $\mathfrak { a } _ { \mathfrak { r } } ( \phi )$ and $\mathfrak { b } _ { \mathfrak { r } } ( \phi )$ , we can deduce the continuity of the controller in any region away from the origin. In the following, we only need to show the continuity of the controller at the origin. From the R-SCP, for arbitrary $\varepsilon > 0$ , there exists $\delta _ { 1 } > 0$ such that for nonzero $\phi \in C ( [ - \Delta , 0 ] , \mathbf { B } ( \delta _ { 1 } ) )$ ) with $\phi ( 0 ) = x ,$ there exists $u \in \mathbf { B } ( \varepsilon )$ such that

$$L _ {f} V _ {\mathrm{r}} (\phi) + L _ {g} V _ {\mathrm{r}} (\phi) u < - \gamma_ {\mathrm{r}} (V _ {\mathrm{r}} (x)) + \eta_ {\mathrm{r}} (\| V _ {\mathrm{r}} (\phi) \|). \tag {14}$$

In addition, since $V _ { \mathsf { r } }$ is continuously differentiable and $g$ in (1) is locally Lipschitz, there exists $\delta _ { 2 } \in \mathbb { R } ^ { + }$ with $\delta _ { 2 } \neq \delta _ { 1 }$ such that for nonzero $\phi \in C ( [ - \Delta , 0 ] , \mathbf { B } ( \delta _ { 2 } ) )$ ,

$$\left\| L _ {g} V _ {\mathrm{r}} (\phi) \right\| \leq \varepsilon . \tag {15}$$

Let $\delta : = \operatorname* { m i n } \{ \delta _ { 1 } , \delta _ { 2 } \}$ , and then (14)-(15) hold for any nonzero $\phi \in { \cal C } ( [ - \Delta , 0 ] , \mathbf { B } ( \delta ) )$ .

From $( 1 1 ) , u \equiv 0 { \mathrm { ~ i f ~ } } \phi = 0$ . Hence, we only need to show that kuk $\leq \bar { \varepsilon } : = ( 2 + \lambda ) \varepsilon$ for all nonzero $\phi \in { \cal C } ( [ - \Delta , 0 ] , \mathbf { B } ( \delta ) )$ , where $\lambda > 0$ is given in (11). For this purpose, we consider the following two cases. If $L _ { g } V _ { \Gamma } ( \phi ) \ = \ 0$ , then $u \ : = \ : 0$ from (12), and thus $\left\| u \right\| \le \bar { \varepsilon }$ for all nonzero $\phi \in \ : C ( [ - \Delta , 0 ] , \mathbf { B } ( \delta ) )$ . If $L _ { g } V _ { \Gamma } ( \phi ) \neq 0$ , then we have from (14) that for any nonzero $\phi \in { \cal C } ( [ - \Delta , 0 ] , \mathbf { B } ( \delta ) )$ ,
