# C.6 Proof of Lemma 4.6

We assume throughout this proof that the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ holds. First, consider a single term $\left\| y _ { t } - \hat { y } _ { t } ^ { \mathrm { S K } } \right\| ^ { 2 } - \left\| y _ { t } - \hat { y } _ { t } ^ { \mathrm { K F } } \right\| ^ { 2 }$ for any $t \in \{ 0 , \ldots , T - 1 \}$ , we have

$$
\begin{array}{l} \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| ^ {2} - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{KF}} \right\| ^ {2} = (\hat {y} _ {t} ^ {\mathrm{KF}} - \hat {y} _ {t} ^ {\mathrm{SK}}) ^ {\top} (2 y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} - \hat {y} _ {t} ^ {\mathrm{KF}}) \\ \leq \left\| \hat {y} _ {t} ^ {\mathrm{KF}} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| \left\| 2 y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} - \hat {y} _ {t} ^ {\mathrm{KF}} \right\| \\ \leq \left\| \hat {y} _ {t} ^ {\mathrm{KF}} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| \left(2 \| y _ {t} \| + \left\| \hat {y} _ {t} ^ {\mathrm{SK}} \right\| + \left\| \hat {y} _ {t} ^ {\mathrm{KF}} \right\|\right). \tag {C.10} \\ \end{array}
$$

Recall from Lemma 3.2 tproof of Lemma 4.5 that $\| y _ { t } \| \le R _ { y }$ $t \in \{ 0 , \ldots , T - 1 \}$ in the. Thus, $\begin{array} { r } { \left\| \hat { y } _ { t } ^ { \mathrm { S K } } \right\| \le \sqrt { h } R _ { y } R _ { N } + R _ { y } \kappa _ { F } \left\| L \right\| \left\| C \right\| \frac { \gamma _ { F } ^ { h } } { 1 - \gamma _ { F } } } \end{array}$ $t \in \{ 0 , \ldots , T - 1 \}$ $\left\| \hat { y } _ { t } ^ { \mathrm { K F } } - \hat { y } _ { t } ^ { \mathrm { S K } } \right\|$ $\left\| \hat { y } _ { t } ^ { \mathrm { K F } } \right\|$ $t \in \{ 0 , \ldots , T - 1 \}$ . To this end, we first prove the following intermediate result.

Lemma C.1. Consider the Kalman filter $\hat { x } _ { t } ^ { \mathrm { K F } }$ given by (2) initialized with $\hat { x } _ { 0 } ^ { \mathtt { K F } } = 0$ . For any $t \in \{ 0 , \ldots , T - 1 \}$ , it holds that

$$\left\| \hat {x} _ {t} ^ {\mathrm{KF}} \right\| \leq R _ {\hat {x}} (t) \triangleq \sqrt {\frac {\| W + L V L ^ {\top} \| \kappa_ {F} ^ {2}}{\lambda_ {\min} (W) \left(1 - \gamma_ {F} ^ {2}\right)}} R _ {y} \| L \| t + \frac {\kappa_ {F} ^ {4} \| W - \Sigma \| \| C \| \| W + L V L ^ {\top} \|}{\lambda_ {\min} (V) \lambda_ {\min} (W) \left(1 - \gamma_ {F}\right) ^ {2} \left(1 + \gamma_ {F}\right)}. \tag {C.11}$$

In addition, for any $t \in \{ 0 , \ldots , T - 1 \}$ , it holds that

$$\left\| \hat {x} _ {t} ^ {\tt S K} \right\| \leq \| L \| R _ {y} \frac {\kappa_ {F}}{1 - \gamma_ {F}},$$

where $\hat { x } _ { t } ^ { \mathrm { S K } }$ is given by (11).

Proof. Recall that we denote
