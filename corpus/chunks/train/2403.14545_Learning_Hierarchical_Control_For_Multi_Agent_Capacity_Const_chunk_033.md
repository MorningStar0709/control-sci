Assume that the hierarchical controller outlined in Alg. 1, where $\pi ^ { H , r - 1 }$ is as in (31) and $\pi ^ { L , r - 1 }$ as in (33) is feasible at iteration $r - 1 \geq 0$ . Therefore, ${ \mathcal { S } } ^ { H , r }$ is non-empty, and $\mathcal { S } _ { i , j } ^ { L , r }$ Si,j is non-empty for each edge $e _ { i , j }$ in G. In particular, there exists a feasible high-level trajectory $\mathbf { x } ^ { H , r - \mathrm { \bar { 1 } } } \in { \mathcal { S } } ^ { H , r }$ with related input $\boldsymbol { u } ^ { H , r - 1 }$ , where $x _ { 0 } ^ { H , \overline { { r } } - 1 } = x _ { 0 } ^ { H , r } = ( D , \mathbf { 0 } _ { L } )$ = xH,r0 = (D, 0L). At event $k = 0$ of iteration r, the N-step high-level trajectory

$$\left(\mathbf {n} _ {0: N} ^ {r - 1}, \mathbf {c} _ {0: N} ^ {r - 1}\right) \in \mathcal {S} ^ {H, r}, \tag {41}$$

where $\mathbf { c } _ { 0 : N } ^ { r - 1 }$ is the sequence of high-level capacity states at events $k = 0$ through k = N of iteration (r − 1) and $\mathbf { n } _ { 0 : N } ^ { r - 1 }$ the sequence of nodes from events $k = 0$ through $k = N$ of iteration (r − 1), associated with the input

$$\mathbf {u} _ {0 | 0} ^ {H, r} = u _ {0} ^ {r - 1} \tag {42}$$

and capacity states

$$\mathbf {c} _ {0: N \mid 0} ^ {r} = \mathbf {c} _ {0: N} ^ {r - 1} \tag {43}$$

is a feasible high-level trajectory which satisfies all constraints $( 2 6 \mathrm { a } ) \textrm { - } ( 3 0 \mathrm { f } )$ . Note particularly that, as follows from Thm. 1, ˆθr−1 $\hat { \theta } _ { D , n _ { 1 } ^ { r - 1 } } ^ { r - 1 } \geq \hat { \theta } _ { D , n _ { 1 } ^ { r - } } ^ { r }$ D,nr−1 ˆθrD,nr−1 , and therefore constraints (28a)- 1 (28c) will be satisfied at iteration r with (43). Thus, (31) is feasible at event $k = 0$ of iteration r.

We denote the optimal N -step high-level state trajectory as determined by the optimal control problem (31) at event $k = 0$ of iteration r as

$$\mathbf {x} _ {0: N \mid 0} ^ {H, r, \star} = (\mathbf {n} _ {0: N \mid 0} ^ {H, r, \star}, \mathbf {c} _ {0: N \mid 0} ^ {H, r, \star}) \tag {44}$$

where n1|0 $n _ { 1 | 0 } ^ { H , r , \star } = j$ H,r,⋆ is sent as the high-level reference node for the low-level controller to track. Since (44) must satisfy constraint (28c), it follows that

$$C _ {l} \geq \hat {\theta} _ {D, j, l} ^ {r}, \forall l \in [ 1, L ].$$

Therefore, the agent state $\boldsymbol { x } _ { 0 } ^ { r }$ is such that it satisfies the initial state conditions in Thm. 1, and the low-level policy (33),(34) will be feasible for all time steps $t \in [ 0 , \mathcal { T } ( \mathbf { x } ^ { r } , D , j ) ]$ ] and bring the agent to node $v _ { j }$ .
