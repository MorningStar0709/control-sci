In the above, we view the measurement in the interval $[ k h , ( k + 1 ) h ]$ as a continuous path-valued measurement. Measure change with discrete-time measurements, inspired from Girsanov, have been adopted extensively, see [44] (see [21] for a review). Note that the absolute continuity condition to allow for measure transformation is applicable for the discrete-time setup as a corollary of the continuous-time setting Section 3. In this case, the policies

$$U _ {k h} = \gamma (\{Y _ {s}, s \leq k h \})$$

are such that on a set of measure $1 - \epsilon , \gamma = \gamma _ { C } ^ { \epsilon }$ for a continuous function $\gamma _ { C } ^ { \epsilon }$ (for any $\epsilon > 0 )$ . Furthermore, the process $Y _ { [ 0 , k h ] }$ and its piece-wise constant interpolation $\begin{array} { r } { \tilde { Y } _ { t } ^ { h } = \sum _ { i = 1 } ^ { k } Y _ { i h } \boldsymbol { 1 } _ { t \in [ i h , ( i + 1 ) h ] } } \end{array}$ are so that as $h  0$

$$\left\| Y - \tilde {Y} ^ {h} \right\|\rightarrow 0.$$

The absolute continuity condition permitting measure transformation is applicable in the discrete-time setting as a consequence of the continuous-time framework discussed in Section 3.

Accordingly, we have that the cost (5.5) under γ is at most $2 ( k + 1 ) \| c \| _ { \infty } \epsilon$ different from the cost of its continuous approximation $\gamma _ { C }$ and the continuous policy $\gamma _ { C }$ asymptotically, as $h  0 ,$ attains the same cost by having access to the sampled measurements; see [39, Prop. 6.1]. We then state the following refinement to Theorem 5.2 with a truly regular discrete-time Partially Observable Markov Decision Process (POMDP) approximation .

Theorem 5.3. Suppose that Assumptions $( A I )$ and (A2) hold . Then

(i) The optimal value of the discrete-time model (5.6) convergences to the optimal value of the original continuous-time model.   
(ii) For every $\epsilon > 0$ , there exists $h > 0$ so that the solution of the discrete-time approximation gives a policy $( i . e . , a$ policy $U _ { k h } ^ { \epsilon } = \gamma ^ { \epsilon } ( Y _ { i h } , i \in \{ 0 , 1 , \cdot \cdot , k \} )$ ) obtained in $( 5 . 6 ) )$ which is near optimal for the original continuous-time model.
