# 4.2 Proof of Theorem 4.1

Notations used in this proof. For notational simplicity in this proof, we assume that any filter parameter $N \in \mathbb { R } ^ { p \times ( p h ) }$ has been readily written in its vectorized form vec $\mathbf { \Psi } : ( N ) \in \mathbb { R } ^ { p ^ { 2 } h }$ so that we simply denote N . We also reload the notations and use $\Pi _ { K _ { N } } ( N )$ to represent the projection of a vector $N \in \mathbb { R } ^ { p ^ { 2 } h }$ onto $\begin{array} { r } { \mathcal { K } _ { N } = \{ N \in \mathbb { R } ^ { p ^ { 2 } h } : \| N \| \le \sqrt { p } \psi \frac { \kappa _ { F } ^ { 2 } } { 1 - \gamma _ { F } } \} , \mathrm { i . e . , } \Pi _ { \mathcal { K } _ { N } } ( N ) = \arg \operatorname* { m i n } _ { N ^ { \prime } \in \mathcal { K } _ { N } } \| N ^ { \prime } - N \| } \end{array}$ 1−γF The following notations will also be used in this proof:

$$R _ {N} \triangleq \sqrt {p} \psi \frac {\kappa_ {F} ^ {2}}{1 - \gamma_ {F}}, \beta_ {y} \triangleq 2 h R _ {y} ^ {2}, \tag {30}l _ {y} \triangleq 2 \sqrt {h} R _ {y} ^ {2} (\sqrt {p} \psi \kappa_ {F} ^ {2} \frac {\sqrt {h}}{1 - \gamma_ {F}} + 1),$$

where $R _ { y }$ is defined in (26).

In the following, we provide the main steps in the proof and the omitted details are included in Appendix C. We begin by introducing an auxiliary function based on conditional expectation, which will be used throughout this proof.

Definition 4.1. Let $\{ \mathcal { F } _ { t } \} _ { t \ge 0 }$ be a filtration with $\mathcal { F } _ { t } \triangleq \sigma ( w _ { 0 } , \ldots , w _ { t - 1 } , v _ { 0 } , \ldots , v _ { t - 1 } )$ for $t \geq 1$ and $\mathcal { F } _ { 0 } \overset { \triangle } { = } \emptyset$ . For any $N \in \mathbb { R } ^ { p ^ { 2 } h }$ and some $h \in \mathbb { Z } _ { \geq 0 }$ , define $f _ { t ; h } ( N ) = \mathbb { E } [ f _ { t } ( N ) | \mathcal { F } _ { t - h } ]$ for all $t \geq h$ , where the expectation E[·] is taken with respect to $\{ w _ { t } \} _ { t \ge 0 }$ and $\{ v _ { t } \} _ { t \geq 0 }$ .

We show that the function $f _ { t ; h } ( \cdot )$ defined above has the following properties.

Lemma 4.1. For any $t \geq h , f _ { t ; h } ( \cdot )$ is α0-strongly convex. In addition, under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ defined in (25), $f _ { t ; h } ( \cdot )$ is $l _ { y } - L i p s c h i t z .$ .

Regret decomposition. We decompose the regret $\mathtt { R } _ { y } ( T )$ in (20) as
