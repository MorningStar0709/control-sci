$$\Phi (i) = \tilde {A} ^ {i} \Phi (0) \oplus \mathcal {A} (i) \qquad \mathcal {A} (i) := \sum_ {j = 0} ^ {i - 1} \tilde {A} ^ {j} \tilde {B} \Psi$$

where $\rho ( { \tilde { \cal A } } ) \ < \ 1$ . The family of compact sets in $\mathbb { R } ^ { n }$ endowed with the Hausdorff metric is a complete space so any Cauchy sequence has a limit in this space. As shown in Kolmanovsky and Gilbert (1998), $\{ \mathcal { A } ( i ) \}$ is a Cauchy sequence which, therefore, converges in the Hausdorff metric to the compact set Φ that satisfies $\Phi \ : = \ : \widetilde { A } \Phi \oplus \widetilde { B } \Psi$ and is the minimal robust positive invariant set for $\phi ^ { + } = \tilde { A } \phi + \tilde { B } \psi , \psi \in \Psi$ . Because Φ(0) is compact, and contains the origin, the set $\stackrel { \sim } { A } ^ { i } \Phi ( 0 )$ converges to {0}. Hence Φ(i) converges in the Hausdorff metric to Φ. Clearly $0 \in \mathcal { A } ( i )$ for all $i \in \mathbb { I } _ { \geq 0 }$ . Because $0 \in \ \Phi ( 0 )$ , it follows that $0 \in \Phi ( i )$ for all $i \in \mathbb { I } _ { \geq 0 } ;$ ; because Φ is closed, $0 \in \Phi$ . By hypothesis $\Phi ( 0 )$ is robust positive invariant for $\phi ^ { + } = \overset \sim A \phi + \overset \sim B \psi , \psi \in \Psi ( 0 )$ so that

$$\Phi (1) = \tilde {A} \Phi (0) + \tilde {B} \Psi \subseteq \Phi (0)$$

Let $i \in \mathbb { I } _ { \geq 0 }$ be arbitrary and assume that Φ(i) is robust positive invariant for $\phi ^ { + } = \tilde { A } \phi + \tilde { B } \psi , \psi \in \Psi$ . Then

$$\Phi (i + 1) = \tilde {A} \Phi (i) \oplus \tilde {B} \Psi \subseteq \Phi (i)$$

so that Φ(i) is robust positive invariant for $\phi ^ { + } \mathit { \Pi } = \widetilde { A } \phi + \widetilde { B } \psi , \psi \in \Psi$ and Φ $\ b ( i + 1 ) \subseteq \Phi ( i )$ . By induction, $\Phi ( i )$ is robust positive invariant for $\phi ^ { + } = \tilde { A } \phi + \tilde { B } \psi , \psi \in \Psi$ and $\Phi ( i { + } 1 ) \subseteq \Phi ( i )$ for all $i \in \mathbb { I } _ { \geq 0 }$ . Hence $\left\{ \Phi ( i ) \right\}$ is a monotonically nonincreasing sequence. The proof that $d _ { H } ( \Phi ( i ) , \Phi ) \leq$ $c d _ { H } ( \Phi ( 0 ) , \Phi ) \gamma ^ { i }$ is left as Exercise 5.8. 

It follows that $\mathbb { \Sigma } ( i )  \mathbb { \Sigma }$ and S(i) → S as i → ∞ where Σ and S satisfy

$$\Sigma = A _ {L} \Sigma \oplus (\mathbb {W} \oplus (- L \mathbb {N}))\mathbb {S} = A _ {K} \mathbb {S} \oplus L (C \mathbb {Z} \oplus \mathbb {N})$$

and are the minimal robust positive invariant sets for, respectively, $\tilde { { \boldsymbol { x } } } ^ { + } = A _ { L } \tilde { { \boldsymbol { x } } } + \tilde { { \boldsymbol { w } } } , \tilde { { \boldsymbol { w } } } \in ( \mathbb { W } \oplus ( - L \mathbb { N } ) )$ ) and $e ^ { + } = A _ { K } e + \delta , \delta \in L ( C \mathbb { Z } \oplus \mathbb { N } )$ ; the sequences $\{ \mathbb { Z } ( i ) \}$ and $\{ \mathbb { S } ( i ) \}$ are nonincreasing and converge in the Hausdorff metric to $\Sigma = \left[ I _ { n } \quad 0 \right] \Phi$ and $\mathbb { S } = \left[ { \begin{array} { l l } { 0 } & { I _ { n } } \end{array} } \right] \ / \oplus$ , respectively.
