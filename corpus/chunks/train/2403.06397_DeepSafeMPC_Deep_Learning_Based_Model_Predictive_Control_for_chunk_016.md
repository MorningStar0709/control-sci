Algorithm 1 Training Process of DeepSafeMPC   
1: Initialize policy network parameters $\theta$ , value function parameters $\phi$ , predictor model parameters $\eta$ 2: Initialize optimization algorithm for MPC with cost function $C$ 3: Set learning rates $\alpha_{\theta}$ , $\alpha_{\phi}$ , $\alpha_{\eta}$ 4: Set horizon $T$ , episode length $L$ , batch size $B$ , maximum steps $I_{\text{max}}$ 5: Initialize data buffer $\mathcal{D}$ , replay buffer $\mathcal{B}$ 6: for each MAPPO training episode $e = 1,2,\ldots,E$ do
7: Collect trajectories by executing policy $\pi_{\theta}$ 8: Store state-action-reward sequences in $\mathcal{D}$ and $\mathcal{B}$ 9: Update policy $\pi_{\theta}$ and value function $V_{\phi}$ by (4) and (5)
10: Clean the replay buffer $\mathcal{B}$ 11: for each predictor model training step do
12: Sample minibatch $D$ from $\mathcal{D}$ 13: Train predictor model $f_{\eta}$ by minimizing MSE loss (6) on $D$ 14: procedure MPC OPTIMIZATION WITH PREDICTOR
15: for each control step $t = 1,2,\ldots,I_{\text{max}}$ do
16: Get current state $s^{t}$ from the environment
17: Generate initial action $\mathbf{a}^{t}$ using policy $\pi_{\theta}(s^{t})$ 18: Initialize trajectory cost $J = 0$ 19: for $t = 0$ to horizon $T$ do
20: Predict next state $s^{t+1}$ using $f_{\eta}(s^{t},\mathbf{a}^{t})$ 21: Optimize $\mathbf{a}^{t+1}$ to minimize $C$ using predictions from $f_{\eta}$ 22: Update trajectory cost $J + = C(s^{t},\mathbf{a}^{t})$ 23: Apply optimized action sequence to the system

So far we have discussed the components of our method, we now explain the implementation of DeepSafeMPC in Fig.1. In the decentralized execution phase, we use the concatenated observations $\{ o _ { 1 } ^ { t } , o _ { 2 } ^ { t } , . . . , o _ { n } ^ { t } \}$ to approximate the states $s ^ { t }$ of the environment. The implementation process is shown in 1. The training process is included in the Algorithm 1.
