Lemma 7 [8, Remark 9] Consider the triangulation $\mathcal { T } ~ = ~ \{ \sigma _ { i } \} _ { i = 1 } ^ { \dot { m } \dot { \tau } }$ , where $\begin{array} { r } { \sigma _ { i } \stackrel { \cdot } { = } \ c o ( \{ x _ { i , j } \} _ { j = 0 } ^ { n } ) } \end{array}$ , and a set $\mathbf { W } = \{ W _ { x } \} _ { x \in \mathbb { E } _ { T } } \subset \mathbb { R } .$ , where $W ( x ) \stackrel { \cdot } { = } W _ { x } , \forall x \in \mathbb { E } \tau$ . For simplex $\sigma _ { i } ,$ let $\mathbf { X } _ { i } ~ \in ~ \mathbb { R } ^ { n \times n }$ be a matrix that has $x _ { i , j } - x _ { i , 0 }$ as its j-th row and $\bar { W } _ { i } \in { \mathbb R } ^ { n }$ be a vector that has $W _ { x _ { i , j } } ~ - ~ W _ { x _ { i , 0 } } ,$ as its $j - t h$ element. The function $W ( x ) = x ^ { \top } \mathbf { X } _ { i } ^ { - 1 } \bar { W } _ { i }$ , is the unique CPA interpolation of W on T for $x \in \sigma _ { i }$ .

Like in [7], Taylor’s Theorem is used to bound the change between a function $\zeta \in \mathcal { C } ^ { 2 }$ when evaluated at an arbitrary point versus the vertices on a simplex.

Theorem 8 (Taylor’s Theorem [6]) Consider $\zeta : \mathbb { R } ^ { n } \to \mathbb { R } ^ { n }$ , where $\zeta \in \mathcal { C } ^ { 2 }$ . For any $\boldsymbol { x } , \boldsymbol { y } \in \mathbb { R } ^ { n }$ ,

$$\zeta^ {(k)} (x) = \zeta^ {(k)} (y) + \langle \nabla \zeta^ {(k)} (x), x - y \rangle + \frac {1}{2} \overline {{\mathbf {H}}} _ {\zeta^ {(k)}} (x, y), \tag {6}$$

where $\overline { { \mathbf { H } } } _ { \zeta ^ { ( k ) } } ( x , y ) = \langle \mathbf { H } _ { \zeta ^ { ( k ) } } ( z ) ( x - y ) , ( x - y ) \rangle$ and z is some convex combination of x and y.

Let $\overline { { \mathbf { H } } } _ { \zeta } ( x , y ) = \left[ \overline { { \mathbf { H } } } _ { \zeta ^ { ( 1 ) } } ( x , y ) \mathbf { \Lambda } . . . \overline { { \mathbf { H } } } _ { \zeta ^ { ( n ) } } ( x , y ) \right] ^ { \top }$ be known as the Taylor remainder.

Although storage functions must satisfy more complicated constraints than Lyapunov functions, the error bounds used to establish CPA Lyapunov functions in [7,8] will be useful here.

Lemma 9 [7, Proposition 2.2 and Lemma 2.3] Consider $\Omega \in \Re ^ { n }$ . Let $\zeta { : } \Omega {  } \mathbb { R } ^ { n }$ where $\zeta \in \mathcal { C } ^ { 2 } ( \Omega )$ . Then, for any $: \in \sigma = c o ( \{ x _ { j } \} _ { j = 0 } ^ { n } ) \in \Omega$ ,
