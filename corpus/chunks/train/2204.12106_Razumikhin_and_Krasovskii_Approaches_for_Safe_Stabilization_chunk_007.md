A function α : $\mathbb { R } ^ { + } \to \mathbb { R } ^ { + }$ is of class  if it is continuous, $\alpha ( 0 ) = 0$ , and strictly increasing;
it is of class $\mathcal { K } _ { \infty }$ if it is of class and unbounded.
A continuous function $\alpha : \mathbb { R }  \mathbb { R }$ is of class  if it is strictly increasing and $\alpha ( 0 ) = 0$ .
A function $\beta : \mathbb { R } ^ { + } \times \mathbb { R } ^ { + } \to \mathbb { R } ^ { + }$ is of class $\mathcal { K L } \operatorname { i f } \beta ( s , t )$ is of class K for any fixed $t \geq 0$ and $\beta ( s , t ) \to 0 \mathrm { a s } t \to \infty$ for any fixed $s \geq 0$ .

In this paper, we focus on nonlinear time-delay control systems with the following dynamics:

$$\dot {x} (t) = f (x _ {t}) + g (x _ {t}) u, \quad t > 0,x (t) = \xi (t), \quad t \in [ - \Delta , 0 ], \tag {1}$$

where $x \in \mathbb { R } ^ { n }$ is the system state, $x _ { t } ( \theta ) = x ( t + \theta ) \in \mathbb { R } ^ { n }$ is the time-delay state with $\theta \in [ - \Delta , 0 ]$ , where $\Delta > 0$ is the upper bound of time delays. The initial state is $\xi \in \ C ( [ - \Delta , 0 ] , \mathbb { X } _ { 0 } )$ with $\mathbb { X } _ { 0 } ~ \subset ~ \mathbb { R } ^ { n }$ and kξk being bounded. The control input u takes value from the set $\mathbb { U } \subseteq \mathbb { R } ^ { m }$ . Here, the input function is not specified explicitly due to the allowance for its dependence on the current state only $( \mathrm { i . e . , } u : \mathbb { R } ^ { + } \to \mathbb { U } )$ or the timedelay trajectory $( \mathrm { i . e . , } u : C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } ) \to \mathbb { U } )$ . The functionals $f : C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } ) \to \mathbb { R } ^ { n }$ and $g : C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } ) \to \mathbb { R } ^ { n \times m }$ are assumed to be continuous and locally Lipschitz, which guarantees the existence of the unique solution to the system (1); see [19, Section 2]. Let $f ( 0 ) = 0 .$ To consider the stabilization problem of the system (1), we assume that the origin is included in the initial set ${ \mathbb X } _ { 0 }$ .

Definition 1 ([32]). Given a control input $u \in \mathbb { R } ^ { m }$ , the system (1) is globally asymptotically stable (GAS), if there exists $\beta \in$ such that $| x ( t ) | \leq \beta ( \| \xi \| , t )$ for all $t \geq 0$ and all bounded $\xi \in \mathbf { \Xi }$ $C ( [ - \Delta , 0 ] , \mathbb { R } ^ { n } )$ ; the system (1) is semi-globally asymptotically stable (semi-GAS), if there exists $\beta \in \mathcal { K L }$ such that |x(t)| ≤ $\beta ( \| \xi \| , t )$ for all $t \geq 0$ and all $\xi \in { \cal C } ( [ - \Delta , 0 ] , \mathbb { X } _ { 0 } )$ .
