Figure 6: Training of the fishing problem with multiple controls with $N = 1 0 .$ . The schedules are $\gamma _ { n } = 2 \mathrm { e } - 3$ and $\sigma _ { n } = 2 \mathrm { e } - 3$ for Adam and $\gamma _ { n } = 5 \mathrm { e } - 1$ and $\sigma _ { n } = 5 \mathrm { e } - 3$ for Adadelta, for epochs 0 to $4 0 \ \gamma _ { n }$ is divided by 10 and $\sigma _ { n }$ is set to 0 beyond.

where the loss function $\ell : \mathbb { R } \to \mathbb { R }$ is continuous, non-decreasing and convex. This is the case in particular for the entropic risk measure where $\ell ( x ) = - \exp ( - \lambda x )$ and the conditional value at risk measure where $\ell ( x ) =$ $( 1 - \alpha ) ^ { - 1 } \operatorname* { m a x } ( x , 0 )$ . Then (4.1) can be rewritten as

$$\inf _ {u, w} J (u, w) := \mathbb {E} \left[ w + \ell \left(Z - \sum_ {k = 0} ^ {N - 1} \langle u _ {t _ {k}}, S _ {t _ {k + 1}} - S _ {t _ {k}} \rangle + \sum_ {k = 0} ^ {N} \langle c _ {t r}, S _ {t _ {k}} * | u _ {t _ {k}} - u _ {t _ {k - 1}} | \rangle - w\right) \right]. \tag {4.3}$$

In the numerical experiments, we analyse the problem of hedging in a Heston model as described in [BGTW19, Section 5]. For even $d _ { 1 } .$ , we consider $d _ { 1 } ^ { \prime } : = d _ { 1 } / 2$ independent Heston models where the price and volatility processes are described by the following SDEs for $1 \leq i \leq d _ { 1 } ^ { \prime }$ :

$$d S _ {t} ^ {1, i} = \sqrt {V _ {t} ^ {i}} S _ {t} ^ {1, i} d B _ {t} ^ {i}, \quad S _ {0} ^ {1, i} = s _ {0} ^ {i}, \tag {4.4}d V _ {t} ^ {i} = a ^ {i} (b ^ {i} - V _ {t} ^ {i}) d t + \eta^ {i} \sqrt {V _ {t} ^ {i}} d W _ {t} ^ {i}, \quad V _ {0} ^ {i} = v _ {0} ^ {i}, \tag {4.5}$$

where $a , b , \eta , s _ { 0 } , v _ { i } \in ( \mathbb { R } ^ { + } ) ^ { d _ { 1 } ^ { \prime } }$ and for each $1 \leq i \leq d _ { 1 } ^ { \prime } , B ^ { i }$ and $W ^ { i }$ are standard Brownian motions with correlation $\rho ^ { i } \in [ - 1 , 1 ]$ . The volatility V itself is not tradable directly but only through options on variance modelled by the following variance swap:

$$S _ {t} ^ {2, i} := \mathbb {E} \left[ \int_ {0} ^ {T} V _ {s} ^ {i} d s \mid \mathcal {F} _ {t} \right] = \int_ {0} ^ {t} V _ {s} ^ {i} d s + L ^ {i} (t, V _ {t} ^ {i}), \tag {4.6}L ^ {i} (t, v) := \frac {v - b ^ {i}}{a ^ {i}} \left(1 - e ^ {a ^ {i} (T - t)}\right) + b ^ {i} (T - t). \tag {4.7}$$

The payoff is given by

$$Z = \sum_ {i = 1} ^ {d _ {1} ^ {\prime}} \left(S _ {T} ^ {1, i} - K ^ {i}\right) _ {+}$$

where $K \in ( \mathbb { R } ^ { + } ) ^ { d _ { 1 } ^ { \prime } }$ . We consider the convex risk measure associated to the value-at-risk i.e. associated to the loss function

$$\ell (x) = \frac {1}{1 - \alpha} \max (x, 0).$$

In the experiments we choose
