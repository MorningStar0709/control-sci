# UNIQUENESS OF THE IOC SOLUTION

The uniqueness of solution to the IOC problem obtained using the hard-constrained method will be reviewed here and shown for the swarm inverse optimal control problem.

Remark 1: The solution of problem (29) or (40) exists and is unique if W is unique and positive definite [39, Theorem 3]. In optimal control problems, generally some of the parameters in the vector c are fixed and known. Hence, this condition reduces to the principal minor of W which excludes the columns for the known parameters [39, Theorem 3]. An alternative compact formulation [38] is: Given a vector $c \in \mathbb { R } ^ { k }$ ; a vector $\bar { c } \in \mathbb { R } ^ { k }$ can be found, populated only by non-zero elements at the corresponding indices for which the elements in c are known. To ensure existence and uniqueness of the solution of problem (29) or (40), it is sufficient that $N _ { h } ^ { T } W N _ { h }$ is full rank for

$$N _ {h} = \ker \left(\operatorname{diag} (\bar {c})\right). \tag {44}$$

The theorem below is for uniqueness of the IOC solution.

Theorem 1: Given the system dynamics $\begin{array} { r l } { \dot { X } _ { i } ( t ) } & { { } = } \end{array}$ $f ( X _ { i } ( t ) , U _ { i } ( t ) )$ for a single flight data of a follower pigeon or $\dot { X } _ { i _ { j } } ( t ) = f ( X _ { i _ { j } } ( t ) , U _ { i _ { j } } ( t ) )$ for multiple flight data of a pigeon, the basis function vector $\phi ( X _ { i } ( t ) , U _ { i } ( t ) )$ or ϕ( $X _ { i _ { j } } ( t ) , U _ { i _ { j } } ( t ) )$ , and a specific system trajectory $X _ { i } ( t )$ or $X _ { i _ { j } } ( t )$ and control signal $U _ { i } ( t )$ or $U _ { i _ { i } } ( t ) , \forall t _ { 0 } \leq t \leq t _ { f } .$ , measured from a follower pigeon $i \in \{ M , \overleftarrow { G } , B , D , H , L , I , C , J \}$ from a single flight data of an optimal trajectory tracking control problem (4) or flight number $j$ of multiple flights for the optimal problem (32), the hard-constrained IOC problem (29) or (40) will have a unique solution, if $N _ { h } ^ { T } W N _ { h }$ is non-singular [38, Theorem 1].
