Figure 1: A scenario tree with nine scenarios, prediction horizon N, and robust horizon $N _ { R } = 2 .$ . The dashed lines represent all the nodes in between $t _ { k + 2 }$ and $t _ { k + N }$ .

The initial condition (2c) is the current state $x _ { k }$ . The equations (2b) are constraints predicting system dynamics, and (2d) are the bounds on state and input variables. The terminal constraints are in (2e), where $\mathbb { X } _ { f } \subset \mathbb { X }$ is the terminal region set.

The standard MPC receives $x _ { k }$ and solves (2) for an optimal input sequence $\nu _ { [ 0 , N - 1 ] } ^ { \star }$ and their corresponding state predictions $\mathbf { z } _ { ( 0 , N ] } ^ { \star }$ in the time interval $( t _ { k } , \ t _ { k + N } ]$ , at every iteration k. The first stage input from the sequence is applied to the plant as $u _ { k }$ ,. Then the prediction horizon is shifted one step, and the problem is re-optimized with the new state $x _ { k + 1 }$ .

Since the current optimal input is obtained with respect to $x _ { k } ,$ , the standard MPC policy yields a feedback control law $u _ { k } = \kappa ( x _ { k } )$ , where  : $\mathbb { R } ^ { n _ { x } } \mapsto \mathbb { R } ^ { n _ { u } }$ . This gives standard MPC with limited robustness to model uncertainty [11, 12, $1 3 , 1 4 ]$ κ κ. However, standard MPC exhibits a substantial decline in performance for significant plant-model mismatch due to uncertainty.
