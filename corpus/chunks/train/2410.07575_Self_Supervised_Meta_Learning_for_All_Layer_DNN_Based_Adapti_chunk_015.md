where ϵ is the observation noise of disturbance $d ,$ and function $\mathcal { M } = \mathrm { d i a g } ( M , \gamma ^ { - 1 } I )$ .

By Cauchy-Schwartz inequality we obtain the following inequality:

$$\dot {V} \leq - 2 \rho V + 2 \sqrt {\frac {V}{\lambda_ {\min} (\mathcal {M})}} D \tag {24}$$

Following the procedure in [5], the tracking error and parameter estimation error exponentially converge to the error ball

$$
\lim _ {t \rightarrow \infty} \left\|\left[\begin{array}{l l}s&\tilde {\theta}\end{array}\right] ^ {\top} \right\| \leq \frac {D}{\rho \lambda_ {\min} (\mathcal {M})} \tag {25}
$$

Thus, the state tracking error q˜ also converges to an error ball exponentially fast when s is exponentially stable.
