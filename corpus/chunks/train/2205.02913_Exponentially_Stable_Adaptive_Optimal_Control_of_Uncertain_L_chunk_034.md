The parameter ?? should be chosen so that the decrease rate of the exponent $e ^ { - l \left( t - t _ { r } ^ { + } \right) }$ from (23) is substantially less than the assumed settling time of transients in (4), i.e. $\begin{array} { r } { l \ll \frac { t _ { s s } } { ( 3 \div 5 ) } } \end{array}$ , where $t _ { s s }$ is the assumed transient duration in the system (4). Otherwise, if a fast decrease rate of $e ^ { - l \left( t - t _ { r } ^ { + } \right) }$ is chosen, the excitation time range of $\overline { { \varphi } } \left( t \right)$ may be substantially shorter than the one of the original regressor $\Psi \left( t \right)$ , and the entire adaptive control scheme will quickly lose its awareness to the new data from the system. In fact, Assumption 1 is needed to guarantee that there can be found no function ${ \overline { { \Psi } } } _ { i } \left( t \right) , i = { \overline { { 1 , n + m } } } \operatorname { i n } { \overline { { \Psi } } } \left( t \right)$ such that:

$$\overline {{{\Psi}}} _ {i} (t) = e ^ {- l \left(t - t _ {r} ^ {+}\right)} \text { or } \exists \ell_ {i} \neq 0 \sum_ {i = 1} ^ {m + n} \ell_ {i} \overline {{{\Psi}}} _ {i} (t) + \ell_ {m + n + 1} e ^ {- l \left(t - t _ {r} ^ {+}\right)} = 0, \tag {36}$$

where $\overline { { \Psi } } _ { i } \left( t \right)$ is the $i ^ { \mathrm { { t h } } }$ element of the regressor $\overline { { \Psi } } \left( t \right)$

Therefore, Assumption 1 is not restrictive and usually met for almost all values of ??.

The parameters ?? and $k _ { 0 }$ define the rate of the excitation propagation throughout the parameterizations (20), (25) and must be chosen on the basis of the required response of the adaptive control system.

The parameter $k _ { 1 }$ is used to make the regressor $\varphi \left( t \right)$ values be of the desired magnitude order over the excitation time range. For example, let it be known that $\forall t \in \left[ t _ { r } ^ { + } ; t _ { e } \right] 1 0 ^ { - 5 } \leq d e t \left\{ H \left[ \overline { { { \varphi } } } \left( t \right) \overline { { { \varphi } } } ^ { \mathrm { T } } \left( t \right) \right] \right\} \leq 1 0 ^ { - 3 }$ , then the choice $k _ { 1 } = 1 0 ^ { 5 }$ guarantees that $\forall t \in \left[ t _ { r } ^ { + } ; t _ { e } \right] \ 0 . 5 \leq \varphi \left( t \right) \leq 0 . 9 9$ . Such normalization of the regressor $\varphi \left( t \right)$ makes it sufficiently easier to choose the parameter ?? of the adaptive law (32) and can be very useful from the point of view of the computational stability of the proposed control system for its successful implementation on the embedded hardware.

The parameter ?? defines the time range, when the filter (31) is sensitive to the new input data, and ensures the boundedness of the regressor Ω (??), function $\Upsilon \left( t \right)$ and disturbance $\varsigma ( t )$ . The parameter ?? is advised to be calculated as $\begin{array} { r } { \sigma = \frac { t _ { s s } } { ( 3 \div 5 ) } } \end{array}$ ??????(3÷5) , as in such case the sensitivity of the proposed adaptive control scheme to new data on $y _ { \theta } \left( t \right)$ and $\Delta \left( t \right)$ is ensured strictly over the assumed time range, when the transients take place in the system (3).
