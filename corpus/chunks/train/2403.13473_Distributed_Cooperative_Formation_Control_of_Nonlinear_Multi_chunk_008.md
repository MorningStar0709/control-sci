# B. Neural Networks

As a general tool to approximate smooth functions, neural networks (NNs) have three parts, including the input layer $( x _ { i } , v _ { i } )$ , the hidden layers of ℎ $( h \in N ^ { * } )$ neurons and the output layer $u _ { i } .$ It can be characterized by a weight vector ???? and other variables such as neural network basic function ???? and an approximation error ????.

A smooth function $g ( \chi ) \colon R ^ { k } \to R ^ { l } ( k , l \in N ^ { * } )$ can be approximated by a neural network of this type, if the input vector $\chi$ is restricted to a compact set $S \subseteq R ^ { k }$ . The approximation can be denoted by

$$g (\chi) = W ^ {T} \Phi (\chi) + b (\chi) \tag {3}$$

where $W \in \mathbb { R } ^ { h } , \ \phi ( \cdot ) \in \mathbb { R } ^ { h }$ consists of the weight vector of the neural network and its basic function, and the estimated error is limited by a positive constant, that is, ∀???? ∈ $\mathbb { R } ^ { k } , | b ( \chi ) | \le b _ { M }$ .

Lemma 3. Let $g ( \xi _ { i } , t )$ represent a smooth function. $g ( \xi _ { i } , t )$ can be approximated by a neural network with ℎ neurons in its hidden layer.

$$g (\xi_ {i}, t) = W _ {i} ^ {T} \Phi (\xi_ {i}, t) + b _ {i} (4)$$

where $\boldsymbol { \Phi } ( \xi _ { i } , t ) \in \mathbb { R } ^ { h }$ is the basic function,

$$W _ {i} = a r g \min _ {w \in \mathbb {R} ^ {q \times h}} \left\{s u p _ {x \in \Omega} \| f (x) - W _ {i} ^ {T} \Phi \| \right\}, W _ {i} \in \mathbb {R} ^ {h} \text {is the}$$

weight vector, and $b _ { i } \in$ ℝ is the error of estimation, which is bounded by a constant $b _ { M } ( b _ { M } \in \mathbb { R } , b _ { M } \geq 0 )$ .
