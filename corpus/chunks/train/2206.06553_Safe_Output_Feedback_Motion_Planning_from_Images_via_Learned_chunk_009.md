# 3.2 Control/observer contraction metrics (CCMs/OCMs)

As our approach builds on contraction theory, we provide an overview here. Control contraction theory [18] studies incremental stabilizability by measuring the distances between trajectories w.r.t. a Riemannian metric $M _ { c } : \mathcal { X }  \mathbb { S } _ { n _ { x } } ^ { > 0 }$ . For (1a) if $w _ { x } \equiv 0 ,$ , a sufficient condition [22] for $M _ { c }$ to be called a control contraction metric (CCM) is:

$$B _ {\perp} ^ {\top} \left(- \partial_ {f} W _ {c} (x) + A (x) W _ {c} (x) + W _ {c} (x) A (x) ^ {\top} + 2 \lambda_ {c} W _ {c} (x)\right) B _ {\perp} \preceq 0 \tag {2a}B _ {\perp} ^ {\top} \left(\partial_ {B ^ {j}} W _ {c} (x)\right) B _ {\perp} = 0, \quad j = 1 \dots n _ {u}, \tag {2b}$$

for all $x \in D _ { c }$ , where $\begin{array} { r } { W _ { c } ( x ) \doteq M _ { c } ^ { - 1 } ( x ) , A ( x ) = \frac { \partial f ( x ) } { \partial x } } \end{array}$ , and $B _ { \perp }$ is a basis for the null-space of B. The CCM also defines a controller $u : x \times x \times u \to \mathcal { U }$ , which takes the current state x(t) and a state/control $x ^ { * } ( t ) , u ^ { * } ( t )$ on the nominal state/control trajectory being tracked $x ^ { * } : [ 0 , T ]  \mathcal { X } , u ^ { * } : [ 0 , T ]  \mathcal { U } _ { \mathit { m } }$ , and returns a u that contracts $x$ towards $x ^ { * }$ at rate $\lambda _ { c } > 0$ . The controller $u ( x , x ^ { * } , u ^ { * } )$ can be computed directly via $W _ { c } ( x )$ (cf. Sec. 4.2). If $w _ { x } \equiv 0$ , for any nominal $x ^ { * } ( t )$ , applying $u ( x , x ^ { * } , u ^ { * } )$ renders the system closed-loop IES, i.e., $\| x ( t ) - x ^ { * } ( t ) \| \leq \alpha _ { c } \| x ( 0 ) - x ^ { * } ( 0 ) \| e ^ { - \lambda _ { c } t }$ for $\alpha _ { c } >$ 0. For bounded $w _ { x } , \left( 1 \mathrm { a } \right)$ remains in a tube around $x ^ { * } ( t )$ ; we exploit this in Sec. 4.2. Contraction also analyzes the convergence of state observers [7,17], i.e., whether a state estimate ${ \hat { x } } ( t )$ approaches the true state $x ( t )$ . Consider the nominal closed-loop system $\dot { x } = f ( x ) + B u ( \hat { x } , x ^ { * } , u ^ { * } )$ with noiseless observations $y \ = \ h ( x , \theta )$ and a nominal observer

$$\dot {\hat {x}} = f (\hat {x}) + B u \left(\hat {x}, x ^ {*}, u ^ {*}\right) + \frac {1}{2} \rho (\hat {x}) M _ {e} (\hat {x}) C (\hat {x}) ^ {\top} (y - h (\hat {x}, \theta)) \tag {3}$$

system, where is called an obs $\begin{array} { r } { C ( x ) \ = \ \frac { \partial h ( x , \theta ) } { \partial x } , \rho ( x ) \ge \ 0 } \end{array}$ is a multiplier term, andM), which should satisfy $M _ { e } : \mathcal { X }  \mathbb { S } _ { n _ { x } } ^ { > 0 }$
