Theorem 1. Suppose that the indicator (10) can correctly determine whether $A _ { d } \in S _ { d } .$ . When the observation horizon $T \to \infty ,$ the inference error of estimator (12) satisfies

$$
\left\{\begin{array}{l}\operatorname * {P r} \left\{\lim _ {T \rightarrow \infty} \| \widehat {A} _ {d} (T) - A _ {d} \| _ {F} ^ {2} = 0 \right\} = 1, \text {   if   } A _ {d} \in \mathcal {S} _ {d}\\\operatorname * {P r} \left\{\lim _ {T \rightarrow \infty} \| \widehat {A} _ {d} (T) - A _ {d} \| _ {F} ^ {2} = \boldsymbol {O} (1) \right\} = 1, \text {   otherwise   }\end{array}. \right. \tag {13}
$$

Proof. First, we consider the constant cooperation pattern case when $A _ { d } \in { \cal S } _ { d }$ . Note that the sample matrix $\Sigma _ { 1 } ( T )$ can be written as

$$
\begin{array}{l} \Sigma_ {1} (T) = \frac {1}{T} \sum_ {t = 1} ^ {T} \left(A _ {d} y _ {t - 1} + v _ {t} - A _ {d} v _ {t - 1}\right) y _ {t - 1} ^ {\intercal} \\ = A _ {d} \Sigma_ {0} (T) + \frac {1}{T} (\Phi_ {T} ^ {+} - A _ {d} \Phi_ {T} ^ {-}) (Y _ {T} ^ {-}) ^ {\intercal} \\ = A _ {d} \Sigma_ {0} (T) + \frac {\Phi_ {T} ^ {+} (X _ {T} ^ {-}) ^ {\intercal}}{T} + \frac {\Phi_ {T} ^ {+} (\Phi_ {T} ^ {-}) ^ {\intercal}}{T} \\ - \frac {A _ {d} \Phi_ {T} ^ {-} (X _ {T} ^ {-}) ^ {\intercal}}{T} - \frac {A _ {d} \Phi_ {T} ^ {-} (\Phi_ {T} ^ {-}) ^ {\intercal}}{T}. \tag {14} \\ \end{array}
$$

Notice that the elements in $X _ { T } ^ { - }$ are bounded when $A _ { d } \in S _ { d } ,$ and $\Phi _ { T } ^ { + }$ is independent of $X _ { T } ^ { - }$ and $\Phi _ { T } ^ { - }$ . Then, when $T \to \infty ,$ we have with probability one that

$$
\begin{array}{l} \Sigma_ {1} (\infty) = A _ {d} \Sigma_ {0} (\infty) + 0 + 0 - 0 - A _ {d} (I _ {N} \otimes \Gamma) \\ \Rightarrow A _ {d} = \Sigma_ {1} (\infty) \left(\Sigma_ {0} (\infty) - I _ {N} \otimes \Gamma\right) ^ {- 1}, \tag {15} \\ \end{array}
$$

which proves the first conclusion.

Next, we focus on the case when $A _ { d } \notin { \cal S } _ { d } .$ . In this situation, the term lim $1 _ { T  \infty } \Phi _ { T } ^ { + } ( X _ { T } ^ { - } ) ^ { \intercal } / T$ will not converge to zero, and thus (14) no longer holds. To deal with this issue, we consider the singular value decomposition (SVD) of $Y _ { T } ^ { - }$ ,

$$Y _ {T} ^ {-} = U \Lambda V ^ {\intercal}, \tag {16}$$
