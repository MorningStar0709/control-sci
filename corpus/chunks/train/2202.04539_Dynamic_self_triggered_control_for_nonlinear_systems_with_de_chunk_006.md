$$G (\xi)
:= \left\{ \begin{array}{l l} \big (x ^ {\top}, e ^ {\top}, - e ^ {\top}, S (\eta , x, e) ^ {\top}, 0, \Gamma (x, e, \eta), 1 \big) ^ {\top} & \ell = 0 \\ \big (x ^ {\top}, (s + e) ^ {\top}, - (s + e) ^ {\top}, \eta^ {\top}, \tau , \tau_ {\max}, 0 \big) ^ {\top} & \ell = 1 \end{array} \right.,
$$

$C : = \left\{ \left( \ell = 0 \wedge \tau \in \left[ 0 , \tau _ { \operatorname* { m a x } } \right] \right) \vee \left( \ell = 1 \wedge \tau \in \left[ 0 , \tau _ { \operatorname* { m a d } } \right] \right) \right\}$ and $\begin{array} { r l r } { D } & { { } : = } & { \left\{ \left( \ell = 0 \land \tau = \tau _ { \operatorname* { m a x } } \right) \lor \left( \ell = 1 \land \bar { \tau } \in [ 0 , \tau _ { \operatorname* { m a d } } ] \right) \right\} } \end{array}$ , $e ( 0 , 0 ) ~ = ~ - x ( 0 , 0 ) , ~ s ( 0 , 0 ) ~ = ~ x ( 0 , 0 ) , ~ \eta ( 0 , 0 ) ~ \in ~ \mathbb { R } ^ { \bar { n } _ { \eta } }$ , $\tau ( 0 , 0 ) ~ = ~ 0 , ~ \tau _ { \mathrm { m a x } } ( 0 , 0 ) ~ = ~ \Gamma ( x ( 0 , 0 ) , e ( 0 , 0 ) , \eta ( 0 , 0 ) )$ and $\ell ( 0 , 0 ) = 1 .$ Here the choice of $s ^ { + }$ is made as in Heemels et al. (2010) to simplify analysis later.

The jumps of $\mathcal { H } _ { S T C }$ represent sampling events and update events in an alternating fashion. This justifies the choice of $k = 2 j \in \mathbb { N } _ { e }$ , where j describes the jumps of the hybrid system, to characterize sampling instants. We further describe the hybrid time before the sampling or update event at time $t _ { j }$ by $r _ { j } ~ = ~ ( t _ { j } , j { - } 1 )$ ) and the hybrid time directly after the event by $r _ { j } ^ { + } = ( t _ { j } , j )$ .

In this paper, our goal is to design functions Γ and S, such that asymptotic stability of the origin of $\mathcal { H } _ { S T C }$ is guaranteed for a region of attraction R according to the following definition.

Definition 1. For the hybrid system $\mathcal { H } _ { S T C }$ , the set $\{ ( x , e , s , \eta , \tau , \tau _ { \mathrm { m a x } } , \ell ) : x = 0 , e = 0 , s = 0 , \eta = 0 \}$ is asymptotically stable with region of attraction $\mathcal R \subseteq \mathbb R ^ { \dot { n } _ { x } }$ , if there exists $\beta \in { \mathcal { K L L } }$ such that all corresponding maximal solutions ξ with $x ( 0 , 0 ) \in \mathcal { R }$ are t−complete and satisfy for

$$
\text {all} (t, j) \in \text {dom} \xi \left\| \left[ \begin{array}{c} x (t, j) \\ e (t, j) \\ s (t, j) \\ \eta (t, j) \end{array} \right] \right\| \leq \beta \left(\left\| \left[ \begin{array}{c} x (0, 0) \\ e (0, 0) \\ s (0, 0) \\ \eta (0, 0) \end{array} \right] \right\|, t, j\right).
$$
