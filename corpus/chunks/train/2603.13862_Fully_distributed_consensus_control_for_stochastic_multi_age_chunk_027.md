Remark IV.1. In fact, the function $\Sigma _ { i }$ admits other design forms. Especially, we can choose $\Sigma _ { i } ( t ) = k _ { 1 } ( k _ { 2 } + \sigma _ { i } ( t ) ) ^ { \mu }$ with $\mu > 1 , k _ { 1 } , k _ { 2 } \geq 1 , c _ { i } ( t ) \geq 1 , \Sigma _ { i } ( t ) \geq 1$ . By selecting the Lyapunov function $\boldsymbol { \tilde { V } } ( \xi ( t ) , t ) = \boldsymbol { V } ( \xi ( t ) , t )$ , it can be deduced that $\begin{array} { r } { \mathfrak { L } \tilde { V } ( \xi ( t ) , t ) = \sum _ { i = 1 } ^ { M } r _ { i } [ \eta _ { 1 } ^ { l } \Sigma _ { i } ( t ) \sigma _ { i } ( t ) + 2 \eta _ { 2 } ^ { l } c _ { i } ( t ) - 2 \eta _ { 2 } ^ { l } \psi _ { i } ] \dot { c } _ { i } ( t ) + } \end{array}$ $\begin{array} { r l } { \eta _ { 1 } ^ { l } \sum _ { i = 1 } ^ { M } r _ { i } c _ { i } ( t ) \Sigma _ { i } ( t ) \mathcal { \bar { L } } \sigma _ { i } ( t ) + \sum _ { i = M + 1 } ^ { N } s _ { i - M } [ \eta _ { 1 } ^ { f } \Sigma _ { i } ( t ) \sigma _ { i } ( t ) + } & { { } } \end{array}$ $\begin{array} { r } { 2 \eta _ { 2 } ^ { f } c _ { i } ( t ) - 2 \eta _ { 2 } ^ { f } \psi _ { i } \rvert \dot { c } _ { i } ( t ) + \eta _ { 1 } ^ { f } \sum _ { i = M + 1 } ^ { N } s _ { i - M } c _ { i } ( t ) \Sigma _ { i } ( t ) \mathfrak { L } \sigma _ { i } ( t ) } \end{array}$ . By the similar steps of the above proof of Theorem IV.1, the m.s. and a.s. consensus of (1) can be solved by the protocol (2) with $\Sigma _ { i } ( t ) = k _ { 1 } ( k _ { 2 } + \sigma _ { i } ( t ) ) ^ { \mu }$ , for $\mu > 1 , k _ { 1 } , k _ { 2 } \ \geq \ 1$ and $c _ { i } ( 0 ) \geq 1 , \Sigma _ { i } ( 0 ) \geq 1$ . In particular, the feedback control structure proposed in [35] is recovered as a special case of this protocol by setting $\mu = 3$ and $k _ { 1 } = k _ { 2 } = 1$ .

Furthermore, for the case of $\mu = 1$ and $\gamma = 0$ , the control protocol (2) can be written as

$$
\begin{array}{l} u _ {i} (t) = k _ {1} \left(k _ {2} c _ {i} (t) + \sigma_ {i} (t)\right) \mathcal {K} \xi_ {i} (t), \\ \therefore (t) = \xi^ {T} (t) \Gamma \xi_ {i} (t) \end{array} \tag {22}
\dot {c} _ {i} (t) = \xi_ {i} ^ {T} (t) \Gamma \xi_ {i} (t),$$

where $\begin{array} { r } { k _ { 1 } , k _ { 2 } ~ > ~ 0 , ~ \xi _ { i } ( t ) ~ = ~ \sum _ { i \in N _ { i } } a _ { i j } ( x _ { i } ( t ) - x _ { j } ( t ) ) } \end{array}$ and $\sigma _ { i } ( t ) = \xi _ { i } ^ { T } ( t ) P \xi _ { i } ^ { T } ( t )$ . Then we have the following theorem.

Theorem IV.2. Suppose the directed graph $\tilde { \mathcal { G } }$ contains a spanning tree. The m.s. and a.s. consensus of (1) can be solved by the protocol (22) with $\boldsymbol { \mathcal { K } } = - B ^ { T } \boldsymbol { P }$ , and $\Gamma = P B B ^ { T } P$ , where $P$ is the solution to the SARE (9).

Proof. Substituting the control protocol (22) into (1) yields the following closed-loop system like (4)
