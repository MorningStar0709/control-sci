We now prove the claim that there exists a finite stopping time $t _ { 1 } > 0$ such that for all $t \geq t _ { 1 }$ , the projection is no longer active $\mathrm { i . e . , } \hat { \theta } _ { t + 1 } = \hat { \theta } _ { t } + \mu _ { t } ^ { - 1 } d _ { t } P _ { t + 1 } \phi _ { t } [ x _ { t + 1 } -$ $f ( \widehat { \theta } _ { t } ^ { \top } \phi _ { t } ) ] ^ { \top } , \forall t > t _ { 1 }$ . Indeed, from $\left( \mathrm { { A . 4 } } \right)$ , there exists a finite stopping time $t _ { 0 } \geq 0$ such that

$$\sum_ {\tau = t _ {0}} ^ {\infty} \left\| a _ {\tau} d _ {\tau} P _ {\tau} \phi_ {\tau} \left(\psi_ {\tau} + w _ {\tau + 1}\right) ^ {\top} \right\| < \frac {\epsilon}{2}, \text { a.s., } \tag {A.5}$$
