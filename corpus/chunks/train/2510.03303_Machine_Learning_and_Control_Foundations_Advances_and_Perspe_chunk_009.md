where $\Pi ( \mu , \nu )$ denotes the set of measures π on $\mathbb { R } ^ { d } \times \mathbb { R } ^ { d }$ that couple $\mu , \nu \in \mathcal { P } _ { a c } ^ { c } ( \mathbb { R } ^ { d } )$ , two compactly supported probability measures on $\mathbb { R } ^ { d }$ .

Theorem 2.2 (Generalization bound, [29]). Assume that the activation function σ is L-Lipschitz. Then, for any $\Theta \in \mathbb { R } ^ { ( d + 2 ) \grave { P } }$ , we have

$$d _ {\mathrm{KR}} (m _ {\text { test }}, m _ {\text { pred }} (\Theta)) \leq \underbrace {d _ {\mathrm{KR}} (m _ {\text { train }} , m _ {\text { test }})} _ {\text { Irreducible error from datasets }} + r (\Theta), \tag {2.11}$$

where

$$r (\Theta) = \underbrace {\frac {1}{N} \sum_ {i \in [ N ]} | f _ {\text { shallow }} (x _ {i} , \Theta) - y _ {i} |} _ {\text { Training bias }} + \underbrace {d _ {\text { KR }} (m _ {X} , m _ {X ^ {\prime}}) \mathcal {L} \sum_ {j \in [ P ]} | w _ {j} | \| a _ {j} \|} _ {\text { Sensitivity term }}. \tag {2.12}$$

Furthermore, let $P \geq N$ , σ be the ReLU activation function (so that L = 1) and Ω be the unit ball in $\mathbb { R } ^ { d + 1 }$ . Then, for any $\epsilon \geq 0$ , the solution Θϵ of (Pϵ) satisfies

$$r (\Theta_ {\epsilon}) \leq \mathcal {U} (\epsilon) := \epsilon + d _ {\mathrm{KR}} (m _ {X}, m _ {X ^ {\prime}}) \text { val } (\mathrm{PR} _ {\epsilon}). \tag {2.13}$$

The irreducible error in (2.11) is independent of the parameters Θ, being completely determined by the training and testing datasets. In contrast, the residual term r(Θ) consists of two components depending on Θ : (1) the fidelity error ϵ on the training set, referred to as the training bias, and (2), the sensitivity or stability of the trained NN.

The second part of the theorem provides a sharp bound on the term r(Θ) for the optimal choice of the parameters Θϵ . In light of the estimate (2.13), the problem of minimizing the right-hand-side upper bound with respect to ϵ arises. The analysis of this problem is conducted in [29].

The main conclusion is that two scenarios have to be distinguished:
