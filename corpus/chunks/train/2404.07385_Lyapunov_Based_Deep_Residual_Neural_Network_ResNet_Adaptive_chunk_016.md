# III. STABILITY ANALYSIS

To facilitate the subsequent analysis, let $z \triangleq [ e ^ { T } , \widetilde { \theta } ^ { T } ] ^ { T } \in$ $\mathbb { R } ^ { \Psi }$ denote a concatenated state, where $\begin{array} { r l r l } { \Psi } & { { } \triangleq } & { n _ { \mathbf { \Gamma } } + } \end{array}$ $\Sigma _ { p = 1 } ^ { m } \Sigma _ { j = 0 } ^ { k _ { p } } L _ { p , j } L _ { p , j + 1 }$ kp . Consider the candidate Lyapunov function $\mathcal { V } _ { L } : \mathbb { R } ^ { \Psi }  \mathbb { R } _ { \ge 0 }$ defined as

$$\mathcal {V} _ {L} (z) \triangleq \frac {1}{2} e ^ {T} e + \frac {1}{2} \tilde {\theta} ^ {T} \Gamma^ {- 1} \tilde {\theta}, \tag {18}$$

which satisfies the inequality $\alpha _ { 1 } \left\| z \right\| ^ { 2 } \leq \gamma _ { L } \left( z \right) \leq \alpha _ { 2 } \left\| z \right\| ^ { 2 }$ , where $\alpha _ { 1 } , \alpha _ { 2 } ~ \in ~ \mathbb { R } _ { > 0 }$ are known constants. The universal function approximation property in (8) holds only on the compact domain Ω; hence, the subsequent stability analysis requires ensuring $x ( t ) \in \Omega$ for all $t \in \mathbb { R } _ { \geq 0 }$ . This is achieved by yielding a stability result which constrains z in a compact domain. Consider the compact domain $\mathcal { D } \triangleq \{ \varsigma \in \mathbb { R } ^ { \tilde { \Psi } }$ : $\| \varsigma \| < \kappa \}$ in which z is supposed to lie, where $\kappa \in \mathbb { R } _ { > 0 }$ is a bounding constant. The subsequent analysis shows that $z ( t ) \in \mathcal { D }$ for all $t \in \mathbb { R } _ { > 0 } , \mathrm { i f } \ z$ is initialized within the set $\begin{array} { r } { S \triangleq \left\{ \varsigma \in \mathbb { R } ^ { \Psi } : \| \varsigma \| < \sqrt { \frac { \alpha _ { 1 } } { \alpha _ { 2 } } } \kappa \right\} } \end{array}$ .

Taking the time-derivative of (4), substituting in (3) and (17), and substituting in (8) yields the closed-loop error system

$$\dot {e} = \Phi^ {\theta^ {*}} (x) - \Phi^ {\hat {\theta}} (x) + \varepsilon (x) - \sigma_ {e} e - \sigma_ {s} \operatorname{sgn} (e). \tag {19}$$
