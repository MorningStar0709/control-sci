# 4.4 Causes for CPSs

We define below the notion of causal model update, which is akin to an intervention in the discrete case.

Definition 17 (Causal model update). Given a signature $S _ { C } = \langle \mathcal { U } , \mathcal { V } , \mathcal { R } \rangle$ , a set of variables ${ \vec { X } } \subseteq \gamma$ and a set of trajectories $\vec { x } = \{ x \mid x \in T r a j s ( \vec { X } ) \}$ , an updated signature, denoted by $S _ { C } ^ { ' } { : }$ , is defined as $\langle \mathcal { U } , \mathcal { V } , \mathcal { R } ^ { \vec { X }  \vec { x } } \rangle$ , where $\mathcal { R } ^ { \vec { X }  \vec { x } }$ is the set of all trajectories $r \in \mathcal { R }$ such that $r \downarrow _ { \{ X \} } { = } _ { d o m ( x ) } x \downarrow _ { \{ X \} }$ , for each $x \in { \vec { x } }$ .

Given a causal model $M _ { C } = \langle S _ { C } , \{ \mathcal { F } _ { Y } ~ | ~ Y \in \mathcal { V } \} \rangle$ , an updated causal model, denoted by $M _ { \vec { X }  \vec { x } } ,$ is defined as $\langle S _ { C } ^ { ' } , \{ { \mathcal { F } } _ { Y } ^ { ' } \mid Y \in \mathcal { V } \} \rangle$ , where the updated function for $\mathcal { R } ^ { \vec { X }  \vec { x } }$ , denoted as $\mathcal { F } _ { Y } ^ { ' }$ , is defined on $\{ x \mathbin { \downarrow } _ { U \cup V \backslash \{ X \} } | x \in \mathcal { R } ^ { \vec { X }  \vec { x } } \}$ .

Intuitively, $M _ { \vec { X }  \vec { x } }$ is an updated causal model obtained by filtering the trajectories of the variables in $\dot { X }$ with the trajectories in ⃗x. A sequence of two causal model updates is denoted by MX⃗ ←⃗x,Y⃗ ←⃗y. $M _ { \vec { X }  \vec { x } , \vec { Y }  \vec { y } } .$

To determine cause, similarly to the discrete version, we use $( M , u )$ to represent a causal model and a context, which is represented by a trajectory $u \in T r a j s ( \mathcal { U } )$ over the exogenous variables. Now, consider a trajectory c that led to Φ where $c \downarrow u { = } _ { \mathrm { d o m } ( u ) } u .$ , then, a cause of Φ in $( M , u )$ , given in the format $( c  \vec { x } )$ , is determined as follows.

Definition 18 (Cause for cyber-physical systems). Given a causal model $M = \langle \langle \mathcal { U } , \mathcal { V } , \mathcal { R } \rangle , \mathcal { F } \rangle$ a setting $u \in T r a j s ( \mathcal { U } )$ , a trajectory $c : T r a j s ( \mathcal { U } \cup \mathcal { V } )$ such that $c \downarrow u { = } _ { \mathrm { d o m } ( u ) } u$ , and a set of trajectory slices ${ \vec { x } } = \{ x \ | \ x \in \ T r a j s ( \mathcal { V } ) \}$ , then $( c  \vec { x } )$ is a cause of Φ in $( M , u )$ when the following three conditions hold:
