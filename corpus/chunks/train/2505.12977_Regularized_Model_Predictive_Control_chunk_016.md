where $\mathrm { { } } P _ { 1 } \succ 0 .$ , as established in Theorem 2, step 1. Suppose the closed-loop system (32) is unstable, i.e., $\exists z \in \mathbb { C } , \quad \exists \rho ^ { \mathsf { H } } \in$ $\mathbb { C } ^ { n }$ such that $| z | \geq 1$ and $\rho K _ { x _ { 1 } } = z \rho .$ . By pre-multiplying equation (33) by ρ and post-multiplying by $\boldsymbol { \rho } ^ { \sf H }$ , we obtain $( \dot { 1 } - | z | ^ { 2 } ) \rho P _ { 1 } \rho ^ { \dot { \mathsf { H } } } \ = \ \rho ( \dot { K _ { u _ { 0 } } ^ { \mathsf { T } } } R K _ { u _ { 0 } } \dot { + } \mathsf { \bar { Q } } ) \rho ^ { \dagger }$ , where the lefthand side is NSD and the right-hand side is PSD. Thus, $\rho ( K _ { u _ { 0 } } ^ { \mathsf { T } } R K _ { u _ { 0 } } + Q ) \rho ^ { \mathsf { H } } = 0$ . This, along with $\rho ( K _ { x _ { 1 } } - A ) \rho ^ { \mathsf { H } } =$ ${ \rho } B K _ { u _ { 0 } } ^ { \mathrm { } } { \rho } ^ { \mathsf { H } }$ , derived from (31) for $l = 1$ and pre- and postmultiplied by $\rho$ and $\rho ^ { \mathsf { H } }$ , implies

$$
\rho \left[ \begin{array}{c} I \\ I \\ K _ {u _ {0}} \end{array} \right] ^ {\mathsf {T}} \left[ \begin{array}{c c} z I - A & - B \\ Q & 0 \\ 0 & R \end{array} \right] \left[ \begin{array}{c} I \\ K _ {u _ {0}} \end{array} \right] \rho^ {\mathsf {H}} = 0. \tag {34}
$$

Since the left and right matrices are nonzero, the equality in (36) holds only if the middle matrix has a nontrivial null space, contradicting Assumptions 2 and 3. Therefore, $K _ { x _ { 1 } }$ and the resulting closed-loop system for l = 1 are stable.

For l = 2, we define the augmented vectors in (4) as index vectors, i.e., $X _ { l } : = \boldsymbol { X } _ { k }$ and $U _ { l } : = U _ { k }$ , within a prediction horizon of size l. The closed-loop system in $( 2 1 )$ is then given by $X _ { 2 } ^ { * } ~ = ~ K _ { X _ { 2 } } x _ { k | k }$ , where $X _ { 2 } ^ { * } = \mathrm { c o l } \{ x _ { k + 1 | k } ^ { * } , x _ { k + 2 | k } ^ { * } \}$ and $K _ { X _ { 2 } } = \mathrm { c o l } \{ K _ { x _ { k + 1 | k } } , K _ { x _ { k + 2 | k } } \}$ . Utilizing the Woodbury matrix identity and block matrix inversion, we obtain

$$
\left[ \begin{array}{l} x _ {k + 1 | k} ^ {*} \\ x _ {k + 2 | k} ^ {*} \end{array} \right] = \left[ \begin{array}{c} P _ {k + 2} ^ {- 1} (P _ {k + 2} ^ {- 1} + B R ^ {- 1} B ^ {\mathsf {T}}) ^ {- 1} A \\ (P _ {k + 2} ^ {- 1} (P _ {k + 2} ^ {- 1} + B R ^ {- 1} B ^ {\mathsf {T}}) ^ {- 1} A) ^ {2} \end{array} \right] x _ {k | k}. \tag {35}
$$
