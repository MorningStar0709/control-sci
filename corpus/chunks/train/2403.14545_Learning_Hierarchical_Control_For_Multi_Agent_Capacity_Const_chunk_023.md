track the evolution of the capacity state accumulation variables along the planned route, and ensure that they satisfy all capacity state constraints C (4). The constraints

$$\sum_ {i, j \in \mathcal {V}} u _ {i, j} \leq N \tag {29a}\sum_ {j \in \mathbf {n} _ {: k} ^ {r} \setminus D} u _ {i, j} = 0 \quad \forall i \in \mathcal {V} \tag {29b}$$

ensure that the path is at most length N does not intersect with any nodes traversed thus far during this iteration (stored in the set $\mathbf { n } _ { : k } ^ { r } )$ . Lastly, the constraints

$$\sum_ {n \in \mathcal {S} ^ {H, r}} \left(\sum_ {i \in \mathcal {V}} u _ {i, n} - \sum_ {i \in \mathcal {V}} u _ {n, i}\right) = 1 \tag {30a}\sum_ {n \in \mathcal {S} ^ {H, r}} \left(\sum_ {i \in \mathcal {V}} u _ {i, n} - \sum_ {i \in \mathcal {V}} u _ {n, i}\right) j = \bar {n} \tag {30b}\sum_ {i \in \mathcal {V}} c _ {\bar {n}, i, l} = \bar {c} _ {l} \quad \forall l \in [ 1, L ] \tag {30c}\bar {c} \leq \tilde {c} \tag {30d}(\bar {n}, \tilde {c}) \in \mathcal {S} ^ {H, r} \tag {30e}\sum_ {i \in \mathcal {V}} u _ {i, j} = 0 \quad \forall j \in P (\bar {n}, \tilde {c}) \tag {30f}$$

impose a terminal constraint on the planned path. Constraint (30a), along with (27c), ensures that the planned path ends in a node in ${ \mathcal { S } } ^ { H , r }$ , a node the agent has visited in a previous task iteration. This node is labeled as n¯ in (30b); the corresponding vector of planned capacity states at node n¯ is labeled as c¯ in (30c). Constraints (30d) and (30e) enforce that the capacity state c¯ planned at node n¯ is less than the capacity state c˜ associated with n¯ in the safe set (recall the formulation choice of capacity states being initialized at 0 and increasing throughout the task). Finally, constraint (30f) ensures that the planned path does not include any nodes visited after the terminal node in a previous iteration, a set denoted $P ( \bar { n } , \tilde { c } )$ .

We can now formulate the optimal control problem. At event k of iteration r, the high-level controller solves

$$u _ {k} ^ {H, r} = \pi^ {H, r} (x _ {k} ^ {H, r}, \hat {\theta} ^ {r}, \hat {\Theta} ^ {r}) \tag {31}= \underset {u, c} {\arg \max} \sum_ {i, j \in \mathcal {V}} u _ {i, j} + Q ^ {H, r} ((\bar {n}, \tilde {c}))\text { s.t. } \quad (2 6) - (3 0).$$
