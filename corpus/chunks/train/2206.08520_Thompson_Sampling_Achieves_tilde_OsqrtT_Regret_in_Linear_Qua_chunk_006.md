# 2. Preliminaries

Notation: We denote the Euclidean norm of a vector x as $\lVert x \rVert _ { 2 }$ . For a matrix $A \in \mathbb { R } ^ { n \times d }$ , we denote $\rho ( A )$ as the spectral radius of A, kAkF as its Frobenius norm and $\| A \|$ as its spectral norm. $\operatorname { t r } ( A )$ denotes its trace, $A ^ { \intercal }$ is the transpose. For any positive definite matrix $V , \| A \| _ { V } = \| V ^ { 1 / 2 } A \| _ { F }$ . For matrices $A , B \in \mathbb { R } ^ { n \times d } , A \bullet B = \operatorname { t r } ( A B ^ { \intercal } )$ denotes their Frobenius inner product. The j-th singular value of a rank-n matrix A is $\sigma _ { j } ( A )$ , where $\sigma _ { \operatorname* { m a x } } ( A ) : = \sigma _ { 1 } ( A ) \geq \ldots \geq \sigma _ { \operatorname* { m i n } } ( A ) : = \sigma _ { n } ( A )$ . I represents the identity matrix with the appropriate dimensions. $\mathcal { M } _ { n } = \mathbb { R } ^ { n \times n }$ denotes the set of $n \mathrm { - }$ dimensional square matrices. $\mathcal { N } ( \boldsymbol { \mu } , \boldsymbol { \Sigma } )$ denotes normal distribution with mean $\mu$ and covariance $\Sigma .$ . $Q ( \cdot )$ denotes the Gaussian Q-function. $O ( \cdot )$ and $o ( \cdot )$ denote the standard asymptotic notation and $f ( T ) = \omega ( g ( T ) )$ is equivalent to $g ( T ) = o ( f ( T ) ) . \tilde { O } ( \cdot )$ presents the order up to logarithmic terms.
