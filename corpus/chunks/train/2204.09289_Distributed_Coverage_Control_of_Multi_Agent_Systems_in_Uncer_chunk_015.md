# 4 Theoretical Analysis

In this section, we provide theoretical results on the finite-time coverage of multi-agent systems using the proposed thermal field-induced coverage control method.

Proposition 4.1. $\forall \Delta t \geq 0 , \pmb { x } \in D _ { i } , i \in { \sf Z } ^ { + } , h _ { i } ( \pmb { x } , t ) = 0 \Longleftrightarrow \nabla T _ { i } ( \pmb { x } , t + \Delta t ) = 0 .$

Proof. For $h _ { i } ( \mathbf { x } , t ) = 0 \Rightarrow \forall T _ { i } ( \mathbf { x } , t ) = 0$ and $\nabla T _ { i } ( \mathbf { x } , t + \Delta t ) = 0 .$ . When $h _ { i } ( \mathbf { x } , t ) = 0 , \mathbf { x } \in D _ { i }$ , rewrite the (8) as follows:

$$\alpha (\frac {\partial^ {2} T _ {i} (\mathbf {x} , t)}{\partial x ^ {2}} + \frac {\partial^ {2} T _ {i} (\mathbf {x} , t)}{\partial y ^ {2}}) = \beta T _ {i} (\mathbf {x}, t).$$

Obviously, $T ( \mathbf { x } , t ) = 0$ is the solution of the partial differential equation and satisfies the boundary conditions at the same time. From which we can deduce

$$\frac {\partial T _ {i} (\mathbf {x} , t)}{\partial x} = 0, \quad \frac {\partial T _ {i} (\mathbf {x} , t)}{\partial y} = 0.$$

Because h is a monotonically decreasing function with t and $h \geq 0 ,$ , we can get $h _ { i } ( \mathbf { x } , t + \Delta t ) =$ 0 easily. Likewise, we can obtain $\nabla T _ { i } ( \mathbf { x } , t + \Delta t ) = 0$ . The conditions $\nabla T _ { i } ( \mathbf { x } , t ) ~ = ~ 0$ and

Algorithm 3: Distributed Coverage Control on Global Region   
1 $k \leftarrow 1;$ 2 while $M(t) > 0$ do
3 if $k < k_{max}$ then
4 Run Algorithm 2;
5 Determine the subregion and move to $D_{j}^{k}$ with (16);
6 Drive agent to the maximum workload with (17);
7 $k \leftarrow k + 1;$ 8 else
9 Run Algorithm 1;
10 end
11 end

5 $T _ { i } ( \mathbf { x } , t + \Delta t ) = 0$ leads to $h _ { i } ( { \bf x } , t ) = 0$ . From

$$\bigtriangledown T _ {i} (\mathbf {x}, t) = 0 \Rightarrow \Delta T _ {i} (\mathbf {x}, t) = 0, \quad \bigtriangledown T _ {i} (\mathbf {x}, t + \Delta t) = 0 \Rightarrow \Delta T _ {i} (\mathbf {x}, t + \Delta t) = 0,$$

we can get

$$h _ {i} (\mathbf {x}, t) = \beta T _ {i} (\mathbf {x}, t) = c _ {1}, \quad h _ {i} (\mathbf {x}, t + \Delta t) = \beta T _ {i} (\mathbf {x}, t + \Delta t) = c _ {2},$$

where $c _ { 1 }$ and $c _ { 2 }$ are constants. Owing to $c _ { 1 } \neq 0$ , we have

$$h _ {i} (\mathbf {x}, t + \Delta t) = \frac {m (\mathbf {x} , t + \Delta t)}{\hat {m} _ {i}},$$
