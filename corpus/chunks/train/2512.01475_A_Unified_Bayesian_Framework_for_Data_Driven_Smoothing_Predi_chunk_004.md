where $\zeta \in \mathbb { R } ^ { p }$ encodes direct observations and possibly design requirements of the trajectory $\mathbf { z } ^ { 0 } , \epsilon \in \mathbb { R } ^ { p }$ represents the uncertainty of the knowledge, and $\Phi \in \mathbb { R } ^ { p \times n L }$ is a known transformation matrix.

By specifying different types of existing knowledge, (1) encompasses a broad range of data-driven tasks in systems and control as follows.

(T1) Smoothing. The complete trajectory is available, but it is corrupted. In this case, we have $p = n L$ ,

$$\Phi = \mathbb {I} _ {n L}, \boldsymbol {\zeta} = \operatorname{col} \left(z _ {1}, z _ {2}, \ldots , z _ {L}\right), \boldsymbol {\epsilon} = \operatorname{col} \left(\nu_ {1}, \nu_ {2}, \ldots , \nu_ {L}\right).$$

(T2) Prediction. The complete input sequence and the initial output sequence of length $\mathbf { \bar { \boldsymbol { L } } } _ { 0 } \ \geq \ \bar { \boldsymbol { l } }$ are available, where l is the lag of G. Let $\breve { L ^ { \prime } } : = \bar { L } - L _ { 0 }$ . In this case, we have $p = n L _ { 0 } + n _ { u } L ^ { \prime }$ ,

$$\Phi = \operatorname{blkdiag} \left(\mathbb {I} _ {n L _ {0}}, \mathbb {I} _ {L ^ {\prime}} \otimes \left[ \mathbb {I} _ {n _ {u}} \mathbf {0} _ {n _ {u} \times n _ {y}} \right]\right),\boldsymbol {\zeta} = \operatorname{col} \left(z _ {1}, \ldots , z _ {L _ {0}}, u _ {L _ {0} + 1}, \ldots , u _ {L}\right),\epsilon = \operatorname{col} \left(\nu_ {1}, \dots , \nu_ {L _ {0}}, w _ {L _ {0} + 1}, \dots , w _ {L}\right).$$

When $L _ { 0 } \ \geq \ l ,$ from (14) of Iannelli et al. (2021), there exists K such that $H ^ { 0 } \stackrel { \cdot } { = } K \Phi H ^ { 0 }$ . Noting that rank $\left( A B \right) \leq$ rank (B), this guarantees that rank $\left( H ^ { \breve { 0 } } \right) = \mathrm { r a n k } \left( \stackrel { \cdot } { \Phi } H ^ { 0 } \right) $ in Assumption 4 below.
