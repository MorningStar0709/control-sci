# (iv) System (1) is detectable.

Proof: $( i ) \Rightarrow ( i i )$ : If there exists a state observer of the form (11), clearly the historical data $( u _ { d } , x _ { d } , y _ { d } )$ should satisfy (11) with $\hat { x } ( k ) = x _ { d } ( k ) , k \in \mathbb { Z } _ { 0 } , \mathrm { i . e . }$ , with zero estimation error. Then (ii) holds with $\Sigma _ { X _ { p } } = A _ { O } , \Sigma _ { U _ { p } }$ = $B _ { O } ^ { u } , \Sigma _ { Y _ { p } } = B _ { O } ^ { y }$ , and $\Sigma _ { Y _ { f } } = N _ { O }$ , in which $A _ { O }$ is Schur stable.

$( i i ) \Rightarrow ( i i i )$ : To prove (iii), we preliminarily show that

$$
r k \left[ \begin{array}{c} X _ {p} \\ U _ {p} \\ Y _ {p} \\ Y _ {f} \end{array} \right] = r k \left(\left[ \begin{array}{c c c} I & 0 & 0 \\ 0 & I & 0 \\ C & 0 & 0 \\ 0 & 0 & I \end{array} \right] \left[ \begin{array}{c} X _ {p} \\ U _ {p} \\ Y _ {f} \end{array} \right]\right) = r k \left[ \begin{array}{c} X _ {p} \\ U _ {p} \\ Y _ {f} \end{array} \right].
$$

By exploiting condition (ii), we obtain

$$
r k \left[ \begin{array}{c} \lambda X _ {p} - X _ {f} \\ U _ {p} \\ Y _ {p} \\ Y _ {f} \end{array} \right]

= r k \left(\left[ \begin{array}{c c c c} \lambda I - \Sigma_ {X _ {p}} & - \Sigma_ {U _ {p}} & - \Sigma_ {Y _ {p}} & - \Sigma_ {Y _ {f}} \\ 0 & I & 0 & 0 \\ 0 & 0 & I & 0 \\ 0 & 0 & 0 & I \end{array} \right] \left[ \begin{array}{c} X _ {p} \\ U _ {p} \\ Y _ {p} \\ Y _ {f} \end{array} \right]\right).
$$

Since $\Sigma _ { X _ { p } }$ is Schur, it follows that the rank of the matrix on the left coincides with the rank of $\left\lceil X _ { p } ^ { \top } \ U _ { p } ^ { \top } \ Y _ { p } ^ { \top } \ Y _ { f } ^ { \top } \right\rceil ^ { }$ for every $\lambda ~ \in ~ \mathbb { C }$ with $| \lambda | \ge 1$ . Therefore, condition (iii) can be derived directly from condition (ii).

$( i i i ) \Rightarrow ( i v )$ : First, we evaluate the rank of the left-hand data matrix. We preliminarily show that

$$
r k \left[ \begin{array}{c} \lambda X _ {p} - X _ {f} \\ U _ {p} \\ Y _ {p} \\ Y _ {f} \end{array} \right] = r k \left(\left[ \begin{array}{c c c} I & 0 & 0 \\ 0 & I & 0 \\ 0 & 0 & I \\ - C & 0 & \lambda I \end{array} \right] \left[ \begin{array}{c} \lambda X _ {p} - X _ {f} \\ U _ {p} \\ Y _ {p} \end{array} \right]\right)

= r k \left[ \begin{array}{c} \lambda X _ {p} - X _ {f} \\ U _ {p} \\ Y _ {p} \end{array} \right].
$$

Introducing the non-singular transformation matrix $\left[ P _ { 1 } \quad P _ { 2 } \right]$ and substituting the system dynamics (5) and

(6), it can be deduced that
