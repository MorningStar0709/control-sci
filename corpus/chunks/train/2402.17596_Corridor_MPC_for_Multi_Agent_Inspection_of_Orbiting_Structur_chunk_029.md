and where $V ( e _ { x } ( N | k \Delta t ) )$ is a positive definite function of the state error. The solution to the CMPC is the optimal solution of the finite horizon optimal control problem (FHOC) in (16). Such solution is an optimal control trajectory $\pmb { u } _ { k } ^ { * } =$ $[ { \pmb u } ^ { * } ( 0 | k \Delta t ) , \dots { \pmb u } ^ { * } ( ( N - 1 ) | k \bar { \Delta t } ) ]$ and a corresponding optimal state trajectory $\pmb { x } ^ { * } = [ \pmb { x } ^ { * } ( 0 | \dot { k } \Delta t ) , \dots \pmb { x } _ { k } ^ { * } ( ( N - 1 ) | k \Delta t ) ]$ so that the cost function $J _ { N } ( e , { \boldsymbol { u } } )$ is minimised along the $N _ { - }$ steps receding horizon of the FHOC problem. In (16), the constraint (16b) forces the state to evolve according to the nominal discrete dynamics of each inspector, (16c) and (16d) are the SD-HOCBF constraint on the velocity and position respectively, (16e) is the control constraint and (16g) sets the initial state of the state trajectory $\pmb { x } ^ { * }$ to be equal to the measured state $\tilde { \mathbf { \pmb { x } } } ( k \Delta t )$ . Once the solution $\boldsymbol { \mathbf { \mathit { u } } } _ { k } ^ { * }$ to (16) is found, the feedback control $K ( \tilde { { \boldsymbol { x } } } , k \Delta t ) = { \boldsymbol { u } } ^ { * } ( 0 | \dot { k } \Delta t )$ is applied in a ZOH fashion on (2) during the interval $[ k \Delta t , ( k + 1 ) \Delta t ]$ . Since at k∆t we have $\tilde { \mathbf { x } } \in \mathcal { C } _ { S , x } ( k \Delta t )$ , Lemma 2 guarantees that $\tilde { { \mathbf { x } } } ( t ) \in \mathcal { C } _ { S , x } ( t ) \forall t \in [ k \Delta t , ( k + 1 ) \Delta t ]$ under $K ( \tilde { \pmb { x } } , k \Delta t )$ .

Given the maximum control input $\epsilon _ { u }$ and a sampling interval $\Delta t ,$ the recursive feasibility of the given CMPC scheme can be assessed numerically by applying the feasibility checking algorithm by [12]. Namely, we verify the compatibility of constraints (16c)-(16e) by solving the following quadratic program (QP)

$$
\underset {\boldsymbol {q}, \boldsymbol {u}} {\min} - \mathbf {1} ^ {T} \boldsymbol {q} \tag {18a}
$$

$$
\boldsymbol {I} _ {2} \boldsymbol {q} \geq \mathbf {0}, \| \boldsymbol {u} \| ^ {2} \leq \epsilon_ {u} ^ {2} \tag {18b}
$$

$$
\zeta_ {\delta \boldsymbol {r}} (\boldsymbol {x}, t, \boldsymbol {u}) \geq \bar {L} _ {\delta \boldsymbol {r}} ^ {l} \Delta t + \bar {c} _ {\delta \boldsymbol {r}} ^ {l} \epsilon_ {d} + q _ {1} \tag {18c}
$$
