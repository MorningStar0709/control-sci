# 2.1. Notation

In this subsection we introduce some notations that we use in this work.

The set of symmetric matrices in $\mathbb { R } ^ { n \times n }$ is denoted by $\mathbb { S } ^ { n }$ , the subset of positive semidefinite $( n \times n )$ -matrices is denoted by $\mathbb { S } _ { \geq 0 } ^ { n } .$ , and the subset of positive definite $( n \times n )$ -matrices is denoted by $\mathbb { S } _ { > 0 } ^ { n }$ . The identity matrix in $\mathbb { R } ^ { n \times n }$ is denoted by $I _ { n }$ . For $n ^ { \prime } , m ^ { \prime } \in \mathbb { N }$ and $A = ( A _ { i , j } ) _ { ( i , j ) \in \{ 1 , \dots , n ^ { \prime } \} \times \{ 1 , \dots , m ^ { \prime } \} } \in \mathbb { R } ^ { n ^ { \prime } \times m ^ { \prime } }$ ′ let $\begin{array} { r } { \| A \| _ { F } = ( \sum _ { i = 1 } ^ { n ^ { \prime } } \sum _ { j = 1 } ^ { m ^ { \prime } } | A _ { i , j } | ^ { 2 } ) ^ { \frac { 1 } { 2 } } } \end{array}$ be the Frobenius norm.

We introduce the following notations for $n ^ { \prime } , m ^ { \prime } , l \in \mathbb { N }$ . For $s \in [ 0 , T ]$ and an $\mathbb { R } ^ { n ^ { \prime } \times m ^ { \prime } } .$ - valued càdlàg semimartingale L we define $\Delta L ( s ) = L ( s ) - L ( s - )$ . We follow the convention that, for $t \in [ 0 , T ] , \ r \in [ t , T ]$ , and a càdlàg semimartingale $L ,$ jumps of the càdlàg integrator L at time t contribute to integrals of the form $\int _ { [ t , r ] } \cdot \cdot \cdot d L ( s ) \cdot \cdot \cdot$ . In contrast, we write $\int _ { ( t , r ] } \cdot \cdot \cdot d L ( s )$ . . . when we do not include jumps of $L$ at time t into the integral. For suitable càdlàg semimartingales A, B, where A is $\mathbb { R } ^ { n ^ { \prime } \times m ^ { \prime } }$ -valued and $B$ is $\mathbb { R } ^ { m ^ { \bar { \prime } } \times l _ { - } }$ -valued, we denote by $[ A , B ]$ the $\mathbb { R } ^ { n ^ { \prime } \times l } .$ -valued covariation.

We define

$$\mathcal {L} ^ {2} = \left\{u \bigg | u \colon [ 0, T ] \times \Omega \to \mathbb {R} ^ {n} \text {progr. measurable and} \| u \| _ {\mathcal {L} ^ {2}} ^ {2} = E \bigg [ \int_ {0} ^ {T} \| u (s) \| _ {F} ^ {2} d s \bigg ] < \infty \right\}$$

and $L ^ { 2 } ( \Omega , \mathcal { F } _ { T } , P ; \mathbb { R } ^ { n } ) = \{ Y \mid Y \colon \Omega  \mathbb { R } ^ { n } $ FT -measurable and $\| Y \| _ { L ^ { 2 } } ^ { 2 } = E [ \| Y \| _ { F } ^ { 2 } ] < \infty \}$ .
