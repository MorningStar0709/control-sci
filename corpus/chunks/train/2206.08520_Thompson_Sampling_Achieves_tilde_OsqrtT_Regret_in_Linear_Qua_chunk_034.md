Using the relation k∇L∗F 12t kF ≥ max(σmin,∗kF 12t kF , λ 12min,tk∇L∗kF ) where σmin,∗ is the min- $\begin{array} { r } { \| \nabla L _ { * } F _ { t } ^ { \frac { 1 } { 2 } } \| _ { F } \ge \operatorname* { m a x } ( \sigma _ { \operatorname* { m i n } , * } \| F _ { t } ^ { \frac { 1 } { 2 } } \| _ { F } , \lambda _ { \operatorname* { m i n } , t } ^ { \frac { 1 } { 2 } } \| \nabla L _ { * } \| _ { F } ) } \end{array}$ $\sigma _ { \mathrm { m i n } , * }$ imum singular value of $\nabla L _ { * }$ , we can further bound (15) from below. From Lemma 9, we can write $\nabla L _ { * } = 2 P ( \Theta _ { * } ) A _ { c , * } \Sigma ,$ where $P ( \Theta _ { * } ) \succ 0$ is the solution to the DARE in (3) and $\Sigma _ { * } = \Sigma ( \Theta _ { * } , K _ { * } ) \succ$ 0 is the stationary state covariance matrix. Notice that the minimum singular value of $\nabla L _ { * }$ is positive $( i . e . \nabla L _ { * }$ is full-rank) if and only if the closed-loop system matrix, $A _ { c , * }$ , is non-singular.

In general, $A _ { c , * }$ can be singular. Assuming that $T _ { w } = O ( ( \sqrt { T } ) ^ { 1 + o ( 1 ) } )$ , under the event $E _ { T }$ , we can use $\| \nabla L _ { * } F _ { t } ^ { \frac { 1 } { 2 } } \| _ { F } \geq \sqrt { \lambda _ { \operatorname* { m i n } , t } } \| \nabla L _ { * } \| _ { F }$ to obtain the following lower bound on $p _ { t } ^ { \mathrm { { o p t } } }$ for $T _ { r } < t \leq T$ :

$$
\begin{array}{l} p _ {t} ^ {\mathrm{opt}} \geq \mathbb {P} _ {t} \bigg \{\xi + 1 \leq - \frac {\sqrt {\lambda_ {\max , t}}}{2 \rho_ {*}} \sqrt {\frac {\lambda_ {\max , t}}{\lambda_ {\min , t}}} \left((\xi + 1) ^ {2} + X\right), \text {and} (\xi + 1) ^ {2} + X \leq \frac {\epsilon_ {*} ^ {2}}{\lambda_ {\max , t}} \bigg \}, \\ \geq \mathbb {P} \bigg \{\xi + 1 \leq - \frac {C}{2 \rho_ {*}} \frac {\sqrt {T} \log T}{T _ {w}} \left((\xi + 1) ^ {2} + X\right), \text {and} (\xi + 1) ^ {2} + X \leq \frac {\epsilon_ {*} ^ {2} T _ {w}}{C \log T} \bigg \}, \\ \end{array}
$$

where $\rho _ { * } : = \| r _ { * } ^ { - 1 } \nabla L _ { * } \| _ { F }$ . Choosing the exploration time as $T _ { w } = \omega ( \sqrt { T } \log T )$ makes the coefficients √T log TT = o(1) to be very small and $\frac { \sqrt { T } \log T } { T _ { w } } = o ( 1 )$ w $\textstyle { \frac { T _ { w } } { \log T } }$ to be very large, leading to constant lower bound on limiting optimistic probability lim $\begin{array} { r } { \operatorname* { i n f } _ { T \to \infty } p _ { T } ^ { \mathrm { o p t } } \ge \mathbb { P } \{ \xi + 1 \le 0 \} = : Q ( 1 ) } \end{array}$ .
