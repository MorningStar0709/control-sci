$\forall \mathbf { x } \in \mathcal { Z } ( \mathbf { w } )$ . If such $\alpha ( \cdot )$ exists, $\begin{array} { r } { \frac { \partial \phi } { \partial \mathbf { x } } ( \mathbf { x } ) \neq 0 \forall \mathbf { x } \in \partial \mathcal { Z } ( \mathbf { w } ) } \end{array}$ ∂ϕ , and $\mathbf { x } ( t _ { 0 } ) \in \mathcal { Z } ( \mathbf { w } )$ , then any Lipschitz continuous controller that satisfies (12) would make all robots maintain at least $F ^ { \prime }$ neighbors without collisions $\forall t \geq t _ { 0 }$ . The issue with (12) is that it is centralized; it depends on the states and dynamics of all agents. A robot $i ~ \in ~ \nu$ cannot compute its control input $u _ { i } \in \mathcal { U } _ { i }$ while ensuring the satisfaction of (12) without considering the states and control inputs of all other robots. Various approaches exist to decouple CBF constraints into multiple distributed CBF constraints [2], [17], [26]. We adopt the strategy from [17], [26] by dividing the responsibility of satisfying (12) among robots.
