Proposition 2 (Linear Decay in Cost Value). Suppose $L _ { 0 } \in S _ { \alpha }$ for some $\alpha > 0$ and a direction $E \neq 0$ is given such that $\| E - \nabla J ( L ) \| _ { F } \leq \gamma \| \nabla J ( L ) \| _ { F } f o$ r some $\gamma < 1$ . Let $\bar { \eta } : = { { ( 1 - \gamma ) } \mathord { \left/ { \vphantom { { ( 1 - \gamma ) } { { ( \gamma + 1 ) } ^ { 2 } } } } \right. \kern - delimiterspace } { ( \gamma + 1 ) } ^ { 2 } } \ell ( \alpha )$ . Then, $L _ { 1 } : = L _ { 0 } - \bar { \eta } E$ remains in the same sublevel set, $\bar { i } . e . , L _ { 1 } \in \mathcal { S } _ { \alpha }$ . Furthermore, we obtain the following linear decay of the cost value:

$$J (L _ {1}) - J (L ^ {*}) \leq [ 1 - c _ {1} (\alpha) \bar {\eta} (1 - \gamma) / 2 ] [ J (L _ {0}) - J (L ^ {*}) ].$$

Next, we guarantee that SGD algorithm with this biased estimation of gradient obtains a linear convergence rate outside a small set $\mathcal { C } _ { \tau }$ around optimality defined as

$$\mathcal {C} _ {\tau} := \{L \in \mathcal {S} \mid \| \nabla J (L) \| _ {F} \leq s _ {0} / \tau \},$$

for some $\tau \in ( 0 , 1 )$ and arbitrarily small $s _ { 0 } > 0$ . First, we assume access to the following oracle that provides a biased estimation of the true gradient.

Assumption 3. Suppose, for some $\alpha > 0 _ { : }$ , we have access to a biased estimate of the gradient $\nabla \widehat { J } ( L )$ such that, there exists constants $s , s _ { 0 } > 0$ implying $\| \nabla \widehat { J } ( L ) - \nabla J ( L ) \| _ { F } \leq s \| \nabla J ( L ) \| _ { F } + s _ { 0 } f o r$ all $L \in { \mathcal { S } } _ { \alpha } \setminus { \mathcal { C } } _ { \tau }$ .

Theorem 1 (Convergence). Suppose Assumption 3 holds with small enough s and $s _ { 0 }$ such that $s \leq \gamma / 2$ and $\mathcal { S } _ { \alpha } \backslash \mathcal { C } _ { \gamma / 2 } ^ { \mathrm { ~ - ~ } }$ is non-empty for some $\gamma \in ( 0 , 1 )$ ). Then, SGD algorithm starting from any $L _ { 0 } \in \mathcal S _ { \alpha } \setminus \mathcal C _ { \gamma / 2 }$ with fixed stepsize $\begin{array} { r } { \bar { \eta } : = \frac { ( 1 - \gamma ) } { ( \gamma + 1 ) ^ { 2 } \ell ( \alpha ) } } \end{array}$ generates a sequence of policies $\{ L _ { k } \}$ that are stable (i.e. each $L _ { k } \in { \cal S } _ { \alpha } )$ and cost values decay linearly before entering $\mathcal { C } _ { \gamma / 2 } ; i . e .$ ,

$$J (L _ {k}) - J (L ^ {*}) \leq [ 1 - c _ {1} (\alpha) \bar {\eta} (1 - \gamma) / 2 ] ^ {k} [ J (L _ {0}) - J (L ^ {*}) ],$$

for each $k \geq 0$ unless $L _ { j } \in \mathcal { C } _ { \gamma / 2 } f o r$ some $j \le k$ .
