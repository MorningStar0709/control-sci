# Remark 5.3 (Rejection of measurement noise):

For system (39) with the controller (40) or (44), the measurement noise is rejected on the sliding surface because of its filter-corrector property. Even when the frequency bands of the variables and noise overlap, the outputs of sliding surface are smoothed and are accurate.

In fact, suppose noise $n _ { 1 } \left( t \right)$ and $n _ { 2 } \left( t \right)$ exist in the measurements of $x _ { 1 } \left( t \right)$ and $x _ { 2 } \left( t \right)$ , respectively. Then, for the sliding surface, we get

$$\dot {x} _ {d} (t) - x _ {2} (t) - n _ {2} (t) + k _ {1} \left[ x _ {d} (t) - x _ {1} (t) - n _ {1} (t) \right] = 0 \tag {51}$$

i.e.,

$$e _ {2} (t) + k _ {1} e _ {1} (t) = k _ {1} n _ {1} (t) + n _ {2} (t) \tag {52}$$

Define the Laplace transforms $E _ { 1 } ( s ) = L [ e _ { 1 } ( t ) ] , E _ { 2 } ( s ) = L [ e _ { 2 } ( t ) ] , N _ { 1 } ( s ) = L [ n _ { 1 } ( t ) ]$ , and $N _ { 2 } \left( s \right) = L \left[ n _ { 2 } \left( t \right) \right]$ . Taking Laplace transform for (52), we get

$$s E _ {1} (s) + k _ {1} E _ {1} (s) = k _ {1} N _ {1} (s) + N _ {2} (s) \tag {53}$$

i.e.,

$$E _ {1} (s) = \frac {k _ {1}}{s + k _ {1}} \left(N _ {1} (s) + \frac {1}{k _ {1}} N _ {2} (s)\right) \tag {54}$$

where, s+k1 $\frac { k _ { 1 } } { s + k _ { 1 } }$ is the form of first-order filter, and $k _ { 1 }$ is the cut-off frequency of the filter. For the filter, the input is the noise, and the output is the system error. As long as $k _ { 1 }$ is less than the minimum frequency of the noise, the noise will be rejected. From $k _ { 1 }$ selection conditions, $k _ { 1 }$ can be neither too large nor too small, for example, $k _ { 1 } = 1 \mathrm { o r } k _ { 1 } = 2 .$ Therefore, the noise $\begin{array} { r } { N _ { 1 } \left( s \right) + \frac { 1 } { k _ { 1 } } N _ { 2 } \left( s \right) } \end{array}$ is rejected sufficiently, and the system error is reduced to be small enough.

![](image/7aa4f1a9fa7ec44fa80f9c0277712f0569ed66ed3f6fee54192887ff2bd0f3c9.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    A["Start"] --> B["e₁(0),e₂(0)"]
    B --> C["Select: e₁c ∈ (0, k₂M - Ld), e₂c ∈ (e1c, √(k₂M - Ld)e1c)"]
    C --> D{|e₁(t)| ≤ e₁c?}
    D -->|Yes| E["If the reference jumps, i.e., x_d(t⁺) ≠ x_d(t⁻)"]
    D -->|No| F["The first controller in (44) with parameters in (46) makes: e₁(t) reach e₁c sign(e₁(t)) and without overshoot e₂(t) → -e₂c sign(e(t))"]
    E --> G["End"]
    F --> G
```
</details>

Figure 4: Flow chart of non-overshooting controller design.
