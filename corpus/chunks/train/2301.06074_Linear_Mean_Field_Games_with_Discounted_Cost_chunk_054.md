Recall that $\begin{array} { r } { \operatorname* { s u p } _ { x \in \mathsf { X } } g ( x ) \leq \dim ( \mathsf { X } ) } \end{array}$ and $\begin{array} { r } { \operatorname* { s u p } _ { x \in \mathsf { X } } g ^ { 2 } ( x ) \leq \dim ( \mathsf { X } ) ^ { 2 } } \end{array}$ . Therefore, we can ∈bound the last term as follows

$$(3 4) \leq \sqrt {\frac {2}{N - 1}} \operatorname{diam} (X) \tag {35}$$

Note that the bound (35) is independent of g. Let $\left\{ g _ { 1 } , \dotsc , g _ { \mathbb { N } ( \varepsilon , F ) } \right\}$ be minimal ε-cover of F . Then using the same trick to establish (25), we can obtain the following bound

$$\mathbb {E} \left[ \sup _ {g \in \mathcal {F}} \left| \int_ {\mathsf {X}} g (x) \mathsf {e} [ d x | \tilde {\mathbf {x}} _ {\{2: N \}} (t + 1) ] - \int_ {\mathsf {X} \times \mathsf {X}} g (x) p (d x | z, \pi^ {*} (z), \mathsf {e} [ \cdot | \tilde {\mathbf {x}} (t) ]) \mathsf {e} [ d z | \tilde {\mathbf {x}} _ {\{2: N \}} (t) ] \right| \right]\leq 2 \varepsilon + \sum_ {i = 1} ^ {\mathbb {N} (\varepsilon , \mathcal {F})} \mathbb {E} \left[ \left| \int_ {\mathsf {X}} g _ {i} (x) \mathsf {e} [ d x | \tilde {\mathbf {x}} _ {\{2: N \}} (t + 1) ] - \int_ {\mathsf {X} \times \mathsf {X}} g _ {i} (x) p (d x | z, \pi^ {*} (z), \mathsf {e} [ d x | \tilde {\mathbf {x}} (t) ]) \mathsf {e} [ d z | \tilde {\mathbf {x}} _ {\{2: N \}} (t) ] \right| \right]$$

(36)

$$\leq 2 \varepsilon + \sqrt {\frac {2}{N - 1}} \operatorname{diam} (X) \mathbb {N} (\varepsilon , \mathcal {F}) (\text { by } (3 5))$$

Now if we combine the bounds (31), (36), (32), and (33) for the expectations (uniform in $g \in { \mathcal { F } } )$ of the terms (27), (28), (29), and (30), we obtain the following

$$
\begin{array}{l} \mathbb {E} \left[ \sup _ {g \in \mathcal {F}} \left| \int_ {\mathsf {X}} g (x) \mathrm{e} [ d x | \tilde {\mathbf {x}} (t) + 1 ] - \int_ {\mathsf {X}} g (x) \mu^ {*} (d x) \right| \right] \\ \leq \left(\mathrm{K} _ {1} + \mathrm{K} _ {2} \mathrm{L} ^ {*} + \mathrm{K} _ {3}\right) \tilde {\alpha} _ {t} (N, \varepsilon) + \sqrt {\frac {2}{N - 1}} \operatorname{diam} (\mathrm{X}) \left\{\mathbb {N} (\varepsilon , \mathcal {F}) + \sqrt {\frac {8}{N - 1}} \right\} + 2 \varepsilon \\ \end{array}
$$

This completes the proof.
