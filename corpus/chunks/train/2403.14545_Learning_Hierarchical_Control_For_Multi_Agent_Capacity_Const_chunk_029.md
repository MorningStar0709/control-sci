# E. Properties

We prove that Alg. 1 with (31) and (33) satisfies iterative feasibility and iterative performance improvement as defined in Sec. IV. We first show that an agent (1) in closed-loop with the low-level policy (33) iteratively traversing a single edge $e _ { i , j }$ exhibits reduced capacity consumption with each subsequent iteration.

Theorem 1: Consider system (1) controlled by the lowlevel policy (33) during consecutive traversals of a single edge $e _ { i , j }$ in G. Let $\bar { S _ { i , j } ^ { L , r } }$ be the safe set corresponding to the edge $e _ { i , j }$ at the start of iteration r as defined in (25). Then, the optimal control problem (33) solved by the lowlevel policy will be feasible for all time steps $0 \leq t \leq t f$ and iterations $r \geq 1$ from any initial state $\boldsymbol { x } _ { t 0 } ^ { r } = \left( c _ { t 0 } ^ { r } , \bar { \chi } _ { i } \right)$ where $c _ { l , t 0 } ^ { r } \in [ 0 , C _ { l } - \hat { \theta } _ { i , j , l } ^ { r } ] , t 0 = \mathbb { Z } ( \mathbf { x } ^ { r } , i , j , \tilde { 0 } )$ , and $t f =$ $\mathcal { T } ( \mathbf { x } ^ { r } , i , j , - 1 )$ . Furthermore, the capacity states drain no more during traversal r than traversal (r − 1), so that

$$\Omega_ {l} (\mathbf {x} ^ {r}, i, j) \leq \Omega_ {l} (\mathbf {x} ^ {r - 1}, i, j).$$

Proof: By construction, $ { \mathcal { S } } _ { i , j } ^ { L , r }$ $\forall r \geq 1$ There exists a feasible trajectory x I(xr−1,i,j) $\mathbf { x } _ { \mathcal { T } ( \mathbf { x } ^ { r - 1 } , i , j ) } ^ { r - 1 } \in S _ { i , j } ^ { L , r }$ ∈ with related input sequence ur−1I(xr−1,i,j) $\mathbf { u } _ { \mathcal { T } ( \mathbf { x } ^ { r - 1 } , i , j ) } ^ { r - 1 } \in \dot { \mathcal { U } }$ , where

$$\mathbf {x} _ {\mathcal {I} (\mathbf {x} ^ {r - 1}, i, j)} ^ {r - 1} = \bigl (\tilde {\mathbf {c}} _ {\mathcal {I} (\mathbf {x} ^ {r - 1}, i, j)} ^ {r - 1}, \chi_ {\mathcal {I} (\mathbf {x} ^ {r - 1}, i, j)} ^ {r - 1} \bigr).$$

We denote $t r = \mathcal { T } ( \mathbf { x } ^ { r - 1 } , i , j , 0 )$ the time at the beginning of edge $e _ { i , j }$ traversal in iteration (r − 1). At time t = t0 of iteration $^ { r , }$ the N -step trajectory

$$\mathbf {x} _ {t 0: t 0 + N \mid t 0} ^ {r} = \left(\mathbf {c} _ {t 0: t 0 + N \mid t 0} ^ {r}, \chi_ {t 0: t 0 + N \mid t 0} ^ {r}\right) \tag {35a}\mathbf {c} _ {t 0: t 0 + N \mid t 0} ^ {r} = \tilde {\mathbf {c}} _ {t r: t r + N} ^ {r - 1} + c _ {t 0} ^ {r} \tag {35b}\chi_ {t 0: t 0 + N \mid t 0} ^ {r} = \chi_ {t r: t r + N} ^ {r - 1} \tag {35c}$$

and related input sequence

$$\left[ u _ {t r} ^ {r - 1}, u _ {t r + 1} ^ {r - 1}, \dots , u _ {t r + N - 1} ^ {r - 1} \right] \tag {36}$$
