$$\| \bar {y} - y _ {\theta} \| _ {L ^ {2} (\Omega)} ^ {2} + \| \bar {p} - p _ {\sigma} \| _ {L ^ {2} (\Omega)} ^ {2} + \| \bar {u} - u _ {\sigma} \| _ {L ^ {2} (\Omega)} ^ {2} \leq c (\boldsymbol {\alpha}, \lambda) \mathcal {L} (y _ {\theta}, p _ {\sigma}).$$

Proof. The proof employs crucially the following auxiliary functions $R \bar { y } , R \bar { p } \in H _ { 0 } ^ { 1 } ( \Omega )$ satisfying

$$
\left\{ \begin{array}{c c} - \Delta R \bar {y} = f + u _ {\sigma}, & \text {in} \Omega , \\ R \bar {y} = 0, & \text {on} \partial \Omega , \end{array} \right. \quad \text {and} \quad \left\{ \begin{array}{c c} - \Delta R \bar {p} = y _ {\theta} - y _ {d}, & \text {in} \Omega , \\ R \bar {p} = 0, & \text {on} \partial \Omega . \end{array} \right. \tag {3.4}
$$

Then subtracting (3.4) from (2.4) yields

$$- \Delta (\bar {y} - R \bar {y}) = \bar {u} - u _ {\sigma} \mathrm{in} \Omega \quad \mathrm{with} \quad \bar {y} - R \bar {y} = 0 \mathrm{on} \partial \Omega ,- \Delta (\bar {p} - R \bar {p}) = \bar {y} - y _ {\theta} \mathrm{in} \Omega \quad \mathrm{with} \quad \bar {p} - R \bar {p} = 0 \mathrm{on} \partial \Omega .$$

Now multiplying the first line by $( { \bar { p } } - R { \bar { p } } )$ and the second line by $( \bar { y } - R \bar { y } )$ , and integrating over Ω yield

$$(\bar {p} - R \bar {p}, \bar {u} - u _ {\sigma}) = (\bar {y} - R \bar {y}, \bar {y} - y _ {\theta}). \tag {3.5}$$

It follows from the first-order necessary optimality condition in (2.8) that

$$(\bar {p} + \lambda \bar {u}, v - \bar {u}) \geq 0, \quad \forall v \in U. \tag {3.6}$$

Also, since $u _ { \sigma } = P _ { U } ( \lambda ^ { - 1 } p _ { \sigma } ) \in U$ , we have

$$\left(p _ {\sigma} + \lambda u _ {\sigma}, v - u _ {\sigma}\right) \geq 0, \quad \forall v \in U. \tag {3.7}$$

Putting $v = u _ { \sigma } \in U$ in (3.6), $v = \bar { u } \in U$ in (3.7) and adding the resulting inequalities give

$$\left(\bar {p} - p _ {\sigma}, u _ {\sigma} - \bar {u}\right) - \lambda \left\| \bar {u} - u _ {\sigma} \right\| _ {L ^ {2} (\Omega)} ^ {2} \geq 0. \tag {3.8}$$

Meanwhile, it follows from the identity (3.5) that

$$
\begin{array}{l} (\bar {p} - p _ {\sigma}, u _ {\sigma} - \bar {u}) = (\bar {p} - R \bar {p}, u _ {\sigma} - \bar {u}) + (R \bar {p} - p _ {\sigma}, u _ {\sigma} - \bar {u}) \\ = - (\bar {y} - R \bar {y}, \bar {y} - y _ {\theta}) + (R \bar {p} - p _ {\sigma}, u _ {\sigma} - \bar {u}) \\ = - \left\| \bar {y} - R \bar {y} \right\| _ {L ^ {2} (\Omega)} ^ {2} - \left(\bar {y} - R \bar {y}, R \bar {y} - y _ {\theta}\right) + \left(R \bar {p} - p _ {\sigma}, u _ {\sigma} - \bar {u}\right). \\ \end{array}
$$
