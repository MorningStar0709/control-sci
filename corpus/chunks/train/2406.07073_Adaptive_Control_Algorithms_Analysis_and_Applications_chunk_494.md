This result will be necessary for establishing the convergence of $\tilde { \theta } ( t )$ toward a constant value. To do this, one considers:

$$
\begin{array}{l} F (t + 1) ^ {- 1} \hat {\theta} (t + 1) = [ F (t) ^ {- 1} + \alpha (t) \bar {\phi} (t) \bar {\phi} ^ {T} (t) ] \\ \times [ \tilde {\theta} (t) + \alpha (t) F (t + 1) \bar {\phi} (t) \bar {\nu} ^ {0} (t + 1) ] \tag {10.156} \\ \end{array}
$$

where:

$$\bar {\nu} ^ {0} (t + 1) = \bar {y} (t + 1) - \hat {\theta} ^ {T} (t) \bar {\phi} (t) = - \tilde {\theta} ^ {T} (t) \phi (t) + \bar {w} (t + 1) \tag {10.157}$$

Introducing (10.157) in (10.156), one gets:

$$
\begin{array}{l} F ^ {- 1} (t + 1) \tilde {\theta} (t + 1) = F ^ {- 1} (t) \tilde {\theta} (t) + \alpha (t) \bar {\phi} (t) \bar {w} (t + 1) \\ = F ^ {- 1} (0) \tilde {\theta} (0) + \sum_ {t = 0} ^ {N} \alpha (t) \bar {\phi} (t) \bar {w} (t + 1) \tag {10.158} \\ \end{array}
$$

Since $F ( t )$ converges in order to conclude that $\tilde { \theta } ( t )$ converges toward a constant value, one has to show that the second term in the RHS of (10.158) converge. To do this, we will use the following result: If $\| \sum _ { t = 0 } ^ { \infty } a ( i ) \|$ is bounded for any sequence $a ( i )$ , then $\textstyle \sum _ { t = 0 } ^ { \infty } a ( i )$ t=0  has a limit (Knopp 1956). In our case:

$$
\begin{array}{l} \left\| \sum_ {t = 0} ^ {N} \alpha (t) \bar {\phi} (t) \bar {w} (t + 1) \right\| \leq \sum_ {t = 0} ^ {N} \alpha (t) \| \bar {\phi} (t) \| \| \bar {w} (t + 1) \| \\ \leq \sum_ {t = 0} ^ {N} \alpha (t) | \bar {w} (t + 1) | \leq \sum_ {t = 0} ^ {N} \alpha (t) \delta (t + 1) \tag {10.159} \\ \end{array}
$$

since $\| \bar { \phi } ( t ) \| \leq 1 ( m ^ { 2 } ( t ) \geq \| \phi ( t ) \| ^ { 2 } )$ . But from (10.155), one has:

$$\lim _ {N \to \infty} \sum_ {t = 0} ^ {N} \alpha (t) \delta (t + 1) \leq \frac {1}{\sigma} \tilde {\theta} (0) F ^ {- 1} (0) \tilde {\theta} (0) \tag {10.160}$$

and therefore lim $\begin{array} { r } { \mathbf { \Phi } _ { N \to \infty } \sum _ { t = 0 } ^ { N } \alpha ( t ) \bar { \phi } ( t ) \bar { w } ( t + 1 ) } \end{array}$ exists. Therefore, $\tilde { \theta } ( t )$ (and $ { \hat { \theta } } ( t ) )$ converges. -
