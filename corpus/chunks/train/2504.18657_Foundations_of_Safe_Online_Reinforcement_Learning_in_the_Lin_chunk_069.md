# D Proof of Lemma 1

proof. Let $\begin{array} { r } { \epsilon _ { \mathrm { L 1 } } = \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$ . We will combine the following two results.

Lemma 12. Under Assumptions 1–3, for any θ such that $\begin{array} { r } { \| { \boldsymbol { \theta } } - { \boldsymbol { \theta } } ^ { * } \| _ { \infty } = \epsilon \leq \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$ 1log46(T ) , the following holds for the class of truncated linear controllers $f o r t \le T$ :

$$\bar {J} (\theta , C _ {K _ {\mathrm{opt}} (\theta , t)} ^ {\theta}, t) - \bar {J} (\theta^ {*}, C _ {K _ {\mathrm{opt}} (\theta^ {*}, t)} ^ {\theta^ {*}}, t) = \tilde {O} _ {T} (\epsilon).$$

The proof of Lemma 12 can be found in Appendix D.1.

Lemma 13. Under Assumptions ${ \cal { 1 - 3 } } ,$ for any $\begin{array} { r } { \| \theta - \theta ^ { * } \| _ { \infty } = \epsilon \leq \frac { 1 } { \log ^ { 4 6 } ( T ) } , t \leq T } \end{array}$ , and $K \in$ $\textstyle { \left[ { \frac { a - 1 } { b } } , { \frac { a } { b } } \right] }$ ,

$$\left| \bar {J} (\theta^ {*}, C _ {K} ^ {\theta}, t) - \bar {J} (\theta , C _ {K} ^ {\theta}, t) \right| = \tilde {O} _ {T} \left(\epsilon + \frac {1}{T ^ {2}}\right). \tag {40}$$

The proof of Lemma 13 can be found in Appendix D.2.

Putting together Lemma 12 and Lemma 13 with $K = K _ { \mathrm { o p t } } ( \theta , t )$ , we have the desired result that

$$\bar {J} (\theta^ {*}, C _ {K _ {\mathrm{opt}} (\theta , t)} ^ {\theta}, t) - \bar {J} (\theta^ {*}, C _ {K _ {\mathrm{opt}} (\theta^ {*}, t)} ^ {\theta^ {*}}, t) = \tilde {O} _ {T} \left(\epsilon + \frac {1}{T ^ {2}}\right).$$
