$$\left\| \varepsilon (k, t _ {f}, t _ {1}: t _ {n}) \right\| _ {\infty} = O \bigl (\exp (- l _ {1} k) + \exp (- l _ {2} t _ {f}) \bigr), \tag {53}$$

as $\left\lceil k \ t _ { f } \right\rceil ^ { \prime } \to \infty$ , where $l _ { 1 } , l _ { 2 }$ are two positive constants given by (68) and (66) respectively.

Proof. The finite-horizon Riccati equation (3) can be written in reversed time as follows

$$\frac {d}{d \tau} \hat {P} (\tau) = A ^ {\prime} \hat {P} (\tau) + \hat {P} (\tau) A - \hat {P} (\tau) B R ^ {- 1} B ^ {\prime} \hat {P} (\tau) + Q _ {x}, \tag {54}$$

where $\tau = t _ { f } - t$ is the mirrored time. Under Assumption 4, there holds $\begin{array} { r } { \operatorname* { l i m } _ { \tau  + \infty } \hat { P } ( \tau ) = P _ { \infty } } \end{array}$ where $P _ { \infty }$ is the unique positive definite solution of the algebraic Riccati equation (45) [21, Theorem 3.4.1]. Consequently, the optimal gain (2b) of the finite-horizon problem also converges to the optimal gain $K _ { \infty }$ (46) of the infinite horizon problem, i.e., lim $\iota _ { \tau  + \infty } \hat { K } ( \tau ) = K _ { \infty }$ . Define

$$\hat {e} ^ {P} (\tau) := \hat {P} (\tau) - P _ {\infty}, \tag {55}e _ {k} ^ {u} (t) := u _ {k} (t) - u ^ {\star} (t),$$

and let $\mathcal { G } _ { x }$ be the linear operator mapping the system input to its state trajectory starting from the origin. It follows that

$$
\begin{array}{l} - K (t) x _ {k} (t) = - K (t) x (t) - K (t) \left(x _ {k} (t) - x (t)\right) \\ = - K (t) x (t) - K (t) \mathcal {G} _ {x} e _ {k} ^ {u} (t) \\ = u ^ {\star} (t) - K (t) \mathcal {G} _ {x} e _ {k} ^ {u} (t) \\ = u _ {k} (t) - e _ {k} ^ {u} (t) - K (t) \mathcal {G} _ {x} e _ {k} ^ {u} (t). \tag {56} \\ \end{array}
$$

Since the optimal gain satisfies

$$K (t) = K _ {\infty} + R ^ {- 1} B ^ {\prime} \hat {e} ^ {P} (t _ {f} - t),$$

we obtain

$$- K _ {\infty} x _ {k} (t) = u _ {k} (t) + \epsilon (k, t _ {f}, t), \quad t \in [ 0, t _ {f} ] \tag {57}$$

from (56), where $\epsilon ( k , t _ { f } , t ) \in \mathbb { R } ^ { m }$ is given by

$$
\begin{array}{l} \epsilon (k, t _ {f}, t) := - e _ {k} ^ {u} (t) - K (t) \mathcal {G} _ {x} e _ {k} ^ {u} (t) \\ + R ^ {- 1} B ^ {\prime} \hat {e} ^ {P} (t _ {f} - t) x _ {k} (t). \tag {58} \\ \end{array}
$$

By applying $u _ { k }$ to the system (5) and sampling n points on the state trajectory in the range $t _ { i } \in [ 0 , \bar { t } ]$ , we can write

$$- K _ {\infty} X _ {t _ {1}: t _ {n}} = U _ {t _ {1}: t _ {n}} + E _ {t _ {1}: t _ {n}}, \tag {59}$$

where

$$E _ {t _ {1}: t _ {n}} = \left[ \epsilon (k, t _ {f}, t _ {1}) \epsilon (k, t _ {f}, t _ {2}) \dots \epsilon (k, t _ {f}, t _ {n}) \right].$$
