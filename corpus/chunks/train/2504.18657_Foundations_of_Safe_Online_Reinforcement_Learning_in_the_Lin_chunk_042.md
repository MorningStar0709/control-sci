Lemma 4. In the setting of Problem 1, for any θ ∈ Θ such that ∥θ − θ∗∥∞ ≤ 1log46(T ) , any $\theta \in \Theta$ $\begin{array} { r } { \| \theta - \theta ^ { * } \| _ { \infty } \leq \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$ $K \in [ \frac { a - 1 } { b } , \frac { a } { b } ]$ , and any x, y such that d $: = | y - x | \leq { \frac { 1 } { \log ^ { 1 0 } ( T ) } }$ ,

$$| C _ {K} ^ {\theta} (x) - C _ {K} ^ {\theta} (y) | = O _ {T} (d). \tag {9}$$

The proof of Lemma 4 can be found in Appendix C.3.

We also will need the following event, which is a subset of the event $E _ { 1 }$ applied only to times $i < t$ .

Definition 2. Define the event $E _ { 1 } ^ { t }$ as the event that for all $i \leq t - 1 , | w _ { i } | \leq \log ^ { 2 } ( T )$ .

We can proceed by bounding the difference in total costs conditional on the event $E _ { \mathrm { L 3 } } ( K , \theta , W ^ { \prime } ) \cap E _ { 1 } ^ { t }$ . Let $d _ { 0 } , d _ { 1 } , . . . , d _ { t }$ and $d _ { 0 } ^ { u } , . . . , d _ { t - 1 } ^ { u }$ respectively be the absolute difference in states and controls when starting at $x _ { 0 } = x$ versus starting at $x _ { 0 } = x + \delta$ and using controller $C _ { K } ^ { \theta }$ with noise $W ^ { \prime }$ . Let $x _ { 0 } , . . . , x _ { t }$ and $u _ { 0 } , . . . , u _ { t - 1 }$ be the states and controls when using controller $C _ { K } ^ { \theta }$ starting at $x _ { 0 } = x$ with noise $W ^ { \prime }$ . Then we have the following result conditional on $E _ { \mathrm { L 3 } } ( K , \theta , W ^ { \prime } ) \cap E _ { 1 } ^ { t }$ for sufficiently large T :
