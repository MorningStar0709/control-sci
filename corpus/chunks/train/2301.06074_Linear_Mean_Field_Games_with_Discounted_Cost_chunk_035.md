Note that (P2) and Lemma 2.1 imply that for any $y \in \mathsf { X } , \mathsf { y } _ { \{ 2 : N \} } , \mathsf { z } _ { \{ 2 : N \} } \in \mathsf { X } ^ { N - 1 }$ 1, we have

$$| V ^ {*} (y, \mathbf {y} _ {\{2: N \}}) - V ^ {*} (y, \mathbf {z} _ {\{2: N \}}) | \leq K _ {2} ^ {*, N} d _ {\mathrm{av}} (\mathbf {y} _ {\{2: N \}}, \mathbf {z} _ {\{2: N \}}) \tag {14}$$

Recall also the following definition for any $M \ge 1 : \mathcal { P } _ { M } ( { \sf X } ) : = \sf e [ \cdot | \sf X ^ { M } ]$ ; that is, ${ \mathcal { P } } _ { M } ( \mathsf { X } )$ is the image of e into $\mathcal { P } ( \mathsf { X } )$ . Now, we define the following set

$$\mathsf {S} ^ {N} := \left\{(\mu , x) \in \mathcal {P} _ {N} (\mathsf {X}) \times \mathsf {X}: \frac {N}{N - 1} \mu - \frac {1}{N - 1} \delta_ {x} \in \mathcal {P} _ {N - 1} (\mathsf {X}) \right\}$$

Note that for any policy $\pi \in \Pi$ , initial state $\mathbf { x } ,$ and $t \ge 0 , \mathrm { M D P } _ { N }$ satisfies the following

$$(\mathsf {e} [ \cdot | \mathbf {x} (t) ], x _ {1} (t)) \in \mathsf {S} ^ {N}$$

that is; $\mathsf { S } ^ { N }$ is the reachability set of the pair $( \mathsf { e } [ \cdot | \mathbf { x } ( t ) ] , x _ { 1 } ( t ) )$ for any $t \geq 0$ .

Recall the optimality equation for $\mathrm { M D P } _ { N }$

$$V ^ {*} (\mathbf {x}) = \min _ {a \in \mathsf {A}} \left[ c (x _ {1}, a, \mathsf {e} [ \cdot | \mathbf {x} ]) + \beta \int_ {\mathsf {X} ^ {N}} V ^ {*} (\mathbf {y}) \prod_ {i = 2} ^ {N} p (d y _ {i} | x _ {i}, \pi^ {*} (x _ {i}), \mathsf {e} [ \cdot | \mathbf {x} ]) \bigotimes p (d y _ {1} | x _ {1}, a, \mathsf {e} [ \cdot | \mathbf {x} ]) \right]$$

It is known that the best-response policy, denoted as $\gamma _ { \mathrm { o p t } } ^ { N } : \mathsf { X } ^ { N } \to \mathsf { A }$ , is the minimizer of the above optimality equation for all $\mathbf { x } \in \mathsf { X } ^ { N }$ . We define $T ^ { \hat { N } } : \mathsf { X } \times \mathsf { S } ^ { N } \to [ 0 , \infty )$ as follows

$$T ^ {N} (y, \mu , x) := \int_ {\mathbf {X} ^ {N - 1}} V ^ {*} (y, \mathbf {y} _ {\{2: N \}}) \prod_ {i = 2} ^ {N} p (d y _ {i} | x _ {i}, \pi^ {*} (x _ {i}), \mu)$$

where

$$(x _ {2}, \dots , x _ {N}) \in \mathsf {e} ^ {- 1} \left(\frac {N}{N - 1} \mu - \frac {1}{N - 1} \delta_ {x}\right) := \left\{\mathbf {x} _ {\{2: N \}} \in \mathsf {X} ^ {N - 1}: \mathsf {e} [ \cdot | \mathbf {x} _ {\{2: N \}} ] = \frac {N}{N - 1} \mu - \frac {1}{N - 1} \delta_ {x} \right\}$$

Since $V ^ { * }$ is symmetric in $( x _ { 2 } , \ldots , x _ { N } )$ , we have
