The reason for doing that is because, under assumptions (H1)–(H4) and (H6), $u ^ { \varepsilon }$ coincides with the value function $V ^ { \varepsilon }$ , which will be defined later on (see (4.4)), of an ε-penalized absolutely continuous/switching (ε-PACS) control problem; see Section 4. Although the solution u to the HJB equation (1.10) can be constructed directly as a limit of $u ^ { \varepsilon , \delta }$ , when $( \varepsilon , \delta )$ goes to (0, 0), we required first to analyse the properties of the optimal stochastic control associated with the ε-PACS control problem mentioned above, in such a way that we can corroborate the equivalence between u and V in O.

We would like to mention that the NPDS (1.13), named in the PDE theory as a nonlinear elliptic cooperative system, is a problem of interest itself because we can find literature related to this problem only when the regime set M is a singleton set; see, i.e., [4, 15, 18].

Under assumptions (H1), (H3) and (H4), the following result is obtained.

Proposition 1.3. Let $\varepsilon , \delta \in ( 0 , 1 )$ be fixed. There exists a unique non-negative solution $u ^ { \varepsilon , \delta } = ( u _ { \ell , \iota } ^ { \varepsilon , \delta } ) _ { ( \ell , \iota ) \in \mathbb { N } \times \mathbb { I } }$ to the NPDS (1.13) where $u _ { \ell , \iota } ^ { \varepsilon , \delta } \in \mathrm { C } ^ { 4 , \alpha } ( \overline { { \mathcal { O } } } )$ for each $( \ell , \iota ) \in \mathbb { M } \times \mathbb { I }$ .

Under assumptions (H1)–(H4), the following result is obtained.

Proposition 1.4. For each $\varepsilon \in ( 0 , 1 )$ fixed, there exists a unique non-negative strong solution $u ^ { \varepsilon } = ( u _ { \ell , \iota } ^ { \varepsilon } ) _ { \mathbb { M } \times \mathbb { I } }$ to the HJB equation (1.15) where $u _ { \ell , \iota } ^ { \varepsilon } \in \mathrm { C } ^ { 0 } ( \overline { { \mathcal { O } } } ) \cap \mathrm { W } _ { \mathrm { l o c } } ^ { 2 , \infty } ( \mathcal { O } )$ for each $( \ell , \iota ) \in \mathbb { M } \times \mathbb { I }$ .
