```mermaid
graph TD
    A["Start"] --> B["User Interface receives controller design requirements"]
    B --> C["Manager Agent parses the controller design requirements"]
    C --> D["Manager Agent plans the design process and assigns tasks to individual agents"]
    D --> E["Model Design Agent selects the pre-configured control object model"]
    D --> F["Objective Design Agent determines control objectives and performance indicators"]
    E --> G["Model Design Agent configures the model according to requirements"]
    F --> H["Control Algorithm Design Agent selects the appropriate control algorithm"]
    G --> I["Control Parameter Design Agent combines model design with controller parameters and evaluates performance"]
    H --> I
    I --> J["Model Design Agent transmission simulation system model"]
    I --> K["Control Algorithm Design Agent transfer controller algorithm code"]
    J --> L["Control Verification Agent verifies the performance of the controller through experiments or simulations"]
    K --> L
    L --> M{Meet performance indicators}
    M -->|No| N["End"]
    M -->|Yes| O["User Interface displays controller design results"]
```
</details>

Fig. 2. Flow chart of Objective Oriented Controller Design.

2) Objective Design Agent: This agent is responsible for determining the control objectives and performance indicators. It needs to set the control objectives of the system according to its actual needs, such as stabilizing the system, tracking a given trajectory, or making the system respond faster, etc. At the same time, it needs to set corresponding performance indicators, such as the stability of the system, tracking error, response time, and oscillation, etc.   
3) Model Design Agent: The Model Design Agent is responsible for establishing a simulation model of the control object. This model can help us understand the dynamic be-

havior of the system and provide necessary information for the design of the controller.
