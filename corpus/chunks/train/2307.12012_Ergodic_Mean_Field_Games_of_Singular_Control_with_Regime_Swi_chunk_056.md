Step 2: Let $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ . By Theorem 3.1, we know that $v ( \cdot , i ; \theta ) \ = \ k _ { 1 } , \ x \ \in \ ( \underline { { x } } , \alpha _ { i } ( \theta ) )$ and $v ( \cdot , i ; \theta ) = k _ { 2 } , \ x \in ( \beta _ { i } ( \theta ) , \overline { { x } } )$ for any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ . Hence, it is sufficient to show that $v ( \cdot , i ; \theta ) \in C ^ { 2 } ( \mathcal { C } ^ { \theta } )$ , for any $( i , \theta ) \in \mathbb { Y } \times \mathbb { R } _ { + }$ . We then define the following boundary value problem:

$$
\left\{ \begin{array}{l} \mathcal {L} _ {\widehat {X}} w (x, i; \theta) + (b _ {x} (x, i) - q _ {i i}) w (x, i; \theta) = f (x, i; \theta), (x, i) \in \mathcal {C} ^ {\theta} \\ w (\alpha_ {i} (\theta), i; \theta) = v (\alpha_ {i} (\theta), i; \theta), w (\beta_ {i} (\theta), i; \theta) = v (\beta_ {i} (\theta), i; \theta), \end{array} \right. \tag {A.12}
$$

where $\begin{array} { r } { \mathcal { L } _ { \widehat { X } } w ( x , i ; \theta ) : = \frac { 1 } { 2 } \sigma ^ { 2 } ( x , i ) w _ { x x } ( x , i ; \theta ) + ( b ( x , i ) + \sigma \sigma _ { x } ( x , i ) ) w _ { x } ( x , i ; \theta ) } \end{array}$ and $f ( x , i ; \theta ) : =$ $\textstyle - \sum _ { j \neq i } { \overleftrightarrow { q } } _ { i j } v ( x , j ; \theta ) - { \bar { \pi } } _ { x } ( x , \theta )$ . Using Condition (4) in Assumption 2.1 we conclude that (A.12) has a unique classical solution $w ( \cdot , i ; \theta ) \in C ^ { 2 } ( \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) )$ (see Theorem 6.13 in [30]). We then define the function $\overline { { w } } : ( \alpha _ { i } ( \theta ) , \beta _ { i } ( \theta ) ) \times \mathbb { Y } \times \mathbb { R } _ { + } \mapsto \mathbb { R }$ by:

$$
\bar {w} (x, j; \theta) := \left\{ \begin{array}{l l} w (x, i; \theta), & j = i \\ v (x, j; \theta), & j \neq i, \end{array} \right.
$$

and the stopping time $\tau _ { Y } : = \operatorname* { i n f } \{ t \geq 0 : Y _ { t } ^ { i } \neq i \}$ . Hence, setting $\eta ( \theta ) : = \tau _ { Y } \wedge \tau ^ { * } ( \theta ) \wedge \sigma ^ { * } ( \theta )$ , from Dynkin’s formula we have:

(A.13)

$$\bar {w} (x, i; \theta) = w (x, i; \theta) = \widehat {\mathbb {E}} _ {(x, i)} \bigg [ v (\widehat {X} _ {\eta (\theta)}, Y _ {\eta (\theta)}; \theta) e ^ {\int_ {0} ^ {\eta (\theta)} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} + \int_ {0} ^ {\eta (\theta)} e ^ {\int_ {0} ^ {t} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} \pi_ {x} (\widehat {X} _ {t}, \theta) d t \bigg ]$$
