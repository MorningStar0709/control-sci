# # Client (trusted)

9: for $i = 1 , \ldots , K$ do   
10: $\tilde { U } ^ { ( i ) }  \mathsf { D e c } ( \mathbf { U } ^ { ( i ) } )$

11: $\begin{array} { r } { \check { s } _ { \ell } ^ { ( i ) } \gets [ \mathsf { D e c } ( \mathbf { s } _ { \ell } ( U ^ { ( i ) } ; x _ { 0 } ) ) ] _ { 1 } \quad ( [ \cdot ] _ { 1 } , } \end{array}$ first element)

12: $\tilde { s } _ { \ell } ( U ^ { ( i ) } ; x _ { 0 } ) \gets$ max $\left( \check { s } _ { \ell } ( U ^ { ( i ) } ; x _ { 0 } ) - \tau _ { s } , 0 \right)$

13: Compute weight $\tilde { r } _ { \ell } ^ { ( i ) } = \exp ( - \eta \tilde { s } _ { \ell } ( U ^ { ( i ) } ; x _ { 0 } ) )$

14: Compute $\begin{array} { r } { \tilde { U } _ { K , \ell } ( \boldsymbol { x } _ { 0 } ) : = \frac { \sum _ { i = 1 } ^ { K } \tilde { U } ^ { ( i ) } \tilde { r } _ { \ell } ^ { ( i ) } } { r } } \end{array}$ P i=1 （20 $\textstyle \sum _ { i = 1 } ^ { K } \tilde { r } _ { \ell } ^ { ( i ) }$

15: $u ( t ) \gets [ I _ { m } , 0 ] \tilde { U } _ { K , \ell } ( x _ { 0 } )$

Theorem 2: For any fixed $x _ { 0 } ~ \in ~ \mathbb { R } ^ { n }$ and $U ^ { ( i ) } ~ \in ~ \mathbb { R } ^ { N n }$ m defined as in (23), there exist $\mathsf { B } ^ { U } , \mathsf { B } ^ { s } \in { \mathcal { O } } ( N _ { \mathsf { c t } } / \Delta )$ such that

$$\left\| \tilde {U} ^ {(i)} - U ^ {(i)} \right\| \leq \mathsf {B} ^ {U} \tag {28}\left\| \tilde {s} _ {\ell} (U ^ {(i)}; x _ {0}) - \bar {s} _ {\ell} (U ^ {(i)}; x _ {0}) \right\| \leq \mathsf {B} ^ {s}. \tag {29}$$

Proof: First, we establish the bound in (28). Using the method in [18, Sec. 4.3], evaluating $\mathbf { c } _ { L U } ^ { ( i ) }$ requires performing Nm homomorphic multiplications followed by Nm homomorphic rotations. Consequently, the decryption error is bounded as

$$
\begin{array}{l} \left\| \operatorname{Dec} \left(\mathbf {c} _ {L U} ^ {(i)}\right) - L _ {U} \xi^ {(i)} \right\| = \left\| \operatorname{Dec} \left(\operatorname{Enc} \left(L _ {U}\right) \otimes \operatorname{Enc} \left(\xi^ {(i)}\right)\right) - L _ {U} \xi^ {(i)} \right\| \\ \leq N m \left(\mathsf {B} ^ {\otimes} (\| L _ {U} \|, \| \xi^ {(i)} \|) + \mathsf {B} ^ {\text { Rot }}\right) \tag {30} \\ \end{array}
$$

by Lemma 1. Further applying Lemma 1 and (30) yields

$$\left\| \tilde {U} ^ {(i)} - U ^ {(i)} \right\| = \left\| \left(\operatorname{Dec} \left(\operatorname{Enc} \left(m _ {U} \left(x _ {0}\right)\right)\right) + \operatorname{Dec} \left(\mathbf {c} _ {L U} ^ {(i)}\right)\right) - U ^ {(i)} \right\|\leq \mathsf {B} ^ {\mathsf {E n c}} + N m \cdot \left(\mathsf {B} ^ {\otimes} (\| L _ {U} \|, \| \xi^ {(i)} \|) + \mathsf {B} ^ {\mathsf {R o t}}\right) =: \mathsf {B} ^ {U}.$$
