# Continuous Control Design

The first ingredient of our solution is the development of a decentralized feedback control law that establishes a transition relation $\pi _ { k }  _ { i } \pi _ { k ^ { \prime } } , \forall k , k ^ { \prime } \in$ $\mathcal { K } _ { \mathcal { R } }$ according to Def. 5.2. The proposed approach is based on the concept of Decentralized Navigation Functions, introduced in [309], for which an overview can be found in Appendix C. More specifically, given that $\boldsymbol { \mathcal { A } } _ { i } ( \boldsymbol { p } _ { i } ( t _ { 0 } ) )$ for some $t _ { 0 } \geq 0$ , we propose a decentralized control law $u _ { i }$ for the transition $\pi _ { k }  _ { i } \pi _ { k ^ { \prime } }$ , as defined in Def. 5.2.

Initially, we define the set of “undesired” regions as $\Pi _ { k , k ^ { \prime } } : = \{ \pi _ { m } \in$ Π, m $\in \mathcal { K } _ { \mathcal { R } } \backslash \{ k , k ^ { \prime } \} \}$ and the corresponding free space $\mathcal { F } _ { i _ { k . k ^ { \prime } } } : = \{ p \in \mathcal { W } ^ { N }$ : $\bar { \mathcal { B } } ( p _ { i } , r _ { i } ) \cap \bar { \mathcal { B } } ( p _ { j } , r _ { j } ) = \emptyset , \forall j \in \mathcal { N } \backslash \{ j \} , \bar { \mathcal { B } } ( p _ { i } , r _ { i } ) \cap \pi = \emptyset , \forall \pi \in \Pi _ { k , k ^ { \prime } } \}$ , with $p : =$ $[ p _ { 1 } ^ { \top } , \ldots , p _ { N } ^ { \top } ] ^ { \top }$ . As the goal configuration we consider the centroid $p _ { \pi _ { k ^ { \prime } } }$ of $\pi _ { k ^ { \prime } }$ and we construct the function $\gamma _ { i _ { k ^ { \prime } } } : \mathbb { R } ^ { 3 }  \mathbb { R } _ { \geq 0 }$ with $\gamma _ { i _ { k ^ { \prime } } } ( p _ { i } ) : = \| p _ { i } - p _ { \pi _ { k ^ { \prime } } } \| ^ { 2 }$ For the collision avoidance between the agents, we employ the function $G _ { i } : \mathcal { F } _ { i _ { k , k ^ { \prime } } } \to \mathbb { R }$ as defined in [309], which encodes the distances among the agents.
