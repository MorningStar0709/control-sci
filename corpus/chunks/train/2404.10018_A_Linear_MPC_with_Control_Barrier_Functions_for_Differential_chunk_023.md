# A. Stability and Complexity Analysis

In this work, we consider ?? as a hyperparameter to be selected such that the feasibility of the problem holds depending on the number of obstacles against the robot. Enforcing a very strong safety constraint could lead to unfeasible solutions.

Assumption 1. The optimization problem (48)-(54) is feasible for the initial time $k = 0 .$ .

Theorem 1. (Asymptotic Convergence of SCMPCDFL scheme) The extended dynamics of the unicycle model in (23) is asymptotically stable and the obstacles are avoided using SCMPCDFL scheme if Assumption 1 hold true and the terminal weight ??¯ in (48) is selected to be equivalent to the infinite horizon cost where

$$\sum_ {i = 0} ^ {\infty} (\| z (i) \| _ {Q} ^ {2} + \| v (i) \| _ {R} ^ {2}) = z ^ {\top} (0) \bar {Q} z (0) \tag {57}$$

and

$$\bar {Q} - (A _ {d} + B _ {d} K) ^ {\top} \bar {Q} (A _ {d} + B _ {d} K) = Q + K ^ {\top} R K. \tag {58}$$

such that the optimization problem in (48)-(54) is recursively feasible given the K is stabilizing $( A _ { d } + B _ { d } K ) ^ { i } ~ \forall i = 1 , . . . N _ { c }$ and ${ \mathcal H } ( ( A _ { d } + B _ { d } K ) z _ { d } ) ~ > ~ ( 1 - \gamma ) { \mathcal H } ( z _ { d } ) ~ \forall z _ { d } ~ \in ~ { \mathcal Z } _ { f }$ with sufficiently large $N _ { c }$ and $0 < \gamma \leq 1$ .
