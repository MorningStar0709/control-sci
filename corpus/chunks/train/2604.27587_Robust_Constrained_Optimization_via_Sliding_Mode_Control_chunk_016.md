# 3.2 Robustness of Sliding Mode Optimization Controller

The sliding mode optimization controller possesses inherent robustness, which can be rigorously analyzed within the framework of differential inclusions and set-valued mappings. Theorem 6 formalizes these robustness properties by considering various perturbation scenarios, including matched, structural uncertainty and $L _ { \infty } .$ bounded measurement noise.

Theorem 6 Consider the nominal system (3) augmented with a Filippov sliding mode control law:

$$\lambda \in - (J _ {h} J _ {h} ^ {\top}) ^ {- 1} J _ {h} \nabla \phi (x) + K (J _ {h} J _ {h} ^ {\top}) ^ {- 1} \mathrm{SGN} (S (x)), \tag {7}$$

Under this control law, the system exhibits the following robustness properties:

(1) Matched Disturbances: If the disturbance $\xi ( t , x ) \in$ range $( J _ { h } ^ { \top } ) \ : = \ : \ker ( J _ { h } ) ^ { \bot }$ , then the sliding manifold $S ( \bar { x } ) \overset { } { = } \left\{ x \in \mathbb { R } ^ { n } \ | \ h ( \bar { x } ) = 0 \right\}$ remains invariant for the perturbed system:

$$\dot {x} = - \nabla \phi (x) - J _ {h} ^ {\top} \lambda + \xi (t, x). \tag {8}$$

where $\xi ( t , x ) = J _ { h } ^ { \top } \eta ( t , x )$ , where $\| \eta ( t , x ) \| \leq \bar { \eta }$

(2) Structured Uncertainty: If the control matrix is subject to bounded time-varying perturbations $\Delta J _ { h } ( t )$ , and the modified matrix preserves full rank: inf t 0 σmin $\left[ ( J _ { h } + \Delta J _ { h } ( t ) ) ( J _ { h } + \Delta J _ { h } ( t ) ) ^ { \top } \right] \ge \alpha >$ 0, Then, the sliding phase and convergence properties are maintained.   
(3) Measurement Noise: If the sliding variable is corrupted by $\mathcal { L } _ { \infty } .$ - bounded noise η˜(t) with $\| \tilde { \eta } ( t ) \| \leq \delta ,$ the system evolves $a s { : } \dot { x } = - \nabla \dot { \phi } ( x ) { - } J _ { h } ^ { \top } \ddot { \lambda } ( \ddot { S } ( x ) _ { m e a s } )$ and the state remains within a boundary layer around the manifold, $i . e . , \| S ( x ) \| \le \Delta ( \delta , K )$ , where $\Delta$ is a function of the noise level and control gain.

PROOF. We provide a detailed analysis for all considered perturbation scenarios to comprehensively establish the robustness properties of the sliding mode optimization controller.
