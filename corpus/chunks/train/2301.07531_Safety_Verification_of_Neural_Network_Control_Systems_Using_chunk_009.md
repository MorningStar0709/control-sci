\tilde {\phi} _ {\ell} (\cdot) = \left\{ \begin{array}{l l} \left[ \begin{array}{c} \phi_ {\ell} (\cdot) \\ \hat {\phi} _ {\ell} (\cdot) \end{array} \right], & 1 \leq \ell \leq \hat {L} - 1 \\ \left[ \begin{array}{c} \phi_ {\ell} (\cdot) \\ \text {purelin} (\cdot) \end{array} \right], & \hat {L} \leq \ell \leq L - 1 \\ \left[ \begin{array}{c} \phi_ {L} (\cdot) \\ \hat {\phi} _ {\hat {L}} (\cdot) \end{array} \right], & \ell = L \\ \text {purelin} (\cdot), & \ell = L + 1 \end{array} \right. \tag {16}
$$

where purelin(·) is linear transfer function, i.e., $x \quad =$ purelin(x).

Proof: Given an input $\eta _ { 0 } \in \mathcal { U }$ and $\tilde { \eta } _ { 0 } = \hat { \eta } _ { 0 } = \eta _ { 0 }$ and considering layers $1 \leq \ell \leq \hat { L } - 1$ of $\tilde { \Phi }$ , we have

$$
\tilde {\boldsymbol {\eta}} _ {\hat {L} - 1} = \left[ \begin{array}{l} \phi_ {L - 1} \circ \dots \circ \phi_ {1} (\mathbf {W} _ {1} \boldsymbol {\eta} _ {0} + \mathbf {b} _ {1}) \\ \hat {\phi} _ {\hat {L} - 1} \circ \dots \circ \hat {\phi} _ {1} (\hat {\mathbf {W}} _ {1} \hat {\boldsymbol {\eta}} _ {0} + \hat {\mathbf {b}} _ {1}) \end{array} \right] \tag {17}
$$

Specifically, we consider \` = 1 such that

$$
\tilde {\boldsymbol {\eta}} _ {1} = \left[ \begin{array}{l} \phi_ {1} (\mathbf {W} _ {1} \boldsymbol {\eta} _ {0} + \mathbf {b} _ {1}) \\ \hat {\phi} _ {1} (\hat {\mathbf {W}} _ {1} \hat {\boldsymbol {\eta}} _ {0} + \hat {\mathbf {b}} _ {1}) \end{array} \right] = \left[ \begin{array}{l} \boldsymbol {\eta} _ {1} \\ \hat {\boldsymbol {\eta}} _ {1} \end{array} \right] \tag {18}
$$

Moreover, when $1 < \ell \leq \hat { L } - 1$ , it leads to

$$
\tilde {\mathbf {W}} _ {\ell} \tilde {\boldsymbol {\eta}} _ {\ell - 1} + \tilde {\mathbf {b}} _ {\ell} = \left[ \begin{array}{l} \mathbf {W} _ {\ell} \boldsymbol {\eta} _ {\ell - 1} + \mathbf {b} _ {\ell} \\ \hat {\mathbf {W}} _ {\ell} \hat {\boldsymbol {\eta}} _ {\ell - 1} + \hat {\mathbf {b}} _ {\ell} \end{array} \right], 1 <   \ell \leq \hat {L} - 1 \tag {19}
$$

Staring from $\ell = 1$ and recursively using (19) into (17), one can obtain

$$
\tilde {\boldsymbol {\eta}} _ {\hat {L} - 1} = \left[ \begin{array}{l} \boldsymbol {\eta} _ {\hat {L} - 1} \\ \hat {\boldsymbol {\eta}} _ {\hat {L} - 1} \end{array} \right] \tag {20}
$$

Furthermore, when $\hat { L } \leq \ell \leq L - 1$ , one can derive
