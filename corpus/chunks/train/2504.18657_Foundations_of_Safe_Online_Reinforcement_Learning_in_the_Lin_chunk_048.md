$$\mathcal {Z} _ {i} := \left\{\min (x _ {i}, y _ {i}) \leq \frac {D _ {L}}{a - b K} \leq \max (x _ {i}, y _ {i}) \leq \frac {D _ {U}}{a - b K} \mathrm{or} \frac {D _ {L}}{a - b K} \leq \min (x _ {i}, y _ {i}) \leq \frac {D _ {U}}{a - b K} \leq \max (x _ {i}, y _ {i}) \right\}$$

and define

$$\kappa (j) = | \{0 \leq i \leq j: \mathcal {Z} _ {i} \} |.$$

Because Lemma 7 implies that $\begin{array} { r } { \frac { a } { b } = \frac { a ^ { * } } { b ^ { * } } + O _ { T } \left( \frac { 1 } { \log ^ { 1 0 } ( T ) } \right) } \end{array}$ , we have for $\begin{array} { r } { K \in \left\lceil \frac { a ^ { * } - 1 + \frac { 1 } { \log ^ { 9 } ( T ) } } { b ^ { * } } , \frac { a } { b } \right\rceil } \end{array}$ that |a∗ −b∗K | ≤ 1− 1log9(T ) $\textstyle | a ^ { * } - b ^ { * } K | \leq 1 - { \frac { 1 } { \log ^ { 9 } ( T ) } }$ . Because $\begin{array} { r } { \epsilon \leq \frac { 1 } { \log ^ { 4 6 } ( T ) } } \end{array}$ , this implies that $\begin{array} { r } { | a ^ { * } - b ^ { * } K | + \mathbf { \bar { \phi } } { O } _ { T } ( \epsilon ) \le 1 - \overline { { \frac { \mathbf { \phi } _ { 1 } } { 2 \log ^ { 9 } ( T ) } } } . } \end{array}$ . This will allow us to bound the ρ in Lemma 6 by 1 − 2log9(T ) . $\textstyle 1 - { \frac { 2 } { \log ^ { 9 } ( T ) } }$ Combining this with Lemma 6, we have the following piece-wise upper bound (note that we combined the W and the “otherwise” case using that $\begin{array} { r } { O _ { T } ( \epsilon ) \le 1 - \frac { 1 } { 2 \log ^ { 9 } ( T ) } } \end{array}$ for suff large T ),

$$
d _ {j + 1} \leq \left\{ \begin{array}{l l} \min \left(2 \left(1 - \frac {1}{2 \log^ {9} (T)}\right) d _ {j}, d _ {j} + O _ {T} (\epsilon) (| x _ {j} | + \| D \| _ {\infty})\right) & \text { if } \mathcal {Z} _ {j} \\ \left(1 - \frac {1}{2 \log^ {9} (T)}\right) d _ {j} & \text { otherwise. } \end{array} \right. \tag {13}
$$

Conditional on event $E _ { \frac { 1 } { 2 } } ^ { t }$ , for all $j \le t , | x _ { j } | \le { \cal O } _ { T } ( \log ^ { 2 } ( T ) )$ ) by Lemma 42 because $\| D \| _ { \infty } \leq$ $\log ^ { 2 } ( T )$ and $| x | \leq 4 \log ^ { 2 } ( T )$ . Starting with the base case that $d _ { 0 } = d _ { \pm }$ , this with Equation (13) implies the following two relationships both hold for $d _ { j + 1 }$ conditional on event $E _ { 1 } ^ { t }$ for sufficiently large T . Equation (14) holds because $\begin{array} { r } { \left( 1 - \frac { 1 } { 2 \log ^ { 9 } ( T ) } \right) \leq 1 } \end{array}$ for sufficiently large T and using the second term in the min of Equation (13). Equation (15) holds using the first term in the min of Equation (13).

$$d _ {j + 1} \leq d + \kappa (j) \cdot O _ {T} (\epsilon \log^ {2} (T)) \tag {14}$$

and
