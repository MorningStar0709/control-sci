Under the gain condition (18), the maximum of the initial tracking error and the disturbance-to-gain ratio is strictly less than $r _ { e } ^ { 2 }$ . Consequently, (26) implies the existence of a constant $\varpi > 0$ such that $\| e ( t ) \| \le r _ { e } - \varpi < r _ { e }$ for all $t \in [ t _ { 0 } , t _ { 0 } + \epsilon _ { 1 } ]$ . As a result, $t \mapsto e ( t )$ is not continuous at $t _ { 0 } + \epsilon _ { 1 }$ , which is a contradiction. Therefore, our initial assumption must be false, proving that $e ( t ) \in \overline { { B } } _ { e } ( r _ { e } )$ for all $t \in [ t _ { 0 } , t _ { 0 } + \delta )$ .

Because the solution is restricted to the compact set $\overline { { B } } _ { e } ( r _ { e } )$ over its maximal interval of existence, it is precompact. Since precompact maximal solutions are complete, we conclude that $\delta = \infty , { \mathrm { i . e . } }$ , the solution is defined and $e ( t ) \in \overline { { B } } _ { e } ( r _ { e } )$ for all $t \in [ t _ { 0 } , t _ { s } ]$ .

Part 2 - State Transition at $t = t _ { s } .$

At $t _ { s } ,$ the tracking error satisfies $e ( t _ { s } ) \in \overline { { B } } _ { e } ( r _ { e } )$ , while the projection operator ensures $\tilde { \theta } ( t _ { s } ) \in \overline { { B } } _ { \widetilde { \theta } } ( 2 r _ { \theta } + \epsilon )$ . Hence, the closed-loop state satisfies

$$
z (t _ {s}) = \left[ \begin{array}{l} e (t _ {s}) \\ \tilde {\theta} (t _ {s}) \end{array} \right] \in \overline {{B}} _ {e} (r _ {e}) \times \overline {{B}} _ {\tilde {\theta}} (2 r _ {\theta} + \epsilon). \tag {27}
$$

Part 3 - Analysis on the interval $[ t _ { s } , \infty ) .$ :

Consider the closed ball $\overline { { B } } _ { z } ( r ) \subset \mathbb { R } ^ { n + p }$ for $r > 0 .$ , where $r > 0$ is selected such that (19) is satisfied. This selection of r ensures that $ { \boldsymbol { z } } ( t _ { s } ) \in B _ { z } ( r )$ . For $t \geq t _ { s }$ , the system operates with a full-rank memory regressor, i.e., such that Assumption 2 holds.

Consider the candidate Lyapunov function $V : \mathbb { R } ^ { n + p }  \mathbb { R }$ defined as

$$V (z) := \frac {1}{2} \| e \| ^ {2} + \frac {1}{2} \tilde {\theta} ^ {\top} \Gamma^ {- 1} \tilde {\theta}. \tag {28}$$

which satisfies the bounds $\underline { { m } } \| z \| ^ { 2 } \leq V ( z ) \leq \overline { { m } } \| z \| ^ { 2 }$ for all $z \in \mathbb { R } ^ { n + p }$ , where $\underline { { m } } , \overline { { m } } > 0$ are constants defined below (19).
