# 2.9 Problems

Problem 2.1 Let

$$
A = \left( \begin{array}{c c c} 1 & 1 & 0 \\ 1 & 0 & 1 \\ 2 & 1 & 1 \\ 1 & 0 & 1 \\ 2 & 0 & 2 \end{array} \right).
$$

Determine the row and column rank of A and find bases for $\operatorname { I m } ( A )$ , Im(A∗), and $\operatorname { K e r } ( A )$ .

Problem 2.2 Let $D _ { 0 } = { \left[ \begin{array} { l l } { 1 } & { 4 } \\ { 2 } & { 5 } \\ { 3 } & { 6 } \end{array} \right] }$ . Find a D such that $D ^ { * } D = I { \mathrm { ~ a n d ~ I m } } D = { \mathrm { I m } } D _ { 0 }$ . Furthermore, find a $D _ { \perp }$ such that $\left[ \begin{array} { l l } { D } & { D _ { \perp } } \end{array} \right]$ is a unitary matrix.

Problem 2.3 Let A be a nonsingular matrix and $x , y \in \mathbb { C } ^ { n }$ . Show

$$(A ^ {- 1} + x y ^ {*}) ^ {- 1} = A - \frac {A x y ^ {*} A}{1 + y ^ {*} A x}$$

and

$$\det (A ^ {- 1} + x y ^ {*}) ^ {- 1} = \frac {\det A}{1 + y ^ {*} A x}.$$

Problem 2.4 Let A and B be compatible matrices. Show

$$B (I + A B) ^ {- 1} = (I + B A) ^ {- 1} B, \quad (I + A) ^ {- 1} = I - A (I + A) ^ {- 1}.$$

Problem 2.5 Find a basis for the maximum dimensional stable invariant subspace of $H = \left[ \begin{array} { c c } { { A } } & { { R } } \\ { { - Q } } & { { - A ^ { * } } } \end{array} \right]$ with

$1 . ~ A = { \left[ \begin{array} { l l } { - 1 } & { 2 } \\ { 3 } & { 0 } \end{array} \right] } , ~ R = { \left[ \begin{array} { l l } { - 1 } & { - 1 } \\ { - 1 } & { - 1 } \end{array} \right] } , { \mathrm { a n d } } ~ Q = 0$   
2. A =  0 10 2  , R =  0 00 −1  , and $Q = { \left[ \begin{array} { l l } { 1 } & { 2 } \\ { 2 } & { 4 } \end{array} \right] }$   
$A = 0 , \ R = { \left[ \begin{array} { l l } { 1 } & { 2 } \\ { 2 } & { 5 } \end{array} \right] }$

Problem 2.6 Let $A = \left[ a _ { i j } \right]$ . Show that $\alpha ( A ) : = \operatorname* { m a x } _ { i , j } | a _ { i j } |$ defines a matrix norm. Give examples so that $\alpha ( A ) < \rho ( A )$ and $\alpha ( A B ) > \alpha ( A ) \alpha ( B )$ .

Problem 2.7 Let $A = { \left( \begin{array} { l l l } { 1 } & { 2 } & { 3 } \\ { 4 } & { 1 } & { - 1 } \end{array} \right) }$ and $B = { \binom { 0 } { 1 } }$  0  . (a) Find all x such that $A x = B$ (b) Find the minimal norm solution x: min $\{ \| x \| : { \overset { } { A } } x = B \}$ .

Problem 2.8 Let $A = { \left( \begin{array} { l l } { 1 } & { 2 } \\ { - 2 } & { - 5 } \\ { 0 } & { 1 } \end{array} \right) } { \mathrm { ~ a n d ~ } } B = { \left( \begin{array} { l } { 3 } \\ { 4 } \\ { 5 } \end{array} \right) }$ Find an x such that $\| A x - B \|$ is minimized.

Problem 2.9 Let $\| A \| < 1$ . Show

1. $( I - A ) ^ { - 1 } = I + A + A ^ { 2 } + \cdot \cdot \cdot$ .   
2. $\left\| ( I - A ) ^ { - 1 } \right\| \leq 1 + \left\| A \right\| + \left\| A \right\| ^ { 2 } + \cdot \cdot \cdot = { \frac { 1 } { 1 - \left\| A \right\| } } .$   
3. 
 (I − A)−1 
 ≥ 11+ A . $\left\| ( I - A ) ^ { - 1 } \right\| \geq { \frac { 1 } { 1 + \| A \| } }$
