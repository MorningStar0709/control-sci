# D. Input Convex Neural Network

Input Convex Neural Network (ICNN) is a feedforward neural network architecture with constraints on the neural network parameters to ensure that the output of the neural network is convex with respect to all inputs (Fully Input Convex Neural Networks or FICNN) or with respect to a subset of inputs (Partially Input Convex Neural Network or PICNN).

FICNN uses the following architecture for $i = 0 , \ldots , k - 1$

$$\boldsymbol {y} _ {i + 1} = \sigma_ {i} \left(\boldsymbol {W} _ {i} ^ {(y)} \boldsymbol {y} _ {i} + \boldsymbol {W} _ {i} ^ {(x)} \boldsymbol {x} + \boldsymbol {b} _ {i}\right), f (\boldsymbol {x}; \boldsymbol {\theta}) = \boldsymbol {y} _ {k}, \tag {6}$$

where input, $\sigma _ { i }$ y0 = 0, W (y)0 $y _ { 0 } = \stackrel { \cdot } { 0 } , W _ { 0 } ^ { ( y ) } \stackrel { \cdot } { = } 0 , \theta = \left\{ W _ { 1 : k - 1 } ^ { ( y ) } , W _ { 0 : k - 1 } ^ { ( x ) } , b _ { 0 : k - 1 } \right\}$ $f ( \pmb { x } ; \pmb { \theta } )$ in x if W (y)1:k− $W _ { 1 : k - 1 } ^ { ( y ) }$ 1 are nonnegative and the activation functions $\sigma _ { i }$ are convex and non-decreasing [31].

PICNN uses the following architecture for $i = 0 , \ldots , k - 1$

$$
\begin{array}{l} \boldsymbol {v} _ {i + 1} = \widetilde {\sigma} _ {i} (\widetilde {\boldsymbol {W}} _ {i} \boldsymbol {v} _ {i} + \widetilde {\boldsymbol {b}} _ {i}), \\ \boldsymbol {y} _ {i + 1} = \sigma_ {i} \left(\boldsymbol {W} _ {i} ^ {(y)} \left(\boldsymbol {y} _ {i} \circ (\boldsymbol {W} _ {i} ^ {(y v)} \boldsymbol {v} _ {i} + \boldsymbol {b} _ {i} ^ {(y)})\right)\right) \\ \left. + \boldsymbol {W} _ {i} ^ {(x)} \left(\boldsymbol {x} \circ \left(\boldsymbol {W} _ {i} ^ {(x v)} \boldsymbol {v} _ {i} + \boldsymbol {b} _ {i} ^ {(x)}\right)\right) + \boldsymbol {W} _ {i} ^ {(v)} \boldsymbol {v} _ {i} + \boldsymbol {b} _ {i}\right), \\ f (\widetilde {x}, x; \theta) = y _ {k}, \tag {7} \\ \end{array}
$$

where $\sigma _ { i }$ an d σi $\widetilde { \sigma } _ { i }$ represent layer activation {W (y) functions, $\begin{array} { r } { \{ W _ { 1 : k - 1 } ^ { ( y ) } , \boldsymbol { \bar { W } } _ { 1 : k - 1 } ^ { ( y v ) } , W _ { 0 : k - 1 } ^ { ( x ) } , W _ { 0 : k - 1 } ^ { ( x v ) } , \boldsymbol { \tilde { W } } _ { 0 : k - 1 } , b _ { 1 : k - 1 } ^ { ( y ) } , } \end{array}$ , W (yv) $\begin{array} { c c c c c c c c c c c c c } { y _ { 0 } } & { = } & { 0 , } & { v _ { 0 } } & { = } & { \widetilde { x } , } & { W _ { 0 } ^ { ( y ) } } & { = } & { 0 , } & { \pmb { \theta } } & { = } & { } \end{array}$ , W (x) e, W (xv) W (y) 0

$b _ { 0 : k - 1 } \ , b _ { 0 : k - 1 } ^ { ( x ) } , \widetilde { b } _ { 0 : k - 1 } \}$ f are the network parameduct. One can show that $f ( \widetilde { \boldsymbol { x } } , \boldsymbol { x } ; \boldsymbol { \theta } )$ is convex in x if W (y)1:k− $W _ { 1 : k - 1 } ^ { ( y ) }$ 1 are nonnegative and the activation functions are convex and non-decreasing [31]. It should be noted that ICNN can only guarantee the convexity and not the strong convexity.
