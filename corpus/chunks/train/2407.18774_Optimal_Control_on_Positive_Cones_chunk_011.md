$$\Lambda_ {*} = S + F ^ {\top} \Lambda_ {*} F - \left(F ^ {\top} \Lambda_ {*} G + R _ {1}\right) \left(R _ {2} + G ^ {\top} \Lambda_ {*} G\right) ^ {- 1} \left(G ^ {\top} \Lambda_ {*} F + R _ {1} ^ {\top}\right).
\begin{array}{l} A ^ {\top} \lambda (t) \longleftrightarrow F ^ {\top} \Lambda (t) F \\ B ^ {\top} \lambda (t) \longleftrightarrow \left[ \begin{array}{c c} 0 & F ^ {\top} \Lambda (t) G \\ G ^ {\top} \Lambda (t) F & G ^ {\top} \Lambda (t) G \end{array} \right]. \\ \end{array}
$$

That is, the equation in Theorem 1(ii) becomes the algebraic Riccati equation (the pseudo-inverses become inverses in light of (9)). This can then be solved in the usual way whenever the pair $\left( F , G \right)$ is stabilizable. Theorem 1(iii) can be similarly analyzed. In this case, the convex program reduces to the semidefinite program

Maximize tr (ΛX0)

$$
\text { subject   to } \left[ \begin{array}{c c} S - \Lambda & R _ {1} \\ R _ {1} ^ {\top} & R _ {2} \end{array} \right] + \left[ \begin{array}{c} F ^ {\top} \\ G ^ {\top} \end{array} \right] \Lambda \left[ \begin{array}{c c} F & G \end{array} \right] \succeq 0,   \Lambda \succeq 0.
$$

To conclude the example, let us finally connect explicitly to the standard LQR problem. The standard LQR problem is concerned with the optimization

$$
\text { Minimize } \quad \sum_ {t = 0} ^ {\infty} \left[ \begin{array}{c} x (t) \\ u (t) \end{array} \right] ^ {\top} \left[ \begin{array}{c c} S & R _ {1} \\ R _ {1} ^ {\top} & R _ {2} \end{array} \right] \left[ \begin{array}{c} x (t) \\ u (t) \end{array} \right] \text { over } \{u (t) \} _ {t = 0} ^ {\infty}
\text { subject to } \quad x (t + 1) = F x (t) + G u (t), x (0) = x _ {0}.$$

To see the connection to the matrix optimization only requires us to notice that whenever

$$
\left[ \begin{array}{l l} X (t) & U _ {1} (t) \\ U _ {1} (t) ^ {\top} & U _ {2} (t) \end{array} \right] = \left[ \begin{array}{l} x (t) \\ u (t) \end{array} \right] \left[ \begin{array}{l l} x (t) ^ {\top} & u (t) ^ {\top} \end{array} \right], \tag {10}
$$

the specified matrix dynamics ensure that

$$X (t + 1) = x (t + 1) x (t + 1) ^ {\top},$$

and also that
