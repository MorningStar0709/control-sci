where the ground-truth parameters are $a = 1 , b = - 1 , x _ { t h } = \ 1$ . A dataset of 15 pairs (x y) is generated from the ground-truth ,dynamics to train standard (unstructured) and structured GP regression models, implemented in GPJax (Pinder and Dodd, 2022). While the unstructured GP model does not assume any prior knowledge of the system, the structured GP model encodes the piecewise-linearity of the dynamics f by replacing the constant mean of an unstructured GP with (63). The unknown parameters a, b and $x _ { t h }$ in (63) are then trained to fit the data, leading to better prediction accuracy as shown in Fig. 6.
