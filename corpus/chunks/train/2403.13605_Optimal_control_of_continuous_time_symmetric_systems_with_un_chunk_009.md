# 1.2.3 Computational complexity

Most existing algorithms require solving a linear algebraic equation like (4) at each iteration, resulting in a computational complexity of at least ${ \cal O } ( l r ^ { 2 } )$ per iteration. For instance, $r = n ^ { 2 } + n m$ in $\begin{array} { r } { [ 3 2 ] , r \geq \frac { n ( n + 1 ) } { 2 } + n m } \end{array}$ in [16, 33, 15, 12, 2, 39]. In [10], the complexity is even higher, with $l \geq N$ and $\begin{array} { r } { r = N \left( \frac { n ( n + 1 ) } { 2 } + n m \right) } \end{array}$ , where N 2 is the number of intervals used to discretize the horizon $[ 0 , t _ { f } ]$ . In contrast, the proposed algorithm has a significantly lower computational complexity of $O ( N m ^ { 2 } )$ per iteration.
