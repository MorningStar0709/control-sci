$$\dot {U} = \left(\frac {\partial U (\sigma)}{\partial \sigma_ {\mathrm{sm}}}, \frac {\partial U (\sigma)}{\partial \sigma_ {\mathrm{rm}}}\right) \binom{\dot {\sigma} _ {\mathrm{sm}}}{\dot {\sigma} _ {\mathrm{rm}}}= - \nabla_ {\sigma_ {\mathrm{rm}}} U ^ {\top} (\Lambda / \Lambda_ {1 1}) \nabla_ {\sigma_ {\mathrm{rm}}} U\leq - \varepsilon \| \nabla_ {\sigma_ {\mathrm{rm}}} U (\sigma) \| ^ {2} \leq - \frac {\varepsilon c ^ {2}}{(l _ {\max} ^ {k}) ^ {2}} U (\sigma) ^ {2 \rho},$$

where we use the fact that $\Lambda / \Lambda _ { 1 1 } ( q , \eta ) \succ \varepsilon I _ { m - k }$ which is derived from Assumption 1 and the decomposition of $\Lambda ( q , \eta )$ as follows:

$$
\Lambda = \left( \begin{array}{c c} I _ {k} & 0 \\ \Lambda_ {1 2} ^ {\top} \Lambda_ {1 1} ^ {- 1} & I _ {m - k} \end{array} \right) \left( \begin{array}{c c} \Lambda_ {1 1} & 0 \\ 0 & \Lambda / \Lambda_ {1 1} \end{array} \right) \left( \begin{array}{c c} I _ {k} & \Lambda_ {1 1} ^ {- 1} \Lambda_ {1 2} \\ 0 & I _ {m - k} \end{array} \right).
$$

Thus, the inequality (19) holds with $a = a _ { k } = l _ { \operatorname* { m a x } } ^ { k }$ even if any sub-sliding mode occurs. Hence, it can be proved that (19) holds in both the reaching mode and any sub-sliding mode by redefining the maximum value of $\boldsymbol { a } _ { k } \boldsymbol { ^ { \prime } } \mathbf { s }$ in each case as $a = \operatorname* { m a x } _ { k } a _ { k }$ . By integrating (19) with respect to time, we obtain

$$U (\sigma (t)) \leq \left(- \frac {\varepsilon c ^ {2}}{a ^ {2}} (t - t _ {0}) + U (\sigma (t _ {0})) ^ {1 - 2 \rho}\right) ^ {\frac {1}{1 - 2 \rho}}.$$

Then, $U ( \sigma )$ becomes zero within a finite time $t _ { 0 } + a ^ { 2 } U ( \sigma ( t _ { 0 } ) ) ^ { 1 - 2 \rho } / ( \varepsilon c ^ { 2 } )$ . Therefore, the sliding variable σ converges to zero in a finite time.

In the last part of the proof, we will prove asymptotic stability of the closed-loop system. The time derivative of the Hamilton function $H _ { \mathrm { s m c } }$ in the reaching mode $\sigma _ { i } \neq 0$ is calculated as

$$\dot {H} _ {\mathrm{smc}} = - \frac {1}{4} \left\| \frac {\partial \sigma}{\partial \eta} ^ {- \top} \eta + 2 \nabla_ {\sigma} U \right\| _ {\Lambda} ^ {2} - \frac {1}{4} \left\| \frac {\partial \sigma}{\partial \eta} ^ {- \top} \eta \right\| _ {\Lambda} ^ {2} \prec 0.$$

In particular, if there are sub-sliding modes where

$$\dot {\sigma} _ {\mathrm{sm}} = 0, \dot {\sigma} _ {\mathrm{rm}} \neq 0,$$

holds, then it follows from (20) that
