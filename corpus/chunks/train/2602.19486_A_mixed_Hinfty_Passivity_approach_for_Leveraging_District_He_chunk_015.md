2) $\mathcal { H } _ { \infty }$ frequency–shaping filter: We introduce a high-pass weighting filter for loop–shaping and impose an $\mathcal { H } _ { \infty }$ performance constraint on the weighted output. The high-pass weight is chosen as W HP(s) = α + s/ωh1+s/ω , $\begin{array} { r } { W _ { \mathrm { H P } } ( s ) = \alpha + \frac { s / \omega _ { h } } { 1 + s / \omega _ { h } } , 0 < \alpha \ll 1 } \end{array}$ , where $\omega _ { h } > 0$ is the cutoff frequency of the first-order low-pass factor $1 / ( 1 + s / \omega _ { h } )$ . A statespace realization of the filter is $\begin{array} { r } { \dot { \mathbf { x } _ { \omega } } = - \omega _ { h } \mathbf { x } _ { \omega } + \omega _ { h } \mathbf { p } ^ { H } , z = ( 1 + } \end{array}$ $\bar { \alpha } ) p ^ { H } { - } x _ { \omega }$ , where $\pmb { x } _ { \omega } \in \mathbb { R } _ { n ^ { H P } }$ stores the low-frequency component of $ { p ^ { H } }$ n , while z represents the weighted high-frequency component to be minimized under the $\mathcal { H } _ { \infty }$ criterion. The small constant α slightly elevates the low-frequency gain for numerical robustness and does not affect the cutoff frequency ωh. Define the augmented weighted state $\pmb { x } _ { \mathrm { l p } } = \big [ \pmb { x } ^ { \top } \quad \pmb { x } _ { \omega } ^ { \top } \big ] ^ { \top } , ( \dot { \omega } ^ { H P }  z )$ admits the state-space form:

$$
\dot {\boldsymbol {x}} _ {\mathrm{lp}} = \underbrace {\left[ \begin{array}{c c} \boldsymbol {A} _ {\mathrm{cl}} (\boldsymbol {K}) & \boldsymbol {0} \\ \omega_ {h}   \boldsymbol {C} _ {y} (\boldsymbol {K}) & - \omega_ {h}   \boldsymbol {I} _ {n ^ {H P}} \end{array} \right]} _ {\boldsymbol {A} _ {\mathrm{lp}} (\boldsymbol {K})} \boldsymbol {x} _ {\mathrm{lp}} + \underbrace {\left[ \begin{array}{c} \boldsymbol {B} _ {\mathrm{cl}} ^ {(\omega)} \\ \boldsymbol {0} \end{array} \right]} _ {\boldsymbol {B} _ {\mathrm{lp}}} \boldsymbol {\omega} ^ {H P}

+ \left[ \begin{array}{c} \boldsymbol {B} _ {\mathrm{cl}} ^ {(h)} \\ \boldsymbol {0} \end{array} \right] \boldsymbol {w} ^ {\boldsymbol {h}}, \tag {21a}
\boldsymbol {z} = \underbrace {\left[ (1 + \alpha) \boldsymbol {C} _ {y} (\boldsymbol {K}) - \boldsymbol {I} _ {n ^ {H P}} \right]} _ {\boldsymbol {C} _ {\mathrm{lp}} (\boldsymbol {K})} \boldsymbol {x} _ {\mathrm{lp}}, \boldsymbol {D} _ {\mathrm{lp}} = \mathbf {0}. \tag {21b}$$
