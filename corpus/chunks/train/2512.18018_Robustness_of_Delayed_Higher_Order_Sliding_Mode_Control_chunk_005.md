# Notation.

The set of real numbers is denoted by R, then $\mathbb { R } _ { + } = \left\{ x \in \mathbb { R } : x \geq 0 \right\}$ and $\mathbb { R } _ { + } ^ { \star } = \mathbb { R } _ { + } \setminus \{ 0 \}$ are the sets of nonnegative and positive reals, respectively.
The Euclidean norm of a vector $x \in \mathbb { R } ^ { n }$ is denoted by x .
• A diagonal matrix with elements $\nu _ { i } , i = 1 , .
.
.
, n$ on the main diagonal is denoted by $\mathrm { d i a g } \{ \nu _ { i } \} _ { i = 1 } ^ { n }$ .
• For a symmetric matrix $P \in \mathbb { R } ^ { n \times n }$ , the minimum and maximum eigenvalues are represented by $\lambda _ { \operatorname* { m i n } } ( P )$ and $\lambda _ { \operatorname* { m a x } } ( P )$ .
• Denote the identity matrix of dimension $n \times n$ by $I _ { n }$ .
• For two metric spaces X and Y , the set of continuous maps between them is denoted by $C ( X , Y )$ .
The Banach space of continuous functions $C ( [ - \tau , 0 ] , \mathbb { R } ^ { n } )$ with a finite $\tau > 0$ will be denoted by $C _ { \tau }$ and equipped with the uniform norm $\| \varphi \| = \operatorname* { m a x } _ { - \tau \leq \xi \leq 0 } | \varphi ( \xi ) |$ for $\varphi \in C _ { \tau }$ .
−τ≤ξ≤0   
For a (Lebesgue) measurable function $d : \mathbb { R } _ { + } \to \mathbb { R } ^ { s }$ and $[ t _ { 0 } , t _ { 1 } ) \subset \mathbb { R } _ { + }$ define the norm $\begin{array} { r } { \| d \| _ { [ t _ { 0 } , t _ { 1 } ) } = \operatorname { e s s } \operatorname* { s u p } _ { t \in [ t _ { 0 } , t _ { 1 } ) } \| d ( t ) \| } \end{array}$ , then $\lVert d \rVert _ { \infty } = \lVert d \rVert _ { [ 0 , + \infty ) }$ and the set of d with the property $\| d \| _ { \infty } < + \infty$ we denote as $\mathcal { L } _ { \infty } ^ { s } \mathrm { ~ ( i .
e .
~ }$ , this is the set of essentially bounded measurable functions).
• For a locally Lipschitz continuous function $V : \mathbb { R } ^ { n }  \mathbb { R } _ { + }$ , the upper directional Dini derivative is defined as follows: $\begin{array} { r } { D ^ { + } V ( x ) v = \operatorname* { l i m } _ { h  0 ^ { + } } { \frac { V ( x ^ { - } + h v ) - V ( x ) } { h } } } \end{array}$ for any $x \in \mathbb { R } ^ { n }$ and $v \in \mathbb { R } ^ { n }$ .
• A function $\sigma \in C ( \mathbb { R } _ { + } , \mathbb { R } _ { + } )$ belongs to class K if it is strictly increasing and $\sigma ( 0 ) = 0 ;$ it additionally belongs to class $\kappa _ { \infty }$ if it is also unbounded.
A function $\beta \in C ( \mathbb { R } _ { + } \times \mathbb { R } _ { + } , \mathbb { R } _ { + } )$ belongs to class KL if $\beta ( \cdot , r ) \in \mathcal { K }$ for any $r \in \mathbb { R } _ { + }$ and $\beta ( r , \cdot )$ is decreasing to zero for any $r > 0$ .
