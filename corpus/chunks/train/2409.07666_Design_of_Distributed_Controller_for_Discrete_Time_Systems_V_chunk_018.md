Then, $K _ { \mathrm { d i a g } } ^ { \infty , \gamma } \subset K _ { s } ^ { \infty , \gamma } \subset K _ { \mathrm { a l l } } ^ { \infty , \gamma }$ holds.

Proof: This can be proved by following the similar procedure to Lemma 5 and [15].

The following theorem provides a less conservative convex solution set to the $H _ { \infty }$ control version of Problem 1 than both $K _ { \mathrm { e x t } } ^ { \infty , \gamma }$ and ${ \mathcal { K } } _ { S } ^ { \infty , \gamma }$ . A clique-wise decomposition is applied to the auxiliary matrix G in (8).

Theorem 2: Let

$$
\begin{array}{l} \mathcal {K} _ {\mathcal {S}, \text {ext}} ^ {\infty , \gamma} = \left\{K = (E ^ {\top} E) ^ {- 1} E ^ {\top} \tilde {Z} \tilde {G} ^ {- 1} E: \exists \tilde {Q} \succ O, \right. \\ \tilde {Z} = \operatorname{blkdiag} \left(\tilde {Z} _ {1}, \dots \tilde {Z} _ {q}\right), \\ \tilde {G} = \operatorname{blkdiag} \left(\tilde {G} _ {1}, \dots \tilde {G} _ {q}\right), \\ \rho \in \mathbb {R}, \eta > 0, \text { s.t. } \\ \Theta_ {\gamma} (\tilde {G}, \tilde {Q}, \tilde {Z}) + \operatorname{blkdiag} (\rho W, O _ {2 n \times 2 n}) \prec O, \\ \tilde {G} ^ {\top} U + U \tilde {G} \succeq \eta U \} \tag {29} \\ \end{array}
$$

with W , U in (9), $\tilde { H }$ in (13) and

$$
\begin{array}{l} \Theta_ {\gamma} (\tilde {G}, \tilde {Q}, \tilde {Z}) = \\ \left[ \begin{array}{c c c c} - \tilde {Q} & * & * & * \\ (\tilde {A} \tilde {G} + \tilde {B} \tilde {Z}) ^ {\top} & \tilde {Q} - \tilde {G} - \tilde {G} ^ {\top} & * & * \\ \tilde {B} _ {v} ^ {\top} & O & - \gamma I & * \\ O & \tilde {C} \tilde {G} + \tilde {D} \tilde {Z} & D _ {w} & - \gamma I \end{array} \right]. \tag {30} \\ \end{array}
$$

Then, ${ \mathcal { K } } _ { \mathrm { e x t } } ^ { \infty , \gamma } \cup { \mathcal { K } } _ { S } ^ { \infty , \gamma } \subset { \mathcal { K } } _ { S , \mathrm { e x t } } ^ { \infty , \gamma } \subset { \mathcal { K } } _ { \mathrm { a l l } } ^ { \infty , \gamma }$ holds.

Proof: See Appendix D. for the proof details.
