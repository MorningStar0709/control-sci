In Alg.
1, when reaching time step $t _ { f }$ and finding that solving CBF-CLF-QP is infeasible, the process reverts to $N _ { c }$ time steps earlier and retunes the hyperparameters to ensure feasibility.
$N _ { c }$ serves as a rollback horizon that enables the algorithm to proactively test feasibility over a past time window, allowing the algorithm to systematically re-evaluate and adjust hyperparameters before reaching infeasibility at $t _ { f } .$ This approach acts as an implicit feasibility test, preventing local infeasibility from propagating through future time steps.
From $4 ^ { \mathrm { t h } }$ step to $1 2 ^ { \mathrm { t h } }$ step, the algorithm first tunes the hyperparameters across the entire time window to ensure global consistency before proceeding to the next iteration $j + 1$ , rather than making iterative adjustments at each time step independently.
This approach allows for a more coordinated and holistic optimization of hyperparameters over the window, reducing local inconsistencies.
After optimizing the hyperparameters for the entire window, the algorithm then iterates to refine the adjustments further, ensuring the minimum value of $\psi _ { m _ { a } - 1 }$ remains above the feasibility-safety threshold ε.
By structuring the tuning process in this way, the algorithm minimizes unnecessary corrections and prevents abrupt fluctuations in hyperparameter values.
Additionally, it anticipates inter-sampling effects by iteratively checking the feasibility-safety threshold, ensuring that the system satisfies safety requirements under the optimized hyperparameters $a _ { i , w } ^ { * } ( t _ { l } )$ .
Note that by using the optimized hyperparameters $a _ { i , w } ^ { * } ( t _ { l } )$ in CBF-CLF-QP, we obtain the optimal control input ${ \mathbf { } } { \mathbf { } } { \mathbf { } } u ^ { * } ( t _ { l } )$ corresponding to these optimized hyperparameters at the same time step.
Since Alg.
1 terminates at $t _ { f } ,$ , which is likely smaller than $T ,$ , to ensure safe control over the entire interval [t0, T ], Alg.
1 may need to be executed multiple times, and each time, the future hyperparameters should be set as $a _ { i , w } ( t _ { k + 1 } ) = a _ { i , w } ^ { * } ( t _ { f } )$ where $k \in \{ f , .
.
.
, N - 1 \}$ .
The variable $j ( t _ { l } )$ records the number of iterations required at each time step within a single execution of the algorithm.
