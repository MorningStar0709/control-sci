While the system state is jointly observable by the agents [35], each agent $i \ ' s$ observation ${ \bf o } _ { i } ( t )$ is local 6. Once per time step, agent $i \in \mathcal N$ is allowed to transmit its local observations through a communication message $\mathbf { c } _ { i } ( t )$ to the CC. The communications between agents and the central controller are done in a synchronous (not sequential) and simultaneous (not delayed) fashion [17]. Each agent i generates its communication message $\mathbf { c } _ { i } ( t )$ by following its communication policy $\pi _ { i } ^ { c } ( \cdot ) : \Omega \ \to \ { \mathcal { C } }$ . In parallel to all other agents, agent i follows the communication policy $\pi _ { i } ^ { c } ( \cdot )$ to map its current observation ${ \bf o } _ { i } ( t )$ to the communication message $\mathbf { c } _ { i } ( t )$ which will be received by the central controller in the same timestep t. The code-book C is a set composed of a finite number of communication code-words s $\mathsf { c } , \mathsf { c } ^ { \prime } , \mathsf { c } ^ { \prime \prime } , . . . , \mathsf { c } ^ { ( | \mathcal { C } | - 1 ) }$ - we use the same notation to refer to the different members of the action, observation and state spaces too. $\scriptstyle { \mathrm { A g e n t s } } ^ { \prime }$ communication messages are sent over an error-free finite-rate bit pipe, with its rate constraint to be $R \in \mathbb { R }$ (bits per channel use) or equivalently (bits per time step). As a result, the size of the quantization codebook should follow the inequality $| { \mathcal { C } } | ~ \leq ~ 2 ^ { R }$ . The CC exploits the received communication messages $\mathbf { c } ( t ) \triangleq \langle \mathbf { c } _ { 1 } ( t ) , . . . , \mathbf { c } _ { N } ( t ) \rangle$ within the last $d$ number of time-steps to generate the action signal ${ \bf \mathfrak { m } } ( t )$ following the control policy $\bar { \pi ^ { m } } ( \cdot ) : \mathcal { C } ^ { N d } \to \mathcal { M } ^ { \tilde { N } }$ . Based on the above description, the environment from the point of view of the CC
