where $K _ { x } \in \mathbb { R } ^ { m \times n } , K _ { r } \in \mathbb { R } ^ { m \times m } , P = P ^ { \mathrm { { T } } } \in \mathbb { R } ^ { n \times n }$ and $V \in \mathbb { R } ^ { n \times m }$ are steady-state solutions of the following differential equations:

$$
\begin{array}{l} - \dot {P} (t) = A ^ {\mathrm{T}} P (t) + P (t) A - P (t) B R ^ {- 1} B ^ {\mathrm{T}} P (t) + Q, \\ \dot {V} (t) = A ^ {\mathrm{T}} V (t) + P (t) B R ^ {- 1} B ^ {\mathrm{T}} V (t) + P (t) B. \end{array} \tag {8}
\dot {V} (t) = - A ^ {\mathrm{T}} V (t) + P (t) B R ^ {- 1} B ^ {\mathrm{T}} V (t) + P (t) B _ {r},$$

and, if $\forall i , j \in \overline { { 1 , 2 n } }$ it holds that $\lambda _ { i } \left( D \right) \gg \lambda _ { i } \left( D \right)$ , then such solutions can be obtained analytically in the following way:

$$P = P \left(\tau_ {\infty}\right) := \Phi_ {2 1} \left(\tau_ {\infty}\right) \Phi_ {1 1} ^ {- 1} \left(\tau_ {\infty}\right),
\Phi \left(\tau_ {\infty}\right) := e ^ {D \tau_ {\infty}} = \left[ \begin{array}{c c} \Phi_ {1 1} \left(\tau_ {\infty}\right) & \Phi_ {1 2} \left(\tau_ {\infty}\right) \\ \Phi_ {2 1} \left(\tau_ {\infty}\right) & \Phi_ {2 2} \left(\tau_ {\infty}\right) \end{array} \right], D = \left[ \begin{array}{c c} - A & B R ^ {- 1} B ^ {\mathrm{T}} \\ Q & A ^ {\mathrm{T}} \end{array} \right], \tag {9}
V = \left(A ^ {\mathrm{T}} - P B R ^ {- 1} B ^ {\mathrm{T}}\right) ^ {- 1} P B _ {r},$$

where $\begin{array} { r } { \Phi ( \tau _ { \infty } ) \in \mathbb { R } ^ { 2 n \times 2 n } , \Phi _ { i j } \in \mathbb { R } ^ { n \times n } , \tau _ { \infty } > 0 , \tau _ { \infty }  \textrm { \scriptscriptstyle { \infty } } } \end{array}$ ∞ is an auxiliary constant, which value is sufficiently high.

Proof. The correctness of (7) and (8) is shown in 5, p. 244-246, the equations (9) are obtained as a combination of the results from $^ 5 , \mathrm { p } . 2 4 4 - 2 4 6 \mathrm { a n d } ^ { 9 }$ , the necessity of the condition $\forall i , j \in \overline { { 1 , 2 n } } \lambda _ { i } ( D ) \gg \lambda _ { i } ( D )$ ) for the existence of analytical solutions (9) is proved in 11 and discussed in detail in 8. □

The optimal control law (7) allows one to define the required control quality for the plant (4) as follows:

$$\dot {x} _ {r e f} (t) = A _ {r e f} x _ {r e f} (t) + B _ {r e f} r = \left(A + B K _ {x}\right) x _ {r e f} (t) + B K _ {r} r, x _ {r e f} \left(t _ {r} ^ {+}\right) = x _ {0}, \tag {10}$$
