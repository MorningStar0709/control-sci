Example 4.1 (Passive linear model identification for an RLC circuit). We consider a RLC circuit where resistor, inductor and capacitor are in series. We set the state variable $x _ { 1 } ( t )$ to be the current in the circuit and $x _ { 2 } ( t )$ to be the voltage of the capacitor. We let input u(t) to be source voltage and output y(t) to be current (i.e. $x _ { 1 } ( t ) )$ . With $R = L = C = 1$ , we write the linear state-space model as

$$
\left[ \begin{array}{l} \dot {x} _ {1} (t) \\ \dot {x} _ {2} (t) \end{array} \right] = \left[ \begin{array}{c c} - 1 & - 1 \\ 1 & 0 \end{array} \right] \left[ \begin{array}{l} x _ {1} (t) \\ x _ {2} (t) \end{array} \right] + \left[ \begin{array}{l} 1 \\ 0 \end{array} \right] u (t) \tag {27}

y (t) = \left[ \begin{array}{c c} 1 & 0 \end{array} \right] \left[ \begin{array}{c} x _ {1} (t) \\ x _ {2} (t) \end{array} \right]
$$

In other words, the real coefficient matrices are $A = { \left[ \begin{array} { l l } { - 1 } & { - 1 } \\ { 1 } & { 0 } \end{array} \right] } ,$ $B = { \binom { 1 } { 0 } } , C = { \Big [ } 1 \quad 0 { \Big ] }$ , and $D = 0 ,$ . Let $P = I _ { 2 } > 0$ . Then we have

$$
\left[ \begin{array}{c c} A ^ {\top} P + P A & P B - C ^ {\top} \\ B ^ {\top} P - C & - (D + D ^ {\top}) \end{array} \right] = \left[ \begin{array}{c c c} - 2 & 0 & 0 \\ 0 & 0 & 0 \\ 0 & 0 & 0 \end{array} \right] \preceq 0.
$$

According to the Positive Real Lemma, the real system is passive. Now we generate trajectory data for input $u ( t ) \ =$ $e ^ { - 0 . 1 t } s i n ( \pi t )$ with initial state being $[ 0 . 1 , 0 . 2 ] ^ { \top }$ . We also add πadditional Gaussian noise ∼ $N ( 0 , 0 . 0 0 5 ^ { 2 } )$ .to make it more re-, .alistic. With the data, we use function ssest from the System Identification Toolbox in MATLAB and identify a baseline system model where the fit goodness reaches $9 5 . 8 I \%$ . The identified model has coefficients $A _ { i d } \ = \ { \left[ \begin{array} { l l } { - 0 . 6 6 6 1 } & { - 0 . 2 0 3 5 } \\ { 3 . 7 2 8 6 } & { - 0 . 2 8 7 6 } \end{array} \right] } ,$ , Bid = $B _ { i d } \ = \ \left\lceil \frac { - 4 . 3 1 4 2 } { - 1 . 7 8 1 5 } \right\rceil , \ C _ { i d } \ = \ \big [ - 0 . 2 3 3 8 0 . 0 1 4 4 \big ] ,$ , and $D _ { i d } ~ = ~ 0$

.However, the following problem is not feasible,

$$
P _ {1} \succeq 0 \quad \left[ \begin{array}{c c} A _ {i d} ^ {\top} P _ {1} + P _ {1} A _ {i d} & P _ {1} B _ {i d} - C _ {i d} ^ {\top} \\ B _ {i d} ^ {\top} P _ {1} - C _ {i d} & - (D _ {i d} + D _ {i d} ^ {\top}) \end{array} \right] \preceq 0, \tag {28}
$$
