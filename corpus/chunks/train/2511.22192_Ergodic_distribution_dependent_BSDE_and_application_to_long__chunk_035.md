and we will follow the classical proof that introduce a discounted approximation of (Decoupled EBSDE), see for instance $[ 9 ] , [ 1 5 ] , [ 1 6 ]$ : Let $\alpha > 0$ ,

$$Y _ {t} ^ {\alpha , x, [ \theta ]} = Y _ {T} ^ {\alpha , x, [ \theta ]} + \int_ {t} ^ {T} \left(f (X _ {s} ^ {x, [ \theta ]}, [ X _ {s} ^ {[ \theta ]} ], Z _ {s} ^ {\alpha , x, [ \theta ]}) - \alpha Y _ {s} ^ {\alpha , x, [ \theta ]}\right) \mathrm{d} s - \int_ {t} ^ {T} Z _ {s} ^ {\alpha , x, [ \theta ]} \mathrm{d} W _ {s}. (\alpha - \mathrm{BSDE})$$

Let us remark once again that (Decoupled EBSDE) becomes (EBSDE) when we replace ?? by a G-measurable r.v. $\theta \in$ $\mathrm { L } ^ { 2 q + 2 } ( \Omega ; \mathbb { R } ^ { d } )$ .

Let us now give the two different sets of assumptions we are going to use to prove the main result:

Assumption (H1).

1. $( \mathcal { H } _ { S D E } 1 )$ and (H0) hold,

2. $\nu > K _ { x } ^ { \sigma } + \sqrt { 2 K _ { x } ^ { \sigma } } K _ { z } ^ { f } .$

3. ?? is invertible.

Assumption $\left( \mathcal { H } _ { 2 } \right)$ .

1. $( \mathcal { H } _ { S D E } 2 )$ and (H0) hold.

Proposition 3.2. Let us assume that $( \mathcal { H } _ { 1 } ) ~ o r ~ ( \mathcal { H } _ { 2 } )$ hold. Then, for all $\alpha > 0 , x \in \mathbb { R } ^ { d } , \theta \in L ^ { 2 q + 2 }$ , there exists a unique solution $( Y ^ { \alpha , x , [ \theta ] } , Z ^ { \alpha , x , [ \theta ] } ) \ t o \ ( \alpha { \mathrm { - B S D E } } )$ such that $Z ^ { ( \alpha , { \boldsymbol { x } } , [ \theta ] } \in L _ { l o c } ^ { 2 } ( \Omega ; L ^ { 2 } ( \mathbb { R } _ { + } ; \mathbb { R } ^ { d } ) )$ and there exists ?? that does not depend on ??, such that $f o r \ a l l \ x \in \mathbb { R } ^ { d } , \theta \in L ^ { 2 q + 2 } ( \Omega ; \mathbb { R } ^ { d } )$ :

$$\left| Y _ {0} ^ {\alpha , x, [ \theta ]} \right| \leq \frac {C}{\alpha} \left(1 + | x | ^ {q + 1} + \| \theta \| _ {2 q + 2} ^ {q + 1}\right). \tag {3.2}$$

Moreover, there exists a measurable function $\zeta ^ { \alpha } : \mathbb { R } ^ { d } \times \mathcal { P } _ { 2 q + 2 } ( \mathbb { R } ^ { d } )  \mathbb { R } ^ { d }$ such that $Z _ { t } ^ { \alpha , x , [ \theta ] } = \zeta ^ { \alpha } ( X _ { t } ^ { x , [ \theta ] } , \left[ X _ { t } ^ { [ \theta ] } \right] )$ a.s. for a.e. $t \in \mathbb { R } _ { + } , \ a l l \ x \in \mathbb { R } ^ { d }$ and $\theta \in L ^ { 2 q + 2 }$ .

Proof. If we consider a solution $( Y ^ { \alpha , x , [ \theta ] } , Z ^ { \alpha , x , [ \theta ] } )$ to (??−BSDE) such that $Z ^ { \alpha , \boldsymbol { x } , [ \theta ] } \in \mathrm { L } _ { \mathrm { l o c } } ^ { 2 } ( \Omega ; \mathrm { L } ^ { 2 } ( \mathbb { R } _ { + } ; \mathbb { R } ^ { d } ) )$ and $Y ^ { \alpha , x , \left[ \theta \right] } ~ \in$ $\cup _ { T > 0 } S _ { T } ^ { 2 }$ , then we have the a priori estimate
