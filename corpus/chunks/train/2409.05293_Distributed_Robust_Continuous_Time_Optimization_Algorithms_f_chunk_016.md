Given the nature of $V _ { S 2 } ( t )$ , where it is locally Lipschitz continuous but nonsmooth at some points and on account of Definition 2, the generalized gradient of $V _ { S 2 } ( t )$ is obtained as

$$\partial V _ {S 2} (t) = \left(\mathcal {D} ^ {\top} \otimes I _ {n}\right) ^ {\top} \left\{\operatorname{SGN} \left(\left(\mathcal {D} ^ {\top} \otimes I _ {n}\right) x\right) \right\} \tag {20}$$

where SGN(.) is defined in [25]. Following Definition 3, the set-valued Lie derivative of $V _ { S 2 } ( t )$ is represented as:

$$\dot {\tilde {V}} _ {S 2} (t) = \bigcap_ {\xi \in \mathrm{SGN} [ (\mathcal {D} ^ {\top} \otimes I _ {n}) x ]} \xi^ {\top} (\mathcal {D} ^ {\top} \otimes I _ {n}) K [ f ] \tag {21}$$

Here, $K [ f ] = \Psi - \beta [ \nabla ^ { 2 } \tilde { L } ( x , t ) ] ^ { - 1 } ( \mathscr { D } \otimes I _ { n } ) \mathsf { S G N } [ ( \mathscr { D } ^ { \top } \otimes I _ { n } ) x ]$ denotes the set-valued Filippov map of the system (17). Due to the presence of an intersection operation on the right side of (21), it indicates that while $\dot { \tilde { V } } _ { S 2 } ( t )$ is not empty and there exists $\xi \in \operatorname { S G N } [ ( D ^ { \top } \otimes I _ { n } ) . x ]$ such that $\xi ^ { \top } ( \mathcal { D } ^ { \top } \otimes \mathbf { \bar { I } } _ { n } ) \tilde { f } < 0$ for all $\tilde { f } \in K [ f ]$ , as a result $\tilde { V } _ { S 2 } ( t )$ moves into the negative halfplane of the real axis. Arbitrarily select $\eta \in \operatorname { S G N } [ ( D ^ { \top } \otimes I _ { n } ) x ]$ ]. Choose $\xi _ { k } = \mathrm { s i g n } [ (  { \boldsymbol { D } } ^ { \top } \otimes I _ { n } ) _ { k } \cdot  { \boldsymbol { x } } ]$ if sig $\gimel [ ( { \mathcal { D } } ^ { \top } \otimes I _ { n } ) _ { k } \cdot x ] \neq 0$ and choose $\xi _ { k } = \eta _ { k }$ if $\mathrm { s i g n } [ ( \boldsymbol { D } ^ { \top } \dot { \otimes } I _ { n } ) _ { k } \cdot \boldsymbol { \dot { x } } ] = 0 ,$ where $\xi _ { k }$ and ηk represent the kth element in vectors ξ and η, respectively. Should $\dot { V } _ { S 2 } ( t ) \neq 0$ , we assume a scenario with $\tilde { a } \in \tilde { V } _ { S 2 } ( t )$ , leading to the following analysis:
