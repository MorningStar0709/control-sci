# 2.2 Canonical homogeneous norm

A continuous positive definite function $\| \cdot \| _ { \mathbf { d } } : \mathbb { R } ^ { n } \mapsto \mathbb { R }$ satisfying $\| \pm \mathbf { d } ( s ) x \| =$ $e ^ { s } \| x \| _ { \mathbf { d } } , \forall x \in \mathbb { R } ^ { n }$ is usually called a d-homogeneous norm [12], $[ 7 ] , [ 1 ]$ . For the weighted dilation $\mathbf { d } ( s ) = { \\overset { \cdot } { ( } } e ^ { 2 s } \ { \underset { e ^ { s } } { 0 } } )$ , a d-homogeneous norm can be defined, for example, as $\| x \| _ { \mathbf { d } } = { \sqrt { | x _ { 1 } | } } + | x _ { 2 } |$ , where $x = ( x _ { 1 } , x _ { 2 } ) ^ { \top } \in \mathbb { R } ^ { 2 }$ .

The d-homogeneous norm $\| \cdot \| _ { \mathbf { d } }$ does not satisfy the triangle inequality in the general case, so, formally, this is not even a semi-norm. However, we would follow the tradition accepted in the systems sciences and call the function $\| \cdot \| _ { \mathbf { d } }$ satisfying the above property by $\mathrm { a \ \ ^ { 6 } n o r m ^ { 3 } }$ , but, basically, we use this name for the canonical homogeneous norm (see below) being a norm (in the classical sense) for the vector space $\mathbb { R } _ { \mathbf { d } } ^ { n }$ homeomorphic to $\mathbb { R } ^ { n }$ . The space $\mathbb { R } _ { \mathbf { d } } ^ { n }$ has the same set of elements as $\mathbb { R } ^ { n }$ , but the rules for addition of vectors and multiplication of a vector by a scalar are modified by means of a special homeomorphism on $\mathbb { R } ^ { n }$ (see [21] for more details).

Definition 1 Let a linear dilation d in $\mathbb { R } ^ { n }$ be continuous and monotone with respect to a norm $\| \cdot \|$ . A function $\| \cdot \| _ { \mathbf { d } } : \mathbb { R } ^ { n } \mapsto [ 0 , + \infty )$ defined as follows: $\| \mathbf { 0 } \| _ { \mathbf { d } } = 0$ and

$$\| x \| _ {\mathbf {d}} = e ^ {s _ {x}}, \text { where } s _ {x} \in \mathbb {R}: \| \mathbf {d} (- s _ {x}) x \| = 1, x \neq \mathbf {0} \tag {3}$$

is said to be a canonical d-homogeneous norm in $\mathbb { R } ^ { n }$

For standard dilation $\mathbf { d } ( s ) = e ^ { s } I _ { n }$ we have $\| x \| _ { \mathbf { d } } = \| x \|$ . In other cases, the canonical homogeneous norm $\| { \boldsymbol { x } } \| _ { \mathbf { d } }$ with $x \neq { \mathbf { 0 } }$ is implicitly defined by the nonlinear algebraic equation (3), which always have a unique solution due to monotonicity of the dilation. In some particular cases, this implicit equation has explicit solution.
