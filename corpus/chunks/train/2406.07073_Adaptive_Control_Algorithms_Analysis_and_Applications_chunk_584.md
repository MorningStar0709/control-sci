Theorem 12.2 (Strategy 2) Consider the indirect adaptive pole placement for the plant model (12.1) where the plant parameters estimates are given by the algorithm of (12.5) through (12.13) and the controller is given by:

$$\hat {S} (t - 1, q ^ {- 1}) u (t) + \hat {R} (t - 1, q ^ {- 1}) y (t) = \hat {\beta} (t - 1) P (q ^ {- 1}) y ^ {*} (t + d + 1) \tag {12.57}$$

or alternatively:

$$
\begin{array}{l} u (t) = - \hat {S} ^ {*} (t - 1, q ^ {- 1}) u (t - 1) - \hat {R} (t - 1, q ^ {- 1}) y (t) \\ + \hat {\beta} (t - 1) P (q ^ {- 1}) y ^ {*} (t + d + 1) \tag {12.58} \\ \end{array}
$$

where:

$$\hat {\beta} (t - 1) = 1 / \hat {B} (t - 1, 1) \tag {12.59}\hat {S} (t, q ^ {- 1}) = 1 + \hat {s} _ {1} (t) q ^ {- 1} + \dots + \hat {s} _ {n _ {S}} (t) q ^ {- n _ {S}} = 1 + q ^ {- 1} \hat {S} ^ {*} (t, q ^ {- 1}) \tag {12.60}
\begin{array}{l} \hat {R} (t, q ^ {- 1}) = \hat {r} _ {0} (t) + \hat {r} _ {1} (t) q ^ {- 1} + \dots + \hat {r} _ {n _ {R}} (t) q ^ {- n _ {R}} \\ = \hat {r} _ {0} (t) + q ^ {- 1} \hat {R} ^ {*} (t, q ^ {- 1}) \tag {12.61} \\ \end{array}
$$

and $\hat { R } ( t , q ^ { - 1 } )$ and $\hat { \boldsymbol S } ( t , q ^ { - 1 } )$ are solutions of (12.24). Assume that the hypotheses (i) through (iv) from Theorem 12.1 hold. Then:

1. The sequences {u(t)} and $\{ y ( t ) \}$ are bounded.   
2. The a priori prediction error converges to zero, $\begin{array} { r } { i . e . : \operatorname* { l i m } _ { t \to \infty } \varepsilon ^ { 0 } ( t + 1 ) = 0 . } \end{array}$   
3. $\begin{array} { r } { \operatorname* { l i m } _ { t \to \infty } P [ y ( t + d ) , - \hat { B } ^ { * } ( t - 1 , q ^ { - 1 } ) \hat { \beta } ( t - 1 ) y ^ { * } ( t + d ) ] = 0 . } \end{array}$   
4. $\begin{array} { r } { \operatorname* { l i m } _ { t  \infty } P [ u ( t ) - \hat { A } ( t - 1 , q ^ { - 1 } ) \hat { \beta } ( t - 1 ) y ^ { * } ( t + d + 1 ) ] = 0 . } \end{array}$

Proof The proof is similar to that of Theorem 12.1, except that one replaces in the various equations: $\varepsilon ( t + 1 )$ by $\varepsilon ^ { 0 } ( t + 1 ) , \hat { y } ( t + 1 )$ by $\hat { y } ^ { 0 } ( t + 1 )$ and the time-varying parameters at t by their values at time t − 1. The major difference occurs in (12.56) which becomes:

$$
\begin{array}{l} \| \phi (t + 1) \| ^ {2} \leq C _ {1} ^ {\prime} + C _ {2} \max _ {0 \leq \tau \leq t} [ \varepsilon^ {0} (t + 1) ^ {2} + \bar {y} ^ {*} (t) ^ {2} ] \\ \leq C _ {1} + C _ {2} \max _ {0 \leq \tau \leq t} \varepsilon^ {0} (t + 1) ^ {2}; \quad c _ {1}, c _ {2} <   \infty \tag {12.62} \\ \end{array}
$$

since $\bar { y } ^ { * } ( t )$ is a bounded signal. On the other hand, from the properties of the parameter estimation algorithm one has (see (12.16)):

$$\lim _ {t \to \infty} \frac {[ \varepsilon^ {0} (t + 1) ] ^ {2}}{1 + \phi^ {T} (t) F (t) \phi (t)} = 0$$

and applying the “bounded growth” lemma (Lemma 11.1) one concludes that $\| \phi ( t ) \|$ is bounded, which implies that y(t) and u(t) are bounded, as well as $\begin{array} { r } { \operatorname* { l i m } _ { t \to \infty } \varepsilon ^ { 0 } ( t + 1 ) = 0 } \end{array}$ . -
