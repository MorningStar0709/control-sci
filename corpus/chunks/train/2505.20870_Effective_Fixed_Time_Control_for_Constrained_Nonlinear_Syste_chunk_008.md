# A. Controller Design

Design the following full-state constraints:

$$
\left\{ \begin{array}{l} w _ {i} = \log \frac {\rho_ {s _ {1}} + x _ {i}}{\rho_ {s _ {1}} - x _ {i}} \\ \dot {w} _ {i} = \frac {e ^ {w _ {i}} + e ^ {- w _ {i}} + 2}{\rho_ {s _ {1}} + \rho_ {s _ {2}}} \dot {x} _ {i} \end{array} \right. \tag {3}
$$

with $i = 1 , 2 , \dots , n$ . In this system, the logarithmic function serves dual purposes: it simplifies controller design while ensuring strict state boundaries; it also reduces response latency and improves robustness against initial configuration deviations in dynamic interference environments.

Define $\Delta _ { i } ( w _ { i } ) = ( e ^ { w _ { i } } + e ^ { - w _ { i } } + 2 ) / ( \rho _ { s _ { 1 } } + \rho _ { s _ { 2 } } )$ , where $i = 1 , 2 , \ldots , n ,$ to handle (3). So we get $\dot { w } _ { i } = \Delta _ { i } ( w _ { i } ) \dot { x } _ { i }$ . Then, the following auxiliary systems have been introduced to facilitate processes in accordance with traditional methods

$$
\left\{ \begin{array}{l} L _ {i} (\bar {w} _ {i + 1}) = \Delta_ {i} (w _ {i}) h _ {i} (\bar {x} _ {i + 1}) - w _ {i + 1} \\ L _ {n} (\bar {w} _ {n}) = \Delta_ {n} (w _ {n}) h _ {n} (\bar {x} _ {n}) \end{array} \right. \tag {4}
$$

Using the auxiliary system constructed above, we rewrite the constrained system (1) as:

$$
\left\{ \begin{array}{l} \dot {w} _ {i} = w _ {i + 1} + L _ {i} (\bar {w} _ {i + 1}) \\ \dot {w} _ {n} = \Delta_ {n} (w _ {n}) g (t) + L _ {n} (\bar {w} _ {n}) \end{array} \right. \tag {5}
$$

Define the tracking error $z _ { i }$ as

$$
\left\{ \begin{array}{l} z _ {1} = w _ {1} - w _ {s} \\ z _ {i} = w _ {i} - \alpha_ {i - 1}, i = 2, 3, \dots , n \end{array} \right. \tag {6}
$$

where $\begin{array} { r } { w _ { s } \ = \ \log \frac { \rho _ { s _ { 1 } } + x _ { r } } { \rho _ { s _ { 1 } } - x _ { r } } } \end{array}$ , the expected reference signal is represented as $x _ { r } ,$ 1  and αi denotes the intermediate controller.

Define $k _ { 1 , o } , ~ k _ { 2 , o }$ and $\tau _ { o }$ as designed positive constants with $o = 1 , . . . , n$ . Establish the Lyapunov function $V _ { 1 }$ and $V _ { i } ,$ based on the backstepping technique, we get
