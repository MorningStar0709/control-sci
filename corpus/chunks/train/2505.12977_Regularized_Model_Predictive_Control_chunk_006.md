where the initial state $x _ { k | k }$ corresponds to the current state at the $k ^ { \mathrm { { t h } } }$ horizon. Based on this setup, we aim to achieve the following two objectives:

● Objective I: For the discrete LTI system (1) with $x _ { k | k } = x _ { k }$ , the goal is to compute the optimal sequences of states $X _ { k } ^ { * }$ and control inputs $U _ { k } ^ { * }$ by solving the following optimization problem:

$$\operatorname * {a r g m i n} _ {U _ {k}, X _ {k}} J _ {k},$$

$\mathrm { s . t . } \quad x _ { i + 1 \mid k } = A x _ { i \mid k } + B u _ { i \mid k } , x _ { i \mid k } \in \mathbb { X } ,$ (5)

$$X _ {k} \in \mathbb {X} ^ {l}, U _ {k} \in \mathbb {U} ^ {l}, k \leq i \leq k + l - 1,$$

which leads to the formulation of the Re-MPC algorithm.

● Objective II: Identify the conditions under which the obtained sequences $X _ { k } ^ { * }$ and $U _ { k } ^ { * }$ guarantee the convergence and stability of the algorithm.

To accomplish these objectives, we employ a PLS-based framework, which effectively addresses the constrained optimization problem (5). By re-examining the standard algebraic Riccati equation and utilizing the obtained feedback gains, we establish the necessary conditions for the stability and convergence of the developed Re-MPC algorithm.
