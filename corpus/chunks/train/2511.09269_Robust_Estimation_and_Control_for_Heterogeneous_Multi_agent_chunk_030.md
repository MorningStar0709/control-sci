Definition 2 ([14]): A system $\dot { x } = f ( x , u , t )$ , with $f ~ :$ : $\mathbb { R } ^ { n } \times \mathbb { R } ^ { m } \times \mathbb { R } _ { > 0 } \to \mathbb { R } ^ { n }$ , is set-Input to State Stable (set-ISS) with respect to u if, for each initial condition $x ( 0 )$ and any locally essentially bounded input u satisfying sup $ \ r _ { t > 0 } \| u ( t ) \| \le$ ∞, the distance $\| x ( t ) \| _ { \cal A } = { \mathrm { i n f } } _ { a \in { \cal A } } \{ \| x - a \| \} { \overset { - } { \mathrm { o f } } } \ x ( t )$ to A satisfies $\begin{array} { r } { \| { \boldsymbol x } ( t ) \| _ { A } \leq \beta ( \| { \boldsymbol x } ( 0 ) \| _ { A } , t ) + \gamma \left( \operatorname* { s u p } _ { 0 \leq \tau \leq t } \| { \boldsymbol u } ( \tau ) \| \right) } \end{array}$ for all $t \in \mathbb { R } _ { \geq 0 }$ , where $\beta$ and $\gamma$ are a KL and K function, respectively.

Assumption 5: The nominal controller $\textbf { \em u } = \textbf { \em \psi } ( \textbf x )$ in (22) guarantees convergence of the multi-agent system to a set A representing the team objective, irrespective of the disturbance ${ \pmb w } ( { \pmb x } , t )$ .

Assumption 5 is not restrictive in practice. Indeed, since ${ \pmb w } ( { \pmb x } , t )$ is uniformly bounded by Assumption 1, (22) can be designed robustly based on the upper bound of ${ \pmb w } ( { \pmb x } , t )$ . Therefore, since the nominal controller in (22) guarantees convergence to the desired set A despite w, the effect of w can be considered as part of the nominal dynamics. Consequently, the behavior of the system under (23) can be studied by analyzing the perturbed system $\dot { { \pmb x } } = \Phi ( { \pmb x } , \tilde { { \pmb x } } , t ) =$ $\pmb { f } ( \pmb { x } ) + \pmb { g } ( \pmb { \psi } ( \pmb { x } , \pmb { x } + \tilde { \pmb { x } } ) ) + \pmb { w } ( \pmb { x } , t )$ , where x˜ is treated as an input disturbance affecting the nominal unforced system.

Let $\begin{array} { r l r } { \delta _ { \tilde { \pmb { x } } } ( t ) } & { = } & { \| [ \delta _ { \tilde { \ b { x } } } ^ { 1 } ( t ) ^ { \top } , \dots , \delta _ { \tilde { \pmb { x } } } ^ { N } ( t ) ^ { \top } ] ^ { \top } \| } \end{array}$ , with $\delta _ { \tilde { x } } ^ { i } ( t ) \ =$ $[ \delta _ { N _ { 1 } ^ { i } } ^ { i } ( t ) , \ldots , \delta _ { N _ { n _ { i } } ^ { i } } ^ { i } ( t ) ] ^ { \dagger }$ , be the norm of the prescribed performance functions associated with the estimation error x˜. Moreover, denote with $\overline { { \delta } } _ { \tilde { \alpha } }$ the desired upper bound on ∥x˜∥.

Assumption 6: There exists a finite time $t _ { x } \ > \ 0$ for which $\delta _ { \tilde { x } } ( t ) \leq \overline { { \delta } } _ { \tilde { x } }$ holds for all $t \geq t _ { x }$ .
