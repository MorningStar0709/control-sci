# 4.1 Adaptive stabilization

In this subsection, building on the asymptotic analysis of parameter estimation in Section 3, we establish the global stability of the closed-loop system (25) and show that the parameter estimates $\widehat { \theta } _ { t }$ converge to the true parameter $\theta ^ { * }$ along the resulting closed-loop trajectory.

We first establish the following global stability result for the adaptive controller (23).

Theorem 2 Under Assumption 1-4, the system (1) with the adaptive control (23) is globally stable in the sense that, for any $x _ { 0 } \in \mathbb { R } ^ { n }$ and $\hat { \theta } _ { 0 } \in \mathbb { R } ^ { ( m + n ) \times n }$ ,

$$\operatorname * {l i m s u p} _ {t \to \infty} \frac {1}{t} \sum_ {\tau = 0} ^ {t} \| x _ {\tau} \| ^ {\gamma} < \infty , a. s., \tag {27}$$

where $\{ x _ { t } \}$ is the trajectory of the closed-loop system under adaptive control (23), γ is given in Assumption 2.

We note that the result of Theorem 2 continues to hold even without a probing signal, i.e., when $v _ { t } \equiv 0$ in (23). In addition to this stability guarantee, the next theorem shows that, if the probing signal induces sufficient excitation, the estimates $\widehat { \theta } _ { t }$ converge to the ground truth $\theta ^ { * }$ .

Theorem 3 Under Assumptions 1–4, if b ∈ [0, (γ−2)24γ(γ+2) ), $\begin{array} { r } { i f b \in [ 0 , \frac { \left( \gamma - 2 \right) ^ { 2 } } { 4 \gamma \left( \gamma + 2 \right) } ) } \end{array}$ (-2)² then the parameter estimates $\widehat { \theta } _ { t }$ generated by Algorithm 1 along the closed-loop trajectory induced by (25) are strongly consistent with the following convergence rate:

$$\left\| \hat {\theta} _ {t} - \theta^ {*} \right\| ^ {2} = O \left(\frac {(\log t) ^ {1 + \delta}}{t ^ {1 - 2 b - \eta}}\right), \quad a. s., \tag {28}$$

where η is any constant satisfying $\begin{array} { r } { \eta \in \left( \frac { 8 b } { \gamma - 2 } , \frac { 2 ( \gamma - 2 ) } { \gamma ( \gamma + 2 ) } \right) } \end{array}$

Proofs of Theorems 2 and 3 are provided in Appendix B.

Theorems 2 and 3 show that the adaptive controller (23), together with the estimator $\widehat { \theta } _ { t } .$ , can simultaneously stabilize the system and ensure convergence of the parameter estimates. Moreover, it is natural to ask whether, the resulting closed-loop trajectory can achieve a performance level comparable to that of a reference trajectory generated by the reference controller $u _ { t } ^ { * } = \pi _ { \theta ^ { * } } ( x _ { t } )$ which presumes knowledge of the true parameter $\theta ^ { * }$ . This question is addressed in the next subsection.
