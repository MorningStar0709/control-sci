# 3.3 Shifting regularization from H to g

The direct data-driven method (13) is conceptually useful but not practically implementable for two reasons: 1) the regularization is imposed on the Hankel matrix $H ,$ which is implicit; and 2) the constraint in (13) is bilinear in H and $^ { g , }$ which is computationally intractable. We here discuss how to shift the regularization from H to g. Our general idea is to select some identification constraints from (10a)-(10d) such that the inner identification problem admits a simpler and potentially explicit optimality condition $\hat { \mathcal { C } } _ { \mathrm { o p t } }$ . Then, we can impose some constraints $h : \mathbb { R } ^ { \bar { n } } \to \mathbb { R }$ on g such that the set

$$\{(g, \sigma_ {y}, u, y) \mid H g = \operatorname{col} (u _ {\text { ini }}, y _ {\text { ini }} + \sigma_ {y}, u, y), H \in \hat {\mathcal {C}} _ {\text { opt }} \}$$

is the same as

$$\{(g, \sigma_ {y}, u, y) \mid \bar {H} g = \operatorname{col} (u _ {\mathrm{ini}}, y _ {\mathrm{ini}} + \sigma_ {y}, u, y), h (g) = 0 \},$$

where $\bar { H }$ is a fixed trajectory matrix. We next move the constraint on $g$ to the cost via regularization, leading to a direct DDPC

$$
\min_ {\substack {g, \sigma_ {y} \in \Gamma , \\ u \in \mathcal {U}, y \in \mathcal {Y}}} \| y \| _ {Q} ^ {2} + \| u \| _ {R} ^ {2} + \lambda_ {y} \| \sigma_ {y} \| _ {2} ^ {2} + \lambda_ {w} | h (g) | \tag{16}
$$

$\mathrm { s u b j e c t ~ t o ~ } \bar { H } g = \mathrm { c o l } ( u _ { \mathrm { i n i } } , y _ { \mathrm { i n i } } + \sigma _ { y } , u , y ) .$

Section 4 will detail three ways to derive an explicit form (16), including regularization using projectionbased norm, causality-based norm, and $l _ { 1 }$ norm.

Similar to (13), to ensure the equivalence of the optimal solution, one may require the regularization parameter $\lambda _ { w }$ in (16) to approach infinity. Fortunately, in our context, we can establish an exact penalty with a finite regularization parameter $\lambda _ { w }$ . In particular, consider a quadratic optimization problem of the form

$$\min _ {x _ {1} \in \mathcal {X}, x _ {2}} x _ {1} ^ {\top} M x _ {1}$$

${ \mathrm { s u b j e c t ~ t o ~ } } A _ { 1 } x _ { 1 } + A _ { 2 } x _ { 2 } = b ,$ (17)

$$\left\| D x _ {2} \right\| _ {p} = 0,$$

where M is positive semidefinite, $A _ { 1 } , A _ { 2 } , b ,$ , and $D$ are problem data of compatible dimensions, X is a convex set, and $\| \cdot \| _ { p }$ is any p-norm. We note that $x _ { 2 }$ plays the role of g in (16). We then consider a regularized version
