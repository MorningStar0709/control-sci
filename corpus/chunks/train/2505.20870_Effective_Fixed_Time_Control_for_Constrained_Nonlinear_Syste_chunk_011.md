where Φ, $\vartheta _ { 1 } , 0 ~ < ~ \theta ~ < ~ 1$ and $\bar { \vartheta } _ { 1 } ~ > ~ \vartheta _ { 1 } / ( 1 - \theta )$ are positive designed parameters. There exists continuous timevarying parameter $\beta ( t ) _ { 1 } , \beta ( t ) _ { 2 }$ which, when $\forall t \in [ t _ { j } , t _ { j + 1 } )$ , satisfies $| \beta _ { 1 } | ~ \leq ~ 1$ and $| \beta _ { 2 } | ~ \leq ~ 1$ . Since $\Phi \ > \ 0$ , therefore a tanh $\begin{array} { c c c } { { ( a / \Phi ) } } & { { > } } & { { 0 . } } \end{array}$ , from (16) we get $\begin{array} { r l r } { d ( t ) } & { { } < } & { 0 } \end{array}$ , it follows that $d ( t ) / ( 1 + \beta _ { 1 } ( t ) \theta ) ~ \leq ~ d ( t ) / ( 1 + \theta )$ and $| \beta _ { 2 } ( t ) \vartheta _ { 1 } / ( 1 + \beta _ { 1 } ( t ) \theta ) | \leq \vartheta _ { 1 } / ( 1 - \theta )$ . Combining Lemma3 and these equations into (10) yields:

$$
\begin{array}{l} \dot {V} \leq - \sum_ {\substack {o = 1 \\ n}} ^ {n} k _ {1, o} z _ {o} ^ {2 q} - \sum_ {o = 1} ^ {n} k _ {2, o} z _ {o} ^ {2 p} + \sum_ {o = 1} ^ {n} \left(\frac {u _ {o} ^ {2}}{2} + \frac {\lambda_ {o} ^ {2}}{2}\right) \tag{18} \\ - \sum_ {o = 1} ^ {n} \frac {\tau_ {o}}{\varepsilon_ {o}} \tilde {\varphi} _ {o} \hat {\varphi} _ {o} + 0. 5 5 7 R \Phi \\ \end{array}
$$

Case 3: Switched-threshold strategy

This switched-threshold approach integrates fixed and relative threshold strategies. The fixed-threshold strategy, $| g ( t ) | \geq G$ , maintains computational errors within predefined limits to ensure stability. Conversely, when $| g ( t ) | \ < \ G$ , the approach transitions to a relative threshold, adjusting thresholds proportionally to the signal amplitude for highprecision tracking. By combining these strategies, the triggering condition is designed as follows:

$$
t _ {k + 1} = \left\{ \begin{array}{l l} \inf \{t \in R | | e (t) | \geq \theta | g (t) | + \vartheta_ {1} \} & | g (t) | <   G \\ \inf \{t \in R | | e (t) | \geq \vartheta \} & | g (t) | \geq G \end{array} \right. \tag {19}
$$

where G is a user-designed parameter and $\theta , \vartheta _ { 1 }$ and $\vartheta$ are the same parameters defined before. Then, we have

$$\bar {e} = \sup | e (t) | \leq \max \left\{\theta | g (t) | + \vartheta_ {1}, \vartheta \right\}, \forall t \in [ t _ {j}, t _ {j + 1}) \tag {20}$$

As with fixed-threshold strategy and relative-threshold strategy, define $| \beta ( t ) | \le 1 , \beta ( t ) _ { 1 } \le 1 , \beta ( t ) _ { 2 } \le 1$ . Similar to (15) and (18), we obtain:
