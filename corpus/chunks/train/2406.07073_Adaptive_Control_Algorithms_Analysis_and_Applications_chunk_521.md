# Remarks

• The resulting closed-loop poles are defined by the polynomial $\lambda A Q + B ^ { * } P$ and they will depend upon A and $B ^ { * }$ . Therefore the location of the closed loop poles cannot be guaranteed.   
• The hypothesis 3 is crucial. If it is not satisfied, $\varepsilon ^ { 0 } ( t + d + 1 )$ ) can go to zero but u(t) and y(t) become unbounded.
