$$\min _ {(\boldsymbol {u} _ {t}) _ {t \in \mathrm{T}}} \sum_ {t \in \mathrm{T} _ {+ 1}} \Phi (\boldsymbol {C} _ {\boldsymbol {\theta}} \boldsymbol {z} _ {t}, \boldsymbol {u} _ {t}) + M \boldsymbol {s} _ {t} ^ {\intercal} \boldsymbol {s} _ {t}, \tag {5a}\text { s.t. } \boldsymbol {z} _ {t + 1} = \boldsymbol {A} _ {\theta} \boldsymbol {z} _ {t} + \boldsymbol {B} _ {\theta} \boldsymbol {u} _ {t} \quad \forall t \in \mathrm{T}, \tag {5b}\underline {{{{\boldsymbol {x}}}}} - \boldsymbol {s} _ {t} \leq \boldsymbol {C} _ {\theta} \boldsymbol {z} _ {t} \leq \bar {\boldsymbol {x}} + \boldsymbol {s} _ {t} \quad \forall t \in \mathrm{T} _ {+ 1}, (5 c)\underline {{{{\boldsymbol {u}}}}} \leq \boldsymbol {u} _ {k} \leq \bar {\boldsymbol {u}} \quad \forall t \in \mathrm{T}, \tag {5d}\boldsymbol {g} (\boldsymbol {C} _ {\theta} \boldsymbol {z} _ {t}, \boldsymbol {u} _ {t}, \boldsymbol {s} _ {t}) \leq 0 \quad \forall t \in \mathrm{T} _ {+ 1}. \tag {5e}$$

Both state variables $\mathbf { \Delta } \mathbf { x } _ { t }$ and control variables $\mathbf { \pmb { u } } _ { t }$ can have upper and lower bounds (see Equations (5c) and (5d)). To guarantee the feasibility of the OCPs, we add slack variables $\mathbf { \Delta } _ { s _ { t } }$ to the state bounds and penalize their use quadratically in the objective function (Equation (5a)) with a penalty factor M. The stage cost Φ of the objective function can be any convex function and depends on the aim of the policy. The linear transition function in Koopman space is given by Equation (5b). Depending on the application, the OCPs can also include further convex inequality constraints (see Equation 5e).

As PPO is an on-policy RL method, the actor must be stochastic during training to ensure exploration. Therefore, during training, we sample the action that the actor will take from a normal distribution $\mathbf { } u _ { t } \sim \mathcal { N } ( u _ { t } ^ { * } , \sigma ^ { 2 } )$ with the mean $\mathbf { \boldsymbol { \mathbf { \mathit { u } } } } _ { t } ^ { * }$ being the deterministic solution of the Koopman-MPC (see Figure 2). Depending on the application, it can be beneficial to use a constant standard deviation $\sigma ,$ or start with a large standard deviation and gradually reduce the amount of exploration, e.g., by reducing σ linearly up to a specific value, as the policy becomes more competent (e.g., Chen et al. (2019)).

![](image/2883e2195dda01fd69f028cd6bbebe9fa981dd4372d00c96105af406d95c60d9.jpg)
