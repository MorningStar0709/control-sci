The above loop lasts for $K ^ { * }$ times, then the $( i - 1 )$ -th agent selects the index $k ^ { * } = \arg \operatorname* { m i n } _ { 1 \leq k \leq K ^ { * } } J _ { i } ^ { k }$ in order to finalize $\varphi _ { i }$ and $p _ { i }$ with $C _ { i } ^ { k ^ { * } }$ .

Theorem 3.2. Algorithm 1 ensures multi-agent systems converge to optimal solutions to Problem (5) with arbitrary small tolerance.

Algorithm 1 Circular Search-and-Monitoring Algorithm   
Initialize: $K^{*}, T_{\varepsilon}, p_{i}, \varphi_{i}, \kappa_{\varphi}, \kappa_{p}$ For $i \in I_N$ , the $i$ -th agent performs as follows

1: for $k = 1: K^{*}$ do

2: Set $t = 0$ 3: while $t < T_{\varepsilon}$ do

4: if $i = \min_{l_k \in \Lambda_k} l_k$ then

5: Set $\varphi_i = 2\pi(k-1)/K^*$ 6: Update $p_i$ with (4) and (7)

7: else

8: Update $\varphi_i$ with (2)

9: Update $p_i$ with (4) and (7)

10: end if

11: end while

12: Save $C_i^k = (\varphi_i^k, p_i^k, J_{E_i}^k)$ and $\Omega_i^k = \{J_{E_i}^k\}$ 13: Receive $\Omega_{i-1}^k$ from $(i-1)$ -th agent

14: while $|\Omega_i^k| \neq |\Omega_i^k \cup \Omega_{i-1}^k|$ do

15: Update $\Omega_i^k = \Omega_i^k \cup \Omega_{i-1}^k$ 16: Send $\Omega_i^k$ to $(i+1)$ -th agent

17: Receive $\Omega_{i-1}^k$ from $(i-1)$ -th agent

18: end while

19: Compute $J_i^k = \sum_{\Omega_i^k} J_E_i^k$ 20: end for

21: Select $k^{*} = \arg\min_{1 \leq k \leq K^{*}} J_i^k$ 22: Finalize $\varphi_i$ and $p_i$ with $C_i^{k*}$

Proof. Let $J ( \varphi ^ { * } , \mathbf { p } ^ { * } )$ and $J ( \varphi ^ { \varepsilon } , \mathbf { p } ^ { \varepsilon } )$ denote the optimal solution to Problem (5) and actual solution with the proposed algorithm, respectively. Then their error can be estimated by

$$
\begin{array}{l} \left| J \left(\varphi^ {*}, \mathbf {p} ^ {*}\right) - J \left(\varphi^ {\varepsilon}, \mathbf {p} ^ {\varepsilon}\right) \right| = \left| J \left(\varphi^ {*}, \mathbf {p} ^ {*}\right) - J \left(\varphi^ {k}, \mathbf {p} ^ {*}\right) + J \left(\varphi^ {k}, \mathbf {p} ^ {*}\right) - J \left(\varphi^ {k}, \mathbf {p} ^ {k}\right) + J \left(\varphi^ {k}, \mathbf {p} ^ {k}\right) - J \left(\varphi^ {\varepsilon}, \mathbf {p} ^ {\varepsilon}\right) \right| \\ \leq | J (\varphi^ {*}, \mathbf {p} ^ {*}) - J (\varphi^ {k}, \mathbf {p} ^ {*}) | + | J (\varphi^ {k}, \mathbf {p} ^ {*}) - J (\varphi^ {k}, \mathbf {p} ^ {k}) | + | J (\varphi^ {k}, \mathbf {p} ^ {k}) - J (\varphi^ {\varepsilon}, \mathbf {p} ^ {\varepsilon}) |. \\ \end{array}
$$
