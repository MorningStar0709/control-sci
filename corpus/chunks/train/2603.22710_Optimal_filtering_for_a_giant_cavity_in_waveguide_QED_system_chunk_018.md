By the Hilbert space projection theorem [30], the estimator admits $\hat { x } ( \bar { t } ) = \bar { x } ( \bar { t ^ { - } } ) + K ( t ) \mathrm { d } \nu ( t )$ . The orthogonality principle then implies $\mathbb { E } [ ( x ( t ) - \dot { x } ( t ) ) \mathrm { d } \nu ^ { T } ( t ) ] = 0 ,$ , which yields $\begin{array} { r l r } { \mathbb { E } [ { \boldsymbol { x } } ( t ) \mathrm { d } \nu ^ { T } ( t ) ] } & { { } = } & { K ( t ) \mathbb { E } [ \mathrm { d } \nu ( t ) \mathrm { d } \nu ^ { T } ( t ) ] } \end{array}$ . Hence, $K ( t ) = \mathbb { E } [ x ( t ) \mathrm { d } \nu ^ { T } ( t ) ] \big ( \mathbb { E } [ \mathrm { d } \nu ( t ) \mathrm { d } \nu ^ { T } ( t ) ] \big ) ^ { - 1 }$ . Since $\mathbb { E } \big [ { \boldsymbol { x } } ( t ) \mathrm { d } { \boldsymbol { \nu } } ^ { T } ( t ) \big ] ~ = ~ [ P ( t ) C ^ { T } + P _ { 1 } ( t ) C _ { d } ^ { T } ] \mathrm { d } t$ , the optimal filter gain is given as

$$K (t) = [ P (t) C ^ {T} + P _ {1} (t) C _ {d} ^ {T} ] (D _ {d} D _ {d} ^ {T}) ^ {- 1}. \qquad (2 7)$$

Substituting (27) into (25), the optimal filter is given by Eq. (22). ■

Remark 4. In contrast to the approach in Ref. [31], where the optimal filter gain depends solely on the covariance matrix $P ( t )$ , the optimal filter gain in (22) involves both $P ( t )$ and $P _ { 1 } ( t )$ . This structural difference is a direct consequence of the output delay inherent in system (20), which introduces additional correlation terms between the current state and delayed states that must be explicitly accounted for in the filter design. The term $D _ { d } D _ { d } ^ { \hat { T } } = \check { I }$ can be obtained from Subsection 2.4. The time evolution of estimation error $P ( t )$ can be deduced as follows.

Theorem 4. Given the linear quantum stochastic delay system (20) and the filter (22), the conditional error covariance ${ \dot { P } } ( t )$ satisfies

$$
\begin{array}{l} \dot {P} (t) = (A - \mathcal {G} (t) C) P (t) + P (t) (A - \mathcal {G} (t) C) ^ {T} \\ + \left(A _ {d} - \mathcal {G} (t) C _ {d}\right) P _ {1} ^ {T} (t) + P _ {1} (t) \left(A _ {d} - \mathcal {G} (t) C _ {d}\right) ^ {T} \\ + B B ^ {T} + \left(B _ {d} - \mathcal {G} (t) D _ {d}\right) \left(B _ {d} - \mathcal {G} (t) D _ {d}\right) ^ {T}, \tag {28} \\ \end{array}
$$

with $P _ { 1 } ( t ) = 0$ for $t < T$ .

Proof. The error dynamics is

$$
\begin{array}{l} \mathrm{d} e (t) = \mathrm{d} x (t) - \mathrm{d} \hat {x} (t) \\ = \left((A - \mathcal {G} (t) C) e (t) + \left(A _ {d} - \mathcal {G} (t) C _ {d}\right) e (t - T)\right) \mathrm{d} t \\ + B \mathrm{d} w (t) + \left(B _ {d} - \mathcal {G} (t) D _ {d}\right) \mathrm{d} w (t - T). \tag {29} \\ \end{array}
$$

Applying the Itˆo product rule to $e ( t ) e ^ { T } ( t )$ gives
