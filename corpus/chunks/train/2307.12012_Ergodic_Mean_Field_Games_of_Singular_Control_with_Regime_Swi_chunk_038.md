$$\mathcal {T} \theta := F \bigg (\sum_ {i = 1} ^ {d} \int_ {\mathcal {I}} f (x) \nu^ {\theta} (d x, i) \bigg) =: F \big (\langle f, \nu^ {\theta} \rangle \big), \quad \theta \in \mathbb {R} _ {+} \tag {4.11}$$

where $\begin{array} { r } { \langle f , \nu ^ { \theta } \rangle : = \sum _ { i \in \mathbb { Y } } \int _ { \mathbb { T } } f ( x ) \nu ^ { \theta } ( d x , i ) } \end{array}$ . Thanks to the previous results, we can now prove the existence and uniqueness of a stationary mean-field equilibrium.

Theorem 4.2. There exists a unique MFG equilibrium $\theta ^ { * } ; i . e .$ , a unique $\theta ^ { \ast } \in \mathbb { R } _ { + }$ such that $\mathcal { T } \theta ^ { * } = \theta ^ { * }$ .

Proof. Step 1: Set of relevant θ. We introduce the following auxiliary Dynkin game

$$
\begin{array}{l} \underline {{v}} (x, i) := \inf _ {\tau \in \mathcal {T}} \sup _ {\sigma \in \mathcal {T}} \widehat {\mathbb {E}} _ {(x, i)} \left[ \int_ {0} ^ {\tau \wedge \sigma} e ^ {\int_ {0} ^ {t} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} \kappa (\widehat {X} _ {s}) d s \right. \tag {4.12} \\ \left. + k _ {1} e ^ {\int_ {0} ^ {\tau} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} \mathbf {1} _ {\{\tau <   \sigma \}} + k _ {2} e ^ {\int_ {0} ^ {\sigma} b _ {x} (\widehat {X} _ {s}, Y _ {s}) d s} \mathbf {1} _ {\{\sigma <   \tau \}} \right]. \\ \end{array}
$$

Arguing as in Section 3.1, the Dynkin game (4.12) admits a value and a saddle point. In particular, we can define $\underline { { \alpha } } _ { i } : = \operatorname* { s u p } \{ x \in \mathcal { I } : \underline { { v } } ( x , i ) \geq k _ { 1 } \}$ and $\beta _ { \scriptscriptstyle + } : = \operatorname* { i n f } \{ x \in \mathcal { T } : \underline { { v } } ( x , i ) \leq k _ { 2 } \}$ , as well as the stopping regions $\underline { { S } } _ { \mathrm { i n f } } : = \{ ( x , i ) \in \mathcal { O } : \underline { { v } } ( x , i ) \geq k _ { 1 } \}$ and $\underline { { S } } _ { \mathrm { s u p } } : = \{ ( x , i ) \in \mathcal { O } : \underline { { v } } ( x , i ) \leq k _ { 2 } \}$ . Thus, we have that the stopping times $\underline { { \tau } } ^ { * } ( x , i ) : = \operatorname* { i n f } \{ t \geq 0 : ( \widehat { X } _ { t } , Y _ { t } ) \in \underline { { S } } _ { \operatorname* { i n f } } \}$ and $\underline { { \sigma } } ^ { * } ( x , i ) : = \operatorname* { i n f } \{ t \geq$ $0 : ( \widehat { X } _ { t } , Y _ { t } ) \in \underline { { S } } _ { \operatorname { s u p } } \}$ realize a saddle-point for (4.12). Also by Condition (3) in Assumption 2.2 and Condition (1) in Assumption 4.1 we conclude that $\pi _ { x } ( x , \theta ) \geq \kappa ( x )$ , for any $\theta \in \mathbb { R } _ { + }$ , so that (cf. (3.4) and (3.6))
