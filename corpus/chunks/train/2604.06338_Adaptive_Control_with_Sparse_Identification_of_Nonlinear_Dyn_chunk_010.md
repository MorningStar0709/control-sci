hold, where $k : = \lambda _ { \operatorname* { m i n } } ( K )$ . Furthermore, assume that for $t \geq t _ { s } .$ the recorded ICL data history is sufficiently rich such that Assumption 2 holds and the control parameters $\gamma , \lambda , \Gamma$ are selected such that

$$r > \frac {m}{\overline {{m}}} \max \left\{r _ {e}, 2 r _ {\theta} + \epsilon \right\} \quad \text { and } \quad \frac {\iota}{\alpha} < \frac {m}{\overline {{m}}} r ^ {2}, \tag {19}$$

where $\begin{array} { r l r l r l r l } { \alpha } & { { } : = } & { \operatorname* { m i n } ( k , \gamma y ) , } & { \iota } & { { } : = } & { \lambda \gamma \sqrt { p } , } & { \underline { { m } } } & { { } : = } & { } \end{array}$ $\frac { 1 } { 2 }$ min $( 1 , \lambda _ { \operatorname* { m i n } } ( \Gamma ^ { - 1 } ) )$ , and $\begin{array} { l l l } { { \overline { { m } } } } & { { : = } } & { { { \frac { 1 } { 2 } } } } \end{array}$ max(1, λmax(Γ−1)). Then, the following hold:

1) The solution is globally defined $( \delta = \infty )$ , and the error state trajectory $t \mapsto e ( t )$ remains inside the the open ball $B _ { e } ( r _ { e } )$ for all $t \in [ t _ { 0 } , t _ { s } )$ .   
2) At time $t _ { s }$ , the closed loop state satisfies $z ( t _ { s } ) \in$ $B _ { z } ( r )$ .   
3) For all $t \geq t _ { s }$ , the closed loop state $z ( t ) \in B _ { z } ( r )$ and converges to a set $\Omega _ { u b , z }$ , defined as

$$\Omega_ {u b, z} := \left\{z \in \mathbb {R} ^ {n + p} \mid \| z \| \leq \sqrt {\frac {\overline {{m}}}{\underline {{m}}} \left(\frac {\iota}{\alpha}\right)} \right\}. \tag {20}$$

Furthermore, the ultimate bound is contained in the inner set, i.e., $\Omega _ { u b , z } \subset \overline { { B } } _ { z } ( r _ { s } )$ .

Proof: Part 1 - Analysis on the interval $[ t _ { 0 } , t _ { s } ) .$

Consider the continuously differentiable candidate Lyapunov function $V _ { e } : \mathbb { R } ^ { n } $ R defined as

$$V _ {e} (e) := \frac {1}{2} e ^ {\top} e. \tag {21}$$

The time derivative of the function $t \mapsto V _ { e } ( e ( t ) )$ along the trajectory $e ( \cdot )$ exists for almost all $t ~ \in ~ [ t _ { 0 } , t _ { 0 } + \delta )$ . Substituting the dynamics from (4), we obtain the bound

$$
\begin{array}{l} \dot {V} _ {e} (e (t)) = e (t) ^ {\top} \dot {e} (t) \\ = e (t) ^ {\top} \left(- K e (t) + d (t, e (t))\right) \\ \leq - k \| e (t) \| ^ {2} + \| e (t) \| \| d (t, e (t)) \|, \tag {22} \\ \end{array}
$$

for almost all $t \in [ t _ { 0 } , t _ { 0 } + \delta )$ . Completing the squares,

$$\dot {V} _ {e} (e (t)) \leq - \frac {k}{2} \| e (t) \| ^ {2} + \frac {\| d (t , e (t)) \| ^ {2}}{2 k}, \tag {23}$$
