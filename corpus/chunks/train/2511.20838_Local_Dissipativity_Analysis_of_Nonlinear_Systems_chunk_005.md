# Notation

The interior, boundary, and closure of the set $\Omega \subset \mathbb { R } ^ { n }$ are denoted as $\Omega ^ { \circ } , \delta \Omega ,$ and Ω. The symbol¯ $\Re ^ { n }$ denotes the set of all compact subsets $\Omega \subset \mathbb { R } ^ { i }$ satisfying i) Ω◦ is connected and contains the origin and ii) $\dot { \Omega } = \overrightarrow { \Omega } ^ { \circ }$ . Let $\mathbb { R } _ { + }$ represent the set of non-negative real numbers. The notation $\mathbb { Z } _ { a } ^ { b } \left( \mathbb { Z } _ { \overline { { a } } } ^ { \underline { { b } } } \right)$ denote the set of integers between a and b inclusive (exclusive). The p-norm of a vector $x \in \mathbb { R } ^ { n }$ is denoted as $\left\| \cdot \right\| _ { p } ,$ , where $p \in \mathbb { Z } _ { 1 } ^ { \infty }$ . Let $B _ { \epsilon } ( 0 )$ denote a closed two-norm ball about the origin with radius ϵ.

The positive definiteness (semi-definiteness) of a matrix is denoted by $\mathbf { P } \succ 0 \left( \mathbf { P } \succeq 0 \right)$ . Identity and zero matrices are denoted as I and 0. Let 1 denote a vector of ones in $\mathbb { R } ^ { n }$ , and let $\operatorname { H e } ( \mathbf { X } ) = \mathbf { X } + \mathbf { X } ^ { \dagger }$ . Recall that for a matrix operator norm, $\mathbf { \left\| A B \right\| } \leq \left\| \mathbf { A } \right\| \left\| \mathbf { B } \right\| [ 2 2 ]$ . From Lemma 2.3 of [7], if h¯ is the maximum absolute value of any element within the matrix $\mathbf { H } \in \mathbb { R } ^ { n \times m }$ , then $\left\| \mathbf { H } \right\| _ { 2 } \leq { \sqrt { n m } } { \bar { h } }$ .

Let the $\mathcal { L } _ { 2 } ^ { n }$ space be a set containing all measurable functions $\mathbf { u } : \mathbb { R } _ { + } \to \mathbb { R } ^ { n }$ that satisfy $\begin{array} { r } { \int _ { 0 } ^ { \infty } \mathbf { u } ( t ) ^ { \top } \mathbf { u } ( t ) d t < \infty } \end{array}$ . The extended $\mathcal { L } _ { 2 e } ^ { n }$ space is defined as the set of all measurable functions u $\colon \mathbb { R } _ { + } \to \mathbb { R } ^ { n }$ for which the truncation to $t \in [ 0 , T ]$ is in $\mathcal { L } _ { 2 } ^ { n }$ for all $T \geq 0$ .
