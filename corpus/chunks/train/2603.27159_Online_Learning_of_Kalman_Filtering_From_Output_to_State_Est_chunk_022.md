Challenges in the regret analysis. For the full information setting in online optimization, it has been shown in the literature that for strongly convex cost functions $f _ { 0 } ( \cdot ) , \ldots , f _ { T - 1 } ( \cdot )$ with $f _ { t } : \mathbb { R } ^ { d } $ R such that $\nabla ^ { 2 } f _ { t } ( N ) \succeq \mu I _ { d }$ for some $\mu > 0 ,$ the OGD algorithm achieves log T -regret $( \mathrm { e . g . , \ [ 4 5 ] ) }$ . However, since the output $y _ { t }$ of system (1) is random (due to the noise terms $w _ { t }$ and $v _ { t } )$ , the objective function $f _ { t } ( N ) = \| y _ { t } - \hat { y } _ { t } ( N _ { t } ) \| ^ { 2 }$ is also random given any $N \in { \cal K } _ { N }$ , which implies that $\nabla ^ { 2 } f _ { t } (  { N } ) \succ 0$ may no longer hold deterministically for any t. Thus, the existing results of OGD for strongly convex functions cannot be applied to our problem. In fact, using only the convexity of√ $f _ { t } ( \cdot )$ (that holds deterministically for all $t \geq 0 )$ , it was shown in [11] that OGD achieves an $\tilde { \mathcal { O } } ( \sqrt { T } )$ regret for learning Kalman filtering for output estimation (of scalar linear dynamical system), where $\tilde { \mathcal { O } } ( \cdot )$ hides factors in log $T$ .

Prior knowledge required by Algorithm 1. As mentioned in (13), we assume known constants $\alpha _ { 0 } , \psi , \bar { \sigma }$ (i.e., upper bounds on system-related matrices) so that the projection $\Pi _ { K _ { N } } ( \cdot )$ in line 5 of the algorithm can be implemented. Meanwhile, recalling from (14) that $\gamma _ { F } = \sqrt { \bar { \sigma } \alpha _ { 0 } }$ , setting the step sizes $\eta _ { t }$ for all $t \geq 1$ and truncated filter length h as Theorem 4.1 require $\alpha _ { 0 } , \bar { \sigma }$ and the knowledge of the horizon length T . In Appendix C, we argue that using a doubling trick that has been widely used for online algorithms (see, e.g., [49]), one can remove the dependency of $\eta _ { t }$ and h on $T$ and achieve the same $\mathcal { O } ( \log ^ { 4 } T )$ regret as Theorem 4.1 without the knowledge of $T .$ . Finally, we note that assuming known constants such as α0, ψ, σ¯ described above is typical in learning to estimate or control of unknown LTI systems [14, 50, 51].
