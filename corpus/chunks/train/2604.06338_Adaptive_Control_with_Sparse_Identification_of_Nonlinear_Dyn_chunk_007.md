# A. Sparse Recursive Cost and Generalized Gradient

To promote sparse parameter estimates while maintaining consistency with the accumulated integral data, consider the ℓ1-regularized cost

$$J (\hat {\theta}) := \frac {1}{2} \hat {\theta} ^ {\top} \mathcal {Y} \hat {\theta} - \hat {\theta} ^ {\top} \mathcal {U} + \lambda \| \hat {\theta} \| _ {1}, \tag {9}$$

where $\lambda > 0$ is the sparsity parameter and $\lVert \hat { \boldsymbol { \theta } } \rVert$ 1 denotes the one-norm of ${ \hat { \theta } } .$ The adaptive update law for ˆθ is derived to minimize the regularized cost using gradient descent. Since the ℓ1−regularization term is nonsmooth at the origin, a generalized gradient of J is utilized, given by the Clarke sub-differential [33]

$$\partial_ {\hat {\theta}} J (\hat {\theta}) = \mathcal {Y} \hat {\theta} - \mathcal {U} + \lambda \operatorname{SGN} (\hat {\theta}), \tag {10}$$

where $\operatorname { S G N } ( { \hat { \theta } } )$ denotes the componentwise set-valued sign map, defined for each component i as $\mathrm { S G N } ( \hat { \theta } _ { i } ) = \big \{ z \^ { } \in \big .$ $[ - 1 , 1 ] \mid z = \mathrm { s i g n } ( \hat { \theta } _ { i } ) \mathrm { i f } \hat { \theta } _ { i } \neq 0 \}$ .

Define the compact set $\Theta : = \mathcal { \bar { \{ \theta } \in \mathbb { R } ^ { p } }  : \lVert \theta \rVert \leq r _ { \theta } \}$ , where $r _ { \theta } > 0$ is a known bound. The SP-ICL update law is designed as a differential inclusion using the negative gradient in (10) as

$$\dot {\hat {\theta}} \in \operatorname{proj} _ {\Theta} (\hat {\theta}, \phi (x, e, \hat {\theta}), \Gamma) - \lambda \gamma \Gamma \operatorname{SGN} (\hat {\theta}) =: G (t, \hat {\theta}), \tag {11}$$

where $\phi ( x , e , \hat { \theta } ) : = \Gamma \Big ( Y ( x ) ^ { \top } e + \gamma \big ( \mathcal { U } - \mathcal { Y } \hat { \theta } \big ) \Big ) , \ \gamma > 0$ is the ICL-learning gain, $\dot { \Gamma } = \Gamma ^ { \top } \succ 0$ is a diagonal adaptation gain matrix, and $\mathrm { p r o j } _ { \Theta } ( \cdot , \cdot , \cdot )$ denotes the smooth projection operator from [4, Appendix E.1], which ensures that ${ \hat { \theta } } ( t ) \in$ Θ for all $t \geq t _ { 0 }$ , in the absence of the signum term in (11).
