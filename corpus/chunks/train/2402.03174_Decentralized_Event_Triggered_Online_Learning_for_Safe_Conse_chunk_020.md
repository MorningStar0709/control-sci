Note that $2 \sqrt { N } \geq \sqrt { N - 1 } + 1$ for $N \in \mathbb { N } .$ , therefore at least one agent belongs to $\mathbb { S } _ { 2 } \cup \mathbb { S } _ { 3 }$ . This also means when $\| \tilde { \pmb { x } } \| > c ^ { - 1 } N \eta$ the inequality $\left| \mathbb { S } _ { 2 } \cup \mathbb { S } _ { 3 } \right| \geq 1$ holds, which is then applied in (28) such that $\dot { V } _ { c }$ is bounded as

$$\dot {V} _ {c} < - \frac {2 \| \tilde {\boldsymbol {x}} \| N \eta^ {2}}{c \| \tilde {\boldsymbol {x}} \| + \| \hat {\boldsymbol {\eta}} (\boldsymbol {x}) \|} (| \mathbb {S} _ {2} \cup \mathbb {S} _ {3} | - 1) \leq 0. \tag {30}$$

This results in the decay of Lyapunov function V when $\| \tilde { \pmb { x } } \| > 2 c ^ { - 1 } N \eta$ , indicating $2 c ^ { - 1 } N \eta$ is the ultimate bound for $\| { \tilde { \pmb x } } \| .$ , which concludes the proof.

Remark 5: The proof for Lemma 3 also indicates the conventional control law (2) cannot achieve average consensus under non-vanishing prediction error $\pmb { f } ( \pmb { x } ) - \hat { \pmb { \mu } } ( \pmb { x } )$ , since the minimal eigenvalue of $\pmb { \mathcal { L } }$ is zero inducing infinite large ǫ by considering $\epsilon = ( c \underline { { { \lambda } } } ( \pmb { \mathcal { L } } ) ) ^ { - 1 } N \underline { { { \eta } } }  \infty$ from (21) with $\pmb { \mathcal { L } } + \pmb { \mathit { I } } _ { N }$ replaced by $\pmb { \mathcal { L } } .$ .

Apply the intermediate result from Lemma 2 and Lemma 3, the proof of Theorem 1 is ready.

Proof of Theorem 1: Using the triangular inequality, the average consensus error $\lVert \pmb { x } \mathrm { - } \mathbf { 1 } _ { N } \bar { \pmb { x } } ^ { * } \rVert$ is ultimately bounded by

$$
\begin{array}{l} \lim _ {t \to \infty} \| \boldsymbol {x} - \mathbf {1} _ {N} \bar {x} ^ {*} \| \leq \lim _ {t \to \infty} \| \tilde {\boldsymbol {x}} \| + \lim _ {t \to \infty} \| \bar {\boldsymbol {x}} - \mathbf {1} _ {N} \bar {x} ^ {*} \| \\ = \lim _ {t \to \infty} \| \tilde {\boldsymbol {x}} \| \leq 2 c ^ {- 1} N \underline {{\eta}} = \epsilon , \tag {31} \\ \end{array}
$$

inherited the probability of at least $1 - N \delta$ from Lemma 3 by considering the probability of Lemma 2 as 1. 
