$$\sum_ {t = 1} ^ {T} V ^ {\pi^ {*}} - V ^ {\pi_ {\theta_ {t}}} = \sum_ {t = 1} ^ {T} \delta_ {t} \leqslant \sqrt {T} \sqrt {\sum_ {t = 1} ^ {T} \delta_ {t} ^ {2}} \leqslant \sqrt {T} \sqrt {\sum_ {t = 1} ^ {T} \frac {5}{(1 - \gamma) c _ {T} ^ {2}} (\delta_ {t} - \delta_ {t + 1})} \leqslant \frac {1}{c _ {T}} \sqrt {\frac {5 T}{(1 - \gamma)}}.$$

We next show that with $\theta _ { m } ^ { ( 1 ) } = 1 / M , \forall m .$ i.e., uniform initialization, in $\mathrm { f } _ { t \geq 1 } c _ { t } = 1 / M$ , which will then complete the proof of Theorem H.4 and of corollary H.2.

Lemma H.5. We have in $\mathrm { f } _ { t \geq 1 } \pi _ { \theta _ { t } } ( m ^ { * } ) > 0 .$ . Furthermore, with uniform initialization of the parameters $\theta _ { m } ^ { ( 1 ) }$ , i.e., $1 / M , \forall m \in [ M ]$ , we have in $\begin{array} { r } { \mathrm { f } _ { t \geq 1 } \pi _ { \theta _ { t } } ( m ^ { * } ) = \frac { 1 } { M } } \end{array}$ .

Proof. We will show that there exists $t _ { 0 }$ such that in $\mathrm { f } _ { t \geqslant 1 } \pi _ { \theta _ { t } } ( m ^ { * } ) = \operatorname* { m i n } _ { 1 \leqslant t \leqslant t _ { 0 } } \pi _ { \theta _ { t } } ( m ^ { * } )$ , where $t _ { 0 } = \operatorname* { m i n } \left\{ t : \pi _ { \theta _ { t } } ( m ^ { * } ) \geqslant C \right\}$ . 16t6t0 We define the following sets.

$$
\begin{array}{l} \mathcal {S} _ {1} = \left\{\theta : \frac {d V ^ {\pi_ {\theta}}}{d \theta_ {m ^ {*}}} \geqslant \frac {d V ^ {\pi_ {\theta}}}{d \theta_ {m}}, \forall m \neq m ^ {*} \right\} \\ \mathcal {S} _ {2} = \left\{\theta : \pi_ {\theta} (m ^ {*}) \geqslant \pi_ {\theta} (m), \forall m \neq m ^ {*} \right\} \\ \mathcal {S} _ {3} = \{\theta : \pi_ {\theta} (m ^ {*}) \geqslant C \} \\ \end{array}
$$

$S _ { 3 }$ $\begin{array} { r } { C : = \frac { M - \Delta } { M + \Delta } } \end{array}$

Claim 2. $( i ) \theta _ { t } \in \mathcal { S } _ { 1 } \implies \theta _ { t + 1 } \in \mathcal { S } _ { 1 }$ and $( i i ) \theta _ { t } \in \mathring { S _ { 1 } } \ \stackrel { = } { \Longrightarrow } \ \pi _ { \theta _ { t + 1 } } ( m ^ { * } ) \geqslant \pi _ { \theta _ { t } } ( m ^ { * } )$

Proof of Claim 2. (i) Fix a m $\neq m ^ { * }$ . We will show that if $\begin{array} { r } { \frac { d V ^ { \pi _ { \theta } } } { d \theta _ { t } ( m ^ { * } ) } \geqslant \frac { d V ^ { \pi _ { \theta } } } { d \theta _ { t } ( m ) } } \end{array}$ , then $\begin{array} { r } { \frac { d V ^ { \pi _ { \theta } } } { d \theta _ { t + 1 } \left( m ^ { * } \right) } \geqslant \frac { d V ^ { \pi _ { \theta } } } { d \theta _ { t + 1 } \left( m \right) } } \end{array}$ . This will prove the first part.
