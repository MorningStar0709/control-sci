(iii) kε-optimal control input reads as $I f \gamma _ { k } ^ { \varepsilon / 2 }$ in (ii) is induced by a transport map $\lambda _ { k } ^ { \varepsilon } = ( \operatorname { i d } _ { X _ { k } } , u _ { k } ^ { \varepsilon / 2 } \circ ( \operatorname { i d } _ { X _ { k } } , T _ { k } ^ { \varepsilon / 2 } ) ) _ { \# } \mu _ { k } .$ $T _ { k } ^ { \varepsilon / 2 } : X _ { k } \to R _ { k } \times . . . \times R _ { N }$ . , the

Before discussing Theorem 4.1 and its implications, we consider the special case when the stage costs $g _ { k }$ do not depend on the reference; i.e., $g _ { k } : X _ { k } \times U _ { k } \to \bar { \mathbb { R } } _ { \ge 0 }$ . For instance, any shortest path problem on a graph can be converted into a finite-horizon optimal control problem (see, e.g., [16]), where the weights of the edges determine the stage costs $g _ { k } ;$ these depend only on the pair $( x _ { k } , u _ { k } )$ . In these cases, the DPA reads

$$
\begin{array}{l} j _ {N} (x _ {N}, r _ {N}) := g _ {N} (x _ {N}, r _ {N}); \\ j _ {k} (x _ {k}, r _ {N}) := \inf _ {u _ {k} \in U _ {k}} g _ {k} (x _ {k}, u _ {k}) + j _ {k + 1} (f _ {k} (x _ {k}, u _ {k}), r _ {N}). \end{array} \tag {4.3}
$$

Accordingly, the ground space ε2 -optimal input is of the form $u _ { k } ^ { \varepsilon / 2 } : X _ { k } \times R _ { N }  U _ { k }$ and the cost-to-go $J _ { k }$ simplifies to a two-marginals optimal transport discrepancy:

Corollary 4.2 (When two marginals are all you need). Consider the setting of Theorem 4.1, with $g _ { k } : X _ { k } \times U _ { k } \to \bar { \mathbb { R } } _ { \geq 0 }$ . At every stage k, the following hold:

(i) The cost-to-go equals the optimal transport discrepancy

$$(4. 4) \quad J _ {k} (\mu_ {k}, \rho_ {N}) = \mathcal {K} [ j _ {k} ] (\mu_ {k}, \rho_ {N}) = \inf _ {\boldsymbol {\gamma} \in \Gamma (\mu_ {k}, \rho_ {N})} \int_ {X _ {k} \times R _ {N}} j _ {k} (x _ {k}, r _ {N}) \mathrm{d} \boldsymbol {\gamma} (x _ {k}, r _ {N}),$$

where $j _ { k }$ is the cost-to-go in the ground space, as in (4.3). Moreover, the DPA yields the optimal solution $J = J _ { 0 }$ .

(ii) $F o r \varepsilon \geq 0$ , suppose $u _ { k } ^ { \varepsilon / 2 } : X _ { k } \times R _ { N }  U _ { k }$ and $\gamma _ { k } ^ { \varepsilon / 2 } \in \Gamma ( \mu _ { k } , \rho _ { N } )$ are ε2 -optimal in (4.3) and (4.1), respectively. Then

$$\lambda_ {k} ^ {\varepsilon} = \left(\mathrm{proj} _ {X _ {k}} ^ {X _ {k} \times R _ {N}}, u _ {k} ^ {\varepsilon / 2}\right) _ {\#} \gamma_ {k} ^ {\varepsilon / 2} \tag {4.5}$$

is an ε-optimal state-input distribution. $I f \varepsilon = 0$ , then $\lambda _ { k } ^ { * } : = \lambda _ { k } ^ { \varepsilon }$ is optimal.
