$$\max _ {k \leq t} \| r _ {k} \| \leq \max _ {k \leq t, k \notin \mathcal {T} _ {t}} \left\| (\Theta_ {*} - \tilde {\Theta} _ {k - 1}) ^ {\intercal} z _ {k} \right\| + \max _ {k \leq t} \| B _ {*} \nu_ {k} + w _ {k} \|$$

The first term is bounded by the Lemma 11. The second term involves summation of independent $\| B _ { * } \| \sigma _ { \nu }$ and $\sigma _ { w }$ Gaussian vectors. Using standard Gaussian tail inequalities, for all $k \leq t .$ , we have $\begin{array} { r } { \| B _ { * } \nu _ { k } + w _ { k } \| \leq ( \| B _ { * } \| \sigma _ { \nu } + \sigma _ { w } ) \sqrt { 2 n \log \frac { n t } { \delta } } } \end{array}$ with probability at least $1 - \delta$ . Therefore, on the joint event of $\hat { E } \cap \tilde { E }$ ,

$$\left\| x _ {t} \right\| \leq \frac {1 8 \kappa^ {3} \bar {\eta} ^ {n + d}}{\gamma (8 \kappa - 1)} \left[ G Z _ {t} ^ {\frac {n + d}{n + d + 1}} \left(\beta_ {t} (\delta) + v (\delta)\right) ^ {\frac {1}{2 (n + d + 1)}} + \left(\left\| B _ {*} \right\| \sigma_ {\nu} + \sigma_ {w}\right) \sqrt {2 n \log \frac {n t}{\delta}} \right] \tag {24}$$

for $t \leq T _ { w }$ with probability $1 - \delta .$ Notice that this bound depends on $Z _ { t }$ and $\beta _ { t } ( \delta )$ which in turn depends on $x _ { t }$ . Using Lemma 5 of Abbasi-Yadkori and Szepesv´ari (2011), one can obtain the following bound

$$\left\| x _ {t} \right\| \leq c ^ {\prime} (n + d) ^ {n + d}. \tag {25}$$

for some constant $c ^ { \prime }$ for all $t \leq T _ { w }$ with probability $1 - 3 \delta$ , which gives the first advertised result.
