$$
\mathbf {H} _ {\left(\mathrm{N}, \mathrm{H}, y _ {1}\right)} = \mathbf {W} \left[ \begin{array}{l l} \boldsymbol {\Sigma} & \mathbf {0} \end{array} \right] _ {\left(\mathrm{N} \times \mathrm{H}\right)} \left[ \begin{array}{l} \mathbf {V} ^ {\top} \\ \mathbf {V} _ {\mathrm{r}} ^ {\top} \end{array} \right] _ {\left(\mathrm{H} \times \mathrm{H}\right)} = \mathbf {W} \boldsymbol {\Sigma} \mathbf {V} ^ {\top} = \mathbf {W} \boldsymbol {\Sigma} ^ {\frac {1}{2}} \boldsymbol {\Sigma} ^ {\frac {1}{2}} \mathbf {V} ^ {\top} = \underbrace {\mathbf {W} \boldsymbol {\Sigma} ^ {\frac {1}{2}} \mathbf {P}} _ {\mathbf {O} _ {\mathrm{N}}} \underbrace {\mathbf {P} ^ {- 1} \boldsymbol {\Sigma} ^ {\frac {1}{2}} \mathbf {V} ^ {\top}} _ {\mathbf {C} _ {\mathrm{H}}} \tag {37}
$$

where $\pmb { \Sigma } = d i a g ( \sigma _ { 1 } , \dots , \sigma _ { \mathrm { N } } ) \in \mathbb { R } ^ { \mathrm { N } \times \mathrm { N } } , \mathbf { P } = \mathbb { R } ^ { \mathrm { N } \times \mathrm { N } }$ is any invertible matrix, $\mathbf { W } \in \mathbb { R } ^ { \mathrm { { N \times N } } }$ , and $\mathbf { V } \in \mathbb { R } ^ { \mathrm { H \times N } }$ . The number of non-zero singular values (diagonal elements of Σ) gives an approximation of the model order n denoted by s. The resulting model with order s is denoted by:

$$\mathbf {x} _ {\mathrm{s} _ {k + 1}} = \mathbf {A} _ {\mathrm{s}} \mathbf {x} _ {\mathrm{s} _ {k}} + \mathbf {B} _ {\mathrm{s}} \mathbf {u} _ {k} \tag {38}\hat {\mathbf {y}} _ {\mathrm{s} _ {k}} = \mathbf {C} _ {\mathrm{s}} \mathbf {x} _ {\mathrm{s} _ {k}}$$

where $\mathbf { A } _ { \mathrm { s } } \in \mathbb { R } ^ { s \times s } , \mathbf { B } _ { \mathrm { s } } \in \mathbb { R } ^ { s \times m } , \mathbf { C } _ { \mathrm { s } } \in \mathbb { R } ^ { p \times s }$ , and $\mathbf { x } _ { \mathbf { s } _ { k } } \in \mathbb { R } ^ { s }$ . Note that, the singular values are nonnegative and ordered in decreasing magnitude. Consequently, the approximate model order s can be computed by solving:

$$s = \underset {i \in \{1, 2, \dots , N \}} {\operatorname{argmax}} i \tag {39}$$

subject to $\sigma _ { i } \geq \epsilon .$

where $\epsilon > 0$ is a tolerance value. Using SVD, a rank−s approximation to ${ \bf H } _ { ( \mathrm { N } , \mathrm { H } , y _ { 1 } ) }$ can be obtained as:
