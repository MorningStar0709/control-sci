On the other hand, if Σ is a feasible candidate for (6), then Proposition 5.3 establishes the existence of a feasible stochastic process $( \chi , w , \hat { w } )$ with the same value for (5) as $\Sigma$ has for (6). This implies that the optimal value of (5) is larger than or equal to the optimal value of (6). □

According to Theorem 5.4, we can utilize the moment relaxation problem (6) to exactly analyze the worst-case performance of a controller (4b). However, if we were willing to minimize this worst-case performance over the controller parameters, we would be confronted with a minimax optimization problem, which is notoriously hard to solve. For this reason, we study the Lagrangian dual of (6), which is a minimization problem, in the next section.
