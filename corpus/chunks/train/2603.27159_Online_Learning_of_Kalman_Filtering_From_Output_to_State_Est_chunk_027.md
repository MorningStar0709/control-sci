where we use the upper bound $\lVert f _ { t } ( N _ { t } ) \rVert \leq l _ { y }$ from Lemma 3.4 and the choice of the step size $\begin{array} { r } { \eta _ { t } = \frac { 2 } { ( \alpha _ { 0 } \log ^ { 2 } T ) t } } \end{array}$ for all $t \geq 1$ . To obtain (b), we first recall the expressions of $\kappa _ { F } , \gamma _ { F }$ in (13) and the choice of $h = \mathcal { O } ( \log T )$ , and then deduce from (26) and (30) that $R _ { y } = \mathcal { O } ( \sqrt { \log T } ) , \beta _ { y } = \mathcal { O } ( \log ^ { 2 } T )$ , $l _ { y } = \mathcal { O } ( \log ^ { 2 } T )$ and $R _ { N } = { \mathcal { O } } ( 1 )$ , where $\mathcal { O } ( \cdot )$ hides the factors stated in Theorem 4.1.

Truncation and steady-state regret. To upper bound the truncation and steady-state regret in (31), we first recall from our discussions in Section 2.1 that $\hat { y } _ { t } ^ { \mathrm { T K } }$ in (17) is a truncated steady-state version of the original Kalman filter $\hat { y } _ { t } ^ { \mathrm { K F } } = C \hat { x } _ { t } ^ { \mathrm { K F } }$ with $\hat { x } _ { t } ^ { \mathrm { K F } }$ given by (2). Therefore, the truncation and steady-state regret in (31) consists of two components corresponding to the truncation and the consideration of the steady-state of the Kalman filter. We have

$$\sum_ {t = 0} ^ {T - 1} \left(f _ {t} (N ^ {\star}) - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{KF}} \right\| ^ {2}\right) = \underbrace {\sum_ {t = 0} ^ {T - 1} \left(\left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{TK}} \right\| ^ {2} - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| ^ {2}\right)} _ {(i)} + \underbrace {\sum_ {t = 0} ^ {T - 1} \left(\left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| ^ {2} - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{KF}} \right\| ^ {2}\right)} _ {(i i)}, \tag {33}$$

where $\hat { y } _ { t } ^ { \mathrm { T K } }$ and $\hat { y } _ { t } ^ { \tt S K }$ are defined in (16) and (17), respectively. We now prove the following results.

Lemma 4.5. Under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ , it holds that

$$\sum_ {t = 0} ^ {T - 1} \big (\left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{TK}} \right\| ^ {2} - \left\| y _ {t} - \hat {y} _ {t} ^ {\mathrm{SK}} \right\| ^ {2} \big) \leq \Big (2 + \sqrt {h} R _ {N} + \| L \| \| C \| \frac {\kappa_ {F}}{1 - \gamma_ {F}} \Big) \frac {R _ {y} ^ {2} \kappa_ {F} \| L \| \| C \|}{1 - \gamma_ {F}}. (3 4)$$

Lemma 4.6. Under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ , it holds that
