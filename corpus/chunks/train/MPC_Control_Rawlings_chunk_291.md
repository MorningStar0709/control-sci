in which $V _ { f } ^ { \prime } ( \cdot )$ and $\mathbb { X } _ { f } ^ { \prime }$ are, respectively, the terminal cost function and terminal constraint set derived in Section 2.5.3.1; the reference r must satisfy the constraint that $\mathbb { X } _ { f } ( r ) \subset \mathbb { X }$ . With this choice, $V _ { f } ( \bar { x } ( r ) , r ) = 0$ and $\mathbb { X } _ { f } ( r )$ is control invariant for $x ^ { + } = A x + B u$ . It is easily shown, with either choice and appropriate assumptions, that the point $\bar { x } ( r )$ is asymptotically, or exponentially, stable for the controlled system with a region of attraction $\mathcal { X } _ { N } ( r ) : = \{ x \mid \mathcal { U } _ { N } ( x , r ) \neq \emptyset \}$ .
