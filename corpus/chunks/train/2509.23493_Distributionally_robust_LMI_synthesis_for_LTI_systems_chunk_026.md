$$
\operatorname{tr} \left(\binom{\Delta}{I} ^ {\top} \left( \begin{array}{c c} I & - I \\ - I & I \end{array} \right) \binom{\Delta}{I} \Sigma_ {\mathrm{nom}}\right) \leq \gamma^ {2}.
$$

In the next step, we add this constraint to the $H _ { 2 }$ norm condition and arrive at the alternative result that the weighted $H _ { 2 }$ norm of (16) is smaller than $\rho$ if and only if there exists a matrix $\mathcal { P } \succ 0$ and a scalar $\lambda \geq 0$ such that

$$0 \succ \mathcal {A} ^ {\top} \mathcal {P A} - \mathcal {P} + \mathcal {C} ^ {\top} \mathcal {C},\rho^ {2} > \operatorname{tr} \left(\left(\Delta^ {\top} \mathcal {B} ^ {\top} \mathcal {P B} \Delta + \Delta \mathcal {D} ^ {\top} \mathcal {D} \Delta\right) \Sigma_ {\text {nom}}\right)
- \lambda   \mathrm{tr} \left(\binom{\Delta}{I} ^ {\top} \left( \begin{array}{c c} I & - I \\ - I & I \end{array} \right) \binom{\Delta}{I} \Sigma_ {\mathrm{nom}}\right) + \lambda \gamma^ {2}.
$$

The equivalence follows directly from Theorem 7.1, since we added a non-negative term to the inequality, which is zero for $\lambda = 0 .$ . In the next step, we introduce an auxiliary variable Q and rewrite the condition as

$$
\Delta^ {\top} (\mathcal {B} ^ {\top} \mathcal {P B} + \mathcal {D} ^ {\top} \mathcal {D}) \Delta + \lambda \binom{\Delta}{I} ^ {\top} \left( \begin{array}{c c} - I & I \\ I & - I \end{array} \right) \binom{\Delta}{I} <   Q,
$$

and tr $\left( Q \Sigma _ { \mathrm { n o m } } \right) + \lambda \gamma ^ { 2 } < \rho ^ { 2 }$ . Equivalently, we can express this as

$$
\left( \begin{array}{l} \Delta \\ I \end{array} \right) ^ {\top} \underbrace {\left( \begin{array}{c c} \mathcal {B} & 0 \\ \mathcal {D} & 0 \\ I & 0 \\ 0 & I \end{array} \right) ^ {\top} \left( \begin{array}{c c c c} \mathcal {P} & 0 & 0 & 0 \\ 0 & I & 0 & 0 \\ 0 & 0 & - \lambda I & \lambda I \\ 0 & 0 & \lambda I & - Q - \lambda I \end{array} \right) \left( \begin{array}{c c} \mathcal {B} & 0 \\ \mathcal {D} & 0 \\ I & 0 \\ 0 & I \end{array} \right)} _ {(\star)} (\Delta) \tag {18}
$$

being negative definite, and $\mathrm { t r } ( Q \Sigma _ { \mathrm { n o m } } ) + \lambda \gamma ^ { 2 } < \rho ^ { 2 }$ .
