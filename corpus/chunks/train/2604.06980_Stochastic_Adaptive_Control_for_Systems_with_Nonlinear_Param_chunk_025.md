Theorem 4 Suppose that Assumptions 1-5 hold, then for any given $\begin{array} { r } { b \in \left[ 0 , \frac { ( \gamma - 2 ) ^ { 2 } } { 4 \gamma ( \gamma + 2 ) } \right) } \end{array}$ h0, (γ−2)4γ(γ+2) and $x _ { 0 } \in \mathbb { R } ^ { n }$ , the closedloop trajectory under adaptive control (23) guarantees that the average tracking error Jt defined in (29) vanishes with the following convergence rate:

$$J _ {t} = O \left(t ^ {- 2 b} + t ^ {\frac {(2 b + \eta - 1) (\gamma - 2)}{\gamma}} (\log t) ^ {\frac {(1 + \delta) (\gamma - 2)}{\gamma}}\right), a. s., \tag {31}$$

where $\delta \ > \ 0$ is given in Algorithm 1, $\gamma ~ > ~ 2 ~ i s$ given in Assumption 2, η is any constant satisfying $\begin{array} { r } { \eta \in \left( \frac { 8 b } { \gamma - 2 } , \frac { 2 ( \gamma - 2 ) } { \gamma ( \gamma + 2 ) } \right) } \end{array}$ .

The proof of Theorem 4 is provided in Appendix D.

As indicated by Theorem 4, the performance gap between the adaptive trajectory and the reference trajectory consists of two sources: one is induced by the probing signal $v _ { t }$ injected through the control law (23), and the other is the estimation error $\lVert \hat { { \boldsymbol { \theta } } } _ { t } - { \boldsymbol { \theta } } ^ { * } \rVert$ at each time step. The decay rates of these two components correspond, respectively, to the two terms in (31).

Theorem 4 focuses on the classical mean-square cost. In some scenarios, however, one may be interested in a more general nonlinear stage cost $c : { \mathbb { R } } ^ { n + m }  { \mathbb { R } }$ and the associated trajectory tracking metric $\begin{array} { r } { \frac { 1 } { t } \sum _ { \tau = 0 } ^ { t } | c ( x _ { t } , u _ { t } ) - } \end{array}$ $c ( x _ { t } ^ { * } , u _ { t } ^ { * } ) | ^ { 2 }$ . In this case, the corresponding convergence result is presented in the following corollary.

Corollary 1 Under the conditions of Theorem 4, with (30) replaced by

$$
\begin{array}{l} \left| c \left(x _ {t} ^ {*} \left(x _ {0}\right), u _ {t} ^ {*} \left(x _ {0}\right)\right) - c \left(x _ {t} ^ {*} \left(x _ {0} ^ {\prime}\right), u _ {t} ^ {*} \left(x _ {0} ^ {\prime}\right)\right) \right| \tag {32} \\ \leq M _ {0} \rho_ {0} ^ {t} \| x _ {0} - x _ {0} ^ {\prime} \|, a. s., \\ \end{array}
$$

and assumithat for any $c ( \cdot )$ pschitz co and any s, we have, $\begin{array} { r } { b \in \left[ 0 , \frac { \left( \gamma - 2 \right) ^ { 2 } } { 4 \gamma \left( \gamma + 2 \right) } \right) } \end{array}$ $x _ { 0 } \in \mathbb { R } ^ { n }$

$$
\begin{array}{l} \frac {1}{t} \sum_ {\tau = 0} ^ {t} | c (x _ {t}, u _ {t}) - c (x _ {t} ^ {*}, u _ {t} ^ {*}) | ^ {2} \\ = O \left(t ^ {- 2 b} + t ^ {\frac {(2 b + \eta - 1) (\gamma - 2)}{\gamma}} (\log t) ^ {\frac {(1 + \delta) (\gamma - 2)}{\gamma}}\right), a. s., \\ \end{array}
$$

where $\delta , \gamma ,$ and η are as in Theorem $\it 4 .$
