# 3.2 Main Results for Direct Data-Driven Control

In order to address the approximation error $r ( z , u )$ , the following proposition should be introduced.

Proposition 1 Consider the lifted bilinear system via the Koopman operator with no disturbance, i.e.,

$$\frac {\mathrm{d} z}{\mathrm{d} t} = A z + B _ {0} u + \sum_ {i = 1} ^ {m} u _ {i} B _ {i} z + r (z, u) \tag {28}$$

where the overall approximation error satisfies Lemma 1. The lifted-state feedback controller $u = \dot { K z } = L ^ { \top } P ^ { - 1 } z$ achieves exponential stability of (7) without disturbance with guaranteed region of attraction ${ \mathbb X } _ { R o A } = \{ x \in { \mathbb X } :$ $z ^ { \top } P ^ {  } { } _ { z } ~ = ~ \Psi ( x ) ^ { \top } P ^ { - 1 } \bar { \Psi } ( x ) ~ \le ~ c \}$ , if there exist $L \in$ $\mathbb { R } ^ { N \times m } , 0 \prec P \overset { \cdot } { = } P ^ { \intercal } \in \mathbb { R } ^ { N \times N }$ $\begin{array} { r } { \frac { 1 } { \alpha } + \frac { 1 } { \beta } = 1 } \end{array}$ , su ch that and scalars $\alpha , \beta , \nu , \tau$ with

$$P A ^ {\top} + B _ {0} L ^ {\top} + A P + L B _ {0} ^ {\top} + \nu \sum_ {i = 1} ^ {m} B _ {i} P B _ {i} ^ {\top} \tag {29}+ \nu^ {- 1} c L L ^ {\top} + \tau^ {- 1} (\alpha c _ {1} ^ {2} P ^ {2} + \beta c _ {2} ^ {2} L L ^ {\top}) + \tau I _ {N} \prec 0.$$

In this paper, Proposition 1 serves as a closed-loop stability criterion under the effect of approximation error. The proof is provided in Appendix A for completeness. Since the controller ensures exponential stability of (7) in disturbance-free scenarios $( d = 0 )$ , it consequently induces input-to-state stability (ISS) with respect to bounded disturbances, as per [13, Lemma 9.2].

Notably, Proposition 1 does not require a priori fixed set for the additional uncertainty variable $\Delta _ { \Phi }$ as introduced in [29], leaving only a single matrix inequality (29) to solve that can be converted to an LMI. Parameters $\alpha , \beta$ can be flexibly adjusted via error coefficients $c _ { 1 } , c _ { 2 }$ . Particularly, setting $\alpha = \beta = 2 , c = 1$ reduces to the case [29, Theorem 6]. Thus, Proposition 1 provides new insights while simplifying controller design.

A core idea in this paper is that three types of uncertainties should be addressed via distinct methods based on their inherent properties. Following [29], estimation error can be bounded by ∥z∥ and ∥u∥. Projection error, expressed via (9) and (17) as

$$r _ {p} (z, u) = \mathcal {L} _ {f} z - A z + \sum_ {i = 1} ^ {m} u _ {i} \left(\mathcal {L} _ {g _ {i}} z - B _ {0, i} - B _ {i} z\right), \tag {30}$$
