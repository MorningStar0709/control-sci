Replacing (8) from the lemma statement in (10) it is evident that $\tilde { \zeta } _ { k } ( \tilde { { \boldsymbol { x } } } ( t ) ) \geq 0 \forall t \in [ k \Delta t , \tau _ { 0 } ]$ which by [15, Thm.
5] ensures that $\tilde { \mathbf { \Gamma } } ( t ) \in \cap _ { r - 1 } ^ { 0 } \bar { \mathcal { C } } _ { S _ { i } } ( t ) \forall t \bar { \mathbf { \Gamma } } \in [ k \Delta t , \tau _ { 0 } ]$ .
We will now prove that $\tilde { \mathbf { x } } ( t ) \in \cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) \forall t \in [ k \Delta t , \tau ]$ by contradiction.
Suppose instead that for some $\begin{array} { r c l } { \tau _ { a } } & { \in } & { \left( \tau _ { 0 } , \tau \right] , \tilde { \pmb { x } } \left( \tau _ { a } \right) } \in  \end{array}$ $\mathbb { D } \backslash \cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t )$ and $\tilde { \pmb { x } } ( t ) \in \mathbb { D }$ for all $t ~ \in ~ [ k \Delta t , \tau _ { a } ] ~ ( \mathrm { i .
e .
} .$ , the solution has left $\cap _ { r - 1 } ^ { 0 } { \mathcal { C } } _ { S _ { i } } ( t )$ , but not D).
Then ${ \tilde { { \mathbf { x } } } } ( t )$ must leave $\cap _ { r - 1 } ^ { 0 } { \mathcal { C } } _ { S _ { i } } ( t )$ at some $t < \tau _ { a }$ .
Furthermore, since the closed-loop dynamics are differentiable on D, x˜(t) is uniquely defined on $[ k \Delta t , \tau _ { a } ]$ (this is shown by repeatedly applying [16, Thm.
54], since ${ \tilde { { \mathbf { x } } } } ( t )$ remains in D over which local differentiability of the closed-loop dynamics holds).
To leave $\cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) , \ \dot { H } _ { r - 1 } ( \tilde { { \pmb x } } , t ) < 0$ must hold on $\partial ( \cap _ { r - 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) )$ .
The maximum negative variation of $\tilde { \zeta } _ { k } ( \tilde { { \pmb x } } ( t ) )$ is then recomputed over the interval $[ k \Delta t , \tau _ { a } ]$ and is again obtained that $\tilde { \zeta } _ { k } ( \tilde { { \pmb x } } ( t ) ) \geq 0 \forall t \in [ k \Delta t , \tau _ { a } ]$ as $L _ { w }$ and $c _ { w }$ are independent of $\tau _ { a }$ and $\tau _ { 0 }$ .
Therefore we see that $\dot { H } _ { r - 1 } ( \tilde { { \mathbf x } } , t ) \geq$ 0 holds for any $\tilde { \mathbf { x } } ( t ) ~ \in ~ \cap _ { r = 1 } ^ { 0 } \mathcal { C } _ { S _ { i } } ( t ) , t ~ \in ~ [ k \Delta t , \tau _ { a } ]$ such that $\tau _ { a } \le ( k + 1 ) \Delta t$ .
Hence, the contradiction is reached, and so ${ \tilde { { \mathbf { x } } } } ( t )$ can never leave $\cap _ { r - 1 } ^ { 0 } { \mathcal { C } } _ { S _ { i } } ( t )$ (and thus D) on $t \in [ k \Delta t , \tau ]$ with $\tau \leq ( k + 1 ) \Delta t$ .
