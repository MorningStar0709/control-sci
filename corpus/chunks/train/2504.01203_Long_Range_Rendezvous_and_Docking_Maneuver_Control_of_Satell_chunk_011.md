# IV. DESIGN OF CROSS-FEEDBACK SLIDING MODE CONTROLLER

In this section, we have implemented a cross-feedback sliding mode controller (CxSMC) to track the trajectory for the chaser satellite in the three-dimensional configuration space SE(3). Stability of the controller is established using a Lyapunov framework.

Lemma 1: For a nonlinear system expressed as $\dot { \tilde { y } } = f \left( \tilde { y } \right)$ , $\widetilde { y } \left( 0 \right) = \widetilde { y } _ { 0 } , \widetilde { y } \epsilon \mathbb { R } ^ { n }$ , if there exist a continuously differentiable Lyapunov function V(x) in the neighborhood $N \subset \mathbb { R } ^ { n }$ of origin, which satisfies $\begin{array} { r l } { \dot { V } \left( \tilde { y } \right) ~ \leq } & { { } - k V \left( \tilde { y } \right) } \end{array}$ where $k \ > \ 0 .$ . Then the origin of the system is asymptotically stable and the settling time T(y) is dependent on initial states as shown below.

$$T (y) \leq T _ {m a x} = \frac {\ln (V)}{k}$$
