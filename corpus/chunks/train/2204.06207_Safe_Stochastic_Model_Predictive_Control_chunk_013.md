# A. Recursive Feasibility

Based on Definition 3, we first prove recursive feasibility of the OCP of the safe SMPC algorithm described in Section III-A.

Theorem 1 (Recursive Feasibility): Let Assumptions 1 and 2 hold and let the system input $\mathbf { \Delta } \mathbf { u } _ { t }$ be determined based on the proposed safe SMPC algorithm in Section III-A. Then, for an admissible ${ \mathbf { } } ^ { \mathbf { } } \mathbf { 0 } ;$ , obtaining a solution $\mathbf { \Delta } \mathbf { u } _ { t }$ is feasible for all $t > 0$ .

Proof: The proof is based on showing that at any time step t it is guaranteed that an admissible input $\mathbf { \Delta } \mathbf { u } _ { t }$ is applied at t and an admissible input ${ \pmb u } _ { t ^ { + } }$ may be obtained at the next time step $t ^ { + } = t + 1$ .

If $U _ { t } ^ { \mathrm { s } * } \neq \emptyset$ and $\overline { { \pmb { x } } } _ { t + 1 } ^ { \mathrm { s } } \in \pmb { \mathcal { X } } _ { 0 } \ominus \mathcal { W } , \pmb { u } _ { t } = \pmb { u } ^ { \mathrm { s } } ( \pmb { x } _ { t } )$ exists and $\pmb { x } _ { t + 1 } ^ { \mathrm { s } } \in \mathcal { X } _ { 0 }$ xst+1 , which guarantees that a solutio n U b∗ $U _ { t + 1 } ^ { \mathrm { b } * }$ 1 exists for $\mathbb { P } ^ { \mathsf { b } } ( { \pmb x } _ { t + 1 } )$ .

In the backup mode, $U _ { t } ^ { \mathrm { b } * }$ exists for $\mathbb { P } ^ { \mathrm { b } } ( \pmb { x } _ { t } )$ as $\mathbf { \boldsymbol { x } } _ { t } ~ \in ~ \mathcal { X } _ { 0 }$ and Assumption 2 ensures that $\mathbb { P } ^ { \mathrm { b } } ( \pmb { x } _ { t + 1 } )$ remains feasible.

Hence, all possibilities are covered. This holds for all $t \in$ N, i.e., admissible inputs are guaranteed at subsequent time steps, which concludes the proof.

Note that no terminal constraint is necessary for the SMPC OCP to ensure recursive feasibility of the overall algorithm. Based on guaranteed recursive feasibility of the safe SMPC algorithm, safety and stability are now discussed.
