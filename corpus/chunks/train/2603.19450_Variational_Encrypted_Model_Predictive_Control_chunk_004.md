# B. Notation

The sets of real, natural, integers and complex numbers are denoted by R, N, Z and C, respectively. For a vector x ∈ Cn, ∥x∥ denotes the infinity norm. The Hadamard product (element-wise multiplication) is denoted by ⊙. We denote by RotVecρ(x) the cyclic upward shift of x by ρ positions (e.g.,

RotVec $( [ 1 , 2 , 3 , 4 ] ^ { \top } ) = [ 2 , 3 , 4 , 1 ] ^ { \top } )$ . For scalars or vectors $v _ { 1 } , \ldots , v _ { n } \ , \ [ v _ { 1 } ; \cdot \cdot \cdot \ ; v _ { n } ] : = [ v _ { 1 } ^ { \top } , \ldots , v _ { n } ^ { \top } ] ^ { \top }$ . Boldface symbols denote encrypted quantities. Lastly, $[ \cdot ] _ { + } : = \operatorname* { m a x } \{ \cdot , 0 \}$ .
