# A. Properties of the HMPC formulation (9)

In this subsection we formally establish the convergence, stability and recursive feasibility guarantees of the HMPC formulation (9). We start by presenting a key concept of the formulation that we label the optimal reachable harmonic reference, which plays a mayor role in the convergence results.

Definition 3 (Optimal reachable harmonic reference). At sample time t, we define the optimal reachable harmonic reference sequence of the HMPC formulation (9) for the given reference $( x _ { r } ( t ) , u _ { r } ( t ) )$ ) as the harmonic sequences $\{ \mathring { x } ^ { k } \} , \ \{ \mathring { u } ^ { k } \}$ parameterized by the unique solution $\mathring \mathbf { x } ( t ) = ( \mathring { x } _ { e } ( t ) , \mathring { x } _ { s } ( t ) , \mathring { x } _ { c } ( t ) )$ and $\mathring \mathbf { u } ( t ) = \big ( \mathring { u } _ { e } ( t ) , \mathring { u } _ { s } ( t ) , \mathring { u } _ { c } ( t ) \big )$ of

$$(\mathring {\mathbf {x}} (t), \mathring {\mathbf {u}} (t)) = \arg \min _ {\mathbf {x} _ {h}, \mathbf {u} _ {h}} V _ {h} (\mathbf {x} _ {h}, \mathbf {u} _ {h}, \mathbf {x} _ {r} (t), \mathbf {u} _ {r} (t)) \tag {10a}\text { s.t. } \left(\mathbf {x} _ {h}, \mathbf {u} _ {h}\right) \in \mathcal {D} \cap \mathcal {C} _ {\sigma}. \tag {10b}$$

The following lemma establishes the relation between the parameters characterizing the optimal reachable harmonic sequences of two consecutive time instants. Its proof can be found in the appendix.

Assume that $\mathbf { x } _ { r } ( t + 1 ) \ = \ T _ { h } ^ { n _ { x } } \mathbf { x } _ { r } ( t )$ and $\mathbf { u } _ { r } ( t + 1 ) \ =$ $\mathcal { T } _ { h } ^ { n _ { u } } \mathbf { u } _ { r } ( t ) . \ T h e n , \mathring { \mathbf { x } } ( t + 1 ) = \mathcal { T } _ { h } ^ { n _ { x } } \mathring { \mathbf { x } } ( t ) , \mathring { \mathbf { u } } ( t + 1 ) = \mathcal { T } _ { h } ^ { n _ { u } } \mathring { \mathbf { u } } ( t ) , \forall t .$

The main consequence of Lemma 1 is that the optimal reachable harmonic reference is in fact a unique trajectory $( \mathring { x } ( \cdot ) , \mathring { u } ( \cdot ) )$ for each harmonic reference trajectory $( x _ { r } ( \cdot ) , u _ { r } ( \cdot ) )$ . We formalize this in the following corollary.

Corollary 2. The optimal reachable harmonic reference sequences obtained at subsequent times t define a unique trajectory $( \mathring { x } ( \cdot ) , \mathring { u } ( \cdot ) )$ given by
