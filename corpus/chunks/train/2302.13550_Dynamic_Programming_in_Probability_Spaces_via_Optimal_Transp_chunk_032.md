Let $\bar { X } : = X _ { 1 } \times . . . \times X _ { k - 1 } , \bar { Z } : = Z _ { 1 } \times . . . \times Z _ { k - 1 }$ , and $\bar { l } : = l _ { 1 } \times \ldots \times l _ { k - 1 }$ . Then, for the $k ^ { \mathrm { t h } }$ argument of $c ,$

$$
\begin{array}{l} \int_ {X \times Y} (c \circ (l \times \mathrm{id} _ {Y})) (x, y) \mathrm{d} \boldsymbol {\mu} (x, y) \\ = \int_ {X \times Z \times Y} (c \circ (l \times \mathrm{id} _ {Y})) (x, y) \mathrm{d} \boldsymbol {\mu} ^ {*} (x, z, y) \\ \stackrel {{\clubsuit}} {{=}} \int_ {X _ {k} \times Z _ {k}} \int_ {\bar {X} \times \bar {Z} \times Y} (c \circ (l \times \mathrm{id} _ {Y})) (\bar {x}, x _ {k}, y) \mathrm{d} \tilde {\boldsymbol {\mu}} ^ {\boldsymbol {x} _ {k} \boldsymbol {z} _ {k}} (\bar {x}, \bar {z}, y) \mathrm{d} \boldsymbol {\mu} _ {\boldsymbol {k}} (x _ {k}, z _ {k}) \\ = \int_ {X _ {k} \times Z _ {k}} \int_ {\bar {X} \times \bar {Z} \times Y} c (\bar {l} (\bar {x}), l _ {k} (x _ {k}), y) d \tilde {\boldsymbol {\mu}} ^ {\boldsymbol {x} _ {k} \boldsymbol {z} _ {k}} (\bar {x}, \bar {z}, y) d \boldsymbol {\mu} _ {\boldsymbol {k}} (x _ {k}, z _ {k}) \\ \end{array}
\stackrel {\spadesuit} {=} \int_ {X _ {k} \times Z _ {k}} \int_ {\bar {X} \times \bar {Z} \times Y} c (\bar {l} (\bar {x}), z _ {k}, y) \mathrm{d} \tilde {\boldsymbol {\mu}} ^ {\boldsymbol {x} _ {k} \boldsymbol {z} _ {k}} (\bar {x}, \bar {z}, y) \mathrm{d} \boldsymbol {\mu} _ {\boldsymbol {k}} (x _ {k}, z _ {k})\stackrel {\clubsuit} {=} \int_ {X \times Z \times Y} c (\bar {l} (\bar {x}), z _ {k}, y) \mathrm{d} \boldsymbol {\mu} ^ {*} (x, z, y),$$

where in $\clubsuit$ we used the disintegration theorem (see [5, Theorem 5.3.1]), which provides us a collection $\{ \tilde { \mu } ^ { x _ { k } z _ { k } } \} _ { ( x _ { k } , z _ { k } ) \in X _ { k } \times Z _ { k } }$ to complement $\mu _ { k }$ . Then, in ♠, we used the definition of ${ \mu } _ { k } \colon \ z _ { k } = l _ { k } ( x _ { k } ) \ \mu _ { k } \mathrm { - ~ a . e . }$ .. Repeating the same steps for the other arguments of $c ,$ we obtain

$$
\begin{array}{l} \mathcal {J} [ c \circ (l \times \mathrm{id} _ {Y}) ] (\mu) \leq \int_ {X \times Y} c (l (x), y) \mathrm{d} \boldsymbol {\mu} (x, y) \\ = \int_ {X \times Z \times Y} c (z, y) \mathrm{d} \boldsymbol {\mu} ^ {*} (x, z, y) = \int_ {Z \times Y} c \mathrm{d} \boldsymbol {\mu} ^ {\prime}. \\ \end{array}
$$

Since $\mu ^ { \prime }$ is arbitrary, it follows that $\mathcal { I } [ c \circ ( l \times \mathrm { i d } _ { Y } ) ] ( \mu ) \leq \mathcal { I } [ c ] ( l _ { \# } \mu )$ .

![](image/33d9dd9c3b11b89aeaaff8b9e502292925f08618783081d84d60579fed699940.jpg)
