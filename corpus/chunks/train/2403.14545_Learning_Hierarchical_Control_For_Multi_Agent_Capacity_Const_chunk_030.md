corresponding to the inputs applied by the low-level policy (33) at iteration (r − 1) satisfy input and state constraints (33c), (33d), (33f), (33h).

To verify this, we first note that it follows from (4) and $g _ { c } \geq 0$ that $\begin{array} { r } { c _ { \mathbb { Z } ( \mathbf x ^ { r - 1 } , i , j , - 1 ) } ^ { r - 1 } ~ = ~ \operatorname* { m a x } _ { t \in \mathbb { Z } ( \mathbf x ^ { r - 1 } , i , j ) } c _ { t } ^ { r - 1 } } \end{array}$ r−1 . By definition (21) we have that

$$\hat {\theta} _ {i, j, l} ^ {r} = \Omega_ {l} (\mathbf {x} ^ {r - 1}, i, j) = \tilde {c} _ {\mathcal {I} (\mathbf {x} ^ {r - 1}, i, j, - 1), l} ^ {r - 1}.$$

Therefore, c˜r−1I(xr−1,i,j) $\tilde { \mathbf { c } } _ { \mathcal { T } ( \mathbf { x } ^ { r - 1 } , i , j ) } ^ { r - 1 } \in \mathcal { C }$ implies that $\tilde { \mathbf { c } } _ { t r : t r + N } ^ { r - 1 , i , j } + c _ { t 0 } ^ { r } \in \mathcal { C }$ for any $c _ { t 0 , l } ^ { r } \in [ 0 , C _ { l } - \widehat \theta _ { i , j , l } ^ { r } ]$ . It follows with the additional (4), (5) that (35) results from applying the feasible actuation inputs in (36), and (33c) is satisfied. Thus, (35), (36) is a feasible solution for (33) at time t = 0 of iteration r.

Assume that at time $t \in \mathcal { T } ( \mathbf { x } ^ { r } , i , j )$ of iteration r, the optimal control problem (33) is feasible, and let $\mathbf { x } _ { t : t + N | t } ^ { r , \star }$ and $\mathbf { u } _ { t : t + N - 1 | t } ^ { r , \star }$ be the corresponding optimal trajectory and input sequence. The terminal constraints (33f), (33h) dictate that $\exists t ^ { \star } \in \mathcal { T } ( \mathbf { x } ^ { r - 1 } , i , j )$ such that

$$\chi_ {t + N | t} ^ {r, \star} = \chi_ {t ^ {\star}} ^ {r - 1}, c _ {t + N | t} ^ {r, \star} - c _ {t 0} ^ {r} \leq \tilde {c} _ {t ^ {\star}} ^ {r - 1}$$

where $( \tilde { c } _ { t ^ { \star } } ^ { r - 1 } , \chi _ { t ^ { \star } } ^ { r - 1 } ) \in \mathcal { S } _ { i , j } ^ { L , r }$ . As the state update in (62f) and (33b) are assumed identical we have

$$x _ {t + 1} ^ {r} = x _ {t + 1 | t} ^ {r, \star}.$$

At time t + 1 of iteration r, the input sequence

$$\left[ u _ {t + 1 \mid t} ^ {r, \star}, u _ {t + 2 \mid t} ^ {r, \star}, \dots , u _ {t + N \mid t} ^ {r, \star}, u _ {t ^ {\star}} ^ {r - 1} \right] \tag {37}$$

results in the non-capacity and capacity state trajectories
