Inequality (42) and definition $\left\| d \right\| = \operatorname* { s u p } _ { 0 \leq t \leq t _ { \operatorname* { m a x } } } \left( \left| d ( t ) \right| \right)$ as well as the fact that $c$ is non-decreasing, give max0 t t  the following differential inequality for $t \in \left[ 0 , { t _ { \operatorname* { m a x } } } \right)$ a.e.:

$$\frac {d}{d t} (H (x (t))) \leq c (\| d \|) H (x (t)) \tag {44}$$

Integrating (44), we obtain that

$$H (x (t)) \leq \exp \left(c (\| d \|) t\right) H \left(x _ {0}\right), \text { for all } t \in [ 0, t _ {\max}) \tag {45}$$

which implies that

$$H (x (t)) \leq \exp \left(c \left(\| d \|\right) t _ {\max}\right) H \left(x _ {0}\right), \text { for all } t \in [ 0, t _ {\max}) \tag {46}$$

Let $r = \exp \Bigl ( c \bigl ( \bigl \| d \bigr \| \Bigr ) t _ { \mathrm { m a x } } \Bigr ) H ( x _ { 0 } ) + \bigl \| d \bigr \|$ and notice that definitions $\left\| d \right\| = \operatorname* { s u p } _ { 0 \leq t \leq t _ { \operatorname* { m a x } } } \left( \left| d ( t ) \right| \right)$ 0≤t≤tmax , (7), (43) and estimate (46) imply that $( \boldsymbol { x } ( t ) , \boldsymbol { d } ( t ) ) \in S _ { r } \times \boldsymbol { D } _ { r }$ for $t \in [ 0 , t _ { \operatorname* { m a x } } )$ a.e.. Since by assumption, $f ( S _ { r } \times D _ { r } )$ is bounded, it follows that there exists a constant $M > 0$ , such that

$$\mid \dot {x} (t) \mid \leq M, \text { for } t \in [ 0, t _ {\max}) \text { a.e. } \tag {47}$$

Inequality (47) implies that

$$\left| x (t) - x _ {0} \right| \leq M t, \text { for all } t \in [ 0, t _ {\max})$$

and consequently (using the triangle inequality)

$$\left| x (t) \right| \leq \left| x _ {0} \right| + M t _ {\max}, \text { for all } t \in [ 0, t _ {\max}) \tag {48}$$

Therefore, estimate (48) shows that $\operatorname* { l i m } _ { t \to t _ { \mathrm { m a x } } ^ { - } } { \Big ( } { \big | } x ( t ) { \big | } { \Big ) } = + \infty$ cannot hold. Consequently, $t _ { \mathrm { m a x } } = + \infty$ t→tmax The proof is complete.
