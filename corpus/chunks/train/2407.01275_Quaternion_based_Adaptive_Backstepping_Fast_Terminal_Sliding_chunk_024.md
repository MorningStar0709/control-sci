$$\dot {s} = \ddot {e} + \gamma_ {1} \dot {e} + \frac {n}{l} \gamma_ {2} e ^ {(n / l - 1)} \dot {e} + \dot {\Omega} _ {e} \tag {34}$$

Based on (34), it is obvious that when e becomes zero, $e ^ { ( \frac { n } { l } - 1 ) }$ could converge to infinity. In such case, the system could be subject to singularity and thereby become unstable. In view of [33], [46], to resolve this issue, the sliding surface described in (33) can be modified by switching threshold value to overcome the singularity issue as follows:

$$s = \dot {e} + \gamma_ {1} e + \gamma_ {2} \Delta (e) + \gamma_ {3} \Omega_ {e} \tag {35}$$

where $\Delta ( e )$ is defined as:

$$
\Delta (e) = \left\{ \begin{array}{l l} e ^ {\frac {n}{l}}, & \text { if   } s = 0 \text {   or   } s \neq 0, | e | > \epsilon \\ e, & \text { if   } s \neq 0, | e | \leqslant \epsilon \end{array} \right. \tag {36}
$$

with ϵ being a small positive value. Based on (35) and (36), let us propose the attitude controller as follows:

$$
\begin{array}{l} \mathcal {T} = - \left(\left[ J \Omega_ {B} \right] _ {\times} \Omega_ {B} + J \left[ \Omega_ {e} \right] _ {\times} R _ {e} \Omega_ {d} - J R _ {e} \dot {\Omega} _ {d}\right) \\ - \left(\frac {1}{2} e _ {0} + \frac {1}{2} [ e ] _ {\times} \Omega_ {e} + \mathcal {I}\right) ^ {- 1} \left(\frac {1}{2} \dot {e} _ {0} \Omega_ {e} + \frac {1}{2} [ \dot {e} ] _ {\times} \Omega_ {e} \right. \tag {37} \\ + \gamma_ {1} \dot {e} + \gamma_ {2} (n / l) e ^ {n / l - 1} \dot {e} + \mu_ {1} s + \hat {\boldsymbol {K}} s i g n (s)) \\ \end{array}
$$

where $\mu _ { 1 }$ is a positive constant and the adaptive law for $\hat { K } { = } [ \hat { k } _ { 1 } , \hat { k } _ { 2 } , \hat { k } _ { 3 } ] ^ { \top }$ is as follows:

$$
\text { Update   Law } \left\{ \begin{array}{l l} \dot {\hat {k}} _ {1} & = \lambda | | s | | \\ \dot {\hat {k}} _ {2} & = \lambda | | s | | \\ \dot {\hat {k}} _ {3} & = \lambda | | s | | \end{array} \right. \tag {38}
$$

where $\lambda > 0 .$ .

Theorem 2. The attitude system (5) becomes asymptotically stable and the attitude error $Q _ { e } = [ e _ { 0 } , e ^ { \top } ] ^ { \top }  [ 1 , 0 , 0 , 0 ] ^ { \top }$ and $\Omega _ { e }  0$ when the proposed input controller is defined as (37) with the adaptation laws in (38).

Proof: The Lyapunov approach and Lemma 1 are used to prove Theorem 2. Let us introduce the following Lyapunov function candidate:

$$V _ {s} = \frac {1}{2} s ^ {\top} s + \frac {1}{2 \lambda} \tilde {\boldsymbol {K}} ^ {\top} \tilde {\boldsymbol {K}} \tag {39}$$

where $\tilde { K } = \hat { K } - K$ . The derivative of (39) is obtained by:
