Consider a discretized particle with 10 evenly spaced radial points indexed by $i \in [ 1 , 1 0 ]$ , where $i = 1$ represents the center of the particle and $i = 1 0$ is the surface of the particle. The spacing between these points is denoted as $\Delta r$ , representing the radial step size. By using central difference approximation and Euler’s forward method, we derive a high-fidelity adversarial MPC in the form of (1), where the parameters $\theta _ { H } \ : = \ \mathbf { c o l } ( q _ { H } , r _ { H } ) \ \in \ \mathbb { R } ^ { 2 }$ are defined for the cost function, $( \mathbf { c } _ { s , 1 } ^ { - } ( k | t ) , \cdot \cdot \cdot , \mathbf { c } _ { s , 1 0 } ^ { - } ( k | t ) , \mathbf { c } _ { s , 1 } ^ { + } ( k | t ) , \cdot \cdot \cdot$ , $\mathbf { c } _ { s , 1 0 } ^ { + } ( k | t ) ) : = x _ { H , k | t } \in \mathbb { R } ^ { 2 0 }$ is the high-fidelity system state at time step t, and prediction step k, $\begin{array} { r } { \Delta I _ { k | t } = I _ { k | t } - I _ { k - 1 | t } : = } \end{array}$ $u _ { k | t } \in \mathbb { R }$ is the incremental current as the control input, $g _ { H } : \mathbb { R } ^ { 2 0 } \times \mathbb { R } \times \mathbb { R } ^ { 2 } \to$ R is the instantaneous cost function, and $g _ { H , N } : \mathbb { R } ^ { 2 0 } \times \mathbb { R } ^ { 2 } \to \mathbb { R }$ is the terminal cost function, which are defined as

$$
\begin{array}{l} g _ {H} (x _ {H, k | t}, u _ {k | t}) = \left\| \frac {3 \sum_ {i = 1} ^ {1 0} w _ {i} r _ {i} ^ {2} x _ {H , k | t} [ i ]}{R _ {s} ^ {3} c _ {s , \max} ^ {-}} - S o C _ {\mathrm{d}} \right\| _ {q _ {H}} ^ {2} \\ + \left\| u _ {k | t} \right\| _ {r _ {H}} ^ {2} \tag {5a} \\ \end{array}
g _ {H, N} \left(x _ {H, N \mid t}\right) = \left\| \frac {3 \sum_ {i = 1} ^ {1 0} w _ {i} r _ {i} ^ {2} x _ {H , N \mid t} [ i ]}{R _ {s} ^ {3} c _ {s , \max} ^ {-}} - S o C _ {\mathrm{d}} \right\| _ {q _ {H}} ^ {2}, \tag {5b}$$

where $w _ { i }$ is the weight for the i-th node, $\boldsymbol { w } _ { 1 } = \frac { 1 } { 2 } , \boldsymbol { w } _ { 2 } = 1$ , $\dots , w _ { 9 } = 1 , x _ { H , k | t } [ i ]$ is the i-th element of vector $x _ { H , k | t }$ , $S o C _ { \mathrm { d } }$ is the desired SoC target, $w _ { 1 0 } = \textstyle { \frac { 1 } { 2 } } , r _ { i }$ represents the radial position of the i-the node within the particle $( r _ { 1 } = 0$ , and $r _ { N } = R _ { s } )$ .

$f _ { H , k | t } : \mathbb { R } ^ { 2 0 } \times \mathbb { R } \to \mathbb { R } ^ { 2 0 }$ denotes the dynamics with initial condition $\begin{array} { r } { \frac { 3 \sum _ { i = 1 } ^ { 1 0 } w _ { i } r _ { i } ^ { 2 } x _ { H , 0 | 0 } [ i ] } { R _ { s } ^ { 3 } c _ { s , \mathrm { m a x } } ^ { - } } = S o C _ { \mathrm { b u l k , i n i t } } . } \end{array}$ , which is defined as
