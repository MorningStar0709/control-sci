Proof. Consider a “sub-problem” of (B.6), which finds the largest open ball satisfying the constraints of (B.6) when the ball center is fixed to $x \in Z \colon$ :

$$\mathcal {R} _ {x} (A, B) \doteq \sup _ {r \geq 0} r \tag {B.7}\text { s.t. } \mathcal {B} _ {r} (x) \cap B = \emptyset , \quad \mathcal {B} _ {r} (x) \subseteq A$$

Compare the value of $\mathcal { R } _ { x } ( X , Y )$ and $\mathcal { R } _ { x } ( X \cap Z , Y \cap Z )$ for any fixed $x \in Z .$ . Note that the feasible set of (B.7) when $A = X \cap Z$ and $B = Y \cap Z$ is contained within the feasible set of (B.7) when $A = X$ and $B = Y \colon$ : any ball which is contained in $Y \cap Z$ and does not intersect X $\cap Z$ is also contained in Y , and does not intersect X. Thus, $\mathcal { R } _ { x } ( X \cap Z , Y \cap Z ) \leq \mathcal { R } _ { x } ( X , Z )$ .

Now, consider the original problem (B.6), which can be rewritten as $\mathcal { R } ( A , B ) =$ $\operatorname { s u p } _ { x \in B } \mathcal { R } _ { x } ( A , B )$ . We have that $\mathcal { R } ( X , Y ) \geq \mathcal { R } ( X \cap Z , Y \cap Z )$ , since Y is a superset of $Y \cap Z$ and thus the feasible set when $B = Y$ is larger than when $B = Y \cap Z$ .

![](image/0a0b129ab286e75ba57325ad86f9414727636112f0f6defa3347d53dfe85138f.jpg)

As an abuse of notation, we refer to R without any arguments as shorthand for $\mathcal { R } ( S _ { x \theta } , D _ { r } \times D _ { \theta } )$ . Let $\begin{array} { r } { \eta ^ { * } ( t ) \ = \ \operatorname* { m i n } _ { \eta \ge 0 , S _ { x \theta } \cap \left( \varOmega _ { c } ( t ) \times \mathcal { B } _ { \eta } ( \theta ) \right) \neq \emptyset } \eta } \end{array}$ be the radius of the smallest ball around $\theta , B _ { \eta ^ { * } ( t ) } ( \theta )$ , such that $( \Omega _ { c } \times \mathcal { B } _ { \eta ^ { * } ( t ) } ( \theta ) ) \cap ( D _ { r } \times D _ { \theta } ) \ \doteq \ \varOmega _ { c } ^ { B }$ contains some datapoint. Then, we write our third error bound $\bar { \epsilon } _ { 3 } ( x ^ { * } , \theta )$ :

Theorem B.2 $( \bar { \epsilon } _ { 3 } ( x ^ { * } , \theta ) )$ . An upper bound on the perception error $\epsilon ( x , \theta )$ can be written based on buffering the local training errors:

$$\epsilon (x, \theta) \leq L _ {p} \mathcal {R} + \max _ {(x _ {i}, \theta_ {i}) \in \mathcal {S} _ {x \theta} \cap \Omega_ {c} ^ {B}} \epsilon (x _ {i}, \theta_ {i}) \doteq \bar {\epsilon} _ {3} (x ^ {*}, \theta) \tag {B.8}$$

Proof. Finally, with an abuse of notation, denote $L _ { p } ( A )$ as the local Lipschitz constant of $\epsilon ( x , \theta )$ in x and θ, valid for all $( x , \theta ) \in A$ .
