# B. Proof of Theorem 2

The proof of the theorem requires some preliminary results. We first establish a one-to-one correspondence between $\mathbb { R } ^ { q L }$ and the subspace of Rq-valued sequences with finitely many non-zero elements, as well as additional elementary results (omitting the proof of those that may be established by direct computation).

Let $( \mathbb { R } ^ { q } ) ^ { \infty }$ be the space of $\mathbb { R } ^ { q } .$ -valued sequences with finitely many non-zero elements, i.e., $\begin{array} { l c l } { ( \mathbb { R } ^ { q } ) ^ { \infty } } & { = } & { \{ w } \in { }  \end{array}$ $( \mathbb { R } ^ { q } ) ^ { \mathbb { Z } _ { + } } : \exists T \in \mathbb { Z } _ { + }$ s.t. $w _ { t } = 0 , \forall t \ \geq \ T \}$ . For $L \in \mathbb { N } .$ , let $\iota _ { L } : \mathbb { R } ^ { q L } \to ( \mathbb { R } ^ { q } ) ^ { \infty }$ be the inclusion map, defined as $\iota _ { L } ( w ) = ( w _ { 1 } , \ldots , w _ { L } , 0 , 0 , \ldots )$ . Then we have $( \mathbb { R } ^ { q } ) ^ { \infty } =$ $\textstyle \bigcup _ { L = 0 } ^ { \infty } \iota _ { L } ( \mathbb { R } ^ { q L } )$ . Thus, each image $\iota _ { L } \big ( \mathbb { R } ^ { q L } \big )$ may be identified with $\mathbb { R } ^ { q L }$ by identifying each $( w _ { 1 } , \dots , w _ { L } , 0 , 0 , \dots ) \in$ $( \mathbb { R } ^ { q } ) ^ { \infty }$ with $w = \operatorname { c o l } \left( w _ { 1 } , \dots , w _ { L } \right) \in \mathbb { R } ^ { q L }$ . Furthermore, the subspace topology on $\iota _ { L } \big ( \mathbb { R } ^ { q L } \big )$ , the quotient topology induced by the map $\iota _ { L } ,$ and the Euclidean topology on $\mathbb { R } ^ { q L }$ , all coincide.

Lemma 4. Let $w \in \mathbb { R } ^ { q L }$ . Then $\iota _ { L } ( w ) \in \ell _ { 2 } ^ { q }$ and $\lVert \boldsymbol { w } \rVert _ { 2 } = \lVert \iota _ { L } ( \boldsymbol { w } ) \rVert _ { \ell _ { 2 } } .$

Lemma 5. Let  be a subset of a topological space and let $f$ : $s $ R be a continuous function. Assume $S _ { 0 } \subseteq S _ { 1 } \subseteq . . . \mathrm { i }$ s a sequence of subsets of S such that $\textstyle S \subseteq \operatorname { c l } \left( \bigcup _ { L = 0 } ^ { \infty } S _ { L } \right)$ then $\mathrm { s u p } _ { S } f = \mathrm { l i m } _ { L  \infty } \mathrm { s u p } _ { S _ { L } } f ,$ whenever the limit exists.
