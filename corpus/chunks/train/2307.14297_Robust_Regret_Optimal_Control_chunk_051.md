Lemma 4. Let $( \hat { A } , \hat { B } , \hat { C } , \hat { D } )$ be given and define $\hat { Q } : = \hat { C } ^ { \top } \hat { C } , \hat { S } : = \hat { C } ^ { \top } \hat { D } ,$ and $\hat { R } = \hat { D } ^ { \top } \hat { D }$ . Assume: $( i ) \hat { R } \succ 0 , ( i i ) ( \hat { A } , \hat { B } )$ is stabilizable, (iii) $\hat { A } - \hat { B } \hat { R } ^ { - 1 } \hat { S } ^ { \top }$ is nonsingular, $( i \nu ) \left[ \begin{array} { c } { { \hat { A } - e ^ { j \theta } I \ \hat { B } } } \\ { { \hat { C } \ \hat { } \hat { } } } \end{array} \right]$ has full column rank for all $\theta \in [ 0 , 2 \pi ]$ , (v) $\hat { A }$ has no eigenvalues on the unit circle, and (vi) Aˆ is nonsingular. Then:

1) There is a unique stabilizing solution $\hat { X } \ge 0$ to the following DARE:

$$
\begin{array}{l} 0 = \hat {X} - \hat {A} ^ {\top} \hat {X} \hat {A} - \hat {Q} \tag {37} \\ + \left(\hat {A} ^ {\top} \hat {X} \hat {B} + \hat {S}\right) \hat {H} ^ {- 1} \left(\hat {A} ^ {\top} \hat {X} \hat {B} + \hat {S}\right) ^ {\top}, \\ \end{array}
$$

where $\hat { H } : = \hat { R } + \hat { B } ^ { \top } \hat { X } \hat { B } \ \succ \ 0 .$ . The gain $\hat { K } _ { x } : = \hat { H } ^ { - 1 } ( \hat { A } ^ { \top } \hat { X } \hat { B } + \hat { S } ) ^ { \top }$ is stabilizing, i.e. $\hat { A } - \hat { B } \hat { K } _ { x }$ is a Schur matrix. Moreover, $( \hat { A } - \hat { B } \hat { K } _ { x } )$ is nonsingular.

2) There is a unique stabilizing solution $\hat { Y } \geq 0$ to the following DARE:

$$0 = \hat {Y} - \hat {A} \hat {Y} \hat {A} ^ {\top} + (\hat {A} \hat {Y} \hat {K} _ {x} ^ {\top}) \hat {W} ^ {- 1} (\hat {A} \hat {Y} \hat {K} _ {x} ^ {\top}) ^ {\top}, \tag {38}$$

where $\hat { W } : = \hat { H } ^ { - 1 } + \hat { K } _ { x } \hat { Y } \hat { K } _ { x } ^ { \top } \succsim 0$ . The gain $\hat { K } _ { y } : = \hat { W } ^ { - 1 } ( \hat { A } \hat { Y } \hat { K } _ { x } ^ { \top } ) ^ { \top }$ is stabilizing, i.e. $\hat { A } ^ { \top } - \hat { K } _ { x } ^ { \top } \hat { K } _ { y }$ is a Schur matrix.

3) Define $\begin{array} { r } { A _ { F } : = \hat { A } - \hat { K } _ { y } ^ { \top } \hat { K } _ { x } , B _ { F } : = \hat { B } - \hat { K } _ { y } ^ { \top } , C _ { F } = \hat { W } ^ { - \frac { 1 } { 2 } } \hat { K } _ { x } , a n d D _ { F } : = \hat { W } ^ { - \frac { 1 } { 2 } } } \end{array}$ . A spectral factorization of $\hat { P } ^ { \sim } \hat { P }$ is $F ^ { \sim } F$ where the spectral factor F is:

$$\tilde {x} _ {t + 1} = A _ {F} \tilde {x} _ {t} + B _ {F} d _ {t} \tag {39}\tilde {e} _ {t} = C _ {F} \tilde {x} _ {t} + D _ {F} d _ {t}.$$
