for some constants $C _ { w }$ and $C _ { z }$ . Hence, we obtain

$$\| w _ {t} ^ {(\ell)} z _ {t} ^ {(\ell) \top} P ^ {(\ell)} \| \leq B _ {w} B _ {z} \| P ^ {(\ell)} \|,$$

We can now apply matrix Hoeffding inequality (Lemma D.1) to $M ^ { ( \ell ) }$

$$
\mathcal {X} _ {\ell} = \left[ \begin{array}{c c} 0 & M ^ {(\ell)} \\ M ^ {(\ell) \top} & 0 \end{array} \right], \mathbb {E} [ \mathcal {X} _ {\ell} \mid P ^ {(\ell)} ] = 0, \mathcal {X} _ {\ell} ^ {2} \preceq B _ {w} ^ {2} B _ {z} ^ {2} \| P ^ {(\ell)} \| ^ {2} I _ {2 d _ {x} + d _ {u}},
$$

Therefore, we have that

$$\mathbb {P} \left(\left\| \sum_ {\ell \in C _ {\hat {j}}} M ^ {(\ell)} \right\| \geq t\right) \leq (2 d _ {x} + d _ {u}) \exp \left(- \frac {t ^ {2}}{8 \sigma^ {2}}\right),$$

where $\begin{array} { r } { \sigma ^ { 2 } = B _ { w } ^ { 2 } B _ { z } ^ { 2 } \sum _ { \ell \in C _ { \hat { i } } } \| P ^ { ( \ell ) } \| ^ { 2 } } \end{array}$ . By setting $\begin{array} { r } { t = \sqrt { \tau _ { k } } \sigma _ { k } B _ { w } B _ { z } \sqrt { \sum _ { \ell \in C _ { \hat { j } } } \| P ^ { ( \ell ) } \| ^ { 2 } } \sqrt { 8 \log \left( \frac { 2 d _ { x } + d _ { u } } { \delta } \right) } } \end{array}$ , we obtain the following expression:

$$\left\| \bar {M} \right\| \leq \frac {\sqrt {\tau_ {k}} \sigma_ {k} B _ {w} B _ {z}}{| C _ {\hat {j}} |} \sqrt {\sum_ {\ell \in C _ {\hat {j}}} \| P ^ {(\ell)} \| ^ {2}} \sqrt {8 \log \left(\frac {2 d _ {x} + d _ {u}}{\delta}\right)},$$

with probability at least $1 - \delta$ .

Controllinprobability $\| P ^ { ( \ell ) } \|$ : We now proceed to prove that  To prove this bound, we first as $\begin{array} { r } { \| P ^ { ( \ell ) } \| \le \frac { C _ { P } } { \sigma _ { k } ^ { 2 } \tau _ { k } } } \end{array}$ some constant is a strictly s $C _ { P }$ , withonary, $1 - \delta _ { P }$ $\{ z _ { t } ^ { ( \ell ) } \} _ { t \in \mathbb { Z } _ { 1 } }$

mean-zero Gaussian process in $\mathbb { R } ^ { d }$ with covariance $\Sigma _ { z } ^ { ( \ell ) } : = \mathbb { E } \left\lceil z _ { t } ^ { ( \ell ) } z _ { t } ^ { ( \ell ) \top } \right\rceil \succeq \sigma _ { k } ^ { 2 } I _ { d }$ and geometric β-mixing, i.e., $\beta ( s ) \le C _ { \beta } \rho ^ { s }$ for some $C _ { \beta } > 0$ and $\rho \in ( 0 , 1 )$ (see [Yu, 1994]).

Let $\Sigma _ { t } ^ { 1 / 2 } : = ( \Sigma _ { z _ { t } } ^ { ( \ell ) } ) ^ { 1 / 2 }$ . As we know that $\{ z _ { t } \} _ { t }$ are Gaussian with covariance $\Sigma _ { z _ { t } } ^ { ( \ell ) }$ , we can write $z _ { t } = \Sigma ^ { 1 / 2 } \dot { y } _ { t }$ where $\{ y _ { t } \} _ { t }$ is a stationary Gaussian process in $\mathbb { R } ^ { d }$ with the same β-mixing rate. Hence, we can write
