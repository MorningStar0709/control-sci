where $V ( \pmb { x } ( t ) ) = ( \pmb { x } ( t ) - \pmb { x } _ { e } ) ^ { T } P ( \pmb { x } ( t ) - \pmb { x } _ { e } ) , P$ is positive definite, $c _ { 3 } > 0 , Q > 0$ and $\delta ( t ) \in \mathbb { R }$ is a relaxation variable (decision variable) to minimize for less violation of the strict CLF constraint.
b(x) has relative degree m and $V ( { \pmb x } )$ has relative degree 1.
The above optimization problem is feasible at a given state x if all the constraints define a non-empty set for the decision variables u, δ.
As discussed in Sec.
II, the optimal control $\boldsymbol { u } ^ { * } ( t _ { k } )$ obtained by solving each QP is applied at the beginning of the interval and held constant for the whole interval.
However, the CBF-CLF-QPs could easily be infeasible at some $t _ { k + 1 }$ .
