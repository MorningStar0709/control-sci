Proof: From Assumption 1, the ball $B _ { r } ( p _ { i } ( t ) )$ encompasses the projection onto the 2-D space of $\mathcal { E } _ { c } ( x _ { s p , i } )$ for any current state $x _ { i } ( t ) \in \mathcal { E } _ { c } ( x _ { s p , i } )$ . Utilizing (8) to adjust the setpoint ensures that $x _ { i } ( t )$ remains within $\mathcal { E } _ { c } ( x _ { s p , i } ^ { \prime } )$ , signifying that its 2-D space projection still falls within $B _ { r } ( p _ { i } ( t ) )$ . Thus, should the updated setpoint $x _ { s p , i } ^ { \prime }$ contravene (7), such a violation would only occur concerning agents situated within the one-hop communication range. □

Proposition 1 highlights a critical aspect of setpoint updates: setpoint updates only impact safety for agents within communication range. This ensures agent i can exchange messages with affected neighbors before executing updates. This allows coordinating to preemptively address any potential safety concerns from the new setpoint through direct communication with impacted agents.

Building upon Proposition 1, let’s define $\mathcal { D } _ { i } ( t )$ as the set comprising all agents $j \neq i$ that are within the one-hop communication range of agent i at time t. The safe setpoint update algorithm for each agent i is given in Algorithm 1. Once agent $i \gamma _ { \mathrm { s } }$ state $x _ { i } ( t )$ reaches its current setpoint $x _ { s p , i }$ (as per Definition 2), it computes a new target setpoint $x _ { s p , i } ^ { \prime }$ . Before updating, agent i checks for any possible safety violations by evaluating the safety condition (7) for all agents in $\mathcal { D } _ { i } ( t )$ . If no violation is detected, $x _ { s p , i }$ is updated to the new $x _ { s p , i } ^ { \prime } ;$ otherwise, the previous setpoint is retained.

By applying Algorithm 1 to each agent, we ensure that the safety condition (7) is satisfied throughout the system. This is guaranteed by the fact that an agent can only update its setpoint after verifying that the new target does not violate safety for any agents within its one-hop communication range, as stated in Proposition 1. The proof of safety relies on the following assumptions:
