$$
\left\{ \begin{array}{l l} \mathcal {L} _ {(\widehat {X}, Y)} v (x, i; \theta) + b _ {x} (x, i) v (x, i; \theta) + \pi_ {x} (x, \theta) = 0, & x \in (\alpha_ {i} (\theta), \beta_ {i} (\theta)), \quad i \in \mathbb {Y}, \\ \mathcal {L} _ {(\widehat {X}, Y)} v (x, i; \theta) + b _ {x} (x, i) v (x, i; \theta) + \pi_ {x} (x, \theta) \leq 0, & x \in (\alpha_ {i} (\theta), \overline {{x}}), \quad i \in \mathbb {Y}, \\ \mathcal {L} _ {(\widehat {X}, Y)} v (x, i; \theta) + b _ {x} (x, i) v (x, i; \theta) + \pi_ {x} (x, \theta) \geq 0, & x \in (\underline {{x}}, \beta_ {i} (\theta)), \quad i \in \mathbb {Y}, \\ v (x, i; \theta) = k _ {1}, & x \in (\underline {{x}}, \alpha_ {i} (\theta) ], \quad i \in \mathbb {Y}, \\ v (x, i; \theta) = k _ {2}, & x \in [ \beta_ {i} (\theta), \overline {{x}}), \quad i \in \mathbb {Y}, \\ v _ {x} (x, i; \theta) = 0, & x \in (\underline {{x}}, \alpha_ {i} (\theta) ], \quad i \in \mathbb {Y}, \\ v _ {x} (x, i; \theta) = 0, & x \in [ \beta_ {i} (\theta), \overline {{x}}), \quad i \in \mathbb {Y}. \end{array} \right. \tag {3.10}
$$

3.2. The solution to the ergodic singular control problem. In this section, via a verification theorem, we establish the relation between the Dynkin game of Section 3.1 and the ergodic singular control problem (3.1).

Proposition 3.4. Recall v as in (3.6), the free boundaries $( \alpha _ { i } ( \theta ) ) _ { i \in \mathbb { Y } } , ~ ( \beta _ { i } ( \theta ) ) _ { i \in \mathbb { Y } }$ as in (3.7), and define

$$U (x, i; \theta) := K _ {i} (\theta) + \int_ {\alpha_ {i} (\theta)} ^ {x} v (y, i; \theta) d y, \quad (x, i, \theta) \in \mathcal {O} \times \mathbb {R} _ {+}, \tag {3.11}$$

with $\begin{array} { r } { K _ { i } ( \theta ) : = - \int _ { \alpha _ { i } ( \theta ) } ^ { \beta _ { i } ( \theta ) } v ( y , i ; \theta ) d y } \end{array}$ . Then we have
