Proof. Recall the linear equivalent model of the unicycle robot in (47) and the cost function in (48). The use of the terminal cost function $\bar { Q }$ to solve the Lyapunov function in (58) will render the optimal cost function for the next time step $J ^ { * } ( k { + } 1 )$ is equal to $J ^ { * } ( k + 1 ) = J ^ { * } ( k ) - ( \| z _ { d } ( k ) \| _ { O } ^ { 2 } + \| \nu ( k ) \| _ { R } ^ { 2 } )$ . Hence, it can be concluded that $J ^ { * } ( k + 1 ) \to \mathbf { \tilde { 0 } }$ as $k \  \ 0 .$ . From (44), one finds as $z  0$ and $\bar { x } \ \to \ 0$ and this proves the asymptotic convergence. We prove the recursive stability using the classical terminal constraints. Let $\tilde { \nu } _ { d } ( k { + } 1 )$ denote the input sequence at time ?? + 1 corresponding to the optimal prediction at time ??. For feasible $\tilde { \nu } _ { d } ( k + 1 )$ the ?? th element (the tail of $\nu _ { d } ^ { * } ( k + N | k ) \ = \ K z _ { d } ^ { * } ( k + N | k )$ is required to satisfy the terminal constraint. This is equivalent to the constraints on the terminal state prediction $z _ { d } ( k + N | k ) \in \mathcal { Z } _ { f } .$ , where $\boldsymbol { \mathcal { Z } } _ { f }$ is the terminal set [38]. The necessary and sufficient conditions for the predictions generated by the tail $\tilde { \nu } _ { d } ( k + 1 )$ are feasible at time ?? + 1 is to have $\boldsymbol { \mathcal { Z } } _ { f }$ is control and safe invariant. The terminal set $\mathcal { Z } _ { f }$ is control invariant if $( A _ { d } + B _ { d } K ) z _ { d } ( k + N | k ) \in$ $\mathcal { Z } _ { f } \forall z _ { d } ( k + N | k ) \in \mathcal { Z } _ { f }$ . The terminal set $\boldsymbol { \mathcal { Z } } _ { f }$ is safe invariant i $\mathrm { ~  ~ { ~ f ~ } ~ } _ { \bigtriangleup } { \mathcal { H } } ( z _ { d } ( k + 1 | N ) ) \geq - \gamma { \mathcal { H } } ( z _ { d } ( k | N ) ) \forall z _ { d } ( k + N | k ) \in { \mathcal { Z } } _ { f }$ . To render $\boldsymbol { \mathcal { Z } } _ { f }$ control invariant, we need to verify that

$$v _ {\max} \leq K (A _ {d} + B _ {d} K) ^ {i} z _ {d} (k + N | k) \leq v _ {\max} \tag {59}
\left[ \begin{array}{l} \underline {{z}} _ {1} \\ \underline {{z}} _ {3} \end{array} \right] \leq (A _ {d} + B _ {d} K) ^ {i} z _ {d} (k + N | k) \leq \left[ \begin{array}{l} \overline {{z}} _ {1} \\ \overline {{z}} _ {3} \end{array} \right] \quad \forall i \geq 0 \tag {60}
$$
