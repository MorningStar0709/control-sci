$$
\left\{ \begin{array}{l l} \mathcal {L} _ {(X, Y)} U (x, i; \theta) + \pi (x, \theta) = \lambda (\theta , i), & x \in (\alpha_ {i} (\theta), \beta_ {i} (\theta)), \quad i \in \mathbb {Y}, \\ \mathcal {L} _ {(X, Y)} U (x, i; \theta) + \pi (x, \theta) \leq \lambda (\theta , i), & x \notin (\alpha_ {i} (\theta), \beta_ {i} (\theta)), \quad i \in \mathbb {Y}, \\ U _ {x} (x, i; \theta) = k _ {1}, & x \in (\underline {{x}}, \alpha_ {i} (\theta) ], \quad i \in \mathbb {Y}, \\ U _ {x} (x, i; \theta) \leq k _ {1}, & x \in (\alpha_ {i} (\theta), \beta_ {i} (\theta)), \quad i \in \mathbb {Y}, \\ U _ {x} (x, i; \theta) = k _ {2}, & x \in [ \beta_ {i} (\theta), \overline {{x}}), \quad i \in \mathbb {Y}, \\ U _ {x} (x, i; \theta) \geq k _ {2}, & x \in (\alpha_ {i} (\theta), \beta_ {i} (\theta)), \quad i \in \mathbb {Y}, \end{array} \right. \tag {3.12}
$$

where

$$\lambda (\theta , i) := \sum_ {j \neq i} q _ {i j} \bigg (\int_ {\alpha_ {j} (\theta)} ^ {\alpha_ {i} (\theta)} v (y, j; \theta) d y + K _ {i} (\theta) - K _ {j} (\theta) \bigg) + \big (b (\alpha_ {i} (\theta), i) k _ {1} + \pi (\alpha_ {i} (\theta), \theta) \big). \tag {3.13}$$

Proof. By Proposition 3.3 and the definition of U, we obtain that $U ( \cdot , i ; \theta ) \in C ^ { 2 } ( { \mathcal { T } } )$ for $i \in \mathbb { Y }$ . Then, recalling $\mathcal { L } _ { ( X , Y ) }$ as in (2.5), we have

$$\mathcal {L} _ {(X, Y)} U (x, i; \theta) + \pi (x, \theta) = \underbrace {\left(\frac {1}{2} \sigma^ {2} (x , i) v _ {x} (x , i ; \theta) + b (x , i) v (x , i ; \theta) + \pi (x , \theta)\right)} _ {(1)}+ \sum_ {j \neq i} q _ {i j} \bigg (K _ {j} (\theta) + \int_ {\alpha_ {j} (\theta)} ^ {x} v (y, j; \theta) d y - K _ {i} (\theta) - \int_ {\alpha_ {i} (\theta)} ^ {x} v (y, i; \theta) d y \bigg). \tag {3.14}$$

Using that v satisfies (3.10) (see in particular the fourth to seventh display equations), via an integration by parts we have that

$$
\begin{array}{l} (1) = \int_ {\alpha_ {i} (\theta)} ^ {x} \left(\frac {1}{2} \sigma^ {2} (y, i) v _ {x x} (y, i; \theta) + (b (y, i) + \sigma \sigma_ {x} (y, i)) v _ {x} (y, i; \theta) \right. \\ \left. + b _ {x} (y, i) v (y, i; \theta) + \pi_ {x} (y, \theta)\right) d y + b (\alpha_ {i} (\theta), i) k _ {1} + \pi (\alpha_ {i} (\theta), \theta). \\ \end{array}
$$

Then, using the second equation in (3.10) we find

$$(1) \leq \int_ {\alpha_ {i} (\theta)} ^ {x} - \sum_ {j \neq i} q _ {i j} (v (y, j; \theta) - v (y, i; \theta)) d y + (b (\alpha_ {i} (\theta), i) k _ {1} + \pi (\alpha_ {i} (\theta), \theta)).$$

Substituting the latter inequality into (3.14) yields
