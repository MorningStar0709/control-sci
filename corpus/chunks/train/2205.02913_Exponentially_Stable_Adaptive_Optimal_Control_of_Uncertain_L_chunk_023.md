# 3.2 Equation (18) parametrization

The first step to obtain the equations (18) is to expand the matrix exponential $\Phi \left( \tau _ { \infty } \right) = e ^ { D \tau _ { \infty } }$ into the Taylor series:

$$\Phi \left(\tau_ {\infty}\right) = e ^ {D \tau_ {\infty}} = \sum_ {k = 0} ^ {p} \frac {1}{k !} D ^ {k} \tau_ {\infty} ^ {k} + \underbrace {\sum_ {k = p + 1} ^ {\infty} \frac {1}{k !} D ^ {k} \tau_ {\infty} ^ {k}} _ {\varepsilon}, \tag {26}$$

where $p \in \mathbb N$ is the degree of the Taylor polynomial, $\varepsilon \in \mathbb { R } ^ { 2 n \times 2 n }$ is the residual.

Proposition 3. The norm of the residual of the $p ^ { \mathrm { t h } }$ Taylor polynomial is upper bounded as follows:

$$\| \varepsilon \| \leq \underbrace {\frac {\tau_ {\infty} ^ {p} \| D \| ^ {p}}{(p + 1) !} \left(e ^ {\| D \| \tau_ {\infty}} - 1\right)} _ {\varepsilon_ {\max}}, \tag {27}$$

moreover, when $\tau _ { \infty } \in L _ { \infty }$ , it holds that $\operatorname* { l i m } _ { p \to \infty } \varepsilon _ { \mathrm { m a x } } = 0 .$

Proof. Proof of Proposition 3 is postponed to Appendix.

Having the measurable functions $z _ { A } \left( t \right)$ and $z _ { B } \left( t \right)$ , the regression with respect to the matrix ?? is obtained as:

$$
z _ {D} (t) := \left[ \begin{array}{c c} - \varphi (t) z _ {A} (t) & z _ {B} (t) R ^ {- 1} z _ {B} ^ {\mathrm{T}} (t) \\ Q \varphi^ {2} (t) & \varphi (t) z _ {A} ^ {\mathrm{T}} (t) \end{array} \right] = \varphi^ {2} (t) D. \tag {28}
$$

Then the equation (26) is multiplied by $\varphi ^ { 2 p } \left( t \right)$ , and the function $z _ { D } \left( t \right)$ is substituted into the obtained result:

$$z _ {\Phi} (t) := \varphi^ {2 p} (t) \Phi (\tau_ {\infty}) - \varphi^ {2 p} (t) \varepsilon ,z _ {\Phi} (t) := \sum_ {k = 0} ^ {p} \frac {1}{k !} \varphi^ {2 p - 2 k} (t) z _ {D} ^ {k} (t) \tau_ {\infty} ^ {k}, \tag {29}$$

where $z _ { D } \left( t \right) \in \mathbb { R } ^ { 2 n \times 2 n }$ and $z _ { \Phi } \left( t \right) \in \mathbb { R } ^ { 2 n \times 2 n }$ are the measurable functions.

The regressions with respect to $\Phi _ { 1 1 } \left( \tau _ { \infty } \right)$ and $\Phi _ { 2 1 } \left( \tau _ { \infty } \right)$ are obtained from (29):
