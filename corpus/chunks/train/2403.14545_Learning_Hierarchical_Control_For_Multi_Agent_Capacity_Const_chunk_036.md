and captures how many unique tasks the agent completes along the trajectory $\mathbf { x } ^ { r }$ before returning to the depot. We note this is the same performance function the high-level policy (31) aims to maximize at each event k. It follows from Thm. 1 and Thm. 2 that the high-level optimal control problem (31) always plans N-step trajectories of nodes and capacity state values which the low-level policy (33) can accurately track. It therefore suffices in this proof to only consider the performance of the high-level policy (31), recognizing that the high-level trajectories are inherently designed to be feasible for the low-level policy.

The proof follows arguments from Thm. 2 in [26]. Consider a closed-loop trajectory $\mathbf { x } ^ { r - 1 }$ produced by Alg. 1 at iteration (r − 1). By definition of the iteration cost, we have

$$
\begin{array}{l} V _ {n} (\mathbf {x} ^ {r - 1}) = V _ {n} ^ {H} (\mathbf {x} ^ {H, r - 1}) \\ \leq V _ {n} ^ {H} \left(\mathbf {x} _ {0: N - 1} ^ {H, r - 1}\right) + Q ^ {H, r} \left(x _ {N} ^ {H, r - 1}\right) \\ \leq \max _ {\mathbf {x} _ {0: N} ^ {H}} V _ {n} ^ {H} (\mathbf {x} _ {0: N - 1} ^ {H}) + Q ^ {H, r} (x _ {N} ^ {H}) (48a) \\ = V _ {n, 0 \rightarrow N} ^ {H, r} (x _ {0} ^ {H, r}), (48b) \\ \end{array}
$$

wher e V H,r $V _ { n , 0  N } ^ { H , r } ( x _ { 0 } ^ { H , r } )$ H,r) is the solution to (31), the predicted closed-loop cost for iteration r at event $k = 0$ from initial state xH0 $x _ { 0 } ^ { H , r } = x _ { 0 } ^ { H , r - 1 }$ = x0 H,r−1 . Note that, as shown in Thm. 2, xH,r0:N $2 , \mathbf { x } _ { 0 : N } ^ { H , r - 1 }$

is a feasible solution (48a). Now, notice that
