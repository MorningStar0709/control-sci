$$v _ {\mathrm{a}} = K (e - T _ {\mathrm{D}} \dot {\theta} _ {\mathrm{m}})$$

其中：K 和 $T_{D}$ 为待求控制器增益。

(a) 画出反馈系统的框图，其中 $\theta_{m}$ 和 $\dot{\theta}_{m}$ 为描述电动机的变量。

(b) 假设 $a_{1}=65$ , $b_{0}=200$ , $c_{0}=10$ 。如果没有负载转矩（即 w=0），则当 $v_{a}=100V$ 时，求电动机的转速（单位 r/min）?

(c) 根据(b)问中的参数值，令控制为 $D = k_{P} + k_{D}s$ ，求 $k_{P}$ 和 $k_{D}$ 的值，使系统在无负载转矩条件下满足：当输入为阶跃信号时，系统超调量近似为 17%，并且在阈值为 5% 条件下，稳态时间小

于或等于0.05s。

(d) 根据(c)问中的计算结果，推导出系统在参考角度输入信号作用下的稳态误差表达式，并计算出 $\theta_{ref} = 1rad$ 时的稳态误差值。

(e) 根据 (c) 问中的计算结果，当 $\theta_{ref}=0$ 时，推导系统在恒定干扰转矩作用下的稳态误差表达式，并计算 w=1.0 时的稳态误差值。

4.26 设计一个汽车速度自动控制系统。假设：

（1）汽车质量为 1000kg，（2）油门是控制量 U，当其角度变化 1 度时，便在汽车上施加 10N 的力，（3）空气的摩擦阻力正比于速度，其比例系数为 $10N \cdot s/m$ 。

(a) 试求从控制输入 U 到汽车速度的传递函数。

(b) 假设速度变化由下式给出，

$$V (s) = \frac {1}{s + 0 . 0 2} U (s) + \frac {0 . 0 5}{s + 0 . 0 2} W (s)$$

其中：V 的单位是 m/s，U 的单位是 (°)，W 为公路等级。设计一个比例控制器 $U = -k_{p} V$ 使速度误差小于 1m/s，此时的公路等级为常数 2%。

(c) 试讨论对系统进行积分控制有哪些优点（如果有的话）?

(d) 假设纯积分控制(即没有比例项)是有利的, 请选择反馈增益使系统处于临界阻尼状态( $\zeta=1$ )。

4.27 考虑如图 4.40 所描述的汽车速度控制系统。

![](image/8b82e339d8db89e34a3535c62ad7dbbee510c13d7f526ec0029a9b4edfe91ad3.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    R --> Hr
    Hr --> Sum1["Σ"]
    Sum1 --> kp["kp"]
    kp --> A["s+a"]
    A["s+a"] --> Sum2["Σ"]
    Sum2 --> Y
    Y --> Hr
    Hr --> Sum1
    Sum1 -->|+| Sum2
    Sum2 -->|+| W
    W --> B["s+a"]
    B["s+a"] --> Sum2
    Sum2 -->|-| Sum1
    Sum1 -->|R 为期望速度，Y 为实际速度，W 为公路等级，（%）|
```
</details>

图 4.40 汽车速度控制系统

(a) 试求从 $W(s)$ 到 $Y(s)$ 和从 $R(s)$ 到 $Y(s)$ 的传递函数。

(b) 假设期望的速度是恒定参考输入信号 r，即 $R(s)=\frac{r_{0}}{s}$ 。假设路面平整，因此 $w(t)=0$ 。计算增益 $k_{P}$ ， $H_{r}$ ， $H_{y}$ 满足下式：

$$\lim _ {t \rightarrow \infty} y (t) = r _ {\circ}$$

讨论时包括开环(假设 $H_{y}=0$ ) 和反馈 ( $H_{y}\neq0$ ) 两种情况。

(c) 假设除参考输入外还有常值干扰 $W(s)=\frac{w_{b}}{s}$ ，重新计算(b)问。特别地，前馈和反馈两种情况下，找出由于公路等级变化而产生的速度变化。并根据得出的结果解释：（1）为什么使用反馈控制是必要的？（2）如何选择增益 $k_{p}$ 以降低稳态误差。

(d) 假设 $w(t)=0$ ，增益 A 受到摄动为 $A+\delta A$ 。试求在前馈、反馈两种情况下由增益变化引起的速度误差。此时如何选择增益来减小 $\delta A$ 的影响。

4.28 考虑如图 4.41 所示的多变量系统。假设系统稳定。分别求每一个干扰输入到相应干扰输出的传递函数，并确定常值干扰输入下的 $y_{1}$ 和 $y_{2}$ 的稳态值。如果多项式输入信号 $w_{i}$ 最高阶次小于 k 时，所有输出的稳态值为零，并且输入信号 $w_{i}$ 的最高阶次小于 k 时系统至少有一个输出为非零常数，此时称多变量系统为 k 型系统。对于 $\omega_{1}$ 和 $\omega_{2}$ 的干扰抑制，系统类型是什么？
