Example 5.1 (Quadrotor Dynamics Identification using Port-Hamiltonian Neural ODE Network). To illustrate a Hamiltonian neural network for system identification, we provide an example from Duong et al. (2024b) where both Hamiltonian structure and the state manifold constraints are encoded in the neural network architecture. Consider a mobile robot, $e . g .$ , a quadrotor, with its generalized coordinate $q = ( z , R ) \in \mathit { S E } ( 3 )$ consisting of its position $z \in \mathbb { R } ^ { 3 }$ and orientation $R \in \ S O ( 3 )$ . Let $\zeta = ( \nu , \omega ) \in \mathbb { R } ^ { 6 }$ be the generalized velocity, consisting of ζ , ωthe body-frame linear velocity $\nu \in \mathbb { R } ^ { 3 }$ and the body-frame angular velocity $\epsilon \mathbb { R } ^ { 3 }$ . The generalized momentum is defined as $p ~ = ~ \left\lceil p _ { \nu } \right\rceil ~ = ~ M ( q ) \zeta ~ \in ~ \mathbb { R } ^ { 6 }$ , where M(q) is the generalized mass $o f$ ω the system, $p _ { \nu }$ is the linear momentum, and $p _ { \omega }$ is the ωangular momentum. The Hamiltonian function is defined as the total energy of the system $\begin{array} { r } { H ( q , p ) = \frac { 1 } { 2 } p ^ { \top } M ^ { - 1 } ( q ) p + V ( q ) } \end{array}$ where $\begin{array} { r } { T = \frac { 1 } { 2 } p ^ { \top } M ^ { - 1 } ( q ) p } \end{array}$ , is the kinetic energy and V(q) is the potential energy.

The equations of motions on the SE(3) manifold are written in port-Hamiltonian form as:

$$\dot {x} = R \frac {\partial H (q , p)}{\partial p _ {v}}, \tag {70a}\dot {R} _ {i} = R _ {i} \times \frac {\partial H (q , p)}{\partial p _ {\omega}}, \quad i = 1, 2, 3 \tag {70b}\dot {p} _ {v} = p _ {v} \times \frac {\partial H (q , p)}{\partial p _ {\omega}} - R ^ {\top} \frac {\partial H (q , p)}{\partial x} + b _ {v} (q) u \tag {70c}\dot {p} _ {\omega} = p _ {\omega} \times \frac {\partial H (q , p)}{\partial p _ {\omega}} + p _ {v} \times \frac {\partial H (q , p)}{\partial p _ {v}} + \tag {70d}\sum_ {i = 1} ^ {3} R _ {i} \times \frac {\partial H (q , p)}{\partial R _ {i}} + b _ {\omega} (q) u,$$

where the input matrix is $\begin{array} { r l } { B ( q ) \ = \ \left\lceil b _ { \nu } ( q ) ^ { \top } \quad b _ { \omega } ( q ) ^ { \top } \right\rceil ^ { \top } } \end{array}$ and the notation × denotes the cross product of two vectors.
