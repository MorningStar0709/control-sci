$$\langle \widetilde {V} \mathbf {u} ^ {\prime}, \mathbf {u} ^ {\prime} \rangle \geq \langle \widetilde {\varepsilon} \mathbf {u}, \mathbf {u} \rangle \geq \lambda_ {\min} (\widetilde {\varepsilon}) \| \mathbf {u} ^ {\prime} \| ^ {2}.$$

□

Consequently, one can set $V _ { 0 } = 0$ to avoid having to specify $u _ { - 1 }$ when initializing the system at time $n = 0 .$ , in case $u _ { k - 1 } ^ { \prime }$ does not appear in the stage constraints (5).

Completing the square in (7), one gets that

$$f (\mathbf {z}) := \frac {1}{2} \langle H \mathbf {z}, \mathbf {z} \rangle \tag {11}$$

satisfies the following for all $\theta _ { n } = { \left[ \begin{array} { c } { x _ { n } } \\ { u _ { n - 1 } } \end{array} \right] } ;$

$f ( \mathbf { z } ) = \frac { 1 } { 2 } J ( \mathbf { u } ^ { \prime } , \theta _ { n } ) + g ( \theta _ { n } )$ , as long as $\begin{array} { r } { \mathbf z = \mathbf u ^ { \prime } + H ^ { - 1 } F \boldsymbol { \theta } _ { n } } \end{array}$ moreover the inverse $H ^ { - 1 }$ is bounded with norm at most $1 / \varepsilon$ because of (9). Hence, we will focus on minimizing $f ( \mathbf { z } )$ .

Letting $\widetilde { p } < \infty$ denote the total number of constraints in $( 5 ) – ( 6 )$ , one can in a similar way to the above derive a vector W $\mathbf { \Lambda } \in \mathbb { R } ^ { \widetilde { p } }$ and bounded linear operators $S \in \mathcal { L } ( X \times U , \mathbb { R } ^ { \widetilde { p } } )$ (or $S \in { \mathcal { L } } ( X , \mathbb { R } ^ { \widetilde { p } } ) ;$ ) and $G \in \mathcal { L } ( \overline { { U ^ { N } } } , \mathbb { R } ^ { \widetilde { p } } )$ , such that all these constraints can be written compactly as

$$W + S \theta_ {n} - G \mathbf {z} \geq 0, \tag {12}$$

where the inequality is understood componentwise in $\mathring { \lfloor } ^ { p } .$
