$$
K _ {\delta} = \left\{x \in \Omega : \begin{array}{c} \delta \leq \| x \| _ {\mathbf {d}} \leq 1, \\ \min \{| g _ {\epsilon} (x) |, | g _ {\epsilon} (\pi_ {\mathbf {d}} (x)) | \} \geq \delta \end{array} \right\}, \tag {20}
$$

where $\pi _ { \mathbf { d } } ( x ) = \mathbf { d } ( - \ln \| x \| _ { \mathbf { d } } ) x$ is the d-homogeneous projector of $x \neq \mathbf { 0 }$ on the unit sphere $S$ and $\delta > 0$ is small enough. In this case, if

$$\frac {g _ {\epsilon} (x)}{g _ {\epsilon} (\pi_ {\mathbf {d}} (x))} > 0, \quad \forall x \in K _ {\delta} \tag {21}$$

then the practical homogeneity degree of the function $g _ { \epsilon }$ can be defined as

$$\nu_ {\epsilon} = \frac {1}{M _ {\delta}} \int_ {K _ {\delta}} \frac {1}{\ln \| x \| _ {\mathbf {d}}} \ln \frac {g _ {\epsilon} (x)}{g _ {\epsilon} (\pi_ {\mathbf {d}} (x))} d x \tag {22}$$

provided that $\epsilon > 0$ is small enough, where $M _ { \delta } = \int _ { K _ { \delta } }$ dx. If the condition (21) is not fulfilled then $g _ { \epsilon }$ is not an approximation of homogeneous function or the approximation precision $\epsilon > 0$ is not enough to detect d-homogeneity.

Proposition 3 Let $g _ { \epsilon }$ be ANN (11) approximating a non-zero continuous function $h \in C ( \mathbb { R } ^ { n } \backslash \{ \mathbf { 0 } \} , \mathbb { R } )$ on a compact $\Omega \subset \mathbb { R } ^ { n }$ with an error $\epsilon > 0$ , i.e., the inequality (10) is fulfilled for $g = h$ . Let the function h be d-homogeneous of degree $\nu \in \mathbb { R }$ . If there exists a norm $\| \cdot \|$ in $\mathbb { R } ^ { n }$ such that

$$S = \{x \in \mathbb {R} ^ {n}: \| x \| = 1 \} \subset \operatorname{int} \Omega \tag {23}$$

and the dilation d is monotone with respect to $\| \cdot \|$ , then for sufficiently small $\delta > 0$ it holds

$$\nu_ {\epsilon} \rightarrow \nu \quad a s \quad \epsilon \rightarrow 0 \tag {24}$$

where $\nu _ { \epsilon } \in \mathbb { R }$ is given by (22).

Proof. On the one hand, since h is d-homogeneous, then, for a sufficiently small $\delta > 0$ , the compact set $K _ { \delta } \subset \Omega \backslash \{ { \bf 0 } \}$ is non-empty. Since h is d-homogeneous then ν = $\begin{array} { r } { \nu = \frac { 1 } { \| x \| _ { \mathbf { d } } } \ln \frac { h ( x ) } { h ( \mathbf { d } ( - \ln \| x \| _ { \mathbf { d } } ) x ) } } \end{array}$ ln for all $x \in K _ { \delta }$ and

$$\nu = \frac {1}{M _ {\delta}} \int_ {K _ {\delta}} \frac {1}{\ln \| x \| _ {\mathbf {d}}} \ln \frac {h (x)}{h (\pi_ {\mathbf {d}} (x))} d x.$$

On the other hand, by definition $\nu _ { \epsilon } ,$ we have
