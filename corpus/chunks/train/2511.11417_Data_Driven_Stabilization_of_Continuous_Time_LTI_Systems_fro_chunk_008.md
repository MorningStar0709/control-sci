# Initialization

Measured dataset:

$$\{(t, u (t), y (t)): t \in [ 0, T ] \}. \tag {7}$$

Controller state dimension: $\mu : = n ( p + m )$

Tuning: $\boldsymbol { \Lambda } \in \mathbb { R } ^ { n \times n }$ Hurwitz with n distinct eigenvalues; $\Gamma \in \mathbb { R } ^ { n }$ such that $( \Lambda , \Gamma )$ is controllable; $\Delta \in \mathbb { R } ^ { p \times p } , \Delta =$ $\Delta ^ { \top } \succeq 0$ . Filter matrices:

$$
F := I _ {p + m} \otimes \Lambda , \quad G := \left[ \begin{array}{l} 0 _ {n p \times m} \\ I _ {m} \otimes \Gamma \end{array} \right], \quad L := \left[ \begin{array}{l} I _ {p} \otimes \Gamma \\ 0 _ {n m \times p} \end{array} \right]. \tag {8}
$$
