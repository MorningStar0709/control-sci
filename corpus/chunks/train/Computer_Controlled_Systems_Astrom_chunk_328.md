# Process Model

Consider a process with the transfer function

$$G (s) = \frac {k}{s ^ {2}}$$

where k = 1. This could, for example, be a simplified model of the arm servo of a compact disc player. The sampled pulse-transfer function is

$$H (z) = \frac {h ^ {2}}{2} \frac {z + 1}{(z - 1) ^ {2}}$$
