$W ^ { \top }$ satisfies the corresponding Hopfield LMI condition for some $P ^ { \prime } \succ 0$ and diagonal $Q ^ { \prime } \succ 0$ .

$$W \in \mathcal {W} (\mathrm{FR}, \cdot , \cdot) \iff W ^ {\top} \in \mathcal {W} (\mathrm{Hop}, \cdot , \cdot).$$

Proof: To prove (i), notice that the left-hand side of each CONE inequality is the sum of the left-hand side of the corresponding MONE inequality and a negative semidefinite matrix: $M _ { 1 } = \left[ \begin{array} { c c } { { - W ^ { \top } Q W } } & { { W ^ { \top } Q } } \\ { { Q W } } & { { - Q } } \end{array} \right]$ in the FRNN case, and $M _ { 2 } = \left\lceil { \begin{array} { c c } { - Q } & { { \bar { Q } } } \\ { Q } & { - Q } \end{array} } \right\rceil$ in the HNN case. Therefore, the CONE conditions imply the corresponding MONE conditions.

To prove (ii), notice that each discrete-time LMI condition entails the corresponding continuous-time condition with $c =$ $( 1 - \rho ^ { 2 } ) / 2 > 0$ and $2 ( \bar { 1 } - c ) = 1 + \rho ^ { 2 }$ . For instance, in the firing-rate MONE case, the LMI (24) implies (25):

$$
\begin{array}{l} \left[ \begin{array}{c c} P & - P \\ - P & P \end{array} \right] \succeq 0 \implies \\ \left[ \begin{array}{c c} - (1 + \rho^ {2}) P & P + W ^ {\top} Q \\ P + Q W & - 2 Q \end{array} \right] \preceq \left[ \begin{array}{c c} - \rho^ {2} P & W ^ {\top} Q \\ Q W & P - 2 Q \end{array} \right] \preceq 0. \\ \end{array}
$$

The proofs of (iii) differ in each of the four cases. In the continuous-time cases, under the substitution $( P , Q , W ) \mapsto$ $( P ^ { - 1 } , Q ^ { - 1 } , W ^ { \top } )$ , each firing-rate condition becomes the corresponding Hopfield condition with W replaced by $W ^ { \top }$ . For the MONE condition (25), this is proven via the congruence transformation with matrix $T = \mathrm { d i a g } ( P ^ { - 1 } , Q ^ { - 1 } ) = \bar { T ^ { \top } } \succ 0 :$

$$
T \left[ \begin{array}{c c} - 2 (1 - c) P & \star \\ P + Q W & - 2 Q \end{array} \right] T = \left[ \begin{array}{c c} - 2 (1 - c) P ^ {- 1} & \star \\ W P ^ {- 1} + Q ^ {- 1} & - 2 Q ^ {- 1} \end{array} \right] \preceq 0,
$$

is equivalent to (25) while also being identical to the LMI (29) with new variables $W ^ { \prime } ~ = ~ W ^ { \top } , ~ P ^ { \prime } ~ = ~ P ^ { - 1 } ~ \succ ~ 0 .$ , and $Q ^ { \prime } = Q ^ { - 1 } \succ 0 .$ . For the continuous-time CONE case, this is proven via the Schur complement: for $Q \succ 0 ,$ , the firing-rate inequality (23) reduces to

$$- 2 (1 - c) P + W ^ {\top} Q W + P Q ^ {- 1} P \preceq 0. \tag {30}$$

Pre- and post-multiplying this inequality by $P ^ { - 1 }$ yields
