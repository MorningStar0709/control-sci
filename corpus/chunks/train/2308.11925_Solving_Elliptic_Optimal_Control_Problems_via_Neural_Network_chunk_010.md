$$
\begin{array}{l} \mathcal {L} (y, p, u) = \left\| \Delta y + f + u \right\| _ {L ^ {2} (\Omega)} ^ {2} + \alpha_ {i} \left\| \Delta p + y - y _ {d} \right\| _ {L ^ {2} (\Omega)} ^ {2} \\ + \alpha_ {b} ^ {y} \| y \| _ {L ^ {2} (\partial \Omega)} ^ {2} + \alpha_ {b} ^ {p} \| p \| _ {L ^ {2} (\partial \Omega)} ^ {2} + \alpha_ {i} ^ {u} \| u + \lambda^ {- 1} p \| _ {L ^ {2} (\Omega)} ^ {2}. \\ \end{array}
$$

Compared with the proposed scheme (2.5), the loss $\mathcal { L } ( \boldsymbol { y } , \boldsymbol { p } , \boldsymbol { u } )$ requires also one $N N u _ { \kappa } \in \mathcal { U }$ for approximating the control $u ,$ and tuning the weight $\alpha _ { i } ^ { u }$ , which might be nontrivial. $A l s o ,$ the work $[ 1 \llap / ]$ does not discuss the constrained case, where the control u is inherently nonsmooth, and thus numerically challenging to approximate using DNNs [51]. A similar approach to $[ 1 \% ]$ was suggested in $[ { \mathcal { Q } } ]$ .
