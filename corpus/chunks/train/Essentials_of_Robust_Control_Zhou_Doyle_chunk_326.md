Problem 12.7 Assume that $G = [ \frac { A \ : | \ : B \ : ] } { C \ : | \ : D \ : ] } \in \mathcal { R } _ { p } ( s )$ and $( A , B )$ is stabilizable. Show that there exists a right coprime factorization $G = N M ^ { - 1 }$ such that $M \in \mathcal { R } \mathcal { H } _ { \infty }$ is an inner if and only if G has no poles on the jω axis. A particular realization is

$$
\left[ \begin{array}{c} M \\ N \end{array} \right] := \left[ \begin{array}{c c} A + B F & B \\ \hline F & I \\ C + D F & D \end{array} \right] \in \mathcal {R H} _ {\infty}
$$

where

$$
F = - B ^ {*} X
X = \operatorname{Ric} \left[ \begin{array}{c c} A & - B B ^ {*} \\ 0 & - A ^ {*} \end{array} \right] \geq 0.
$$

Problem 12.8 A right coprime factorization of $G = N M ^ { - 1 }$ with $N , M \in \mathcal { R } \mathcal { H } _ { \infty }$ is called a normalized right coprime factorization if $M ^ { \sim } M + N ^ { \sim } N = I ;$ that is, if $\left[ \begin{array} { l } { M } \\ { N } \end{array} \right]$ is an inner. Similarly, an $l c f G = \tilde { M } ^ { - 1 } \tilde { N }$ is called a normalized $l e f t$ coprime factorization if $\left[ \begin{array} { l l } { \tilde { M } } & { \tilde { N } } \end{array} \right]$ is a co-inner. Let a realization of G be given by $G = \left[ { \frac { A \left\lfloor B \right\rceil } { C \left\lceil D \right\rceil } } \right]$ and define $R = I + D ^ { * } D > 0$ and $\tilde { R } = I + D D ^ { * } > 0$ .

(a) Suppose $( A , B )$ is stabilizable and $( C , A )$ has no unobservable modes on the imaginary axis. Show that there is a normalized right coprime factorization $G = N M ^ { - 1 }$

$$
\left[ \begin{array}{c} M \\ N \end{array} \right] := \left[ \begin{array}{c c} A + B F & B R ^ {- 1 / 2} \\ \hline F & R ^ {- 1 / 2} \\ C + D F & D R ^ {- 1 / 2} \end{array} \right] \in \mathcal {R H} _ {\infty}
$$

where

$$F = - R ^ {- 1} (B ^ {*} X + D ^ {*} C)$$

and

$$
X = \mathrm{Ric} \left[ \begin{array}{c c} A - B R ^ {- 1} D ^ {*} C & - B R ^ {- 1} B ^ {*} \\ - C ^ {*} \tilde {R} ^ {- 1} C & - (A - B R ^ {- 1} D ^ {*} C) ^ {*} \end{array} \right] \geq 0.
$$

(b) Suppose $( C , A )$ is detectable and $( A , B )$ has no uncontrollable modes on the imaginary axis. Show that there is a normalized left coprime factorization $G = \tilde { M } ^ { - 1 } \breve { N }$

$$
\left[ \begin{array}{c c} \tilde {M} & \tilde {N} \end{array} \right] := \left[ \begin{array}{c c c} A + L C & L & B + L D \\ \hline \tilde {R} ^ {- 1 / 2} C & \tilde {R} ^ {- 1 / 2} & \tilde {R} ^ {- 1 / 2} D \end{array} \right]
$$

where

$$L = - (B D ^ {*} + Y C ^ {*}) \tilde {R} ^ {- 1}$$

and
