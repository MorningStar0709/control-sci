# III. EXISTENCE AND UNIQUENESS OF THE SOLUTION TO THE PATH-DEPENDENT STOCHASTIC SYSTEMS

Consider the following path-dependent stochastic system

$$
\begin{array}{l} d y (t) = \left[ \int_ {0} ^ {t} e ^ {\gamma s} y ^ {T} (s) Q _ {1} y (s) d s) \right] \mathfrak {h} (y (t)) d t + \mathfrak {f} (y (t)) d t \tag {5} \\ + \mathfrak {g} (y (t)) d w (t), \\ \end{array}
$$

where $\boldsymbol { y } ( t ) \in \mathbb { R } ^ { n } , \ \boldsymbol { \gamma }$ are constants, $Q _ { 1 } \in \mathbb { R } ^ { n \times n }$ is constant matrices with $Q _ { 1 } ~ > ~ 0 ;$ , and $w ( t )$ is a standard Brownian motion defined on the complete probability space $( \Omega , \mathcal { F } , \mathbb { P } )$ . The nonlinear term ${ \mathfrak { f } } , { \mathfrak { g } } ,$ h satisfy the following assumption.

Assumption III.1 (Local Lipschitz Condition). For each $q >$ 0 and $\forall y _ { 1 } , y _ { 2 } \in \mathbb { R } ^ { n }$ with $\| y _ { 1 } \| \vee \| y _ { 2 } \| < q$ , there exists a constant $L _ { q } > 0$ such that

$$
\begin{array}{l} \left\| \mathfrak {f} \left(y _ {1}\right) - \mathfrak {f} \left(y _ {2}\right) \right\| \vee \left\| \mathfrak {g} \left(y _ {1}\right) - \mathfrak {g} \left(y _ {2}\right) \right\| \vee \left\| \mathfrak {h} \left(y _ {1}\right) - \mathfrak {h} \left(y _ {2}\right) \right\| \\ \leq L _ {q} \| y _ {1} - y _ {2} \|. \\ \end{array}
$$

In the interest of consistency, we introduce the following functional Ito formula [29]. Define ˆ $y _ { t } = \{ y ( \theta ) : 0 \leq \theta \leq t \}$ . Let $\overline { { T } } > 0$ be a fixed terminal time. For each $t \in [ 0 , \overline { { T } } ]$ , denote by $\Lambda _ { t }$ the set of cadlag functions from $[ 0 , t ] \ t _ { 0 } \mathbb { R } ^ { n }$ , and define $\textstyle \Lambda \equiv \bigcup _ { t \in [ 0 , \overline { { T } } ] } \Lambda _ { t }$ . Let y be a continuous semimartingale process, with y(t) its value at time t and $y _ { t } \in \Lambda _ { t }$ its path over $[ 0 , t ]$ . Let $U : \Lambda \times [ 0 , \overline { { T } } ] \to \mathbb { R }$ be a smooth functional in the sense of Dupire, i.e., U is Λ-continuous, twice continuously differentiable in the spatial direction (in the sense of $\nabla _ { y } )$ and once continuously differentiable in the temporal direction (in the sense of $\nabla _ { t } ) .$ , with these derivatives themselves $\Lambda -$ continuous. Then the functional Ito formula (Theorem 3.1 ofˆ [29]) states that for any $t \in [ 0 , \overline { { T } } ]$ :
