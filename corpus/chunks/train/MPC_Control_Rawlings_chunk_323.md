# Exercise 2.15: Stability using observability instead of IOSS

Assume that the system $x ^ { + } = f ( x , u ) , y = h ( x )$ is \`-observable, i.e., there exists a $\alpha \in \mathcal K$ and an integer $N _ { 0 } \geq 1$ such that

$$\sum_ {j = 0} ^ {N _ {0} - 1} \ell (y (i), u (i)) \geq \alpha (| x |)$$

for all x and all u; here $x ( i ) : = \phi ( i ; x , { \bf u } )$ and $y ( i ) : = h ( x ( i ) )$ . Prove the result given in Section 2.7 that the origin is asymptotically stable for the closed-loop system $x ^ { + } =$ $f ( x , \kappa _ { N } ( x ) )$ ) using the assumption that $x ^ { + } = f ( x , u ) , y = h ( x )$ is \`-observable rather than IOSS. Assume that $N \geq N _ { 0 }$ .
