$$
p (x; \mu , \Sigma) = \left\{ \begin{array}{l l} \det \left(\Sigma_ {1}\right) ^ {- \frac {1}{2}} f \left(\left\| U _ {1} ^ {\top} \boldsymbol {\delta} \right\| _ {\Sigma_ {1} ^ {- 1}} ^ {2}\right), & U _ {2} ^ {\top} \boldsymbol {\delta} = \mathbf {0} _ {m - r}, \\ 0, & U _ {2} ^ {\top} \boldsymbol {\delta} \neq \mathbf {0} _ {m - r}, \end{array} \right.
$$

where $\delta : = x - \mu$ .

This work considers the following assumption.

Assumption 2. The random variable ǫ is subject to zeromean elliptical distributions: $\mathcal { E } _ { f } ^ { { \mathrm { } } } \left( \mathbf { 0 } _ { p } , \Sigma _ { \epsilon } \right)$ , where $\Sigma _ { \epsilon } \in \mathbb { S } _ { + } ^ { p }$ . The random variables $\nu _ { t }$ and $\nu _ { t } ^ { d }$ are realizations of zeromean stationary elliptical processes:

$$
\left[ \begin{array}{c} \nu_ {t} ^ {d} \\ \nu_ {t + \tau} ^ {d} \end{array} \right] \sim \mathcal {E} _ {f} \left(\mathbf {0} _ {2 n}, \left[ \begin{array}{c c} \Sigma_ {\nu} ^ {d} (0) & \Sigma_ {\nu} ^ {d} (\tau) \\ \Sigma_ {\nu} ^ {d} (\tau) & \Sigma_ {\nu} ^ {d} (0) \end{array} \right]\right), \tag {3}

\left[ \begin{array}{c} \nu_ {t} \\ \nu_ {t + \tau} \end{array} \right] \sim \mathcal {E} _ {f} \left(\mathbf {0} _ {2 n}, \left[ \begin{array}{c c} \Sigma_ {\nu} (0) & \Sigma_ {\nu} (\tau) \\ \Sigma_ {\nu} (\tau) & \Sigma_ {\nu} (0) \end{array} \right]\right), \tag {4}
$$

for $\tau \in \mathbb { N }$ , where $\Sigma _ { \nu } ^ { d } ( \tau ) = :$ blkdiag $\left( \Sigma _ { w } ^ { d } ( \tau ) , \Sigma _ { v } ^ { d } ( \tau ) \right) \in \mathbb { S } _ { + } ^ { n }$ , $\Sigma _ { \nu } ( \tau ) = :$ blkdiag $( \Sigma _ { w } ( \tau ) , \Sigma _ { v } ( \tau ) ) \ \in \ \dot { \mathbb { S } } _ { + } ^ { n } , \ \Sigma _ { w } ^ { d } ( \tau ) , \bar { \Sigma } _ { w } ( \tau ) \ \in$ $\mathbb { S } _ { + } ^ { n _ { u } }$ , and $\Sigma _ { v } ^ { d } ( \tau ) , \Sigma _ { v } ( \tau ) \in \mathbb { S } _ { + } ^ { n _ { y } }$ .
