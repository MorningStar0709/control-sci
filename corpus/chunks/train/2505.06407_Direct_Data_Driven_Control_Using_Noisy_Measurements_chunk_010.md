# III. MAIN RESULTS

This section first presents a data-based control solution with MSS guarantee for the linear system (1a)-(1b). It then extends the results to optimize a linear quadratic cost function over controllers with MSS guarantees. That is, it presents a data-based solution to the Linear Quadratic Regulator (LQR) problem with MSS guarantee.
