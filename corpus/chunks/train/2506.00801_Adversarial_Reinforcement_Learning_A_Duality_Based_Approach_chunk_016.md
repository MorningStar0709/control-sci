# 3.1.1 The Robbins-Monro Method

Under some regularity conditions, we can develop unbiased estimators for the gradient (13). Suppose that the solution to (12) exists at $\pmb { a } ^ { b }$ and interchanging expectation and differentiation in (15) is feasible. Then,

$$\nabla_ {\phi} \mathbb {E} \left[ \max _ {\boldsymbol {a} = \{\boldsymbol {a} _ {t} \} _ {t = 0} ^ {T - 1}} Y (\phi^ {b - 1}, \boldsymbol {a}, \xi) \mid \boldsymbol {s} _ {0} = \boldsymbol {s} \right] = \mathbb {E} \left[ \nabla_ {\phi} \max _ {\boldsymbol {a} = \{\boldsymbol {a} _ {t} \} _ {t = 0} ^ {T - 1}} Y (\phi^ {b - 1}, \boldsymbol {a} (\xi), \xi) \mid \boldsymbol {s} _ {0} = \boldsymbol {s} \right]. \tag {15}$$

Note that the differentiation operation inside the expectation on the right-hand side of the above equality is taken under fixed $\xi .$ . This pathwise derivative estimation is well-studied in simulation literature; see Glasserman [2004], Asmussen and Glynn [2007], and Chau and Fu [2015] and the reference therein. Hence,

$$\nabla_ {\phi} \max _ {\boldsymbol {a} = \left\{\boldsymbol {a} _ {t} \right\} _ {t = 0} ^ {T - 1}} Y \left(\phi^ {b - 1}, \boldsymbol {a}, \xi\right) = \nabla_ {\phi} \left(\sum_ {t = 0} ^ {T - 1} r _ {t} \left(\boldsymbol {s} _ {t}, \boldsymbol {a} _ {t} ^ {b}\right) + R \left(\boldsymbol {s} _ {T}\right) - z ^ {b - 1} \left(\boldsymbol {a} ^ {b} (\xi), \xi\right)\right) = \nabla_ {\phi} Y \left(\phi^ {b - 1}, \boldsymbol {a} ^ {b} (\xi), \xi\right) \tag {16}$$

provides an unbiased estimator for the gradient on the left-hand side of (15). Fixed $\xi$ . Changes in $\phi$ affect $Y ( \phi ^ { b - 1 } , \pmb { a } ( \xi ) , \xi )$ directly through $z ^ { \phi ^ { b - 1 } }$ and indirectly through the optimal solution $\pmb { a } ^ { \phi ^ { b - 1 } }$ to the inner optimization problem (12). However, under certain conditions, the indirect effect is negligible due to the envelope theorem Milgrom and Segal [2002]. Using this intuition, we can show that
