# V. CONTROL STRATEGY

Here we present the two SD-HOCBF that will be used in CMPC scheme applied to solve Problem 1,

$$
h _ {\delta \boldsymbol {r}} (\tilde {\boldsymbol {x}}, t) = \epsilon_ {\delta \boldsymbol {r}} ^ {2} - \| \delta \tilde {\boldsymbol {r}} - \delta \boldsymbol {r} _ {r} (t) \| ^ {2} = \epsilon_ {\delta \boldsymbol {r}} ^ {2} - \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {r}} (t) \| ^ {2}, \tag {13a}
$$

$$
h _ {\delta \boldsymbol {v}} (\tilde {\boldsymbol {x}}, t) = \epsilon_ {\delta \boldsymbol {v}} ^ {2} - \| \delta \tilde {\boldsymbol {v}} - \delta \boldsymbol {v} _ {r} (t) \| ^ {2} = \epsilon_ {\delta \boldsymbol {v}} ^ {2} - \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} (t) \| ^ {2} \tag {13b}
$$

where $h _ { \delta r } ( \tilde { { \pmb x } } , t )$ bounds the position error to tackle Problem 1, and $h _ { \delta v } ( \tilde { { \pmb x } } , t )$ bounds the maximum velocity error to provide recursive feasibility guarantees on the satisfaction of the former, as we will demonstrate.

It can be shown that $h _ { \delta r } ( \tilde { { \pmb x } } , t )$ is a relative degree two HOCBF for (2) while $h _ { \delta v } ( \tilde { { \pmb x } } , t )$ is relative degree one. Given the class-K functions $\alpha _ { \delta { r } _ { 0 } } ( x ) = p _ { \delta { r } _ { 0 } } x , \alpha _ { \delta { r } _ { 1 } } ( x ) = p _ { \delta { r } _ { 1 } } x$ for $h _ { \delta r }$ and $\alpha _ { \delta { \pmb v } _ { 0 } } ( { \pmb x } ) = p _ { \delta { \pmb v } _ { 0 } } { \pmb x }$ for $h _ { \delta { \pmb v } } ,$ with $p _ { \delta r _ { 0 } } , p _ { \delta r _ { 1 } } , p _ { \delta v _ { 0 } } \in$ $\mathbb { R } _ { > 0 }$ , we define functions $\zeta _ { \delta r }$ and $\zeta _ { \delta \mathbfit { v } }$ as

$$
\tilde {\zeta} _ {\delta \boldsymbol {v}} = - 2 \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} ^ {T} (\boldsymbol {f} _ {v} + \boldsymbol {u} + \boldsymbol {d}) + p _ {\delta \boldsymbol {v} _ {0}} (\epsilon_ {\delta \boldsymbol {v}} ^ {2} - \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} \| ^ {2}), \tag {14a}
$$

$$
\tilde {\zeta} _ {\delta \boldsymbol {r}} = - 2 \| \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {v}} \| ^ {2} - 2 \tilde {\boldsymbol {e}} _ {\delta \boldsymbol {r}} ^ {T} (\boldsymbol {f} _ {v} + \boldsymbol {d} + \boldsymbol {u}) - \tag {14b}
$$
