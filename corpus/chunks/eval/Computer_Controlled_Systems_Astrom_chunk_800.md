# Recursion in the Number of Parameters

When extra parameters are introduced, the vector $\hat{\theta}$ will have more components and there will be additional columns in the matrix $\Phi$ . The calculations can be arranged so that it is possible to make a recursion in the number of parameters in the model. The recursion involves an inversion of a matrix of the same dimension as the number of added parameters.
