$$
\begin{array}{l} | (T - T _ {0}) J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, \{w _ {t} \} _ {t = T _ {0}} ^ {T - 1}) - (T - T _ {0}) J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, \{w _ {t} ^ {\prime} \} _ {t = T _ {0}} ^ {T - 1}) | \\ = | (i - T _ {0}) J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, i - T _ {0}, \{w _ {t} \} _ {t = T _ {0}} ^ {i - 1}) - (i - T _ {0}) J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, i - T _ {0}, \{w _ {t} ^ {\prime} \} _ {t = T _ {0}} ^ {i - 1}) | + \\ | T ^ {\prime} J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T ^ {\prime}, x _ {i + 1}, \{w _ {t} \} _ {t = i + 1} ^ {T - 1}) - T ^ {\prime} J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T ^ {\prime}, y _ {i + 1}, \{w _ {t} \} _ {t = i + 1} ^ {T - 1}) | \\ = | J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - i - 1, x _ {i + 1}, \{w _ {t} \} _ {t = i + 1} ^ {T - 1}) - J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - i - 1, y _ {i + 1}, \{w _ {t} \} _ {t = i + 1} ^ {T - 1}) | \\ = O _ {T} (1). \\ \end{array}
$$

Note that in the first equality we also cancelled out the controls at time i which are the same for both trajectories. In the second equality, we used the fact that $\{ w _ { t } \} _ { t = T _ { 0 } } ^ { i - 1 } = \{ w _ { t } ^ { \prime } \} _ { t = T _ { 0 } } ^ { i - 1 }$ , and in the final line we used Equation (152). □
