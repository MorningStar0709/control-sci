$$
\begin{array}{l} \sum_ {t = 0} ^ {T _ {r}} \| (\Theta_ {*} - \tilde {\Theta} _ {t}) ^ {\intercal} z _ {t} \| ^ {2} \leq \sum_ {t = 0} ^ {T _ {r}} \left(1 + \frac {(1 + \kappa^ {2}) (n + d) ^ {2 (n + d)}}{\mu}\right) ^ {\tau_ {0}} \| V _ {\tau} ^ {1 / 2} (\tilde {\Theta} _ {\tau} - \Theta_ {*}) \| ^ {2} \| z _ {t} \| _ {V _ {t} ^ {- 1}} ^ {2} (120) \\ \leq \sum_ {t = 0} ^ {T _ {r}} \left(1 + \frac {(1 + \kappa^ {2}) (n + d) ^ {2 (n + d)}}{\mu}\right) ^ {\tau_ {0}} \left(\beta_ {T} (\delta) + v _ {T} (\delta)\right) ^ {2} \| z _ {t} \| _ {V _ {t} ^ {- 1}} ^ {2}, (121) \\ \leq \frac {2 (1 + \kappa^ {2}) (n + d) ^ {2 (n + d)}}{\mu} \left(1 + \frac {(1 + \kappa^ {2}) (n + d) ^ {2 (n + d)}}{\mu}\right) ^ {\tau_ {0}} (\beta_ {T} (\delta) + v _ {T} (\delta)) ^ {2} \log \left(\frac {\det (V _ {T _ {r}})}{\det (\mu I)}\right) (122) \\ \end{array}
$$

where in (121) we used the fact that on the event of $E _ { T }$ , using triangle inequality, we have $\lVert \tilde { \Theta } _ { \tau } -$ $\Theta _ { * } \| _ { V _ { \tau } } \le \| \tilde { \Theta } _ { \tau } - \hat { \Theta } _ { \tau } \| _ { V _ { \tau } } + \| \hat { \Theta } _ { \tau } - \Theta _ { * } \| _ { V _ { \tau } } \le v _ { \tau } ( \delta ) + \beta _ { \tau } ( \delta ) \le v _ { T } ( \delta ) + \beta _ { T } ( \delta )$ and in (122) we used used the upper bound of $\| z _ { t } \| _ { V _ { \star } ^ { - 1 } }$ to utilize Lemma 10 of Abbasi-Yadkori and Szepesv´ari (2011). Similarly, on the even of $E _ { t } .$ , for $t > T _ { r }$ , we get:

$$\sum_ {t = T _ {r} + 1} ^ {T} \| (\Theta_ {*} - \tilde {\Theta} _ {t}) ^ {\intercal} z _ {t} \| ^ {2} \leq \frac {2 (1 + \kappa^ {2}) X _ {s} ^ {2}}{\mu} \left(1 + \frac {(1 + \kappa^ {2}) X _ {s} ^ {2}}{\mu}\right) ^ {\tau_ {0}} (\beta_ {T} (\delta) + \upsilon_ {T} (\delta)) ^ {2} \log \left(\frac {\det (V _ {T})}{\det (V _ {T _ {r}})}\right)$$

Lemma 20 (Bounding $R _ { T } ^ { \bf R L S }$ for TSAC) Let $R _ { T } ^ { R L S }$ be as defined by (113). Under the event of $E _ { T }$ , setting $\mu = ( 1 + \kappa ^ { 2 } ) X _ { s } ^ { 2 }$ , we have

$$\left| R _ {T} ^ {R L S} \right| = \tilde {O} \left((n + d) ^ {(\tau_ {0} + 2) (n + d) + 1. 5} \sqrt {n} \sqrt {T _ {r}} + (n + d) n \sqrt {T - T _ {r}}\right).$$

Proof
