for $t \in ( t _ { i } ^ { s } , t _ { i + 1 } ^ { s } ) , j \in \mathbb { N }$ where $\varrho > 0$ is given by (35). Using Cauchy-Schwarz inequality and (11) under the consideration of (47), we obtain that $( \dot { U } _ { j } ^ { s } ) ^ { 2 } \leq 2 \| k \| ^ { 2 } V ( t _ { j } ^ { s } )$ . Thus, recalling Assumption 1 from which it follows that $q \dot { > } \lambda / 2 \varepsilon$ for $\theta _ { 1 } = 0$ , we write (48) as

$$\dot {V} \leq 2 \varrho V (t) + \frac {2 \varepsilon^ {2} \| k \| ^ {2}}{\lambda} V (t _ {j} ^ {s}) + \left(\frac {\varepsilon^ {2} p _ {1 0} ^ {2}}{\lambda} + \frac {1}{2}\right) \tilde {u} ^ {2} (1, t), \tag {49}$$

for $t \in ( t _ { i } ^ { s } , t _ { i + 1 } ^ { s } ) , j \in \mathbb { N }$ . Now let us find a known upper-bound for |u˜(1, t)|. From Lemma 1 of [15], we obtain that $\| \tilde { w } _ { x } [ t ] \| \leq$ $( \| \dot { w } _ { x } [ 0 ] \| + M _ { 1 } \| \tilde { w } [ 0 ] \| ) e ^ { - \sigma ^ { * } t }$ and $\lVert \tilde { w } [ t ] \rVert \leq \lVert \tilde { w } [ 0 ] \rVert e ^ { - \sigma ^ { * } t }$ for all $t \geq 0$ where w˜ is the observer error target state obtained via (9) with $\theta _ { 1 } = 0 , \theta _ { 2 } = 1$ (see [15] for details), $M _ { 1 }$ is given by (37), and $\sigma ^ { * }$ is given by (35). Thus, using (8), Agmon’s and Young’s inequalities, we obtain that

$$
\begin{array}{l} | \tilde {u} (1, t) | = | \tilde {w} (1, t) | \leq (\sqrt {2}) ^ {- 1} \| \tilde {w} [ t ] \| + (\sqrt {2}) ^ {- 1} \| \tilde {w} _ {x} [ t ] \| \\ \leq (\sqrt {2}) ^ {- 1} (M _ {1} + 1) \| \tilde {w} [ 0 ] \| e ^ {- \sigma^ {*} t} + (\sqrt {2}) ^ {- 1} \| \tilde {w} _ {x} [ 0 ] \| e ^ {- \sigma^ {*} t}, \tag {50} \\ \end{array}
$$

for all $t \geq 0$ . But, using (9) with $\theta _ { 1 } = 0 , \theta _ { 2 } = 1$ , and Cauchy-Schwarz inequality, we show that $\lVert \tilde { w } [ 0 ] \rVert \leq \Omega _ { 1 } \lVert \tilde { u } [ 0 ] \rVert$ and $\lVert \tilde { w } _ { x } [ 0 ] \rVert \leq \lVert \tilde { u } _ { x } [ 0 ] \rVert + \Omega _ { 2 } \lVert \tilde { u } [ 0 ] \rVert$ where $\Omega _ { 1 }$ and $\Omega _ { 2 }$ are given by (38) and (39), respectively. Again using Cauchy-Schwarz inequality, we obtain from (7) that $\lVert \tilde { u } [ 0 ] \rVert \leq \lVert u [ 0 ] \rVert + \lVert \hat { u } [ 0 ] \rVert$ and $\| \tilde { u } _ { x } [ 0 ] \| \leq \| u _ { x } [ 0 ] \| + \| \hat { u } _ { x } [ 0 ] \|$ . Thus, from (50), we obtain the following bound for $\tilde { u } ^ { 2 } ( 1 , t )$

$$| \tilde {u} (1, t) | \leq \Psi_ {0} e ^ {- \sigma^ {*} t}, \tag {51}$$
