<details>
<summary>text_image</summary>

L_k
Ĵ_k^o
k
</details>

Fig. 4: Schematic of the critic and a Lyapunov function behavior under Algorithm 3.

whence

$$\forall k \in \mathbb {N} _ {0} \| X _ {k \delta} \| \leq \alpha_ {\mathrm{low}} ^ {- 1} (\alpha_ {\mathrm{up}} (\hat {\alpha} _ {\mathrm{low}} ^ {- 1} (\hat {J} _ {0} ^ {\circ}))),$$

Thus, an overshoot bound $S ^ { * }$ may be set so as to satisfy:

$$S ^ {*} > \alpha_ {\text { low }} ^ {- 1} (\alpha_ {\text { up }} (\hat {\alpha} _ {\text { low }} ^ {- 1} (\hat {J} _ {0} ^ {\circ}))). \tag {43}$$

Now, if $X _ { k \delta } \notin \mathcal { B } _ { s ^ { * } }$ , then

$$\nu (X _ {k \delta}) > 6 \mathrm{Lip} _ {J} \sigma_ {\max} \mathrm{Lip} _ {Z}. \tag {44}$$

We have, denoting $\Delta \bullet _ { k } \triangleq \bullet _ { k + 1 } - \bullet _ { k } \colon$

$$\forall k \in \hat {\mathbb {K}} \Delta \hat {J} _ {k} ^ {\circ} \leq - 6 \operatorname{Lip} _ {J} \sigma_ {\max} \operatorname{Lip} _ {Z} \delta , \tag {45}\forall k \in \mathbb {K} _ {L} \Delta L _ {k} \leq - 6 \mathrm{Lip} _ {J} \sigma_ {\max} \mathrm{Lip} _ {Z} \delta .$$

If the critic always succeeded, $B _ { s }$ ∗ would be reached after no more than

$$\hat {T} := \frac {\hat {J} _ {0} ^ {\circ} - \hat {\alpha} _ {\text { low }} (s ^ {*})}{6 \operatorname{Lip} _ {J} \sigma_ {\max} \operatorname{Lip} _ {Z} \delta} \tag {46}$$

steps. If the stabilizing policy were always invoked instead, $\boldsymbol { B } _ { s ^ { * } }$ would be reached after no more than

$$T _ {L} := \frac {L _ {0} - \alpha_ {\mathrm{low}} (s ^ {*})}{6 \mathrm{Lip} _ {J} \sigma_ {\mathrm{max}} \mathrm{Lip} _ {Z} \delta}$$

steps.

But, unlike $\hat { J } _ { 0 } ^ { \circ }$ , the Lyapunov function $L _ { k }$ may step-wise increase when $\textit { k } \in \hat { \mathbb { K } }$ (see Figure 4). However, $L _ { k }$ never exceeds $\alpha _ { \mathrm { u p } } \big ( \hat { \alpha } _ { \mathrm { l o w } } ^ { - 1 } \big ( \hat { J } _ { 0 } ^ { \circ } \big ) \big )$ as shown above. Thus, let us define

$$T _ {L} := \frac {\alpha_ {\mathrm{up}} (\hat {\alpha} _ {\mathrm{low}} ^ {- 1} (\hat {J} _ {0} ^ {\circ})) - \alpha_ {\mathrm{low}} (s ^ {*})}{6 \mathrm{Lip} _ {J} \sigma_ {\max} \mathrm{Lip} _ {Z} \delta}. \tag {47}$$

This is a reaching time of $B _ { s }$ ∗ if only η were invoked after a worst growth of $L _ { k }$ after a successful critic update. We say $\mathbf { \ddot { a } }$ reaching time” to stress that it is effectively just a bound. Now, let $T ^ { * } : =$ max $\{ \hat { T } , T _ { L } \}$ }.
