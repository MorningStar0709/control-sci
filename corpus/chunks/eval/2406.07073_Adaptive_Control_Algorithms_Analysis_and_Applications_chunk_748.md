1. H (z) given by (C.3) is a positive real discrete time transfer matrix.   
2. The system (C.1)–(C.2) is passive.   
3. There is a positive definite matrix P , a positive semidefinite matrix Q and matrices S and R such that:

$$A ^ {T} P A - P = - Q \tag {C.4}C - B ^ {T} P A = S ^ {T} \tag {C.5}D + D ^ {T} - B ^ {T} P B = R \tag {C.6}
M = \left[ \begin{array}{l l} Q & S \\ S ^ {T} & R \end{array} \right] \geq 0 \tag {C.7}
$$

4. There is a positive definite matrix P and matrices K and L such that:

$$A ^ {T} P A - P = - L L ^ {T} \tag {C.8}C - B ^ {T} P A = K ^ {T} L ^ {T} \tag {C.9}D + D ^ {T} - B ^ {T} P B = K ^ {T} K \tag {C.10}$$

5. Every solution x(t), (x(0), u(t)) of (C.1), (C.2) satisfies the following equality:

$$
\begin{array}{l} \sum_ {t = 0} ^ {t _ {1}} y ^ {T} (t) u (t) = \frac {1}{2} x ^ {T} \left(t _ {1} + 1\right) P x \left(t _ {1} + 1\right) - \frac {1}{2} x ^ {T} (0) P x (0) \\ + \frac {1}{2} \sum_ {t = 0} ^ {t _ {1}} [ x ^ {T} (t), u ^ {T} (t) ] M \left[ \begin{array}{l} x (t) \\ u (t) \end{array} \right] \tag {C.11} \\ \end{array}
$$

6. The system (C.1)–(C.2) is hyperstable.

Proof Detailed proof can be found in Hitz and Anderson (1969), Popov (1973). Property (2) results immediately from property (5) $\begin{array} { r } { ( \gamma ^ { 2 } = - \frac { 1 } { 2 } x ^ { T } ( 0 ) P x ( 0 ) ) } \end{array}$ . Equivalence between (3) and (4) results immediately from (C.7), which taking into account the properties of positive semidefinite matrices can be expressed as:

$$
\left[ \begin{array}{c c} Q & S \\ S ^ {T} & R \end{array} \right] = N N ^ {T} = \left[ \begin{array}{c} L \\ K ^ {T} \end{array} \right] [ L ^ {T} K ] = \left[ \begin{array}{c c} L L ^ {T} & L K \\ K ^ {T} L ^ {T} & K ^ {T} K \end{array} \right] \geq 0
$$

where L is a $( n \times q )$ -dimensional matrix and $K ^ { T }$ is a $( m \times q )$ -dimensional matrix. Replacing Q by $L L ^ { T } , S ^ { T }$ by $K ^ { T } L ^ { T }$ and R by $K ^ { T } K$ in (C.4) through (C.6), one gets (C.8) through (C.10). We will limit ourselves to show that $( 3 )  ( 5 )$ , since this will be used subsequently for other proofs.

Using (C.4) and (C.1), one has:

$$
\begin{array}{l} x ^ {T} (t) Q x (t) = - x ^ {T} (t + 1) P x (t + 1) + x ^ {T} (t) P x (t) \\ + u ^ {T} (t) B ^ {T} P x (t + 1) + x ^ {T} (t + 1) P B u (t) - u ^ {T} (t) B ^ {T} P B u (t) \\ = - x ^ {T} (t + 1) P x (t + 1) + x ^ {T} (t) P x (t) + u ^ {T} (t) B ^ {T} P [ A x (t) + B u (t) ] \\ + \left[ x ^ {T} (t) A ^ {T} + u ^ {T} (t) B ^ {T} \right] P B u (t) - u ^ {T} (t) B ^ {T} P B u (t) \tag {C.12} \\ \end{array}
$$
