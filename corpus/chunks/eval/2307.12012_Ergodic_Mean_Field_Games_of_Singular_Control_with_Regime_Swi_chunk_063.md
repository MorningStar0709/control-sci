uniformly in compact sets of $\left[ 0 , T \right] \mathrm { { a s } } \ n \uparrow \infty$ . Defining $X : = \dim _ { n \to \infty } \Gamma ( X ^ { ( n ) } )$ , and taking $\xi =$ $\xi ^ { + } - \xi ^ { - } = \Xi ( X )$ , we write $\overset { \cdot } { X ^ { \xi } } \overset { \cdot } { = } I ( X ^ { \xi } ) + \xi$ so that the pair $( X ^ { \xi } , \xi )$ satisfies the properties of Skorokhod problem in Definition 3.1 for every $t \in [ 0 , T ]$ . To extend the solution to $[ 0 , \infty )$ , we observe that (B.3) does not depend from the initial conditions. Hence, the result holds on each interval with length T and, by writing $\begin{array} { r } { [ 0 , \infty ) : = \bigcup _ { n = 1 } ^ { \infty } [ ( n - 1 ) T , n T ) } \end{array}$ , the proof is complete. 

The next Proposition establishes a comparison principle for the optimally controlled state with respect to the mean-field parameter.

Proposition B.2. Let $x _ { 1 } , x _ { 2 } \in \mathcal { T }$ and $\theta _ { 1 } , \theta _ { 2 } \in \mathbb { R } _ { + }$ such that $x _ { 1 } \leq x _ { 2 }$ and $\theta _ { 1 } \leq \theta _ { 2 }$ then it is true that:

$$X _ {t} ^ {x _ {1}, \xi^ {*} (\theta_ {1})} \leq X _ {t} ^ {x _ {2}, \xi^ {*} (\theta_ {2})}, \mathbb {P} \otimes d t - a. s. \tag {B.6}$$

Proof. Let $\{ X ^ { ( n , x , \theta ) } \} _ { n \in \mathbb { N } }$ to be a sequence of F-adapted processes such that

$$d X _ {t} ^ {(n, x, \theta)} = \left(b (X _ {t} ^ {(n, x, \theta)}, Y _ {t}) + g _ {n} ^ {(\theta)} (X _ {t} ^ {(n, x, \theta)}, Y _ {t})\right) d t + \sigma (X _ {t} ^ {(n, x, \theta)}, Y _ {t}) d W _ {t}, \tag {B.7}$$

where,

$$
g _ {n} ^ {(\theta)} (x, i) := \left\{ \begin{array}{l l} 0, & x \in [ \alpha_ {i} (\theta), \beta_ {i} (\theta) ] \\ - n (x - \alpha_ {i} (\theta)), & x <   \alpha_ {i} (\theta) \\ - n (x - \beta_ {i} (\theta)), & x > \beta_ {i} (\theta). \end{array} \right.
$$

Let $\{ \tau _ { k } \} _ { k \in \mathbb { N } }$ be the sequence of random times where the jumps of Y occurred. Thanks to Proposition 5.2.18 in [38] and Lemma 4.1, by an induction argument we find

$$X _ {t} ^ {(n, x _ {1}, \theta_ {1})} \leq X _ {t} ^ {(n, x _ {2}, \theta_ {2})}, \quad \mathbb {P} \text {-a.s. for any} t \in [ \tau_ {k}, \tau_ {k + 1}), \quad \text {for any} k \in \mathbb {N} _ {0}.$$

Then, using Theorem 1.4.1 in [47] (with estimates as in Proposition 2.3 in [53]), we obtain, $\mathbb { P } \otimes d t { - } \mathbf { a } . \mathbf { s } .$ ,

$$X _ {t} ^ {x _ {1}, \xi^ {*} (\theta_ {1})} = \lim _ {n \uparrow \infty} X _ {t} ^ {(n, x _ {1}, \theta_ {1})} \leq \lim _ {n \uparrow \infty} X _ {t} ^ {(n, x _ {2}, \theta_ {2})} = X _ {t} ^ {x _ {2}, \xi^ {*} (\theta_ {2})},$$
