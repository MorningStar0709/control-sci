# H.1 Proof of Proposition 11

proof. By Lemma 25, $a ^ { * } - b ^ { * } F _ { \mathrm { o p t } } ( \theta ^ { * } ) < 1 - c _ { \mathrm { L 2 5 } }$ , which implies by Lemma 28 that ${ \bar { J } } ( \theta ^ { * } , C _ { F } ^ { \mathrm { u n c } } )$ is twice differentiable at the point $F = F _ { \mathrm { o p t } } ( \theta ^ { * } )$ with first and second derivatives that are both finite and independent of T . We also have by Lemma 23 that $\begin{array} { r } { | F _ { \mathrm { o p t } } ( \hat { \theta } _ { \mathrm { w u } } ) - F _ { \mathrm { o p t } } ( \theta ^ { * } ) | \leq } \end{array}$ $\tilde { O } _ { T } ( T ^ { - 1 / 4 } )$ conditional on event $E _ { 2 } ^ { 0 }$ . Therefore, conditional on event $E _ { 2 } ^ { 0 }$ and for sufficiently large $T$ , we can do a second order Taylor expansion of ${ \bar { J } } ( \theta ^ { * } , C _ { F } ^ { \mathrm { u n c } } )$ around $F = F _ { \mathrm { o p t } } ( \theta ^ { * } )$ to get that

$$\left| T \cdot \bar {J} \left(\theta^ {*}, C _ {F _ {\text {opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\text {unc}}\right) - T \cdot \bar {J} \left(\theta^ {*}, C _ {F _ {\text {opt}} (\theta^ {*})} ^ {\text {unc}}\right) \right| = \tilde {O} _ {T} (\sqrt {T}). \tag {142}$$

Because the lowest-cost unconstrained linear controller $C _ { F _ { \mathrm { o p t } } ( \theta ^ { * } ) } ^ { \mathrm { u n c } }$ has the lowest cost among all unconstrained controllers Anderson and Moore [2007],

$$T \cdot \bar {J} (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\theta^ {*})} ^ {\mathrm{unc}}) - T \cdot \bar {J} (\theta^ {*}, C _ {K _ {\mathrm{opt}} (\theta^ {*}, T)} ^ {\theta^ {*}}) \leq 0. \tag {143}$$

Combining Equations (142) and (143) and multiplying by $( T - T _ { 0 } ) / T$ , we have

$$(T - T _ {0}) \cdot \bar {J} (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}) - (T - T _ {0}) \cdot \bar {J} (\theta^ {*}, C _ {K _ {\mathrm{opt}} (\theta^ {*}, T)} ^ {\theta^ {*}}) = \tilde {O} _ {T} (\sqrt {T}). \tag {144}$$

Now we just need to convert this to a result about finite time cost rather than infinite cost which requires the following lemma.

Lemma 37. Under Assumptions 1–3 and 4, for any $\theta \in \Theta$ and K satisfying $1 - ( a - b K ) =$ $\epsilon > 0$ for some $\epsilon = \Omega _ { T } ( 1 )$ ,

$$| \bar {J} (\theta , C _ {K} ^ {\mathrm{unc}}, T) - \bar {J} (\theta , C _ {K} ^ {\mathrm{unc}}) | = O _ {T} \left(\frac {1}{T}\right).$$

The proof of Lemma 37 can be found in Appendix H.5.

For sufficiently large T , conditional on event $E _ { 2 } ^ { 0 } { \mathrm { : } }$
