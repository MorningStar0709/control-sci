$$\| \tilde {z} (\cdot , t) \| _ {\mathcal {H}} + | \tilde {\zeta} (t) - \zeta_ {*} | \rightarrow 0 \text {as} t \rightarrow + \infty , \tag {7.7}$$

where $\zeta _ { * }$ is a constant that may not be zero. Since the ˇz-subsystem of (7.1) is a well-known exponentially stable heat equation with inhomogeneous terms $- q \tilde { z } ( 0 , t )$ and $c _ { 1 } \tilde { z } ( 1 , t )$ , we can conclude from [5, Lemma 3.1] that $\| \tilde { z } ( \cdot , t ) \| _ { \mathcal { H } } \to 0$ as $t \to + \infty$ . As a result, the boundedness (7.3) follows immediately from (7.7).

By (7.6), it follows that

$$\lim _ {t \to + \infty} \int_ {t} ^ {t + \tau} \check {z} ^ {2} (1, s) d s = 0, \forall \tau > 0. \tag {7.8}$$

H¨older’s inequality yields

$$\int_ {t} ^ {t + \tau} | \check {z} (1, s) | d s \leq \sqrt {\tau} \left(\int_ {t} ^ {t + \tau} \check {z} ^ {2} (1, s) d s\right) ^ {1 / 2}, \forall \tau > 0. \tag {7.9}$$

Consequently, it follows from (7.8), (7.9) and the assumption (7.4) that

$$\lim _ {t \to + \infty} \int_ {t} ^ {t + \tau} u _ {0} (s) d s = \lim _ {t \to + \infty} \int_ {t} ^ {t + \tau} d (s) d s \neq 0. \tag {7.10}$$

Note that the $\tilde { z } , \tilde { \zeta } { \mathrm { - s u b s y s t e m } }$ of (7.1) is equivalent to system (2.7), we employ Lemma 5.2 to get $\zeta _ { * } = 0$ . So (7.5) holds. □

Proof of Theorem 4.1. Let $d ( t ) = v _ { x } ( 1 , t )$ , where $v _ { x } ( 1 , t )$ is given by (4.4). Owing to the assumption (4.12), $\| d \| _ { L ^ { \infty } ( 0 , \infty ) } < + \infty$ and

$$\sup _ {t \geq 0} \| v (\cdot , t) \| _ {\mathcal {H}} < + \infty , \tag {7.11}$$

where v is given by (4.2). By Lemma 7.1, the transformed system (7.1) admits a weak solution $( \tilde { z } ( \cdot , t ) , \check { z } ( \cdot , t ) , \tilde { \zeta } ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ such that (7.2) and (7.3) hold. In terms of the operators Π and $\Upsilon _ { b }$ that are given by (6.37) and (6.39) respectively, we define

$$
\left( \begin{array}{c} z (\cdot , t) \\ \hat {z} (\cdot , t) \\ \zeta (t) \end{array} \right) = \left( \begin{array}{c c c} 1 & \Pi^ {- 1} & 0 \\ 0 & \Pi^ {- 1} & 0 \\ 0 & 0 & \Upsilon_ {b} \end{array} \right) \left( \begin{array}{c} \tilde {z} (\cdot , t) \\ \check {z} (\cdot , t) \\ \tilde {\zeta} (t) \end{array} \right). \tag {7.12}
$$

A direct computation shows that such a defined $( z ( \cdot , t ) , \hat { z } ( \cdot , t ) , \zeta ( t ) ) \in C ( 0 , \infty ; \mathcal { H } ^ { 2 } \times \mathbb { R } )$ is governed by
