# 10 Appendix

Lemma 10.1. Let $\omega > 0$ . Suppose that the functions γ1, $\gamma _ { 2 } \in L ^ { 2 } ( 0 , \infty )$ and $\gamma _ { 3 } \in L ^ { \infty } ( 0 , \infty )$ . If the nonnegative function η satisfies the differential inequality

$$\dot {\eta} (t) \leq - \omega \eta (t) + [ \gamma_ {1} (t) + \gamma_ {3} (t) ] \gamma_ {2} (t), \tag {10.1}$$

then η(t) → 0 as $t \to + \infty$ .

Proof. By the Cauchy’s inequality,

$$| [ \gamma_ {1} (t) + \gamma_ {3} (t) ] \gamma_ {2} (t) | \leq \frac {1}{2} \gamma_ {1} ^ {2} (t) + \frac {1}{2} \gamma_ {2} ^ {2} (t) + | \gamma_ {2} (t) \gamma_ {3} (t) |.$$

If we let $\begin{array} { r } { g _ { 1 } ( t ) = | \gamma _ { 2 } ( t ) \gamma _ { 3 } ( t ) | , g _ { 2 } ( t ) = \frac { 1 } { 2 } \gamma _ { 1 } ^ { 2 } ( t ) + \frac { 1 } { 2 } \gamma _ { 2 } ^ { 2 } ( t ) } \end{array}$ , then $g _ { 1 } \in L ^ { 2 } ( 0 , \infty )$ and $g _ { 2 } \in L ^ { 1 } ( 0 , \infty )$ . Furthermore,

$$\dot {\eta} (t) \leq - \omega \eta (t) + g _ {1} (t) + g _ {2} (t). \tag {10.2}$$

Since $g _ { 1 } \in L ^ { 2 } ( 0 , \infty )$ , for any $\sigma > 0$ , there exists a $t _ { 1 } > 0$ such that $\| g _ { 1 } \| _ { L ^ { 2 } ( t _ { 1 } , \infty ) } < \sigma$ . It follows from the H¨older’s inequality that

$$\int_ {t _ {1}} ^ {t} e ^ {- \omega (t - s)} g _ {1} (s) d s \leq \left(\int_ {t _ {1}} ^ {t} e ^ {- 2 \omega (t - s)} d s\right) ^ {1 / 2} \left(\int_ {t _ {1}} ^ {t} g _ {1} ^ {2} (s) d s\right) ^ {1 / 2} \leq \frac {\| g _ {1} \| _ {L ^ {2} (t _ {1} , \infty)}}{\sqrt {2 \omega}} < \frac {\sigma}{\sqrt {2 \omega}}. \tag {10.3}$$

Since $g _ { 2 } \in L ^ { 1 } ( 0 , \infty )$ , for any $\sigma > 0$ , there exists a $t _ { 2 } > 0$ such that $\| g _ { 2 } \| _ { L ^ { 1 } ( t _ { 2 } , \infty ) } < \sigma$ , which means that

$$\int_ {t _ {2}} ^ {t} e ^ {- \omega (t - s)} g _ {2} (s) d s \leq \int_ {t _ {2}} ^ {t} g _ {2} (s) d s \leq \| g _ {2} \| _ {L ^ {1} (t _ {2}, \infty)} < \sigma . \tag {10.4}$$

Choosing $t _ { 0 } = \operatorname* { m a x } \{ t _ { 1 } , t _ { 2 } \}$ and taking (10.3) and (10.4) into account, we solve (10.2) to get

$$
\begin{array}{l} \eta (t) \leq e ^ {- \omega t} \eta (0) + e ^ {- \omega \left(t - t _ {0}\right)} \int_ {0} ^ {t _ {0}} e ^ {- \omega \left(t _ {0} - s\right)} \left[ g _ {1} (s) + g _ {2} (s) \right] d s + \int_ {t _ {0}} ^ {t} e ^ {- \omega (t - s)} \left[ g _ {1} (s) + g _ {2} (s) \right] d s \tag {10.5} \\ \leq e ^ {- \omega t} \eta (0) + e ^ {- \omega (t - t _ {0})} \int_ {0} ^ {t _ {0}} e ^ {- \omega (t _ {0} - s)} [ g _ {1} (s) + g _ {2} (s) ] d s + \left(\frac {1}{\sqrt {2 \omega}} + 1\right) \sigma . \\ \end{array}
$$

Passing to the limit as t  + in (10.5), we obtain

$$\lim _ {t \to + \infty} \eta (t) \leq \left(\frac {1}{\sqrt {2 \omega}} + 1\right) \sigma , \tag {10.6}$$

which leads to η(t)  0 as t  + due to the arbitrariness of σ.
