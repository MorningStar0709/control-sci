where $\mathcal { M } = \{ 1 , 2 , \dots , M \}$ and $\mathcal { V } = \{ D , 2 , 3 , \ldots , | V | \}$ . The first decision variable $\dot { v } \in \mathbb { B } ^ { | V | \times | V | \times | M | }$ contains the binary decision variables $v _ { i , j } ^ { m }$ that describe whether agent m is routed along edge $e _ { i , j }$ . The objective function therefore maximizes how many total nodes are visited collectively by the agents. The second decision variable $\boldsymbol { x } \in \mathbb { R } ^ { n \times \bar { T } \times m }$ tracks all states $\boldsymbol { x } _ { t } ^ { m }$ of each mth agent at each time step $t \in [ 0 , \bar { T } ]$ under the inputs $u \in \mathbb { R } ^ { u \times ( \breve { \bar { T } } - 1 ) \times m }$ . $T \in \mathbb { R } ^ { | V | \times \mathbf { \bar { M } } }$ contains the time steps $T _ { j } ^ { m }$ when agent m reaches node $v _ { j }$ . If agent m never reaches node $v _ { j } , T _ { j } ^ { m } = 0$ . The optimal control problem (8) simultaneously searches for a path $v ^ { \star }$ that maximizes the number of nodes reached by agents, and a sequence of continuous actuation inputs $u ^ { \star }$ that result in the agents visiting the nodes before returning to the depot. Constraints (62b) - (62c) ensure that each node is visited by at most one agent, and constraint (62d) ensures that the planned path is continuous. Constraints (62f) - (62g) describe the dynamics and system constraints of the agent, including all capacity state constraints. The constraints (62h)-(62i) indicate that at time $t ~ = ~ 0$ and $t \ = \ T ^ { m }$ , corresponding to the start and end of the task, agent m is at the depot. Constraint (62j) ensures that the agent reaches each node $v _ { j }$ assigned to it at some time $T _ { j } ^ { m }$ . Lastly, constraints (62k)-(62l) ensure that the agent visits assigned nodes in the order indicated by $v ^ { \star }$ . This problem formulation is based on the Flow Model formulation of the capacitated vehicle routing problem. For more details on this formulation, we refer to [25].
