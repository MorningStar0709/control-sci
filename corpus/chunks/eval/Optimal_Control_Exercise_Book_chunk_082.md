Combined with the nonlinear differential equation, we can notice that the only way to obtain 0 is to make $c _ { 1 } \to \infty$ . This means that, indeed, s(t) is independent from time and is a constant $s \in \mathbb { R } ^ { }$ :

$$s (t) = s = 0 \quad \forall t \in [ 0, 1 ] \tag {380}$$

The optimal value function is therefore independent from time and will be:

$$\hat {V} (x) = s x ^ {2} = 0 \tag {381}$$

Which yields the optimal control $u ^ { * } ( t )$ :

$$u ^ {*} (t) = 0, \quad \forall t \in [ 0, 1 ] \tag {382}$$

Since we have that ${ \dot { x } } ( t ) = x ( t ) u ( t ) = 0$ , then the system will keep the initial state $x ( 0 ) = x ( t ) = 1 , \forall t \in [ 0 , 1 ]$ .
