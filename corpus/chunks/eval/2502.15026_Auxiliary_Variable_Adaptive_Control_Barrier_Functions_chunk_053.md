# C. Obstacle Avoidance for Mixed Relative Degree Systems We consider a nonlinear vehicle dynamics in the form

$$
\underbrace {\left[ \begin{array}{c} \dot {x} (t) \\ \dot {y} (t) \\ \dot {\theta} (t) \\ \dot {\phi} (t) \\ \dot {v} (t) \end{array} \right]} _ {\dot {\boldsymbol {x}} (t)} = \underbrace {\left[ \begin{array}{c} v (t) \cos (\theta (t)) \\ v (t) \sin (\theta (t)) \\ \phi (t) \\ 0 \\ 0 \end{array} \right]} _ {f (\boldsymbol {x} (t))} + \underbrace {\left[ \begin{array}{c c} 0 & 0 \\ 0 & 0 \\ 0 & 0 \\ 1 & 0 \\ 0 & \frac {1}{M} \end{array} \right]} _ {g (\boldsymbol {x} (t))} \underbrace {\left[ \begin{array}{c} u _ {1} (t) \\ u _ {2} (t) \end{array} \right]} _ {\boldsymbol {u} (t)}. \tag {61}
$$

In (61), M denotes the mass of the vehicle and $( x , y )$ denote the coordinates of the unicycle, v is its linear speed, θ denotes the heading angle, ϕ denotes the rotation speed, and u represent the angular acceleration $( u _ { 1 } )$ and the driven force $( u _ { 2 } ) .$ , respectively. Vehicle limitations are the same as those in Sec. V-B2. To satisfy the constraint on rechability, we define a CLF $V ( \pmb { x } ( t ) ) : = ( 0 . 1 ( \theta ( t ) - \theta _ { d } ) + \phi ( t ) ) ^ { 2 }$ with θd = atan2( yd−y(t) ) $\begin{array} { r } { \theta _ { d } = a t a n 2 \big ( \frac { y _ { d } - y ( t ) } { x _ { d } - x ( t ) } \big ) , c _ { 1 } = c _ { 2 } = 1 } \end{array}$ xd−x(t) , c1 = c2 = 1 to stabilize θ(t) to θd and $\theta ( t )$ $\theta _ { d }$ ϕ(t) to 0. The relaxed constraint in (6) is then formulated the same as (51) as a soft constraint. The parameters are $v ( 0 ) = 2 m / s , \phi ( 0 ) = 0 . 0 1 r a d / s , M = 1 6 5 0 k g , ( x _ { o } , y _ { o } ) =$ $( 0 , 0 ) , r _ { o } = 1 m , r _ { d } = 0 . 2 m , \triangle t = 0 . 0 1 s , u _ { 1 , \mathrm { { m a x } } } = - u _ { 1 , \mathrm { { m i n } } } =$ $5 r a d / s ^ { 2 } , u _ { 2 , \mathrm { m a x } } = - u _ { 2 , \mathrm { m i n } } = 8 2 5 0 N , T = 5 s$ .
