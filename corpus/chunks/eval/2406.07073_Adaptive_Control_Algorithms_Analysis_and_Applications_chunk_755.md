$$
\begin{array}{l} \lim _ {t _ {1} \rightarrow \infty} \left[ x ^ {T} (t _ {1} + 1) P x (t _ {1} + 1) + \sum_ {t = 0} ^ {t _ {1}} x ^ {T} (t) M _ {0} x (t) + \sum_ {t = 0} ^ {t _ {1}} u ^ {T} (t) K u (t) \right] \\ \leq x (0) P x (0) + \bar {x} ^ {T} (0) \bar {P} (0) \bar {x} (0) \tag {C.48} \\ \end{array}
$$

with $\bar { P } ( t ) , Q ( t ) , S ( t ) , R ( t )$ bounded or unbounded for t > 0. Since $P > 0 , M _ { 0 } >$ $0 , K > 0$ , this implies that:

$$\lim _ {t \to \infty} x (t) = 0; \quad \lim _ {t \to \infty} u (t) = 0; \quad \forall x (0), \forall \bar {x} (0) < \infty \tag {C.49}$$

and from (C.2), it also results that:

$$\lim _ {t \rightarrow \infty} y (t) = 0 \tag {C.50}$$

Bearing in mind that $u ( t ) = - \bar { y } ( t )$ , one has:

$$\lim _ {t \rightarrow \infty} \bar {y} (t) = 0 \tag {C.51}$$

2. From (C.47), taking into account (C.45), one obtains:

$$
\begin{array}{l} \lim _ {t \rightarrow \infty} \left\{\bar {x} ^ {T} (t _ {1} + 1) \bar {P} (t _ {1} + 1) \bar {x} (t _ {1} + 1) \right. \\ \left. + \sum_ {t \to \infty} ^ {t _ {1}} [ \bar {x} ^ {T} (t) \bar {u} ^ {T} (t) ] \left[ \begin{array}{c c} Q (t) & S (t) \\ S ^ {T} (t) & R (t) \end{array} \right] \left[ \begin{array}{c} \bar {x} (t) \\ \bar {u} (t) \end{array} \right] \right\} \\ \leq x ^ {T} (0) P x (0) + \bar {x} ^ {T} (0) \bar {P} (0) \bar {x} (0) \tag {C.52} \\ \end{array}
$$

with $\bar { P } ( t ) , S ( t ) , Q ( t ) , R ( t )$ bounded or unbounded for $t > 0$ . This implies that:

$$
\lim _ {t \rightarrow \infty} [ \bar {x} ^ {T} (t), \bar {u} ^ {T} (t) ] \left[\begin{array}{l l}Q (t)&S (t)\\S ^ {T} (t)&R (t)\end{array}\right]\left[\begin{array}{l}\bar {x} (t)\\\bar {u} (t)\end{array}\right] = 0 \tag {C.53}
$$

3. From (C.52) one also has:

$$\bar {x} ^ {T} (t) \bar {P} (t) \bar {x} (t) \leq C _ {1} < \infty ; \quad \forall t > 0 \tag {C.54}$$

4. Denote

$$V (t) = x ^ {T} (t) P x (t) + \bar {x} ^ {T} (t) \bar {P} (t) \bar {x} (t) \tag {C.55}$$

Again from (C.47), one obtains:

$$
\begin{array}{l} V (t + 1) - V (t) = - \bar {y} ^ {T} (t) [ \Lambda - \Gamma (t) ] \bar {y} (t) - x ^ {T} (t) M _ {0} (t) x (t) \\ - u ^ {T} (t) K u (t) - [ x ^ {T} (t), u ^ {T} (t) ] M \left[ \begin{array}{c} x (t) \\ u (t) \end{array} \right] \\ - \left[ \bar {x} ^ {T} (t), y ^ {T} (t) \right] \bar {M} (t) \left[ \begin{array}{c} \bar {x} (t) \\ y (t) \end{array} \right] \leq 0 \tag {C.56} \\ \end{array}
$$

Since $V ( t ) > 0 ; \forall x ( t ) \neq 0 ; \forall \bar { x } ( t ) \neq 0$ and $V ( t + 1 ) - V ( t ) \leq 0 , V ( t )$ will converge to a fixed value. One has proven already that $x ( t ) \to 0$ . One concludes that:
