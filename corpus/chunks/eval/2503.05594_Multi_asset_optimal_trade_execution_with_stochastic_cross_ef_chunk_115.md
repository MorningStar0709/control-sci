Figure 8: The optimal strategy in the setting of Example C.16, where $T = 1 , x = ( 1 0 0 , 0 ) ^ { \top }$ , $\Xi = 0 , \rho = I _ { 2 } ,$ and with $\gamma$ from (C.36).

Observe that

$$
\begin{array}{l} (\rho + \mathscr {A}) ^ {- 1} \mathscr {A} = \frac {1}{2} O ^ {\top} \left(\operatorname{diag} \left(\widetilde {\rho} _ {1}, \dots , \widetilde {\rho} _ {n}\right) + \frac {1}{2} \boldsymbol {\mu}\right) ^ {- 1} O O ^ {\top} \boldsymbol {\mu} O \\ = \frac {1}{2} O ^ {\top} \boldsymbol {\mu} \big (\operatorname{diag} (\widetilde {\rho} _ {1}, \dots , \widetilde {\rho} _ {n}) + \frac {1}{2} \boldsymbol {\mu} \big) ^ {- 1} O \\ = \mathcal {A} O ^ {\top} \big (\mathrm{diag} (\widetilde {\rho} _ {1}, \ldots , \widetilde {\rho} _ {n}) + \frac {1}{2} \pmb {\mu} \big) ^ {- 1} O = \mathcal {A} (\rho + \mathcal {A}) ^ {- 1}. \\ \end{array}
$$

This, (C.34), and (C.35) demonstrate that $D ^ { * }$ is constant on $( 0 , T )$ . Since $\gamma ^ { - { \frac { 1 } { 2 } } } , \theta ,$ , and $\mathcal { H } ^ { \ast }$ here all are of finite variation, we obtain that the optimal strategy, which is given by $X ^ { * } ( 0 - ) = 0 , X ^ { * } ( T ) = 0$ , and $X ^ { \ast } ( s ) = \gamma ^ { - \frac { 1 } { 2 } } ( s ) ( \theta ( s ) - I _ { n } ) \mathcal { H } ^ { \ast } ( s ) , s \in [ 0 , T )$ , has finite variation. Further, (16) and (17) are satisfied. We thus conclude that $X ^ { \ast } \in \mathcal { A } ^ { f v }$ . □

Note that the price impact $\gamma _ { : }$ , the BSDE solution ${ \mathcal { V } } \equiv Y$ , the auxiliary process $\theta ,$ the optimal state $\mathcal { H } ^ { \ast }$ , the optimal strategy $X ^ { \ast }$ , and the optimal deviation $D ^ { * }$ in Lemma C.15 are deterministic. To point out that our model allows for stochastic liquidity, we next provide a simulation of the optimal strategy in a setting with cross-impact where the price impact $\gamma$ is a (non-deterministic) stochastic process and where the resulting optimal strategy is no longer deterministic.

Example C.16. Within Setting C.12 let $n = 2 , m = 1 , d = 0 , x = ( 1 0 0 , 0 ) ^ { \top } , \lambda _ { 1 } ( 0 ) =$ $1 = \lambda _ { 2 } ( 0 ) , \widetilde { \rho } _ { 1 } = 1 = \widetilde { \rho } _ { 2 } , \mu = ( 3 , 1 ) ^ { \top }$ , and $\sigma = ( 1 , 1 ) ^ { \top }$ . Furthermore, let O be defined as e ein (C.29) in Example C.14. Note that the only difference in the present set-up to the one in Example C.14 is that we now include a non-zero $\sigma .$ . The price impact $\gamma$ is then given by
