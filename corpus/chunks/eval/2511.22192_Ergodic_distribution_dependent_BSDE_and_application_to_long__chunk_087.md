Lemma 5.3. We assume that $( \mathcal { H } _ { O C P } )$ holds. For $( Y ^ { T , x , [ \theta ] } , Z ^ { T , x , [ \theta ] } )$ the solution to (Decoupled BSDE), we have that, for all admissible control ??,

$$J ^ {T} (x, [ \theta ], \boldsymbol {a}) \geq Y _ {0} ^ {T, x, [ \theta ]}.$$

Moreover, if the infimum is attained for every $x , z \in \mathbb { R } ^ { d }$ in (5.3), there exists $\bar { \pmb a } ^ { T }$ an admissible control s.t. $J ^ { T } ( x , [ \theta ] , \bar { \pmb { a } } ^ { T } ) =$ $Y _ { 0 } ^ { T , x , [ \theta ] }$ where

$$\bar {a} _ {t} ^ {T} := \varphi \left(X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}} ^ {T}}, \left[ X _ {t} ^ {[ \theta ], 0} \right], Z _ {t} ^ {T, x, [ \theta ]}\right) = \varphi \left(X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}} ^ {T}}, \left[ X _ {t} ^ {[ \theta ], 0} \right], \nabla_ {x} u ^ {T} (t, X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}} ^ {T}}, \left[ X _ {t} ^ {[ \theta ], 0} \right]) \sigma (X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}} ^ {T}}, \left[ X _ {t} ^ {[ \theta ], 0} \right])\right).$$

Lemma 5.4. We assume that $( \mathcal { H } _ { O C P } )$ holds. For $( Y ^ { x , [ \theta ] } , Z ^ { x , [ \theta ] } , \lambda )$ the solution to (Decoupled EBSDE), we have that, for all admissible control ??,

$$J (x, [ \theta ], \boldsymbol {a}) \geq \lambda .$$

Moreover, if the infimum is attained for every $x , z \in \mathbb { R } ^ { d }$ in (5.3), there exists ??¯ an admissible control s.t $J ( x , [ \theta ] , \bar { a } ) = \lambda$ , where

$$\bar {a} _ {t} = \varphi (X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}}}, \left[ X _ {t} ^ {[ \theta ], 0} \right], Z _ {t} ^ {x, [ \theta ]}) = \varphi (X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}}}, \left[ X _ {t} ^ {[ \theta ], 0} \right], \nabla_ {x} u ^ {T} (t, X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}}}, \left[ X _ {t} ^ {[ \theta ], 0} \right]) \sigma (X _ {t} ^ {x, [ \theta ], \bar {\boldsymbol {a}}}, \left[ X _ {t} ^ {[ \theta ], 0} \right])).$$

Proofs of Lemma 5.3 and Lemma 5.4. The proofs follow from the definition of ?? , see for instance [14, Theorem 7.1] in the strong dissipative and non-McKean-Vlasov’s framework □

Theorem 5.5. We assume that $( \mathcal { H } _ { O C P } )$ holds. For every control ??, we have:

$$\liminf _ {T \to \infty} \frac {J ^ {T} (x , [ \theta ] , \boldsymbol {a})}{T} \geq \lambda . \tag {5.4}$$

Furthermore, if the infimum is attained for every ??, [??], ?? in (5.3), we have that there exists $\ell \in \mathbb { R } , C > 0 , \eta > 0$ such that:
