The marginal on $\Omega ^ { t }$ is $\mathbb { P } ^ { t }$ ,   
● The joint distribution of $( w ( t + 1 ) , \hat { w } ( t + 1 ) ) \mathrm { ~ i s ~ } \pi _ { t + 1 }$

The measures $\mathbb { P } ^ { t }$ are consistent: the projection of $\mathbb { P } ^ { t + 1 }$ onto $\Omega ^ { t }$ is $\mathbb { P } ^ { t }$ , as the gluing lemma preserves marginals at each step. Define the infinite space:

$$\Omega^ {\infty} = \Omega \times \prod_ {t = 0} ^ {\infty} \mathbb {R} ^ {d},$$

with the product σ-algebra, supporting $( \omega , \hat { w } ( 0 ) , \hat { w } ( 1 ) , \ldots )$ . The projection $p ^ { t } : \Omega ^ { \infty }  \Omega ^ { t }$ is:

$$p ^ {t} (\omega , \hat {w} (0), \hat {w} (1), \dots) = (\omega , \hat {w} (0), \dots , \hat {w} (t)).$$

Since $\mathbb { P } ^ { t + 1 } \circ ( p ^ { t } ) ^ { - 1 } = \mathbb { P } ^ { t }$ , Kolmogorov’s extension theorem yields a unique $\mathbb { P } ^ { \infty }$ on $\Omega ^ { \infty }$ such that $\mathbb { P } ^ { \infty } \circ ( p ^ { t } ) ^ { - 1 } = \mathbb { P } ^ { t }$ for all $t .$

On $\Omega ^ { \infty }$ , define:

$$
\begin{array}{l} w (\omega , \hat {w} (0), \hat {w} (1), \dots) = w (\omega), \\ \hat {w} (\omega , \hat {w} (0), \hat {w} (1), \dots) (t) = \hat {w} (t). \\ \end{array}
$$

nder $\mathbb { P } ^ { \infty } , \mathbb { P } _ { w } ^ { \infty } = \mathbb { P } _ { w }$ (margi on Ω is P), and $\mathbb { P } _ { ( w ( t ) , \hat { w } ( t ) ) } ^ { \infty } =$ $\pi _ { t }$ $t ,$ $\mathbb { P } ^ { t }$

Thus, $( w , \hat { w } )$ on $( \Omega ^ { \infty } , \mathbb { P } ^ { \infty } )$ satisfies the constraints of (5), and the objective values equal those of w in (4). This establishes the one-to-one correspondence, completing the proof. ◻

While the main contribution of this article concerns Wasserstein ambiguity sets, we wish to highlight the advantages of the modular robust control framework by briefly sketching the compatibility of the Wasserstein ambiguity set description presented in this article with autocorrelation conditions as proposed in [7], [22].

In this course, let us assume that the autocorrelation terms of the disturbance input w $\in \mathcal { W } _ { \mathrm { c o r } }$ satisfy additionally the N constraints

$$\operatorname * {l i m s u p} _ {T \to \infty} \frac {1}{T - l} \sum_ {t = l} ^ {T - 1} \sum_ {k = 0} ^ {l} \mathrm{tr} \left(\mathbb {E} w _ {t - k} w _ {t} ^ {\top} M _ {i} ^ {k}\right) \leq \gamma_ {i}, \quad i = 1, \dots , N \tag {22}$$

defined by matrices $M _ { i } ^ { k } \in \mathbb { R } ^ { n _ { w } \times n _ { w } }$ and $\gamma _ { i } \in \mathbb { R }$ . This restriction can be formulated in the (integral quadratic constraint) form
