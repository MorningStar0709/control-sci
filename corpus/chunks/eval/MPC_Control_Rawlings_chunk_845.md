Definition A.19 (Convex cone). A subset C of $\mathbb { R } ^ { n } , C \neq \emptyset$ , is called a cone if $x \in C$ implies $\lambda x \in C$ for all $\lambda \geq 0$ . A cone C is pointed if $C \cap { - C = \{ 0 \} }$ . A convex cone is a cone that is convex.

An example of a cone is a halfspaces with a boundary that is a hyperplane passing through the origin; an example of a pointed cone is the positive orthant. A polyhedron C defined by $C : = \{ x \mid \langle a _ { i } , x \rangle \leq$ $0 , i \in \mathcal { I } \}$ is a convex cone that is pointed

Definition A.20 (Polar cone). Given a cone $C \subset \mathbb { R } ^ { n }$ , the cone $C ^ { * }$ defined by

$$C ^ {*} := \{h \mid \langle h, x \rangle \leq 0 \forall x \in C \}$$

is called the polar cone of C.

An illustration of this definition when C is a polyhedron containing the origin is given in Figure A.6. In this figure, H is the hyperplane with normal h passing through the origin.

![](image/76bbed5e3b28677afe32d8ebdcc04b9cf500c09a38e8872211f3a6508b213ac6.jpg)

<details>
<summary>text_image</summary>

h
C*
H
0
C
</details>

Figure A.6: Polar cone.

Definition A.21 (Cone generator). A cone K is said to be generated by a set $\{ a _ { i } \mid i \in \mathcal { I } \}$ where I is an index set if

$$K = \left\{\sum_ {i \in \mathcal {I}} \mu_ {i} a _ {i} \mid \mu_ {i} \geq 0, i \in \mathcal {I} \right\}$$

in which case we write $K = \mathrm { c o n e } \{ a _ { i } \mid i \in \mathcal { I } \}$ .

We make use of the following result:

Proposition A.22 (Cone and polar cone generator).

(a) Suppose C is a convex cone containing the origin and defined by

$$C := \{x \in \mathbb {R} ^ {n} \mid \langle a _ {i}, x \rangle \leq 0, i \in \mathcal {I} \}$$

Then

$$C ^ {*} = \operatorname{cone} \left\{a _ {i} \mid i \in \mathcal {I} \right\}$$

(b) $I f C$ is a closed convex cone, then $( C ^ { * } ) ^ { * } = C$ .

(c) $I f C _ { 1 } \subset C _ { 2 }$ , then $C _ { 2 } ^ { * } \subset C _ { 1 } ^ { * }$ .

Proof.

(a) Let the convex set K be defined by

$$K := \operatorname{cone} \left\{a _ {i} \mid i \in \mathcal {I} \right\}$$

We wish to prove $C ^ { * } = K$ . To prove $K \subset C ^ { * }$ , suppose h is an arbitrary point in $K : = \mathrm { c o n e } \{ a _ { i } \mid i \in \mathcal { I } \}$ . Then $\begin{array} { r } { h = \sum _ { i \in \mathcal { I } } \mu _ { i } a _ { i } } \end{array}$ where $\mu _ { i } \geq 0$ for all $i \in { \mathcal { I } } ,$ . Let x be an arbitrary point in C so that $\langle a _ { i } , x \rangle \leq 0$ for all $i \in { \mathcal { I } }$ . Hence
