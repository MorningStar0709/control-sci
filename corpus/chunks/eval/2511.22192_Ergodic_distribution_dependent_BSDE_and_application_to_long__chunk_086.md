$$J (x, [ \theta ], \boldsymbol {a}) = \limsup _ {T \to \infty} \frac {1}{T} \mathbb {E} _ {T} ^ {\boldsymbol {a}} \left[ \int_ {0} ^ {T} L (X _ {s} ^ {x, [ \theta ], \boldsymbol {a}}, [ X _ {s} ^ {[ \theta ], 0} ], a _ {s}) \mathrm{d} s \right]$$

and the associated optimal control problem as the minimization of ?? over all admissible control ??. As usual, we introduce the associated Hamiltonian function as

$$f (x, [ \theta ], z) := \inf _ {a \in \mathscr {A}} \{L (x, [ \theta ], a) + z R a \}. \tag {5.3}$$

Using Filippov’s theorem (see [20, Theorem 4]), if the infimum is attained, there exists $\varphi : \mathbb { R } ^ { d } \times \mathcal { P } _ { 2 q + 2 } \times \mathbb { R } ^ { d } \to \mathcal { A }$ measurable s.t.

$$f (x, [ \theta ], z) = L (x, [ \theta ], \varphi (x, [ \theta ], z)) + z R (\varphi (x, [ \theta ], z)).$$

Following lemmas are direct applications of the definitions and assumptions given above.

Lemma 5.2. Assume that Assumption 2 holds. Then the function ?? given by (5.3) satisfies that

1. for all $\boldsymbol { x } \in \mathbb { R } ^ { d }$ and all $\theta \in L ^ { 2 q + 2 } , | f ( x , [ \theta ] , 0 ) | \leq C ( 1 + | x | ^ { q + 1 } + \| \theta \| _ { 2 q + 2 } ^ { q + 1 } )$ ,   
2. for all $x , x ^ { \prime } , z , z ^ { \prime } \in \mathbb { R } ^ { d }$ and all $\theta , \theta ^ { \prime } \in L ^ { 2 q + 2 } ( \Omega ; \mathbb R ^ { d } )$ ,

$$
\begin{array}{l} | f (x, [ \theta ], z) - f (x ^ {\prime}, [ \theta^ {\prime} ], z ^ {\prime}) | \leq C (1 + | x | ^ {q} + | x ^ {\prime} | ^ {q} + \| \theta \| _ {2 q + 2} ^ {q} + \| \theta^ {\prime} \| _ {2 q + 2} ^ {q}) \left(K _ {x} ^ {L} | x - x ^ {\prime} | ^ {\epsilon} + K _ {\mathcal {L}} ^ {L} \mathcal {W} _ {2} ([ \theta ], [ \theta^ {\prime} ]) ^ {\epsilon}\right) \\ + | R \mathcal {A} | _ {\infty} | z - z ^ {\prime} |. \\ \end{array}
$$

where $| R \mathcal { A } | _ { \infty } = s u p \left\{ | R a | , a \in \mathcal { A } \right\}$ . In particular ?? satisfies (H0) with $K _ { z } ^ { f } = | R \mathcal { A } | _ { \infty }$ .

Proof. 1. is a direct consequence of (5.3) and Assumption 2-1., 2. is a consequence of [13, Lemma 5.2].

![](image/02fb34cf8efd29e3a31b2c3a93b4d0dc18b0bafef1abe34c54c39a3e0d59796a.jpg)

Assumption $\left( \mathcal { H } _ { O C P } \right)$ . Assume that Assumption 2 holds and that one of the following is fulfilled

1. $( \mathcal { H } _ { S D E } 1 )$ holds, ?? is uniformly elliptic and $\nu > K _ { x } ^ { \sigma } + \sqrt { 2 K _ { x } ^ { \sigma } } | R \mathcal { A } | _ { \infty }$ .   
2. $( \mathcal { H } _ { S D E } 2 )$ holds.
