If the conditions are violated at k, i. e., $w _ { k } ~ = ~ w ^ { \# }$ , but can be recovered at $k + 1$ , then $\begin{array} { r c l } { u _ { k } } & { = } & { \mu ( x _ { k } , \widehat { \theta } _ { k } ) } \end{array}$ and $\hat { J } ( x _ { k + 1 } ( u _ { k } ) , w _ { k + 1 } ) \leq V ( x _ { k + 1 } ( u _ { k } ) ) + \| w _ { k + 1 } - w ^ { \# } \| \bar { \varphi }$ can be used to obtain

$$\Delta_ {k} \mathcal {V} _ {c} \leq - \delta \bar {\nu} + \| w _ {k + 1} - w ^ {\#} \| \bar {\varphi}$$

from (48). Since $\varepsilon _ { w }$ upper bounds $\lVert w _ { k + 1 } - w _ { k } \rVert$ with margin (47), $\Delta _ { k } \mathcal { V } _ { c } \leq - \delta 3 \bar { \nu } / 5 .$

Case $\begin{array} { r l } & { 2 \mathbin { \lrcorner } \mathcal { V } _ { c } ( x _ { k } , \tilde { \theta } _ { k } ) \leq \frac { 3 v ^ { \ast } } { 4 } } \end{array}$ : Due to the specification of $v ^ { * }$ as in Section II-E and

$$q _ {1} (\| x _ {k} \|) \leq \hat {J} (x _ {k}, w _ {k}) \leq \mathcal {V} _ {c} (x _ {k}, \tilde {\theta} _ {k}) \leq \frac {3 v ^ {*}}{4} \Rightarrow \| x _ {k} \| \leq \frac {3 r ^ {*}}{4},$$

the state has entered the target ball. Using (41), if $\delta > 0$ satisfies

$$\bar {w} L _ {\varphi} \delta \bar {\mathcal {F}} \leq \frac {v ^ {*}}{4}$$

then

$$\mathcal {V} _ {c} (x (t; x _ {k}), \tilde {\theta} _ {k}) \leq \underbrace {\mathcal {V} _ {c} (x _ {k} , \tilde {\theta} _ {k})} _ {\leq \frac {3 v ^ {*}}{4}} + \underbrace {\bar {w} L _ {\varphi} \delta \bar {\mathcal {F}}} _ {\leq \frac {v ^ {*}}{4}} \leq v ^ {*}.$$

Subsequently $\| x ( t ; x _ { k } ) \| \leq r , t \in [ 0 , \delta )$ . Being in Case 1, the state may either remain there or trigger Case 2.

Therefore, choosing the sampling time $0 < \delta \leq \bar { \delta } .$ , where

$$\bar {\delta} := \max _ {0 < \delta \leq \delta_ {a}} \delta$$

such that $\bar { w } L _ { \varphi } \delta \bar { \mathcal { F } } \leq \frac { v ^ { * } } { 4 } , ~ \bar { J } + \overline { { w } } L _ { \varphi } \delta \bar { \mathcal { F } } \leq q _ { 1 } ( R ^ { * } )$ (49)

$$\delta^ {2} \bar {\Delta} \leq \frac {\bar {\nu}}{1 0} \delta ,$$

and $\bar { \delta } _ { a }$ from Case 2.1, renders $x = 0$ practically semi-globally stable as per Definition 2 and $\hat { \theta } = \theta$ globally stable.

Proof: (of Prop. 1) The proof of Thm. 1 can be transferred to a {I}-subsystem case using the new $\alpha _ { \nu }$ to calculate a minimum decay rate of c. Specifically, the analysis is performed using the balls $\underline { { { B } } } _ { { \mathcal { R } } } , \ { \mathcal { R } } \in \{ R , r , R ^ { * } , r ^ { * } \}$ , and new bounding functions $\alpha _ { 1 , 2 }$ to relate level sets of a partition of $\mathcal { V } _ { c }$ to the former. ■
