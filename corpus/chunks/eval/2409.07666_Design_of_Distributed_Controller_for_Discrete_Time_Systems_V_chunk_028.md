Proof: (Proof of Lemma 5) First, we show $\kappa _ { s } \subset { \hat { \kappa } } _ { s }$ . By combining two LMIs in (8), we get $\Phi ( \tilde { Q } , \tilde { Z } ) + \rho / \eta ( W \tilde { R } +$ $\tilde { R } W ) \succ O$ . Pre- and post-multiplying with R˜ and utilizing Finsler’s lemma yields $\exists \beta \in \mathbb { R } , \tilde { \tilde { R } } ^ { - 1 } \Phi ( \tilde { Q } , \tilde { Z } ) \tilde { R } ^ { - 1 } + \beta W \succ$

O. This satisfies a matrix inequality condition of $\hat { \kappa } _ { s }$ , thus $\kappa _ { s } \subset \hat { \kappa } _ { s }$ .

Next, we prove $k _ { \mathrm { d i a g } } ~ \subset ~ \kappa _ { s }$ . For $K \in \mathcal { K } _ { \mathrm { d i a g : } }$ , we have $K \ = \ Z Q ^ { - 1 } \ \in \ { \bar { \mathcal { S } } }$ with some $Z \in \ S$ and $Q \ =$ blkdiag $\{ Q _ { 1 } , \dots , Q _ { N } \} \succ { \cal O }$ . From lemma 3, block-diagonal matrix K˜ such that $\begin{array} { r } { K \ = \ ( E ^ { \top } E ) ^ { - 1 } E ^ { \top } . } \end{array}$ KE˜ exists. We define $\tilde { Q } ~ = ~ \mathrm { b l k d i a g } ( \cdot \cdot \cdot , \tilde { Q } _ { k } , \cdot \cdot \cdot ) ~ \succ ~ O$ with $\begin{array} { r l } { \tilde { Q } _ { k } } & { { } = } \end{array}$ blkdiag $( \cdots , | \mathcal { Q } _ { \mathcal { G } } ^ { j } | Q _ { j } , \cdots ) \succ O$ . By setting $\tilde { Z } = \tilde { K } \tilde { Q }$ , we get j∈Ck $( E ^ { \top } E ) ^ { - 1 } E ^ { \top } \tilde { Z } \tilde { Q } ^ { - 1 } E = ~ ( E ^ { \top } E ) ^ { - 1 } E ^ { \top } \tilde { Z } E ( E ^ { \top } E ) ^ { - 1 } Q ^ { - 1 } =$ $\dot { Z } Q ^ { - 1 } = K$ since $\tilde { Q } ^ { - 1 } E \doteq E ( \bar { E } ^ { \top } E ) ^ { - 1 } Q ^ { - 1 }$ and Z can be represented as $Z = ( E ^ { \top } E ) ^ { - 1 } \dot { E } ^ { \top } \tilde { Z } \tilde { Q } ^ { - 1 } E \in \mathcal { S }$ from lemma 3.

$$
\begin{array}{l} O \prec \left[ \begin{array}{c c} Q ^ {- 1} & * \\ Q ^ {- 1} (A + B K) & Q ^ {- 1} \end{array} \right] \\ = \left[ \begin{array}{c c} E ^ {\top} \tilde {Q} ^ {- 1} E & * \\ E ^ {\top} \tilde {Q} ^ {- 1} E (A + B K) & E ^ {\top} \tilde {Q} ^ {- 1} E \end{array} \right] \\ = \left[ \begin{array}{c c} E ^ {\top} & O \\ O & E ^ {\top} \end{array} \right] \left[ \begin{array}{c c} \tilde {Q} ^ {- 1} & * \\ \tilde {A} + \tilde {B} \tilde {K} & \tilde {Q} ^ {- 1} \end{array} \right] \left[ \begin{array}{c c} E & O \\ O & E \end{array} \right]. \\ \end{array}
$$

By using Finsler’s lemma, and utilizing $\tilde { Q } U = U \tilde { Q }$ , we get

$$
\begin{array}{l} O \prec \Phi (\tilde {Q}, \tilde {Z}) + \rho \tilde {R} W \tilde {R} \\ = \Phi (\tilde {Q}, \tilde {Z}) + \rho W \tilde {R} ^ {2} W \\ \preceq \Phi (\tilde {Q}, \tilde {Z}) + \mu W, \\ \end{array}
$$

where $\mu = | \rho | \lambda _ { \mathrm { m a x } } ^ { 2 } ( \tilde { R } )$ . Moreover, $U \tilde { Q } + \tilde { Q } U = U ^ { 2 } \tilde { Q } + $ $\tilde { Q } U ^ { 2 } = 2 U \tilde { Q } U \succeq 2 \lambda _ { \operatorname* { m i n } } ( \tilde { Q } ) U$ . Here, $\lambda _ { \operatorname* { m a x } / \operatorname* { m i n } } ( A )$ denote the maximum/minimum eigenvalue of A. Therefore, we obtain $K \subset \kappa _ { s }$ and thus $\mathcal { K } _ { \mathrm { d i a g } } \subset \mathcal { K } _ { S }$ .
