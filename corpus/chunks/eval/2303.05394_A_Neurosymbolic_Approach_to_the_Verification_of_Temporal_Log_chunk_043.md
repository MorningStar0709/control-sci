\- The nonlinearity is a vector of non-differentiable activation functions.

Lemma 4. [18]: L ${ \mathit { e t } } \ \phi ( w ) = \operatorname* { m a x } ( \alpha w , \beta w ) , \ w \in \mathcal { X } \ \subseteq \ \mathbb { R } ^ { m } , 0 \leq \alpha \leq \beta < \infty$ and define $\mathcal { T } ^ { + } , \mathcal { T } ^ { - }$ , and $\mathcal { T } ^ { \pm }$ as the set of activations that are known to be always active, always inactive, or unknown on $\mathcal { X } , \ i . e . , \ \mathcal { T } ^ { + } = \{ i \ | \ w _ { i } \ \geq \ 0 , \forall w \in \mathcal { X } \}$ , ${ \mathcal { T } } ^ { - } = \{ i \ | \ w _ { i } < 0 , \forall w \in \mathcal { X } \}$ , and ${ \cal T } ^ { \pm } = \{ 1 , \cdots , m \} \setminus ( \bar { \cal T } ^ { + } \cup { \cal T } ^ { - } )$ . Define α = $[ \alpha + ( \beta - \alpha ) \mathbf { 1 } _ { \mathcal { T } ^ { + } } ( 1 ) , \cdot \cdot \cdot , \alpha + ( \beta - \alpha ) \mathbf { 1 } _ { \mathcal { T } ^ { + } } ( m ) ]$ ] and $\beta = [ \beta - ( \beta - \alpha ) \mathbf { 1 } _ { \mathcal { T } ^ { - } } ( 1 ) , \cdots , \beta -$ $( \beta - \alpha ) \mathbf { 1 } _ { \mathcal { T } ^ { - } } ( m ) ]$ . Then φ satisfies the δQC defined by (X , Q), where

$$
\mathcal {Q} = \{Q \mid Q = \left[ \begin{array}{c c} - 2 \operatorname{diag} (\boldsymbol {\alpha} \circ \boldsymbol {\beta} \circ \lambda) & \operatorname{diag} ((\boldsymbol {\alpha} + \boldsymbol {\beta}) \circ \lambda) \\ \operatorname{diag} ((\boldsymbol {\alpha} + \boldsymbol {\beta}) \circ \lambda) & - 2 \operatorname{diag} (\lambda) \end{array} \right], \tag {21}
e _ {i} ^ {\top} \lambda \in \mathbb {R} _ {+} \text { for } i \in \mathcal {I} ^ {\pm} \}.$$

Therefore, To capture the slope bounds, we firstly determine $\mathcal { T } ^ { + } , \mathcal { T } ^ { - }$ through the pre-activation bound computation and as an example given the slope bounds $\pmb { \alpha } _ { \ell }$ and $\beta _ { \ell }$ on the \`-th layer of TNN we claim for, $e _ { j } ^ { \top } \lambda _ { \ell } \in \mathbb { R } _ { + }$ and $j \in \mathcal { I } ^ { \pm }$ :
