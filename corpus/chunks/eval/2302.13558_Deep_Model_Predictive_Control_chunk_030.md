# C Proofs

Proof of Lemma 1. (i) Since $\begin{array} { r } { V _ { a } ( K _ { t } ) = \frac { 1 } { \theta } \operatorname { t r } ( \tilde { K } _ { t } ^ { \top } \tilde { K } _ { t } ) = \frac { 1 } { \theta } \sum _ { i = 1 } ^ { m } \left\| K _ { t } ^ { ( i ) } - W ^ { * ( i ) } \right\| ^ { 2 } \leqslant \frac { 4 } { \theta } \sum _ { i = 1 } ^ { m } \bar { W } _ { i } ^ { 2 } = \frac { 4 } { \theta } \bar { W } . } \end{array}$

(ii) We first compute

$$
\begin{array}{l} V _ {a} (K _ {t + 1}) = \frac {1}{\theta} \operatorname{tr} (\tilde {K} _ {t + 1} ^ {\top} \tilde {K} _ {t + 1}) \\ = \frac {1}{\theta} \operatorname{tr} \left(\left(\bar {K} _ {t + 1} - W ^ {*}\right) ^ {\top} \left(\bar {K} _ {t + 1} - W ^ {*}\right)\right) + \frac {1}{\theta} \operatorname{tr} \left(\alpha_ {t}\right), \\ \end{array}
$$

where

$$\alpha_ {t} = \left(K _ {t + 1} - \bar {K} _ {t + 1}\right) ^ {\top} \left(K _ {t + 1} - \bar {K} _ {t + 1}\right) + 2 \left(K _ {t + 1} - \bar {K} _ {t + 1}\right) ^ {\top} \left(\bar {K} _ {t + 1} - W ^ {*}\right)= - \left(K _ {t + 1} - \bar {K} _ {t + 1}\right) ^ {\top} \left(K _ {t + 1} - \bar {K} _ {t + 1}\right) + 2 \left(K _ {t + 1} - \bar {K} _ {t + 1}\right) ^ {\top} \left(K _ {t + 1} - W ^ {*}\right).$$

One important property of the projection (21) is the following [54, (4.61)]:

$$\left(W ^ {* (i)} - K _ {t} ^ {(i)}\right) ^ {\top} \left(\bar {K} _ {t} ^ {(i)} - K _ {t} ^ {(i)}\right) \leqslant 0 \text { for each } i = 1, \dots , m. \tag {26}$$

Since $( K _ { t + 1 } ^ { ( i ) } - \bar { K } _ { t + 1 } ^ { ( i ) } ) ^ { \top } ( K _ { t + 1 } ^ { ( i ) } - W ^ { * } ) \leqslant 0$ due to (26), we can ensure $\mathrm { t r } ( \alpha _ { t } ) \leqslant 0$ . Therefore,

$$
\begin{array}{l} V _ {a} \left(K _ {t + 1}\right) \leqslant \frac {1}{\theta} \operatorname{tr} \left(\left(\bar {K} _ {t + 1} - W ^ {*}\right) ^ {\top} \left(\bar {K} _ {t + 1} - W ^ {*}\right)\right) \\ = V _ {a} (K _ {t}) + \frac {1}{\left\| \phi_ {j} (x _ {t}) \right\| ^ {2}} \operatorname{tr} \left(\tilde {u} _ {t} \tilde {u} _ {t} ^ {\top} + 2 \tilde {K} _ {t} ^ {\top} \phi_ {j} (x _ {t}) \tilde {u} _ {t} ^ {\top}\right). \\ \end{array}
$$

By substituting $\tilde { K } _ { t } ^ { \top } \phi _ { j } ( x _ { t } ) = - \tilde { u } _ { t } + \varepsilon _ { j } ( x _ { t } )$ in the above inequality, we get
