$$\dot {V} _ {\xi} \leq - \mu \lambda_ {\min} (P) \left\| e _ {r e f} \right\| ^ {2} - \frac {\gamma_ {1}}{2} \| \tilde {\theta} \| ^ {2} + \frac {1}{2 \gamma_ {1}} \left(\frac {\gamma_ {0} \lambda_ {\max} (\omega \omega^ {\mathrm{T}}) + \gamma_ {1}}{\rho} \varsigma_ {\mathrm{UB}}\right) ^ {2} \leq - \eta_ {\xi_ {2}} V + \frac {1}{2 \gamma_ {1}} \left(\frac {\gamma_ {0} \lambda_ {\max} (\omega \omega^ {\mathrm{T}}) + \gamma_ {1}}{\rho} \varsigma_ {\mathrm{UB}}\right) ^ {2} \tag {E36}$$

$\begin{array} { r } { \eta _ { \xi 2 } = \operatorname* { m i n } \left\{ \frac { \mu \lambda _ { \operatorname* { m i n } } ( P ) } { \lambda _ { \operatorname* { m a x } } ( P ) } ; \gamma _ { 1 } \right\} } \end{array}$

max The inequality (E36) is solved, and it is obtained for $t \geq t _ { e }$ that:

$$V _ {\xi} (t) \leq e ^ {- \eta_ {\xi_ {2}} (t - t _ {e})} V (t _ {e}) + \int_ {t _ {e}} ^ {t} e ^ {- \eta_ {2} (t - \tau)} \frac {1}{2 \eta_ {\xi_ {2}} \gamma_ {1}} \left(\frac {\gamma_ {0} \lambda_ {\max} (\omega (\tau) \omega^ {\mathrm{T}} (\tau)) + \gamma_ {1}}{\rho} \varsigma_ {\mathrm{UB}}\right) ^ {2} d \tau \leq\leq e ^ {- \eta_ {\xi_ {2}} (t - t _ {e})} V (t _ {e}) + \frac {\varsigma_ {\mathrm{UB}} ^ {2}}{2 \eta_ {\xi_ {2}} \rho^ {2} \gamma_ {1}} \int_ {t _ {e}} ^ {t} e ^ {- \eta_ {\xi_ {2}} (t - \tau)} (\gamma_ {0} \lambda_ {\max} (\omega (\tau) \omega^ {\mathrm{T}} (\tau)) + \gamma_ {1}) ^ {2} d \tau . \tag {E37}\underbrace {\varepsilon_ {\xi} ^ {2}} _ {}$$

For further proof of Theorem, it is to be shown that $\varepsilon _ { \xi } \in L _ { \infty } . \operatorname { S o }$ , we need to prove that $\forall t \ge t _ { e } \lambda _ { \operatorname* { m a x } } \left( \omega \left( t \right) \omega ^ { \mathrm { T } } \left( t \right) \right) \le \lambda _ { \operatorname* { m a x } } ^ { \mathrm { U B } }$

Following the definition ?? $\mathbf { \Psi } ( t ) = \left[ x ^ { \mathrm { T } } \left( t \right) r ^ { \mathrm { T } } \right] ^ { \mathrm { T } }$ , we have that $\lambda _ { \operatorname* { m a x } } \left( \omega \left( t \right) \omega ^ { \mathrm { T } } \left( t \right) \right) \leq \lambda _ { \operatorname* { m a x } } ^ { \mathrm { U B } } \mathrm { i f } \ x \left( t \right)$ and ?? are bounded. The reference signal ?? is bounded according to the problem statement. So, the boundedness of the state vector $x \left( t \right)$ is to be shown.

To do that, the following quadratic function is considered:

$$V _ {e _ {r e f}} = e _ {r e f} ^ {\mathrm{T}} \overline {{P}} e _ {r e f} \tag {E38}$$
