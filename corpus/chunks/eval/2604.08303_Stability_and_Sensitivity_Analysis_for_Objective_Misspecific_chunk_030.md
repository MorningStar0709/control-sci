$$\Delta z _ {t} = \sum_ {j \in N} B _ {j} \Xi_ {j} \Delta u _ {t} ^ {(j)}, \Delta c _ {t} ^ {(j)} = - F _ {x} ^ {(j)} \Delta y _ {t}. \tag {19}$$

By substituting the interconnection equations into the storage function inequalities and adding them altogether, we can write the overall inequality for the interconnection of $\Psi _ { 0 }$ and $\Psi _ { i } { } ^ { \prime } \mathbf { s }$ in terms of $\Delta y _ { t }$ and $\begin{array} { r } { \dot { \Delta } u _ { t } ^ { ( j ) , } } \end{array}$ u(j)t ’s collected under ∆ˆut = $\Delta \hat { u } _ { t } =$ $[ \Delta u _ { t } ^ { ( 1 ) } \cdot \cdot \cdot \Delta u _ { t } ^ { ( n ) } ] .$ . This is represented as $V _ { \bar { x } } ( x _ { t + 1 } ) V _ { \bar { x } } ( x _ { t } ) =$ $\begin{array} { r } { \dot { V } _ { \bar { x } } ^ { 0 } ( \dot { x } _ { t + 1 } ) V _ { \bar { x } } ^ { 0 } ( \dot { x } _ { t } ) \dot { \sum } _ { j \in N } V _ { \bar { x } } ^ { j } } \end{array}$ , which corresponds to:

$$
V _ {\bar {x}} \left(x _ {t + 1}\right) - V _ {\bar {x}} \left(x _ {t}\right) \leq \left[ \begin{array}{c} \Delta y _ {t} \\ \Delta \hat {u} _ {t} \end{array} \right] ^ {\top} \left(\left[ \begin{array}{c c} A ^ {\top} P A - P & A ^ {\top} P \hat {B} \\ \hat {B} ^ {\top} P A & \hat {B} ^ {\top} P \hat {B} \end{array} \right] + \lambda W\right) \left[ \begin{array}{c} \Delta y _ {t} \\ \Delta \hat {u} _ {t} \end{array} \right] \tag {20}
$$

where $\lambda > 0 , \hat { B } = \left\lceil B _ { 1 } \Xi _ { 1 } \quad B _ { 2 } \Xi _ { 2 } \quad \ldots \quad B _ { n } \Xi _ { n } \right\rceil$ , and the blocks of W are defined as in (9). Thus, if

$$
\left[ \begin{array}{c c} A ^ {\top} P A - P & A ^ {\top} P \hat {B} \\ \hat {B} ^ {\top} P A & \hat {B} ^ {\top} P \hat {B} \end{array} \right] + \lambda W \preceq - \epsilon I \tag {21}
$$

for some $\epsilon > 0 .$ , then $V _ { \bar { x } } ( x _ { t + 1 } ) - V _ { \bar { x } } ( x _ { t } ) < 0 .$

5) Stability of the overall system: The discrete-time Lyapunov theorem [23] is applied to show the closed-loop stability of the overall system. Conditions (i) $V _ { \bar { x } } ( \bar { x } ) = 0 $ , (ii) $V _ { \bar { x } } ( x ) > 0 , \forall x \in \mathbb { R } ^ { n _ { x } } \backslash \{ \bar { x } \}$ , (iii) Vx¯(x) → ∞ as ∥x∥ → ∞: are satisfied as a quadratic storage function is selected. (iv) $V _ { \bar { x } } ( f ( x ) ) - V _ { \bar { x } } ( x ) < 0 , \forall x \in \mathbb { R } ^ { n _ { x } }$ : is satisfied as the decrease condition is imposed by (21). The equilibrium point x¯ is globally asymptotically stable for $\epsilon > 0$ and stable if $\epsilon = 0$ . Input constraints are always satisfied by the solution of (5), thus, the problem is recursively feasible ∀x $\in \mathbb { R } ^ { n _ { x } }$ . ■
