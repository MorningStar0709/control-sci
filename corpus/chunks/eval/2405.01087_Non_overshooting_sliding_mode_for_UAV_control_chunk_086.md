From $\operatorname* { l i m } _ { \rho \to + \infty } \operatorname { t a n h } ( \rho \cdot x ) = \operatorname { s i g n } ( x )$ , the tanh-function-based sliding mode (24) becomes the ideal 2- sliding mode (18). This concludes the proof. 

Proof of Theorem 5.1:

Define $e _ { 1 } \left( t \right) = x _ { d } \left( t \right) - x _ { 1 } \left( t \right)$ and $e _ { 2 } \left( t \right) = \dot { x } _ { d } \left( t \right) - x _ { 2 } \left( t \right)$ . Then, the error system is

$$\dot {e} _ {1} (t) = e _ {2} (t)\dot {e} _ {2} (t) = - h (t) - u (t) + \ddot {x} _ {d} (t) + \delta (t) \tag {170}$$

The desired stable sliding mode (18) in Theorem 3.1 is selected, where, $d ( t ) = \ddot { x } _ { d } \left( t \right) + \delta \left( t \right)$ . In order to turn the error system (170) into the sliding mode (18), we select

$$
\begin{array}{l} \dot {e} _ {2} (t) = - h (t) - u (t) + \ddot {x} _ {d} (t) + d (t) \\ = \left\{ \begin{array}{l} - k _ {c} \operatorname{sign} \left[ e _ {2} (t) + e _ {2 c} \operatorname{sign} \left(e _ {1} (t)\right) \right] + \ddot {x} _ {d} (t) + \delta (t), \text {if} | e _ {1} (t) | > e _ {1 c}; \\ - k _ {2} \operatorname{sign} \left[ e _ {2} (t) + k _ {1} e _ {1} (t) \right] + \ddot {x} _ {d} (t) + \delta (t), \text {if} | e _ {1} (t) | \leq e _ {1 c} \end{array} \right. \tag {171} \\ \end{array}
$$

Therefore, we get the controller as follows:

$$
u (t) = \left\{ \begin{array}{l} k _ {c} \text { sign } \left[ e _ {2} (t) + e _ {2 c} \text { sign } \left(e _ {1} (t)\right) \right] - h (t), \text { if } | e _ {1} (t) | > e _ {1 c} \\ k _ {2} \text { sign } \left[ e _ {2} (t) + k _ {1} e _ {1} (t) \right] - h (t), \text { if } | e _ {1} (t) | \leq e _ {1 c} \end{array} \right. \tag {172}
$$

Thus, for the uncertain system (39), when the controller (40) is selected, the system is stable, and $x _ { 1 }$ tracking $x _ { d } \left( t \right)$ is non-overshooting. This concludes the proof. 

Proof of Theorem 5.2:

Define $e _ { 1 } \left( t \right) = x _ { d } \left( t \right) - x _ { 1 } \left( t \right)$ and $e _ { 2 } \left( t \right) = \dot { x } _ { d } \left( t \right) - x _ { 2 } \left( t \right)$ . Then, the error system is

$$
\begin{array}{l} \dot {e} _ {1} (t) = e _ {2} (t) \\ \dot {e} _ {2} (t) = - h (t) - u (t) + \ddot {x} _ {d} (t) + \delta (t) \tag {173} \\ \end{array}
$$

The desired stable sliding mode (24) in Theorem 3.2 is selected, where, $d ( t ) = \ddot { x } _ { d } \left( t \right) + \delta \left( t \right)$ . In order to turn the error system (173) into the sliding mode (24), we select
