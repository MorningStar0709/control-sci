$$
\begin{array}{l} 0 = V - \hat {A} _ {1 1} ^ {- 1} V \hat {A} _ {1 1} ^ {- \top} - \tilde {Q} \tag {45} \\ + \left(\hat {A} _ {1 1} ^ {- 1} V X B _ {d}\right) \left(\gamma_ {d} ^ {2} I + B _ {d} ^ {\top} X V X B _ {d}\right) ^ {- 1} \left(\hat {A} _ {1 1} ^ {- 1} V X B _ {d}\right) ^ {\top}, \\ \end{array}
$$

where:

$$\tilde {Q} := \gamma_ {J} ^ {2} \hat {A} _ {1 1} ^ {- 1} \left(X ^ {- 1} - \hat {A} _ {1 1} X ^ {- 1} \hat {A} _ {1 1} ^ {\top} - B _ {u} (R + B _ {u} ^ {\top} X B _ {u}) ^ {- 1} B _ {u} ^ {\top}\right) \hat {A} _ {1 1} ^ {- \top}.$$

It can be shown that $\tilde { Q }$ is positive semidefinite using the DARE for X. Thus Equation 45 for V corresponds to the DARE in Lemma 1 with $( A , B _ { u } , C _ { e } , D _ { e u } , Q , R , S )$ replaced by $( \hat { A } ^ { - \top } , X B _ { d }$ , $\left[ \tilde { Q } _ { \ l } ^ { \frac 1 2 } \right] , \left[ \ l _ { \gamma _ { d } I } ^ { \phantom { \dagger } 0 } \right] , \tilde { Q } , \gamma _ { d } ^ { 2 } I , 0 )$ . It can be verified that the DARE (45) satisfies conditions (i)-(iv) in Lemma 1 and hence there exists a stabilizing stabilizing solution $V \succeq 0$ . It can also be shown, by direct substitution, that $\hat { X }$ given in (44) is positive semidefinite and satisfies the DARE (37). Hence this choice for $\hat { X }$ is the unique stabilizing solution.

The stabilizing gain resulting from the special structure of ${ \hat { A } } , { \hat { B } } .$ , and $\hat { X }$ is:

$$
\hat {K} _ {x} := \hat {H} ^ {- 1} \hat {B} ^ {\top} \hat {X} \hat {A} = \left[ \begin{array}{c c} 0 & - \hat {H} ^ {- 1} B _ {d} ^ {\top} X V \hat {A} _ {1 1} ^ {- \top} \end{array} \right].
$$

As a result, Statement 3 of Lemma 4 gives a spectral factorization with the form:

$$
A _ {F} := \hat {A} - \hat {K} _ {y} ^ {\top} \hat {K} _ {x} \left[ \begin{array}{c c} \hat {A} _ {1 1} & A _ {F, 1 2} \\ 0 & A _ {F, 2 2} \end{array} \right],

C _ {F} := \hat {W} ^ {- \frac {1}{2}} \hat {K} _ {x} = \left[ \begin{array}{c c} 0 & C _ {F, 2} \end{array} \right].
$$

The first block of states are unobservable in the output and hence can be removed. Thus the spectral factorization has a minimal realization with state dimension $n _ { x }$ as claimed.

Finally, we will verify the assumption above that X is nonsingular. Assume, to the contrary, that X is singular, i.e. there is a nonzero vector v such that $X v = 0$ . Re-write the DARE (7) as:

$$
\begin{array}{l} X = (A - B _ {u} K _ {x}) ^ {\top} X (A - B _ {u} K _ {x}) + (Q - S R ^ {- 1} S ^ {\top}) \\ + \left(K _ {x} - R ^ {- 1} S ^ {\top}\right) ^ {\top} R (K _ {x} - R ^ {- 1} S ^ {\top}). \tag {46} \\ \end{array}
$$
