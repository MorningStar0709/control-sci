$$\left\| R _ {e} w _ {q} (t) \right\| \leq \sqrt {\bar {\lambda} \left(W _ {e}\right)} \bar {w} _ {x} + \frac {1}{2} \rho \bar {\lambda} \left(M _ {e}\right) ^ {1 / 2} \left(L _ {\hat {h} ^ {- 1}} \sqrt {\bar {\sigma} \left(B _ {y}\right)} \bar {w} _ {y} + \bar {\epsilon} _ {\{1, 2, 3 \}} \left(x ^ {*}, \theta\right)\right) \tag {C.11}$$

Proof. The first term of the bound, $\sqrt { \bar { \lambda } ( W _ { e } ) } \bar { w } _ { x }$ , follows the same logic from Lemma 1, except this time $\| R _ { e } ( \cdot ) \| \le \sqrt { \bar { \lambda } ( W _ { e } ) }$ . The second term follows from first combining the final line of (17) with one of the three error bounds $\bar { \epsilon } _ { \{ 1 , 2 , 3 \} }$ , and substituting that combination into the integrand of (9) (which here simplifies to $\| R _ { e } w _ { q } ( t ) \| )$ . Specifically, we have the following:

$$
\begin{array}{l} \| R _ {e} \frac {1}{2} \rho M _ {e} C _ {r} ^ {\top} (\hat {h} ^ {- 1} (y, \theta) - C _ {r} x) \| = \frac {\rho}{2} \| R _ {e} M _ {e} C _ {r} ^ {\top} (\hat {h} ^ {- 1} (y, \theta) - C _ {r} x) \| \\ = \frac {\rho}{2} \| R _ {e} ^ {- \top} C _ {r} ^ {\top} (\hat {h} ^ {- 1} (y, \theta) - C _ {r} x) \| \\ \leq \frac {\rho}{2} \| R _ {e} ^ {- \top} C _ {r} ^ {\top} \| \| \hat {h} ^ {- 1} (y, \theta) - C _ {r} x \| \\ \leq \frac {\rho}{2} \bar {\lambda} (M _ {e}) ^ {1 / 2} \left(L _ {\hat {h} ^ {- 1}} \sqrt {\bar {\sigma} (B _ {y})} \bar {w} _ {y} + \bar {\epsilon} _ {\{1, 2, 3 \}} (x, \theta)\right) \\ \end{array}
$$

The simplification in the second equality is done via properties of the Cholesky decomposition: i.e., $R _ { e } M _ { e } = R _ { e } ( R _ { e } ^ { \top } R _ { e } { ) ^ { - 1 } = R _ { e } R _ { e } ^ { - 1 } R _ { e } ^ { - \top } = R _ { e } ^ { - \top } }$ . The final inequality follows from $\| R _ { e } ^ { - \top } \| \leq \sqrt { \bar { \lambda } ( M _ { e } ) } , \| C _ { r } \| \leq 1$ , and applying the triangle inequality.

Theorem C.1 (From derivative to value). Let RHS denote the right hand side of (19). Given bounds on the Riemannian distances at $t = 0 \colon d _ { c } ( 0 ) \leq \bar { d } _ { c } ( 0 )$ and $d _ { e } ( 0 ) \leq \bar { d } _ { e } ( 0 )$ , upper bounds $\bar { d } _ { c } ( t ) \geq d _ { c } ( t )$ and $\bar { d } _ { e } ( t ) \geq d _ { e } ( t )$ for all $t \in [ 0 , T ]$ can be written as

$$
\left[ \begin{array}{c} d _ {c} (t) \\ d _ {e} (t) \end{array} \right] \leq \int_ {\tau = 0} ^ {t} R H S \Big (\tau , \left[ \begin{array}{c} d _ {c} \\ d _ {e} \end{array} \right] \Big) d \tau \doteq \left[ \begin{array}{c} \bar {d} _ {c} (t) \\ \bar {d} _ {e} (t) \end{array} \right], d _ {c} (0) = \bar {d} _ {c} (0), d _ {e} (0) = \bar {d} _ {e} (0).
$$
