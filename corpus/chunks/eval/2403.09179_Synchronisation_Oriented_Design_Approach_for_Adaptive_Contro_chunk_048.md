# 1. Observer

Let us frame an observer to be a certainty-equivalent copy of the plant with an observation error feedback term as

$$\Sigma_ {o}: \quad \dot {\hat {x}} (t) = A x (t) + b u (t) + A _ {o} \tilde {x} (t), \quad \hat {x} (0) = \hat {x} _ {0} \tag {A.1}$$

where ??ˆ (??) denotes the observer state, $\tilde { x } \left( t \right) : = \hat { x } \left( t \right) - x \left( t \right)$ denotes the observation error, and $A _ { o }$ is a Hurwitz matrix which is a design parameter. Then, the observation error dynamics can be obtained from Eqs. (1) and (A.1) as

$$\Sigma_ {f} = \Sigma_ {o} - \Sigma_ {p}: \quad \dot {\tilde {x}} (t) = A _ {o} \tilde {x} (t) - b \Delta (t), \quad \tilde {x} (0) = \hat {x} _ {0} - x _ {0} \tag {A.2}$$

The evolution of ??˜ (??) over time following Eq. (A.2) is driven by the unknown quantity $\Delta \left( t \right)$ , however, $\tilde { x } \left( t \right)$ is measurable as it can be obtained by computing the difference between ??ˆ (??) and ?? (??).

The system $\Sigma _ { f }$ in Eq. (A.2) describes a low-pass filter for which bandwidth is determined by $A _ { o }$ . The simplest design is to take $A _ { o } = - \omega _ { f } I$ with a scalar constant $\omega _ { f } > 0$ . In this case, the observation error is related to the uncertainty as

$$\tilde {x} (t) = e ^ {- \omega_ {f} t} \tilde {x} (0) - b \Delta_ {f} (t) \tag {A.3}$$

where the filtered uncertainty $\Delta _ { f } \left( t \right)$ is given by

$$\Delta_ {f} (t) = \int_ {0} ^ {t} e ^ {- \omega_ {f} (t - \tau)} \Delta (\tau) d \tau = C (s) \Delta (t) \tag {A.4}$$

with ?? (??) = $\begin{array} { r } { C \left( s \right) = \frac { \omega _ { f } } { s + \omega _ { f } } } \end{array}$ ??+?? ?? . ?? ??

The instantaneous rejection approach is to set $\hat { \Delta } \left( t \right)$ in Eq. (4) to be equal to $\Delta _ { f } \left( t \right)$ . It is impossible to measure $\Delta _ { f } \left( t \right)$ directly, however, $b \Delta _ { f } \left( t \right) = e ^ { - \omega _ { f } t } \tilde { x } \left( 0 \right) - \tilde { x } \left( t \right)$ is a known signal since the right-hand-side can be computed.

One can then solve the linear equation and get

$$\hat {\Delta} (t) = b ^ {\dagger} \left\{e ^ {- \omega_ {f} t} \tilde {x} (0) - \tilde {x} (t) \right\} \tag {A.5}$$

where $\begin{array} { r } { b ^ { \dagger } = \frac { b ^ { T } } { b ^ { T } b } } \end{array}$ for a vector $b \neq 0$ .
