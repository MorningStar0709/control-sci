# Appendix G. Technical Theorems

Theorem 27 (Theorem 1 of Abbasi-Yadkori et al. (2011)) Let $\left( \mathcal { F } _ { t } ; k \ge \ 0 \right)$ be a filtration and $( m _ { k } ; k \ge 0 )$ be an $\mathbb { R } ^ { d } .$ -valued stochastic process adapted to $\left( \mathcal { F } _ { k } \right) , \left( \eta _ { k } ; k \ge 1 \right)$ be a real-valued martingale difference process adapted to $( \mathcal { F } _ { k } )$ . Assume that $\eta _ { k }$ is conditionally sub-Gaussian with constant R. Consider the martingale

$$S _ {t} = \sum_ {k = 1} ^ {t} \eta_ {k} m _ {k - 1}$$

and the matrix-valued processes

$$V _ {t} = \sum_ {k = 1} ^ {t} m _ {k - 1} m _ {k - 1} ^ {\intercal}, \quad \overline {{V}} _ {t} = V + V _ {t}, \quad t \geq 0$$

Then for any $0 < \delta < 1$ , with probability $1 - \delta$

$$\forall t \geq 0, \| S _ {t} \| _ {\overline {{V}} _ {t} ^ {- 1}} ^ {2} \leq 2 R ^ {2} \log \left(\frac {\det (\overline {{V}} _ {t}) ^ {1 / 2} \det (V) ^ {- 1 / 2}}{\delta}\right)$$

Theorem 28 (Azuma’s inequality) Assume that $X _ { s }$ is a supermartingale and $| X _ { s } - X _ { s - 1 } | \le c _ { s }$ almost surely for $s \geq 0 .$ . Then for all $t > 0$ and all $\epsilon > 0 \AA$ ,

$$P \left(\left| X _ {t} - X _ {0} \right| \geq \epsilon\right) \leq 2 \exp \left(\frac {- \epsilon^ {2}}{2 \sum_ {s = 1} ^ {t} c _ {s} ^ {2}}\right)$$

Lemma 29 (Lemma 10 of Abbasi-Yadkori and Szepesvari ´ (2011)) The following holds for any $t \geq 1$ :

$$\sum_ {k = 0} ^ {t - 1} \left(\| z _ {k} \| _ {V _ {k} ^ {- 1}} ^ {2} \wedge 1\right) \leq 2 \log \frac {\det (V _ {t})}{\det (\lambda I)}$$

Further, when the covariates satisfy $\| z _ { t } \| \leq c _ { m } , t \geq 0$ with some $c _ { m } > 0 \ : w . p .$ . 1 then

$$\log \frac {\det (V _ {t})}{\det (\lambda I)} \leq (n + d) \log \left(\frac {\lambda (n + d) + t c _ {m} ^ {2}}{\lambda (n + d)}\right)$$

Lemma 30 (Norm of Subgaussian vector) Let $\boldsymbol { v } \in \mathbb { R } ^ { d }$ be a entry-wise R-subgaussian random variable. Then with probability $1 - \delta , \| v \| \leq R \sqrt { 2 d \log ( d / \delta ) }$ .
