# Symmetric Matrix

A symmetric matrix is one whose row elements are the same as the corresponding column elements. Thus, $a_{ij} = a_{ji}$ . In other words, if $A = A'$ , then the matrix A is symmetric.
