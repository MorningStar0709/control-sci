$$
\begin{array}{l} \frac {D _ {U} \tilde {O} _ {T} (T ^ {- 1 / 4}) \left(1 + K _ {\mathrm{opt}} (\hat {\theta} _ {s})\right)}{(\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}) - \tilde {O} _ {T} (T ^ {- 1 / 4}) (1 + K _ {\mathrm{opt}} (\hat {\theta} _ {s}))) (\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}))} \\ \leq \frac {\tilde {O} _ {T} (T ^ {- 1 / 4}) D _ {U} (1 + O _ {T} (1))}{(c _ {\mathrm{L25}} - \tilde {O} _ {T} (T ^ {- 1 / 4}) (1 + O _ {T} (1))) c _ {\mathrm{L25}}} \\ \leq \epsilon^ {*} / 2. \tag {128} \\ \end{array}
$$

Finally, rearranging Equation (127) gives exactly the desired result.
