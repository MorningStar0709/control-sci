$$\sum_ {t _ {j} \in S ^ {\prime}} \frac {\eta_ {j}}{2} \left\| \nabla \tilde {f} _ {t _ {j}} (M _ {t _ {j}}) \right\| ^ {2} \leq (l _ {x} + 2 \sqrt {h} R _ {y}) ^ {2} \sum_ {t _ {j} \in S ^ {\prime}} \frac {\eta_ {j}}{2}= \frac {(l _ {x} + 2 \sqrt {h} R _ {y} R _ {\tilde {v}}) ^ {2}}{\alpha_ {0}} \sum_ {j = 1} ^ {\lfloor T / \tau \rfloor - 1} \frac {1}{j}\leq \frac {(l _ {x} + 2 \sqrt {h} R _ {y} R _ {\tilde {v}}) ^ {2}}{\alpha_ {0}} (1 + \log \lfloor T / \tau \rfloor),$$

ty follows from the choice of the step size . Since the above inequality holds for any $\begin{array} { r } { \eta _ { j } = \frac { 1 } { \alpha _ { 0 } j } } \end{array}$ for all on of t $j \geq 1$ and the factdom variables $| S ^ { \prime } | = \left\lfloor T / \tau \right\rfloor - 1$ $b _ { 0 } , \dots , b _ { \lfloor T / \tau \rfloor }$ , taking the expectation $\mathbb { E } _ { b } [ \cdot ]$ with respect to $b _ { 0 } , \dots , b _ { \lfloor T / \tau \rfloor }$ on both sides of the above inequality completes the proof of part (ii).

Proof of part (iii). From the definition of $S ^ { \prime }$ in (40) and the assumption that $\tau \geq h .$ , we see that from $M _ { t _ { j } - h }$ to $M _ { t _ { j } }$ for any $t _ { j } \in S ^ { \prime }$ , Algorithm 2 performs the update in line 9 at most once at the step $t _ { j - 1 }$ . Thus, we have

$$
\begin{array}{l} \left\| M _ {t _ {j}} - M _ {t _ {j} - h} \right\| \stackrel {(a)} {\leq} \left\| \eta_ {j - 1} \nabla \tilde {f} _ {t _ {j - 1}} (M _ {t _ {j - 1}}) \right\| \\ \stackrel {(b)} {\leq} \frac {1}{\alpha_ {0} (j - 1)} (l _ {x} + 2 \sqrt {h} R _ {y} R _ {\tilde {v}}), \forall 2 \leq j \leq \lfloor T / \tau \rfloor , \\ \end{array}
$$

where (a) follows from line 9 of Algorithm 2 and [67, Proposition 2.2]; (b) follows from the choice of the step size ηj = 1α0j $\begin{array} { r } { \eta _ { j } = \frac { 1 } { \alpha _ { 0 } j } } \end{array}$ for all $j \geq 1$ and the upper bound shown in (D.17). Also note that $\begin{array} { r } { \| M _ { t _ { 1 } } - M _ { t _ { 1 } - h } \| \leq \left\| \eta _ { 0 } \nabla \tilde { f } _ { t } ( M _ { t _ { 1 } - h } ) \right\| = 0 } \end{array}$ since $\eta _ { 0 } = 0$ . Summing over all $t \in S ^ { \prime }$ , we get

$$
\begin{array}{l} \sum_ {t _ {j} \in S ^ {\prime}} \left\| M _ {t _ {j}} - M _ {t _ {j} - h} \right\| \leq \frac {l _ {x} + 2 \sqrt {h} R _ {y} R _ {\tilde {v}}}{\alpha_ {0}} \sum_ {j = 2} ^ {\lfloor T / \tau \rfloor - 1} \frac {1}{j - 1} \\ \leq \frac {l _ {x} + 2 \sqrt {h} R _ {y} R _ {\tilde {v}}}{\alpha_ {0}} (1 + \log \lfloor T / \tau \rfloor). \\ \end{array}
$$
