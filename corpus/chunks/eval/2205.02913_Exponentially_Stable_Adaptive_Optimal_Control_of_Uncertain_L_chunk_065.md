$$
\begin{array}{l} \dot {V} _ {\xi} = - \mu e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} + t r \left(- K ^ {\mathrm{T}} N ^ {\mathrm{T}} e _ {r e f} e _ {r e f} ^ {\mathrm{T}} N K + 2 \tilde {\theta} ^ {\mathrm{T}} \omega e _ {r e f} ^ {\mathrm{T}} N K \pm \tilde {\theta} ^ {\mathrm{T}} \omega \omega^ {\mathrm{T}} \tilde {\theta} - \tilde {\theta} ^ {\mathrm{T}} \left[ \gamma \Omega^ {2} \tilde {\theta} + \gamma \Omega \varsigma \right]\right) = - \mu e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} + \\ + t r \left\{- \left(e _ {r e f} ^ {\mathrm{T}} N K - \tilde {\theta} ^ {\mathrm{T}} \omega\right) ^ {2} + \tilde {\theta} ^ {\mathrm{T}} \omega \omega^ {\mathrm{T}} \tilde {\theta} - \tilde {\theta} ^ {\mathrm{T}} \left[ \gamma \Omega^ {2} \tilde {\theta} + \gamma \Omega \varsigma \right] \right\} \leq - \mu e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} + t r \left\{\tilde {\theta} ^ {\mathrm{T}} \omega \omega^ {\mathrm{T}} \tilde {\theta} - \tilde {\theta} ^ {\mathrm{T}} \left[ \gamma \Omega^ {2} \tilde {\theta} + \gamma \Omega \varsigma \right] \right\}. \tag {E27} \\ \end{array}
$$

Two different cases are to be considered: $t < t _ { e } ; t \geq t _ { e } .$

Firstly, let $t < t _ { e }$ . In accordance with Proposition 6, in the worst case scenario $\forall t < t _ { e }$ it holds that $\Omega ( t ) < \rho \Leftrightarrow \gamma \Omega ( t ) = 0$ 号 and $\left\| \tilde { \theta } \left( t \right) \right\| = \left\| \tilde { \theta } \left( t _ { r } ^ { + } \right) \right\|$ . Then $\forall t < t _ { e }$ the equation (E27) is rewritten as:

$$
\begin{array}{l} \dot {V} _ {\xi} \leq - \mu e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} + t r \left\{\tilde {\theta} ^ {\mathrm{T}} \left(t _ {r} ^ {+}\right) \omega \omega^ {\mathrm{T}} \tilde {\theta} \left(t _ {r} ^ {+}\right) \pm \tilde {\theta} ^ {\mathrm{T}} \tilde {\theta} \right\} \leq \\ \leq - \mu e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} - t r \left\{\tilde {\theta} ^ {\mathrm{T}} \tilde {\theta} \right\} + t r \left\{\tilde {\theta} ^ {\mathrm{T}} \left(t _ {r} ^ {+}\right) \omega \omega^ {\mathrm{T}} \tilde {\theta} \left(t _ {r} ^ {+}\right) + \tilde {\theta} ^ {\mathrm{T}} \left(t _ {r} ^ {+}\right) \tilde {\theta} \left(t _ {r} ^ {+}\right) \right\}. \tag {E28} \\ \end{array}
$$

Let the notion of the maximum eigenvalue of $\omega \left( t \right) \omega ^ { \mathrm { T } } \left( t \right)$ over $\left[ t _ { r } ^ { + } ; t _ { e } \right)$ be introduced:
