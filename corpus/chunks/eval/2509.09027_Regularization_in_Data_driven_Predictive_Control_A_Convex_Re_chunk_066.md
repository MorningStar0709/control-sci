# B.5 Proof of Proposition 5

The key idea for the proof of the Proposition 5 is that we can first partition both $H _ { y }$ and $\tilde { H } _ { y }$ into the part in the row space of $H _ { u }$ and the part in the null space of $H _ { u } , \ i . e . , \ H _ { y } \ = \ H _ { 1 } + H _ { 2 } , \tilde { H } _ { y } \ = \ \tilde { H } _ { 1 } + \tilde { H } _ { 2 }$ , where row $\left( H _ { 1 } \right)$ , row $( \tilde { H } _ { 1 } ) \subseteq \operatorname { r o w } ( H _ { u } )$ and row $\left( H _ { 2 } \right)$ , row $( \tilde { H } _ { 2 } ) \subseteq$ null( $\left( \boldsymbol { H _ { u } } \right)$ . Then, we illustrate that the optimal solution for ${ \tilde { H } } _ { 1 }$ is $H _ { 1 }$ and thus the problem becomes approximately $H _ { 2 }$ with a low rank matrix ${ \tilde { H } } _ { 2 }$ which admits an analytical solution via SVD.

We can equivalently formulate the optimization problem (35) as

$$\min _ {\tilde {H} _ {1}, \tilde {H} _ {2}} \| H _ {1} + H _ {2} - \tilde {H} _ {1} - \tilde {H} _ {2} \| _ {F}$$

$\mathrm { s u b j e c t ~ t o ~ } \mathrm { r a n k } ( \tilde { H } _ { 2 } ) = n ,$ (B.5)

$$\operatorname{row} \left(\tilde {H} _ {1}\right) \subseteq \operatorname{row} \left(H _ {u}\right),\operatorname{row} \left(\tilde {H} _ {2}\right) \subseteq \operatorname{null} \left(H _ {u}\right).$$

The equivalence of (35) and (B.5) comes from

(1) For any feasible solution $\tilde { H } _ { y }$ of (35), we can let $\tilde { H } _ { 1 } = \tilde { H } _ { y } \Pi _ { 2 }$ and $\tilde { H } _ { w } = \tilde { H } _ { y } ( I - \Pi _ { 2 } )$ , which satisfies constraints for (B.5) and provides the same value for the objective function.

(2) Conversely, for any feasible solution $\tilde { H } _ { 1 }$ nad ${ \tilde { H } } _ { 2 }$ of (B.5), we can let $\tilde { H } _ { y } = \tilde { H } _ { 1 } + \tilde { H } _ { 2 }$ which is feasible for (35) with the same objective function value.

We can expand the objective function in (B.5) as

$$
\begin{array}{l} \left\| H _ {1} + H _ {2} - \tilde {H} _ {1} - \tilde {H} _ {2} \right\| _ {F} \\ = \operatorname{tr} \left(\left(H _ {1} + H _ {2} - \tilde {H} _ {1} - \tilde {H} _ {2}\right) \left(H _ {1} + H _ {2} - \tilde {H} _ {1} - \tilde {H} _ {2}\right) ^ {\intercal}\right) \\ = \operatorname{tr} ((H _ {1} - \tilde {H} _ {1}) (H _ {1} - \tilde {H} _ {1}) ^ {\top}) + \operatorname{tr} ((H _ {2} - \tilde {H} _ {2}) (H _ {2} - \tilde {H} _ {2}) ^ {\top}) \\ + 2 \operatorname{tr} \left(\left(H _ {1} - \tilde {H} _ {1}\right) \left(H _ {2} - \tilde {H} _ {2}\right) ^ {\top})\right) \\ = \| H _ {1} - \tilde {H} _ {1} \| _ {F} + \| H _ {2} - \tilde {H} _ {2} \| _ {F}, \\ \end{array}
$$
