$$
\begin{array}{l} \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} - D _ {U} \\ = \frac {D _ {U}}{\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} - D _ {U} + \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} - \frac {D _ {U}}{\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} \\ \leq \bar {w} - \epsilon^ {*} + D _ {U} \frac {\left(\hat {a} _ {s} - a ^ {*}\right) + \left(b ^ {*} - \hat {b} _ {s}\right) K _ {\text {opt}} \left(\hat {\theta} _ {s}\right)}{\left(a ^ {*} - b ^ {*} K _ {\text {opt}} \left(\hat {\theta} _ {s}\right)\right) \left(\hat {a} _ {s} - \hat {b} _ {s} K _ {\text {opt}} \left(\hat {\theta} _ {s}\right)\right)} (Eq(126)) \\ = \bar {w} - \epsilon^ {*} + D _ {U} \frac {(\hat {a} _ {s} - a ^ {*}) + (b ^ {*} - \hat {b} _ {s}) K _ {\mathrm{opt}} (\hat {\theta} _ {s})}{\left(\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}) - (\hat {a} _ {s} - a ^ {*}) - (b ^ {*} - \hat {b} _ {s}) K _ {\mathrm{opt}} (\hat {\theta} _ {s})\right) (\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}))} \\ \leq \bar {w} - \epsilon^ {*} + D _ {U} \frac {\| \theta^ {*} - \hat {\theta} _ {s} \| _ {\infty} (1 + | K _ {\mathrm{opt}} (\hat {\theta} _ {s}) |)}{(\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}) - \| \theta^ {*} - \hat {\theta} _ {s} \| _ {\infty} (1 + | K _ {\mathrm{opt}} (\hat {\theta} _ {s}) |)) (\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}))} \\ \leq \bar {w} - \epsilon^ {*} + \frac {D _ {U} \tilde {O} _ {T} (T ^ {- 1 / 4}) \left(1 + | K _ {\mathrm{opt}} (\hat {\theta} _ {s}) |\right)}{(\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}) - \tilde {O} _ {T} (T ^ {- 1 / 4}) (1 + | K _ {\mathrm{opt}} (\hat {\theta} _ {s}) |)) (\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}))} \\ \leq \bar {w} - \epsilon^ {*} / 2. \quad \text {Eq (128)} (127) \\ \end{array}
$$

To see the last inequality, note that Lemma 25 gives that $1 > \hat { a } _ { s } - \hat { b } _ { s } K _ { \mathrm { o p t } } ( \hat { \theta } _ { s } ) \geq c _ { \mathrm { L 2 5 } }$ . This implies that $| K _ { \mathrm { o p t } } ( \hat { \theta } _ { s } ) | = O _ { T } ( 1 )$ , and therefore for sufficiently large $T$ we have that
