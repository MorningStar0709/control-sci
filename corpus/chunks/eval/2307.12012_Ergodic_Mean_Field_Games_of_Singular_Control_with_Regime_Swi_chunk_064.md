thus completing the proof.

We are now in the position to provide the proof of Theorem 3.2

Proof of Theorem 3.2. By Proposition B.1, there exists a unique solution to $\mathbf { S P } ( \alpha ( \theta ) , \beta ( \theta ) , x , i )$ , denoted by $( X ^ { \xi ^ { * } ( \theta ) } , \xi ^ { * } ( \theta ) )$ . This satisfies the properties collected in Definition 3.1. We prove that $\xi ^ { * } ( \theta ) \in \mathcal { A } _ { e }$ and verify that the optimal policy has the form as in (3.18). By Property (1) in Definition 3.1, we have that

$$X _ {t} ^ {\xi^ {*} (\theta)} \in [ \alpha_ {Y _ {t}} (\theta), \beta_ {Y _ {t}} (\theta) ], \forall t \geq 0 \mathbb {P} _ {(x, i)} - a. s.,$$

so that

$$\operatorname * {l i m s u p} _ {T \uparrow \infty} \frac {1}{T} \mathbb {E} _ {(x, i)} \left[ | X _ {T} ^ {\xi^ {*} (\theta)} | \right] = 0. \tag {B.8}$$

It remains to show that $\mathbb { E } \big [ | \xi _ { T } ^ { * } ( \theta ) | \big ] < \infty$ for any $T < \infty$ . To that end, let $T < \infty$ and $\psi : \mathcal { O } \to$ R be any classical solution of

$$
\left\{ \begin{array}{l} \mathcal {L} _ {(X, Y)} \psi (x, i) = 0, x \in (\alpha_ {i} (\theta), \beta_ {i} (\theta)), i \in \mathbb {Y} \\ \psi_ {x} (\alpha_ {i} (\theta), i) = 1, \quad \psi_ {x} (\beta_ {i} (\theta), i) = - 1. \end{array} \right. \tag {B.9}
$$

Applying It ˆo-Meyer’s formula to $\{ \psi ( X _ { t } ^ { \xi ^ { * } ( \theta ) } , Y _ { t } ) \} _ { t \geq 0 }$ on [0, T ] we obtain

$$
\begin{array}{l} \mathbb {E} _ {(x, i)} \big [ \psi (X _ {T} ^ {\xi^ {*} (\theta)}, Y _ {T}) \big ] = \psi (x, i) + \mathbb {E} _ {(x, i)} \bigg [ \int_ {0} ^ {T} \mathcal {L} _ {(X, Y)} \psi (X _ {t} ^ {\xi^ {*} (\theta)}, Y _ {t}) d t \bigg ] \\ + \mathbb {E} _ {(x, i)} \bigg [ \int_ {0} ^ {T} \psi_ {x} (X _ {t} ^ {\xi^ {*} (\theta)}, Y _ {t}) d \xi_ {t} ^ {*} (\theta) \bigg ], \\ \end{array}
$$

and by using the fact that ψ is a solution to (B.9) we conclude that

$$\mathbb {E} _ {(x, i)} \left[ \psi (X _ {T} ^ {\xi^ {*} (\theta)}, Y _ {T}) \right] = \psi (x, i) + \mathbb {E} _ {(x, i)} \left[ | \xi_ {T} ^ {*} (\theta) | \right].$$

Since $X ^ { \xi ^ { * } ( \theta ) } \in [ \alpha _ { Y _ { t } } ( \theta ) , \beta _ { Y _ { t } } ( \theta ) ]$ , for any $t \geq 0 \quad \mathbb { P } _ { ( x , i ) } { \cdot } \mathbf { a . s } .$ ., we obtain that

$$\mathbb {E} _ {(x, i)} \left[ \psi (X _ {T} ^ {\xi^ {*} (\theta)}, Y _ {T}) \right] \leq \max _ {i \in \mathbb {Y}} \max _ {x \in [ \alpha_ {i} (\theta), \beta_ {i} (\theta) ]} | \psi (x, i) | < \infty ,$$

and therefore
