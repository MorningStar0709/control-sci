An infinite run of $\boldsymbol { A } _ { t }$ starting at a state $( q _ { 1 } , v _ { 1 } )$ is an infinite sequence of time and discrete transitions $\left( q _ { 1 } , v _ { 1 } \right) \stackrel { \delta _ { 1 } } { \longrightarrow } \left( q _ { 1 } ^ { \prime } , v _ { 1 } ^ { \prime } \right) \stackrel { e _ { 1 } } { \longrightarrow } \left( q _ { 2 } , v _ { 2 } \right) \stackrel { \delta _ { 2 } } { \longrightarrow } \left( q _ { 2 } ^ { \prime } , v _ { 2 } ^ { \prime } \right) \ldots ,$ where $e _ { i } = ( q _ { i } , g _ { i } , R _ { i } , \sigma _ { i } , q _ { i } ^ { \prime } ) , \forall i \in \mathbb { N }$ . This run corresponds to the timed word $\mathsf { w } _ { t } = ( \sigma _ { 1 } , \tau _ { 1 } ) ( \sigma _ { 2 } , \tau _ { 2 } )$ , with $\tau _ { i + 1 } = \tau _ { i } + \delta _ { i } , \forall i \in \mathbb { N }$ . The run is called accepting if $q _ { j } \in F$ for infinitely many $j \in \mathbb N$ . A timed word is called accepting if there exists an accepting run associated with it. The problem of deciding the language emptiness of a given TBA is PSPACE-complete [305]. In other words, an accepting run of a given TBA can be synthesized, if one exists. $\operatorname { A n y }$ timed formula $\Phi$ over Ψ originating from the decidable fragment of timed logics (e.g., MITL, discrete-time MTL, finite $\mathrm { M T L ^ { 2 } }$ , coFlat-MTL, Bounded-MTL [382, 383]) can be algorithmically translated into a TBA with input alphabet $2 ^ { \Psi }$ , such that the language of timed words that satisfy $\varphi$ is the language of timed words produced by the TBA.
