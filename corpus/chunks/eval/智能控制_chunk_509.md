# 10.6.2 TSP问题的遗传算法设计

采用遗传算法进行路径优化,分为以下几步。

第1步:参数编码和初始群体设定

一般来说,遗传算法对解空间的编码大多采用二进制编码形式,但对于 TSP 一类排序问题,采用对访问城市序列进行排列组合的方法编码,即某个巡回路径的染色体个体是该巡回路径的城市序列。

针对 TSP 问题, 编码规则通常是取 N 进制编码, 即每个基因仅从 1 到 N 的整数里取一个值, 每个个体的长度为 N, N 为城市总数。定义一个 s 行 t 列的 pop 矩阵来表示群体, t 为城市个数 +1, 即 $N+1$ , s 为样本中个体数目。针对 30 个城市的 TSP 问题, t 取值为 31, 即矩阵每一行的前 30 个元素表示经过的城市编号, 最后一个元素表示经过这些城市要走的距离。

参数编码和初始群体设定 Matlab 程序为

```matlab
pop = zeros(s, t);
for i = 1:s
    pop(i, 1:t-1) = randperm(t-1);
end 
```

其中 randperm(t-1) 为随机生成 1 到 t-1 个自然数。

第2步:计算路径长度的函数设计

在 TSP 的求解中,用距离的总和作为适应度函数,来衡量求解结果是否最优。将 pop 矩阵中每一行表示经过的距离的最后一个元素作为路径长度。

两个城市 $m$ 和 $n$ 间的距离为

$$d _ {m n} = \sqrt {\left(x _ {m} - x _ {n}\right) ^ {2} + \left(y _ {m} - y _ {n}\right) ^ {2}} \tag {10.4}$$

用于计算路径长度的程序为 chap10\_1dis.m。

通过样本的路径长度可以得到目标函数和自适应度函数。根据 t 的定义,两两城市组合数共有 t-2 组,则目标函数为

$$J (t) = \sum_ {j = 1} ^ {t - 2} d (j) \tag {10.5}$$

自适应度函数取目标函数的倒数,即

$$f (t) = \frac {1}{J (t)} \tag {10.6}$$

第 3 步: 计算选择算子

选择就是从群体中选择优胜个体、淘汰劣质个体的操作,它是建立在群体中个体适应度评估基础上的。仿真中采用最优保存方法,即用群体中适应度最大的 c 个个体直接替换适应度最小的 c 个个体。选择算子函数为 chap10\_4select.m。

第4步:计算交叉算子

交叉算子在遗传算法中起着核心的作用,它是指将个体进行两两配对,并以交叉概率 $p_{c}$ 将配对的父代个体加以替换重组而生成新个体的操作。如果当前随机值大于 $p_{c}$ ，则随机选择两个个体进行交叉。

有序交叉法实现的步骤是：

(1) 随机选取两个交叉点 crosspoint(1) 和 crosspoint(2)。

(2) 两后代 $X_{1}^{\prime}$ 和 $X_{2}^{\prime}$ 先分别按对应位置复制双亲 $X_{1}$ 和 $X_{2}$ 匹配段中的两个子串 $A_{1}$ 和 $B_{1}$ , 即 $X_{1}^{\prime}$ 中的子串 $A_{1}$ 与 $X_{1}$ 中的子串 $A_{1}$ 相同, $X_{2}^{\prime}$ 中的子串 $B_{1}$ 与 $X_{2}$ 中的子串 $B_{1}$ 相同。

(3) 在对应位置交换 $X_{1}$ 和 $X_{2}$ 双亲匹配段 $A_{1}$ 和 $B_{1}$ 以外的城市, 如果交换后, 后代 $X_{1}^{\prime}$ 中的某一城市 a 与 $X_{1}^{\prime}$ 子串 $A_{1}$ 中的城市重复, 则在子串 $B_{1}$ 中找到与子子串 $A_{1}$ 中城市 a 对应位置处的城市 b, 并用城市 b 取代城市 a。如果城市 b 与 $X_{1}^{\prime}$ 子串 $A_{1}$ 中的城市还重复, 则在子串 $B_{1}$ 中找到与子串 $A_{1}$ 中 b 对应位置处的城市 c, 并用城市 c 取代城市 b, 直到 $X_{1}^{\prime}$ 中的城市均不重复为止。对后代 $X_{2}^{\prime}$ 也采用同样方法, 如图 10-4 所示。

![](image/2165050e74fbd3410241372363e515e2482556f0b62a1784c06e5edb9833fddb.jpg)

<details>
<summary>text_image</summary>

X₁: 9 8 | 4 5 6 7 1 | 3 2 0
          A₁
X₂: 8 7 | 1 4 0 3 2 | 9 6 5
          B₁
          ↓
X₁′: 8 3 | 4 5 6 7 1 | 9 0 2
          A₁
X₂′: 9 8 | 1 4 0 3 2 | 7 5 6
          B₁
</details>

图10-4 有序交叉算子

以图 10-4 为例, 具体的有序交叉法实现步骤为:

(1) 确定两个交叉点的位置分别为 2 和 7;

(2) 两后代 $X_{1}^{\prime}$ 和 $X_{2}^{\prime}$ 分别按对应位置复制双亲 $X_{1}$ 和 $X_{2}$ 匹配段中两个子串 $A_{1}$ 和 $B_{1}$ , 在对应位置交换 $X_{1}$ 和 $X_{2}$ 双亲匹配段 $A_{1}$ 和 $B_{1}$ 以外的城市, 得到 $X_{1}^{\prime}: 87 \mid 45671 \mid 965$ 。
