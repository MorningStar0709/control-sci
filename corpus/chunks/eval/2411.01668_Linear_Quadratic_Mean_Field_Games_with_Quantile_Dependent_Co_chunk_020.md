where $\begin{array} { r } { F : = \exp \left( \int _ { 0 } ^ { s } 2 \left( a - \frac { b ^ { 2 } } { r } \Pi _ { u } \right) d u \right) V _ { 0 } + \sigma ^ { 2 } \int _ { 0 } ^ { s } \exp \left( \int _ { \tau } ^ { s } 2 \left( a - \frac { b ^ { 2 } } { r } \Pi _ { u } \right) d u \right) } \end{array}$ dτ. Since $\| \Pi \| _ { \infty } \leq$ M for a given $M > 0$ , the terms inside the integrand I(s) are uniformly bounded: first,

$$| 2 a \Pi_ {s} - \frac {b ^ {2}}{r} \Pi_ {s} ^ {2} + q | \leq 2 | a | M + \frac {b ^ {2}}{r} M ^ {2} + q;$$

second, the exponential term inside I(s) that involves Π is also uniformly bounded as follows:

$$q \exp \left[ \Phi_ {\alpha} ^ {- 1} F ^ {\frac {1}{2}} \right] \leq q \exp \left[ | \Phi_ {\alpha} ^ {- 1} | \cdot (V _ {0} + \sigma^ {2} T) ^ {\frac {1}{2}} e ^ {(T (| a | + \frac {b ^ {2}}{r} M))} \right]$$

since $| F | \le \bigl ( V _ { 0 } + \sigma ^ { 2 } T \bigr ) e ^ { \bigl ( 2 T ( | a | + \frac { b ^ { 2 } } { r } M ) \bigr ) }$ . Let the uniform bound be denoted by

$$C := 2 | a | M + \frac {b ^ {2}}{r} M ^ {2} + q + q \exp \left[ \mu^ {*} + | \Phi_ {\alpha} ^ {- 1} | \cdot (V _ {0} + \sigma^ {2} T) ^ {\frac {1}{2}} e ^ {(T (| a | + \frac {b ^ {2}}{r} M))} \right] > 0$$

with $\mu ^ { * } : = \operatorname* { m a x } \{ \mu _ { 0 } , e ^ { a T } \mu _ { 0 } \}$ . Then we have

$$| \mathcal {T} (\Pi) (t _ {1}) - \mathcal {T} (\Pi) (t _ {2}) | \leq \int_ {t _ {1}} ^ {t _ {2}} | I (s) | d s \leq \int_ {t _ {1}} ^ {t _ {2}} C d s = C | t _ {2} - t _ {1} |.$$

Hence, if we choose $\begin{array} { r } { \delta = \frac { \varepsilon } { C } } \end{array}$ , then $| T ( \Pi ) ( t _ { 1 } ) - T ( \Pi ) ( t _ { 2 } ) | < \varepsilon$ for all $\Pi \in K$ whenever $| t _ { 1 } - t _ { 2 } | < \delta$ . That is, the set $\{ \mathcal { T } ( \Pi ) : \Pi \in K \}$ is equicontinuous.
