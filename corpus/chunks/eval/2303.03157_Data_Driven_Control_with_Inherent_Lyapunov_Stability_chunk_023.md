# B. Proof of Theorem IV.1

Proof. We prove the exponential stability of the closedloop system $f _ { u ^ { * } } ^ { * }$ by deducing an exponentially decaying upper bound of $\| x ( t ) \| _ { 2 }$ from that of the Lyapunov function $V ( x ( t ) )$ .

First, we compute the upper bound of V from the Lyapunov condition (5) which holds for the projected model. Let $x ( t )$ be a trajectory generated by the closed-loop system $f _ { u } ^ { * } ,$ ∗ with an initial state $x ( 0 ) \in \mathcal { X }$ . Then, for all $t \geq 0$ ,

$$\frac {d}{d t} V (x (t)) = \nabla V (x (t)) ^ {\top} f ^ {*} (x (t), u ^ {*} (x (t))) \tag {25}\leq - \alpha V (x (t)).$$

By integrating this inequality, we get the upper bound as

$$V (x (t)) \leq V (x (0)) e ^ {- \alpha t}. \tag {26}$$

Next, we deduce an exponentially decaying upper bound of $\| x ( t ) \| _ { 2 }$ from (26). From the positive definite structure of V in (10), $V ( x ) \geq \epsilon _ { \mathrm { p d } } \| x \| _ { 2 } ^ { 2 }$ , which implies

$$\| x (t) \| _ {2} \leq \sqrt {\frac {V (x)}{\epsilon_ {\mathrm{pd}}}} \leq \sqrt {\frac {V (x (0)) e ^ {- \alpha t}}{\epsilon_ {\mathrm{pd}}}}. \tag {27}$$

Then, the remaining step is expressing the upper bound in terms of $\| x ( 0 ) \| _ { 2 }$ . Given $r _ { 2 } \ge r _ { 1 } > 0$ , we define

$$M = \sup _ {x \in \mathbb {R} ^ {n}} \frac {V (x)}{\| x \| _ {2} ^ {2}} \text { s.t. } r _ {1} \leq \| x \| _ {2} \leq r _ {2}. \tag {28}$$

The feasible set of the optimization in (28) is closed and bounded, hence compact. Then, the supremum M is attained on the feasible set and, thus, is finite. Since the feasible set includes $B _ { r _ { 1 } , r _ { 2 } } , ( 2 8 )$ implies that for any $ { \boldsymbol { x } } ( 0 ) \in B _ { r _ { 1 } , r _ { 2 } } ,$ $V ( x ( 0 ) ) \leq M \| \dot { x } ( \bar { 0 } ) \| _ { 2 } ^ { 2 }$ . Together with (27),

$$\| x (t) \| _ {2} \leq \sqrt {\frac {V (x (0)) e ^ {- \alpha t}}{\epsilon_ {\mathrm{pd}}}} \leq \sqrt {\frac {M}{\epsilon_ {\mathrm{pd}}}} \| x (0) \| _ {2} e ^ {- \alpha t / 2}. \tag {29}$$

Thus, the system $f _ { u ^ { * } } ^ { * }$ ∗ is exponentially stable at the origin w.r.t. Br1,r2 . $B _ { r _ { 1 } , r _ { 2 } } .$

If there exists $c > 0$ such that $V ( x ) \ \leq \ c \| x \| _ { 2 } ^ { 2 }$ for all $x \in \mathcal { X }$ , then similarly (or by Proposition II.3), the system $f _ { u ^ { * } } ^ { * }$ is exponentially stable at the origin w.r.t. X . □
