8.14 Servo with flexible shaft A control system is to be designed for the servo of Problem 2.5 (Chapter 2). The design is to be carried out for the rigid-shaft case, with flexibility included as a modeling error.

a. With $K$ at its nominal value, calculate $P = \theta_2 / v$ ; calculate the same transfer function for $K = \infty$ , i.e., for a rigid shaft.   
b. Determine a multiplicative uncertainty frequency weight $W_{1}(s)$ so that $W_{1}(s)\Delta$ “covers” the modeling difference between nominal and infinite K, given the infinite-K model as the base case.

Using $\theta_{2}$ and $\theta_{2d}$ (the set point) as measured outputs, design a control that satisfies the following:

\- With $S = 1 - \theta_2 / \theta_{2d}$ and $W_{2}(s) = 10 / (\frac{s}{30} + 1)$ ,

$$\left| W _ {2} S \right| \leq 1, \quad \text { all } \omega ,$$

for the model with infinite $K$ .

- The system is robustly stable for the uncertainty $W_{1} \Delta$ .   
- The magnitude of $|\frac{1}{6}(v / \theta_{2d})| \leq 1$ , all $\omega$ , for the model with infinite $K$ .

Display the weighted transmission resulting from your design. (Note: It may be necessary to add fictitious noise inputs to the measured outputs.)

![](image/419ba6d3fce3cec442d1335e057ba4961351cf220aa96ada56f3d1aa65952ae6.jpg)

8.15 Drum speed control We wish to design a control system for the drum speed problem of Problem 2.6 (Chapter 2). The general objective is to regulate drum speed without inducing undue stresses in the two shafts. Assume the measurements to be $\omega_0, \Delta_1$ , and $\Delta_2$ , (the latter two through strain gauges). The load torque $T_0$ is assumed to be generated by passing a signal $w$ of unit 2-norm through the transfer function $1000/s$ . The requirements are as follows:

- $|\omega_0 / w| \leq 0.1$ at all frequencies.   
- $|\Delta_i / w| \leq 0.1, i = 1, 2$ , at all frequencies.

\- $|u_i / w| \leq U, i = 1, 2,$ at all frequencies.

Using an $H^{\infty}$ approach, find, by trial and error, the minimum U for which all specifications are satisfied. Display the weighted frequency responses that correspond to the specifications. It may be necessary to add fictitious measurement noise to the outputs.

M
