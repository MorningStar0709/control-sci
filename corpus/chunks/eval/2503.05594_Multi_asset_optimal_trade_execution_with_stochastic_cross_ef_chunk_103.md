Remark C.6. The fact that $\rho$ in Corollary C.5 is symmetric and commutes with $\gamma$ implies that there exist an orthogonal matrix $\widetilde { O } \in \mathbb { R } ^ { n \times n }$ and diagonal matrices $\widetilde { \rho } , \widetilde { \gamma } \in$ $\mathbb { R } ^ { n \times n }$ such that $\rho = \widetilde { O } ^ { \intercal } \widetilde { \rho } \widetilde { O }$ and $\gamma = \widetilde { O } ^ { \intercal } \widetilde { \gamma } \widetilde { O }$ e e(see, for example, [17, Chapter 4, Theorem 5]). The optimal strategy $X ^ { * }$ ecan thus be represented in the form $\widetilde { \cal O } X ^ { * } ( 0 - ) = \widetilde { \cal O } x , \widetilde { \cal O } X ^ { * } ( T ) =$ 0, and

$$
\begin{array}{l} \widetilde {O} X ^ {*} (s) = \left(I _ {n} + (T - s) \widetilde {\rho}\right) \left(2 I _ {n} + T \widetilde {\rho}\right) ^ {- 1} \widetilde {O} x \\ - \left(I _ {n} + (T - s) \widetilde {\rho}\right) \left(2 I _ {n} + T \widetilde {\rho}\right) ^ {- 1} \widetilde {\gamma} ^ {- 1} \widetilde {O} d, \quad s \in [ 0, T). \\ \end{array}
$$

Note that in this representation the position $X ^ { \ast }$ and the deviation $D ^ { * }$ are orthogonally transformed with $\bar { \tilde { O } }$ into ${ \widetilde { O } } X ^ { * }$ and $\widetilde O D ^ { * }$ . Moreover, observe that the components of ${ \widetilde { O } } X ^ { * }$ are the optimal strategies of certain single-asset optimal trade execution problems. Namely, for each $j \in \{ 1 , \ldots , n \}$ we have that $( \widetilde O X ^ { * } ) _ { j }$ is the optimal strategy in the single-asset model that has the resilience $\widetilde { \rho } _ { j , j }$ , the price impact $\widetilde { \gamma } _ { j , j }$ , the initial position $( \widetilde { O } x ) _ { j }$ , and the initial deviation $( \widetilde { O } d ) _ { j }$ .

Observe that if $d = 0$ in the setting $\mathrm { o f ^ { 9 } }$ Corollary C.5, then the optimal strategy does not depend on the price impact $\gamma .$ However, the optimal strategy can still be influenced by cross-effects between the assets. This is due to the resilience $\rho .$ Moreover, although the price impact $\gamma$ does not affect the optimal strategy, it has an impact on the optimal costs. Furthermore, note that if, in addition, $\rho$ is a diagonal matrix, then we obtain that the optimal strategy is composed of the optimal strategies for the respective single-asset models. In the next example we consider such a situation and analyze how the execution costs differ depending on whether cross-impact is taken into account.
