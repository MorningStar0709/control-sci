# Useful Properties

This appendix provides some additional technical properties that are useful throughout the thesis.

Proposition G.1. Let $f : \mathbb { R } _ { > 0 } \to \mathbb { R }$ , with $f ( x ) : = \exp ( x ) ( \exp ( x ) - 1 ) - x ^ { 2 }$ Then it holds that $f ( x ) \geq 0 , \forall x \in \mathbb { R } _ { \geq 0 }$ .

Proof. It holds tha t ∂f(x) = 2 exp(2x) − exp(x) − 2x > 0, ∀x ∈ R≥0. Hence, $\begin{array} { r } { \frac { \partial f ( x ) } { \partial x } = 2 \exp ( 2 x ) - \exp ( x ) - 2 x > 0 , \forall x \in \mathbb { R } _ { \ge 0 } } \end{array}$ ∂x $f ( x ) \geq f ( 0 ) = 0 , \forall x \in \mathbb { R } _ { \geq 0 }$ .

Proposition G.2. [169] Let $R _ { 1 } , R _ { 2 } \ \in \ \mathbb { S O } ( 3 )$ , and $e _ { R } : = S ^ { - 1 } ( R _ { 1 } ^ { \top } R _ { 2 } -$ $R _ { 2 } ^ { \top } R _ { 1 } )$ . Then $\begin{array} { r } { \| e _ { R } \| ^ { 2 } : = \| R _ { 1 } - R _ { 2 } \| _ { \mathrm { F } } ^ { 2 } \Big ( 1 - \frac { 1 } { 8 } \| R _ { 1 } - R _ { 2 } \| _ { \mathrm { F } } ^ { 2 } \Big ) } \end{array}$ .

Proposition G.3. Let $R _ { 1 } , R _ { 2 } \in \mathbb { S O } ( 3 )$ . Then, for the rotation matrix $R _ { 2 } ^ { \top } R _ { 1 } \in \mathbb { S } \mathbb { O } ( 3 )$ it holds that $- 1 \leq \operatorname { t r } ( R _ { 2 } ^ { \top } R _ { 1 } ) \leq 3 ; \operatorname { t r } ( R _ { 2 } ^ { \top } R _ { 1 } ) = 3$ if and only if $R _ { 2 } ^ { \top } R _ { 1 } = I _ { 3 } \Leftrightarrow R _ { 1 } = R _ { 2 } ; \mathrm { t r } [ R _ { 2 } ^ { \top } R _ { 1 } ] = - 1$ when $R _ { 1 } = R _ { 2 } \exp ( \pi \hat { s } )$ , for every sˆ in the unit sphere.

Let $x , y \in \mathbb { R } ^ { 3 } , R \in \mathbb { S } \mathbb { O } ( 3 )$ , and $A \in \mathbb { R } ^ { 3 \times 3 }$ . Then the following hold [170]:

• $x ^ { \top } S ( y ) x = 0 ;$   
• $S ( R x ) = R S ( x ) R ^ { \top }$ ;   
$\bullet \ : - \textstyle { \frac { 1 } { 2 } } \mathrm { t r } \left[ S ( x ) S ( y ) \right] = x ^ { \top } y ;$   
$\begin{array} { r } { \bullet \operatorname { t r } \left[ A S ( x ) \right] = \frac { 1 } { 2 } \mathrm { t r } \left[ S ( x ) ( A - A ^ { \top } ) \right] = - x ^ { \top } S ^ { - 1 } ( A - A ^ { \top } ) . } \end{array}$

We provide next some useful properties of linear algebra.

Definition G.1. A matrix $A \in \mathbb { R } ^ { n \times m }$ is left equivalent (or row equivalent ) to a matrix $B \in \mathbb { R } ^ { n \times m }$ if and only if there exists an invertible matrix $P \in \mathbb { R } ^ { n \times n }$ such that $A = P B$ .

The following propositions can be proved:

Proposition G.4. Let $A , B \in \mathbb { R } ^ { n \times m }$ Then A and B are left equivalent if and only if they have the same nullspace nu $\operatorname { l l } ( A ) = \operatorname { n u l l } ( B ) , \operatorname { i . e . , } A x = 0 \Leftrightarrow$ $B x = 0$ , for any $x \in \mathbb { R } ^ { m }$ .
