For $x > 0$ and $k \in \mathbb { N } , \ x ^ { k } / k ! \leq \exp ( x )$ . Thus, $\begin{array} { r } { \frac { e c _ { u } ^ { \kappa } 2 ^ { \kappa - 1 } } { ( \kappa - 1 ) ! ( 1 - \epsilon ) ^ { \kappa - 1 } } \leq e c _ { u } \exp ( \frac { 2 c _ { u } } { 1 - \epsilon } ) = : c _ { e } } \end{array}$ , which is an ϵ−dependent constant. Next we upper bound the term $\textstyle \prod _ { j = b + 1 } ^ { i } p _ { j }$ using integral lower bound as below:

$$(b + 1) ^ {1 - \epsilon} + \dots + i ^ {1 - \epsilon} \geq \frac {1}{2 - \epsilon} \left(i ^ {2 - \epsilon} - b ^ {2 - \epsilon}\right). \tag {65}$$

Thus, using the above discussion, we simplify (63) to get

$$\mathbb {P} \left(y _ {1} + \dots + y _ {i} \leq n\right) \leq c _ {e} \exp \left(- \frac {1}{2 - \epsilon} \left(i ^ {2 - \epsilon} - b ^ {2 - \epsilon}\right)\right) n ^ {i - b} i ^ {\epsilon (\kappa - 1)}. \tag {66}$$

We upper bound the summation given in the statement of Lemma 6. From (66) and using the fact that $d \geq b ;$

$$
\begin{array}{l} \sum_ {i = d} ^ {n} i \mathbb {P} (y _ {1} + \ldots + y _ {i} \leq n) \leq c _ {e} \sum_ {i = d} ^ {n} i \exp \left(- \frac {1}{2 - \epsilon} (i ^ {2 - \epsilon} - b ^ {2 - \epsilon})\right) (n + 1) ^ {i - b} i ^ {\epsilon (\kappa - 1)} \\ \leq c _ {e} (n + 1) ^ {- b} \exp \left(\frac {b ^ {2 - \epsilon}}{2 - \epsilon}\right) \sum_ {i = d} ^ {\infty} i \exp \left(- \frac {i ^ {2 - \epsilon}}{2 - \epsilon} + i \log (n + 1) + \frac {\epsilon}{1 - \epsilon} \log (i) i ^ {\epsilon}\right) \\ \leq \tilde {c} _ {e} \exp \left(- b \log (n + 1) + \frac {b ^ {2 - \epsilon}}{2 - \epsilon}\right) \\ \leq \tilde {c} _ {e} \exp \Big (- b (b - 1) ^ {1 - \epsilon} + \frac {b ^ {2 - \epsilon}}{2 - \epsilon} \Big) = \tilde {c} _ {e} \exp \left(- b ^ {2 - \epsilon} \bigg (\left(1 - \frac {1}{b}\right) ^ {1 - \epsilon} - \frac {1}{2 - \epsilon} \bigg)\right), \\ \end{array}
$$

where we have used $b = \lceil ( \log ^ { \frac { 1 } { 1 - \epsilon } } ( n + 1 ) ) \rceil$ in the last line. The third inequality holds as for $i \geq d ,$ the negative term inside the second exponential function is dominating. Further, as n grows, b converges to infinity; hence, in the final term, the exponential term converges to zero. Thus, we can bound the sum with a constant. □
