Without restriction we can take same constants ?? and ?? for ?? and ??.

Let us set, for any admissible control ?? and $T > 0$ the following Girsanov’s change of probability

$$\rho_ {T} ^ {\pmb {a}} = \exp \left(- \frac {1}{2} \int_ {0} ^ {T} | R a _ {t} | ^ {2} \mathrm{d} s + \int_ {0} ^ {T} (R a _ {t}) ^ {\top} \mathrm{d} W _ {t}\right) \quad \mathrm{and} \quad \mathbb {P} _ {T} ^ {\pmb {a}} = \rho_ {T} ^ {\pmb {a}} \mathbb {P}.$$

Thanks to our assumptions, $\begin{array} { r } { W _ { t } ^ { \pmb { a } } : = W _ { t } - \int _ { 0 } ^ { t } R a _ { s } \mathrm { d } s } \end{array}$ is a P???? −Brownian motion and so, we can write $X ^ { [ \theta ] }$ and $X ^ { x , [ \theta ] }$ the respective solutions to (MV-SDE) and (Decoupled SDE) w.r.t to that new Brownian motion without control on the distribution. Namely,

$$\mathrm{d} X _ {t} ^ {[ \theta ], \boldsymbol {a}} = \left(b (X _ {t} ^ {[ \theta ], \boldsymbol {a}}, [ X _ {t} ^ {[ \theta ], \boldsymbol {a}} ]) + \sigma (X _ {t} ^ {[ \theta ], \boldsymbol {a}}, [ X _ {t} ^ {[ \theta ], \boldsymbol {a}} ]) R a _ {t}\right) \mathrm{d} t + \sigma (X _ {t} ^ {[ \theta ], \boldsymbol {a}}, [ X _ {t} ^ {[ \theta ], \boldsymbol {a}} ]) \mathrm{d} W _ {t} ^ {\boldsymbol {a}}, \tag {5.1}$$

and

$$\mathrm{d} X _ {t} ^ {x, [ \theta ], \boldsymbol {a}} = \left(b (X _ {t} ^ {x, [ \theta ], \boldsymbol {a}}, [ X _ {t} ^ {[ \theta ], 0} ]) + \sigma (X _ {t} ^ {x, [ \theta ], \boldsymbol {a}}, [ X _ {t} ^ {[ \theta ], 0} ]) R a _ {t}\right) \mathrm{d} t + \sigma (X _ {t} ^ {x, [ \theta ], \boldsymbol {a}}, [ X _ {t} ^ {[ \theta ], 0} ]) \mathrm{d} W _ {t} ^ {\boldsymbol {a}}, \tag {5.2}$$

where $X ^ { \cdot , 0 }$ satisfies (5.1) without control, i.e. $a _ { t } = 0$ for all $t \geq 0$ .

Remark 5.1. We stress the fact that (5.2) is no longer of McKean-Vlasov’s type but still have a distribution dependency w.r.t a McKean-Vlasov SDE.

Let us set the finite horizon cost as

$$J ^ {T} (x, [ \theta ], \pmb {a}) = \mathbb {E} _ {T} ^ {\pmb {a}} \biggl [ \int_ {0} ^ {T} L (X _ {s} ^ {x, [ \theta ], \pmb {a}}, [ X _ {s} ^ {[ \theta ], 0} ], a _ {s}) \mathrm{d} s \biggr ] + \mathbb {E} _ {T} ^ {\pmb {a}} \biggl [ g (X _ {T} ^ {x, [ \theta ], \pmb {a}}, [ X _ {T} ^ {[ \theta ], 0} ]) \biggr ]$$

and the associated optimal control problem as the minimization of $J ^ { T }$ over all admissible control ??. We can also define the ergodic cost as
