# Exercise A.39: Ordering random variables

We can order two random variables A and B if they obey an inequality such as $A \geq B$ . The frequency interpretation of the probability distribution, $P _ { A } ( c ) = \operatorname* { P r } ( A \leq c )$ , then implies that $P _ { A } ( c ) \leq P _ { B } ( c )$ for all c.

If $A \geq B ,$ show that

$$\mathcal {E} (A) \geq \mathcal {E} (B)$$
