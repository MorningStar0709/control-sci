# TLA Algorithm

Initialize Slow network with critics $Q _ { \theta _ { l 1 } } ^ { l } , Q _ { \theta _ { l 2 } } ^ { l }$ and actor $\pi _ { \phi _ { l } } ^ { l }$

Initialize Fast network with critics $Q _ { \theta _ { q 1 } } ^ { q } , Q _ { \theta _ { q 2 } } ^ { q }$ Qθq 2 and actor πqϕq $\pi _ { \phi _ { q } } ^ { q }$

Initialize Gate network with Q-function $Q _ { \theta } ^ { g }$

Initialize target networks $\theta _ { l 1 } ^ { \prime }  \theta _ { l 1 } , \theta _ { l 2 } ^ { \prime }  \theta _ { l 2 } , \phi _ { l } ^ { \prime }  \phi _ { l } , \theta _ { q 1 } ^ { \prime }  \theta _ { q 1 } , \theta _ { q 2 } ^ { \prime }  \theta _ { q 2 } , \phi _ { q } ^ { \prime } $ $\phi _ { q }$

Initialize replay buffers $B _ { l } , B _ { q } , B _ { g }$

for t = 1 to $T$ do

if t mod τ then

select slow action with exploration noise $a _ { l } \sim \pi ^ { l } ( s ) + \epsilon , \epsilon \sim \mathcal { N } ( 0 , \sigma )$

select gate action with epsilon greedy policy $a _ { g } \sim \pi ^ { g } ( s , a _ { l } )$

if $a _ { g } = 0$ then

$$r _ {l} = 0r _ {g} = 0s _ {l} = 0$$

else

$$r _ {l} = - p * \taur _ {g} = - j * \tau$$

end if

end if

if $a _ { g } = 1$ then

select fast action with exploration noise $a _ { q } \sim \pi ^ { q } ( s ) + \epsilon , \epsilon \sim \mathcal { N } ( 0 , \sigma )$

$$a = a _ {q}$$

else

$$a = a _ {l}$$

end if

Perform the action a and observe the reward r and new state $s ^ { \prime }$

if $a _ { g } = 1$ then

$$r = r - j (a / a _ {m a x})$$

end if

Store transition tuple $( s , a , r , s ^ { \prime } )$ in $B _ { q }$

$$r _ {p} + = rr _ {g} + = r$$

if t + 1 mod τ then

Store transition tuple $( s _ { l } , a _ { l } , r _ { l } , s ^ { \prime } )$ in $\boldsymbol { B _ { l } }$

Store transition tuple $( ( s _ { l } , a _ { l } ) , a _ { g } , r _ { g } , s ^ { \prime } )$ in $B _ { g }$

end if

Sample mini-batches and update parameters for slow and fast according to TD3

Sample mini-batch and update parameters for Gate according to Q-learning

end for
