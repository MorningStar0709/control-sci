# Exercise A.9: Continuous function

Show that $f : \mathbb { R } ^ { n }  \mathbb { R } ^ { m }$ is continuous at xˆ implies $f ( x _ { i } )  f ( { \hat { x } } )$ for any sequence $\{ x _ { i } \}$ satisfying $x _ { i } \to { \hat { x } } \operatorname { a s } i \to \infty$ .
