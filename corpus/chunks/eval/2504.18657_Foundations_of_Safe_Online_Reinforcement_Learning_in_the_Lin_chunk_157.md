# I General Lemmas

The following four lemmas are used throughout the appendix and follow directly from results in Schiffer and Janson [2024].

Lemma 40 (Lemma 13 in Schiffer and Janson [2024]). Suppose $w _ { t }$ for $t < T$ are sub-Gaussian and F is an event such that $\mathbb { P } ( F ) = 1 - o _ { T } ( 1 / T ^ { 1 1 } )$ . Then

$$\mathbb {E} [ \max _ {i \leq t} w _ {i} ^ {2} | \neg F ] \mathbb {P} (\neg F) = o _ {T} \left(\frac {1}{T ^ {1 0}}\right).$$

Lemma 41 (Lemma 11 in Schiffer and Janson [2024]). Let $x , y$ be two random variables independent of noises $W ^ { \prime } = \{ w _ { i } ^ { \prime } \} _ { i = 0 } ^ { t - 1 }$ such that for some $L = \tilde { O } _ { T } ( 1 )$ , both $\mathbb { P } ( | x | \geq L ) \mathbb { E } [ x ^ { 2 } \ |$ $\begin{array} { r } { | x | \ge L ] = o _ { T } \left( \frac { 1 } { T ^ { 1 0 } } \right) } \end{array}$ and $\begin{array} { r } { \mathbb { P } ( | y | \geq L ) \mathbb { E } [ y ^ { 2 } \mid | y | \geq L ] = o _ { T } \left( \frac { 1 } { T ^ { 1 0 } } \right) } \end{array}$ and $\mathbb { P } ( | x | \leq 4 \log ^ { 2 } ( T ) ) =$ $1 - o _ { T } ( 1 / T ^ { 1 1 } )$ and $\mathbb { P } ( | y | \le 4 \log ^ { 2 } ( T ) ) = 1 - o _ { T } ( 1 / T ^ { 1 1 } )$ . Then under Assumptions 1–3, if $\lVert \theta - \theta ^ { * } \rVert _ { \infty } = \epsilon \leq \epsilon _ { \mathrm { L 2 } }$ , then for any $K \in ( \frac { a - 1 } { b } , \frac { a } { b } )$ and $t \leq T$ ,

$$\left| \mathbb {E} \left[ t \cdot J (\theta^ {*}, C _ {K} ^ {\theta}, t, x, W ^ {\prime}) - t \cdot J (\theta^ {*}, C _ {K} ^ {\theta}, t, y, W ^ {\prime}) \right] \right| = \tilde {O} _ {T} \left(\mathbb {E} [ | x - y | ] + \epsilon + \frac {1}{T ^ {2}}\right). \tag {154}$$

proof. This follows directly by Lemma 11 in Schiffer and Janson [2024] and Lemmas 1 and 2. □

Lemma 42 (Lemma 12 in Schiffer and Janson [2024]). Let $x _ { 0 } , x _ { 1 } , . . . x _ { T }$ be the sequences of states when starting at state $x _ { 0 } = x$ and using controller $C _ { t }$ at time t. Suppose that the control $C _ { t } ( x _ { t } )$ is safe for dynamics $\theta _ { t }$ and $\begin{array} { r } { \| \theta _ { t } - \theta ^ { * } \| \le \frac { 1 } { \log ( T ) } } \end{array}$ for all $t < T$ . For sufficiently large $T _ { \perp }$ ,

$$\forall t \leq T, | x _ {t} | = O _ {T} (| x | + \| D \| _ {\infty} + \max _ {i \leq t - 1} | w _ {i} |).\forall t < T, | C _ {t} (x _ {t}) | = O _ {T} (| x | + \| D \| _ {\infty} + \max _ {i \leq t - 1} | w _ {i} |).$$

Lemma 43 (Lemma 4 in Schiffer and Janson [2024]). Let $| x _ { 0 } | \leq 4 \log ^ { 2 } ( T )$ . Suppose for all $t < T$ , the control used by controller $C _ { t }$ at time t is safe for fixed dynamics $\theta _ { t }$ and for all $t \leq T$ ,
