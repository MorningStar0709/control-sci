$$\dot {V} _ {\xi} \leq - \mu e _ {r e f} ^ {\mathrm{T}} P e _ {r e f} + t r \left\{\tilde {\theta} ^ {\mathrm{T}} \omega \omega^ {\mathrm{T}} \tilde {\theta} - \tilde {\theta} ^ {\mathrm{T}} \left[ \frac {\left(\gamma_ {0} \lambda_ {\max} (\omega \omega^ {\mathrm{T}}) + \gamma_ {1}\right) \Omega^ {2}}{\Omega^ {2}} \tilde {\theta} + \frac {\left(\gamma_ {0} \lambda_ {\max} (\omega \omega^ {\mathrm{T}}) + \gamma_ {1}\right) \Omega}{\Omega^ {2}} \varsigma \right] \right\} \leq \tag {F33}\leq - \mu e _ {r e f} ^ {\mathrm{T}} P e _ {r e f} + t r \left\{\tilde {\theta} ^ {\mathrm{T}} \omega \omega^ {\mathrm{T}} \tilde {\theta} - \tilde {\theta} ^ {\mathrm{T}} \left(\gamma_ {0} \lambda_ {\max} \left(\omega \omega^ {\mathrm{T}}\right) + \gamma_ {1}\right) \tilde {\theta} - \tilde {\theta} ^ {\mathrm{T}} \left(\frac {\left(\gamma_ {0} \lambda_ {\max} \left(\omega \omega^ {\mathrm{T}}\right) + \gamma_ {1}\right)}{\rho} \varsigma\right) \right\}.$$

It holds for any possible unbounded ?? (??) that:

$$\left. t r \left\{\tilde {\theta} ^ {\mathrm{T}} (t) (\omega (t) \omega (t) ^ {\mathrm{T}} - \left[ \gamma_ {0} \lambda_ {\max} (\omega (t) \omega^ {\mathrm{T}} (t)) + \gamma_ {1} \right] I _ {(m + n) \times (m + n)}) \tilde {\theta} (t) \right\} \leq - \gamma_ {1} t r \left\{\tilde {\theta} ^ {\mathrm{T}} (t) \tilde {\theta} (t) \right\}. \right. \tag {E34}$$

So (E33) is written as:

$$\dot {V} _ {\xi} \leq - \mu e _ {r e f} ^ {\mathrm{T}} P e _ {r e f} - \gamma_ {1} t r \left\{\tilde {\theta} ^ {\mathrm{T}} \tilde {\theta} + \tilde {\theta} ^ {\mathrm{T}} \frac {\gamma_ {0} \lambda_ {\max} (\omega \omega^ {\mathrm{T}}) + \gamma_ {1}}{\rho} \varsigma \right\} \leq - \mu \lambda_ {\min} (P) \| e _ {r e f} \| ^ {2} - \gamma_ {1} \| \tilde {\theta} \| ^ {2} + \frac {\gamma_ {0} \lambda_ {\max} (\omega \omega^ {\mathrm{T}}) + \gamma_ {1}}{\rho} \varsigma_ {\mathrm{UB}} \| \tilde {\theta} \|. \tag {E35}$$

Assuming ?? = √??1 ‖??̃‖ , ?? = ??0??max(????T)+??1√ ?? $\begin{array} { r } { a = \sqrt { \gamma _ { 1 } } \left\| \tilde { \theta } \right\| , b = \frac { \gamma _ { 0 } \lambda _ { \mathrm { m a x } } \left( \omega \omega ^ { \mathrm { T } } \right) + \gamma _ { 1 } } { \rho \sqrt { \gamma _ { 1 } } } \varsigma _ { \mathrm { U B } } } \end{array}$ ?? ??1 , and applying the inequality $- a ^ { 2 } + a b \leq - { \frac { 1 } { 2 } } a ^ { 2 } + { \frac { 1 } { 2 } } b ^ { 2 }$ , it follows from (E35) that:
