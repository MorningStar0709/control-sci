# (4) 最小实现

对于不可控又不可观测的线性定常系统,其传递函数矩阵只能描述系统中可控且可观测的那一部分,是对系统结构的一种不完全描述。从状态空间角度,系统规范分解后的可控、可观测子系统为原系统的最小实现。而从传递函数角度,将系统传递函数零、极点对消后,所得的模型为最小实现形式。MATLAB中的minreal命令可用来直接求取系统的最小实现。

命令格式: sysr = minreal(sys)
