Remark 10: It is because (65) is based on timeregularization that $\varsigma _ { d }$ can take any value in $\mathbb { R } _ { > 0 }$ in (64). When $\varsigma _ { d }$ is bigger than the decay rate of the Lyapunov function V along the solutions to the considered system, a transmission instant will occur immediately at $t _ { k } + \tau _ { d } ( \sigma )$ with $k \in \mathcal N$ and $\tau _ { d } ( \sigma )$ -periodic sampling will occur. A reasonable option is to select $\varsigma _ { d } > 0$ such that $\varsigma _ { d } S < S \Omega S$ in view of (30c), so that the decay rate of η is not faster than the one of $V ( x )$ along the solutions to the system ignoring sampling and d. l
