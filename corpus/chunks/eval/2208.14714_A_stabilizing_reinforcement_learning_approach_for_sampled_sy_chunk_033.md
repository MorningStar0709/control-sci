Case 1.1 - repeated violation: If at two consecutive sampling times kδ and $( k + 1 ) \delta$ the desired decay is not achieved, i. e., the bounds $\bar { \varepsilon } _ { 1 , w }$ are not satisfied for $( u _ { k } , w _ { k } )$ , the backup pair $( \mu ( x _ { k } , \hat { \theta } _ { k } ) , w ^ { \# } )$ will result in recovering the decay of the Lyapunov function $V _ { c }$ associated with V as per Lemma 1 on the set $\begin{array} { r l } { B _ { R ^ { * } } } & { { } \backslash \ B _ { r } , } \end{array}$ ∗ as well as τ of the parameter estimation dynamics (6) as follows.

With $\hat { J } ( x _ { k } , w _ { k } ) = V ( x _ { k } ) , \mathcal { V } _ { c }$ reduces to $V _ { c }$ of Lemma 1 and the decay rate for $\delta  0$ will be given by ν as per (8). Since along $\mathcal { F } ( \boldsymbol { x } , \boldsymbol { u } , \theta )$ and $\Gamma \tau ( x , w ^ { \# } )$ , the Taylor expansion yields

$$V _ {c} \left(x _ {k + 1} (\mu (x _ {k}, \hat {\theta} _ {k})), \tilde {\theta} _ {k + 1}\right) - V _ {c} (x _ {k}, \tilde {\theta} _ {k})\leq - \delta \nu (x _ {k}, \hat {\theta} _ {k}) + \sigma (\delta^ {2})$$

for any $\delta > 0 ,$ , given balls $B _ { r ^ { * } , R ^ { * } }$ , there exists $\bar { \delta } _ { a } > 0$ such that for any $0 < \delta \leq \bar { \delta } _ { a }$ , for all $x _ { k } \in B _ { R ^ { * } } \setminus B _ { r ^ { * } } , k \in \mathbb { N } _ { 0 }$ ,

$$\Delta_ {k} \mathcal {V} _ {c} = V _ {c} \left(x _ {k + 1} \left(\mu (x _ {k}, \hat {\theta} _ {k})\right), \tilde {\theta} _ {k + 1}\right) - V _ {c} (x _ {k}, \tilde {\theta} _ {k}) \tag {48}\leq - \frac {\delta}{2} \nu (x _ {k}, \hat {\theta} _ {k}) \leq - \delta \bar {\nu},$$

with consecutive employment of $w _ { k , k + 1 } = w ^ { \# }$ and $u _ { k } ~ =$ $\mu ( x _ { k } , \hat { \theta } _ { k } )$ .

Case 1.2 - single violation: Consider (43) and (44), by which

$$
\begin{array}{l} \Delta_ {k} \mathcal {V} _ {c} = \Delta_ {k} \hat {J} + \Delta_ {k} \mathcal {T} \\ = \hat {J} (x _ {k + 1} (u _ {k}), w _ {k + 1}) - \hat {J} (x _ {k}, w _ {k}) + \Delta_ {k} \mathcal {T} \\ - \hat {J} (x _ {k + 1} (u _ {k}), w _ {k}) + \hat {J} (x _ {k + 1} (u _ {k}), w _ {k}) \\ \leq - \frac {\delta}{2} \nu (x _ {k}, \hat {\theta} _ {k}) + \varepsilon_ {1} + \delta^ {2} \bar {\Delta} \\ + \hat {J} (x _ {k + 1} (u _ {k}), w _ {k + 1}) - \hat {J} (x _ {k + 1} (u _ {k}), w _ {k}). \\ \end{array}
$$

If the conditions are violated at k+1 but hold at $k , w _ { k + 1 } = w ^ { \# }$ and thus

$$\underbrace {\hat {J} (x _ {k + 1} (u _ {k}) , w _ {k + 1})} _ {= V (x _ {k + 1} (u _ {k}))} - \underbrace {\hat {J} (x _ {k + 1} (u _ {k}) , w _ {k})} _ {\geq V (x _ {k + 1} (u _ {k}))}\leq V (x _ {k + 1} (u _ {k})) - V (x _ {k + 1} (u _ {k})) = 0.$$
