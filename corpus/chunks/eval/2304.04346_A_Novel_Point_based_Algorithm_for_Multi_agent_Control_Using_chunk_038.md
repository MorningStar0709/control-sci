DecTiger(3, 1, 0.99): α-constraints   
![](image/fc8c2c664ff10c4c8bba617f8f03057a0c0ea3fcf51e6da7066d63c7a950dfc1.jpg)  
Fig. 3. Number of α-vectors and α-constraints for DecTiger $( 2 , 1 , \beta )$ . $\mathcal { \nu } ^ { \mathcal { \ell } }$ is the set of α-vector for stage \` defined in Algorithm 4. ${ \bf \bar { \boldsymbol { c } } } ^ { \ell }$ is the set of α-constraints for stage \` defined in Algorithm 5.   
Fig. 4. Number of α-vectors and α-constraints for DecTiger (3, 1, β). $\nu ^ { \widetilde { \ell } }$ is the set of α-vector for stage \` defined in Algorithm 4. $\mathit { \bar { c } } ^ { \ell }$ is the set of α-constraints for stage \` defined in Algorithm 5.
