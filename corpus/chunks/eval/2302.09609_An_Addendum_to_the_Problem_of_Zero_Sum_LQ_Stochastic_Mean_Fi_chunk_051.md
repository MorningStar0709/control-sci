$\begin{array} { r } { \tilde { v } ( t , \tilde { x } ^ { 1 } , \tilde { x } ^ { 2 } ) = \frac { 1 } { 2 } \left( \left. X ( t ) \tilde { x } ^ { 1 } , \tilde { x } ^ { 1 } \right. + \left. \hat { X } ( t ) \tilde { x } ^ { 2 } , \tilde { x } ^ { 2 } \right. \right) , t \in [ s , T ] , \tilde { x } ^ { 1 } \in \mathbb { R } ^ { n } , \tilde { x } ^ { 2 } \in \mathbb { R } ^ { n } } \end{array}$ and taking into account the Riccati equations (7) and (8) we get:

$$\mathcal {J} ^ {1} (s, T, 0, \tilde {u} _ {1} ^ {1}, \tilde {u} _ {2} ^ {1}) + \mathcal {J} ^ {2} (s, T, x _ {s}, \tilde {u} _ {1} ^ {2}, \tilde {u} _ {2} ^ {2}) = \frac {1}{2} \left\langle \hat {X} (s) x _ {s}, x _ {s} \right\rangle \tag {155}$$

Let:

$$
\begin{array}{l} \Gamma_ {T} ^ {1} = \mathbb {E} \int_ {s} ^ {T} \left\{\left\langle M (t) \hat {x} ^ {1} (t), \tilde {x} ^ {1} (t) \right\rangle + \left\langle L (t) \hat {x} ^ {1} (t), \tilde {u} ^ {1} (t) \right\rangle + \left\langle L (t) \tilde {x} ^ {1} (t), (u ^ {1} (t) - \tilde {u} ^ {1} (t)) \right\rangle \right. \\ \left. + \left\langle R (t) \tilde {u} ^ {1} (t), \left(u ^ {1} (t) - \tilde {u} ^ {1} (t)\right) \right\rangle \right\} d t \tag {156} \\ \end{array}

\begin{array}{l} \Gamma_ {T} ^ {2} = \int_ {s} ^ {T} \left\{\left\langle \hat {M} (t) \hat {x} ^ {2} (t), \tilde {x} ^ {2} (t) \right\rangle + \left\langle \hat {L} (t) \hat {x} ^ {2} (t), \tilde {u} ^ {2} (t) \right\rangle + \left\langle \hat {L} (t) \tilde {x} ^ {2} (t), (u ^ {2} (t) - \tilde {u} ^ {2} (t)) \right\rangle \right. \\ \left. + \left\langle \hat {R} (t) \tilde {u} ^ {2} (t), \left(u ^ {2} (t) - \tilde {u} ^ {2} (t)\right) \right\rangle \right\} d t \tag {157} \\ \end{array}
$$

Using the Itˆo formula to systems (25)-(26), (31)-(32) and to the function

$\tilde { v } ( t , \tilde { x } ^ { 1 } , \hat { x } ^ { 1 } , \tilde { x } ^ { 2 } , \hat { x } ^ { 2 } ) = \left. X ( t ) \tilde { x } ^ { 1 } , \hat { x } ^ { 1 } \right. + \left. \hat { X } ( t ) \tilde { x } ^ { 2 } , \hat { x } ^ { 2 } \right. , t \in [ s , \ T ] , \ \tilde { x } ^ { 1 } \in \mathbb { R } ^ { n } , \ \hat { x } ^ { 1 } \in \mathbb { R } ^ { n } , \ \tilde { x } ^ { 2 } \in \mathbb { R } ^ { n } , \ \hat { x } ^ { 2 } \in \mathbb { R } ^ { n } \ , \ \hat { x } ^ { 2 } \in \mathbb { R } ^ { n }$ and taking into account the Riccati equations (7) and (8) we get:

$$\Gamma_ {T} ^ {1} + \Gamma_ {T} ^ {2} = - \mathbb {E} \left\{\left\langle G _ {T} \hat {x} ^ {1} (T), \tilde {x} ^ {1} (T) \right\rangle \right\} - \left\{\left\langle \hat {G} _ {T} \hat {x} ^ {2} (T), \tilde {x} ^ {2} (T) \right\rangle \right\} \tag {158}$$

This ends the proof of this implication.

(ii) The inequality (148) is obtained from (146) written for $u _ { 1 } ( \cdot )$ replaced by $\tilde { u } _ { 1 } ( \cdot )$ and taking into account the condition (147).   
(iii) The inequality (150) is obtained from (146) written for $u _ { 2 } ( \cdot )$ replaced by $\tilde { u } _ { 2 } ( \cdot )$ and taking into account the condition (149). This ends the proof.
