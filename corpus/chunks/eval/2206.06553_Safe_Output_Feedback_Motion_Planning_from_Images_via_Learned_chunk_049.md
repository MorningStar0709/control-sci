1. Suppose $M _ { c }$ is constant; then, its maximum and minimum eigenvalues trivially extend for all of $\mathcal { X } .$ If $M _ { c }$ is polynomial and is generated via the SoS program in (D.12), its eigenvalues are by construction bounded by $1 / \beta$ and $1 / \bar { \beta } ;$ these provide globally-valid eigenvalue bounds over $\mathcal { X }$ . Moreover, the CCM-based controller contracts the nominal dynamics at rate $\lambda _ { c }$ if 1) for all time $t \in [ 0 , T ]$ , the geodesic connecting $x ^ { * } ( t )$ and $x ( t )$ (denoted as $\gamma _ { c } ^ { t } ( s ) , s \in [ 0 , 1 ] )$ is contained within the contraction domain $D _ { c }$ , and 2) there exists a feasible control input which achieves the $\lambda _ { c }$ contraction rate. Alg. 1 enforces both of these conditions: 1) line 9 of $\mathrm { A l g }$ . 1 ensures $\Omega _ { c } ( t ) \subseteq D _ { c } ,$ , since $\varOmega _ { c }$ is defined as a sublevel set with respect to the Riemannian metric induced by $M _ { c } .$ , i.e., $\gamma _ { c } ^ { t } ( s ) \subseteq \varOmega _ { c } ( t ) \subseteq D _ { c } ; 2 )$ by (2), the feedback controller $u ( \hat { x } , x ^ { * } , u ^ { * } )$ is always feasible (i.e., (13) is nonempty) as long as $\hat { x } \in D _ { c } ;$ this is enforced by line 10 of Alg. 1.   
2. In this work, we use constant $M _ { e }$ , so its maximum and minimum eigenvalues trivially extend for all of  . The OCM-based observer contracts at rate $\lambda _ { e }$ for its nominal dynamics if for all time $t \in [ 0 , T ]$ , the geodesic connecting $x ( t )$ and ${ \hat { x } } ( t )$ (denoted as $\gamma _ { e } ^ { t } ( s ) , s \in [ 0 , 1 ] )$ is contained within the contraction domain $D _ { e }$ . In Alg. 1, line 10 ensures $\Omega _ { e } ( t ) \subseteq D _ { e }$ , since $\varOmega _ { \epsilon }$ is defined as a sublevel set with respect to the Riemannian metric induced by $W _ { e } , \gamma _ { e } ^ { t } ( s ) \subseteq \mathcal { Q } _ { e } ( t ) \subseteq D _ { \epsilon }$ .   
3. From its definition in the paragraph before (15), $L _ { \varDelta k }$ is valid if $d _ { c } ( t ) \leq \bar { c }$ and $d _ { e } ( t ) \leq \bar { e } ,$ and $x ^ { * } \in D _ { x } , u ^ { * } \in \mathcal { U }$ . We ensure this via the check in $\mathrm { A l g . \ 1 }$ , line $8 . \ L _ { \hat { h } - 1 }$ , and the error bound constants $\bar { \epsilon } _ { 1 } , L _ { p } ,$ and $\mathcal { R }$ are valid if $x \in D _ { r } ;$ ; this is checked in line 9 of Alg. 1.

□

Remark 2 (Overall correctness probability). If all FTG-estimated constants are obtained according to Rem. 1 with probability ρ of correctness, using samples which are mutually independent, then the overall probability of the correctness of CORRT can be bounded by the product of each constant being estimated correctly; for example, if $L _ { \varDelta k } , L _ { \hat { h } ^ { - 1 } }$ , and $\bar { \epsilon } _ { 1 }$ are estimated, then CORRT returns a safely-trackable trajectory with probability at least $\rho ^ { 3 }$ .
