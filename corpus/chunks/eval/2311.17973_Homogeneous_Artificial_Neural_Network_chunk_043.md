Figure 2: Level lines $\| x \| _ { \bf d } = 1 ~ ( \mathrm { r e d } )$ , $\| x \| _ { \mathbf { d } ^ { * } } = 1$ (blue) and $\| \boldsymbol { x } \| _ { \mathbf { d } , \epsilon } = 1$ (green)

$$
\| x \| _ {\mathbf {d} ^ {*}} = \left(\Psi^ {\top} (x) Q \Psi (x)\right) ^ {1 / 1 0}, \quad Q = \left( \begin{array}{c c} 0. 1 2 2 5 & 0. 0 1 7 6 \\ 0. 0 1 7 6 & 0. 0 7 2 0 \end{array} \right),
$$

where $x = ( x _ { 1 } , x _ { 2 } ) ^ { \top } \in \mathbb { R } ^ { 2 } , \Psi ( x ) = \Big ( { } _ { | x _ { 2 } | ^ { 5 / 4 } s i g n ( x _ { 2 } ) } \Big )$ and the matrix $Q$ is selected $\| \cdot \| _ { \mathbf { d } }$ $\| \cdot \| _ { \mathbf { d } ^ { * } } ^ { \prime }$ unit sphere $S = \{ x \in \mathbb { R } ^ { 2 } : \| x \| = 1 \}$ the difference between norms is about $\epsilon \approx 0 . 0 0 7$ . Such an error is unacceptable for some control applications, where a high control precision is required. To improve the approximation, we use the homogeneous ANN (38) with the sigmoid activation function $\sigma$ and randomly selected parameters $\mathbf { \Phi } _ { A } \in \mathbb { R } ^ { N \times 1 }$ and $\boldsymbol { b } \in \mathbb { R } ^ { N }$ . By definition, the canonical homogeneous norm equals 1 on the unit sphere, i.e., $\| { \boldsymbol { x } } \| _ { \mathbf { d } } = 1$ for all $x \in S$ . The vector $C \in \mathbb { R } ^ { 1 \times N }$ is optimized using least square criterion

$$\sum_ {i = 1} ^ {M} \left(1 - \| x _ {i} \| _ {\mathbf {d} ^ {*}} C \sigma (A \mathbf {d} (- \ln \| x _ {i} \| _ {\mathbf {d} ^ {*}}) x + b)\right) ^ {2} \to \min _ {C \in \mathbb {R} ^ {1 \times N}},$$

where the points $x _ { i }$ are randomly distributed on the unit sphere S. For $N = 1 0$ such homogeneous ANN

$$\| x \| _ {\mathbf {d}, \epsilon} = \| x \| _ {\mathbf {d} ^ {*}} C \sigma (A \mathbf {d} (- \ln \| x \| _ {\mathbf {d} ^ {*}}) x + b)$$

improves the approximation precision of $\| { \boldsymbol { x } } \| _ { \mathbf { d } }$ on the unit sphere in two times $\epsilon \approx 0 . 0 0 3 4$ in comparison with the norm $\| \cdot \| _ { \mathbf { d } ^ { * } } ( \mathrm { s e e ~ F i g . ~ 2 } )$ .

For $N = 2 0$ we have $\epsilon \approx 5 \cdot 1 0 ^ { - 5 }$ . The plot for the latter two cases is not presented since the corresponding spheres are visually indistinguishable with the unit sphere. Therefore, the canonical homogeneous norm estimated by the homogeneous ANN can be efficiently utilized in control engineering for implementation of the implicit homogeneous controller [22], [21] in digital devices.
