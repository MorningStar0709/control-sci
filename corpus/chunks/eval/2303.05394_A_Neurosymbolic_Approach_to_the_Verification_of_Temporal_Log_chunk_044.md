$$
\left[ \begin{array}{c} \delta p _ {\ell} \\ \delta z _ {\ell} \end{array} \right] ^ {\top} \underbrace {\left[ \begin{array}{c c} - 2 \operatorname{diag} (\boldsymbol {\alpha} _ {\ell} \circ \boldsymbol {\beta} _ {\ell} \circ \lambda_ {\ell}) & \operatorname{diag} ((\boldsymbol {\alpha} _ {\ell} + \boldsymbol {\beta} _ {\ell}) \circ \lambda_ {\ell}) \\ \operatorname{diag} ((\boldsymbol {\alpha} _ {\ell} + \boldsymbol {\beta} _ {\ell}) \circ \lambda_ {\ell}) & - 2 \operatorname{diag} (\lambda_ {\ell}) \end{array} \right]} _ {Q _ {\ell}} \left[ \begin{array}{c} \delta p _ {\ell} \\ \delta z _ {\ell} \end{array} \right] \geq 0,
$$

S-procedure for $Q _ { \mathrm { i n f o } } { \mathrm { : } }$ Consider the weigh matrices on TNN in Figure 11. This weigh matrices are built from 4 sub-blocks,

– $W _ { \ell } ^ { l l }$ : This subblock connects the linear portion of layer \`−1 to linear portion of layer \`.   
– $W _ { \ell } ^ { l n } { \mathrm { : } }$ : This subblock connects the linear portion of layer \` − 1 to non-linear portion of layer \`.   
– $\bar { W } _ { \ell } ^ { n l }$ : This subblock connects the non-linear portion of layer \` − 1 to linear portion of layer \`.   
– $\bar { W } _ { \ell } ^ { n n }$ : This subblock connects the non-linear portion of layer \` − 1 to nonlinear portion of layer \`.

Therefore the pre-activation $p \ell$ is computed as a linear combination of previous activations $z _ { i } , i = 0 , \cdots , \ell - 1$ through the following iterative formula.

$$
\left[ \begin{array}{c} t _ {1} \\ p _ {1} \end{array} \right] = \underbrace {\left[ \begin{array}{c} W _ {1} ^ {l n} \\ \hline \\ W _ {1} ^ {n n} \end{array} \right]} _ {W _ {1}} \underbrace {z _ {0}} _ {\mathbf {s} _ {0}} + b _ {1}, \left[ \begin{array}{c} t _ {\ell} \\ p _ {\ell} \end{array} \right] = \underbrace {\left[ \begin{array}{c c} W _ {\ell} ^ {l l} & W _ {\ell} ^ {l n} \\ \hline \\ W _ {\ell} ^ {n l} & W _ {\ell} ^ {n n} \end{array} \right]} _ {W _ {\ell}, \ell = 2, \dots , N} \left[ \begin{array}{c} t _ {\ell - 1} \\ z _ {\ell - 1} \end{array} \right] + b _ {\ell},
$$

then the difference of pre-activations $\delta p _ { \ell }$ are,
