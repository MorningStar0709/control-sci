$$\hat {u} (t + 1) = d (t + 1) + \frac {B _ {M} ^ {*} (q ^ {- 1})}{A _ {M} (q ^ {- 1})} \hat {y} (t) \tag {15.30}$$

Proof Under Assumption (H2) (perfect matching condition) the output of the primary path can be expressed as:

$$z (t) = G \left(q ^ {- 1}\right) y (t) \tag {15.31}$$

where y(t) is a dummy variable given by:

$$y (t + 1) = - S ^ {*} (q ^ {- 1}) y (t) + R (q ^ {- 1}) u (t + 1)
= \theta^ {T} \varphi (t) = [ \theta_ {S} ^ {T}, \theta_ {R} ^ {T} ] \left[ \begin{array}{l} \varphi_ {y} (t) \\ \varphi_ {u} (t) \end{array} \right] \tag {15.32}
$$

where

$$\varphi^ {T} (t) = [ - y (t), \dots , - y (t - n _ {S} + 1), u (t + 1), \dots , u (t - n _ {R} + 1) ]= \left[ \varphi_ {y} ^ {T} (t), \varphi_ {u} ^ {T} (t) \right] \tag {15.33}$$

and u(t) is given by

$$u (t + 1) = d (t + 1) + \frac {B _ {M} ^ {*} (q ^ {- 1})}{A _ {M} (q ^ {- 1})} y (t) \tag {15.34}$$

For a fixed value of the parameter vector $\hat { \theta }$ characterizing the estimated filter $\hat { N } ( q ^ { - 1 } )$ of same dimension as the optimal filter $N ( q ^ { - 1 } )$ , the output of the secondary path can be expressed by (in this case $\hat { z } ( t ) = \hat { z } ^ { 0 } ( t )$ and $\hat { y } ( t ) = \hat { y } ^ { 0 } ( t ) )$ :

$$- \hat {z} (t) = G (q ^ {- 1}) [ - \hat {y} (t) ] \tag {15.35}$$

where

$$- \hat {y} (t + 1) = - \hat {\theta} ^ {T} \phi (t) \tag {15.36}$$

The key observation is that the dummy variable y(t + 1) can be expressed as:

$$
\begin{array}{l} y (t + 1) = \theta^ {T} \phi (t) + \theta^ {T} [ \varphi (t) - \phi (t) ] \\ = \theta^ {T} \phi (t) + \theta_ {S} ^ {T} [ \varphi_ {y} - \phi_ {\hat {y}} ] + \theta_ {R} ^ {T} [ \varphi_ {u} - \phi_ {\hat {u}} ] \tag {15.37} \\ \end{array}
$$

Define the dummy error (for a fixed vector $\hat { \theta } )$

$$\varepsilon (t + 1) = y (t + 1) - \hat {y} (t + 1) \tag {15.38}$$

and the residual error

$$\nu (t + 1) = z (t) - \hat {z} (t) = G (q ^ {- 1}) \varepsilon (t + 1) \tag {15.39}$$

It results from (15.37) that:

$$y (t + 1) = \theta^ {T} \phi (t) - S ^ {*} (q ^ {- 1}) \varepsilon (t) + R (q ^ {- 1}) [ u (t + 1) - \hat {u} (t + 1) ] \tag {15.40}$$

But taking into account the expressions of u(t) and u(t) ˆ given by (15.34) and (15.19) (or (15.30)) respectively one gets:

$$y (t + 1) = \theta^ {T} \phi (t) - (S ^ {*} (q ^ {- 1}) - \frac {R (q ^ {- 1}) B _ {M} ^ {*} (q ^ {- 1})}{A _ {M} (q ^ {- 1})}) \varepsilon (t) \tag {15.41}$$

and therefore:

$$\varepsilon (t + 1) = [ \theta - \hat {\theta} ] ^ {T} \phi (t) - \left(S ^ {*} (q ^ {- 1}) - \frac {R (q ^ {- 1}) B _ {M} ^ {*} (q ^ {- 1})}{A _ {M} (q ^ {- 1})}\right) \varepsilon (t) \tag {15.42}$$

This gives:
