# Appendix B. Solution of Problem 2 via Dynamic Programming

In this section, we first demonstrate that the principle of optimality holds for Problem 2, and then use a standard stochastic Hamilton-Jacobi-Bellman (HJB) equation to derive the optimal control.

Specifically, for any fixed initial time $t \in [ 0 , T ]$ and initial state $\check { \xi } \in \left\{ \check { \xi } \in L _ { \mathcal { F } _ { t } } ^ { 2 } \left( \Omega , \mathbb { R } ^ { n } \right) : \mathbb { E } \big [ \check { \xi } | \mathcal { F } _ { t } ^ { 0 } \big ] = 0 \right\}$ , we aim to find the control process $\alpha ^ { * } \in \breve { \mathcal { U } } _ { t } : = \left\{ \alpha \in L _ { \mathcal { F } } ^ { 2 } \left( [ t , T ] , \mathbb { R } ^ { d } \right) : \mathbb { E } \left[ \alpha _ { \tau } \big | \mathcal { F } _ { \tau } ^ { 0 } \right] = 0 , d \tau \otimes d \mathbb { P } - a . s . \right\}$ that minimizes the cost functional given by

$$\breve {J} _ {t, \check {\xi}} (\alpha) := \mathbb {E} \left[ \int_ {t} ^ {T} \left[ z _ {\tau} ^ {\intercal} Q _ {\tau} z _ {\tau} + 2 z _ {\tau} ^ {\intercal} S _ {\tau} \alpha_ {\tau} + \alpha_ {\tau} ^ {\intercal} R _ {\tau} \alpha_ {\tau} \right] d \tau + z _ {T} ^ {\intercal} Q _ {T} z _ {T} \mid \mathcal {F} _ {t} \right] \tag {B.1}$$

where the sate process $z \in \mathcal { C } _ { \mathcal { F } } \left( [ t , T ] , \mathbb { R } ^ { n } \right)$ satisfies

$$d z _ {\tau} = \left[ A _ {\tau} z _ {\tau} + B _ {\tau} \alpha_ {\tau} \right] d \tau + D _ {\tau} d W _ {\tau}, \quad z _ {t} = \check {\xi}. \tag {B.2}$$

The associated value function for this problem is defined by

$$V (t, \breve {\xi}) := \inf _ {\alpha \in \breve {\mathcal {U}} _ {t}} \breve {J} _ {t, \breve {\xi}} (\alpha), \quad \mathbb {P} - a. s., \quad \forall (t, \breve {\xi}) \in [ 0, T) \times \left\{\breve {\xi} \in L _ {\mathcal {F} _ {t}} ^ {2} (\Omega , \mathbb {R} ^ {n}): \mathbb {E} \left[ \breve {\xi} | \mathcal {F} _ {t} ^ {0} \right] = 0 \right\}. \tag {B.3}$$

Appendix B.1. Principle of Optimality

We begin with the following result.

Theorem 4. For every $t \in [ 0 , T ) , \check { \xi } \in \left\{ \check { \xi } \in L _ { \mathcal { F } _ { t } } ^ { 2 } \left( \Omega , \mathbb { R } ^ { n } \right) : \mathbb { E } \big [ \check { \xi } | \mathcal { F } _ { t } ^ { 0 } \big ] = 0 \right\}$ and $h \in ( t , T )$ , the value function $V ( . , . )$ associated with the problem defined by (B.1)-(B.2) satisfies
