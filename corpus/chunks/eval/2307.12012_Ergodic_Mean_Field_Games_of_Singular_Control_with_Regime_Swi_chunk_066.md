# APPENDIX C. PROOF OF (1) IN THEOREM 4.1: THE FOKKER-PLANCK COUPLED SYSTEM

We follow closely the proof of Theorem 1 in [25]. Let $\mu ^ { \theta }$ be the cumulative distribution function of the stationary distribution $\nu ^ { \theta }$ (cf. Proposition 4.2). To simplify the notation, we drop the dependence on $\theta ;$ that is, we set $\mu : = \mu ^ { \theta }$ as well as $\alpha _ { i } : = \alpha _ { i } ( \theta ) , \beta _ { i } : = \beta _ { i } ( \theta )$ for any $i \in \mathbb { Y }$ . We divide the rest of the proof in three steps.

Step 1. We first derive a variational equation for the function $\mu .$ To this end, introduce the generator $\mathcal { L } _ { ( X , Y ) } ^ { \alpha , \beta }$ of the process $( X ^ { \xi } , Y )$ , where $X ^ { \xi }$ is reflected at the boundaries $( \alpha , \beta ) : = \{ ( \alpha _ { i } , \beta _ { i } ) \} _ { i \in \mathbb { Y } }$ . Namely, for any $\phi : = ( \phi ( \cdot , i ) ) _ { i \in \mathbb { Y } } \in C ^ { 2 } ( \mathbb { Z } ; \mathbb { R } ^ { d } )$ define

$$\mathcal {L} _ {(X, Y)} ^ {\alpha , \beta} \phi (x, i) := \frac {1}{2} \sigma^ {2} (x, i) \phi_ {x x} (x, i) + b (x, i) \phi_ {x} (x, i) + \sum_ {j \in \mathbb {Y}} q _ {i j} \widehat {\phi} (x, j), \quad (x, i) \in \mathcal {O},$$

where $\widehat { \phi } ( x , i ) : = \phi ( \alpha _ { i } ^ { - } \vee x \wedge \beta _ { i } , i )$ . From Chapter IV in [26], we have that $\mu$ solves the equation

$$\sum_ {i \in \mathbb {Y}} \int_ {\alpha_ {i} ^ {-}} ^ {\beta_ {i}} \mathcal {L} _ {(X, Y)} ^ {\alpha , \beta} \phi (x, i) d \mu (x, i) = 0, \quad \text { for any } \phi := (\phi (\cdot , i)) _ {i \in \mathbb {Y}} \in C ^ {2} (\mathcal {I}; \mathbb {R} ^ {d}). \tag {C.1}$$

Hence, by Theorem 5.3.4 in [6] we obtain that $\mu$ is absolutely continuous with respect to the Lebesgue measure with density function $\mu _ { x }$ .

Next, split the interval $\left[ \operatorname* { m i n } _ { i \in \mathbb { Y } } \alpha _ { i } , \operatorname* { m a x } _ { i \in \mathbb { Y } } \beta _ { i } \right]$ in the disjoint sub-intervals

$$I _ {k} = (l _ {k - 1}, l _ {k}), \quad k = 1,.., K,$$

with $\begin{array} { r } { { l _ { 0 } } = \operatorname* { m i n } _ { i \in \mathbb { Y } } \{ \alpha _ { i } \} , { l _ { K } } = \operatorname* { m a x } _ { i \in \mathbb { Y } } \{ \beta _ { i } \} } \end{array}$ and

$$
l_{k + 1} = \min_{\substack{i\in \mathbb{Y}:\\ \alpha_{i} > l_{k}}}\{\alpha_{i}\} \wedge \min_{\substack{i\in \mathbb{Y}:\\ \beta_{i} > l_{k}}}\{\beta_{i}\} .
$$

Introduce the set of test functions
