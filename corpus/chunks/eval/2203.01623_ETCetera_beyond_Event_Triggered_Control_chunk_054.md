# Compute its SAIST value
traffic2 = abstr.TrafficModelLinearPETC(trigger, depth 
    → =100, etc_only=True, solver='z3',
    → stop_if_mace_equivalent=True, smart_mace=True,
    → strategy=strat, strategy_transition=strat_tran)
regions2, transitions2 = traffic2.create_abstraction()
print(f'Optimized SAIST is {traffic2.limavg}') 
```

This code reproduces part of the results in [12], giving the following output (the numbers are normalized by ℎ):

```txt
1 SAIST is 2.3333333333333335
2 Smallest average cycles: {(8, 1, 1, 1, 1, 2)}
3 Optimized SAIST is 5.0 
```

The algorithm terminated in 5 minutes.
