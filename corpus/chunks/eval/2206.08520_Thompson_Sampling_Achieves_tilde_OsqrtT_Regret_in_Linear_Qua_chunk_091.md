From Lemma 10 of Abbasi-Yadkori and Szepesv´ari (2011), we have that log( det(VTr )det(µI) ) $\begin{array} { r } { \log ( \frac { \operatorname* { d e t } ( V _ { T _ { r } } ) } { \operatorname* { d e t } ( \mu I ) } ) \leq ( n + } \end{array}$ d) log(1+ Tr(1+κ2)(n+d)2(n+d) ) $\begin{array} { r } { \log ( 1 + \frac { T _ { r } ( 1 + \kappa ^ { 2 } ) ( n + d ) ^ { 2 ( n + d ) } } { \mu ( n + d ) } , } \end{array}$ and log( det(VT )det(VT ) ) $\begin{array} { r } { \log ( \frac { \operatorname* { d e t } ( V _ { T } ) } { \operatorname* { d e t } ( V _ { T _ { r } } ) } ) \leq ( n + d ) \log ( 1 + \frac { T _ { r } ( 1 + \kappa ^ { 2 } ) ( n + d ) ^ { 2 ( n + d ) } + ( T - T _ { r } ) X _ { s } ^ { 2 } } { \mu ( n + d ) } ) } \end{array}$ .

After inserting these quantities into (126), we have the dimension dependency of $( n + d ) ^ { 2 ( n + d ) } \times$ ${ \sqrt { n ( n + d ) } } \times ( n + d ) ^ { ( n + d ) \tau _ { 0 } } \times ( n + d )$ on the first term where $\sqrt { n ( n + d ) }$ is due to $\beta _ { T } ( \delta ) + v _ { T } ( \delta )$ .

For the second term, for large enough $T ,$ , we have the dimension dependency of $n \times { \sqrt { n ( n + d ) } } \times$ $n ^ { ( \tau _ { 0 } / 2 ) } \times \sqrt { n + d } .$ , where n comes from $X _ { s } ^ { 2 }$ . Thus, we achieve the following bound for $\big | R _ { T } ^ { \mathrm { R L S } } \big |$ :

$$\left| R _ {T} ^ {\mathrm{RLS}} \right| = \tilde {O} \left((n + d) ^ {(\tau_ {0} + 2) (n + d) + 1. 5} \sqrt {n} \sqrt {T _ {r}} + (n + d) n ^ {1. 5 + \tau_ {0} / 2} \sqrt {T - T _ {r}}\right).$$

With the choice of $\mu = ( 1 + \kappa ^ { 2 } ) X _ { s } ^ { 2 }$ , the dependency of $\boldsymbol { n } ^ { ( \tau _ { 0 } / 2 ) }$ on the second term can be converted to a scalar multiplier of $\sqrt { 2 } ^ { \tau _ { 0 } }$ and reduces the dependency of $X _ { s } ^ { 2 }$ to $X _ { s }$ , which gives the advertised bound.
