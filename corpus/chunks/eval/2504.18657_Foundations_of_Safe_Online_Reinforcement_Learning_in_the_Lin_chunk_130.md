Now we want to bound the difference in cost with high probability instead of in expectation. Let X be the set of times $t \in [ 0 : \tau ]$ such that $C ( y _ { t } ) \neq - K ^ { \prime } y _ { t } ~ \mathrm { ( i . e . ~ } ~ C ( y _ { t } ) \in \{ v _ { t } ^ { \mathrm { s a f e L } } , v _ { t } ^ { \mathrm { s a f e U } } \} )$ Note that the event $\{ t \in X \}$ is the same as the event $\begin{array} { r } { \left\{ y _ { t } \geq \frac { D _ { U } } { a _ { m } - b _ { m } K ^ { \prime } } \mathrm { ~ o r ~ } y _ { t } \leq \frac { D _ { L } } { a _ { m } - b _ { m } K ^ { \prime } } \right\} } \end{array}$ DLam−bmK′ }.

By Lemma 34, $\mathbb { P } ( t \in X \mid H _ { t } ) \leq c v$ for some constant $c > 0$ m for all t. Therefore, $M _ { k } =$ $\scriptstyle \sum _ { t = 0 } ^ { \tau } \left( 1 _ { t \in X } - c v \right)$ is a supermartingale. By Azuma–Hoeffding’s inequality, with probability $1 - o _ { T } ( 1 / T ^ { 1 0 } )$ ,

$$| X | \leq O _ {T} (v \tau) + \log (T) \sqrt {\tau}.$$

Define A as the event that $| X | \leq O _ { T } ( v \tau ) + \log ( T ) \sqrt { \tau }$ . Define $\kappa = \lceil \log _ { a - b K ^ { \prime } } ( v ) \rceil$ . Note that

$$\kappa = \lceil \log_ {a - b K ^ {\prime}} (v) \rceil\leq \left\lceil \frac {\log (v)}{\log (a - b K ^ {\prime})} \right\rceil= O (\log (v)) \quad \text { Lemma } 1 0 8 \tag {122}$$

Define

$$G = \left\{t \in [ 0: \tau ]: \exists i \in [ t - \kappa : t ] \text {such that} C (y _ {i}) \neq - K ^ {\prime} y _ {i} \right\}.$$

Under event A,

$$| G | \leq | X | \cdot (\kappa + 1) \leq (O _ {T} (v \tau) + \log (T) \sqrt {\tau}) (\kappa + 1). \tag {123}$$

By Lemma 35, if $t \not \in G$ , then
