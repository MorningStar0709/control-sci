$$\mathbb {P} \left(C (y _ {t}) = v _ {t} ^ {\text { safeU }} \mid H _ {t}\right) = O _ {T} (\nu) \cdot 1 _ {K ^ {\prime} - K _ {D _ {U}} ^ {\theta} \leq \frac {(| K ^ {\prime} | + 1) \beta}{b}}. \tag {111}$$

Furthermore,

$$\mathbb {P} \left(C (y _ {t}) = v _ {t} ^ {\text { safeL }} \mid H _ {t}\right) = O _ {T} (\upsilon) \cdot 1 _ {K ^ {\prime} - K _ {D _ {U}} ^ {\theta} \leq \frac {(| K ^ {\prime} | + 1) \beta}{b}}. \tag {112}$$

The proof of Lemma 34 can be found in Appendix G.12. Because the equations in Lemma 34 hold for any $H _ { t }$ , this lemma implies that

$$\mathbb {P} \left(C (y _ {t}) = v _ {t} ^ {\mathrm{safeU}}\right) = O _ {T} (\upsilon) \cdot 1 _ {K ^ {\prime} - K _ {D _ {U}} ^ {\theta} \leq \frac {(| K ^ {\prime} | + 1) \beta}{b}} \tag {113}$$

and

$$\mathbb {P} \left(C (y _ {t}) = v _ {t} ^ {\text { safeL }}\right) = O _ {T} (\upsilon) \cdot 1 _ {K ^ {\prime} - K _ {D _ {U}} ^ {\theta} \leq \frac {(| K ^ {\prime} | + 1) \beta}{b}}. \tag {114}$$

By Lemma 34, if $\begin{array} { r } { K ^ { \prime } - K _ { D _ { U } } ^ { \theta } > \frac { ( | K ^ { \prime } | + 1 ) \beta } { b } } \end{array}$ (|K′|+1)β , then for all t, $t ,$

$$\mathbb {P} \left(C (y _ {t}) = v _ {t} ^ {\mathrm{safeU}} \mathrm{or} C (y _ {t}) = v _ {t} ^ {\mathrm{safeL}}\right) = 0.$$

Therefore in this case, the controllers C and $C _ { K ^ { \prime } } ^ { \mathrm { u n c } }$ are equivalent, which implies all of the Kdesired results. For the rest of the proof, we will address the case when $\begin{array} { r } { K ^ { \prime } - K _ { D _ { U } } ^ { \theta } \le \frac { ( | K ^ { \prime } | + 1 ) \beta } { b } } \end{array}$ ≤ ( b This combined with the definition of ϵ gives that

$$\left| K ^ {\prime} - K _ {D _ {U}} ^ {\theta} \right| \leq \min \left(\frac {\left(\left| K ^ {\prime} \right| + 1\right) \beta}{b}, \epsilon\right) = O _ {T} (v). \tag {115}$$

Lemma 35. Using the notation and assumptions in the proof of Lemma 29, if Equation (115) holds then for all $t \geq 0$ ,

$$
d _ {t + 1} = \left\{ \begin{array}{l l} (a - b K ^ {\prime}) d _ {t} & \text { if } \frac {D _ {L}}{a _ {m} - b _ {m} K ^ {\prime}} \leq y _ {t} \leq \frac {D _ {U}}{a _ {m} - b _ {m} K ^ {\prime}} \\ (a - b K ^ {\prime}) d _ {t} + O _ {T} (\upsilon) & \text { otherwise }, \end{array} \right. \tag {116}
$$

and

$$
\left| C _ {K ^ {\prime}} ^ {\text { unc }} \left(x _ {t}\right) - C \left(y _ {t}\right) \right| = \left\{ \begin{array}{l l} \left| K ^ {\prime} \right| d _ {t} & i f \frac {D _ {L}}{a _ {m} - b _ {m} K ^ {\prime}} \leq y _ {t} \leq \frac {D _ {U}}{a _ {m} - b _ {m} K ^ {\prime}} \\ O _ {T} (v) & o t h e r w i s e. \end{array} \right. \tag {117}
$$
