Adding the term $2 \boldsymbol { u } ^ { T } ( t ) S ^ { T } \boldsymbol { x } ( t ) + \boldsymbol { u } ^ { T } ( t ) R \boldsymbol { u } ( t )$ in both sides of (C.12) and taking into account (C.5) and (C.6), one gets:

$$
\begin{array}{l} x ^ {T} (t) Q x (t) + 2 u ^ {T} (t) S ^ {T} x (t) + u ^ {T} (t) R u (t) \\ = - x ^ {T} (t + 1) P x (t + 1) + x ^ {T} (t) P x (t) \\ + 2 u ^ {T} (t) C x (t) + u ^ {T} (t) \left[ D + D ^ {T} \right] u (t) \\ = - x ^ {T} (t + 1) P x (t + 1) \\ + x ^ {T} (t) P x (t) + 2 y ^ {T} (t) u (t) \tag {C.13} \\ \end{array}
$$

Summing up from 0 to $t _ { 1 }$ , one gets (C.11).

![](image/6d51eb02042b3fa6070e85aa32707660c08ce31a1cfe844296a391633c254f7c.jpg)

Definition C.10 A m × m discrete transfer matrix H (z) of real rational functions is strictly positive real if and only if:

1. All the elements H (z) are analytic in $| z | \geq 1$ .   
2. The matrix

$$H (e ^ {j \omega}) + H ^ {T} (e ^ {- j \omega})$$

is a positive definite Hermitian for all real $\omega .$

Definition C.11 The system (C.1)–(C.2) is termed state strictly passive if:

$$\eta (0, t _ {1}) \geq \beta_ {1} \| x (t _ {1} + 1) \| ^ {2} - \beta_ {0} \| x (0) \| ^ {2} + \mu \| x \| _ {2 T} ^ {2}; \quad \beta_ {1}, \beta_ {0}, \mu > 0; \forall t _ {1} \geq 0$$

and for all u(t) and x(t) that satisfy (C.1).

Lemma C.4 (Generalized Positive Real Lemma) Consider the system (C.1)–(C.2) characterized by the transfer matrix H (z) given in (C.3). Assume that there is a symmetric positive matrix P , a semi-positive definite matrix Q, symmetric matrices R, Δ, K, Mo and a matrix S of appropriate dimensions such that:

$$A ^ {T} P A - P = - Q - C ^ {T} \Delta C - M _ {0} \tag {C.14}C ^ {T} - B ^ {T} P A = S ^ {T} + D ^ {T} \Delta C \tag {C.15}D + D ^ {T} - B ^ {T} P B = R + D ^ {T} \Delta D + K \tag {C.16}
M = \left[ \begin{array}{c c} Q & S \\ S ^ {T} & R \end{array} \right] \geq 0 \tag {C.17}
$$

Then:

1. Every solution x(t, x(0), u(t)) of (C.1)–(C.2) satisfies the following equality:

$$
\begin{array}{l} \sum_ {t = 0} ^ {t _ {1}} y ^ {T} (t) u (t) = \frac {1}{2} x ^ {T} (t _ {1} + 1) P x (t _ {1} + 1) - \frac {1}{2} x ^ {T} (0) P x (0) \\ + \frac {1}{2} \sum_ {t = 0} ^ {t _ {1}} [ x ^ {T} (t) u ^ {T} (t) ] M \left[ \begin{array}{c} x (t) \\ u (t) \end{array} \right] + \frac {1}{2} \sum_ {t = 0} ^ {t _ {1}} x ^ {T} (t) M _ {0} x (t) \\ + \frac {1}{2} \sum_ {t = 0} ^ {t _ {1}} u ^ {T} (t) K u (t) + \frac {1}{2} \sum_ {t = 0} ^ {t _ {1}} y ^ {T} (t) \Delta y (t) \tag {C.18} \\ \end{array}
$$

2. For $M _ { 0 } = 0 , \varDelta = 0 , K = 0$

• The system (C.1)–(C.2) is passive   
• $H ( z ) ~ i s p o s i t i \nu e ~ r e a l$

3. For $M _ { 0 } \geq 0 , \varDelta \geq 0 , K > 0$
