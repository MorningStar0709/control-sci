# H.2 Proof of Proposition 12

proof. We will apply the standard McDiarmid’s inequality to the function

$$f (\{w _ {t} \} _ {t = T _ {0}} ^ {T - 1}) = (T - T _ {0}) J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, W ^ {\prime}).$$

To do this, we need a bounded difference inequality. We will show the following.

Lemma 38. For $i \in [ T _ { 0 } : T - 1 ]$ , let $\{ w _ { t } ^ { \prime } \} _ { t = T _ { 0 } } ^ { T - 1 }$ be such that $w _ { t } ^ { \prime } = w _ { t }$ for $t \neq i$ and $w _ { i } ^ { \prime } \sim \mathcal { D }$ is independent of $\{ w _ { t } \} _ { t = T _ { 0 } } ^ { T - 1 } . \ I f \ | F _ { \mathrm { o p t } } ( \hat { \theta } _ { \mathrm { w u } } ) - F _ { \mathrm { o p t } } ( \theta ^ { * } ) | \leq \tilde { O } _ { T } ( T ^ { - 1 / 4 } )$ , then for sufficiently large $T _ { \perp }$ ,

$$| (T - T _ {0}) \cdot J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, \{w _ {t} \} _ {t = T _ {0}} ^ {T - 1}) - (T - T _ {0}) \cdot J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, \{w _ {t} ^ {\prime} \} _ {t = T _ {0}} ^ {T - 1}) | \leq c.$$

for some $c = \tilde { O } _ { T } ( 1 )$

The proof of Lemma 38 can be found in Appendix H.6.

Under event $E _ { 2 } ^ { 0 }$ , by Lemma 23 we have that $| F _ { \mathrm { o p t } } ( \hat { \theta } _ { \mathrm { w u } } ) - F _ { \mathrm { o p t } } ( \theta ^ { * } ) | \le \tilde { O } _ { T } ( T ^ { - 1 / 4 } )$ . Furthermore, conditional on $E _ { 2 } ^ { 0 }$ and $\hat { \theta } _ { \mathrm { w u } }$ the random variables $\{ w _ { t } \} _ { t = T _ { 0 } } ^ { T - 1 }$ are still i.i.d. because the noise random variables are independent of the history. Therefore, conditional on event $E _ { 2 } ^ { 0 }$ we can use Lemma 38 with the standard McDiarmid’s inequality McDiarmid et al. [1989] and get

$$
\begin{array}{l} \mathbb {P} \left(\left| (T - T _ {0}) \cdot J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, \{w _ {t} \} _ {t = T _ {0}} ^ {T - 1}) - \mathbb {E} [ (T - T _ {0}) \cdot J (\theta^ {*}, C _ {F _ {\mathrm{opt}} (\hat {\theta} _ {\mathrm{wu}})} ^ {\mathrm{unc}}, T - T _ {0}, 0, \{w _ {t} \} _ {t = T _ {0}} ^ {T - 1}) ] \right| \geq \epsilon   \Big |   \hat {\theta} _ {\mathrm{wu}}\right) \\ \leq 2 \exp \left(- 2 \frac {\epsilon^ {2}}{c ^ {2} (T - T _ {0})}\right). \\ \end{array}
$$

Because
