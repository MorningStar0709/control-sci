$$\left\| \Delta (\tilde {\nu} _ {T}) \right\| _ {0} + \left\| \Delta (\tilde {\mu} _ {T}) \right\| _ {0} < \left\| \Delta (\overline {{\nu}} _ {T}) \right\| _ {0} + \left\| \Delta (\overline {{\mu}} _ {T}) \right\| _ {0}. \tag {7.1}$$

Now,

$$
\| \Delta (\tilde {\nu} _ {T}) \| _ {0} = \left\| \left( \begin{array}{c} \tilde {\nu} (1) - \tilde {\nu} (0) \\ \tilde {\nu} (2) - \tilde {\nu} (1) \\ \vdots \\ \tilde {\nu} (T - 1) - \tilde {\nu} (T - 2) \end{array} \right) \right\| _ {0} = \left\| \left( \begin{array}{c} \overline {{\alpha}} (\tilde {v} _ {0}, \tilde {v} _ {1}) \\ \overline {{\alpha}} (\tilde {v} _ {1}, \tilde {v} _ {2}) \\ \vdots \\ \overline {{\alpha}} (\tilde {v} _ {T - 2}, \tilde {v} _ {T - 1}) \end{array} \right) \right\| _ {0}
$$

and

$$
\| \tilde {\mu} _ {T} \| _ {0} = \left\| \left( \begin{array}{c} \tilde {\mu} (0) \\ \tilde {\mu} (1) \\ \vdots \\ \tilde {\mu} (T - 1) \end{array} \right) \right\| _ {0} = \left\| \left( \begin{array}{c} \overline {{\beta}} (\tilde {v} _ {0}, \tilde {v} _ {1}) \\ \overline {{\beta}} (\tilde {v} _ {1}, \tilde {v} _ {2}) \\ \vdots \\ \overline {{\beta}} (\tilde {v} _ {T - 2}, \tilde {v} _ {T - 1}) \end{array} \right) \right\| _ {0},
$$

where $\tilde { W } _ { T } = \tilde { v } _ { 0 } , \big ( \tilde { v } _ { 0 } , \tilde { v } _ { 1 } \big ) , \tilde { v } _ { 1 } , \dots , \tilde { v } _ { r - 1 } , \big ( \tilde { v } _ { r - 1 } , \tilde { v } _ { r } \big )$ , v˜r is the ξ-hands-off walk corresponding to $\left( \tilde { \nu } _ { T } , \tilde { \mu } _ { T } \right)$ . Here, the correspondence is in the sense of construction of $\left( \tilde { \nu } _ { T } , \tilde { \mu } _ { T } \right)$ from W˜ T as in Algorithm 1. In view of (7.1), we must have that
