From the definition of $\{ \zeta _ { t } \}$ , the sequence $\{ \zeta _ { t } , \mathcal { F } _ { t } \}$ is a martingale difference sequence, i.e., $\mathbb { E } [ \zeta _ { t + 1 } ~ | ~ \mathcal { F } _ { t } ]$ = $0 , \forall t \geq 0$ , and it satisfies $\operatorname* { s u p } _ { t } \mathbb { E } [ \| \zeta _ { t + 1 } \| ^ { 2 } | \mathcal { F } _ { t } ] < \infty$ .

Moreover, $\phi _ { t + 1 } - \zeta _ { t + 1 }$ is $\mathcal { F } _ { t } { \mathrm { - m e a s u r a b } }$ le for all $t \geq 0 .$ , and using (B.13), (C.3)-(C.4), and Assumption 2,

$$\sum_ {\tau = 0} ^ {t - 1} \| \phi_ {\tau + 1} - \zeta_ {\tau + 1} \| ^ {2} = O (t), a. s., t \to \infty . \tag {C.10}$$

Then, by the martingale convergence theorem (Theorem 2.8 in Chen and Guo (1991)), we have $\scriptstyle \sum _ { \tau = 0 } ^ { t } ( \phi _ { \tau } \ -$ $\zeta _ { \tau } ) \zeta _ { \tau } ^ { \top } = O ( t ^ { \frac { 1 } { 2 } } ( \log t ) ^ { \frac { 2 } { 3 } } )$ , a.s. Since $\begin{array} { r } { b < \frac { ( \gamma - 2 ) ^ { 2 } } { 4 \gamma ( \gamma + 2 ) } < \frac { 1 } { 4 } } \end{array}$ ,

$$\sum_ {\tau = 0} ^ {t} (\phi_ {\tau} - \zeta_ {\tau}) \zeta_ {\tau} ^ {\top} = o (t ^ {1 - 2 b}), \text { a.s. } \tag {C.11}$$

Note that $\begin{array} { r l r } { \sum _ { \tau = 0 } ^ { t } \phi _ { \tau } \phi _ { \tau } ^ { \top } } & { { } = } & { \sum _ { \tau = 0 } ^ { t } ( \phi _ { \tau } - \zeta _ { \tau } + \zeta _ { \tau } ) ( \phi _ { \tau } - } \end{array}$ $\begin{array} { r } { \zeta _ { \tau } + \zeta _ { \tau } ) ^ { \top } \geq \sum _ { \tau = 0 } ^ { t } ( \phi _ { \tau } - \zeta _ { \tau } ) \zeta _ { \tau } ^ { \top } + \sum _ { \tau = 0 } ^ { t } \zeta _ { \tau } ( \phi _ { \tau } - \zeta _ { \tau } ) ^ { \top } + } \end{array}$ $\scriptstyle \sum _ { \tau = 0 } ^ { t } \zeta _ { \tau } \zeta _ { \tau } ^ { \top }$ τ=0   τ  τ=0     . Using (C.11) and Lemma 3, the first two terms are $o ( t ^ { 1 - 2 b } )$ , a.s., while the last term scales as $t ^ { 1 - 2 b } .$ $\textstyle { \bigl ( } { \frac { 8 b } { \gamma - 2 } } , { \frac { 2 ( \gamma - 2 ) } { \gamma ( \gamma + 2 ) } } { \bigr ) }$ , a.s. Hence, (C.9) holds. Moreover, for each , we have $\eta \in$

$$
\begin{array}{l} \sum_ {\tau = 0} ^ {t} \frac {\phi_ {\tau} \phi_ {\tau} ^ {\top}}{1 + \| \phi_ {\tau} \| ^ {2}} \geq \sum_ {\tau = 0} ^ {t} \frac {\phi_ {\tau} \phi_ {\tau} ^ {\top}}{1 + \| \phi_ {\tau} \| ^ {2}} I (1 + \| \phi_ {\tau} \| ^ {2} \leq \tau^ {\eta}) \\ = \sum_ {\tau = 0} ^ {t} \frac {\phi_ {\tau} \phi_ {\tau} ^ {\top}}{\tau^ {\eta}} - \sum_ {\tau = 0} ^ {t} \frac {\phi_ {\tau} \phi_ {\tau} ^ {\top}}{\tau^ {\eta}} I (1 + \| \phi_ {\tau} \| ^ {2} > \tau^ {\eta}). \tag {C.12} \\ \end{array}
$$

Then, note that
