$$c _ {\mu} (x, a) - \lambda_ {1} (x, a) - \lambda_ {2} (x) + \beta \sum_ {y \in \mathsf {X}} \lambda_ {2} (y) p _ {\mu} (y | x, a) = 0, \forall (x, a) \in \mathsf {X} \times \mathsf {A} \tag {59}\partial_ {\mu (x)} g (\zeta , \mu) - \gamma_ {1} (x) - \gamma_ {2} - \gamma_ {3} (x) + \sum_ {y \in \mathsf {X}} \gamma_ {3} (y) p _ {\zeta} (y | x) = 0, \forall x \in \mathsf {X} \tag {60}\lambda , \gamma , \zeta , \mu \geq 0 \tag {61}\hat {\zeta} \geq (1 - \beta) \mu + \beta \zeta p _ {\mu}, \mu \geq \zeta p _ {\mu}, \mu (\mathsf {X}) \geq 1 \tag {62}- \sum_ {(x, a) \in \mathsf {X} \times \mathsf {A}} \zeta (x, a) \lambda_ {1} (x, a) + \sum_ {y \in \mathsf {X}} \left(- \hat {\zeta} (y) + (1 - \beta) \mu (y) + \beta \zeta p _ {\mu} (y)\right) \lambda_ {2} (y) = 0 \tag {63}- \sum_ {x \in \mathsf {X}} \mu (x) \gamma_ {1} (x) + (1 - \mu (\mathsf {X})) \gamma_ {2} + \sum_ {y \in \mathsf {X}} (- \mu (y) + \zeta p _ {\mu} (y)) \gamma_ {3} (y) = 0 \tag {64}$$

Here, (59) is indeed the Bellman optimality equation, where $\lambda _ { 2 }$ is the optimal value function. Now, we transform joint KKT conditions into a root finding problem. To this end, we introduce slack variables $( \bar { \lambda } , \bar { \gamma } )$ , where $\bar { \lambda } \in \mathbb { R } ^ { | \mathsf { X } \times \mathsf { A } | } \times \mathbb { R } ^ { | \mathsf { X } | }$ and $\bar { \gamma } \in \mathbf { \mathbb { R } } ^ { | \mathsf { X } | } \times \mathbb { R } \times \mathbb { R } ^ { | \mathsf { X } | }$ , and define

$$
H (z) := H (\zeta , \mu , \lambda , \gamma , \bar {\lambda}, \bar {\gamma}) := \left( \begin{array}{c} \mathbf {F} (\zeta , \mu , \lambda , \gamma) \\ \mathbf {h} (\zeta , \mu) + (\bar {\lambda}, \bar {\gamma}) \\ (\lambda , \gamma) \circ (\bar {\lambda}, \bar {\gamma}) \end{array} \right)
$$

and

$$Z := \left\{z = (\zeta , \mu , \lambda , \gamma , \bar {\lambda}, \bar {\gamma}): (\lambda , \gamma), (\bar {\lambda}, \bar {\gamma}) \geq 0 \right\}$$
