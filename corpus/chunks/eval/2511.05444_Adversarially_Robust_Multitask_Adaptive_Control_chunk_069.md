# Estimation error:

$$
\left\| \left[ \begin{array}{c c} \hat {A} _ {k} ^ {(i)} & \hat {B} _ {k} ^ {(i)} \end{array} \right] - \left[ \begin{array}{c c} A _ {\star} ^ {(i)} & B _ {\star} ^ {(i)} \end{array} \right] \right\| _ {F} ^ {2} \leq \frac {C _ {\mathrm{stat}} \sigma_ {w} ^ {2} (d _ {x} ^ {2} + d _ {x} d _ {u}) \log (2 T ^ {2})}{\sigma_ {k} ^ {2} M _ {j} \tau_ {k}} + C _ {\mathrm{mis}, 1} \exp \left(- C _ {\mathrm{mis}, 2} \sigma_ {k} ^ {2} \tau_ {k}\right), \tag {29}
$$

where we demonstrate that the estimation error holds for the next epoch $k + 1$ by balancing the exploration and exploitation as $\begin{array} { r } { \tau _ { k } \sigma _ { k } ^ { 2 } \geq \frac { 1 } { 2 } \tau _ { k + 1 } \sigma _ { k + 1 } ^ { 2 } } \end{array}$ . In addition, we guarantee that the state and controller norm bounds are not violated by combining the fact that $\Big \| P _ { \hat { K } _ { k + 1 } } ^ { ( i ) } \Big \| \leq 2 ( P _ { 0 } ^ { \vee } )$ P (i) Kˆk+1 and $\begin{array} { r } { \tau _ { k } \geq \tau _ { 1 } \geq \frac { c \log \frac { 1 } { P _ { \star } ^ { \wedge } } } { \log \left( 1 - \frac { 1 } { P _ { \star } ^ { \wedge } } \right) } } \end{array}$ c log 1P ∧⋆ for a sufficiently large constant $^ { c , }$ along with Lemma D.6 to obtain

$$
\left\| x _ {t} ^ {(i)} \right\| \leq 4 0 (P _ {0} ^ {\vee}) ^ {2} (\Psi_ {B} ^ {\vee}) \max _ {1 \leq t \leq T} \left\| \left[ \begin{array}{c} w _ {t} ^ {(i)} \\ g _ {t} ^ {(i)} \end{array} \right] \right\|, \forall t = \{\tau_ {k} + 1, \dots , \tau_ {k + 1} \}, \tag {30}
$$

which guarantees that the state norm requirement is satisfied. For the controller norm, we have that $\left\| \hat { K } _ { k + 1 } ^ { ( i ) } \right\| ^ { 2 } \leq \left\| P _ { \hat { K } _ { k + 1 } } \right\| \leq 2 P _ { 0 } ^ { \vee }$ PKˆ k+1 , and thus $\left\| \hat { K } _ { k + 1 } ^ { ( i ) } \right\| \le K _ { b }$ . We conclude the proof by noting that for epoch k the bounded state and controller norm requirements hold with probability $1 - { \textstyle { \frac { 1 } { 2 } } } T ^ { - 2 }$ , then for epoch $k + 1$ also holds with at least the same probability. Then, by union bounding for all the epochs, we have that $\mathcal { E } _ { \mathsf { s u c c e s s } }$ holds under probability of at least $1 - T ^ { - 2 }$ . □
