$$A ^ {T} P A - P + C ^ {T} C = - L L ^ {T} \tag {C.65}- B ^ {T} P A - D ^ {T} C = K ^ {T} L ^ {T} \tag {C.66}B ^ {T} P B - D ^ {T} D + \gamma^ {2} I = K ^ {T} K \tag {C.67}$$

4. Every solution $x ( t ) ( x ( 0 ) , u ( t ) )$ of (C.1)–(C.2) satisfies the following equality:

$$
\begin{array}{l} \sum_ {t = 0} ^ {t _ {1}} y ^ {T} (t) y (t) = \gamma^ {2} \sum_ {t = 0} ^ {t _ {1}} u ^ {T} (t) u (t) + x ^ {T} (0) P x (0) - x ^ {T} (t _ {1} + 1) P x (t _ {1} + 1) \\ - \sum_ {t = 0} ^ {t _ {1}} [ x ^ {T} (t), u ^ {T} (t) ] M \left[ \begin{array}{l} x (t) \\ u (t) \end{array} \right] \tag {C.68} \\ \end{array}
$$

Remark From (C.68) one gets for $x ( 0 ) = 0$ :

$$\| y (t) \| _ {2} ^ {2} \leq \gamma^ {2} \| u \| _ {2} ^ {2} \leq \| H \| _ {\infty} ^ {2} \| u \| ^ {2} \tag {C.69}$$

Proof We will only show that (C.61) through (C.64) implies (C.68). From (C.61), one gets:

$$
\begin{array}{l} x ^ {T} (t) Q x (t) = - x ^ {T} (t) C ^ {T} C x (t) + x ^ {T} (t) P x (t) - x ^ {T} (t) A ^ {T} P A x (t) \\ = - x ^ {T} (t) C ^ {T} C x (t) + x ^ {T} (t) P x (t) - x ^ {T} (t + 1) P x (t + 1) \\ + 2 u ^ {T} (t) B ^ {T} P A x (t) - u ^ {T} (t) B ^ {T} P B u (t) \tag {C.70} \\ \end{array}
$$

Using (C.62) and (C.63) and adding the terms $2 \boldsymbol { u } ^ { T } ( t ) \boldsymbol { S } ^ { T } \boldsymbol { x } ( t ) + \boldsymbol { u } ^ { T } ( t ) \boldsymbol { R } \boldsymbol { u } ( t )$ in the both sides of (C.58), one obtains:

$$
\begin{array}{l} [ x ^ {T} (t), u ^ {T} (t) ] M \left[ \begin{array}{c} x (t) \\ u (t) \end{array} \right] = - y ^ {T} (t) y (t) + \gamma^ {2} u ^ {T} (t) u (t) + x ^ {T} (t) P x (t) \\ - x ^ {T} (t + 1) P x (t + 1) \tag {C.71} \\ \end{array}
$$

Summing up now from 0 to $t _ { 1 }$ , one gets (C.65).

Theorem C.3 (Small Gain) Consider the feedback connection $( F i g . \mathbf { C . } 5 )$ between a linear time invariant block $S _ { 1 }$ (state: x) characterized by $\| S _ { 1 } \| _ { \infty } < 1$ and a block $S _ { 2 }$ characterized by $\| S _ { 2 } \| _ { \infty } \leq 1$ . Then:

$$\lim _ {t \to \infty} x (t) = 0; \quad \lim _ {t \to \infty} u _ {1} (t) = \lim _ {t \to \infty} y _ {1} (t) = 0 \tag {C.72}$$

Fig. C.6 Equivalent representation of the system shown in Fig. C.4   
![](image/db7a5d75ec3287c61fac0bd69aaba59a4cc293c35f44fb6c03514049f324c06a.jpg)

<details>
<summary>flowchart</summary>
