3) Inequalities for dissipativity and input-output relations: Dissipation inequalities with storage functions are derived for each main subsystem block, namely, $V _ { 0 }$ and $V _ { i } ^ { > } \mathrm { { s } }$ for each subsystem $\Psi _ { 0 }$ and $\Psi _ { i } \mathrm { } ^ { \prime } \mathbf { s }$ separately. The following are defined: $\Delta \bar { z _ { t } } = ( z _ { t } - \bar { z } ) , \Delta y _ { t } = \hat { ( y _ { t } - \bar { y } ) , } \Delta u _ { t } ^ { ( j ) } = ( u _ { t } ^ { ( j ) ^ { } } - \bar { u } _ { t } ^ { ( j ) } )$ (u(t u¯ , andstem, ∆c(j)t $\Delta c _ { t } ^ { ( j ) } = ( c _ { t } ^ { ( j ) } - \bar { c } _ { t } ^ { ( j ) } )$ = (c(j)t − $\Psi _ { 0 }$ a quadratic storage function family is chosen as $V _ { \bar { x } } ^ { 0 } ( x _ { t } ) =$ $( \bar { \Delta x _ { t } } ) ^ { \top } P ( \Delta x _ { t } )$ for some positive definite P . Evaluating the evolution of $V _ { \bar { x } } ^ { 0 }$ through the time steps results in:

$$
V _ {\bar {x}} ^ {0} (x _ {t + 1}) - V _ {\bar {x}} ^ {0} (x _ {t}) = \left[ \begin{array}{c} \Delta y _ {t} \\ \Delta z _ {t} \end{array} \right] ^ {\top} \left[ \begin{array}{c c} A ^ {\top} P A - P & A ^ {\top} P \\ \star & P \end{array} \right] \left[ \begin{array}{c} \Delta y _ {t} \\ \Delta z _ {t} \end{array} \right] \tag {16}
$$

$\phi ^ { ( j ) } ( \cdot ) \mathbf { \bar { s } }$ are static nonlinear mappings: they are memoryless, lossless, and thus, do not store energy; their storage functions $V _ { \bar { x } } ^ { j , } \mathbf { \bar { s } }$ are equal to 0 [23]. By [7, Proposition 2], $\phi ^ { ( j ) } ( \cdot )$ are ρi-cocoercive, therefore, the following relationship between the input and the output is known, and achieved by rearranging the ρi-cocoercivity inequality:

$$(\Delta u _ {t} ^ {(j)}) ^ {\top} \Delta c _ {t} ^ {(j)} - \rho_ {i} (\Delta u _ {t} ^ {(j)}) ^ {\top} \Delta u _ {t} ^ {(j)} \geq 0 \tag {17}$$

Since $V _ { \bar { x } } ^ { j } ~ = ~ 0 , ~ ( 1 7 )$ and $V _ { \bar { x } } ^ { j }$ are compared to each other to achieve the following relationship between the storage function $V _ { \bar { x } } ^ { i }$ and the input-output of $\Psi _ { i }$ :

$$
V _ {\bar {x}} ^ {j} \leq \left[ \begin{array}{c} \Delta u _ {t} ^ {(j)} \\ \Delta c _ {t} ^ {(j)} \end{array} \right] ^ {\top} \left[ \begin{array}{c c} - \rho_ {i} I & \frac {1}{2} I \\ \star & 0 \end{array} \right] \left[ \begin{array}{c} \Delta u _ {t} ^ {(j)} \\ \Delta c _ {t} ^ {(j)} \end{array} \right], \tag {18}
$$

4) Connection of main blocks $\Psi _ { 0 }$ and $\Psi _ { i } \ ' .$ : Next, the connection between the two main dissipativity blocks are applied through the interconnection equations given as:
