# G.11 Proof of Lemma 32

proof. In Algorithm 2, $\hat { \theta } _ { s }$ satisfies

$$\hat {\theta} _ {s} = \operatorname * {a r g m a x} _ {\| \theta - \hat {\theta} _ {s} ^ {\mathrm{pre}} \| \leq \epsilon_ {s}} a - b K _ {\mathrm{opt}} (\theta).$$

Under event $E _ { 2 } ^ { s } .$ , we have that $\| \hat { \theta } _ { s } ^ { \mathrm { p r e } } - \theta ^ { * } \| \leq \epsilon _ { s }$ , which implies that

$$\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s}) \geq a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\theta^ {*}).$$

Therefore, we have that (using Lemma 20 in the equality)

$$\frac {D _ {U}}{\hat {a} _ {s} - \hat {b} _ {s} K _ {\mathrm{opt}} (\hat {\theta} _ {s})} - D _ {U} \leq \frac {D _ {U}}{a ^ {*} - b ^ {*} K _ {\mathrm{opt}} (\theta^ {*})} - D _ {U} = \bar {w} - \epsilon^ {*}. \tag {126}$$

Under event $E _ { 2 } ^ { s }$ , we also have that $\| \hat { \theta } _ { s } - \theta ^ { * } \| _ { \infty } \leq \tilde { O } _ { T } ( T ^ { - 1 / 4 } )$ , therefore
