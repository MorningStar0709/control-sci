$$
\begin{array}{l} \operatorname{cl} \left(\bigcup_ {L = 0} ^ {\infty} \Pi_ {L} (\mathcal {B})\right) = \operatorname{cl} \left(\mathcal {B} \cap \left(\mathbb {R} ^ {q}\right) ^ {\infty}\right) \subseteq \operatorname{cl} (\mathcal {B}) \cap \operatorname{cl} \left(\left(\mathbb {R} ^ {q}\right) ^ {\infty}\right) \\ = \operatorname{cl} (\mathcal {B}) \cap (\mathbb {R} ^ {q}) ^ {\mathbb {Z} _ {+}} = \operatorname{cl} (\mathcal {B}) = \mathcal {B}, \\ \end{array}
$$

where we have used $( \mathbb { R } ^ { q } ) ^ { \mathbb { Z } + } = \mathrm { c l } \left( ( \mathbb { R } ^ { q } ) ^ { \infty } \right)$ [38, p151] and the completeness of B.

(⊇). Let $w \in B$ and recall that $w \in \operatorname { c l } \left( \bigcup _ { L = 0 } ^ { \infty } \Pi _ { L } ( B ) \right)$ if and only if w is the limit of some sequence of points in $\textstyle \bigcup _ { L = 0 } ^ { \infty } \Pi _ { L } ( B )$ . Consider the sequence $\bar { w } _ { L } = \Pi _ { L } ( w )$ for $L \in \mathbb { Z } _ { + }$ . Clearly, $\bar { w } _ { L } \in \bigcup _ { L = 0 } ^ { \infty } \Pi _ { L } ( B )$ . Furthermore, since $B \in \mathcal { L } ^ { q } \cap \ell _ { 2 } ^ { q } , \bar { w } _ { L } \to w .$ , as desired. □

Proof of Theorem 2. By assumption, P and $\tilde { P }$ are bounded linear operators, so $\mathrm { g a p } _ { \ell _ { 2 } } ( P , \tilde { P } )$ is well-defined. Then
