# E Proof of Lemma 1

Proof. [Proof of Case (i).] Let $\alpha ( \cdot ) \equiv \operatorname* { m i n } _ { i \in [ n ] } \alpha _ { i }$ and $\beta ( \cdot ) \equiv \operatorname* { m i n } _ { i \in [ n ] } \beta _ { i }$ . Then $\alpha ( \cdot )$ and $\beta ( \cdot )$ are positive constant functions, and hence are nonincreasing and nondecreasing, respectively. Moreover, i $\operatorname { 1 f } _ { z \in \mathbb { R } ^ { n } } \alpha \breve { ( } \Vert f ( z ) \Vert ) > 0$ and $\begin{array} { r } { \operatorname* { s u p } _ { z \in \mathbb { R } ^ { n } } \bar { \beta } ( \| f ( z ) \| ) < \infty } \end{array}$ . We now verify (2). In fact, for and $c > 0$ and all $\| y \| , \| z \| \leq c ,$

$$
\begin{array}{l} (y - z) ^ {\top} (f (y) - f (z)) = \sum_ {i = 1} ^ {n} \left(y _ {i} - z _ {i}\right) ^ {\top} \left(f _ {i} \left(y _ {i}\right) - f _ {i} \left(z _ {i}\right)\right) \\ \geq \sum_ {i = 1} ^ {n} \alpha_ {i} \| y _ {i} - z _ {i} \| ^ {2} \geq (\min _ {i \in [ n ]} \alpha_ {i}) \| y - z \| ^ {2} = \alpha (c) \| y - z \| ^ {2}. \tag {E.1} \\ \end{array}
$$

where $y _ { i }$ and $z _ { i }$ denote the i-th components of vectors y and z, respectively. Similarly, we have $\| f ( y ) - f ( z ) \| \leq$ $\beta ( c ) \| y - \bar { z } \|$ . Thus, (2) holds and Case (i) follows.

[Proof of Case (ii).] With $\alpha ( \cdot )$ and $\beta ( \cdot )$ defined in (4), it is clear that α(·) is nonincreasing and $\beta ( \cdot )$ is nondecreasing, respectively. Besides, since $\begin{array} { r } { \operatorname* { s u p } _ { z \in \mathbb { R } ^ { n } } f ( z ) < } \end{array}$ $\infty ,$ , it follows form (3) that $\operatorname* { i n f } _ { z \in \mathbb { R } ^ { n } } \alpha ( \| f ( z ) \| ) > 0$ and $\begin{array} { r } { \operatorname* { s u p } _ { z \in \mathbb { R } ^ { n } } \beta ( \| f ( z ) \| ) < \infty } \end{array}$ . Moreover, for any $c \geq 0$ and $\| y \| , \| z \| \leq c ,$ by the mean-value theorem, we have

$$(y _ {i} - z _ {i}) (f _ {i} (y _ {i}) - f _ {i} (z _ {i})) \geq (\inf _ {| x | \leq c} \frac {d}{d x} f _ {i} (x)) (y _ {i} - z _ {i}) ^ {2}.$$

where $y _ { i }$ and $z _ { i }$ are the i-th components of vectors y and z, respectively. Then, using an argument analogous to that in $( \mathrm { E . 1 } )$ , we obtain $( \breve { y } - z ) ^ { \breve { \top } } ( f ( y ) - f ( z ) \breve { ) } \geq$ $\begin{array} { r } { ( \operatorname* { i n f } _ { i \in [ n ] , | x | \leq c } \frac { d } { d x } f _ { i } ( x ) ) \| y - z \| ^ { 2 } = \alpha ( c ) \| y - z \| ^ { 2 } } \end{array}$ . Similarly, we have $\| { \dot { f } } ( y ) - f ( z ) \| \leq \beta ( c ) \| y - z \|$ . Case (ii) follows. ✷
