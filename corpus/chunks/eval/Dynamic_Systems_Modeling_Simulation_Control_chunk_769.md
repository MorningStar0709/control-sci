# Sinks Library

Scope: plots the input signal with respect to simulation time.

To File: writes simulation time (row 1) and the input signal (row 2) to a MAT file that is defined by the user.

To Workspace: writes the input signal to MATLAB’s workspace. The user defines the variable name and format (Array is the recommended format).
