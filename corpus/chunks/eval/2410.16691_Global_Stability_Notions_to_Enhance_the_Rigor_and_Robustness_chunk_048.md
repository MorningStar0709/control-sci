We prove (120) by contradiction. Suppose that $\operatorname* { l i m } _ { t \to + \infty } \operatorname* { s u p } _ { } \big ( \big | y ( t ) \big | \big ) > b \big ( \big \| d \big \| _ { \infty } \big )$ . Then by virtue of (2) there exists $\varepsilon > 0$ and a sequence of times $\left\{ t _ { i } \ge 0 : i = 1 , 2 , \ldots \right\}$ with $\operatorname* { l i m } _ { i \to + \infty } \left( t _ { i } \right) = + \infty$ such that $\left| y \left( t _ { i } \right) \right| = \left| h \left( x ( t _ { i } ) \right) \right| \geq b \left( \left\| d \right\| _ { \infty } \right) + \varepsilon$ . Since the solution $\ b { x } ( t ) \in \mathbb { R } ^ { n }$ is bounded, it follows that the sequence $\left\{ \boldsymbol { x } ( t _ { i } ) \in \mathbb { R } ^ { n } : i = 1 , 2 , \ldots \right\}$ is bounded. By taking a subsequence if necessary, we may assume that there exists $x ^ { * } \in \mathbb { R } ^ { n }$ such that $\left( x ( t _ { i } ) \right) = x ^ { * }$ . Consequently, by exploiting continuity of $Q _ { s } , ( 1 1 9 )$ and the fact that $s = \| d \| _ { \alpha }$ we $\mathrm { g e t } \ \Big | h \Big ( x ^ { * } \Big ) \Big | \geq b \Big ( \big \| d \big \| _ { \infty } \Big ) + \varepsilon = b \big ( s \big ) + \varepsilon > b ( s )$ and $Q _ { s } \left( x ^ { * } \right) = 0$ . Clearly, this contradicts (115).

Inequality (120) and the fact that $x _ { 0 } \in \mathbb { R } ^ { n } , d \in L ^ { \infty } \left( \mathbb { R } _ { + } ; D \right)$ are arbitrary imply the p-OAG property with asymptotic residual constant $\tilde { \alpha } = b ( 0 )$ and asymptotic gain function $\tilde { \gamma } ( s ) = b ( s ) - b ( 0 )$ . The proof is complete.

Example 10: Consider the following planar system

$$\dot {\xi} = 0\dot {y} = - y p (\xi , y, d) + \left(1 - y ^ {2}\right) ^ {+} q (\xi , y, d) \tag {121}x = (\xi , y) ^ {\prime} \in \mathbb {R} ^ {2}, d \in \mathbb {R}$$

where $p , q : \mathbb { R } ^ { 3 }  \mathbb { R }$ are locally Lipschitz functions that satisfy $q ( 0 , 0 , 0 ) = 0$ and $p \big ( \xi , y , d \big ) > 0$ for all $( \xi , y , d ) \in \mathbb { R } ^ { 3 }$ . System (121) is not necessarily a system that satisfies the IOS property (e.g., consider the input-free case $p \big ( \xi , y , d \big ) \equiv 1$ and $q \left( \xi , y , d \right) = 2 y )$ . However, system (121) satisfies the p-UBIBS property (use Theorem 5 with $H _ { 1 } ( x ) = \xi ^ { 2 } / 2 , H _ { 2 } ( x ) = y ^ { 2 } / 2 { \mathrm { ~ a n d ~ } } R ( s ) \equiv 1 / 2 )$ .

The function

$$V (\xi , y) = y ^ {2} / 2 \tag {122}$$

gives for all $( x , d ) \in \mathbb { R } ^ { 2 } \times$ :

$$\dot {V} (\xi , y, d) = - y ^ {2} p (\xi , y, d) + y (1 - y ^ {2}) ^ {+} q (\xi , y, d)$$
