$\eta _ { \tilde { \theta } } \geq$ $\left\{ \frac { \overline { { a } } _ { 0 } } { \overline { { a } } _ { 1 } } , \gamma _ { 0 } \lambda _ { \operatorname* { m a x } } ^ { \mathrm { U B } } + \gamma _ { 1 } \right\}$ $\tilde { \theta } \left( t \right)$

1The next step is to analyze the properties of the augmented tracking error $\xi \left( t \right)$ .

As $A _ { r e f }$ is a Hurwitz matrix and ?? is of full column rank, then according to the corollary of the KYP lemma (2), there exist ??, ??, ??, ??, which satisfy (2). Then the quadratic function to analyze the stability of (12) is chosen:

$$
\begin{array}{l} V _ {\xi} = e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} + \frac {1}{2} t r (\tilde {\theta} ^ {\mathrm{T}} \tilde {\theta}), H = b l o c k d i a g \left\{M, \frac {1}{2} I \right\} \\ \underbrace {\lambda_ {\min} (H)} _ {\lambda_ {m}} \| \xi \| ^ {2} \leq V (\| \xi \|) \leq \underbrace {\lambda_ {\max} (H)} _ {\lambda_ {M}} \| \xi \| ^ {2}, \tag {E25} \\ \end{array}
$$

Applying $t r \left( A B \right) = B A$ , the derivative of (E25) with respect to (12), (32) is written as:

$$
\begin{array}{l} \dot {V} _ {\xi} = e _ {r e f} ^ {\mathrm{T}} \left(A _ {r e f} ^ {\mathrm{T}} M + M A _ {r e f}\right) e _ {r e f} + 2 e _ {r e f} ^ {\mathrm{T}} M B \tilde {\theta} ^ {\mathrm{T}} \omega - t r \left(\tilde {\theta} ^ {\mathrm{T}} \gamma \Omega^ {2} \tilde {\theta} + \tilde {\theta} ^ {\mathrm{T}} \gamma \Omega \varsigma\right) = \\ = - \mu e _ {r e f} ^ {\mathrm{T}} M e _ {r e f} + t r \left(- K ^ {\mathrm{T}} N ^ {\mathrm{T}} e _ {r e f} e _ {r e f} ^ {\mathrm{T}} N K + 2 \tilde {\theta} ^ {\mathrm{T}} \omega e _ {r e f} ^ {\mathrm{T}} N K - \tilde {\theta} ^ {\mathrm{T}} \left[ \gamma \Omega^ {2} \tilde {\theta} + \gamma \Omega \varsigma \right]\right). \tag {E26} \\ \end{array}
$$

Without the loss of generality, ?? is chosen so as $\begin{array} { r } { K K ^ { \mathrm { T } } = K ^ { \mathrm { T } } K = I _ { m \times m } } \end{array}$ . Then, completing the square in (E26), it is obtained:
