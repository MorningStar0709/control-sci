where cl $\in \mathsf { C L }$ is a clock, $\psi \in \mathbb { Q }$ is a clock constraint, and ./∈ $\{ < , > , \geq , \leq , = \}$ . A clock valuation is a mapping $v : { \mathsf { C L } } \to$ R that assigns a value to each clock. A clock ${ \mathsf { c l } } _ { i }$ has valuation $v _ { i }$ for $i \in \{ 1 , \ldots , | \mathsf { C L } | \}$ . Given $v : = ( v _ { 1 } , \ldots , v _ { | \mathsf { C L } | } )$ and $t \in \mathbb { R } _ { \geq 0 }$ , we denote by $v \models \phi$ and $t \models \phi$ the fact that the valuation v and the time instant $t ,$ respectively, satisfy the clock constraint $\phi .$ .

Definition F.9. A Timed B¨uchi Automaton is a tuple

$$\mathcal {A} _ {t} := (Q, Q _ {0}, \mathrm{CL}, \Psi , E, F),$$

where $Q$ is a finite set of locations, $Q _ { 0 } \subseteq Q$ is the set of initial locations, CL is a finite set of clocks, Ψ is a finite set of atomic propositions that defines the input alphabet $2 ^ { \Psi } , E \subset Q \times \Phi ( { \mathsf { C L } } ) \times 2 ^ { \mathsf { C L } } \times 2 ^ { \Psi } \times Q$ gives the set of edges of the form $e = ( q , g , R , \alpha , q ^ { \prime } )$ , where $q , q ^ { \prime }$ are the source and target locations, $g$ is the guard of edge, R is a set of clocks to be reset upon executing the edge, and $\alpha$ is an input string; finally, $F \subseteq Q$ is a set of accepting locations.
