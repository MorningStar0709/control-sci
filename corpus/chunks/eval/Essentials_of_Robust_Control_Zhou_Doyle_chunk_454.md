$$
\left\{P _ {1}: \vec {\delta} _ {g} (P, P _ {1}) < b \right\}
= \left\{P _ {1}: P _ {1} = (N + \Delta_ {N}) (M + \Delta_ {M}) ^ {- 1},   \Delta_ {N},   \Delta_ {M} \in \mathcal {H} _ {\infty}, \left\| \left[ \begin{array}{c} \Delta_ {N} \\ \Delta_ {M} \end{array} \right] \right\| _ {\infty} <   b \right\}.
$$

Proof. Suppose $\vec { \delta } _ { g } ( P , P _ { 1 } ) < b$ and let $P _ { 1 } = N _ { 1 } M _ { 1 } ^ { - 1 }$ be a normalized right coprime factorization. Then there exists a $Q \in { \mathcal { H } } _ { \infty }$ such that

$$
\left\| \left[ \begin{array}{c} M \\ N \end{array} \right] - \left[ \begin{array}{c} M _ {1} \\ N _ {1} \end{array} \right] Q \right\| _ {\infty} <   b.
$$

Define

$$
\left[ \begin{array}{c} \Delta_ {M} \\ \Delta_ {N} \end{array} \right] := \left[ \begin{array}{c} M _ {1} \\ N _ {1} \end{array} \right] Q - \left[ \begin{array}{c} M \\ N \end{array} \right] \in \mathcal {H} _ {\infty}.
$$

$\left\| \left[ \begin{array} { l } { \Delta _ { M } } \\ { \Delta _ { N } } \end{array} \right] \right\| _ { \infty } < b$ and P1 = (N

To show the converse, note that $P _ { 1 } = ( N + \Delta _ { N } ) ( M + \Delta _ { M } ) ^ { - 1 }$ and there exists a $\tilde { Q } ^ { - 1 } \in \mathcal { H } _ { \infty }$ such that $P _ { 1 } = \Big \{ ( N + \Delta _ { N } ) \tilde { Q } \Big \} \Big \{ ( M + \Delta _ { M } ) \tilde { Q } \Big \} ^ { - 1 }$ is a normalized right coprime factorization. Hence by definition, $\vec { \delta } _ { g } ( P , P _ { 1 } )$ can be computed as

$$
\vec {\delta} _ {g} (P, P _ {1}) = \inf _ {Q} \left\| \left[ \begin{array}{c} M \\ N \end{array} \right] - \left[ \begin{array}{c} M + \Delta_ {M} \\ N + \Delta_ {N} \end{array} \right] \tilde {Q} Q \right\| _ {\infty} \leq \left\| \left[ \begin{array}{c} M \\ N \end{array} \right] - \left[ \begin{array}{c} M + \Delta_ {M} \\ N + \Delta_ {N} \end{array} \right] \right\| _ {\infty} <   b
$$

where the first inequality follows by taking $Q = \tilde { Q } ^ { - 1 } \in \mathcal { H } _ { \infty }$

The following is a list of useful properties of the gap metric shown by Georgiou and Smith [1990].

• If $\delta _ { g } ( P _ { 1 } , P _ { 2 } ) < 1$ , then $\delta _ { g } ( P _ { 1 } , P _ { 2 } ) = { \vec { \delta } } _ { g } ( P _ { 1 } , P _ { 2 } ) = { \vec { \delta } } _ { g } ( P _ { 2 } , P _ { 1 } )$ .   
$\bullet \ \operatorname { I f } \ b \leq \lambda ( P ) : = \operatorname* { i n f } _ { \operatorname { R e } s > 0 } \underline { { \sigma } } \left( \left[ \begin{array} { l } { M ( s ) } \\ { N ( s ) } \end{array} \right] \right) , \mathrm { t h e n }$

$$\left\{P _ {1}: \vec {\delta} (P, P _ {1}) < b \right\} = \left\{P _ {1}: \delta (P, P _ {1}) < b \right\}.$$

Recall that
