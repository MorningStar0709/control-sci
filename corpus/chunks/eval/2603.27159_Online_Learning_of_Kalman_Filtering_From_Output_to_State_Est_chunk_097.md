# D.5 Proof of Lemma 5.5

Suppose throughout this proof that the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { \imath }$ holds and consider any $\delta \in ( 0 , 1 )$

Proof of part (i). The proof of part (i) follows directly from that of Lemma 4.4. Specifically, we observe that if we replace N with M, instead define the time indices $t _ { i , j } = k + i + ( j - 1 ) h$ for all $i \in \{ 0 , \ldots , h - 1 \}$ and all $j \in [ T _ { i } ]$ , where $T _ { i } = \operatorname* { m a x } \{ j : t _ { i , j } \leq T - 1 \}$ , and replace δ with $\delta / 4$ , the proof of Lemma 4.4 still holds, which yields

$$\sum_ {t = \tau} ^ {T - 1} X _ {t} (M ^ {\star}) \leq \frac {3 2 l _ {x} ^ {2} h \log \frac {1 2 h}{\delta}}{\alpha_ {0}} + \frac {\alpha_ {0}}{4} \sum_ {t = 0} ^ {T - 1} \| M _ {t} - M ^ {\star} \|.$$

Since the above inequality holds for any realization of the random variables $b _ { 0 } , \dots , b _ { \lfloor T / \tau \rfloor }$ , taking the expectation $\mathbb { E } _ { b } [ \cdot ]$ with respect to $b _ { 0 } , \dots , b _ { \lfloor T / \tau \rfloor }$ on both sides of the above inequality proves part (i).

Proof of part (ii). First, we get from $\tilde { f } _ { t _ { j } } ( M _ { t _ { j } } ) = \left\| x _ { t _ { j } } + \tilde { v } _ { t _ { j } } - \hat { x } _ { t _ { j } } ( M _ { t _ { j } } ) \right\| ^ { 2 }$ that

$$\nabla \tilde {f} _ {t _ {j}} (M _ {t _ {j}}) = 2 Y _ {t _ {j} - 1: t _ {j} - h} ^ {\top} (Y _ {t _ {j} - 1: t _ {j} - h} M _ {t _ {j}} - x _ {t _ {j}} - \tilde {v} _ {t _ {j}})= \nabla f _ {t _ {j}} (M _ {t _ {j}}) - 2 Y _ {t _ {j} - 1: t _ {j} - h} \tilde {v} _ {t _ {j}}, \tag {D.16}$$

where $Y _ { t _ { j } - 1 : t _ { j } - h }$ is defined in (22) and the second equation follows from (23). Now, under the event $\mathcal { E } _ { \tilde { v } }$ defined in (39), we have $\begin{array} { r } { \operatorname* { m a x } _ { 0 \leq t \leq T - 1 } \| \tilde { v } _ { t } \| \leq R _ { \tilde { v } } } \end{array}$ , which implies

$$\left\| \nabla \tilde {f} _ {t _ {j}} (M _ {t _ {j}}) \right\| ^ {2} \leq \left(\left\| \nabla f _ {t _ {j}} \right\| (M _ {t _ {j}}) + 2 \left\| Y _ {t - 1: t - h} \right\| \left\| \tilde {v} _ {t _ {j}} \right\|\right) ^ {2}\leq (l _ {x} + 2 \sqrt {h} R _ {y} R _ {\tilde {v}}) ^ {2}, \forall t _ {j} \in S, \tag {D.17}$$

where we also use the upper bound $\| Y _ { t - 1 : t - h } \| \le \sqrt { h } R _ { y }$ shown in Lemma 3.2 and $\left\| \nabla \tilde { f } _ { t } ( M _ { t _ { j } } ) \right\| \leq l _ { x }$ shown in Lemma 3.3.

Thus, we get that under the event $\mathcal { E } _ { \tilde { v } }$ ,
