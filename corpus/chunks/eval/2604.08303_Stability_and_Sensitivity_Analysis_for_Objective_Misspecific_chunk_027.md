blkdiag within where $\begin{array} { r c l } { \tilde { Q _ { i } } ^ { ( j ) } } & { = } & { \mathrm { b l k d i a g } ( Q _ { i } ^ { ( j ) } , \dots , Q _ { i } ^ { ( j ) } ) \quad \tilde { R _ { i _ { i k } } } ^ { ( j ) } } \end{array}$ $\ k ( R _ { i _ { i k } } ^ { ( j ) } , \ldots , R _ { i _ { i k } } ^ { ( j ) } )$ R(j)iik ) $_ { i k }$ R˜iik osition, A˜ is = $\ddot { R _ { i } ^ { ( j ) } } , \ : \tilde { q _ { i } } ^ { ( j ) } = \mathrm { c o l } ( q _ { i } ^ { ( j ) } , \ldots , q _ { i } ^ { ( j ) } )$ the free response matrix of the global dynamics (3), and ${ \tilde { B } } _ { i }$ the impulse response matrix of agent i, which are expressed explicitly as:

$$
\tilde {A} = \left[ \begin{array}{c} I \\ A \\ \vdots \\ A ^ {K} \end{array} \right], \quad \tilde {B} _ {i} = \left[ \begin{array}{c c c c} 0 & \dots & \dots & 0 \\ B _ {i} & 0 & \dots & 0 \\ A B _ {i} & B _ {i} & \dots & 0 \\ \vdots & \vdots & \ddots & \vdots \\ A ^ {K - 1} B _ {i} & \dots & A B _ {i} & B _ {i} \end{array} \right] \tag {13}
$$

Recall that each agent’s finite horizon vGNE prediction is the solution to a variational inequality. Variational inequalities can also be expressed as normal cone inclusion problems of the form $F ( u ) + N z \ni 0$ . The solution mapping $\mathcal { S } ^ { ( j ) }$ can be rewritten as $\mathcal { S } ^ { ( j ) } ( \mathbf { x } ) = \phi ^ { ( j ) } ( - F _ { x } ^ { ( j ) } \mathbf { x } )$ by defining $\phi ^ { ( j ) }$ , a static nonlinearity, as: $\phi ^ { ( j ) } ( \cdot ) = \left( F _ { u } ^ { ( j ) } + \mathcal { N } _ { \mathcal { Z } } \right) ^ { - 1 } ( \cdot )$ .

![](image/5e1526ccf4d89a66e97e932357f03f5dcebfe77c7ab0b353f998643bf3ce6664.jpg)

<details>
<summary>flowchart</summary>

```mermaid
graph TD
    A["+"] -->|z_t| B["Ψ_0: {x_{t+1} = Ax_t + z_t\ny_t = x_t}"]
    A --> C["B_1 Ξ_1"]
    A --> D["B_n Ξ_n"]
    C -->|u^(1)(t)| E["Ψ_1: u^(1)(t) = φ^(1)(c^(1)(t))"]
    D -->|u^(n)(t)| F["Ψ_n: u^(n)(t) = φ^(n)(c^(n)(t))"]
    E -->|c^(1)(t)| G["-F_x^(1)"]
    F -->|c^(n)(t)| H["-F_x^(n)"]
    G --> I["y_t"]
    H --> I
```
</details>

Fig. 6: Block diagram of the feedback connection between $\Psi _ { 0 }$ and $\Psi _ { j } \mathbf { \dot { s } } ,$ , modeling heterogneous MPG system.

The first block, $\Psi _ { 0 } ,$ , representing the LTI system is:

$$
\Psi_ {0}: \left\{ \begin{array}{c} x _ {t + 1} = A x _ {t} + z _ {t} \\ y _ {t} = x _ {t} \end{array} \right. \tag {14}
$$
