$$
\bar {\sigma} \left(\left[ \begin{array}{c c} D \cdot I _ {n _ {v}} & 0 \\ 0 & I _ {n _ {e}} \end{array} \right] M \left[ \begin{array}{c c} D ^ {- 1} \cdot I _ {n _ {w}} & 0 \\ 0 & I _ {n _ {d}} \end{array} \right]\right) <   1. \tag {48}
$$

B) $\ b { I f } \Delta \in \mathbb { C } ^ { n _ { w } \times n _ { v } }$ and $\Delta _ { P } \in \mathbb { C } ^ { n _ { d } \times n _ { \epsilon } }$ satisfy

$$
\det \left(I - M \left[ \begin{array}{c c} \Delta & 0 \\ 0 & \Delta_ {P} \end{array} \right]\right) = 0. \tag {49}
$$

then max $( \bar { \sigma } ( \Delta ) , \bar { \sigma } ( \Delta _ { P } ) ) > 1$ .

C) $F _ { U } ( M , \Delta )$ is well-posed and $\bar { \sigma } ( F _ { U } ( M , \Delta ) ) < 1$ for all $\Delta \in \mathbb { C } ^ { n _ { w } \times n _ { v } }$ with $\bar { \sigma } ( \Delta ) \leq 1$

Proof. Equivalence of A) and B) follows from the fact that the structured singular value (µ) is equal to its upper bound for the case of two full, complex blocks. This was originally shown in [40] but a more recent reference is Theorem 9.1 of [33]. Equivalence of B) and C) is a special case of the main-loop theorem (Theorem 4.3 of [33]).

We’ll sketch the proof of sufficiency, i.e. $\mathbf { A } ) {  } \mathbf { B } )  \mathbf { C } )$ . This direction is used to verify the bound on robust regret and can be shown with only linear algebra facts.

$A \to B )$ : Assume A) is true and that $\Delta , \Delta _ { P }$ satisfy (49). We will show that max $\big ( \bar { \sigma } ( \Delta ) , \bar { \sigma } ( \Delta _ { P } ) \big ) >$ 1, i.e. B) holds.

Note that $\Delta = D ^ { - 1 } \Delta D$ because $D ~ > ~ 0$ is a scalar. Use this relation and the Sylvester determinant identity to re-write (49) as:

$$
\det \Big (I - \underbrace {\left[ \begin{array}{c c} D \cdot I _ {n _ {v}} & 0 \\ 0 & I _ {n _ {e}} \end{array} \right]} _ {:= \hat {M}} M \left[ \begin{array}{c c} D ^ {- 1} \cdot I _ {n _ {w}} & 0 \\ 0 & I _ {n _ {d}} \end{array} \right] \underbrace {\left[ \begin{array}{c c} \Delta & 0 \\ 0 & \Delta_ {P} \end{array} \right]} _ {:= \hat {\Delta}} \Big) = 0.
$$

Thus $I - \hat { M } \hat { \Delta }$ is singular so there is a nonzero vector v such that $\hat { M } \hat { \Delta } v = v$ . Hence $\| \hat { M } \hat { \Delta } \| _ { 2  2 } \ge$ ∥Mˆ ∆ˆ v∥2∥ ∥ = 1. Further, the induced 2-norm is sub-multiplicative: $\begin{array} { r } { \frac { \| \hat { M } \hat { \Delta } v \| _ { 2 } } { \| v \| _ { 2 } } = 1 } \end{array}$ v 2

$$\| \hat {M} \hat {\Delta} \| _ {2 \rightarrow 2} \leq \| \hat {M} \| _ {2 \rightarrow 2} \cdot \| \hat {\Delta} \| _ {2 \rightarrow 2}.$$
