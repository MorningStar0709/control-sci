# F Proof of Lemma 2

Proof. [Proof of Lemma 2.] Following the analysis ideas of the classical LS for linear stochastic regression models (see, Lai and Wei (1982), Guo (1995)), we consider the stochastic time-varying Lyapunov function:

$$V _ {t} = \operatorname{tr} \left[ \left(\theta^ {*} - \hat {\theta} _ {t}\right) ^ {\top} P _ {t} ^ {- 1} \left(\theta^ {*} - \hat {\theta} _ {t}\right) \right], t \geq 0.$$

For each $t \geq 0 ,$ define

$$e _ {t} = \hat {\theta} _ {t} + \mu_ {t} ^ {- 1} d _ {t} P _ {t + 1} \phi_ {t} [ x _ {t + 1} - f (\hat {\theta} _ {t} ^ {\top} \phi_ {t}) ] ^ {\top}.$$

We first prove the following inequality:

$$V _ {t + 1} \leq \mathrm{tr} \left[ (\theta^ {*} - e _ {t}) ^ {\top} P _ {t + 1} ^ {- 1} (\theta^ {*} - e _ {t}) \right]. \tag {F.1}$$

To prove (F.1), we consider two cases. Case $( i ) \colon \mathrm { I f } e _ { t } \notin \Theta ,$ , then by (9) and the definition of the projection operator $\Pi _ { \Theta } ^ { t + 1 } ( \cdot )$ , we have $\begin{array} { r } { \widehat { \theta } _ { t + 1 } = \arg \operatorname* { m i n } _ { y \in \Theta _ { \epsilon } } \| e _ { t } - y \| _ { P _ { t + 1 } ^ { - 1 } } } \end{array}$ . Here, the norm $\| \cdot \| _ { P _ { t + 1 } ^ { - 1 } }$ is defined by ∥x $| _ { P _ { t + 1 } ^ { - 1 } } = ( \mathrm { t r } [ x ^ { \top } P _ { t + 1 } ^ { - 1 } x ] ) ^ { \frac { 1 } { 2 } }$ for all $\boldsymbol { x } \in \mathbb { R } ^ { ( m + n ) \times n }$ and $t \geq 0$ . Since Θϵ is convex and compact, and $\theta ^ { * } \in \Theta ,$ ϵ by construction, (F.1) follows from the non-expansive property of the projection operator (see Lemma 1 in Zhang et al. $( 2 0 2 \hat { 2 } ) )$ . Case (ii): If instead $e _ { t } \in \Theta$ , it satisfies $\widehat { \theta } _ { t + 1 } = e _ { t }$ , hence, the inequality (F.1) holds with equality. Combining the two cases, (F.1) holds for all $t \geq \bar { 0 }$ .

From (A.8), we have $P _ { t + 1 } ^ { - 1 } = P _ { t } ^ { - 1 } + \mu _ { t } ^ { - 1 } d _ { t } ^ { 2 } \phi _ { t } \phi _ { t } ^ { \top }$ . Hence,

$$
\begin{array}{l} \left(\theta^ {*} - \hat {\theta} _ {t}\right) ^ {\top} P _ {t + 1} ^ {- 1} \left(\theta^ {*} - \hat {\theta} _ {t}\right) \tag {F.2} \\ = V _ {t} + \mu_ {t} ^ {- 1} d _ {t} ^ {2} t r [ (\theta^ {*} - \hat {\theta} _ {t}) ^ {\top} \phi_ {t} \phi_ {t} ^ {\top} (\theta^ {*} - \hat {\theta} _ {t}) ], \\ \end{array}
$$

and

$$
\begin{array}{l} a _ {t} P _ {t + 1} ^ {- 1} P _ {t} \phi_ {t} = a _ {t} \left(I + \mu_ {t} ^ {- 1} d _ {t} ^ {2} \phi_ {t} \phi_ {t} ^ {\top} P _ {t}\right) \phi_ {t} \tag {F.3} \\ = a _ {t} \phi_ {t} \left(1 + \mu_ {t} ^ {- 1} d _ {t} ^ {2} \phi_ {t} ^ {\top} P _ {t} \phi_ {t}\right) = \mu_ {t} ^ {- 1} \phi_ {t}. \\ \end{array}
$$
