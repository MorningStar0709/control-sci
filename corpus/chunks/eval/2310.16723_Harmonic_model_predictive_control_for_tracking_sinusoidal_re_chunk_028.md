$$\| \mathcal {T} _ {w} ^ {m} (z - v) \| _ {D} ^ {2} = (z - v) ^ {\top} (\mathcal {T} _ {w} ^ {m}) ^ {\top} D \mathcal {T} _ {w} ^ {m} (z - v)\stackrel {(*)} {=} (z - v) ^ {\top} D (z - v) = \| z - v \| _ {D} ^ {2},$$

where (∗) follows from the definition of $\mathcal { T } _ { w } ^ { m }$ (6) and the well known identity $\sin ^ { 2 } ( w ) + \cos ^ { 2 } ( w ) = 1$ . The equality then follows from the fact that $T _ { h } \in \mathbb { D } _ { + } ^ { n _ { x } }$ and $S _ { h } \in \mathbb { D } _ { + } ^ { n _ { u } }$ . Therefore, (13) is equivalent to (10), whose solution is $( \mathring \mathbf { x } ( t ) , \mathring \mathbf { u } ( t ) )$ by definition, thus $\hat { \mathbf { x } } _ { h } ^ { * } = \mathring { \mathbf { x } } ( t )$ and $\hat { \mathbf { u } } _ { h } ^ { * } = \mathring { \mathbf { u } } ( t )$ . ■

Proof of Theorem 1. The proof is nearly identical to the recursive feasibility proof of the original HMPC formulation [15, Theorem 1] since the constraints of (9) are identical to the ones of the HMPC formulation presented in [15] with the exception of constraint (9e), which instead reads as $x ^ { N } = x _ { e } + x _ { c }$ in [15]. This difference, however, is simply a time-shift which we take to simplify the notation when working with harmonic references. Thus, since the recursive feasibility does not depend on the reference, the proof of the theorem follows identically to the proof of [15, Theorem 1] but taking [15, Eq. (19b)] as $\bar { u } _ { N - 1 } ^ { + } = u _ { e } + u _ { s } \sin ( w N ) + u _ { c } \cos ( w N )$ . ■
