# APPENDIX: HCF STATE TRANSFORMATION

The mapping from $\eta ( \cdot , t )$ to $\bar { \pmb x } ( z , t )$ is obtained by applying (19) to (16), which yields (29) with kernel functions $\bar { a } _ { 0 , 1 } ^ { + } ( z , \tau ) = \bar { a } _ { 0 } ^ { + } ( \phi _ { 2 } ( z ) - \tau _ { 1 } + \tau ) , \bar { a } _ { 0 , 2 } ^ { + } ( z , \tau ) = \bar { a } _ { 0 } ^ { + } ( \phi _ { 2 } ( z ) +$ $\tau _ { 1 } + \tau ) , \bar { a } _ { 0 , 3 } ^ { + } ( z , \tau ) = \bar { a } _ { 0 } ^ { + } ( - \phi _ { 2 } ( z ) - \tau _ { 1 } + \tau ) , \bar { a } _ { 0 , 4 } ^ { + } ( z , \tau ) =$ $\bar { a } _ { 0 } ^ { + } ( - \phi _ { 2 } ( z ) + \tau _ { 1 } + \tau ) , \bar { a } _ { 0 , 1 } ^ { - } ( z , \tau ) = \bar { a } _ { 0 } ^ { - } ( \phi _ { 2 } ( z ) + \tau _ { 1 } - \tau )$ , and $\bar { a } _ { 0 . 2 } ^ { - } ( z , \tau ) = \bar { a } _ { 0 } ^ { - } ( \phi _ { 2 } ( z ) - \tau _ { 1 } - \tau )$ .

Based on this result, the inverse mapping (30) can be derived, where $b _ { 0 . 1 } ^ { + } ( \tau , \sigma ) = \bar { a } _ { 0 } ^ { + } ( \Delta \tau - \tau + \sigma ) , b _ { 0 . 2 } ^ { + } ( \tau , \sigma ) =$ $\bar { a } _ { 0 } ^ { + } ( \tau _ { 1 } + \tau _ { 2 } - \tau + \sigma ) , b _ { 0 , 1 } ^ { - } ( \tau , \sigma ) = \bar { a } _ { 0 } ^ { - } ( - \Delta \tau + \tau ^ { - } - \sigma )$ , and $b _ { 0 . 2 } ^ { - } ( \tau , \sigma ) = \bar { a } _ { 0 } ^ { - } ( - \tau _ { 1 } - \tau _ { 2 } + \tau - \sigma )$ . The mapping (30a) from $\bar { \pmb x } ( z , t )$ to the first HCF state component $\eta _ { 1 } ( \cdot , t )$ follows from evaluating (29a) for $z = \psi _ { 1 } ( \tau _ { 1 } - \tau )$ and (29c) for $z = \psi _ { 1 } ( \tau - \tau _ { 1 } )$ , respectively. The mapping (30b) for the second HCF state component is derived from (29b) and (29d) by replacing $\eta _ { 1 } ( \cdot , t )$ with $\bar { x } _ { 1 } ^ { \mp } ( \cdot , t )$ using (30a) followed by analogous substitutions for z as for (29a) and (29c).
