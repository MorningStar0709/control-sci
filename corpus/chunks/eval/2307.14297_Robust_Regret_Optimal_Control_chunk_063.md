Thus $\| \hat { M } \hat { \Delta } \| _ { 2 \to 2 } \ge 1$ implies $\| \hat { \Delta } \| _ { 2 \to 2 } \geq \| \hat { M } \| _ { 2 \to 2 } ^ { - 1 }$ . Finally, combine this with $\| \hat { M } \| _ { 2  2 } < 1$ by assumption A), to conclude that $\bar { \sigma } ( \hat { \Delta } ) \ = \ \| \hat { \Delta } \| _ { 2 \to 2 } \ > \ 1$ . Condition B) follows because max $( \bar { \sigma } ( \Delta ) , \bar { \sigma } ( \Delta _ { P } ) ) = \bar { \sigma } ( \hat { \Delta } ) > 1$ .

$B )  { } C )$ : Assume B) is true and that $\Delta$ satisfies $\bar { \sigma } ( \Delta ) \leq 1$ . We will show that $F _ { U } ( M , \Delta )$ is well-posed and $\bar { \sigma } ( F _ { U } ( M , \Delta ) ) < 1$ , i.e. C) holds.

Set $\Delta _ { P } = 0$ so that max $( \bar { \sigma } ( \Delta ) , \bar { \sigma } ( \Delta _ { P } ) ) \leq 1$ . It follows from B) that:

$$
0 \neq \det \left(I - M \left[ \begin{array}{c c} \Delta & 0 \\ 0 & 0 \end{array} \right]\right) = \det (I _ {n _ {v}} - M _ {1 1} \Delta).
$$

Hence $F _ { U } ( M , \Delta )$ is well-posed.

Next, we will show $\gamma : = \bar { \sigma } ( F _ { U } ( M , \Delta ) ) < 1$ . This holds trivially if $\gamma = 0$ so we’ll assume that $\gamma > 0$ . There exist vectors $d \in \mathbb { C } ^ { n _ { d } }$ and $e \in \mathbb { C } ^ { n _ { e } }$ such that $e = F _ { U } ( M , \Delta ) d , \parallel d \parallel _ { 2 } = 1$ , and $\| e \| _ { 2 } = \gamma$ . Define $\Delta _ { P } : = \gamma ^ { - 2 } d e ^ { * }$ so that $\Delta _ { P } e = d$ and $\bar { \sigma } ( \Delta _ { P } ) = \gamma ^ { - 1 }$ . By construction, $F _ { U } ( M , \Delta ) \Delta _ { P } e = e$ and hence $I - F _ { U } ( M , \Delta ) \Delta _ { P }$ is singular.

We have det $\left( I - M _ { 1 1 } \Delta \right) \neq 0$ by well-posedness. Hence singularity of $I - F _ { U } ( M , \Delta ) \Delta _ { P }$ and the definition of $F _ { U } ( M , \Delta )$ imply:

$$
\begin{array}{l} 0 = \det (I - M _ {1 1} \Delta) \cdot \det (I - F _ {U} (M, \Delta) \Delta_ {P}) \\ = \det (I - M _ {1 1} \Delta) \cdot \det (I - M _ {2 2} \Delta_ {P} - M _ {2 1} \Delta (I - M _ {1 1} \Delta) ^ {- 1} M _ {1 2} \Delta_ {P}). \\ \end{array}
$$

This can be rewritten, by the block matrix determinant formula, as:

$$
0 = \det \left[ \begin{array}{c c} I - M _ {1 1} \Delta & - M _ {1 2} \Delta_ {P} \\ - M _ {2 1} \Delta & I - M _ {2 2} \Delta_ {P} \end{array} \right] = \det \left(I - M \left[ \begin{array}{c c} \Delta & 0 \\ 0 & \Delta_ {P} \end{array} \right]\right).
$$

Finally, assumption B) implies that max $( \bar { \sigma } ( \Delta ) , \bar { \sigma } ( \Delta _ { P } ) ) > 1$ . Moreover, $\bar { \sigma } ( \Delta ) \leq 1$ , and $\bar { \sigma } ( \Delta _ { P } ) =$

$\gamma ^ { - 1 }$ give:
