# 6 Appendix

Since $\dot { h } = u$ (relative degree 1), the discontinuous control can be replaced by the Super-Twisting Algorithm (STA) to ensure continuous control with finite-time convergence. The STA is

$$u = - K _ {1} | h | ^ {1 / 2} \operatorname{sgn} (h) + z, \quad \dot {z} = - K _ {2} \operatorname{sgn} (h), \quad K _ {1}, K _ {2} > 0, \tag {15}$$

leading to the second-order sliding dynamics $\begin{array} { r l } { \dot { h } } & { { } = } \end{array}$ $- K _ { 1 } | \tilde { h | } ^ { 1 / 2 } \mathrm { s g n } ( h ) + z , \dot { z } = - K _ { 2 } \mathrm { s g n } \tilde { ( h ) }$ . The control is continuous since the discontinuity is only in ˙z. Finitetime convergence to $h = 0 , \dot { h } = 0$ holds if $K _ { 2 } ~ > ~ 0$ , $K _ { 1 } > 2 \sqrt { K _ { 2 } } ;$ a practical choice is $K _ { 1 } = 1 . 5 \sqrt { K _ { 2 } } .$ . The Super-Twisting Algorithm offers an attractive alternative, preserving finite-time convergence of SMC while ensuring continuous control and eliminating chattering for relative degree 1 systems.

<table><tr><td>Method</td><td>Dynamics</td><td>FT</td><td>Continuity</td><td>Chattering</td></tr><tr><td>Ideal SMC</td><td> $\dot{h} = -K \text{sgn}(h)$ </td><td>Yes</td><td>Discontinuous</td><td>High</td></tr><tr><td>Smooth SMC</td><td> $\dot{h} = -K \text{sat}(h/\epsilon)$ </td><td>No</td><td>Continuous</td><td>Low</td></tr><tr><td>STA</td><td> $\dot{h} = -K_1|h^{1/2} \text{sgn}(h)$  $\dot{z} = -K_2 \text{sgn}(h)$ </td><td>Yes</td><td>Continuous</td><td>None</td></tr></table>
