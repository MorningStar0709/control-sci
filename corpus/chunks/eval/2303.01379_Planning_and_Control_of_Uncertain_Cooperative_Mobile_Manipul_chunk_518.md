where $\mathsf { K } [ F ] ( x , z , t ) , \mathsf { K } [ G ] ( x , z , t ) , \mathsf { K } [ u ] ( x , t )$ are the Filippov regularizations (see (A.5)) of the respective terms.
For u specifically, $\mathsf { K } [ \boldsymbol { u } ] ( \boldsymbol { x } , t )$ is formed by substituting the term $s _ { v }$ with its reguralized term, which is $\begin{array} { r } { \mathsf { S } _ { v } = \frac { r _ { v } \varepsilon _ { v } } { \parallel r _ { v } \varepsilon _ { v } \parallel } } \end{array}$ if $\| r _ { v } \varepsilon _ { v } \| \neq 0$ , and $\mathsf { S } _ { v } \in ( - 1 , 1 ) ^ { n }$ otherwise.
Note that, in any case, it holds that $( r _ { v } \varepsilon _ { v } ) ^ { \top } \mathsf { S } _ { v } = \| r _ { v } \varepsilon _ { v } \|$ .
Define now $\widetilde { \boldsymbol { x } } : = [ \boldsymbol { x } ^ { \top } , \boldsymbol { z } ^ { \top } , \hat { d } ] \in \mathbb { R } ^ { 2 n + n _ { z } + 1 }$ and consider the open set $\mathcal { D } _ { c } : = \{ ( \widetilde { x } , t ) \in \mathbb { R } ^ { 2 n + n _ { z } + 1 } \times [ t _ { 0 } , \infty ) : ( x , t ) \in \mathcal { D } _ { u , t } \}$ .
Since $\rho _ { p _ { i } } ( t _ { 0 } ) > | e _ { p _ { i } } ( t _ { 0 } ) |$ and $\rho _ { v _ { i } } ( t _ { 0 } ) > | e _ { v _ { i } } ( t _ { 0 } ) | , \forall i \in \{ 1 \ldots , n \}$ , the set $\mathcal { D } _ { c }$ is nonempty.
Moreover, since $T ( \cdot )$ , and hence its derivative, are analytic, their zero sets have zero measure [374] and thus the right hand-side of (6.23) is Lebesgue measurable and locally essentially bounded in x over the set $\{ \widetilde { x } : ( \widetilde { x } , t ) \in \mathcal { D } _ { c } \}$ , and Lebesgue measurable in t over the set $\{ t : ( \widetilde { { \boldsymbol { x } } } , t ) \in \mathcal { D } _ { c } \}$ .
e e eHence, according to Prop.
A.1 of Appendix A, for each initial condition $( \widetilde { x } ( t _ { 0 } ) , t _ { 0 } ) \in \mathcal { D } _ { c }$ , there exists at least one Filippov solution $\widetilde { x } ( t )$ of (6.23), edefined in $I _ { t } : = [ t _ { 0 } , t _ { \mathrm { m a x } } )$ , where $t _ { \mathrm { m a x } } > t _ { 0 }$ such that $( \widetilde { { \boldsymbol x } } ( t ) , t ) \in { \mathcal { D } } _ { c } , \forall t \in I _ { t }$ .
eBy applying (6.16), we conclude the existence of the respective Filippov solutions $\varepsilon _ { p } ( t ) , \varepsilon _ { v } ( t ) \in \mathbb R ^ { n } , \forall t \in I _ { t }$ .
Let now $\widetilde { \boldsymbol { x } } ( t _ { 0 } )$ denote the initial condition of the system (6.23) satisfying $( \widetilde { x } ( t _ { 0 } ) , t _ { 0 } ) \in \mathcal { D } _ { c }$ and consider the family of eFilippov solutions starting from $\widetilde { \boldsymbol { x } } ( t _ { 0 } )$ denoted by the set $\mathfrak { X }$ .
