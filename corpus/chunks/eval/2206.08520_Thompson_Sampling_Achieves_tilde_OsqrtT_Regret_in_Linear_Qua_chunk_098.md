$$
\begin{array}{l} R _ {k} ^ {T S, 2} \leq \frac {1}{p _ {t _ {k}} ^ {\mathrm{opt}}} \mathbb {E} \left[ \left| J _ {*} (\tilde {\Theta} _ {t _ {k}} ^ {\prime}, \sigma_ {w} ^ {2} I) - \mathbb {E} \left[ J _ {*} (\tilde {\Theta} _ {t _ {k}}, \sigma_ {w} ^ {2} I) \mid \mathcal {F} _ {t _ {k}} ^ {\mathrm{cnt}}, E _ {t _ {k}} \right] \right| \mid \mathcal {F} _ {t _ {k}} ^ {\mathrm{cnt}}, E _ {t _ {k}} \right] \\ = \frac {\sigma_ {w} ^ {2}}{p _ {t _ {k}} ^ {\mathrm{opt}}} \mathbb {E} \left[ \left| \operatorname{tr} \left(P (\tilde {\Theta} _ {t _ {k}} ^ {\prime}) - \mathbb {E} \left[ P (\tilde {\Theta} _ {t _ {k}}) \mid \mathcal {F} _ {t _ {k}} ^ {\mathrm{cnt}}, E _ {t _ {k}} \right]\right) \right| \mid \mathcal {F} _ {t _ {k}} ^ {\mathrm{cnt}}, E _ {t _ {k}} \right] \\ \leq \frac {n \sigma_ {w} ^ {2}}{p _ {t _ {k}} ^ {\text { opt }}} \mathbb {E} \left[ \left\| P (\tilde {\Theta} _ {t _ {k}} ^ {\prime}) - \mathbb {E} \left[ P (\tilde {\Theta} _ {t _ {k}}) \mid \mathcal {F} _ {t _ {k}} ^ {\text { cnt }}, E _ {t _ {k}} \right] \right\| _ {2} \mid \mathcal {F} _ {t _ {k}} ^ {\text { cnt }}, E _ {t _ {k}} \right] \tag {130} \\ \end{array}
$$

where we used the relation $| \mathrm { t r } ( A ) | \leq n \| A \| _ { 2 }$ . Denoting $P _ { k } : = \mathbb { E } \lceil P ( \tilde { \Theta } _ { t _ { k } } ) \rceil \mathcal { F } _ { t _ { k } } ^ { \mathrm { c n t } } , E _ { t _ { k } } \rceil$ , the following definition will be used in the rest of the section to understand the behavior of $R _ { k } ^ { T S , 2 }$

$$\Delta_ {k} := \mathbb {E} \left[ \| P (\tilde {\Theta} _ {t _ {k}}) - P _ {k} \| _ {2} \mid \mathcal {F} _ {t _ {k}} ^ {\mathrm{cnt}}, E _ {t _ {k}} \right] \tag {131}$$

The following lemma will be used to bound $\Delta _ { k }$ from above.

Lemma 23 For any $\Theta \in { \cal S } ,$ , any positive definite matrix $V \in \mathbb { R } ^ { ( n + d ) \times ( n + d ) }$ , and for any $i , j \in [ n ]$

$$\| \nabla P _ {i j} (\Theta) \| _ {V} \leq \Gamma \| H (\Theta) \| _ {V},$$

where $\Gamma \geq 0$ is a problem dependent constant.

Proof Let $\delta P ( \Theta , \delta \Theta )$ be the differential of $P ( \Theta )$ in the direction δΘ. Then, we have that

$$
\begin{array}{l} \delta P (\Theta , \delta \Theta) = A _ {c} (\Theta) ^ {\intercal} \delta P (\Theta , \delta \Theta) A _ {c} (\Theta) \tag {132} \\ + A _ {c} (\Theta) ^ {\intercal} P (\Theta) \delta \Theta^ {\intercal} H (\Theta) + H (\Theta) ^ {\intercal} \delta \Theta P (\Theta) A _ {c} (\Theta) \\ \end{array}
$$
