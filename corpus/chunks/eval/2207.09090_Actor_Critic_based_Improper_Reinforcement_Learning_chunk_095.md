where (i) is by taking an extra expectation without changing the inner summand, (ii) follows by Lemma J.1 and Lemma 5 in (Xu et al., 2020), (iii) follows by the value difference lemma (Lemma I.4), (iv) follows by defining $\begin{array} { r } { \left. \frac { \nu _ { \pi ^ { * } } } { \nu _ { \pi _ { t } } } \right. _ { \infty } : = } \end{array}$ νπθt maxs,m νπ∗ (s,m)νπθt (s,m) , (v) follows because νπθt (s, m) > (1 − γ)νπθ0 (s, m), (vi) follows by Lemma6 in (Xu et al., 2020)and $\begin{array} { r } { \operatorname* { m a x } _ { s , m } \frac { \nu _ { \pi ^ { * } } ( s , m ) } { \nu _ { \pi _ { \theta _ { t } } } ( s , m ) } } \end{array}$ $\nu _ { \pi _ { \theta _ { t } } } ( s , m ) \geqslant ( 1 - \gamma ) \nu _ { \pi _ { \theta _ { 0 } } } ( s , m )$

Lemma J.2.

Next, we denote $\begin{array} { r } { \Delta _ { a c t o r } : = \operatorname* { m a x } _ { \theta \in \mathbb { R } ^ { M } } \operatorname* { m i n } _ { w \in \mathbb { R } ^ { d } } \mathbb { E } _ { \nu _ { \pi _ { \theta } } } [ [ \psi _ { \theta } ^ { \top } w - A _ { \pi _ { \theta } } ( s , m ) ] ^ { 2 } ] } \end{array}$ as the actor error.

$$
\begin{array}{l} \mathrm{D} (\theta_ {\mathrm{t}}) - \mathrm{D} (\theta_ {\mathrm{t+1}}) \\ \geqslant (1 - \gamma) (V (\pi^ {*}) - V (\pi_ {\theta_ {t}})) - \sqrt {\frac {1}{1 - \gamma} \left\| \frac {\nu_ {\pi^ {*}}}{\nu_ {\pi_ {\theta_ {0}}}} \right\| _ {\infty}} \alpha \sqrt {\Delta_ {a c t o r}} - \alpha C _ {s o f t} \lambda \\ - 2 \alpha \left\| u _ {t} (w _ {t}) - u _ {\theta_ {t}} ^ {\lambda} \right\| _ {2} - \frac {\alpha^ {2} \left\| u _ {t} (w _ {t}) \right\| _ {2} ^ {2}}{2} \\ \geqslant (1 - \gamma) (V (\pi^ {*}) - V (\pi_ {\theta_ {t}})) - \sqrt {\frac {1}{1 - \gamma} \left\| \frac {\nu_ {\pi^ {*}}}{\nu_ {\pi_ {\theta_ {0}}}} \right\| _ {\infty}} \alpha \sqrt {\Delta_ {a c t o r}} - \alpha C _ {s o f t} \lambda \\ - 2 \alpha \left\| u _ {t} (w _ {t}) - u _ {\theta_ {t}} ^ {\lambda} \right\| _ {2} - \frac {\alpha^ {2} \left\| u _ {t} (w _ {t}) - u ^ {\lambda} (\theta_ {t}) \right\| _ {2} ^ {2}}{2} - \frac {\alpha^ {2}}{\lambda^ {2}} \left\| \nabla_ {\theta} V (\theta_ {t}) \right\| _ {2} ^ {2}. \\ \end{array}
$$

Rearranging and dividing by $( 1 - \gamma ) \alpha ,$ , and taking expectation both sides we get
