where we recall that $x _ { 0 } = 0$ , and $r \in \{ 1 , 4 , - 2 \}$ and $a = 1 / 5$ . It follows that for any $t \in \{ 0 , \ldots , T - 1 \}$

$$
\begin{array}{l} | x _ {t} | \leq \max _ {0 \leq t \leq T - 1} | \tilde {w} _ {t} | \sum_ {s = 0} ^ {t - 1} 4 \left(\frac {1}{5}\right) ^ {t - (s + 1)} \\ \leq 5 \max _ {0 \leq t \leq T - 1} | \tilde {w} _ {t} |, \\ \end{array}
$$

which also implies that

$$x _ {t} ^ {2} \leq 2 5 \max _ {0 \leq t \leq T - 1} | \tilde {w} _ {t} | ^ {2}. \tag {D.13}$$

By the definition of the event $\mathcal { E } _ { w }$ defined in (25), we know that under the event $\mathcal { E } _ { w }$

$$| \tilde {w} _ {t} | \leq \sqrt {5 \sigma_ {w} \log \frac {3 T}{\delta}}, \forall t \in \{0, \dots , T - 1 \}.$$

It follows from (D.13) that

$$x _ {t} ^ {2} \leq 1 2 5 \sigma_ {w} \log \frac {3 T}{\delta}, \forall t \in \{0, \dots , T - 1 \}.$$

From (D.13), we also get that

$$\mathbb {E} \left[ x _ {t} ^ {2} \right] \leq 2 5 \mathbb {E} \left[ \max _ {0 \leq t \leq T - 1} | \tilde {w} _ {t} | ^ {2} \right],$$

which implies via Lemma E.2 that

$$\mathbb {E} \left[ x _ {t} ^ {2} \right] \leq 1 2 5 \sigma_ {w} (1 + \log T), \forall t \in \{0, \dots , T - 1 \}.$$

Now, considering the term in $\mathbb { E } [ \mathbb { R } _ { \mathbf { x } } ( T ) ] - \mathbb { R } _ { \mathbf { x } } ( T )$ corresponding to any $t \in \{ 0 , \ldots , T - 1 \}$ as per (D.3), we have

$$
\begin{array}{l} \left| \mathbb {E} \left[ (\hat {x} _ {t} - x _ {t}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}} - x _ {t}) ^ {2} \right] - \left((\hat {x} _ {t} - x _ {t}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}} - x _ {t}) ^ {2}\right) \right| \\ \leq 2 \mathbb {E} \left[ \hat {x} _ {t} ^ {2} + x _ {t} ^ {2} \right] + 2 \left(\hat {x} ^ {\mathrm{KF}}\right) ^ {2} + 2 x _ {t} ^ {2} \\ \leq 2 c _ {\mathcal {A}} + 2 \mathbb {E} \left[ x _ {t} ^ {2} \right] + 2 \left(\hat {x} _ {t} ^ {\mathrm{KF}}\right) ^ {2} + 2 x _ {t} ^ {2}. \tag {D.14} \\ \end{array}
$$

From Lemma C.1 used in the proof of Lemma 4.6, one can show that $| \hat { x } _ { t } ^ { \mathrm { K F } } | = \mathcal { O } ( \sqrt { \log T } t )$ for all $t \in \{ 0 , \ldots , T - 1 \}$ , under the event $\mathcal { E } _ { w } \cap \mathcal { E } _ { v }$ , where $\mathcal { E } _ { w }$ and $\mathcal { E } _ { v }$ are given by (25) defined for system (D.1).
