Recalling that $R _ { t } ^ { \mathrm { s } ( 1 ) } = \mathbb { E } _ { w , v } \big [ ( \hat { x } _ { t } ^ { \mathrm { s } ( \mathrm { i } ) } - x _ { t } ^ { \mathrm { s } ( \mathrm { i } ) } ) ^ { 2 } - ( \hat { x } _ { t } ^ { \mathrm { K F } , \mathrm { s } ( \mathrm { i } ) } - x _ { t } ^ { \mathrm { s } ( \mathrm { i } ) } ) ^ { 2 } \big ]$ (i) − xs(i)t )2 − (xˆKF,s(i)t − for all $t \geq 0$ , we know by the optimality of the Kalman filter (i.e., (5)) that $R _ { t } ^ { \mathbf { s } ( 1 ) } \geq 0$ . Going back to (D.4), we now get that for any $t \geq 1$ ,

$$
\begin{array}{l} \mathbb {E} \big [ (\hat {x} _ {t} - x _ {t}) ^ {2} - (\hat {x} _ {t} ^ {\mathrm{KF}} - x _ {t}) ^ {2} \big ] = \frac {1}{3} \big (R _ {t} ^ {\mathbf {s} (2)} - R _ {t} ^ {\mathbf {s} (1)} + R _ {t} ^ {\mathbf {s} (3)} - R _ {t} ^ {\mathbf {s} (1)} + 3 R _ {t} ^ {\mathbf {s} (1)} \big) \\ \geq 6 \sigma_ {w} - \frac {5 6 + 5 0 \sigma_ {w}}{1 8 7 5 \sigma_ {w} T}. \tag {D.12} \\ \end{array}
$$

Finally, noticing that the initial values satisfy $\hat { x } _ { 0 } = \hat { x } _ { t } ^ { \mathbf { s ( i ) } } = x _ { 0 } = 0$ and summing (D.12) over all $t \in \{ 1 , \ldots , T - 1 \}$ , we complete the proof of (36) in the theorem.

Notice that we have proved (36) holds for any algorithm that only has access to $y _ { 0 } , \ldots , y _ { t - 1 }$ when designing $\hat { x } _ { t }$ for all $t \geq 0 ,$ , and (36) holds even when the algorithm is applied to the class of stable scalar LTI systems given by (D.1). Thus, to prove (37), we consider any algorithm that returns $\hat { x } _ { t }$ such that $\| \hat { x } _ { t } \| \le c _ { A }$ for all $t \geq 0$ , and also apply the algorithm to the class of systems in (D.1). Moreover, we see that to prove (37), it suffices for us to lower bound $\mathtt { R _ { x } } ( T ) - \mathbb { E } [ \mathtt { R _ { x } } ( T ) ]$ (i.e., upper bound $\mathbb { E } [ \mathrm { R } _ { \mathbf { x } } ( T ) ] - \mathrm { R } _ { \mathbf { x } } ( T ) )$ with a high probability. Towards this end, we first prove the following result.

Lemma D.4. Consider system (D.1), any $T \geq 3$ and any $\delta \in ( 0 , 1 )$ . Then, under the event $\mathcal { E } _ { w }$ defined in (25) for system (D.1), it holds that

$$\left| x _ {t} \right| ^ {2} \leq 1 2 5 \sigma_ {w} \log \frac {T}{\delta}, \forall t \in \{0, \dots , T - 1 \}.$$

In addition, it holds that

$$\mathbb {E} \left[ | x _ {t} | ^ {2} \right] \leq 1 2 5 \sigma_ {w} (1 + \log T), \forall t \in \{0, \dots , T - 1 \}.$$

Proof. For any $t \geq 0$ , we derive from (D.1) that

$$
\begin{array}{l} x _ {t} = a ^ {t} x _ {0} + \sum_ {s = 0} ^ {t - 1} a ^ {t - (s + 1)} w _ {t} \\ = \sum_ {s = 0} ^ {t - 1} a ^ {t - (s + 1)} r \tilde {w} _ {t}, \\ \end{array}
$$
