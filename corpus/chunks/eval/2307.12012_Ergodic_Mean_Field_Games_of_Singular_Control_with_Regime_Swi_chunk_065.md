$$\mathbb {E} _ {(x, i)} \big [ | \xi_ {T} ^ {*} (\theta) | \big ] \leq \mathbb {E} _ {(x, i)} \big [ \psi (X _ {T} ^ {\xi^ {*} (\theta)}, Y _ {T}) \big ] + \psi (x, i) < \infty , \text { for any } T < \infty . \tag {B.10}$$

Combining (B.8) and (B.10) we conclude that $\xi ^ { * } \in \mathcal { A } _ { e }$ . Finally, defining

$$\widehat {\xi} _ {t} ^ {+} (\theta) := \sup _ {0 \leq s \leq t} \left(\alpha_ {Y _ {s}} (\theta) - I (X ^ {\widehat {\xi} (\theta)}) _ {s} + \widehat {\xi} _ {s} ^ {-} (\theta)\right) ^ {+} \text { and } \widehat {\xi} _ {t} ^ {-} (\theta) := \sup _ {0 \leq s \leq t} \left(I (X ^ {\widehat {\xi} (\theta)}) _ {s} + \widehat {\xi} _ {s} ^ {+} (\theta) - \beta_ {Y _ {s}} (\theta)\right) ^ {+}$$

it is easy to see that $\widehat \xi ( \theta ) : = \widehat \xi ^ { + } ( \theta ) - \widehat \xi ^ { - } ( \theta )$ satisfies the properties in Definition 3.1. Since, however, the solution to $\mathbf { S P } ( \alpha ( \theta ) , \beta ( \theta ) , x , i )$ is unique, it follows that $\xi ^ { * , + } \equiv { \widehat { \xi } } ^ { + }$ and $\xi ^ { * , - } \equiv { \widehat { \xi } } ^ { - }$ . 
