In view of (63), for any $z \in \mathbb { R } ^ { 2 n } , z ^ { \top } \Psi ( \sigma ) z < 0$ implies $\langle \nabla V ( x ) , ( A + B K ) x + \bar { B } K e \rangle = z ^ { \top } M z < - \varsigma \rho _ { 1 } x ^ { \top } \bar { S x } =$ “ $- \varsigma \rho _ { 1 } V ( x )$ . This implies that for any solution x to $( 7 ) , ( 8 )$ , (61), (62), for any $t \in [ t _ { k } , t _ { k + 1 } ]$ , as long $\mathrm { a s } z ( t ) ^ { \top } \Psi ( \sigma ) z ( t ) < 0$ (which is the case right after a transmission whenever $x ( 0 ) \neq$ 0), we have $\dot { V } ( x ( t ) ) < - \varsigma \rho _ { 1 } V ( x ( t ) )$ and thus $V ( x ( t ) ) ~ <$ $\eta ( t )$ in view of (61) and (62). Hence, as the time it takes for $z ^ { \top } \Psi ( { \boldsymbol { \sigma } } ) z$ to grow from $- \sigma ^ { 2 } | x |$ to 0 along (7), (8) is lower bounded by $\tau ( \sigma )$ in view of item (a) of Theorem 2, the time it takes for $V ( x )$ to reach η, which is the inter-transmission time generated by (62), is bigger than the former and is thus also lower bounded by $\tau ( \sigma )$ . Item (a) of Proposition 5 is then satisfied as when $x ( 0 ) = 0$ no transmissions ever occur.

The proof of item (b) follows by considering Lyapunov function $U ( x , \eta ) ~ = ~ \operatorname* { m a x } \{ V ( x ) , \eta \}$ for any $\boldsymbol { x } ^ { \mathrm { ~ ~ } } \in ~ \mathbb { R } ^ { n }$ and $\eta \geqslant 0 ,$ , and exploiting the fact that η upper-bounds V along the solutions to (7), (8), (61), (62) and strictly decreases outside the origin. 

We notice that the same type of stability property is stated in items (b) of Propositions 3 and 5.

In the noisy case, the ISS Lyapunov function is given by $V ( x ) = x ^ { \top } \dot { S x }$ for any $x \in \mathbb { R } ^ { n }$ with $S = ( X _ { 0 } Y ) ^ { - { \bar { 1 } } }$ and Y a solution to (24), assuming it exists, see Section IV-B. We modify the dynamics of threshold η in (61) as

$$\dot {\eta} (t) = - \varsigma_ {d} \eta (t) + \nu , \quad \eta (0) \geqslant V (x (0)) \tag {64}$$

where $\varsigma _ { d } > 0$ and $\nu \geqslant 0$ are both arbitrary. We emphasize that the decay rate of η, namely ςd, can take any value in $\mathbb { R } _ { > 0 } ,$ which was not the case in (61); we elaborate more on this point in Remark 10 below. We then “robustify” the triggering rule (62) as: $t _ { 0 } = 0$ and

$$t _ {k + 1} = \inf \left\{t \in \mathbb {R}: t \geqslant t _ {k} + \tau_ {d} (\sigma) \text { and } V (x (t)) \geqslant \eta (t) \right\}, \tag {65}$$
