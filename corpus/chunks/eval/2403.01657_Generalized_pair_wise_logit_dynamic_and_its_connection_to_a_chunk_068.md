$$\frac {\partial \hat {\Phi} (t , x _ {i - 1 / 2})}{\partial t}= \frac {1}{N} \sum_ {j = 1} ^ {N} \frac {\hat {\Phi} (t , x _ {i - 1 / 2}) - \hat {\Phi} (t , x _ {j - 1 / 2})}{1 + e _ {\kappa} \left(\frac {\hat {\Phi} (t , x _ {i - 1 / 2}) - \hat {\Phi} (t , x _ {j - 1 / 2})}{\eta}\right)}+ \frac {1}{N} \sum_ {j = 1} ^ {N} F \left(\frac {1}{1 + e _ {\kappa} \left(\frac {\hat {\Phi} (t , x _ {i - 1 / 2}) - \hat {\Phi} (t , x _ {j - 1 / 2})}{\eta}\right)}\right) - U (x _ {i - 1 / 2}, \hat {\mu} (t, \cdot)) \tag {122}\left(= \Xi (i, \hat {\mu}, \hat {\Phi})\right)$$

The semi-discretized system containing (120) and (122) are discretized in time using the classical forward Euler discretization. If we introduce the discrete time step  kt = $t _ { k } = \frac { k } { M } T$ with $M \in \mathbb { N }$ $( k = 0 , 1 , 2 , . . . , M )$ and $\Delta t = \frac { 1 } { M } T$ , by using the superscript $\ " ( k ) \ "$ to represent the value at time $t _ { k }$ , we set M the full discretization:

$$\hat {\mu} ^ {(k)} \left(\Omega_ {i}\right) = \hat {\mu} ^ {(k - 1)} \left(\Omega_ {i}\right) + \Lambda (i, \hat {\mu} ^ {(k - 1)}, \hat {\Phi} ^ {(k - 1)}) \Delta t, i = 1, 2, \dots , N \text { and } k = 1, 2, 3, \dots , M, \tag {123}$$

and

$$\hat {\Phi} ^ {(k - 1)} \left(x _ {i - 1 / 2}\right) = \hat {\Phi} ^ {(k)} \left(x _ {i - 1 / 2}\right) - \Xi \left(i, \hat {\mu} ^ {(k)}, \hat {\Phi} ^ {(k)}\right) \Delta t, i = 1, 2,..., N \text { and } k = 1, 2, 3,..., M. \tag {124}$$

We also need to discretize the initial condition of we use the uniform distribution ( in  ). The system (123)–(124) couples forward and backward equations and is solved using Algorithm C1 (e.g., Barker et al., 2022). Here, the superscript “ ” represents the iteration step, and $\varepsilon \big ( = 1 0 ^ { - 1 0 } \big )$ is the error tolerance. The initial guesses of Algorithm C1 are $\hat { \mu } _ { ( 0 ) } ^ { ( \cdot ) } \equiv 0$ and $\hat { \Phi } _ { ( 0 ) } ^ { ( \cdot ) } \equiv 0$ .Thealgoitmasaueiddmngfact $\omega \in \left( 0 , 1 \right)$
