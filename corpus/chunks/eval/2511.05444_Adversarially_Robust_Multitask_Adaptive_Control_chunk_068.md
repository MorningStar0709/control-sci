$$
\left\| x _ {t} ^ {(i)} \right\| \leq 4 0 (P _ {0} ^ {\vee}) ^ {2} \Psi_ {B} ^ {\vee} \max _ {0 \leq t \leq T - 1} \left\| \left[ \begin{array}{c} w _ {t} ^ {(i)} \\ g _ {t} ^ {(i)} \end{array} \right] \right\|, \forall t = \{0, 1, \dots , \tau_ {1} \}, \tag {27}
$$

where we use (26) to obtain

$$\left\| x _ {t} ^ {(i)} \right\| ^ {2} \leq 7 6 8 0 0 (P _ {0} ^ {\vee}) ^ {4} (\Psi_ {B} ^ {\vee}) ^ {2} \sigma_ {w} ^ {2} (d _ {x} + d _ {u}) \log (2 M T), \forall t = \{0, 1, \dots , \tau_ {1} \}$$

with probability $1 - { \textstyle { \frac { 1 } { 2 } } } T ^ { - 2 }$ , for all systems $i \in [ M ]$ , which implies that $\left\| x _ { t } ^ { ( i ) } \right\| ^ { 2 } \leq x _ { b } ^ { 2 }$ log T which satisfies the state norm requirement to not abort as described in Algorithm 1. On the other hand, to verify that the controller norm requirement is satisfied we note that $\| K _ { 0 } ^ { ( i ) } \| ^ { 2 } \le P _ { 0 } ^ { \vee } \le 2 P _ { 0 } ^ { \vee }$ , which leads to $\lVert K _ { 0 } ^ { ( i ) } \rVert \le K _ { b }$ . Therefore, we have that $\mathcal { E } _ { \mathrm { b o u n d } }$ holds with probability $1 - { \textstyle { \frac { 1 } { 2 } } } T ^ { - 2 }$ .

We now proceed to control the estimation error at the first epoch. Note that by making $\tau _ { 1 }$ sufficiently large such that $\tau _ { 1 } \geq c ( 1 + \log ( 4 T ^ { 2 } ) )$ , for a sufficiently large constant c, we can guarantee the following upper bound for the model estimation:

$$
\left\| \left[ \begin{array}{c c} \hat {A} _ {1} ^ {(i)} & \hat {B} _ {1} ^ {(i)} \end{array} \right] - \left[ \begin{array}{c c} A _ {\star} ^ {(i)} & B _ {\star} ^ {(i)} \end{array} \right] \right\| _ {F} ^ {2} \leq \frac {C _ {\mathrm{stat}} \sigma_ {w} ^ {2} (d _ {x} ^ {2} + d _ {x} d _ {u}) \log (2 T ^ {2})}{\sigma_ {1} ^ {2} M _ {j} \tau_ {1}} + C _ {\mathrm{mis,1}} \exp \left(- C _ {\mathrm{mis,2}} \sigma_ {1} ^ {2} \tau_ {1}\right)
$$

for any system $i \in C _ { j }$ as discussed in Lemma E.1. Our induction step follows by considering the following inductive hypothesis:

$$
\text { Bounded   state: } \left\| x _ {\tau_ {k}} ^ {(i)} \right\| \leq 1 6 (P _ {0} ^ {\vee}) ^ {3 / 2} \Psi_ {B} ^ {\vee} \max _ {0 \leq t \leq T - 1} \left\| \left[ \begin{array}{c} w _ {t} ^ {(i)} \\ g _ {t} ^ {(i)} \end{array} \right] \right\|, \tag {28}
$$

and
