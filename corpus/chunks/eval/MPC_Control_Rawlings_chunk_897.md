# Exercise A.14: Directional derivative

Suppose that $f : \mathbb { R } ^ { n }  \mathbb { R } ^ { m }$ has a derivative $f _ { x } ( { \hat { x } } )$ at xˆ. Show that for any h, the directional derivative $d f ( { \hat { x } } ; h )$ exists and is given by

$$d f (\hat {x}; h) = f _ {x} (\hat {x}) h = (\partial f (x) / \partial x) h.$$
