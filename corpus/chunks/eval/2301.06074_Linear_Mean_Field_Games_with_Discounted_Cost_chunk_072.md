<table><tr><td>Player 1</td><td>Player 2</td></tr><tr><td>Given  $\mu$ : minimize $_{\zeta \in \mathcal{M}_{+}(\mathsf{X} \times \mathsf{A})}$   $\langle \zeta, c_{\mu} \rangle$ subject to  $\hat{\zeta} \geq (1 - \beta)\mu + \beta \zeta p_{\mu}$ </td><td>Given  $\zeta$ : minimize $_{\mu \in \mathcal{M}_{+}(\mathsf{X})}$   $g(\zeta, \mu)$ subject to  $\mu \geq \zeta p_{\mu},$   $\langle \mu, \mathbf{1} \rangle \geq 1$ </td></tr></table>

Here, 1 denotes the constant function 1. To express the GNEP problem using inequality constraints, an additional constraint $\langle \mu , \mathbf { 1 } \rangle \geq 1$ is added without the need to increase the number of constraints significantly. While the following result is similar to Lemma 5.2, its proof is not straightforward and will be provided.

LEMMA 5.3. $I f \left( \zeta ^ { * } , \mu ^ { * } \right)$ is an equilibrium solution of the above GNEP with inequality constraint, then $( \mu ^ { * } , \pi ^ { * } )$ is MFE, where $\zeta ^ { * } ( d x , d a ) = \pi ^ { * } ( d a | x ) \hat { \zeta } ^ { * } ( d x )$ .

PROOF. Since

$$\hat {\zeta} ^ {*} \geq (1 - \beta) \mu^ {*} + \beta \zeta^ {*} p _ {\mu^ {*}}$$

we have $\varsigma ^ { * } ( \mathsf { X } \times \mathsf { A } ) \geq \left( 1 - \beta \right) \mu ^ { * } ( \mathsf { X } ) + \beta \zeta ^ { * } ( \mathsf { X } \times \mathsf { A } ) \mu ^ { * } ( \mathsf { X } )$ . Similarly, since

$$\mu^ {*} \geq \zeta^ {*} p _ {\mu^ {*}}$$

we have $\mu ^ { * } ( \mathsf { X } ) \geq \zeta ^ { * } ( \mathsf { X } \times \mathsf { A } ) \mu ^ { * } ( \mathsf { X } )$ . Hence

$$\zeta^ {*} (\mathrm{X} \times \mathrm{A}) \geq (1 - \beta) \zeta^ {*} (\mathrm{X} \times \mathrm{A}) \mu^ {*} (\mathrm{X}) + \beta \zeta^ {*} (\mathrm{X} \times \mathrm{A}) \mu^ {*} (\mathrm{X}) = \zeta^ {*} (\mathrm{X} \times \mathrm{A}) \mu^ {*} (\mathrm{X})$$

and

$$\mu^ {*} (\mathsf {X}) \geq \zeta^ {*} (\mathsf {X} \times \mathsf {A}) \mu^ {*} (\mathsf {X})$$

Therefore, $\mu ^ { * } ( \mathsf { X } ) \leq 1$ and $\zeta ^ { * } ( \mathsf { X } \times \mathsf { A } ) \leq 1$ . Since $\langle \mu ^ { * } , \mathbf { 1 } \rangle = \mu ^ { * } ( \mathsf { X } ) \geq 1$ , we also have $\mu ^ { * } ( \mathsf { X } ) = 1$ and

$$\zeta^ {*} (\mathsf {X} \times \mathsf {A}) \geq (1 - \beta) + \beta \zeta^ {*} (\mathsf {X} \times \mathsf {A})$$
