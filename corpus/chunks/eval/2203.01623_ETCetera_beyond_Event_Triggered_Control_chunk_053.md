# Now create an abstraction to design an improved 
    → sampling strategy
traffic_synth = abstr.TrafficModelLinearPETC(trigger,
    → depth=1, etc_only=False, solver='z3',
    → early_trigger_only=True)
strat, strat_tran = traffic_synth.
    → optimize_sampling_strategy()
print(strat) # Inspect strategy
