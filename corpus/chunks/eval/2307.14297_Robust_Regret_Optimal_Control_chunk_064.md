$$\gamma^ {- 1} = \max \left(\bar {\sigma} (\Delta), \bar {\sigma} (\Delta_ {P})\right) > 1.$$

Hence $\bar { \sigma } ( F _ { U } ( M , \Delta ) ) = \gamma < 1$ .

Next, consider the uncertain interconnection in Figure 15 where M and $\Delta$ are discrete-time systems. Assume M has the following state-space representation:

$$
\left[ \begin{array}{c} x _ {t + 1} \\ v _ {t} \\ e _ {t} \end{array} \right] = \left[ \begin{array}{c c c} A & B _ {w} & B _ {d} \\ C _ {v} & D _ {v w} & D _ {v d} \\ C _ {e} & D _ {e w} & D _ {e d} \end{array} \right] \left[ \begin{array}{c} x _ {t} \\ w _ {t} \\ d _ {t} \end{array} \right]. \tag {50}
$$

Moreover, assume $\Delta$ has the state-space representation:

$$
\left[ \begin{array}{l} x _ {t + 1} ^ {\Delta} \\ w _ {t} \end{array} \right] = \left[ \begin{array}{l l} A _ {\Delta} & B _ {\Delta} \\ C _ {\Delta} & D _ {\Delta} \end{array} \right] \left[ \begin{array}{l} x _ {t} ^ {\Delta} \\ v _ {t} \end{array} \right]. \tag {51}
$$

Combine the $v _ { t }$ and $w _ { t }$ output equations of (50) and (51):

$$
\left[ \begin{array}{c c} I _ {n _ {v}} & - D _ {v w} \\ - D _ {\Delta} & I _ {n _ {w}} \end{array} \right] \left[ \begin{array}{l} v _ {t} \\ w _ {t} \end{array} \right] = \left[ \begin{array}{c c} C _ {v} & 0 \\ 0 & C _ {\Delta} \end{array} \right] \left[ \begin{array}{l} x _ {t} \\ x _ {t} ^ {\Delta} \end{array} \right] + \left[ \begin{array}{c} D _ {v d} \\ 0 \end{array} \right] d _ {t}. \tag {52}
$$

We say that (system) interconnection $F _ { U } ( M , \Delta )$ is well-posed if $\left[ \begin{array} { l l } { I _ { n _ { v } } } & { - D _ { v w } } \\ { - D _ { \Delta } } & { I _ { n _ { w } } } \end{array} \right]$ −Dvw is nonsingular. This condition is equivalent to nonsingularity of $I _ { n _ { v } } - D _ { v w } D _ { \Delta }$ . Hence if the system is wellposed then (52) has a unique solution $( v _ { t } , w _ { t } )$ for any $( x _ { t } , x _ { t } ^ { \Delta } , d _ { t } )$ . In addition, $F _ { U } ( M , \Delta )$ has a well-defined state-space representation for the dynamics from d to e:

$$
\left[ \begin{array}{c} x _ {t + 1} \\ x _ {t + 1} ^ {\Delta} \end{array} \right] = \tilde {A} \left[ \begin{array}{c} x _ {t} \\ x _ {t} ^ {\Delta} \end{array} \right] + \tilde {B} d _ {t}

e _ {t} = \tilde {C} \left[ \begin{array}{l} x _ {t} \\ x _ {t} ^ {\Delta} \end{array} \right] + \tilde {D} d _ {t},
$$

where:
