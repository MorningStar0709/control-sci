# B. Proof of Lemma 3

Lemma 3 is a direct consequence of the next result.

Lemma 6: Let $B \in \mathbb { R } ^ { n \times p }$ and $C \in \mathbb { R } ^ { q \times n }$ be given matrices. Then for any $\epsilon > 0$ and any $D \in \mathcal { D }$ with D in (22), we have $B D ^ { \top } C + \dot { C } ^ { \top } D B ^ { \top } \preceq \epsilon ^ { - 1 } \dot { B } B ^ { \top } + \epsilon C ^ { \top } \Delta \Delta ^ { \top } C .$ . Proof. A completion of squares $( \sqrt { \epsilon ^ { - 1 } } B - \sqrt { \epsilon } C ^ { \top } D ) ( \sqrt { \epsilon ^ { - 1 } } B -$ $\sqrt { \epsilon } C ^ { \top } D ) ^ { \top } \succeq 0$ gives the result.

Proof of Lemma 3. Let (24a) hold. By a Schur complement, this is equivalent to $X _ { 1 } Y + ( X _ { 1 } Y ) ^ { \top } + \bar { \Omega } + \epsilon ^ { - 1 } Y ^ { \top } Y + \bar { \epsilon } \Delta \Delta ^ { \top } \prec$ 0. By applying Lemma 6 with $B = - Y ^ { \top }$ and $C = I .$ , we obtain $\bar { X _ { 1 } Y } + ( X _ { 1 } Y ) ^ { \top } + \Omega - D Y - ( D Y ) ^ { \top } \prec 0 \quad \forall D \in \mathcal { D }$ , which corresponds to (23). 
