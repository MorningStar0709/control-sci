Proof. Consider the dynamics in Equation 14 and define $\hat { Q } : = \hat { C } ^ { \top } \hat { C } , \hat { S } : = \hat { C } ^ { \top } \hat { D } = 0$ , and $\hat { R } = \hat { D } ^ { \top } \hat { D }$ . Note that $\hat { S } = 0$ and $\hat { R } = \gamma _ { d } ^ { 2 } I$ . These matrices satisfy the following:

(i) $\hat { R } = \gamma _ { d } ^ { 2 } I \succ 0$ by the assumption that $\gamma _ { d } > 0$

(ii) $( { \hat { A } } , { \hat { B } } )$ is stabilizable because $( \hat { A } _ { 1 1 } ^ { - \top } , - X B _ { d } )$ is stabilizable (by assumption) and $\hat { A } _ { 1 1 }$ is a Schur matrix.

$( \mathrm { v } , \mathrm { v i } )$ Aˆ is nonsingular and has no eigenvalues on the unit circle. This follows because the eigenvalues of $\hat { A }$ are the union of the eigenvalues of $\hat { A } _ { 1 1 }$ and $\hat { A } _ { 1 1 } ^ { - \top }$ . Hence stability and nonsingularity of $\hat { A } _ { 1 1 }$ (Lemma 1) implies that $\hat { A }$ is nonsingular and has no eigenvalues on the unit circle.

(iii) $\hat { A } - \hat { B } \hat { R } ^ { - 1 } \hat { S } ^ { \top }$ is nonsingular because $\hat { S } = 0$ and $\hat { A }$ is nonsingular.

(iv) $\left[ \begin{array} { c c } { \hat { A } - e ^ { j \theta } I } & { \hat { B } } \\ { \hat { C } } & { \hat { D } } \end{array} \right]$ has full column rank for all $\theta \in [ 0 , 2 \pi ]$ . Based on the structure of the matrices, this follows because $( \hat { A } _ { 1 1 } ^ { - \top } , - X B _ { d } )$ is stabilizable (by assumption) and $\hat { A } _ { 1 1 }$ is a Schur matrix.

In summary, the matrices satisfy assumptions $( \mathrm { i } ) { - } ( \mathrm { v i } )$ in Lemma 4. This ensures that $\hat { P } ^ { \sim } \hat { P }$ has a spectral factor $F .$ .

The spectral factor in Lemma 4 has the same state dimension as $\hat { A }$ which is $2 n _ { x }$ . The remainder of the proof shows that a minimal realization for the spectral factor can be constructed with dimension $n _ { x }$ . The key point is to verify that the unique solution to the DARE (37) in Statement 1 of Lemma 4 has the following special structure:

$$
\hat {X} = \gamma_ {J} ^ {2} \left[ \begin{array}{c c} X & I \\ I & X ^ {- 1} \end{array} \right] + \left[ \begin{array}{c c} 0 & 0 \\ 0 & V \end{array} \right]. \tag {44}
$$

This form assumes that X is nonsingular which will be verified below. The variable V is defined

to be the unique positive semidefinite solution to the following DARE:
