# Algorithm C1

Step 1 (Initialization): Prepare initial guesses $\hat { \mu } _ { ( 0 ) } ^ { ( \cdot ) }$ and $\hat { \Phi } _ { ( 0 ) } ^ { ( \cdot ) }$ . Initialize the iteration count

Step2 (Computation of $\hat { \Phi } _ { ( l + 1 ) } ^ { ( \cdot ) } ) \colon$ Compute (124) as $\hat { \Phi } _ { ( l + 1 ) } ^ { ( k - 1 ) } \left( x _ { i - 1 / 2 } \right) = \hat { \Phi } _ { ( l + 1 ) } ^ { ( k ) } \left( x _ { i - 1 / 2 } \right) - \Xi \left( i , \hat { \mu } _ { ( l ) } ^ { ( k ) } , \hat { \Phi } _ { ( l + 1 ) } ^ { ( k ) } \right) \Delta t$ at each  i N= 1, 2,..., from  k M= to  k = 1 .

Step 3 (Computation of $\hat { \mu } _ { ( l + 1 ) } ^ { ( \cdot ) } ) \colon$ Compute (123) as $\hat { \mu } _ { ( l + 1 ) } ^ { ( k ) } \left( \Omega _ { i } \right) = \hat { \mu } _ { ( l + 1 ) } ^ { ( k - 1 ) } \left( \Omega _ { i } \right) + \Lambda \left( i , \hat { \mu } _ { ( l + 1 ) } ^ { ( k - 1 ) } , \hat { \Phi } _ { ( l + 1 ) } ^ { ( k - 1 ) } \right) .$ each $i = 1 , 2 , . . . , N$ from  to

Step 4 (Damped update): Update numerical solutions as $\hat { \mu } _ { ( l + 1 ) } ^ { ( \cdot ) }  \omega \hat { \mu } _ { ( l + 1 ) } ^ { ( \cdot ) } + ( 1 - \omega ) \hat { \mu } _ { ( l ) } ^ { ( \cdot ) }$ and $\hat { \Phi } _ { ( l + 1 ) } ^ { ( \cdot ) }  \omega \hat { \Phi } _ { ( l + 1 ) } ^ { ( \cdot ) } + ( 1 - \omega ) \hat { \Phi } _ { ( l ) } ^ { ( \cdot ) }$ at all $i = 1 , 2 , . . . , N$ and $k = 1 , 2 , . . . , M$

Step 5 (Convergence check): If $\left| \hat { \Phi } _ { ( l + 1 ) } ^ { ( k ) } \left( x _ { i - 1 / 2 } \right) - \hat { \Phi } _ { ( l ) } ^ { ( k ) } \left( x _ { i - 1 / 2 } \right) \right| , \left| \hat { \mu } _ { ( l + 1 ) } ^ { ( k ) } \left( x _ { i - 1 / 2 } \right) - \hat { \mu } _ { ( l ) } ^ { ( k ) } \left( x _ { i - 1 / 2 } \right) \right| \leq \varepsilon$ at all $i = 1 , 2 , . . . , N$ and $k = 0 , 1 , 2 , . . . , M$ , terminate the algorithm, and the candidate $\hat { \mu } _ { ( l + 1 ) } ^ { ( \cdot ) } , \hat { \Phi } _ { ( l + 1 ) } ^ { ( \cdot ) }$ is regarded as the numerical solution. Otherwise, set  and go to Step 2.
