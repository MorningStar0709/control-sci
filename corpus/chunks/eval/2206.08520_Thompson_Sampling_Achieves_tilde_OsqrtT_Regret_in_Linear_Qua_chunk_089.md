$$
\begin{array}{l} \left| R _ {T} ^ {\mathrm{RLS}} \right| \leq \sum_ {t = 0} ^ {T} \left| \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| ^ {2} - \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\| ^ {2} \right| (123) \\ = \sum_ {t = 0} ^ {T _ {r}} \left| \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| ^ {2} - \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\| ^ {2} \right| + \sum_ {t = T _ {r}} ^ {T} \left| \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| ^ {2} - \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\| ^ {2}\right) \\ \leq \biggl (\sum_ {t = 0} ^ {T _ {r}} \Bigl (\left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| - \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\| \Bigr) ^ {2} \biggr) ^ {\frac {1}{2}} \biggl (\sum_ {t = 0} ^ {T _ {r}} \Bigl (\left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| + \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\| \Bigr) ^ {2} \biggr) ^ {\frac {1}{2}} \\ \left.
\right.
+ \left(\sum_ {t = T _ {r}} ^ {T} \left(\left\| P \left(\tilde {\Theta} _ {t}\right) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| - \left\| P \left(\tilde {\Theta} _ {t}\right) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\|\right) ^ {2}\right) ^ {\frac {1}{2}} \left(\sum_ {t = T _ {r}} ^ {T} \left(\left\| P \left(\tilde {\Theta} _ {t}\right) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| + \left\| P \left(\tilde {\Theta} _ {t}\right) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\|\right) ^ {2}\right) ^ {\frac {1}{2}} (124) \\ \leq \left(\sum_ {t = 0} ^ {T _ {r}} \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \left(\tilde {\Theta} _ {t} - \Theta_ {*}\right) ^ {\intercal} z _ {t} \right\| ^ {2}\right) ^ {\frac {1}{2}} \left(\sum_ {t = 0} ^ {T _ {r}} \left(\left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \tilde {\Theta} _ {t} ^ {\intercal} z _ {t} \right\| + \left\| P (\tilde {\Theta} _ {t}) ^ {\frac {1}{2}} \Theta_ {*} ^ {\intercal} z _ {t} \right\|\right) ^ {2}\right) ^ {\frac {1}{2}} \\ \left.
\right.
