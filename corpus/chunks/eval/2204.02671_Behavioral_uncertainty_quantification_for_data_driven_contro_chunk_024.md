Proof. The sequence $\operatorname { s u p } _ { S _ { L } } f$ is non-decreasing, so lim $\begin{array} { r l r } { \operatorname { \mathrm { 1 } } _ { L \to \infty } \operatorname* { s u p } _ { S _ { L } } f } & { { } = } & { \operatorname* { s u p } _ { L \in \mathbb { Z } _ { + } } \operatorname* { s u p } _ { S _ { L } } f . } \end{array}$ Furthermore, by assumption, ${ \cal S } _ { L } \subseteq { \cal S }$ for every $L \in \mathbb { Z } _ { + }$ , so $\begin{array} { r } { \operatorname* { s u p } _ { S } f \geq \operatorname* { s u p } _ { L \in \mathbb { Z } _ { + } } \operatorname* { s u p } _ { S _ { L } } f . } \end{array}$ To prove the reverse inequality, consider a sequence $\begin{array} { r l r } { w _ { L } } & { { } \in } & { s } \end{array}$ such that $f ( w _ { L } ) \to \operatorname { s u p } _ { S } f .$ . Then, for every $L \in \mathbb { Z } _ { + }$ there is $\epsilon _ { L } > 0$ such that $f ( w ) > f ( w _ { L } ) - 1 / L$ for every w $\in \mathcal { N } _ { \epsilon _ { L } } ( w _ { L } )$ , with $\mathcal { N } _ { \epsilon _ { L } } ( w _ { L } )$ an ǫL-neighborhood of wL. By assumption, ${ \mathcal { S } } \subseteq \operatorname { c l } \left( \bigcup _ { L = 1 } ^ { \infty } S _ { L } \right)$ , so every $\mathcal { N } _ { \epsilon _ { L } } ( w _ { L } )$ contains a point $\bar { w } _ { L } \in S _ { k _ { I } }$ L for some $S _ { k _ { L } }$ . Thus, $f ( \bar { w } _ { L } ) \to \operatorname* { s u p } _ { s } f$ . This, in turn, implies $\operatorname* { s u p } _ { S } f \leq \operatorname* { s u p } _ { L \in \mathbb { Z } _ { + } } \operatorname* { s u p } _ { S _ { L } } f .$ □

Given $L \in \mathbb { Z } _ { + } , \operatorname { l e t } \Pi _ { L } : ( \mathbb { R } ^ { q } ) \to ( \mathbb { R } ^ { q } ) ^ { \mathbb { Z } _ { + } }$ be the truncation operator, defined as [37, p.13]

$$
\Pi_ {L} (w) = \left\{ \begin{array}{l l} w _ {t}, & \text { for } t \in [ 0, L - 1 ] \cap \mathbb {Z} _ {+}, \\ 0, & \text { otherwise }, \end{array} \right.
$$

with the convention that $\Pi _ { 0 } ( w ) = w$ for all $w \in ( \mathbb { R } ^ { q } ) ^ { \mathbb { Z } _ { + } }$ .

Lemma 6. Let $\textstyle B \in { \mathcal { L } } ^ { q }$ . Then $\iota _ { L } ( B | _ { L } ) = \Pi _ { L } ( B )$

Lemma 7. Let $B \in \mathcal { L } ^ { q } \cap \ell _ { 2 } ^ { q }$ . Then $\begin{array} { r } { \boldsymbol { B } = \operatorname { c l } \left( \bigcup _ { L = 0 } ^ { \infty } \Pi _ { L } ( \boldsymbol { B } ) \right) } \end{array}$ .

Proof. (⊆). Since $B \cap ( \mathbb { R } ^ { q } ) ^ { \infty } = \bigcup _ { L = 0 } ^ { \infty } \Pi _ { L } ( B )$ , we have
