# ControlMind Starboard

ControlMind 科学评测工作台前端，基于 Next.js。它负责展示三条赛道的演示入口、依赖状态、来源矩阵和医学 RAG 问答界面。

## Local Run

```bash
npm install
npm run dev
```

默认访问地址：

```text
http://localhost:3000
```

后端代理默认指向：

```text
http://127.0.0.1:17001
```

FastAPI 后端需要使用项目约定的 `myenv` 环境启动。
